# 🚀 Инструкция по развертыванию TASK-084

## Обзор изменений

TASK-084 реализует иерархическую сортировку модулей в интерфейсе "Ручное управление кешем" для улучшения UX администраторов.

### Основные изменения:
- ✅ **CacheManagementService** - расширена логика категоризации и сортировки
- ✅ **CacheManagement.vue** - новый компонент с иерархическим UI
- ✅ **CacheModuleCard.vue** - обновлен с поддержкой категорий
- ✅ **cache-helpers.js** - утилиты для работы с кешем
- ✅ **CacheStats.vue** - обновлена статистика с распределением по группам

## 📋 План развертывания

### Этап 1: Подготовка (30 минут)

#### Резервное копирование
```bash
# Создание резервных копий текущих файлов
cp vue-app/src/services/cache-management-service.js cache-management-service.js.backup
cp vue-app/src/components/cache/CacheModuleCard.vue CacheModuleCard.vue.backup
cp vue-app/src/components/cache/CacheStats.vue CacheStats.vue.backup

# Проверка что бэкапы созданы
ls -la *.backup
```

#### Проверка зависимостей
```bash
# Убедиться что все необходимые файлы существуют
ls -la vue-app/src/services/cache-management-service.js
ls -la vue-app/src/utils/cache-helpers.js
ls -la vue-app/src/components/cache/CacheManagement.vue
ls -la vue-app/src/components/cache/CacheModuleCard.vue
ls -la vue-app/src/components/cache/CacheStats.vue
```

### Этап 2: Развертывание сервисов (20 минут)

#### Обновление CacheManagementService
```bash
# Копирование обновленного сервиса
cp /path/to/new/cache-management-service.js vue-app/src/services/cache-management-service.js

# Проверка синтаксиса
node -c vue-app/src/services/cache-management-service.js
```

#### Создание утилит
```bash
# Создание директории utils если не существует
mkdir -p vue-app/src/utils

# Копирование утилит
cp /path/to/new/cache-helpers.js vue-app/src/utils/cache-helpers.js

# Проверка синтаксиса
node -c vue-app/src/utils/cache-helpers.js
```

### Этап 3: Развертывание компонентов (30 минут)

#### Обновление CacheModuleCard
```bash
# Копирование обновленного компонента
cp /path/to/new/CacheModuleCard.vue vue-app/src/components/cache/CacheModuleCard.vue

# Проверка что компонент импортирует новые утилиты
grep -n "cache-helpers" vue-app/src/components/cache/CacheModuleCard.vue
```

#### Обновление CacheStats
```bash
# Копирование обновленного компонента
cp /path/to/new/CacheStats.vue vue-app/src/components/cache/CacheStats.vue
```

#### Создание нового CacheManagement компонента
```bash
# Копирование нового основного компонента
cp /path/to/new/CacheManagement.vue vue-app/src/components/cache/CacheManagement.vue

# Проверка импортов
grep -n "import.*from" vue-app/src/components/cache/CacheManagement.vue
```

### Этап 4: Сборка и тестирование (20 минут)

#### Сборка приложения
```bash
# Установка зависимостей если нужно
npm install

# Сборка production версии
npm run build

# Проверка что сборка прошла успешно
ls -la dist/
```

#### Тестирование функциональности
```bash
# Запуск локального сервера для тестирования
npm run serve

# Или тестирование через PHP
php test-cache-084.php
```

### Этап 5: Финализация и откат (10 минут)

#### Проверка работоспособности
- [ ] Открыть страницу управления кешем
- [ ] Проверить разделение на основные/побочные модули
- [ ] Проверить сортировку в правильном порядке
- [ ] Проверить работу всех операций с кешем

#### План отката (при проблемах)
```bash
# Восстановление из бэкапов
cp cache-management-service.js.backup vue-app/src/services/cache-management-service.js
cp CacheModuleCard.vue.backup vue-app/src/components/cache/CacheModuleCard.vue
cp CacheStats.vue.backup vue-app/src/components/cache/CacheStats.vue

# Удаление новых файлов
rm vue-app/src/utils/cache-helpers.js
rm vue-app/src/components/cache/CacheManagement.vue

# Пересборка
npm run build
```

## 🔧 Использование нового интерфейса

### Для администраторов:
1. **Перейти** в раздел "Ручное управление кешем"
2. **Основные модули** отображаются вверху с приоритетами (1-7)
3. **Побочные модули** сгруппированы по типам (users, activity, webhooks)
4. **Операции** очистки и создания кеша работают как прежде

### Для разработчиков:
```javascript
// Использование нового API категоризации
import { CacheManagementService } from '@/services/cache-management-service.js';

const result = await CacheManagementService.getCacheStatus();
// result.primaryModules - отсортированные основные модули
// result.secondaryModules - сгруппированные побочные модули
```

## 📊 Метрики успеха

После развертывания проверить:
- ✅ Основные модули отображаются первыми (7 модулей)
- ✅ Побочные модули сгруппированы (минимум 9 модулей)
- ✅ Сортировка работает корректно
- ✅ Все операции с кешем функционируют
- ✅ Интерфейс адаптивен на мобильных устройствах

## 🚨 Возможные проблемы

### Проблема: Модули не категоризируются
**Решение:** Проверить что PRIMARY_MODULE_IDS содержит правильные ID

### Проблема: Ошибки JavaScript в консоли
**Решение:** Проверить импорты и синтаксис в новых файлах

### Проблема: Стили не применяются
**Решение:** Проверить что CSS скомпилировался в dist/assets/

### Проблема: API возвращает ошибку
**Решение:** Проверить доступность `/api/admin/cache-status.php`

## 📞 Поддержка

При проблемах:
1. **Проверить логи** сервера и браузера
2. **Выполнить откат** по инструкции выше
3. **Связаться** с разработчиком TASK-084

---

**Подготовлено для:** TASK-084 Иерархическая сортировка модулей кеша
**Дата:** 2026-01-10
**Версия:** 1.0