/**
 * Константы секторов для дашборда сектора 1С
 * 
 * Определяет ID всех секторов и хранителя для разделения тикетов
 * по принадлежности к сектору
 */

/**
 * ID сектора 1С
 * 
 * Основной сектор, для которого создан дашборд
 */
export const SECTOR_1C_ID = 366;

/**
 * ID сектора Железо
 * 
 * Сотрудники этого сектора могут обрабатывать тикеты с тегом 1С,
 * но они не являются сотрудниками сектора 1С
 */
export const SECTOR_HARDWARE_ID = 368;

/**
 * ID сектора Битрикс24
 * 
 * Сотрудники этого сектора могут обрабатывать тикеты с тегом 1С,
 * но они не являются сотрудниками сектора 1С
 */
export const SECTOR_BITRIX24_ID = 369;

/**
 * ID сектора PDM
 * 
 * Сотрудники этого сектора могут обрабатывать тикеты с тегом 1С,
 * но они не являются сотрудниками сектора 1С
 */
export const SECTOR_PDM_ID = 367;

/**
 * ID хранителя нулевой очереди
 * 
 * Хранитель объектов (ID: 1051) хранит тикеты в нулевой точке
 * и не отображается в колонках сотрудников
 */
export const KEEPER_USER_ID = 1051;

/**
 * Массив всех ID секторов (кроме хранителя)
 * 
 * Используется для проверки принадлежности сотрудника к какому-либо сектору
 */
export const ALL_SECTOR_IDS = [
  SECTOR_1C_ID,
  SECTOR_HARDWARE_ID,
  SECTOR_BITRIX24_ID,
  SECTOR_PDM_ID
];

/**
 * Массив ID других секторов (не 1С)
 * 
 * Используется для определения сотрудников из других секторов
 */
export const OTHER_SECTOR_IDS = [
  SECTOR_HARDWARE_ID,
  SECTOR_BITRIX24_ID,
  SECTOR_PDM_ID
];

