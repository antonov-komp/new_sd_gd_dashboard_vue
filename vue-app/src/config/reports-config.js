/**
 * Конфигурация отчётов для стартовой страницы
 * 
 * Определяет список доступных отчётов с их метаданными
 * 
 * Использование:
 * import { getReports } from '@/config/reports-config.js';
 * const reports = getReports();
 */

export const reportsConfig = {
  /**
   * Массив отчётов, доступных всем пользователям с доступом
   * 
   * @type {Array<Object>}
   * 
   * Каждый отчёт содержит:
   * - id: уникальный идентификатор
   * - title: название отчёта
   * - icon: иконка (эмодзи или код иконки)
   * - route: маршрут для навигации
   * - description: описание отчёта
   * - active: флаг активности (true - отчёт виден, false - скрыт)
   */
  reports: [
    {
      id: 1,
      title: 'Отчёт по тикетам',
      icon: '📊',
      route: '/reports/tickets',
      description: 'Статистика по тикетам и обращениям',
      active: true
      // Дополнительные параметры (опционально)
      // category: 'tickets',
      // requiredAccess: 'basic'
    },
    {
      id: 2,
      title: 'Отчёт по производительности',
      icon: '📈',
      route: '/reports/performance',
      description: 'Анализ производительности работы отдела',
      active: false
    },
    {
      id: 3,
      title: 'Отчёт по срокам',
      icon: '📉',
      route: '/reports/deadlines',
      description: 'Анализ соблюдения сроков выполнения задач',
      active: false
    },
    {
      id: 4,
      title: 'Сводный отчёт',
      icon: '📋',
      route: '/reports/summary',
      description: 'Общая сводка по всем показателям',
      active: false
    },
    {
      id: 5,
      title: 'Дашборд сектора 1С',
      icon: '📋',
      route: '/dashboard/sector-1c',
      description: 'Управление тикетами сектора 1С',
      active: true
    }
    // Добавьте другие отчёты по мере необходимости
    // Чтобы скрыть отчёт, установите active: false
  ]
};

/**
 * Получение списка активных отчётов
 * 
 * Возвращает только отчёты с active: true
 * 
 * @returns {Array<Object>} Массив активных отчётов
 */
export function getReports() {
  return reportsConfig.reports.filter(report => report.active === true);
}

/**
 * Получение списка всех отчётов (включая неактивные)
 * 
 * @returns {Array<Object>} Массив всех отчётов
 */
export function getAllReports() {
  return [...reportsConfig.reports];
}

/**
 * Получение отчёта по ID
 * 
 * @param {number} reportId - ID отчёта
 * @returns {Object|null} Объект отчёта или null
 */
export function getReportById(reportId) {
  return reportsConfig.reports.find(report => report.id === reportId) || null;
}

