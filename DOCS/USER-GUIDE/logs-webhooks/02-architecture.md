# Архитектура модуля логов вебхуков

**Дата создания:** 2025-12-09 09:50 (UTC+3, Брест)  
**Версия:** 1.0  
**Тип:** Руководство пользователя

---

## 📋 Обзор архитектуры

Модуль **WebhookLogs** построен по принципу **модульной архитектуры** с чётким разделением ответственности между компонентами.

**Основные принципы:**
- Каждый компонент имеет одну ответственность
- Компоненты взаимодействуют через интерфейсы
- Легко тестировать и расширять
- Соответствие стандартам PSR-12

---

## 🏗️ Структура модуля

### Расположение файлов:

```
/var/www/back/
├── api/                          # API endpoints (тонкие слои)
│   ├── webhook-handler.php      # Обработчик вебхуков
│   ├── webhook-logs.php         # API для чтения логов
│   └── webhook-realtime.php     # SSE для реального времени
│
├── src/WebhookLogs/              # Модуль логирования
│   ├── Config/                   # Конфигурация
│   │   └── WebhookLogsConfig.php
│   ├── Service/                  # Бизнес-логика
│   │   ├── WebhookHandlerService.php
│   │   ├── WebhookValidationService.php
│   │   ├── WebhookLoggingService.php
│   │   ├── WebhookLogsApiService.php
│   │   └── WebhookRealtimeService.php
│   ├── Repository/               # Работа с данными
│   │   └── WebhookLogsRepository.php
│   ├── Entity/                   # Сущности
│   │   ├── WebhookEvent.php
│   │   └── WebhookLogEntry.php
│   ├── Exception/                # Обработка ошибок
│   │   ├── WebhookException.php
│   │   ├── WebhookValidationException.php
│   │   └── WebhookLoggingException.php
│   ├── Utils/                    # Утилиты
│   │   ├── EventDetailsExtractor.php
│   │   └── LogFileManager.php
│   ├── Autoloader.php           # Автозагрузка классов
│   └── bootstrap.php            # Инициализация модуля
│
└── logs/webhooks/                # Хранилище логов
    ├── tasks/                    # Логи событий задач
    ├── smart-processes/          # Логи смарт-процессов
    └── errors/                   # Логи ошибок
```

---

## 🧩 Компоненты модуля

### 1. Config (Конфигурация)

**Назначение:** Централизованное хранение всех настроек модуля.

**Класс:** `WebhookLogsConfig`

**Что хранит:**
- Пути к директориям логов
- Форматы файлов
- Лимиты пагинации
- Категории событий
- Настройки кеширования

**Пример использования:**
```php
use WebhookLogs\Config\WebhookLogsConfig;

$path = WebhookLogsConfig::getBaseLogsPath();
$categories = WebhookLogsConfig::getCategories();
```

---

### 2. Service (Сервисы бизнес-логики)

**Назначение:** Инкапсуляция бизнес-логики обработки вебхуков.

#### 2.1. WebhookHandlerService

**Что делает:**
- Обрабатывает входящие вебхуки от Bitrix24
- Координирует работу других сервисов
- Формирует ответы для Bitrix24

**Используется в:** `api/webhook-handler.php`

#### 2.2. WebhookValidationService

**Что делает:**
- Валидирует подпись вебхука (HMAC-SHA256)
- Проверяет структуру данных
- Обеспечивает безопасность

#### 2.3. WebhookLoggingService

**Что делает:**
- Логирует события вебхуков
- Определяет категорию события
- Извлекает детали события
- Сохраняет в файлы через Repository

#### 2.4. WebhookLogsApiService

**Что делает:**
- Предоставляет API для чтения логов
- Фильтрует логи по параметрам
- Сортирует и пагинирует результаты
- Кеширует запросы

**Используется в:** `api/webhook-logs.php`

#### 2.5. WebhookRealtimeService

**Что делает:**
- Отслеживает новые логи в реальном времени
- Отправляет события через SSE (Server-Sent Events)
- Оптимизирует проверку новых событий

**Используется в:** `api/webhook-realtime.php`

---

### 3. Repository (Репозиторий)

**Назначение:** Абстракция работы с файлами логов.

**Класс:** `WebhookLogsRepository`

**Что делает:**
- Читает и записывает JSON-файлы
- Управляет структурой директорий
- Кеширует метаданные файлов
- Оптимизирует работу с большими файлами

**Методы:**
- `save()` — сохранение записи
- `read()` — чтение записей
- `readByDate()` — чтение за дату
- `readByHour()` — чтение за час

---

### 4. Entity (Сущности)

**Назначение:** Представление данных в виде объектов.

#### 4.1. WebhookEvent

**Что представляет:** Событие вебхука от Bitrix24.

**Содержит:**
- Тип события (например, `ONTASKADD`)
- Данные события
- Временная метка
- IP адрес клиента
- Полный payload

#### 4.2. WebhookLogEntry

**Что представляет:** Запись в логе (сохранённая в файле).

**Содержит:**
- Временная метка
- Тип события
- Категория
- IP адрес
- Payload
- Детали события (извлечённые данные)

---

### 5. Exception (Исключения)

**Назначение:** Типизированная обработка ошибок.

**Классы:**
- `WebhookException` — базовый класс
- `WebhookValidationException` — ошибки валидации
- `WebhookLoggingException` — ошибки логирования

**Преимущества:**
- Понятные типы ошибок
- Детальная информация об ошибке
- Единообразная обработка

---

### 6. Utils (Утилиты)

**Назначение:** Вспомогательные функции.

#### 6.1. EventDetailsExtractor

**Что делает:**
- Извлекает детали из событий
- Поддерживает разные типы событий
- Расширяемая архитектура

**Пример:**
- Для `ONTASKADD` извлекает: `task_id`, `task_title`, `created_by`
- Для `ONCRMDYNAMICITEMADD` извлекает: `entity_id`, `title`, `entity_type_id`

#### 6.2. LogFileManager

**Что делает:**
- Управляет файлами логов
- Создаёт директории
- Проверяет права доступа

---

## 🔄 Поток данных

### Поток 1: Обработка вебхука

```
Bitrix24
  ↓ POST запрос
api/webhook-handler.php (тонкий слой)
  ↓
WebhookHandlerService
  ├──→ WebhookValidationService (валидация)
  ├──→ WebhookLoggingService (логирование)
  │     ├──→ EventDetailsExtractor (извлечение деталей)
  │     └──→ WebhookLogsRepository (сохранение)
  │           └──→ logs/webhooks/{category}/YYYY-MM-DD-HH.json
  └──→ Ответ Bitrix24
```

### Поток 2: Чтение логов

```
Веб-интерфейс (Vue.js)
  ↓ GET запрос
api/webhook-logs.php (тонкий слой)
  ↓
WebhookLogsApiService
  ├──→ WebhookLogsRepository (чтение файлов)
  ├──→ Фильтрация по параметрам
  ├──→ Сортировка
  ├──→ Пагинация
  └──→ JSON ответ
```

### Поток 3: Реальное время

```
Веб-интерфейс (Vue.js)
  ↓ SSE соединение
api/webhook-realtime.php (тонкий слой)
  ↓
WebhookRealtimeService
  ├──→ WebhookLogsRepository (проверка новых логов)
  └──→ SSE события (new_logs, error, timeout)
```

---

## 🔗 Взаимодействие компонентов

### Зависимости:

```
WebhookHandlerService
  ├──→ WebhookValidationService
  └──→ WebhookLoggingService
        ├──→ EventDetailsExtractor
        └──→ WebhookLogsRepository
              └──→ WebhookLogsConfig

WebhookLogsApiService
  ├──→ WebhookLogsRepository
  └──→ WebhookLogsConfig

WebhookRealtimeService
  ├──→ WebhookLogsRepository
  └──→ WebhookLogsConfig
```

**Принцип:**
- Сервисы зависят от Repository и Config
- Repository зависит от Config
- Entity используются всеми сервисами
- Exception используются для обработки ошибок

---

## 📊 Формат хранения данных

### Структура файлов:

**Путь:** `logs/webhooks/{category}/YYYY-MM-DD-HH.json`

**Пример:** `logs/webhooks/tasks/2025-12-09-10.json`

**Формат файла:**
```json
[
  {
    "timestamp": "2025-12-09T10:15:30+03:00",
    "ip": "195.208.184.34",
    "event": "ONTASKADD",
    "category": "tasks",
    "payload": {
      "event": "ONTASKADD",
      "data": {
        "TASK": {
          "ID": "12345",
          "TITLE": "Новая задача"
        }
      }
    },
    "details": {
      "task_id": 12345,
      "task_title": "Новая задача",
      "created_by": 42
    }
  }
]
```

---

## 🎯 Преимущества архитектуры

### Модульность:
- Каждый компонент можно тестировать отдельно
- Легко заменить реализацию (например, Repository на БД)

### Расширяемость:
- Легко добавить новые типы событий
- Легко добавить новые категории
- Легко добавить новые фильтры

### Поддерживаемость:
- Понятная структура
- Чёткое разделение ответственности
- Документированный код

### Производительность:
- Кеширование метаданных
- Оптимизация чтения файлов
- Батчинг операций

---

## 📝 Следующие шаги

После изучения архитектуры переходите к:
- **03-initialization.md** — как инициализировать модуль
- **04-webhook-handler.md** — как работает обработчик вебхуков

---

**Связанные документы:**
- `01-introduction.md` — введение в модуль
- `src/WebhookLogs/README.md` — техническая документация

---

**Последнее обновление:** 2025-12-09 09:50 (UTC+3, Брест)


