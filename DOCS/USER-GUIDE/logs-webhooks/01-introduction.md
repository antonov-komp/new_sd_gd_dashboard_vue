# Модуль логов вебхуков Bitrix24: Введение

**Дата создания:** 2025-12-09 09:50 (UTC+3, Брест)  
**Версия:** 1.0  
**Тип:** Руководство пользователя

---

## 📋 Что это такое?

Модуль **WebhookLogs** — это внутренний модуль REST-приложения для обработки, логирования и анализа исходящих вебхуков от Bitrix24.

**Основное назначение:**
- Приём событий от Bitrix24 через исходящие вебхуки
- Автоматическое логирование всех событий в структурированные JSON-файлы
- Предоставление API для чтения и анализа логов
- Визуализация логов через веб-интерфейс (Vue.js)

---

## 🎯 Зачем это нужно?

### Проблема без модуля:
- Нет истории событий от Bitrix24
- Сложно отладить проблемы интеграции
- Невозможно проанализировать паттерны событий
- Нет возможности аудита изменений

### Решение с модулем:
- ✅ Полная история всех событий от Bitrix24
- ✅ Удобный анализ и отладка интеграций
- ✅ Мониторинг работы вебхуков в реальном времени
- ✅ Аудит изменений в задачах и смарт-процессах
- ✅ Визуализация данных через веб-интерфейс

---

## 🔄 Как это работает (общий принцип)

```
Bitrix24 → [Исходящий вебхук] → api/webhook-handler.php → [Валидация] → [Логирование] → logs/webhooks/
                                                                                              ├── tasks/
                                                                                              ├── smart-processes/
                                                                                              └── errors/
                                                                                                    ↓
                                                                                          [Веб-интерфейс Vue.js]
                                                                                                    ↓
                                                                                          [Анализ и визуализация]
```

### Процесс работы:

1. **Bitrix24 отправляет событие** (например, создание задачи)
2. **Вебхук приходит** на `api/webhook-handler.php`
3. **Модуль валидирует** подпись вебхука (безопасность)
4. **Модуль определяет категорию** события (задачи, смарт-процессы)
5. **Модуль логирует** событие в JSON-файл
6. **Пользователь просматривает** логи через веб-интерфейс

---

## 📦 Что входит в модуль?

### Компоненты модуля:

1. **Обработчик вебхуков** (`api/webhook-handler.php`)
   - Принимает POST-запросы от Bitrix24
   - Валидирует подпись запроса
   - Обрабатывает события

2. **API для чтения логов** (`api/webhook-logs.php`)
   - Предоставляет REST API для получения логов
   - Поддерживает фильтрацию и пагинацию

3. **SSE для реального времени** (`api/webhook-realtime.php`)
   - Отправляет новые логи в реальном времени
   - Используется веб-интерфейсом для автообновления

4. **Веб-интерфейс** (Vue.js)
   - Просмотр логов в удобном формате
   - Фильтрация и поиск
   - Статистика и визуализация

5. **Структура хранения** (`logs/webhooks/`)
   - JSON-файлы с логами событий
   - Разбивка по категориям и часам

---

## 🏗️ Архитектура модуля

Модуль построен по **модульной архитектуре** с разделением ответственности:

```
src/WebhookLogs/
├── Config/              # Конфигурация (пути, настройки)
├── Service/             # Бизнес-логика (обработка, логирование, API)
├── Repository/          # Работа с файлами (чтение/запись)
├── Entity/              # Сущности данных (события, записи логов)
├── Exception/           # Обработка ошибок (исключения)
└── Utils/               # Вспомогательные утилиты
```

**Принцип работы:**
- Каждый компонент имеет чёткую ответственность
- Компоненты взаимодействуют через интерфейсы
- Легко тестировать и расширять

---

## 📊 Какие события обрабатываются?

### Категория "Задачи" (tasks):
- `ONTASKADD` — создание задачи
- `ONTASKUPDATE` — обновление задачи
- `ONTASKDELETE` — удаление задачи
- `ONTASKCOMMENTADD` — добавление комментария
- `ONTASKCOMMENTUPDATE` — обновление комментария
- `ONTASKCOMMENTDELETE` — удаление комментария

### Категория "Смарт-процессы" (smart-processes):
- `ONCRMDYNAMICITEMADD` — создание элемента
- `ONCRMDYNAMICITEMUPDATE` — обновление элемента
- `ONCRMDYNAMICITEMDELETE` — удаление элемента

### Категория "Ошибки" (errors):
- Ошибки валидации вебхуков
- Ошибки обработки событий
- Ошибки записи логов

---

## 🔐 Безопасность

Модуль обеспечивает безопасность через:

1. **Валидация подписи вебхука**
   - HMAC-SHA256 подпись от Bitrix24
   - Проверка через `hash_equals()` (защита от timing attacks)
   - Невалидные запросы отклоняются

2. **Проверка доступа к веб-интерфейсу**
   - Доступ только для пользователей из разрешённых отделов
   - Проверка через Bitrix24 API

3. **Логирование ошибок**
   - Все ошибки логируются для анализа
   - Не раскрываются детали системы

---

## 📚 Документация

Это руководство состоит из нескольких документов:

1. **01-introduction.md** (этот документ) — введение в модуль
2. **02-architecture.md** — архитектура модуля
3. **03-initialization.md** — инициализация и настройка
4. **04-webhook-handler.md** — обработка вебхуков
5. **05-logging.md** — логирование событий
6. **06-api-reading.md** — API для чтения логов
7. **07-realtime.md** — обновления в реальном времени
8. **08-web-interface.md** — веб-интерфейс
9. **09-troubleshooting.md** — решение проблем

---

## 🚀 Быстрый старт

### Для администратора:

1. Настроить вебхук в Bitrix24
2. Сохранить секрет в `webhook-secret.php`
3. Проверить работу: `php check-webhook-logs.php`

### Для пользователя:

1. Открыть веб-интерфейс: `/admin/webhook-logs`
2. Просмотреть логи событий
3. Использовать фильтры для поиска

---

## 📝 Следующие шаги

После прочтения этого введения переходите к:
- **02-architecture.md** — чтобы понять структуру модуля
- **03-initialization.md** — чтобы настроить модуль

---

**Связанные документы:**
- `DOCS/DEVELOPER-GUIDE/webhook-logs.md` — руководство для разработчиков
- `DOCS/TASKS/ARCHIVE/TASK-018-refactor-webhook-logs-module.md` — документация рефакторинга
- `src/WebhookLogs/README.md` — техническая документация модуля

---

**Последнее обновление:** 2025-12-09 09:50 (UTC+3, Брест)


