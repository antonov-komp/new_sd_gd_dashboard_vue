# Инициализация и настройка модуля логов вебхуков

**Дата создания:** 2025-12-09 09:50 (UTC+3, Брест)  
**Версия:** 1.0  
**Тип:** Руководство пользователя

---

## 📋 Обзор

Модуль **WebhookLogs** автоматически инициализируется при подключении. Однако для правильной работы требуется настройка вебхука в Bitrix24 и сохранение секрета.

---

## 🚀 Автоматическая инициализация

### Подключение модуля

Модуль подключается автоматически в API endpoints:

```php
// api/webhook-handler.php
require_once(__DIR__ . '/../src/WebhookLogs/bootstrap.php');
```

**Что происходит при инициализации:**
1. Регистрируется автозагрузка классов
2. Проверяется конфигурация
3. Создаются необходимые директории (если их нет)

---

## ⚙️ Настройка конфигурации

### Конфигурация через WebhookLogsConfig

Все настройки модуля хранятся в классе `WebhookLogsConfig`.

**Основные настройки:**

```php
use WebhookLogs\Config\WebhookLogsConfig;

// Базовый путь к логам
$basePath = WebhookLogsConfig::getBaseLogsPath();
// Результат: /var/www/back/logs/webhooks/

// Категории событий
$categories = WebhookLogsConfig::getCategories();
// Результат: ['tasks', 'smart-processes', 'errors']

// Лимит пагинации по умолчанию
$limit = WebhookLogsConfig::getDefaultPaginationLimit();
// Результат: 50

// Формат имени файла
$format = WebhookLogsConfig::getLogFileDateFormat();
// Результат: 'Y-m-d-H'
```

### Инициализация директорий

Модуль автоматически создаёт необходимые директории:

```php
use WebhookLogs\Config\WebhookLogsConfig;

// Создание всех необходимых директорий
WebhookLogsConfig::initialize();
```

**Что создаётся:**
- `logs/webhooks/tasks/`
- `logs/webhooks/smart-processes/`
- `logs/webhooks/errors/`

---

## 🔐 Настройка вебхука в Bitrix24

### Шаг 1: Создание исходящего вебхука

1. Войдите в Bitrix24 как администратор
2. Перейдите: **Настройки** → **Разработчикам** → **Другое** → **Исходящие вебхуки**
3. Нажмите **Добавить вебхук**

### Шаг 2: Настройка вебхука

**URL вебхука:**
```
https://your-domain.com/api/webhook-handler.php
```

**События для выбора:**
- ✅ **Задачи** (все события ONTASK*)
- ✅ **Смарт-процессы** (все события ONCRMDYNAMIC*)

**Важно:** Выберите только те события, которые нужно логировать.

### Шаг 3: Сохранение секрета

После создания вебхука Bitrix24 предоставит **токен (секрет)**.

**Сохраните секрет в файл:**

**Файл:** `webhook-secret.php`

**Содержимое:**
```php
<?php
return 'n7t5pttg22m1udi0cr6di7shs1f2aoke';
```

**Важно:**
- Файл не должен попадать в git (добавьте в `.gitignore`)
- Установите права доступа: `chmod 600 webhook-secret.php`
- Храните секрет в безопасности

### Шаг 4: Альтернативные способы хранения секрета

**Вариант 1: Переменная окружения**

```bash
export BITRIX24_WEBHOOK_SECRET="your_secret_here"
```

**Вариант 2: Файл settings.json** (не рекомендуется для продакшена)

```json
{
  "bitrix24": {
    "webhook_secret": "your_secret_here"
  }
}
```

---

## ✅ Проверка настройки

### Проверка через утилиту

Запустите скрипт проверки:

```bash
php check-webhook-logs.php
```

**Что проверяется:**
- Существование директорий для логов
- Наличие файлов логов за текущий час
- Количество записей в текущем файле
- Настройка секрета вебхука

**Пример вывода:**
```
=== Проверка логов вебхуков ===

Категория: tasks
Папка: /var/www/back/logs/webhooks/tasks/
  Текущий файл: 2025-12-09-10.json
  ✅ Файл существует, записей: 5
  Последние 3 записи:
    [3] ONTASKADD - 2025-12-09T10:15:30+03:00
    [4] ONTASKUPDATE - 2025-12-09T10:20:45+03:00
    [5] ONTASKCOMMENTADD - 2025-12-09T10:25:10+03:00

=== Проверка конфигурации ===
✅ Токен настроен: n7t5pttg22...
```

### Проверка через тестовое событие

1. Создайте задачу в Bitrix24
2. Проверьте поступление вебхука `ONTASKADD`
3. Проверьте создание файла в `logs/webhooks/tasks/`

---

## 🔧 Настройка прав доступа

### Права на директории логов

```bash
# Установка владельца
chown -R www-data:www-data /var/www/back/logs/webhooks/

# Установка прав доступа
chmod -R 755 /var/www/back/logs/webhooks/
```

**Важно:**
- Веб-сервер должен иметь права на запись
- Пользователь должен иметь права на чтение (для веб-интерфейса)

### Права на файл секрета

```bash
# Установка владельца
chown www-data:www-data /var/www/back/webhook-secret.php

# Установка прав доступа (только чтение для владельца)
chmod 600 /var/www/back/webhook-secret.php
```

---

## 🌐 Настройка веб-сервера

### Nginx

**Пример конфигурации:**

```nginx
location /api/webhook-handler.php {
    fastcgi_pass unix:/var/run/php/php8.4-fpm.sock;
    fastcgi_index index.php;
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    
    # Увеличение лимитов для больших payload
    client_max_body_size 10M;
    fastcgi_read_timeout 300;
}

location /api/webhook-realtime.php {
    fastcgi_pass unix:/var/run/php/php8.4-fpm.sock;
    fastcgi_index index.php;
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    
    # Настройки для SSE
    proxy_buffering off;
    proxy_cache off;
    fastcgi_read_timeout 300;
}
```

### Apache

**Пример конфигурации:**

```apache
<Location /api/webhook-handler.php>
    Require all granted
    LimitRequestBody 10485760
</Location>

<Location /api/webhook-realtime.php>
    Require all granted
    # Отключение буферизации для SSE
    SetEnv no-buffer 1
</Location>
```

---

## 🔍 Проверка доступности endpoint

### Проверка через curl

```bash
# Проверка обработчика вебхуков
curl -X POST https://your-domain.com/api/webhook-handler.php \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "event=ONTASKADD&data[TASK][ID]=123"

# Проверка API для чтения логов
curl "https://your-domain.com/api/webhook-logs.php?category=tasks&date=2025-12-09"
```

### Проверка через браузер

Откройте в браузере:
- `https://your-domain.com/api/webhook-logs.php` — должен вернуть JSON
- `https://your-domain.com/admin/webhook-logs` — веб-интерфейс

---

## ⚠️ Типичные проблемы при настройке

### Проблема 1: Вебхук не приходит

**Причины:**
- Неправильный URL в Bitrix24
- Endpoint недоступен извне
- Firewall блокирует запросы
- SSL сертификат недействителен

**Решение:**
- Проверьте URL в настройках Bitrix24
- Проверьте доступность через `curl`
- Проверьте настройки firewall
- Проверьте SSL сертификат

### Проблема 2: Ошибка валидации подписи

**Причины:**
- Неправильный секрет в `webhook-secret.php`
- Секрет не совпадает с Bitrix24

**Решение:**
- Проверьте секрет в `webhook-secret.php`
- Убедитесь, что секрет совпадает с Bitrix24
- Пересоздайте вебхук, если необходимо

### Проблема 3: Нет прав на запись в директорию

**Причины:**
- Неправильные права доступа
- Неправильный владелец директории

**Решение:**
- Установите права: `chmod -R 755 logs/webhooks/`
- Установите владельца: `chown -R www-data:www-data logs/webhooks/`

---

## 📝 Следующие шаги

После настройки модуля переходите к:
- **04-webhook-handler.md** — как работает обработчик вебхуков
- **05-logging.md** — как работает логирование событий

---

**Связанные документы:**
- `02-architecture.md` — архитектура модуля
- `DOCS/GUIDES/webhook-setup.md` — подробная настройка вебхука
- `DOCS/GUIDES/webhook-troubleshooting.md` — решение проблем

---

**Последнее обновление:** 2025-12-09 09:50 (UTC+3, Брест)



