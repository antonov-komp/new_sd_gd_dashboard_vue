# Решение проблем: Типичные проблемы и их решение

**Дата создания:** 2025-12-09 09:51 (UTC+3, Брест)  
**Версия:** 1.0  
**Тип:** Руководство пользователя

---

## 📋 Обзор

Этот документ описывает типичные проблемы при работе с модулем логов вебхуков и способы их решения.

---

## 🔧 Проблемы с вебхуками

### Проблема 1: Вебхук не приходит

**Симптомы:**
- События в Bitrix24 не логируются
- Нет файлов в `logs/webhooks/`
- Нет записей в веб-интерфейсе

**Причины и решения:**

#### 1.1. Неправильный URL в Bitrix24

**Проверка:**
- Откройте настройки вебхука в Bitrix24
- Проверьте URL: `https://your-domain.com/api/webhook-handler.php`

**Решение:**
- Убедитесь, что URL правильный
- Проверьте, что используется HTTPS (если требуется)
- Проверьте, что домен доступен извне

#### 1.2. Endpoint недоступен

**Проверка:**
```bash
curl -X POST https://your-domain.com/api/webhook-handler.php \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "event=ONTASKADD&data[TASK][ID]=123"
```

**Решение:**
- Проверьте настройки веб-сервера
- Проверьте, что PHP обрабатывает файлы
- Проверьте права доступа к файлу

#### 1.3. Firewall блокирует запросы

**Проверка:**
- Проверьте логи firewall
- Проверьте настройки iptables/ufw

**Решение:**
- Разрешите входящие соединения на порт 443 (HTTPS)
- Проверьте настройки CloudFlare (если используется)

#### 1.4. SSL сертификат недействителен

**Проверка:**
```bash
curl -v https://your-domain.com/api/webhook-handler.php
```

**Решение:**
- Обновите SSL сертификат
- Проверьте срок действия сертификата

---

### Проблема 2: Ошибка валидации подписи

**Симптомы:**
- В логах ошибок: `Webhook validation failed`
- HTTP статус 401
- События не логируются

**Причины и решения:**

#### 2.1. Неправильный секрет

**Проверка:**
```bash
cat webhook-secret.php
```

**Решение:**
- Убедитесь, что секрет совпадает с Bitrix24
- Пересоздайте вебхук в Bitrix24
- Обновите секрет в `webhook-secret.php`

#### 2.2. Секрет не читается

**Проверка:**
```bash
php -r "echo include 'webhook-secret.php';"
```

**Решение:**
- Проверьте права доступа к файлу
- Убедитесь, что файл существует
- Проверьте формат файла (должен возвращать строку)

---

### Проблема 3: События логируются в errors

**Симптомы:**
- События появляются в `logs/webhooks/errors/`
- Не определяются категории

**Причины и решения:**

#### 3.1. Неизвестный тип события

**Проверка:**
- Откройте файл ошибки
- Проверьте тип события

**Решение:**
- Добавьте поддержку нового типа события
- Или игнорируйте, если событие не нужно логировать

---

## 📁 Проблемы с файлами

### Проблема 4: Нет прав на запись

**Симптомы:**
- Ошибка: `Permission denied`
- Файлы не создаются
- События не логируются

**Решение:**
```bash
# Установка владельца
chown -R www-data:www-data /var/www/back/logs/webhooks/

# Установка прав
chmod -R 755 /var/www/back/logs/webhooks/
```

**Проверка:**
```bash
ls -la logs/webhooks/
```

---

### Проблема 5: Диск переполнен

**Симптомы:**
- Ошибка: `No space left on device`
- Файлы не создаются

**Проверка:**
```bash
df -h
```

**Решение:**
- Освободите место на диске
- Удалите старые логи
- Настройте ротацию логов

---

### Проблема 6: Файлы повреждены

**Симптомы:**
- Ошибка при чтении JSON
- Некорректный формат файла

**Проверка:**
```bash
php -r "json_decode(file_get_contents('logs/webhooks/tasks/2025-12-09-10.json'));"
```

**Решение:**
- Восстановите файл из бэкапа
- Или удалите повреждённый файл (данные будут потеряны)

---

## 🌐 Проблемы с API

### Проблема 7: API не отвечает

**Симптомы:**
- Веб-интерфейс не загружается
- Ошибка: `Failed to fetch`

**Проверка:**
```bash
curl https://your-domain.com/api/webhook-logs.php
```

**Решение:**
- Проверьте настройки веб-сервера
- Проверьте права доступа к файлу
- Проверьте логи ошибок PHP

---

### Проблема 8: Медленная работа API

**Симптомы:**
- Долгая загрузка логов
- Таймауты

**Причины и решения:**

#### 8.1. Большие файлы логов

**Решение:**
- Используйте фильтры (дата, час)
- Уменьшите лимит пагинации
- Настройте ротацию логов

#### 8.2. Нет кеширования

**Проверка:**
- Проверьте, включено ли кеширование
- Проверьте настройки кеша

**Решение:**
- Включите кеширование
- Увеличьте TTL кеша

---

## 🔄 Проблемы с реальным временем

### Проблема 9: SSE не работает

**Симптомы:**
- Автообновление не работает
- Соединение разрывается

**Причины и решения:**

#### 9.1. Буферизация в Nginx

**Решение:**
```nginx
location /api/webhook-realtime.php {
    ...
    proxy_buffering off;
    add_header X-Accel-Buffering no;
}
```

#### 9.2. Таймаут соединения

**Решение:**
- Увеличьте таймаут в настройках PHP
- Увеличьте таймаут в настройках веб-сервера

---

### Проблема 10: Новые события не приходят

**Симптомы:**
- Соединение установлено
- Но новые события не появляются

**Проверка:**
- Проверьте, что события логируются
- Проверьте фильтры в запросе SSE

**Решение:**
- Проверьте `last_timestamp` в запросе
- Проверьте фильтры (category, event)
- Перезапустите соединение

---

## 🖥️ Проблемы с веб-интерфейсом

### Проблема 11: Интерфейс не открывается

**Симптомы:**
- Страница не загружается
- Ошибка 404

**Проверка:**
- Проверьте URL
- Проверьте настройки роутинга

**Решение:**
- Проверьте конфигурацию Vue Router
- Проверьте настройки веб-сервера

---

### Проблема 12: Нет доступа к интерфейсу

**Симптомы:**
- Сообщение "Нет доступа"
- Показывается информация об отделах

**Решение:**
- Проверьте настройки доступа
- Убедитесь, что ваш отдел в списке разрешённых
- Обратитесь к администратору

---

### Проблема 13: Логи не загружаются

**Симптомы:**
- Список пустой
- Ошибка загрузки

**Проверка:**
- Откройте консоль браузера (F12)
- Проверьте ошибки в Network

**Решение:**
- Проверьте работу API
- Проверьте фильтры
- Проверьте права доступа

---

## 🔍 Диагностика

### Проверка работы модуля

**Скрипт проверки:**
```bash
php check-webhook-logs.php
```

**Что проверяется:**
- Существование директорий
- Наличие файлов логов
- Количество записей
- Настройка секрета

---

### Просмотр логов ошибок

**Логи PHP:**
```bash
tail -f /var/log/php/error.log
```

**Логи веб-сервера:**
```bash
# Nginx
tail -f /var/log/nginx/error.log

# Apache
tail -f /var/log/apache2/error.log
```

**Логи модуля:**
```bash
ls -la logs/webhooks/errors/
```

---

### Проверка конфигурации

**Проверка путей:**
```php
php -r "
require 'src/WebhookLogs/bootstrap.php';
use WebhookLogs\Config\WebhookLogsConfig;
echo WebhookLogsConfig::getBaseLogsPath();
"
```

**Проверка категорий:**
```php
php -r "
require 'src/WebhookLogs/bootstrap.php';
use WebhookLogs\Config\WebhookLogsConfig;
print_r(WebhookLogsConfig::getCategories());
"
```

---

## 📞 Получение помощи

### Логи для отладки

**Соберите следующую информацию:**
1. Вывод `php check-webhook-logs.php`
2. Последние строки логов ошибок
3. Конфигурация веб-сервера (без секретов)
4. Версия PHP
5. Версия модуля

### Обращение за помощью

**Куда обращаться:**
- К разработчикам модуля
- В систему поддержки
- В документацию

**Что указать:**
- Описание проблемы
- Шаги для воспроизведения
- Собранные логи
- Конфигурация системы

---

## 📝 Чек-лист диагностики

### Базовые проверки

- [ ] Вебхук настроен в Bitrix24
- [ ] URL вебхука правильный
- [ ] Секрет сохранён в `webhook-secret.php`
- [ ] Директории логов существуют
- [ ] Права доступа установлены
- [ ] PHP обрабатывает файлы
- [ ] Веб-сервер настроен правильно
- [ ] SSL сертификат действителен

### Проверка работы

- [ ] Тестовое событие создано в Bitrix24
- [ ] Вебхук пришёл на endpoint
- [ ] Событие залогировано
- [ ] Файл создан в `logs/webhooks/`
- [ ] API возвращает логи
- [ ] Веб-интерфейс открывается
- [ ] Логи отображаются в интерфейсе

---

## 🔗 Полезные ссылки

- `01-introduction.md` — введение в модуль
- `03-initialization.md` — настройка модуля
- `04-webhook-handler.md` — обработка вебхуков
- `DOCS/DEVELOPER-GUIDE/webhook-logs.md` — руководство для разработчиков

---

**Последнее обновление:** 2025-12-09 09:51 (UTC+3, Брест)



