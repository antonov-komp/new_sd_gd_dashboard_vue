# Руководство пользователя: Интерфейс логов вебхуков

**Дата создания:** 2025-12-07  
**Версия:** 1.0

---

## 📋 Содержание

1. [Введение](#введение)
2. [Доступ к интерфейсу](#доступ-к-интерфейсу)
3. [Основные функции](#основные-функции)
4. [Работа с фильтрами](#работа-с-фильтрами)
5. [Поиск логов](#поиск-логов)
6. [Экспорт данных](#экспорт-данных)
7. [Реальное время](#реальное-время)
8. [Статистика и графики](#статистика-и-графики)
9. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## 🎯 Введение

Интерфейс логов вебхуков позволяет просматривать, фильтровать и анализировать события, поступающие от Bitrix24 через вебхуки. Интерфейс предоставляет удобные инструменты для мониторинга и анализа данных.

---

## 🔐 Доступ к интерфейсу

### Требования

- Доступ к Bitrix24 порталу
- Принадлежность к разрешённым отделам (настраивается администратором)
- Современный браузер (Chrome, Firefox, Safari, Edge)

### Вход

1. Откройте главную страницу приложения
2. Перейдите в раздел "Логи вебхуков" или откройте `/admin/webhook-logs`
3. Если у вас нет доступа, вы увидите соответствующее сообщение

---

## 📊 Основные функции

### Просмотр логов

Интерфейс отображает список логов в виде таблицы со следующими колонками:

- **Время** — дата и время события
- **Событие** — тип события (ONTASKADD, ONTASKUPDATE и т.д.)
- **Категория** — категория события (Задачи, Смарт-процессы, Ошибки)
- **IP** — IP-адрес источника
- **Детали** — краткая информация о событии
- **Действия** — кнопка "Просмотр" для детального просмотра

### Детальный просмотр

При клике на лог открывается модальное окно с полной информацией:

- Основная информация (время, событие, категория, IP)
- Полный JSON payload события
- Возможность копирования данных

**Копирование данных:**
- Кнопка "Копировать JSON" — копирует весь payload
- Кнопки "Копировать значение" — копируют отдельные поля

---

## 🔍 Работа с фильтрами

### Доступные фильтры

1. **Категория** — фильтрация по категориям:
   - Задачи
   - Смарт-процессы
   - Ошибки

2. **Тип события** — фильтрация по типу события (например, ONTASKADD)

3. **Дата** — выбор конкретной даты

4. **Диапазон дат** — выбор периода (от и до)

5. **Час** — фильтрация по часу дня (0-23)

6. **IP-адрес** — фильтрация по IP-адресу источника

7. **Статус** — фильтрация по статусу (успех/ошибка)

### Быстрые фильтры

Для удобства доступны быстрые фильтры:

- **Сегодня** — события за сегодня
- **Вчера** — события за вчера
- **Последние 7 дней** — события за последнюю неделю
- **Последние 30 дней** — события за последний месяц

### Сохранение фильтров

- Фильтры автоматически сохраняются в URL
- При перезагрузке страницы фильтры восстанавливаются
- Можно поделиться ссылкой с применёнными фильтрами

### Очистка фильтров

Нажмите кнопку "Очистить фильтры" для сброса всех фильтров.

---

## 🔎 Поиск логов

### Как использовать поиск

1. Введите текст в поле поиска
2. Поиск выполняется автоматически с задержкой (debounce)
3. Результаты отображаются в реальном времени

### Что ищется

Поиск работает по следующим полям:

- Тип события (event)
- Содержимое payload
- Детали события (details)
- IP-адрес

### Очистка поиска

Нажмите кнопку "✕" в поле поиска для очистки.

---

## 📤 Экспорт данных

### Форматы экспорта

1. **CSV** — табличный формат для Excel
2. **JSON** — формат данных для программной обработки

### Область экспорта

- **Все записи** — экспорт всех логов с учётом фильтров
- **Выбранные записи** — экспорт только выбранных логов

### Как экспортировать

1. Выберите записи (опционально) с помощью чекбоксов
2. Нажмите кнопку "Экспорт"
3. Выберите формат (CSV/JSON)
4. Выберите область экспорта
5. Нажмите "Экспортировать"

### Имя файла

Файл автоматически получает имя с метаданными:

- Дата экспорта
- Применённые фильтры
- Количество записей

**Пример:** `webhook-logs_2025-12-07_tasks_150.csv`

---

## ⚡ Реальное время

### Автообновление

Интерфейс поддерживает обновление данных в реальном времени через Server-Sent Events (SSE).

### Как включить

1. Найдите переключатель "Автообновление" в верхней части страницы
2. Включите переключатель
3. Индикатор состояния покажет статус соединения:
   - 🟢 **Подключено** — соединение активно
   - 🟡 **Подключение...** — идёт подключение
   - ⚪ **Отключено** — соединение неактивно
   - 🔴 **Ошибка** — произошла ошибка

### Новые события

При получении новых событий:

1. Появляется индикатор с количеством новых событий
2. Нажмите "Применить" для добавления событий в список
3. Нажмите "✕" для отклонения

### Ограничения

- Соединение автоматически закрывается через 5 минут (защита от утечек)
- Происходит автоматическое переподключение
- При неактивной вкладке соединение может приостанавливаться

---

## 📈 Статистика и графики

### Дашборд

В верхней части страницы отображается дашборд с ключевыми метриками:

- **Всего событий** — общее количество логов
- **Задачи** — количество событий категории "Задачи"
- **Смарт-процессы** — количество событий категории "Смарт-процессы"
- **Ошибки** — количество событий категории "Ошибки"
- **Средний размер payload** — средний размер данных события
- **Уникальные IP** — количество уникальных IP-адресов

### Графики

1. **Временная линия событий** — график распределения событий по времени
2. **Распределение по категориям** — круговая диаграмма
3. **Распределение по типам событий** — столбчатая диаграмма

### Переключение периодов

В графиках можно переключать периоды для анализа данных.

---

## ❓ Часто задаваемые вопросы

### Как найти конкретное событие?

Используйте поиск или фильтры. Например:
- Для поиска по типу события используйте фильтр "Тип события"
- Для поиска по содержимому используйте поле поиска

### Почему не загружаются логи?

Проверьте:
1. Есть ли у вас доступ к интерфейсу
2. Правильно ли настроены фильтры
3. Есть ли логи за выбранный период
4. Работает ли API endpoint

### Как экспортировать большой объём данных?

Для больших объёмов (>10,000 записей):
1. Используйте фильтры для уменьшения объёма
2. Экспортируйте данные частями (по датам)
3. Обратитесь к администратору для серверного экспорта

### Почему автообновление не работает?

Проверьте:
1. Включён ли переключатель "Автообновление"
2. Статус соединения в индикаторе
3. Консоль браузера на наличие ошибок
4. Работает ли SSE endpoint

### Как поделиться результатами поиска?

Фильтры и поиск сохраняются в URL. Просто скопируйте ссылку из адресной строки и поделитесь ею.

### Можно ли использовать на мобильных устройствах?

Да, интерфейс адаптивен и работает на мобильных устройствах. Для лучшего просмотра таблицы рекомендуется горизонтальная ориентация.

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте [документацию по известным проблемам](../../TESTING/webhook-logs-known-issues.md)
2. Обратитесь к администратору системы
3. Проверьте консоль браузера на наличие ошибок

---

**Последнее обновление:** 2025-12-07

