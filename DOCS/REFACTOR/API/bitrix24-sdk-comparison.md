# Сравнение методов работы с данными Bitrix24

**Дата создания:** 2025-12-18 11:52 (UTC+3, Брест)  
**Версия:** 1.0  
**Статус:** 🟢 Активен

---

## 🎯 Назначение документа

Данный документ сравнивает различные методы работы с данными Bitrix24, доступные для разработки приложений и интеграций. Документ описывает установку, использование, преимущества и недостатки каждого метода, а также рекомендации по выбору.

**Текущее состояние проекта:**
- **Основной метод:** CRest PHP SDK (backend)
- **Дополнительный метод:** BX24.js (frontend, частично)

---

## 📋 Методы работы с Bitrix24

### 1. CRest PHP SDK ⭐ (Текущий)

**Тип:** PHP SDK для серверной стороны  
**Версия:** 1.36  
**Статус в проекте:** ✅ Активно используется

#### Описание

CRest — это лёгкий PHP SDK для вызова методов Bitrix24 REST API через вебхуки и OAuth 2.0. Это простой и минималистичный инструмент, который предоставляет базовую функциональность для работы с Bitrix24.

**Официальный репозиторий:** https://github.com/bitrix-tools/crest

#### Установка

**Способ 1: Клонирование репозитория**
```bash
git clone https://github.com/bitrix-tools/crest.git
cd crest
```

**Способ 2: Ручная установка**
1. Скачать файлы `crest.php` и `settings.php`
2. Разместить в проекте
3. Настроить параметры в `settings.php`

**Текущая установка в проекте:**
- Файл: `crest.php` (корень проекта)
- Файл: `settings.php` (конфигурация)

#### Настройка

**Файл:** `settings.php`

```php
<?php
// Вариант 1: Webhook (рекомендуется для облачной версии)
define('C_REST_WEB_HOOK_URL', 'https://rest-api.bitrix24.com/rest/1/your-webhook-code/');

// Вариант 2: OAuth (для локальных приложений)
define('C_REST_CLIENT_ID', 'local.5c8bb1b0891cf2.87252039');
define('C_REST_CLIENT_SECRET', 'your-secret-key');

// Дополнительные настройки
define('C_REST_CURRENT_ENCODING', 'UTF-8');
define('C_REST_BLOCK_LOG', false);
define('C_REST_LOGS_DIR', __DIR__ . '/logs/');
define('C_REST_LOG_TYPE_DUMP', true);
define('C_REST_IGNORE_SSL', false);
```

#### Использование

**Базовый пример:**
```php
<?php
require_once __DIR__ . '/crest.php';

// Вызов метода Bitrix24 API
$result = CRest::call('crm.item.list', [
    'entityTypeId' => 140,
    'filter' => [
        '>=createdTime' => '2025-12-15 00:00:00',
        '<=createdTime' => '2025-12-21 23:59:59'
    ],
    'select' => ['id', 'title', 'stageId', 'createdTime'],
    'start' => 0
]);

if (isset($result['error'])) {
    error_log("Bitrix24 API error: " . $result['error_description']);
} else {
    $items = $result['result']['items'] ?? [];
    foreach ($items as $item) {
        // Обработка данных
    }
}
```

**Батч-запросы:**
```php
$batch = [
    'get_tickets' => [
        'method' => 'crm.item.list',
        'params' => [
            'entityTypeId' => 140,
            'filter' => ['stageId' => 'DT140_12:UC_0VHWE2']
        ]
    ],
    'get_users' => [
        'method' => 'user.get',
        'params' => ['ID' => '1,2,3']
    ]
];

$result = CRest::callBatch($batch);
// Результат: $result['result']['result']['get_tickets'], $result['result']['result']['get_users']
```

**Установка приложения:**
```php
$result = CRest::installApp();
if ($result['install']) {
    // Приложение установлено
}
```

#### Преимущества

✅ **Простота:**
- Минималистичный код (один файл)
- Легко понять и использовать
- Нет зависимостей

✅ **Гибкость:**
- Поддержка вебхуков и OAuth
- Автоматическое обновление токенов
- Настраиваемое логирование

✅ **Проверенность:**
- Используется в проекте
- Стабильная работа
- Хорошая документация

✅ **Производительность:**
- Низкие накладные расходы
- Прямые HTTP-запросы через cURL

#### Недостатки

❌ **Ограниченная функциональность:**
- Только базовые методы REST API
- Нет встроенной обработки ошибок
- Нет типизации данных

❌ **Устаревший подход:**
- Версия 1.36 (последнее обновление давно)
- Нет поддержки современных PHP-практик
- Нет использования PSR-стандартов

❌ **Отсутствие расширений:**
- Нет встроенного кеширования
- Нет retry-логики
- Нет rate limiting

❌ **Ручная обработка:**
- Нужно вручную обрабатывать пагинацию
- Нужно вручную обрабатывать ошибки
- Нет автоматической нормализации данных

#### Текущее использование в проекте

**Файлы:**
- `crest.php` — основной класс
- `settings.php` — конфигурация
- `api/bitrix24.php` — прокси для Vue.js
- `api/graph-1c-admission-closure.php` — использование CRest
- `api/get-sector-data.php` — использование CRest
- `api/tickets-time-tracking-sector-1c.php` — использование CRest

**Паттерн использования:**
```php
require_once __DIR__ . '/../crest.php';

$result = CRest::call('crm.item.list', [
    'entityTypeId' => 140,
    'filter' => [...],
    'select' => [...],
    'start' => 0
]);

// Обработка пагинации вручную
do {
    $items = $result['result']['items'] ?? [];
    // Обработка данных
    $start += 50;
} while (isset($result['result']['next']));
```

---

### 2. B24PhpSDK

**Тип:** PHP SDK для серверной стороны  
**Версия:** Современная (активно поддерживается)  
**Статус в проекте:** ❌ Не используется

#### Описание

B24PhpSDK — это официальный PHP SDK для работы с Bitrix24 REST API. Предоставляет более современный и функциональный интерфейс по сравнению с CRest.

**Официальный репозиторий:** https://github.com/bitrix24/b24phpsdk

#### Установка

**Способ 1: Composer (рекомендуется)**
```bash
composer require bitrix24/b24phpsdk
```

**Способ 2: Клонирование репозитория**
```bash
git clone https://github.com/bitrix24/b24phpsdk.git
```

#### Настройка

```php
<?php
require_once 'vendor/autoload.php';

use Bitrix24\SDK\Core\Core;
use Bitrix24\SDK\Core\Credentials\WebhookUrl;
use Bitrix24\SDK\Core\Credentials\ApplicationProfile;

// Вариант 1: Webhook
$core = new Core(
    new WebhookUrl('https://rest-api.bitrix24.com/rest/1/your-webhook-code/')
);

// Вариант 2: OAuth
$core = new Core(
    new ApplicationProfile(
        'your-client-id',
        'your-client-secret',
        'your-domain'
    )
);
```

#### Использование

**Базовый пример:**
```php
<?php
use Bitrix24\SDK\Core\Core;
use Bitrix24\SDK\Core\Credentials\WebhookUrl;
use Bitrix24\SDK\Services\CRM\Item\Service\ItemService;

$core = new Core(new WebhookUrl('https://rest-api.bitrix24.com/rest/1/your-webhook-code/'));

// Получение сервиса для работы с CRM элементами
$itemService = $core->getService(ItemService::class);

// Получение списка тикетов
$items = $itemService->list(
    entityTypeId: 140,
    filter: [
        '>=createdTime' => '2025-12-15 00:00:00',
        '<=createdTime' => '2025-12-21 23:59:59'
    ],
    select: ['id', 'title', 'stageId', 'createdTime']
);

// Автоматическая пагинация
foreach ($items as $item) {
    // Обработка данных
    echo $item->getId() . ': ' . $item->getTitle() . PHP_EOL;
}
```

**Работа с сервисами:**
```php
// CRM элементы
$itemService = $core->getService(ItemService::class);
$item = $itemService->get(140, 12345);

// Пользователи
$userService = $core->getService(UserService::class);
$user = $userService->get(1);

// Задачи
$taskService = $core->getService(TaskService::class);
$task = $taskService->get(12345);
```

**Обработка ошибок:**
```php
try {
    $item = $itemService->get(140, 12345);
} catch (Bitrix24ApiException $e) {
    // Обработка ошибок API
    error_log("Bitrix24 API error: " . $e->getMessage());
} catch (TransportException $e) {
    // Обработка сетевых ошибок
    error_log("Network error: " . $e->getMessage());
}
```

#### Преимущества

✅ **Современный подход:**
- Использует современные PHP-практики (типизация, интерфейсы)
- Поддержка PHP 8.0+
- Следование PSR-стандартам

✅ **Расширенная функциональность:**
- Типизированные сервисы для разных сущностей
- Автоматическая пагинация
- Встроенная обработка ошибок
- Retry-логика

✅ **Удобство использования:**
- Объектно-ориентированный подход
- Автодополнение в IDE
- Чёткая структура API

✅ **Активная поддержка:**
- Регулярные обновления
- Официальная поддержка от Bitrix24
- Активное сообщество

#### Недостатки

❌ **Сложность внедрения:**
- Требует рефакторинга существующего кода
- Больше зависимостей (Composer)
- Более сложная настройка

❌ **Производительность:**
- Больше накладных расходов
- Больше памяти
- Медленнее для простых запросов

❌ **Кривая обучения:**
- Нужно изучать новую структуру
- Другая модель работы с данными
- Больше абстракций

#### Сравнение с CRest

| Критерий | CRest | B24PhpSDK |
|----------|-------|-----------|
| Простота | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Функциональность | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Производительность | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Поддержка | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Современность | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Размер | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

### 3. BX24.js

**Тип:** JavaScript SDK для клиентской стороны  
**Версия:** Встроенная в Bitrix24  
**Статус в проекте:** ✅ Частично используется

#### Описание

BX24.js — это встроенная JavaScript-библиотека Bitrix24 для работы с REST API из клиентского кода. Библиотека доступна только внутри Bitrix24 (в iframe или встроенных приложениях).

**Документация:** https://dev.1c-bitrix.ru/rest_help/js_library/index.php

#### Установка

**Способ 1: Автоматическая загрузка (встроенное приложение)**
```html
<!-- Библиотека загружается автоматически при установке приложения -->
<script src="//api.bitrix24.com/api/v1/"></script>
```

**Способ 2: Ручная загрузка**
```html
<script src="https://api.bitrix24.com/api/v1/"></script>
```

**Текущая установка в проекте:**
- Файл: `vue-app/src/services/bitrix24-bx-api.js` — обёртка над BX24.js
- Используется для установки приложения и получения информации о пользователе

#### Настройка

Не требуется — библиотека работает автоматически при наличии в контексте Bitrix24.

#### Использование

**Базовый пример:**
```javascript
// Инициализация
BX24.init(() => {
    console.log('Bitrix24 API initialized');
    
    // Вызов метода
    BX24.callMethod('crm.item.list', {
        entityTypeId: 140,
        filter: {
            '>=createdTime': '2025-12-15 00:00:00'
        },
        select: ['id', 'title', 'stageId']
    }, (result) => {
        if (result.error()) {
            console.error('Error:', result.error());
        } else {
            const items = result.data().result.items;
            // Обработка данных
        }
    });
});
```

**Текущее использование в проекте:**
```javascript
// vue-app/src/services/bitrix24-bx-api.js
export class Bitrix24BxApi {
    static init(callback) {
        return new Promise((resolve, reject) => {
            if (typeof BX24 === 'undefined') {
                reject(new Error('Bitrix24 API not loaded'));
                return;
            }
            
            BX24.init(() => {
                if (callback) callback();
                resolve();
            });
        });
    }
    
    static callMethod(method, params = {}) {
        return new Promise((resolve, reject) => {
            BX24.callMethod(method, params, (result) => {
                if (result.error()) {
                    reject(new Error(result.error().error_description));
                } else {
                    resolve(result.data());
                }
            });
        });
    }
}
```

**Установка приложения:**
```javascript
BX24.init(() => {
    // Завершение установки
    BX24.installFinish();
});
```

**Получение информации о пользователе:**
```javascript
BX24.getUser((user) => {
    console.log('Current user:', user);
});

BX24.getAuth((auth) => {
    console.log('Auth info:', auth);
});
```

#### Преимущества

✅ **Встроенность:**
- Не требует дополнительных зависимостей
- Автоматическая авторизация
- Работает только внутри Bitrix24

✅ **Простота:**
- Минималистичный API
- Легко использовать
- Хорошая документация

✅ **Интеграция:**
- Прямой доступ к контексту Bitrix24
- Информация о пользователе
- Управление установкой приложения

#### Недостатки

❌ **Ограничения:**
- Работает только внутри Bitrix24 (iframe)
- Не работает в standalone-режиме
- Нет поддержки вебхуков

❌ **Устаревший подход:**
- Callback-based API (не Promises)
- Нет поддержки async/await из коробки
- Старый стиль кода

❌ **Ограниченная функциональность:**
- Только базовые методы
- Нет типизации
- Нет встроенной обработки ошибок

#### Сравнение с другими методами

| Критерий | BX24.js | CRest | B24JsSDK |
|----------|---------|-------|----------|
| Работа вне Bitrix24 | ❌ | ✅ | ✅ |
| Простота | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Современность | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Функциональность | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

### 4. B24JsSDK

**Тип:** JavaScript SDK для клиентской стороны  
**Версия:** Современная (активно поддерживается)  
**Статус в проекте:** ❌ Не используется

#### Описание

B24JsSDK — это современный JavaScript SDK для работы с Bitrix24 REST API. Поддерживает работу как внутри Bitrix24, так и вне его (через вебхуки). Предоставляет современный API с поддержкой Promises и async/await.

**Официальный репозиторий:** https://github.com/bitrix24/b24jssdk

#### Установка

**Способ 1: NPM (рекомендуется)**
```bash
npm install @bitrix24/b24jssdk
```

**Способ 2: CDN**
```html
<script src="https://unpkg.com/@bitrix24/b24jssdk/dist/b24jssdk.umd.js"></script>
```

#### Настройка

**Вариант 1: Webhook (для работы вне Bitrix24)**
```javascript
import { B24 } from '@bitrix24/b24jssdk';

const b24 = new B24({
    mode: 'webhook',
    webhook: 'https://rest-api.bitrix24.com/rest/1/your-webhook-code/'
});
```

**Вариант 2: Frame (для работы внутри Bitrix24)**
```javascript
import { B24, initializeB24Frame } from '@bitrix24/b24jssdk';

// Инициализация фрейма
const b24 = await initializeB24Frame();
```

**Вариант 3: OAuth (для локальных приложений)**
```javascript
import { B24 } from '@bitrix24/b24jssdk';

const b24 = new B24({
    mode: 'oauth',
    clientId: 'your-client-id',
    clientSecret: 'your-client-secret',
    domain: 'your-domain.bitrix24.com'
});
```

#### Использование

**Базовый пример:**
```javascript
import { B24 } from '@bitrix24/b24jssdk';

const b24 = new B24({
    mode: 'webhook',
    webhook: 'https://rest-api.bitrix24.com/rest/1/your-webhook-code/'
});

// Вызов метода (async/await)
try {
    const result = await b24.call('crm.item.list', {
        entityTypeId: 140,
        filter: {
            '>=createdTime': '2025-12-15 00:00:00'
        },
        select: ['id', 'title', 'stageId']
    });
    
    const items = result.result.items;
    // Обработка данных
} catch (error) {
    console.error('Error:', error);
}
```

**Работа с хуками (React/Vue):**
```javascript
import { useB24Helper } from '@bitrix24/b24jssdk';

// В React компоненте
function MyComponent() {
    const { call, loading, error } = useB24Helper();
    
    const fetchTickets = async () => {
        try {
            const result = await call('crm.item.list', {
                entityTypeId: 140
            });
            // Обработка данных
        } catch (err) {
            // Обработка ошибок
        }
    };
    
    return (
        <button onClick={fetchTickets} disabled={loading}>
            Загрузить тикеты
        </button>
    );
}
```

**Батч-запросы:**
```javascript
const batch = {
    tickets: ['crm.item.list', { entityTypeId: 140 }],
    users: ['user.get', { ID: '1,2,3' }]
};

const result = await b24.batch(batch);
// Результат: result.result.tickets, result.result.users
```

**Автоматическая пагинация:**
```javascript
// Автоматическая загрузка всех страниц
const allItems = await b24.callAll('crm.item.list', {
    entityTypeId: 140,
    filter: {...}
});
```

#### Преимущества

✅ **Современный подход:**
- Поддержка async/await
- TypeScript типы
- Современный JavaScript (ES6+)

✅ **Универсальность:**
- Работает внутри и вне Bitrix24
- Поддержка вебхуков и OAuth
- Гибкая настройка

✅ **Расширенная функциональность:**
- Автоматическая пагинация
- Батч-запросы
- Retry-логика
- Встроенная обработка ошибок

✅ **Интеграция с фреймворками:**
- React хуки (useB24Helper)
- Vue композаблы
- Удобные утилиты

✅ **Активная поддержка:**
- Регулярные обновления
- Официальная поддержка от Bitrix24
- Активное сообщество

#### Недостатки

❌ **Зависимости:**
- Требует npm/package manager
- Увеличивает размер бандла
- Дополнительные зависимости

❌ **Сложность внедрения:**
- Требует рефакторинга существующего кода
- Нужно изучить новый API
- Больше абстракций

❌ **Производительность:**
- Больше накладных расходов
- Больше памяти
- Медленнее для простых запросов

#### Сравнение с BX24.js

| Критерий | BX24.js | B24JsSDK |
|----------|---------|----------|
| Работа вне Bitrix24 | ❌ | ✅ |
| Современность | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Функциональность | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Простота | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Размер | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 📊 Сравнительная таблица всех методов

| Критерий | CRest | B24PhpSDK | BX24.js | B24JsSDK |
|----------|-------|-----------|---------|----------|
| **Тип** | PHP SDK | PHP SDK | JS SDK | JS SDK |
| **Работа вне Bitrix24** | ✅ | ✅ | ❌ | ✅ |
| **Работа внутри Bitrix24** | ✅ | ✅ | ✅ | ✅ |
| **Простота** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Функциональность** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Современность** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Поддержка** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Производительность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Размер** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Типизация** | ❌ | ✅ | ❌ | ✅ |
| **Автопагинация** | ❌ | ✅ | ❌ | ✅ |
| **Retry-логика** | ❌ | ✅ | ❌ | ✅ |
| **Обработка ошибок** | ⚠️ Ручная | ✅ Встроенная | ⚠️ Ручная | ✅ Встроенная |
| **Текущее использование** | ✅ Основной | ❌ | ✅ Частично | ❌ |

---

## 🎯 Рекомендации по выбору

### Для Backend (PHP)

#### Текущая ситуация: CRest

**Рекомендация:** Рассмотреть переход на B24PhpSDK для новых модулей

**Причины:**
- Более современный подход
- Лучшая поддержка
- Расширенная функциональность
- Типизация и автодополнение

**План миграции:**
1. Установить B24PhpSDK через Composer
2. Создать новый модуль с использованием B24PhpSDK
3. Постепенно мигрировать существующие модули
4. Сохранить CRest для обратной совместимости

#### Когда использовать CRest:
- ✅ Простые интеграции
- ✅ Минимальные зависимости
- ✅ Быстрый прототип
- ✅ Существующий код

#### Когда использовать B24PhpSDK:
- ✅ Новые проекты
- ✅ Сложная логика
- ✅ Требуется типизация
- ✅ Нужна расширенная функциональность

### Для Frontend (JavaScript)

#### Текущая ситуация: BX24.js (частично)

**Рекомендация:** Рассмотреть переход на B24JsSDK для новых модулей

**Причины:**
- Современный API (async/await)
- Работает вне Bitrix24
- Расширенная функциональность
- Интеграция с фреймворками

**План миграции:**
1. Установить B24JsSDK через npm
2. Создать обёртку для совместимости
3. Постепенно мигрировать существующие сервисы
4. Сохранить BX24.js для установки приложения

#### Когда использовать BX24.js:
- ✅ Установка приложения
- ✅ Получение информации о пользователе
- ✅ Простые запросы внутри Bitrix24
- ✅ Минимальные зависимости

#### Когда использовать B24JsSDK:
- ✅ Новые проекты
- ✅ Работа вне Bitrix24
- ✅ Сложная логика
- ✅ Интеграция с React/Vue

---

## 🔄 План миграции

### Этап 1: Подготовка (1-2 недели)

1. **Изучение B24PhpSDK:**
   - Установка через Composer
   - Изучение документации
   - Создание тестового модуля

2. **Изучение B24JsSDK:**
   - Установка через npm
   - Изучение документации
   - Создание тестового компонента

3. **Создание обёрток:**
   - Обёртка для совместимости с CRest
   - Обёртка для совместимости с BX24.js

### Этап 2: Пилотный проект (2-3 недели)

1. **Выбор модуля:**
   - Выбрать один модуль для миграции
   - Создать новую версию с B24PhpSDK/B24JsSDK
   - Сравнить производительность

2. **Тестирование:**
   - Функциональное тестирование
   - Производительность
   - Обработка ошибок

### Этап 3: Постепенная миграция (1-2 месяца)

1. **Миграция модулей:**
   - По одному модулю
   - Сохранение обратной совместимости
   - Обновление документации

2. **Оптимизация:**
   - Удаление дублирующего кода
   - Унификация подходов
   - Улучшение производительности

### Этап 4: Финальная миграция (1 месяц)

1. **Завершение миграции:**
   - Миграция всех модулей
   - Удаление старого кода
   - Обновление документации

2. **Оптимизация:**
   - Финальная оптимизация
   - Улучшение обработки ошибок
   - Мониторинг производительности

---

## 📚 Связанные документы

- [Архитектура получения данных из Bitrix24](./bitrix24-data-flow.md)
- [Документация модулей](../../MODULES/README.md)
- [Bitrix24 REST API Reference](../../API-REFERENCES/bitrix24-rest-api.md)

---

## 🔗 Полезные ссылки

### CRest PHP SDK
- Репозиторий: https://github.com/bitrix-tools/crest
- Документация: Встроенная в код

### B24PhpSDK
- Репозиторий: https://github.com/bitrix24/b24phpsdk
- Документация: https://github.com/bitrix24/b24phpsdk/wiki

### BX24.js
- Документация: https://dev.1c-bitrix.ru/rest_help/js_library/index.php

### B24JsSDK
- Репозиторий: https://github.com/bitrix24/b24jssdk
- Документация: https://github.com/bitrix24/b24jssdk/wiki
- NPM: https://www.npmjs.com/package/@bitrix24/b24jssdk

---

## 🔄 История изменений

- **2025-12-18 11:52 (UTC+3, Брест):** Создан документ с сравнением методов работы с Bitrix24

---

**Автор:** Рефактор-менеджер  
**Версия документа:** 1.0

