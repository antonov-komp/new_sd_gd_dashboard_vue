# План рефакторинга: Дашборд сектора 1С (TASK-005)

**Дата создания:** 2025-12-06 12:00 (UTC+3, Брест)  
**Статус:** Планирование  
**Приоритет:** Средний  
**Исполнитель:** Рефактор-менеджер

---

## 📋 Анализ текущего состояния

### Проблемы текущей реализации

#### 1. **Монолитный сервис (641 строка)**
- `dashboard-sector-1c-service.js` содержит слишком много ответственностей:
  - Data access (загрузка из API)
  - Business logic (группировка, фильтрация)
  - Data mapping (маппинг форматов)
  - Error handling
- Сложно тестировать
- Сложно поддерживать
- Нарушение принципа Single Responsibility

#### 2. **Производительность**
- Фильтрация по тегу сектора на клиенте (вместо сервера)
- Нет кеширования данных
- Множественные проверки форматов данных в циклах
- Загрузка всех тикетов по стадиям отдельно (можно оптимизировать)

#### 3. **Архитектура**
- Нет разделения на слои (data access, business logic, presentation)
- Маппинг данных смешан с бизнес-логикой
- Нет типизации (TypeScript или JSDoc)
- Дублирование логики обработки ошибок

#### 4. **Компоненты**
- `DashboardSector1C.vue` содержит слишком много логики управления состоянием
- Нет композаблов для переиспользования логики
- Дублирование логики Drag & Drop в компонентах

#### 5. **Тестируемость**
- Сложно тестировать из-за монолитной структуры
- Нет моков для API
- Логика тесно связана с Bitrix24 API

---

## 🎯 Цели рефакторинга

1. **Разделение ответственностей** — каждый модуль отвечает за одну задачу
2. **Улучшение производительности** — кеширование, оптимизация запросов
3. **Повышение тестируемости** — изоляция логики, моки
4. **Улучшение поддерживаемости** — понятная структура, типизация
5. **Переиспользование кода** — композаблы, утилиты

---

## 📐 План рефакторинга (5 этапов)

### **ЭТАП 1: Разделение сервиса на слои**

**Цель:** Разделить монолитный сервис на отдельные модули по ответственности

**Задачи:**
1. Создать структуру папок:
   ```
   services/dashboard-sector-1c/
   ├── data/
   │   ├── ticket-repository.js      # Работа с API (crm.item.*)
   │   ├── employee-repository.js     # Работа с API (user.get)
   │   └── api-client.js              # Базовый клиент для API
   ├── mappers/
   │   ├── ticket-mapper.js           # Маппинг тикетов
   │   ├── stage-mapper.js            # Маппинг этапов
   │   └── employee-mapper.js         # Маппинг сотрудников
   ├── filters/
   │   └── sector-filter.js           # Фильтрация по сектору
   ├── groupers/
   │   └── ticket-grouper.js          # Группировка тикетов
   └── index.js                       # Публичный API сервиса
   ```

2. Вынести data access в репозитории:
   - `ticket-repository.js` — методы `getAllTickets()`, `getTicketsByStage()`, `assignTicket()`, `createTicket()`
   - `employee-repository.js` — методы `getEmployeesByIds()`

3. Вынести маппинг в отдельные модули:
   - `ticket-mapper.js` — `mapTicket()`, `mapPriority()`, `mapStatus()`
   - `stage-mapper.js` — `mapStageId()`, `mapStageIdToBitrix()`
   - `employee-mapper.js` — `mapEmployee()`

4. Вынести бизнес-логику:
   - `sector-filter.js` — фильтрация по тегу сектора
   - `ticket-grouper.js` — группировка по этапам и сотрудникам

5. Создать публичный API в `index.js`:
   - Методы `getSectorData()`, `assignTicket()`, `createTicket()`
   - Использует все модули внутри

**Ожидаемый результат:**
- Каждый модуль отвечает за одну задачу
- Легко тестировать каждый модуль отдельно
- Легко заменить реализацию (например, моки для тестов)

**Файлы для создания:**
- `services/dashboard-sector-1c/data/ticket-repository.js`
- `services/dashboard-sector-1c/data/employee-repository.js`
- `services/dashboard-sector-1c/data/api-client.js`
- `services/dashboard-sector-1c/mappers/ticket-mapper.js`
- `services/dashboard-sector-1c/mappers/stage-mapper.js`
- `services/dashboard-sector-1c/mappers/employee-mapper.js`
- `services/dashboard-sector-1c/filters/sector-filter.js`
- `services/dashboard-sector-1c/groupers/ticket-grouper.js`
- `services/dashboard-sector-1c/index.js`

**Файлы для удаления:**
- `services/dashboard-sector-1c-service.js` (после миграции)

---

### **ЭТАП 2: Оптимизация производительности**

**Цель:** Улучшить производительность загрузки и обработки данных

**Задачи:**
1. Добавить кеширование:
   - Создать `services/dashboard-sector-1c/cache/cache-manager.js`
   - Кешировать данные тикетов на 5 минут
   - Кешировать данные сотрудников на 30 минут
   - Инвалидация кеша при обновлении тикетов

2. Оптимизировать загрузку тикетов:
   - Загружать все стадии одним запросом (если возможно)
   - Использовать batch-запросы для получения данных сотрудников
   - Добавить debounce для обновления данных

3. Оптимизировать фильтрацию:
   - Вынести фильтрацию по тегу в отдельный метод
   - Использовать `Map` для быстрого поиска
   - Мемоизация результатов фильтрации

4. Добавить виртуализацию списков:
   - Для больших списков тикетов использовать виртуализацию
   - Компонент `VirtualList.vue` для отображения длинных списков

**Ожидаемый результат:**
- Сокращение времени загрузки на 30-50%
- Сокращение количества запросов к API
- Улучшение отзывчивости интерфейса

**Файлы для создания:**
- `services/dashboard-sector-1c/cache/cache-manager.js`
- `services/dashboard-sector-1c/utils/batch-loader.js`
- `components/dashboard/VirtualList.vue` (опционально)

---

### **ЭТАП 3: Создание композаблов и утилит**

**Цель:** Вынести переиспользуемую логику в композаблы

**Задачи:**
1. Создать композабл для управления состоянием дашборда:
   - `composables/useDashboardState.js` — состояние (stages, employees, tickets)
   - `composables/useDashboardActions.js` — действия (load, assign, move)

2. Создать композабл для Drag & Drop:
   - `composables/useDragAndDrop.js` — логика перетаскивания
   - Переиспользовать в `EmployeeColumn.vue`, `ZeroPoint.vue`, `TicketCard.vue`

3. Создать композабл для работы с уведомлениями:
   - `composables/useNotifications.js` — обёртка над BX.UI.Notification

4. Создать утилиты:
   - `utils/validation.js` — валидация данных
   - `utils/error-handler.js` — обработка ошибок
   - `utils/constants.js` — константы (стадии, приоритеты)

**Ожидаемый результат:**
- Переиспользование логики между компонентами
- Упрощение компонентов
- Легче тестировать логику отдельно от компонентов

**Файлы для создания:**
- `composables/useDashboardState.js`
- `composables/useDashboardActions.js`
- `composables/useDragAndDrop.js`
- `composables/useNotifications.js`
- `utils/validation.js`
- `utils/error-handler.js`
- `utils/constants.js`

---

### **ЭТАП 4: Улучшение компонентов**

**Цель:** Упростить компоненты, вынести логику в композаблы

**Задачи:**
1. Рефакторинг `DashboardSector1C.vue`:
   - Использовать `useDashboardState` и `useDashboardActions`
   - Упростить setup() функцию
   - Вынести обработку ошибок в композабл

2. Рефакторинг компонентов с Drag & Drop:
   - `EmployeeColumn.vue` — использовать `useDragAndDrop`
   - `ZeroPoint.vue` — использовать `useDragAndDrop`
   - `TicketCard.vue` — использовать `useDragAndDrop`

3. Добавить типизацию (JSDoc):
   - Добавить JSDoc комментарии для всех props, emits, методов
   - Указать типы данных

4. Улучшить обработку ошибок:
   - Использовать `useNotifications` для уведомлений
   - Использовать `error-handler.js` для обработки ошибок

**Ожидаемый результат:**
- Компоненты стали проще и понятнее
- Логика вынесена в композаблы
- Лучшая типизация и документация

**Файлы для обновления:**
- `components/dashboard/DashboardSector1C.vue`
- `components/dashboard/EmployeeColumn.vue`
- `components/dashboard/ZeroPoint.vue`
- `components/dashboard/TicketCard.vue`
- `components/dashboard/DashboardStage.vue`

---

### **ЭТАП 5: Тестирование и документация**

**Цель:** Добавить тесты и улучшить документацию

**Задачи:**
1. Создать unit-тесты:
   - Тесты для репозиториев (с моками API)
   - Тесты для мапперов
   - Тесты для фильтров и групперов
   - Тесты для композаблов

2. Создать integration-тесты:
   - Тесты для компонентов (с моками сервисов)
   - Тесты для Drag & Drop

3. Улучшить документацию:
   - Обновить `DOCS/TASKS/TASK-005-IMPLEMENTATION-SUMMARY.md`
   - Создать `DOCS/ARCHITECTURE/dashboard-sector-1c.md` с описанием архитектуры
   - Добавить примеры использования композаблов

4. Добавить JSDoc комментарии:
   - Для всех публичных методов
   - Для всех типов данных
   - Для всех констант

**Ожидаемый результат:**
- Покрытие тестами > 70%
- Полная документация архитектуры
- Примеры использования

**Файлы для создания:**
- `tests/unit/services/dashboard-sector-1c/` (все модули)
- `tests/integration/components/dashboard/` (компоненты)
- `DOCS/ARCHITECTURE/dashboard-sector-1c.md`

---

## 📊 Метрики успеха

### До рефакторинга:
- Размер сервиса: 641 строка (монолитный)
- Время загрузки: ~3-5 секунд
- Количество запросов: 3+ (по стадиям) + N (сотрудники)
- Покрытие тестами: 0%
- Количество ответственностей в сервисе: 5+

### После рефакторинга:
- Размер модулей: < 200 строк каждый
- Время загрузки: ~1-2 секунды (с кешем)
- Количество запросов: 1-2 (оптимизировано)
- Покрытие тестами: > 70%
- Количество ответственностей в модуле: 1

---

## 🔄 Порядок выполнения

1. **ЭТАП 1** — основа для всех остальных этапов
2. **ЭТАП 2** — можно выполнять параллельно с этапом 1
3. **ЭТАП 3** — зависит от этапа 1
4. **ЭТАП 4** — зависит от этапов 1 и 3
5. **ЭТАП 5** — финальный этап, зависит от всех предыдущих

---

## ⚠️ Риски и митигация

### Риск 1: Поломка функциональности при рефакторинге
**Митигация:**
- Постепенная миграция (сначала создать новые модули, затем переключить компоненты)
- Сохранение старого сервиса до полной миграции
- Тестирование после каждого этапа

### Риск 2: Увеличение сложности из-за большого количества файлов
**Митигация:**
- Чёткая структура папок
- Документация архитектуры
- Примеры использования

### Риск 3: Проблемы с производительностью кеша
**Митигация:**
- Настройка TTL кеша
- Инвалидация кеша при обновлениях
- Мониторинг использования памяти

---

## 📝 Чек-лист перед началом

- [ ] Изучить текущую реализацию
- [ ] Создать ветку для рефакторинга
- [ ] Создать структуру папок
- [ ] Написать план миграции для каждого этапа
- [ ] Согласовать план с командой

---

## 🔗 Связанные документы

- `DOCS/TASKS/TASK-005-dashboard-sector-1c.md` — исходная задача
- `DOCS/TASKS/TASK-005-IMPLEMENTATION-SUMMARY.md` — итоги реализации
- `vue-app/src/services/dashboard-sector-1c-service.js` — текущий сервис

---

**Дата создания:** 2025-12-06 12:00 (UTC+3, Брест)  
**Автор:** Рефактор-менеджер

