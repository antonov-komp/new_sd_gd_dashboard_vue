# TASK-069: План рефакторинга модуля tickets-time-tracking-sector-1c

**Дата создания:** 2025-12-23 18:18 (UTC+3, Брест)  
**Версия:** 1.0  
**Статус:** Планирование  
**Исполнители:** Рефактор-менеджер, Технический писатель

---

## 📋 Обзор задачи

### Цель рефакторинга

Разделить монолитный файл `api/tickets-time-tracking-sector-1c.php` (1222 строки) на модульную архитектуру с чётким разделением слоёв и сервисов, сохранив весь функционал и работоспособность модуля.

### Текущее состояние

- **Файл:** `api/tickets-time-tracking-sector-1c.php` (1222 строки)
- **Тип:** Монолитный PHP-скрипт с множеством функций
- **Зависимости:** CRest (Bitrix24 REST API клиент)
- **Функционал:**
  - Получение сотрудников сектора 1С
  - Получение записей трудозатрат
  - Матчинг задач с тикетами
  - Агрегация по неделям и сотрудникам
  - Пагинация задач

### Целевое состояние

Модульная архитектура с разделением на слои:
- **Config** — конфигурация
- **Util** — утилиты (даты, недели, валидация)
- **Bitrix** — клиент Bitrix24
- **Repository** — репозитории для работы с данными
- **Domain** — доменная логика (матчинг, агрегация)
- **Service** — бизнес-логика
- **Controller** — обработка HTTP-запросов

---

## 🎯 Принципы рефакторинга

1. **Сохранение функционала** — все существующие возможности должны работать идентично
2. **Модульность** — каждый компонент имеет одну ответственность
3. **Тестируемость** — компоненты легко тестировать изолированно
4. **Читаемость** — код должен быть понятным и документированным
5. **Совместимость** — API endpoint остаётся доступным по тому же URL

---

## 📐 Целевая структура модуля

```
api/
├── tickets-time-tracking-sector-1c.php          # Legacy endpoint (перенаправляет на bootstrap.php)
└── tickets-time-tracking-sector-1c/            # Новый модульный код
    ├── bootstrap.php                           # Точка входа нового модуля
    ├── controller/
    │   └── TimeTrackingController.php         # Обработка HTTP-запросов
    ├── service/
    │   └── TimeTrackingService.php            # Основная бизнес-логика
    ├── bitrix/
    │   └── Bitrix24Client.php                 # Клиент Bitrix24 REST API
    ├── repository/
    │   ├── EmployeeRepository.php              # Репозиторий сотрудников
    │   ├── TaskRepository.php                  # Репозиторий задач
    │   ├── TicketRepository.php               # Репозиторий тикетов
    │   └── ElapsedTimeRepository.php          # Репозиторий трудозатрат
    ├── domain/
    │   ├── TaskTicketMatcher.php              # Матчинг задач с тикетами
    │   ├── TimeAggregator.php                 # Агрегация по неделям и сотрудникам
    │   └── EmployeeSummaryBuilder.php          # Построение summary по сотрудникам
    ├── config/
    │   └── TimeTrackingConfig.php             # Конфигурация модуля
    └── util/
        ├── WeekHelper.php                      # Утилиты для работы с неделями
        ├── DateHelper.php                      # Утилиты для работы с датами
        └── ResponseHelper.php                  # Утилиты для формирования ответов
```

---

## 📅 Поэтапный план (10 этапов)

### Этап 1: Анализ и подготовка структуры

**Цель:** Проанализировать текущий код, выделить функциональные блоки и спроектировать структуру модуля.

**Задачи:**
- [ ] Провести анализ всех функций в текущем файле
- [ ] Выделить функциональные блоки (получение данных, матчинг, агрегация)
- [ ] Определить зависимости между компонентами
- [ ] Спроектировать интерфейсы и контракты
- [ ] Создать диаграмму архитектуры
- [ ] Подготовить список тестовых сценариев

**Результат:**
- Документ с анализом текущего кода
- Диаграмма целевой архитектуры
- Список интерфейсов и контрактов
- План тестирования

**Время:** 2-3 часа

---

### Этап 2: Создание базовой структуры модуля и конфигурации

**Цель:** Создать структуру папок и базовые классы конфигурации.

**Задачи:**
- [ ] Создать структуру папок модуля
- [ ] Создать класс `TimeTrackingConfig` с константами:
  - `SECTOR_1C_DEPARTMENT_ID`
  - `ENTITY_TYPE_ID`
  - `SECTOR_1C_TAG`
  - Размеры страниц для пагинации
- [ ] Создать базовый namespace для модуля
- [ ] Настроить автозагрузку классов (если используется)
- [ ] Создать `bootstrap.php` как точку входа

**Результат:**
- Структура папок создана
- Класс `TimeTrackingConfig` реализован
- `bootstrap.php` создан (пока пустой)

**Время:** 1-2 часа

---

### Этап 3: Выделение утилит (даты, недели, валидация)

**Цель:** Выделить вспомогательные функции в отдельные классы-утилиты.

**Задачи:**
- [ ] Создать `WeekHelper`:
  - `getWeekBounds()` — границы текущей ISO-недели
  - `getFourWeeksBounds()` — границы 4 недель
  - `getWeekNumberByDate()` — определение номера недели по дате
  - `isInRange()` — проверка попадания даты в интервал
- [ ] Создать `DateHelper`:
  - Методы для работы с датами в UTC
  - Преобразование форматов дат
- [ ] Создать `ResponseHelper`:
  - `jsonResponse()` — формирование JSON-ответа
  - `parseJsonBody()` — парсинг тела запроса

**Результат:**
- Классы `WeekHelper`, `DateHelper`, `ResponseHelper` реализованы
- Все функции протестированы изолированно

**Время:** 2-3 часа

---

### Этап 4: Создание клиента Bitrix24

**Цель:** Создать абстракцию для работы с Bitrix24 REST API.

**Задачи:**
- [ ] Создать класс `Bitrix24Client`:
  - Обёртка над `CRest::call()` и `CRest::callBatch()`
  - Обработка ошибок API
  - Логирование запросов
  - Методы для работы с пользователями, задачами, тикетами
- [ ] Выделить методы:
  - `getUsers()` — получение пользователей
  - `getTasks()` — получение задач
  - `getTasksBatch()` — батч-запрос задач
  - `getElapsedItems()` — получение записей трудозатрат
  - `getTickets()` — получение тикетов
- [ ] Добавить обработку ошибок и логирование

**Результат:**
- Класс `Bitrix24Client` реализован
- Все методы протестированы

**Время:** 3-4 часа

---

### Этап 5: Создание репозиториев (Employees, Tasks, Tickets, ElapsedTime)

**Цель:** Создать репозитории для работы с данными из Bitrix24.

**Задачи:**
- [ ] Создать `EmployeeRepository`:
  - `getSector1CEmployees()` — получение сотрудников сектора 1С
  - `getEmployeesData()` — получение данных сотрудников по ID
- [ ] Создать `TaskRepository`:
  - `getTasksByIds()` — получение задач по ID
  - `getTasksWithElapsedTime()` — получение задач с трудозатратами
  - `getTasksDetails()` — получение детальной информации о задачах
- [ ] Создать `TicketRepository`:
  - `getTicketsByIds()` — получение тикетов по ID
  - `getTicketsByProduct()` — получение тикетов по продукту
- [ ] Создать `ElapsedTimeRepository`:
  - `getElapsedTimeRecords()` — получение записей трудозатрат
  - Фильтрация по сотрудникам и периоду

**Результат:**
- Все репозитории реализованы
- Репозитории используют `Bitrix24Client`
- Методы протестированы

**Время:** 4-5 часов

---

### Этап 6: Создание доменных сервисов (матчинг, агрегация)

**Цель:** Выделить доменную логику в отдельные сервисы.

**Задачи:**
- [ ] Создать `TaskTicketMatcher`:
  - `matchTasksWithTickets()` — матчинг задач с тикетами
  - Парсинг поля `ufCrmTask` (формат `T8c_3093`)
  - Альтернативные варианты матчинга
- [ ] Создать `TimeAggregator`:
  - `aggregateByWeeksAndEmployees()` — агрегация по неделям и сотрудникам
  - Подсчёт уникальных задач и тикетов
  - Округление значений
- [ ] Создать `EmployeeSummaryBuilder`:
  - `createEmployeesSummary()` — построение summary по сотрудникам
  - Агрегация метрик по всем неделям

**Результат:**
- Доменные сервисы реализованы
- Логика изолирована и протестирована

**Время:** 4-5 часов

---

### Этап 7: Создание основного сервиса бизнес-логики

**Цель:** Создать сервис, который оркестрирует всю бизнес-логику.

**Задачи:**
- [ ] Создать `TimeTrackingService`:
  - Метод `getTimeTrackingData()` — основной метод получения данных
  - Оркестрация работы репозиториев и доменных сервисов
  - Формирование структуры ответа
  - Обработка опциональных параметров (`includeTaskDetails`)
  - Подсчёт общих метрик
- [ ] Интегрировать все компоненты:
  - Репозитории
  - Доменные сервисы
  - Утилиты
- [ ] Добавить обработку ошибок и логирование

**Результат:**
- Класс `TimeTrackingService` реализован
- Вся бизнес-логика собрана в одном месте
- Сервис протестирован

**Время:** 3-4 часа

---

### Этап 8: Создание контроллера

**Цель:** Создать контроллер для обработки HTTP-запросов.

**Задачи:**
- [ ] Создать `TimeTrackingController`:
  - Парсинг входных данных (тело запроса)
  - Валидация параметров (`product`, `weekStartUtc`, `weekEndUtc`)
  - Вызов `TimeTrackingService`
  - Формирование HTTP-ответа
  - Обработка исключений
- [ ] Реализовать методы:
  - `handleRequest()` — основной метод обработки запроса
  - `validateRequest()` — валидация входных данных
  - `formatResponse()` — форматирование ответа

**Результат:**
- Класс `TimeTrackingController` реализован
- Контроллер протестирован

**Время:** 2-3 часа

---

### Этап 9: Рефакторинг endpoint и интеграция

**Цель:** Интегрировать новый модуль и обновить endpoint.

**Задачи:**
- [ ] Обновить `bootstrap.php`:
  - Подключение всех необходимых классов
  - Инициализация контроллера
  - Обработка запроса
- [ ] Обновить `tickets-time-tracking-sector-1c.php`:
  - Перенаправление на `bootstrap.php`
  - Сохранение обратной совместимости
- [ ] Провести интеграционное тестирование:
  - Проверить все сценарии использования
  - Сравнить ответы старого и нового кода
  - Проверить производительность

**Результат:**
- Endpoint работает через новый модуль
- Обратная совместимость сохранена
- Все тесты проходят

**Время:** 3-4 часа

---

### Этап 10: Тестирование и документация

**Цель:** Завершить рефакторинг тестированием и документацией.

**Задачи:**
- [ ] Написать unit-тесты для всех компонентов:
  - Утилиты
  - Репозитории
  - Доменные сервисы
  - Основной сервис
  - Контроллер
- [ ] Провести интеграционное тестирование:
  - Все сценарии использования API
  - Граничные случаи
  - Обработка ошибок
- [ ] Обновить документацию:
  - Описание архитектуры модуля
  - API-документация
  - Примеры использования
- [ ] Провести code review
- [ ] Подготовить миграционный план (если нужен)

**Результат:**
- Все тесты написаны и проходят
- Документация обновлена
- Code review пройден
- Модуль готов к использованию

**Время:** 4-6 часов

---

## 📊 Итоговая оценка времени

| Этап | Время (часы) |
|------|--------------|
| Этап 1: Анализ и подготовка | 2-3 |
| Этап 2: Базовая структура | 1-2 |
| Этап 3: Утилиты | 2-3 |
| Этап 4: Клиент Bitrix24 | 3-4 |
| Этап 5: Репозитории | 4-5 |
| Этап 6: Доменные сервисы | 4-5 |
| Этап 7: Основной сервис | 3-4 |
| Этап 8: Контроллер | 2-3 |
| Этап 9: Интеграция | 3-4 |
| Этап 10: Тестирование и документация | 4-6 |
| **ИТОГО** | **28-39 часов** |

---

## ✅ Критерии успешности рефакторинга

1. **Функциональность:**
   - Все существующие функции работают идентично
   - API endpoint возвращает те же данные в том же формате
   - Обработка ошибок работает корректно

2. **Архитектура:**
   - Код разделён на слои (Config, Util, Bitrix, Repository, Domain, Service, Controller)
   - Каждый компонент имеет одну ответственность
   - Зависимости явные и управляемые

3. **Качество кода:**
   - Код соответствует стандартам PSR-12
   - Все методы документированы
   - Логирование добавлено для всех операций

4. **Тестируемость:**
   - Компоненты легко тестировать изолированно
   - Unit-тесты покрывают основную функциональность
   - Интеграционные тесты проверяют работу модуля

5. **Документация:**
   - Архитектура модуля задокументирована
   - API-документация обновлена
   - Примеры использования добавлены

---

## 🔗 Связанные документы

- **Исходный файл:** `api/tickets-time-tracking-sector-1c.php`
- **Контракт:** TASK-050-02 (если есть)
- **Пример архитектуры:** `DOCS/ARCHITECTURE/graph-admission-closure-analysis.md`
- **Стандарты кода:** PSR-12

---

## 📝 Примечания

- Каждый этап будет детализирован в отдельном документе
- Рефакторинг выполняется поэтапно с сохранением работоспособности на каждом этапе
- После каждого этапа проводится проверка работоспособности
- При необходимости этапы могут быть разбиты на подэтапы

---

**История правок:**
- 2025-12-23 18:18 (UTC+3, Брест): Создан план рефакторинга

