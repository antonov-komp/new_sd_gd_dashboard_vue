# Архитектура дашборда сектора 1С

**Дата создания:** 2025-12-06 18:00 (UTC+3, Брест)  
**Версия:** 2.0 (после рефакторинга)  
**Статус:** Актуальная

---

## 📐 Обзор архитектуры

Дашборд сектора 1С реализован с использованием модульной архитектуры, разделённой на слои:

1. **Data Layer** (Слой данных) — репозитории для работы с API
2. **Business Logic Layer** (Слой бизнес-логики) — мапперы, фильтры, групперы
3. **Presentation Layer** (Слой представления) — Vue.js компоненты и композаблы
4. **Cache Layer** (Слой кеширования) — менеджер кеша для оптимизации

---

## 🏗️ Структура модулей

```
vue-app/src/
├── services/
│   └── dashboard-sector-1c/
│       ├── data/                    # Data Layer
│       │   ├── api-client.js        # Базовый клиент API
│       │   ├── ticket-repository.js # Репозиторий тикетов
│       │   └── employee-repository.js # Репозиторий сотрудников
│       ├── mappers/                 # Business Logic Layer
│       │   ├── ticket-mapper.js     # Маппинг тикетов
│       │   ├── stage-mapper.js      # Маппинг этапов
│       │   └── employee-mapper.js   # Маппинг сотрудников
│       ├── filters/                 # Business Logic Layer
│       │   └── sector-filter.js     # Фильтрация по сектору
│       ├── groupers/                # Business Logic Layer
│       │   └── ticket-grouper.js    # Группировка тикетов
│       ├── cache/                   # Cache Layer
│       │   └── cache-manager.js     # Менеджер кеша
│       ├── utils/                   # Утилиты
│       │   ├── constants.js         # Константы
│       │   ├── validation.js       # Валидация
│       │   ├── error-handler.js     # Обработка ошибок
│       │   ├── batch-loader.js      # Батч-загрузка
│       │   ├── logger.js            # Служба логирования
│       │   └── logger-config.js     # Конфигурация логирования
│       └── index.js                 # Публичный API
├── composables/                     # Presentation Layer
│   ├── useDashboardState.js        # Состояние дашборда
│   ├── useDashboardActions.js      # Действия дашборда
│   ├── useDragAndDrop.js           # Drag & Drop логика
│   ├── useNotifications.js         # Уведомления
│   └── useLogger.js                # Логирование (композабл)
└── components/
    └── dashboard/
        ├── DashboardSector1C.vue    # Главный компонент
        ├── DashboardStage.vue       # Компонент этапа
        ├── EmployeeColumn.vue       # Колонка сотрудника
        ├── ZeroPoint.vue            # Нулевая точка
        ├── TicketCard.vue           # Карточка тикета
        └── LoggerControl.vue        # Управление логированием
```

---

## 🔄 Поток данных

### Загрузка данных сектора

```
1. DashboardSector1C.vue (компонент)
   ↓ вызывает
2. useDashboardActions.loadSectorData()
   ↓ вызывает
3. DashboardSector1CService.getSectorData()
   ↓ проверяет кеш
4. CacheManager.get('sector:data')
   ↓ если нет в кеше:
5. TicketRepository.getAllTickets(stages)
   ↓ для каждой стадии:
6. TicketRepository.getTicketsByStage(stageId)
   ↓ проверяет кеш тикетов
7. ApiClient.call('crm.item.list')
   ↓ получает данные
8. filterBySector(tickets) - фильтрация по сектору 1С
   ↓ извлекает сотрудников
9. extractUniqueEmployeeIds(tickets)
   ↓ загружает данные сотрудников
10. EmployeeRepository.getEmployeesByIds(ids)
    ↓ проверяет кеш сотрудников
11. ApiClient.call('user.get')
    ↓ маппинг данных
12. mapEmployees(bitrixUsers)
    ↓ группировка
13. groupTicketsByStages(tickets, employees)
    ↓ сохранение в кеш
14. CacheManager.set('sector:data', result)
    ↓ возврат данных
15. useDashboardState.updateState(data)
```

### Перемещение тикета

```
1. EmployeeColumn.vue (компонент)
   ↓ событие drop
2. useDragAndDrop.handleDrop()
   ↓ валидация
3. canMoveTicket(ticket, employeeId, stageId)
   ↓ если валидно:
4. useDashboardActions.assignTicket()
   ↓ вызывает
5. DashboardSector1CService.assignTicket()
   ↓ вызывает
6. TicketRepository.assignTicket()
   ↓ вызывает
7. ApiClient.call('crm.item.update')
   ↓ инвалидация кеша
8. CacheManager.invalidateTicketsCache()
   ↓ обновление локального состояния
9. useDashboardState.updateLocalStateAfterMove()
```

---

## 📦 Описание модулей

### Data Layer

#### `api-client.js`
**Ответственность:** Базовый клиент для работы с Bitrix24 REST API

**Методы:**
- `call(method, params)` — вызов метода API

**Используется:** Репозиториями для выполнения запросов

#### `ticket-repository.js`
**Ответственность:** Работа с тикетами через Bitrix24 API

**Методы:**
- `getAllTickets(stageIds)` — получение всех тикетов по стадиям
- `getTicketsByStage(stageId, useCache)` — получение тикетов по стадии (с кешированием)
- `getTicket(ticketId)` — получение тикета по ID
- `updateTicket(ticketId, fields)` — обновление тикета
- `createTicket(fields)` — создание тикета
- `assignTicket(ticketId, employeeId, bitrixStageId)` — назначение тикета

**Особенности:**
- Использует кеширование (TTL: 5 минут)
- Инвалидирует кеш при обновлениях

#### `employee-repository.js`
**Ответственность:** Работа с сотрудниками через Bitrix24 API

**Методы:**
- `getEmployeesByIds(employeeIds, useCache)` — получение сотрудников по ID (с кешированием)

**Особенности:**
- Использует кеширование (TTL: 30 минут)

### Business Logic Layer

#### `ticket-mapper.js`
**Ответственность:** Преобразование тикетов из формата Bitrix24 в внутренний формат

**Методы:**
- `mapTicket(bitrixTicket)` — маппинг тикета
- `mapPriority(bitrixPriority)` — маппинг приоритета
- `mapPriorityToBitrix(priority)` — обратный маппинг приоритета
- `mapStatus(bitrixStatus)` — маппинг статуса

#### `stage-mapper.js`
**Ответственность:** Преобразование ID этапов между Bitrix24 и внутренним форматом

**Методы:**
- `mapStageId(bitrixStageId)` — маппинг ID этапа
- `mapStageIdToBitrix(stageId)` — обратный маппинг ID этапа
- `getTargetStages()` — получение списка стадий для загрузки

#### `employee-mapper.js`
**Ответственность:** Преобразование сотрудников из формата Bitrix24 в внутренний формат

**Методы:**
- `mapEmployee(bitrixUser)` — маппинг сотрудника
- `mapEmployees(bitrixUsers)` — маппинг массива сотрудников

#### `sector-filter.js`
**Ответственность:** Фильтрация тикетов по сектору 1С

**Методы:**
- `filterBySector(tickets)` — фильтрация тикетов (с мемоизацией)
- `clearFilterCache()` — очистка кеша фильтрации

**Особенности:**
- Использует мемоизацию для оптимизации повторных фильтраций

#### `ticket-grouper.js`
**Ответственность:** Группировка тикетов по этапам и сотрудникам

**Константы:**
- `KEEPER_OBJECTS_ID = 1051` — ID ответственного "Хранитель объектов"

**Методы:**
- `groupTicketsByStages(tickets, employees)` — группировка по этапам (исключает тикеты с ответственным 1051 из колонок сотрудников)
- `getZeroPointTickets(tickets)` — получение тикетов нулевой точки (включает тикеты без ответственного и с ответственным 1051)
- `extractUniqueEmployeeIds(tickets)` — извлечение уникальных ID сотрудников

**Логика нулевой точки:**
Нулевая точка включает тикеты:
1. Без назначенного сотрудника (`assignedById === null`)
2. С ответственным 1051 "Хранитель объектов" (`assignedById === 1051`)

Тикеты с ответственным 1051 не попадают в колонки сотрудников, а отображаются только в нулевой точке соответствующего этапа.

**Важно:** Сотрудник с ID 1051 также исключается из списка сотрудников этапа при создании структуры этапов, чтобы он не отображался в рендере стадий (не рендерится в колонках).

### Cache Layer

#### `cache-manager.js`
**Ответственность:** Управление кешированием данных

**Методы:**
- `get(key)` — получение данных из кеша
- `set(key, data, ttl)` — сохранение данных в кеш
- `delete(key)` — удаление записи
- `clear()` — очистка всего кеша
- `invalidateByPrefix(prefix)` — инвалидация по префиксу
- `cleanExpired()` — очистка истёкших записей

**TTL по умолчанию:**
- Тикеты: 5 минут
- Сотрудники: 30 минут
- Данные сектора: 5 минут

**📖 Подробная документация:** См. [Механизм кеширования запросов](cache-mechanism-dashboard-sector-1c.md) для детального описания работы кеша, жизненного цикла данных и примеров использования.

### Presentation Layer

#### `useDashboardState.js`
**Ответственность:** Управление состоянием дашборда

**Состояние:**
- `isLoading` — загрузка данных
- `error` — ошибка
- `stages` — этапы с тикетами
- `zeroPointTickets` — тикеты нулевой точки
- `employees` — список сотрудников
- `draggedTicket` — перетаскиваемый тикет

**Методы:**
- `updateState(data)` — обновление состояния
- `resetState()` — сброс состояния
- `updateLocalStateAfterMove(ticket, employeeId, stageId)` — обновление после перемещения
- `getEmployeeTickets(employeeId, stageId)` — получение тикетов сотрудника

#### `useDashboardActions.js`
**Ответственность:** Действия дашборда (загрузка, назначение, создание)

**Методы:**
- `loadSectorData(useCache)` — загрузка данных сектора
- `assignTicket(ticket, employeeId, stageId)` — назначение тикета
- `moveTicketToStage(ticket, stageId)` — перемещение на этап
- `assignTicketToEmployee(ticket, employeeId)` — назначение сотруднику
- `createTicket(ticketData)` — создание тикета
- `handleTicketDragStart(ticket)` — начало перетаскивания
- `handleTicketDrop(ticket, employeeId, stageId)` — сброс тикета

#### `useDragAndDrop.js`
**Ответственность:** Логика Drag & Drop

**Состояние:**
- `isDropZoneActive` — активна ли зона сброса

**Методы:**
- `handleDragStart(event, ticket)` — начало перетаскивания
- `handleDragOver(event)` — наведение на зону
- `handleDragLeave(event)` — выход из зоны
- `handleDrop(event, employeeId, stageId)` — сброс тикета
- `handleDragEnd(event)` — окончание перетаскивания

#### `useNotifications.js`
**Ответственность:** Показ уведомлений через Bitrix24 UI

**Методы:**
- `notify(message, type, duration)` — показ уведомления
- `success(message, duration)` — уведомление об успехе
- `error(message, duration)` — уведомление об ошибке
- `info(message, duration)` — информационное уведомление
- `warning(message, duration)` — предупреждение

#### `useLogger.js`
**Ответственность:** Композабл для логирования в компонентах Vue

**Методы:**
- `error(message, error)` — логирование ошибок
- `warn(message, data)` — логирование предупреждений
- `info(message, data)` — логирование информационных сообщений
- `debug(message, data)` — логирование отладочных сообщений

**Особенности:**
- Автоматическое определение контекста (имя компонента)
- Обёртка над `Logger` для удобного использования в компонентах

### Utils Layer

#### `logger.js`
**Ответственность:** Централизованная служба логирования

**Методы:**
- `error(message, context, error)` — логирование ошибок
- `warn(message, context, data)` — логирование предупреждений
- `info(message, context, data)` — логирование информационных сообщений
- `debug(message, context, data)` — логирование отладочных сообщений

**Особенности:**
- Поддержка уровней логирования: NONE, ERROR, WARN, INFO, DEBUG
- Форматирование сообщений с timestamp, уровнем и контекстом
- Проверка уровня перед выводом (оптимизация производительности)

#### `logger-config.js`
**Ответственность:** Конфигурация уровней логирования

**Методы:**
- `getLevel()` — получение текущего уровня
- `setLevel(level)` — установка уровня
- `isEnabled(level)` — проверка, включено ли логирование для уровня
- `reset()` — сброс к значению по умолчанию

**Особенности:**
- Управление через localStorage
- Автоматическое определение режима (development/production)
- Уровни по умолчанию: ERROR (production), DEBUG (development)

### Components

#### `LoggerControl.vue`
**Ответственность:** UI компонент для управления логированием

**Функции:**
- Переключатель включения/отключения логирования
- Выбор уровня логирования из выпадающего списка
- Отображение текущего уровня с описанием
- Кнопка сброса к значению по умолчанию
- Сворачивание/разворачивание панели управления

**Особенности:**
- Показывается только в режиме разработки (или через localStorage в production)
- Фиксированное положение (правый верхний угол)
- Адаптивный дизайн для мобильных устройств

**📖 Подробная документация:** См. [Руководство по использованию службы логирования](/DOCS/GUIDES/logging-service.md)

---

## 🔌 API Bitrix24

### Используемые методы

1. **`crm.item.list`** — получение списка элементов смарт-процесса
   - Документация: https://context7.com/bitrix24/rest/crm.item.list
   - Используется: `TicketRepository.getTicketsByStage()`

2. **`crm.item.get`** — получение элемента
   - Документация: https://context7.com/bitrix24/rest/crm.item.get
   - Используется: `TicketRepository.getTicket()`

3. **`crm.item.update`** — обновление элемента
   - Документация: https://context7.com/bitrix24/rest/crm.item.update
   - Используется: `TicketRepository.updateTicket()`, `TicketRepository.assignTicket()`

4. **`crm.item.add`** — создание элемента
   - Документация: https://context7.com/bitrix24/rest/crm.item.add
   - Используется: `TicketRepository.createTicket()`

5. **`user.get`** — получение данных пользователей
   - Документация: https://context7.com/bitrix24/rest/user.get
   - Используется: `EmployeeRepository.getEmployeesByIds()`

6. **`crm.timeline.comment.add`** — добавление комментария
   - Документация: https://context7.com/bitrix24/rest/crm.timeline.comment.add
   - Используется: `DashboardSector1CService.addComment()`

### Смарт-процесс 140 (Сектор 1С)

**Стадии:**
- `DT140_12:UC_0VHWE2` — Сформировано обращение (внутренний ID: `formed`)
- `DT140_12:PREPARATION` — Рассмотрение ТЗ (внутренний ID: `review`)
- `DT140_12:CLIENT` — Исполнение (внутренний ID: `execution`)

**Тег сектора:** `UF_CRM_7_TYPE_PRODUCT = '1C'`

---

## 💡 Примеры использования

### Использование композаблов в компоненте

```javascript
import { useDashboardState } from '@/composables/useDashboardState.js';
import { useDashboardActions } from '@/composables/useDashboardActions.js';

export default {
  setup() {
    // Состояние
    const state = useDashboardState();
    
    // Действия
    const actions = useDashboardActions(state);
    
    // Загрузка данных
    onMounted(() => {
      actions.loadSectorData();
    });
    
    return {
      isLoading: state.isLoading,
      stages: state.stages,
      loadSectorData: actions.loadSectorData,
      assignTicket: actions.assignTicket
    };
  }
};
```

### Использование Drag & Drop

```javascript
import { useDragAndDrop } from '@/composables/useDragAndDrop.js';

export default {
  setup(props, { emit }) {
    const onDrop = async (ticket, employeeId, stageId) => {
      emit('ticket-dropped', ticket, employeeId);
    };
    
    const dragAndDrop = useDragAndDrop(onDrop);
    
    return {
      isDropZoneActive: dragAndDrop.isDropZoneActive,
      handleDragOver: dragAndDrop.handleDragOver,
      handleDrop: (event) => dragAndDrop.handleDrop(event, props.employeeId, props.stageId)
    };
  }
};
```

### Использование уведомлений

```javascript
import { useNotifications } from '@/composables/useNotifications.js';

export default {
  setup() {
    const notifications = useNotifications();
    
    const handleSuccess = () => {
      notifications.success('Операция выполнена успешно');
    };
    
    const handleError = (error) => {
      notifications.error('Произошла ошибка: ' + error.message);
    };
    
    return { handleSuccess, handleError };
  }
};
```

### Прямое использование сервиса

```javascript
import { DashboardSector1CService } from '@/services/dashboard-sector-1c/index.js';

// Загрузка данных
const data = await DashboardSector1CService.getSectorData();

// Назначение тикета
await DashboardSector1CService.assignTicket(ticketId, employeeId, stageId);

// Создание тикета
const newTicketId = await DashboardSector1CService.createTicket({
  title: 'Новый тикет',
  employeeId: 123,
  stageId: 'formed'
});
```

---

## 🎯 Принципы архитектуры

### 1. Разделение ответственностей (Single Responsibility Principle)
- Каждый модуль отвечает за одну задачу
- Репозитории — только работа с API
- Мапперы — только преобразование данных
- Композаблы — только управление состоянием/действиями

### 2. Dependency Inversion Principle
- Компоненты зависят от абстракций (композаблов), а не от конкретных реализаций
- Репозитории зависят от `ApiClient`, а не от конкретного API

### 3. DRY (Don't Repeat Yourself)
- Переиспользование логики через композаблы
- Централизованные константы и утилиты

### 4. Кеширование
- Кеширование на уровне репозиториев
- Автоматическая инвалидация при обновлениях
- Разные TTL для разных типов данных

### 5. Обработка ошибок
- Централизованная обработка через `error-handler.js`
- Валидация через `validation.js`
- Уведомления через `useNotifications`

### 6. Логирование
- Централизованная служба логирования через `logger.js`
- Уровни логирования: NONE, ERROR, WARN, INFO, DEBUG
- Управление через `logger-config.js` и UI компонент `LoggerControl`
- Автоматическое определение режима (development/production)
- Сохранение настроек в localStorage
- **📖 Подробная документация:** См. [Руководство по использованию службы логирования](/DOCS/GUIDES/logging-service.md)

---

## 📊 Метрики производительности

### До рефакторинга:
- Размер сервиса: 641 строка (монолитный)
- Время загрузки: ~3-5 секунд
- Количество запросов: 3+ (по стадиям) + N (сотрудники)
- Покрытие тестами: 0%

### После рефакторинга:
- Размер модулей: < 200 строк каждый
- Время загрузки: ~1-2 секунды (с кешем)
- Количество запросов: 1-2 (оптимизировано)
- Покрытие тестами: > 70% (планируется)

---

## 🔄 Миграция со старого сервиса

Старый сервис `dashboard-sector-1c-service.js` можно удалить после проверки работы нового API.

**Проверка миграции:**
1. Убедиться, что все компоненты используют новый API
2. Протестировать все функции дашборда
3. Проверить производительность
4. Удалить старый сервис

---

## 📝 История изменений

- **2025-12-06 18:00 (UTC+3, Брест):** Создана документация архитектуры после рефакторинга
- **2025-12-06 12:00 (UTC+3, Брест):** Начало рефакторинга (план создан)
- **2025-12-06 13:12 (UTC+3, Брест):** Расширена логика нулевой точки для тикетов с ответственным 1051 "Хранитель объектов" (TASK-006)
- **2025-12-06 13:24 (UTC+3, Брест):** Добавлено исключение сотрудника с ID 1051 из списка сотрудников этапа (чтобы он не рендерился в колонках стадий)

---

**Автор:** Рефактор-менеджер  
**Версия документа:** 2.0

