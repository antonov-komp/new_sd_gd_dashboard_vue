# Архитектура системы слепков состояния сектора (Graph State Snapshots)

**Дата создания:** 2025-12-08 10:37 (UTC+3, Брест)  
**Версия:** 1.0  
**Описание:** Документация по архитектуре системы создания и хранения слепков состояния сектора 1С для визуализации графов состояния.

---

## 📋 Обзор

Система слепков состояния сектора позволяет сохранять моментальные снимки состояния дашборда сектора 1С в определённые моменты времени (начало/конец недели, вручную) для последующего анализа и визуализации изменений.

---

## 🏗️ Архитектура компонентов

### Структура сервисов

```
vue-app/src/services/graph-state/
├── SectorDataAdapter.js          # Адаптер для получения данных сектора
├── SnapshotService.js            # Сервис для работы со слепками
└── snapshot-utils.js             # Вспомогательные утилиты (опционально)
```

### Поток данных

```
DashboardSector1CService.getSectorData()
    ↓
SectorDataAdapter.getSectorDataForSnapshot()
    ↓
SnapshotService.createSnapshot()
    ↓
Laravel API (сохранение в файловую систему)
```

---

## 📊 Структура данных сектора

### Общая структура

Данные сектора, возвращаемые `DashboardSector1CService.getSectorData()`, имеют следующую структуру:

```javascript
{
  stages: Array<Stage>,           // Массив этапов с сотрудниками и тикетами
  employees: Array<Employee>,    // Список всех сотрудников
  zeroPointTickets: Object        // Тикеты без ответственного по этапам
}
```

### Структура этапа (Stage)

```javascript
{
  id: string,                     // ID этапа: 'formed' | 'review' | 'execution'
  name: string,                   // Название этапа: 'Сформировано обращение' | 'Рассмотрение ТЗ' | 'Исполнение'
  color: string,                  // Цвет этапа (hex): '#007bff' | '#ffc107' | '#28a745'
  employees: Array<Employee>      // Массив сотрудников этапа с их тикетами
}
```

**Этапы:**
- `formed` — Сформировано обращение (ID в Bitrix24: `DT140_12:UC_0VHWE2`)
- `review` — Рассмотрение ТЗ (ID в Bitrix24: `DT140_12:PREPARATION`)
- `execution` — Исполнение (ID в Bitrix24: `DT140_12:CLIENT`)

### Структура сотрудника (Employee)

```javascript
{
  id: number,                     // ID сотрудника (из Bitrix24)
  name: string,                   // ФИО сотрудника
  position: string,                // Должность
  email: string,                  // Email сотрудника
  sectorId: number | null,        // ID сектора сотрудника (366 = сектор 1С)
  departmentId: number | null,    // ID отдела (из Bitrix24)
  isFromSector1C: boolean,        // Принадлежность к сектору 1С
  tickets: Array<Ticket>,         // Все тикеты сотрудника (для обратной совместимости)
  ticketsInsideSector: Array<Ticket>,  // Тикеты внутри сектора
  ticketsOutsideSector: Array<Ticket>  // Тикеты вне сектора
}
```

**Пример:**
```javascript
{
  id: 456,
  name: 'Петр Петров',
  position: 'Разработчик',
  email: 'petrov@example.com',
  sectorId: 366,
  departmentId: 123,
  isFromSector1C: true,
  tickets: [...],
  ticketsInsideSector: [...],
  ticketsOutsideSector: [...]
}
```

### Структура тикета (Ticket)

```javascript
{
  id: number,                     // ID тикета (из Bitrix24)
  title: string,                  // Название тикета
  priority: string,                // Приоритет: 'high' | 'medium' | 'low'
  status: string,                  // Статус: 'in_progress' | 'new' | 'done' | 'pending'
  assigneeId: number | null,      // ID ответственного сотрудника
  stageId: string,                // ID этапа: 'formed' | 'review' | 'execution'
  createdAt: string,              // Дата создания (ISO 8601)
  modifiedAt: string,              // Дата последнего изменения (ISO 8601)
  amount: number,                 // Сумма (если применимо)
  currency: string,               // Валюта (по умолчанию 'RUB')
  description: string             // Описание тикета
}
```

**Пример:**
```javascript
{
  id: 789,
  title: 'Разработка нового функционала',
  priority: 'high',
  status: 'in_progress',
  assigneeId: 456,
  stageId: 'execution',
  createdAt: '2025-12-01T10:00:00+03:00',
  modifiedAt: '2025-12-05T14:30:00+03:00',
  amount: 50000,
  currency: 'RUB',
  description: 'Описание задачи...'
}
```

### Структура нулевой точки (zeroPointTickets)

Тикеты без ответственного сотрудника, сгруппированные по этапам:

```javascript
{
  formed: Array<Ticket>,          // Тикеты этапа "Сформировано обращение" без ответственного
  review: Array<Ticket>,          // Тикеты этапа "Рассмотрение ТЗ" без ответственного
  execution: Array<Ticket>        // Тикеты этапа "Исполнение" без ответственного
}
```

**Особенности:**
- Тикеты с ответственным ID 1051 (Хранитель объектов) автоматически попадают в нулевую точку
- Тикеты без ответственного (`assigneeId === null`) также попадают в нулевую точку

---

## 🔄 Преобразование данных сектора в формат слепка

### Схема преобразования

Данные сектора, полученные через `SectorDataAdapter.getSectorDataForSnapshot()`, преобразуются в формат слепка для сохранения:

```
Секторные данные (stages, employees, zeroPointTickets)
    ↓
Нормализация (извлечение статистики, ID тикетов)
    ↓
Формирование структуры слепка (metadata, statistics, ticketIds, tickets)
    ↓
Сохранение в файловую систему через Laravel API
```

### Структура слепка (Snapshot)

**Версия формата:** 1.0

Слепок состояния сектора — это моментальный "снимок" состояния всех тикетов сектора 1С на определённый момент времени. Структура спроектирована с учётом версионирования формата для обеспечения обратной совместимости.

```javascript
{
  metadata: {
    version: string,               // Версия формата: "1.0"
    createdAt: string,             // Дата и время создания (ISO 8601): "2025-12-08T08:00:00+03:00"
    createdBy: {
      id: number,                 // ID создателя: 123
      name: string                // Имя создателя: "Иван Иванов"
    },
    type: string,                 // Тип слепка: 'week_start' | 'week_end' | 'manual' | 'current'
    sectorId: string,             // ID сектора: "1C"
    sectorName: string             // Название сектора: "Сектор 1С"
  },
  statistics: {
    stages: {
      formed: {
        count: number,            // Количество тикетов: 5
        stageId: string,         // ID этапа в Bitrix24: "DT140_12:UC_0VHWE2"
        stageName: string        // Название этапа: "Сформировано обращение"
      },
      review: {
        count: number,            // Количество тикетов: 3
        stageId: string,         // ID этапа в Bitrix24: "DT140_12:PREPARATION"
        stageName: string        // Название этапа: "Рассмотрение ТЗ"
      },
      execution: {
        count: number,            // Количество тикетов: 8
        stageId: string,         // ID этапа в Bitrix24: "DT140_12:CLIENT"
        stageName: string        // Название этапа: "Исполнение"
      },
      total: number              // Общее количество тикетов: 16
    },
    employees: [
      {
        id: number,               // ID сотрудника: 456
        name: string,             // Имя сотрудника: "Петр Петров"
        ticketsByStage: {
          formed: number,         // Количество тикетов на этапе "formed": 2
          review: number,         // Количество тикетов на этапе "review": 1
          execution: number       // Количество тикетов на этапе "execution": 3
        },
        totalTickets: number      // Общее количество тикетов у сотрудника: 6
      }
    ],
    zeroPoint: {
      unassigned: number,         // Количество тикетов без ответственного: 2
      keeper: number,             // Количество тикетов с ответственным "Хранитель объектов" (ID: 1051): 1
      total: number               // Общее количество тикетов в нулевой точке: 3
    }
  },
  ticketIds: Array<number>,       // Массив ID всех тикетов сектора: [789, 790, 791]
  tickets: [                      // Массив с минимальной информацией о тикетах
    {
      id: number,                 // ID тикета: 789
      title: string,              // Название тикета: "Название тикета"
      assignedTo: {
        id: number,               // ID ответственного: 456
        name: string              // Имя ответственного: "Петр Петров"
      },
      createdAt: string,          // Дата создания (ISO 8601): "2025-12-01T10:00:00+03:00"
      updatedAt: string           // Дата последнего обновления (ISO 8601): "2025-12-05T14:30:00+03:00"
    }
  ]
}
```

**Пояснения к структуре:**
- `ticketIds` — массив ID всех тикетов сектора (для быстрого доступа и загрузки деталей)
- `tickets` — массив с минимальной информацией о тикетах (первоначальный набор полей версии 1.0)
- Детальная информация о тикетах загружается отдельно по требованию через API Bitrix24 (`crm.item.get`)
- Структура спроектирована для оптимизации размера слепка и возможности расширения в будущем

**Версионирование формата:**
- Формат версии: `MAJOR.MINOR` (например, `1.0`, `1.1`, `2.0`)
- **MAJOR** версия увеличивается при несовместимых изменениях (удаление обязательных полей, изменение типов)
- **MINOR** версия увеличивается при добавлении новых опциональных полей
- При чтении слепков старых версий использовать адаптеры/конвертеры для приведения к текущему формату

---

## 🔌 Интеграция с существующими компонентами

### SectorDataAdapter

**Расположение:** `vue-app/src/services/graph-state/SectorDataAdapter.js`

**Назначение:** Адаптер для получения данных сектора в формате, пригодном для создания слепков.

**Методы:**
- `getSectorDataForSnapshot(options)` — получение данных сектора
- `isDataAvailable()` — проверка доступности данных

**Зависимости:**
- `DashboardSector1CService` — для получения данных сектора

**Пример использования:**
```javascript
import SectorDataAdapter from '@/services/graph-state/SectorDataAdapter';

// Получение данных с кешированием
const sectorData = await SectorDataAdapter.getSectorDataForSnapshot({
  useCache: true,
  onProgress: (progressInfo) => {
    console.log('Прогресс:', progressInfo.progress, '%');
  }
});
```

### SnapshotService

**Расположение:** `vue-app/src/services/graph-state/SnapshotService.js`

**Назначение:** Сервис для создания, чтения и управления слепками состояния сектора.

**Методы:**
- `createSnapshot(sectorData, type, metadata)` — создание слепка
- `getSnapshot(date, type)` — получение слепка
- `getAllSnapshots(period)` — получение всех слепков за период
- `deleteSnapshot(date, type)` — удаление слепка

**Зависимости:**
- `SectorDataAdapter` — для получения данных сектора
- Laravel API — для сохранения/чтения слепков

---

## 📝 Кеширование и производительность

### Кеширование данных сектора

`DashboardSector1CService.getSectorData()` использует кеширование через `CacheManager`:
- TTL кеша: 5 минут (настраивается через `CacheManager.TICKETS_TTL`)
- Ключ кеша: `sector_data_1c`
- Кеш хранится в памяти браузера (localStorage или sessionStorage)

### Оптимизация загрузки

- Использование кеша для быстрого создания слепков
- Отслеживание прогресса загрузки через колбэк `onProgress`
- Пагинация при загрузке тикетов из Bitrix24 (максимум 50 элементов за запрос)

---

## 🔍 Особенности работы с данными

### Фильтрация по сектору 1С

Тикеты фильтруются по тегу сектора:
- Поле Bitrix24: `UF_CRM_7_TYPE_PRODUCT`
- Значение: `'1C'`

### Определение сектора сотрудника

Сектор сотрудника определяется по:
- Отделу сотрудника (`UF_DEPARTMENT`)
- ID сектора 1С: `366`

### Исключение тикетов "Хранитель объектов"

Тикеты с ответственным ID `1051` (Хранитель объектов):
- Исключаются из колонок сотрудников
- Автоматически попадают в нулевую точку (`zeroPointTickets`)

---

## 🔄 Нормализация данных сектора

### Процесс нормализации

Данные сектора, полученные через `DashboardSector1CService.getSectorData()`, преобразуются в формат слепка версии 1.0 через модуль `snapshot-normalizer.js`.

**Модуль нормализации:** `vue-app/src/services/graph-state/snapshot-normalizer.js`

### Шаг 1: Извлечение статистики по этапам

**Входные данные:**
```javascript
stages: [
  {
    id: 'formed',
    name: 'Сформировано обращение',
    employees: [
      {
        id: 456,
        name: 'Петр Петров',
        tickets: [
          { id: 789, title: 'Тикет 1', ... },
          { id: 790, title: 'Тикет 2', ... }
        ]
      }
    ]
  }
]
```

**Выходные данные:**
```javascript
statistics: {
  stages: {
    formed: {
      count: 2,
      stageId: 'DT140_12:UC_0VHWE2',
      stageName: 'Сформировано обращение'
    },
    review: { ... },
    execution: { ... },
    total: 16
  }
}
```

**Логика:**
- Для каждого этапа подсчитывается общее количество тикетов у всех сотрудников
- Используется маппинг внутренних ID этапов на Bitrix24 ID:
  - `formed` → `DT140_12:UC_0VHWE2`
  - `review` → `DT140_12:PREPARATION`
  - `execution` → `DT140_12:CLIENT`

### Шаг 2: Извлечение статистики по сотрудникам

**Выходные данные:**
```javascript
statistics: {
  employees: [
    {
      id: 456,
      name: 'Петр Петров',
      ticketsByStage: {
        formed: 2,
        review: 1,
        execution: 3
      },
      totalTickets: 6
    }
  ]
}
```

**Логика:**
- Собираются все уникальные сотрудники из всех этапов
- Для каждого сотрудника подсчитывается количество тикетов по каждому этапу
- Подсчитывается общее количество тикетов у сотрудника

### Шаг 3: Извлечение статистики нулевой точки

**Входные данные:**
```javascript
zeroPointTickets: {
  formed: [
    { id: 792, assignedTo: null, ... },
    { id: 793, assignedTo: { id: 1051, name: 'Хранитель объектов' }, ... }
  ]
}
```

**Выходные данные:**
```javascript
statistics: {
  zeroPoint: {
    unassigned: 1,
    keeper: 1,
    total: 2
  }
}
```

**Логика:**
- Тикеты без ответственного (`assignedTo === null`) → `unassigned`
- Тикеты с ответственным "Хранитель объектов" (ID: 1051) → `keeper`
- Общее количество: `total = unassigned + keeper`

### Шаг 4: Извлечение данных о тикетах

**Выходные данные:**
```javascript
ticketIds: [789, 792],
tickets: [
  {
    id: 789,
    title: 'Название тикета',
    assignedTo: { id: 456, name: 'Петр Петров' },
    createdAt: '2025-12-01T10:00:00+03:00',
    updatedAt: '2025-12-05T14:30:00+03:00'
  }
]
```

**Логика:**
- Собираются все тикеты из всех этапов и нулевой точки
- Для каждого тикета извлекается минимальная информация (ID, заголовок, ответственный, даты)
- Формируется массив уникальных ID тикетов
- Даты нормализуются в формат ISO 8601

### Функции нормализации

**Основная функция:**
- `normalizeSectorDataToSnapshot(sectorData)` — нормализация данных сектора в формат слепка

**Вспомогательные функции:**
- `extractStagesStatistics(stages)` — извлечение статистики по этапам
- `extractEmployeesStatistics(stages)` — извлечение статистики по сотрудникам
- `extractZeroPointStatistics(zeroPointTickets)` — извлечение статистики нулевой точки
- `extractTicketsData(stages, zeroPointTickets)` — извлечение данных о тикетах
- `normalizeDate(date)` — нормализация даты в формат ISO 8601
- `mapStageIdToBitrix(stageId)` — маппинг внутреннего ID этапа на Bitrix24 ID

### Интеграция с SectorDataAdapter

`SectorDataAdapter.getSectorDataForSnapshot()` использует нормализацию по умолчанию:

```javascript
const normalizedData = await SectorDataAdapter.getSectorDataForSnapshot({
  useCache: true,
  normalize: true  // По умолчанию true
});
```

### Интеграция с композаблами

**Композабл `useGraphState`:**
- Управляет состоянием загрузки данных сектора
- Предоставляет метод `loadSectorDataForSnapshot()` для загрузки и нормализации данных
- Предоставляет метод `createSnapshot()` для создания слепков
- Интегрирован с `useNotifications` для уведомлений об ошибках

**Пример использования:**
```javascript
import { useGraphState } from '@/composables/useGraphState';

const { 
  isLoading, 
  error, 
  currentSectorData, 
  loadSectorDataForSnapshot, 
  createSnapshot 
} = useGraphState();

// Загрузка данных
await loadSectorDataForSnapshot(true);

// Создание слепка
await createSnapshot('week_start', {
  createdBy: { id: 123, name: 'Иван Иванов' }
});
```

---

## 📁 Архитектура хранения слепков

### Место хранения

**Рекомендуемое место:** Laravel Storage  
**Путь:** `storage/app/snapshots/sector-1c/`

**Преимущества:**
- Интеграция с Laravel
- Управление через Storage facade
- Легко настроить права доступа
- Можно использовать для разных окружений

### Структура папок

**Рекомендуемая структура:** По годам и неделям

```
storage/app/snapshots/sector-1c/
├── 2025/
│   ├── week-49/
│   │   ├── week-start-2025-12-01.json
│   │   └── week-end-2025-12-07.json
│   ├── week-50/
│   │   ├── week-start-2025-12-08.json
│   │   ├── week-end-2025-12-14.json
│   │   └── manual-2025-12-10-14-30.json
│   └── week-51/
│       └── ...
├── 2026/
│   └── ...
└── current/
    └── current-state-2025-12-08-08-46.json
```

**Преимущества:**
- Логичная организация по времени
- Легко найти слепки за конкретную неделю
- Масштабируемость

### Правила именования файлов

**Формат имени файла:**
```
{type}-{date}-{time}.json
```

**Типы слепков:**
- `week-start` — начало недели (автоматическое, понедельник 00:00)
- `week-end` — конец недели (автоматическое, воскресенье 23:59)
- `manual` — ручное создание администратором
- `current` — текущее состояние (моментальный слепок)

**Примеры имён:**
```
week-start-2025-12-01.json
week-end-2025-12-07.json
manual-2025-12-10-14-30.json
current-2025-12-08-08-46.json
```

**Формат даты и времени:**
- Дата: `YYYY-MM-DD` (ISO 8601)
- Время (для manual/current): `HH-MM-SS` (24-часовой формат)
- Часовой пояс: UTC+3 (Брест)

### Определение номера недели

**Правило:** Неделя начинается в понедельник 00:00 и заканчивается в воскресенье 23:59.

**Формула:** ISO Week Number (неделя года по ISO 8601)

**Пример:**
- 2025-12-01 (понедельник) → неделя 49
- 2025-12-07 (воскресенье) → неделя 49
- 2025-12-08 (понедельник) → неделя 50

### Организация слепков

**1. Автоматические слепки:**
- **Начало недели:** Создаются в понедельник 00:00
- **Конец недели:** Создаются в воскресенье 23:59
- **Хранение:** В папке соответствующей недели
- **Имя:** `week-start-{date}.json` или `week-end-{date}.json`

**2. Ручные слепки:**
- **Создание:** По запросу администратора
- **Хранение:** В папке текущей недели
- **Имя:** `manual-{date}-{time}.json`

**3. Текущее состояние:**
- **Создание:** При открытии дашборда (опционально)
- **Хранение:** В папке `current/`
- **Имя:** `current-state-{date}-{time}.json`
- **Особенность:** Может перезаписываться (только последний)

### Права доступа и безопасность

**Права на файлы:**
- **Чтение:** Все пользователи дашборда
- **Создание:** Только администраторы
- **Удаление:** Только администраторы

**Защита:**
- Файлы не должны быть доступны через веб-сервер напрямую
- Доступ только через Laravel API
- Валидация прав доступа перед операциями

### Управление историей

**Стратегия хранения:**
- **Хранить все слепки** (для построения графиков изменений)
- **Не удалять автоматически** (только по запросу администратора)
- **Архивация:** Старые слепки (старше года) можно архивировать

**Очистка:**
- Ручная очистка администратором
- Опционально: автоматическая архивация слепков старше N месяцев

---

## 📚 Связанные документы

- **TASK-019-03-01:** Изучение и анализ существующего сервиса дашборда, создание метода-обёртки
- **TASK-019-03-02:** Нормализация данных сектора и преобразование в формат слепка
- **DOCS/ARCHITECTURE/dashboard-sector-1c.md:** Архитектура дашборда сектора 1С
- **DOCS/ARCHITECTURE/cache-mechanism-dashboard-sector-1c.md:** Механизм кеширования дашборда

---

## История правок

- **2025-12-08 10:37 (UTC+3, Брест):** Создан документ, добавлен раздел "Структура данных сектора"
- **2025-12-08 10:39 (UTC+3, Брест):** Добавлен раздел "Структура слепка (Snapshot)" с детальным описанием версии 1.0
- **2025-12-08 10:40 (UTC+3, Брест):** Добавлен раздел "Архитектура хранения слепков" с описанием структуры папок, правил именования и управления историей
- **2025-12-08 11:02 (UTC+3, Брест):** Добавлен раздел "Нормализация данных сектора" с описанием процесса нормализации и всех шагов преобразования данных

---

**Автор:** Bitrix24 Программист (Vue.js)  
**Версия документа:** 1.0

