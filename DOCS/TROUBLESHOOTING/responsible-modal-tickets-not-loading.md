# Проблема: Тикеты не загружаются в ResponsibleModal при клике на категорию

**Дата создания:** 2025-12-23 21:30 (UTC+3, Брест)  
**Проблема:** При клике на категорию в попапе "Закрытые за неделю" и затем на ответственного тикеты не появляются в списке.

---

## 🔍 Описание проблемы

**Шаги воспроизведения:**
1. Открыть модуль "График приема и закрытий сектора 1С"
2. Выбрать режим "4 недели"
3. Кликнуть на карточку "Закрытые за неделю" (текущая неделя)
4. Открывается попап "Закрытые за неделю · Неделя 52"
5. Кликнуть на категорию "Тикеты закрытые этой неделью и созданные этой неделью" (2 тикета)
6. Открывается список ответственных
7. Кликнуть на ответственного
8. **Проблема:** Тикеты не появляются в списке (карточками)

---

## 🔧 Добавленное логирование

Добавлено детальное логирование в функции:
- `handleCategoryClick()` — при клике на категорию
- `loadEmployeeTickets()` — при загрузке тикетов сотрудника

**Логи для проверки в консоли браузера:**

### 1. При клике на категорию:
```
[ResponsibleModal] handleCategoryClick called: {
  categoryId: "created-this-week",
  categoryLabel: "Тикеты закрытые этой неделью и созданные этой неделью",
  categoryCount: 2,
  responsibleCount: 1,
  responsibleWithTickets: 0,  // ⚠️ Если 0, значит тикеты не предзагружены
  responsibleSample: [...]
}
```

### 2. При клике на ответственного:
```
[ResponsibleModal] loadEmployeeTickets called: {
  employeeId: 123,
  selectedCategory: "created-this-week",
  hasSelectedCategory: true,
  hasResponsible: true,
  responsibleCount: 1,
  categoryData: {...}
}
```

### 3. Проверка тикетов в категории:
```
[ResponsibleModal] Employee found in category: {
  employee: {...},
  hasTickets: false,  // ⚠️ Если false, тикеты не предзагружены
  ticketsCount: 0
}
```

### 4. Загрузка через API (fallback):
```
[ResponsibleModal] Tickets not found in category, loading from API
[ResponsibleModal] API response: {
  hasResponsibleCreatedThisWeek: true,
  thisWeekCount: 1,
  thisWeekSample: [{
    id: 123,
    name: "Иванов Иван",
    count: 2,
    hasTickets: true,  // ✅ Тикеты должны быть здесь
    ticketsCount: 2
  }]
}
```

---

## 🐛 Возможные причины

### 1. Тикеты не предзагружены в категории

**Проблема:** `category.responsible[employee].tickets` пустой или отсутствует.

**Причина:** При первом запросе не был передан параметр `includeTickets: true` для категорий.

**Решение:** Проверить, что в `GraphAdmissionClosureDashboard.vue` при загрузке данных передаётся `includeTickets: true`.

**Проверка:**
```javascript
// В GraphAdmissionClosureDashboard.vue, функция loadData()
fetchAdmissionClosureStats({
  product: '1C',
  periodMode: 'weeks',
  weekStartUtc,
  weekEndUtc,
  includeTickets: true,  // ✅ Должно быть true
  includeNewTicketsByStages: true,
  includeCarryoverTicketsByDuration: true
})
```

### 2. Тикеты не возвращаются из API

**Проблема:** API не возвращает тикеты в `responsibleCreatedThisWeek` или `responsibleCreatedOtherWeek`.

**Причина:** Бэкенд не включает тикеты в ответ, даже если передан `includeTickets: true`.

**Решение:** Проверить логи бэкенда и убедиться, что тикеты включаются в ответ.

**Проверка в Network (DevTools):**
1. Открыть DevTools → Network
2. Найти запрос к `/api/graph-1c-admission-closure.php`
3. Проверить ответ:
   ```json
   {
     "data": {
       "responsibleCreatedThisWeek": [
         {
           "id": 123,
           "name": "Иванов Иван",
           "count": 2,
           "tickets": [  // ✅ Должен быть массив тикетов
             { "id": 1001, ... },
             { "id": 1002, ... }
           ]
         }
       ]
     }
   }
   ```

### 3. Тикеты теряются при обогащении данных

**Проблема:** Тикеты теряются в функции `enrichResponsibleWithNames()`.

**Причина:** Функция не сохраняет свойство `tickets` при обогащении данных.

**Решение:** Проверить, что в `enrichResponsibleWithNames()` сохраняются все свойства, включая `tickets`.

**Проверка (строка 499-507):**
```javascript
return responsible.map(r => {
  if (r.id && nameMap.has(r.id)) {
    return {
      ...r, // ✅ Должно сохранять все свойства, включая tickets
      name: nameMap.get(r.id)
    };
  }
  return r;
});
```

### 4. Ошибка в `prepareTicketsForDisplay()`

**Проблема:** Тикеты теряются или не обрабатываются в `prepareTicketsForDisplay()`.

**Причина:** Ошибка в функции обогащения тикетов.

**Решение:** Проверить логи ошибок в консоли:
```
[ResponsibleModal] Error preparing tickets: ...
```

---

## 🔍 Инструкции по диагностике

### Шаг 1: Проверить предзагрузку данных

1. Открыть DevTools → Console
2. Обновить страницу (F5)
3. Найти логи:
   ```
   [TASK-070] Preloaded responsibleCreatedThisWeek for current week: X employees
   ```
4. Проверить детали:
   ```
   [TASK-070] responsibleCreatedThisWeek details: [...]
   ```
5. Убедиться, что в деталях есть `tickets` для каждого сотрудника.

### Шаг 2: Проверить данные категории

1. Открыть попап "Закрытые за неделю"
2. Кликнуть на категорию
3. В консоли найти:
   ```
   [ResponsibleModal] handleCategoryClick called: {...}
   ```
4. Проверить `responsibleWithTickets` — должно быть > 0, если тикеты предзагружены.

### Шаг 3: Проверить загрузку тикетов

1. Кликнуть на ответственного
2. В консоли найти:
   ```
   [ResponsibleModal] loadEmployeeTickets called: {...}
   ```
3. Проверить:
   - `hasTickets` в "Employee found in category" — должно быть `true`
   - Если `false`, проверить "Tickets not found in category, loading from API"
   - Проверить "API response" — должно быть `hasTickets: true`

### Шаг 4: Проверить финальный результат

1. В консоли найти:
   ```
   [ResponsibleModal] Tickets after prepareTicketsForDisplay: {...}
   ```
2. Проверить `ticketsCount` — должно быть > 0.
3. Если 0, проверить ошибки:
   ```
   [ResponsibleModal] Error preparing tickets: ...
   ```

---

## ✅ Решение

### Вариант 1: Тикеты не предзагружены

**Если `responsibleWithTickets: 0` в логах:**

1. Проверить, что в `GraphAdmissionClosureDashboard.vue` передаётся `includeTickets: true`:
   ```javascript
   fetchAdmissionClosureStats({
     // ...
     includeTickets: true  // ✅ Должно быть true
   })
   ```

2. Проверить, что бэкенд возвращает тикеты в ответе API.

### Вариант 2: Тикеты не загружаются через API fallback

**Если тикеты не найдены в категории и не загружаются через API:**

1. Проверить параметры запроса в Network:
   - `weekStartUtc` и `weekEndUtc` должны быть корректными
   - `includeTickets: true` должен быть в запросе

2. Проверить ответ API:
   - `responsibleCreatedThisWeek` или `responsibleCreatedOtherWeek` должны содержать тикеты
   - Тикеты должны быть в `employee.tickets`

### Вариант 3: Ошибка в `prepareTicketsForDisplay()`

**Если есть ошибка в логах:**

1. Проверить ошибку:
   ```
   [ResponsibleModal] Error preparing tickets: ...
   ```
2. Исправить ошибку в `prepareTicketsForDisplay()` или использовать fallback (тикеты без обогащения).

---

## 📝 Следующие шаги

1. **Открыть консоль браузера** и воспроизвести проблему
2. **Скопировать логи** из консоли (все логи с префиксом `[ResponsibleModal]`)
3. **Проверить Network** — запросы к API и их ответы
4. **Определить причину** на основе логов
5. **Применить решение** в зависимости от причины

---

**Автор:** Технический писатель  
**Дата:** 2025-12-23 21:30 (UTC+3, Брест)

