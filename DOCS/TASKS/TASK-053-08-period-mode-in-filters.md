# TASK-053-08: Перенос выбора режима в фильтры с визуальным выделением

**Дата создания:** 2025-12-17 15:15 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-053: Изменение формата выбора периода и внедрение 3-месячного режима](./TASK-053-period-selection-3-months-mode.md)  
**Этап:** 3.1 (Часть 3: Рефакторинг выбора режима в фильтры)

---

## 📋 Описание

Перенести выбор режима отображения из глобального компонента в панель фильтров. Выбор режима должен находиться в секции "Период" как выпадающий список. Текущий загруженный режим должен быть визуально подсвечен и недоступен для выбора, можно выбрать только другой режим, что запустит прелоадер и перерендерит интерфейс.

---

## 🎯 Контекст

### Текущее состояние

**Текущая реализация:**
- Глобальный компонент `PeriodModeSelector` отображается отдельно от фильтров
- Выбор режима происходит через radio buttons
- Компонент находится вне панели фильтров

**Проблема:**
- Выбор режима логически относится к фильтрам, но находится отдельно
- Не соответствует общей структуре интерфейса
- Пользователю неочевидно, что это часть фильтрации данных

### Требуемое состояние

**Новая реализация:**
- Выбор режима находится в панели фильтров в секции "Период"
- Реализован как выпадающий список (`<select>`)
- Текущий режим визуально подсвечен и недоступен для выбора (disabled)
- Можно выбрать только другой режим
- При выборе другого режима запускается прелоадер и перерендеривается интерфейс

---

## 🔧 Технические требования

### Изменения в FiltersPanel.vue

**Новые props:**
```typescript
{
  showPeriodMode: boolean,  // Показывать выбор режима (только для графика приёма и закрытий)
  periodMode: 'weeks' | 'months'  // Текущий выбранный режим
}
```

**Новые emits:**
```typescript
{
  'update:periodMode': (mode: 'weeks' | 'months') => void
}
```

**Структура в секции "Период":**
- Выпадающий список "Режим отображения" (показывается только если `showPeriodMode === true`)
- Обычный выбор периода (как сейчас)

**Визуальное оформление:**
- Текущий режим: подсвечен (синий фон или жирный шрифт), disabled
- Другой режим: доступен для выбора, обычное оформление
- Hover-эффекты для доступных опций

### Удаление PeriodModeSelector

**Из GraphAdmissionClosureDashboard.vue:**
- Удалить импорт `PeriodModeSelector`
- Удалить компонент из template
- Удалить стили для `.period-selector-container`

**Из GraphAdmissionClosureMonthsDashboard.vue:**
- Убедиться, что компонент не используется

### Логика переключения режимов

**В GraphAdmissionClosureDashboard.vue:**
- Передать `showPeriodMode="true"` и `period-mode="periodMode"` в `FiltersPanel`
- Обработать событие `@update:period-mode`:
  - Сохранить режим в localStorage
  - Обновить `periodMode.value`
  - Запустить `loadData()` (покажет прелоадер и перерендерит интерфейс)

**В GraphAdmissionClosureMonthsDashboard.vue:**
- Передать `showPeriodMode="true"` и `period-mode="'months'"` в `FiltersPanel`
- Обработать событие `@update:period-mode`:
  - Если выбран 'weeks' → эмитить глобальное событие для переключения на недельный режим
  - Родительский компонент обработает событие и переключит режим

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Обновление FiltersPanel.vue

1. **Добавить новые props:**
   - `showPeriodMode: Boolean` (default: false)
   - `periodMode: String` (default: 'weeks', validator: ['weeks', 'months'])

2. **Добавить emit:**
   - `update:periodMode`

3. **Добавить выпадающий список в секцию "Период":**
   - Показывать только если `showPeriodMode === true`
   - Разместить перед обычным выбором периода
   - Опции: "4 последние недели" и "3 последних месяца"
   - Текущий режим: disabled и визуально подсвечен

4. **Добавить обработчик изменения:**
   - `handlePeriodModeChange(value)`
   - Валидация значения
   - Сохранение в localStorage
   - Эмит события `update:periodMode`

5. **Добавить стили:**
   - Стили для выпадающего списка
   - Стили для подсветки текущего режима
   - Стили для disabled состояния

### Шаг 2: Удаление PeriodModeSelector

1. **Из GraphAdmissionClosureDashboard.vue:**
   - Удалить импорт `PeriodModeSelector`
   - Удалить компонент из template
   - Удалить стили `.period-selector-container`
   - Удалить обработчик `handlePeriodModeChange` (если был отдельный)

2. **Проверить GraphAdmissionClosureMonthsDashboard.vue:**
   - Убедиться, что компонент не используется

### Шаг 3: Интеграция в дашборды

1. **В GraphAdmissionClosureDashboard.vue:**
   - Добавить `showPeriodMode="true"` в `FiltersPanel`
   - Добавить `period-mode="periodMode"` в `FiltersPanel`
   - Добавить обработчик `@update:period-mode="handlePeriodModeChange"`
   - Обновить `handlePeriodModeChange` для сохранения в localStorage и запуска загрузки

2. **В GraphAdmissionClosureMonthsDashboard.vue:**
   - Добавить `showPeriodMode="true"` в `FiltersPanel`
   - Добавить `period-mode="'months'"` в `FiltersPanel`
   - Добавить обработчик `@update:period-mode="handlePeriodModeChange"`
   - Реализовать `handlePeriodModeChange` для эмита глобального события

3. **Обработка глобального события:**
   - В `GraphAdmissionClosureDashboard.vue` добавить обработчик глобального события
   - При получении события переключить режим и перерендерить интерфейс

---

## 🔍 Детали реализации

### Визуальное выделение текущего режима

**Подход 1: CSS стили для disabled опции**
```css
.period-mode-select option:disabled {
  background-color: var(--b24-primary-light, #e7f3ff);
  color: var(--b24-primary, #007bff);
  font-weight: 600;
}
```

**Подход 2: Кастомный select с визуализацией**
- Использовать кастомный компонент вместо нативного `<select>`
- Полный контроль над визуализацией
- Более сложная реализация

**Рекомендация:** Начать с подхода 1 (CSS стили), при необходимости перейти к подходу 2.

### Логика disabled состояния

**Важно:** Нативный `<select>` с `disabled` опциями может работать по-разному в разных браузерах. Нужно протестировать:
- Chrome/Edge
- Firefox
- Safari

**Альтернатива:** Использовать кастомный компонент с полным контролем над поведением.

### Переключение между режимами

**Последовательность действий:**
1. Пользователь выбирает другой режим в списке
2. Эмитится событие `update:periodMode`
3. Обработчик в дашборде:
   - Сохраняет режим в localStorage
   - Обновляет `periodMode.value`
   - Устанавливает `isLoading = true` (показывает прелоадер)
   - Вызывает `loadData()` (загружает данные)
4. После загрузки:
   - `isLoading = false` (скрывает прелоадер)
   - Условный рендеринг показывает соответствующий дашборд

**Для месячного дашборда:**
- При выборе 'weeks' эмитится глобальное событие
- Родительский компонент (`GraphAdmissionClosureDashboard`) обрабатывает событие
- Переключает `periodMode.value = 'weeks'`
- Условный рендеринг показывает недельный дашборд

---

## 💻 Примеры кода

### FiltersPanel.vue (обновлённая секция "Период")

```vue
<template>
  <!-- Секция: Период -->
  <div class="filter-section">
    <template v-if="!weekPickerMode">
      <h3 class="section-title">
        <span class="section-icon">📅</span>
        Период
      </h3>
      <div class="section-content">
        <!-- Выбор режима отображения (только для графика приёма и закрытий) -->
        <div v-if="showPeriodMode" class="period-mode-group">
          <label class="period-mode-label">Режим отображения:</label>
          <select
            :value="periodMode"
            @change="handlePeriodModeChange($event.target.value)"
            class="period-mode-select"
          >
            <option 
              value="weeks"
              :disabled="periodMode === 'weeks'"
            >
              4 последние недели
            </option>
            <option 
              value="months"
              :disabled="periodMode === 'months'"
            >
              3 последних месяца
            </option>
          </select>
        </div>
        
        <!-- Обычный выбор периода -->
        <select
          :value="dateRange"
          @change="handleDateRangeChange($event.target.value)"
          class="date-range-select"
        >
          <option value="last-week">Последняя неделя</option>
          <option value="last-2-weeks">Последние 2 недели</option>
          <option value="last-month">Последний месяц</option>
          <option value="custom">Произвольный период</option>
        </select>
        
        <!-- Календарь для произвольного периода -->
        <!-- ... -->
      </div>
    </template>
  </div>
</template>

<script setup>
// ... существующий код ...

const props = defineProps({
  // ... существующие props ...
  showPeriodMode: {
    type: Boolean,
    default: false
  },
  periodMode: {
    type: String,
    default: 'weeks',
    validator: (value) => ['weeks', 'months'].includes(value)
  }
});

const emit = defineEmits([
  // ... существующие emits ...
  'update:periodMode'
]);

/**
 * Обработка изменения режима отображения
 */
function handlePeriodModeChange(value) {
  if (!['weeks', 'months'].includes(value)) {
    console.warn('[FiltersPanel] Invalid periodMode:', value);
    return;
  }
  
  // Не обрабатываем, если выбран текущий режим (не должно происходить, так как disabled)
  if (value === props.periodMode) {
    return;
  }
  
  // Сохранение в localStorage
  try {
    localStorage.setItem('graph-admission-closure-period-mode', value);
  } catch (error) {
    console.warn('[FiltersPanel] Failed to save periodMode to localStorage:', error);
  }
  
  emit('update:periodMode', value);
  emit('apply');
}
</script>

<style scoped>
/* ... существующие стили ... */

.period-mode-group {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  margin-bottom: var(--spacing-sm);
}

.period-mode-label {
  font-size: var(--font-size-xs);
  color: var(--b24-text-secondary);
  font-weight: 500;
}

.period-mode-select {
  width: 100%;
  padding: var(--spacing-sm);
  border: 1px solid var(--b24-border-medium);
  border-radius: var(--radius-sm);
  font-size: var(--font-size-sm);
  background-color: var(--b24-bg-white);
  cursor: pointer;
  transition: border-color var(--transition-base);
}

.period-mode-select:hover {
  border-color: var(--b24-primary);
}

.period-mode-select:focus {
  outline: 2px solid var(--b24-primary);
  outline-offset: 2px;
  border-color: var(--b24-primary);
}

/* Стили для disabled опций (подсветка текущего режима) */
.period-mode-select option:disabled {
  background-color: var(--b24-primary-light, #e7f3ff);
  color: var(--b24-primary, #007bff);
  font-weight: 600;
}

.period-mode-select option:not(:disabled) {
  background-color: var(--b24-bg-white);
  color: var(--b24-text-primary);
}
</style>
```

### GraphAdmissionClosureDashboard.vue (обновлённая интеграция)

```vue
<template>
  <div class="ac-dashboard">
    <!-- ... -->
    
    <div class="dashboard-layout">
      <div class="filters-container">
        <FiltersPanel
          :stages="filters.stages"
          :employees="filters.employees"
          :dateRange="filters.dateRange"
          :customDateRange="filters.customDateRange"
          :hasActiveFilters="hasActiveFilters"
          :hideStages="true"
          :weekPickerMode="false"
          :showPeriodMode="true"
          :period-mode="periodMode"
          @update:stages="updateStages"
          @update:employees="updateEmployees"
          @update:dateRange="updateDateRange"
          @update:customDateRange="updateCustomDateRange"
          @update:period-mode="handlePeriodModeChange"
          @reset="resetFilters"
          @apply="applyFilters"
        />
      </div>
      
      <!-- ... -->
    </div>
  </div>
</template>

<script setup>
// ... существующий код ...

/**
 * Обработка изменения режима периода
 */
function handlePeriodModeChange(mode) {
  if (!['weeks', 'months'].includes(mode)) {
    console.warn('[GraphAdmissionClosureDashboard] Invalid periodMode:', mode);
    return;
  }
  
  // Не обрабатываем, если режим не изменился
  if (mode === periodMode.value) {
    return;
  }
  
  periodMode.value = mode;
  
  // Сохранение в localStorage
  try {
    localStorage.setItem('graph-admission-closure-period-mode', mode);
  } catch (error) {
    console.warn('[GraphAdmissionClosureDashboard] Failed to save mode to localStorage:', error);
  }
  
  // Перезагрузка данных при изменении режима (покажет прелоадер)
  loadData();
}

/**
 * Обработка глобального события изменения режима периода
 */
function handleGlobalPeriodModeChange(event) {
  const { mode } = event.detail;
  if (['weeks', 'months'].includes(mode)) {
    handlePeriodModeChange(mode);
  }
}

onMounted(() => {
  // ... существующий код ...
  
  // Подписка на глобальное событие изменения режима
  window.addEventListener('period-mode-change', handleGlobalPeriodModeChange);
  
  // ... остальной код ...
});

onUnmounted(() => {
  window.removeEventListener('period-mode-change', handleGlobalPeriodModeChange);
});
</script>
```

### GraphAdmissionClosureMonthsDashboard.vue (обновлённая интеграция)

```vue
<template>
  <div class="ac-dashboard-months">
    <!-- ... -->
    
    <div class="dashboard-layout">
      <div class="filters-container">
        <FiltersPanel
          :stages="filters.stages"
          :employees="filters.employees"
          :dateRange="filters.dateRange"
          :customDateRange="filters.customDateRange"
          :hasActiveFilters="hasActiveFilters"
          :hideStages="true"
          :weekPickerMode="false"
          :showPeriodMode="true"
          :period-mode="'months'"
          @update:stages="updateStages"
          @update:employees="updateEmployees"
          @update:dateRange="updateDateRange"
          @update:customDateRange="updateCustomDateRange"
          @update:period-mode="handlePeriodModeChange"
          @reset="resetFilters"
          @apply="applyFilters"
        />
      </div>
      
      <!-- ... -->
    </div>
  </div>
</template>

<script setup>
// ... существующий код ...

/**
 * Обработка изменения режима периода
 * Эмитим событие для родительского компонента
 */
function handlePeriodModeChange(mode) {
  if (!['weeks', 'months'].includes(mode)) {
    console.warn('[GraphAdmissionClosureMonthsDashboard] Invalid periodMode:', mode);
    return;
  }
  
  // Если выбран 'weeks', нужно переключиться на недельный режим
  if (mode === 'weeks') {
    // Сохраняем режим в localStorage
    try {
      localStorage.setItem('graph-admission-closure-period-mode', mode);
    } catch (error) {
      console.warn('[GraphAdmissionClosureMonthsDashboard] Failed to save mode to localStorage:', error);
    }
    
    // Эмитим глобальное событие для родительского компонента
    window.dispatchEvent(new CustomEvent('period-mode-change', { detail: { mode } }));
  }
  // Если выбран 'months', ничего не делаем (уже в месячном режиме)
}
</script>
```

---

## ✅ Критерии приёмки

- [x] Глобальный компонент `PeriodModeSelector` удалён из интерфейса
- [x] Выбор режима находится в панели фильтров в секции "Период"
- [x] Выпадающий список реализован как `<select>` с двумя опциями
- [x] Текущий режим визуально подсвечен (синий фон, жирный шрифт)
- [x] Текущий режим недоступен для выбора (disabled)
- [x] Можно выбрать только другой режим
- [x] При выборе другого режима запускается прелоадер
- [x] После загрузки перерендеривается соответствующий интерфейс
- [x] Переключение работает в обе стороны (weeks ↔ months)
- [x] Режим НЕ сохраняется в localStorage (режим определяется только выбором из попапа и переключением через фильтры)
- [x] Визуальное оформление соответствует гайдлайнам Bitrix24
- [x] Старый выбор периода (Последняя неделя, последние 2 недели и т.д.) скрыт для модуля графика приёма и закрытий
- [x] Попап показывается всегда при входе в модуль (без проверки флага в localStorage)
- [x] Прелоадер показывается корректно перед загрузкой данных
- [x] Данные загружаются корректно после выбора режима

---

## 🔗 Зависимости

**Зависит от:**
- [TASK-053-01: Создание переключателя режимов](./TASK-053-01-period-mode-selector.md) — использует логику выбора режима
- [TASK-053-02: Создание информационного попапа](./TASK-053-02-period-mode-info-modal.md) — попап показывается при первом входе
- [TASK-053-06: Создание отдельного шаблона](./TASK-053-06-months-dashboard-template.md) — использует оба дашборда

**Зависит от этого этапа:**
- Нет (это финальный этап рефакторинга)

---

## 📝 История правок

- **2025-12-17 15:15 (UTC+3, Брест):** Создана подзадача TASK-053-08
  - Определены требования для переноса выбора режима в фильтры
  - Зафиксированы технические требования и примеры кода
  - Добавлены критерии приёмки

- **2025-12-17 15:26 (UTC+3, Брест):** Реализация завершена
  - ✅ Обновлён `FiltersPanel.vue`: добавлен выпадающий список с визуальным выделением текущего режима
  - ✅ Текущий режим отображается с синим фоном (`--b24-primary-light`), жирным шрифтом и disabled состоянием
  - ✅ Добавлена подсказка под списком с указанием текущего режима (`.period-mode-hint`)
  - ✅ Обновлена логика `handlePeriodModeChange` в `FiltersPanel.vue` для проверки изменения режима (не обрабатывать, если режим не изменился)
  - ✅ Обновлена логика `handlePeriodModeChange` в `GraphAdmissionClosureDashboard.vue` для проверки изменения режима
  - ✅ Обновлена логика `handlePeriodModeChange` в `GraphAdmissionClosureMonthsDashboard.vue` для обработки переключения на недельный режим через глобальное событие
  - ✅ Добавлены CSS стили для визуального выделения текущего режима:
    - `.period-mode-select--current-weeks` — для недельного режима
    - `.period-mode-select--current-months` — для месячного режима
  - ✅ Добавлены стили для disabled опций с подсветкой (`option:disabled`)
  - ✅ `PeriodModeSelector` уже был удалён из дашбордов ранее
  - ✅ Переключение между режимами работает корректно с показом прелоадера и перерендерингом интерфейса

- **2025-12-17 16:23 (UTC+3, Брест):** Дополнительные улучшения и исправления
  - ✅ Убрана проверка флага показа попапа из localStorage — попап показывается всегда при входе в модуль
  - ✅ Убрано сохранение режима в localStorage — режим определяется только выбором из попапа и переключением через фильтры
  - ✅ Убран старый выбор периода (Последняя неделя, последние 2 недели и т.д.) из фильтров для модуля графика приёма и закрытий
  - ✅ Исправлена логика показа попапа — `showPeriodModeInfo` установлен в `true` при инициализации компонента
  - ✅ Исправлена логика загрузки данных:
    - Добавлен обработчик `handleCloseModal` для установки `isLoading = true` сразу при закрытии попапа
    - Обновлён `handleStartLoading` для корректной загрузки данных после выбора режима
    - Добавлена проверка режима перед вызовом `loadData()`
    - Исправлена логика в `loadData()` для сброса `isLoading` при неправильном режиме
  - ✅ Добавлена автоматическая очистка старых флагов из localStorage при монтировании компонента
  - ✅ Исправлено условие рендеринга контента — добавлена проверка `!isLoading` для предотвращения показа пустого интерфейса
  - ✅ Добавлено логирование для отладки процесса загрузки данных

---

## 💡 Примечания

- **UX:** Выбор режима логически относится к фильтрам, поэтому должен быть там
- **Визуальное выделение:** Текущий режим должен быть очевидно выделен, чтобы пользователь понимал, какой режим активен
- **Disabled состояние:** Важно протестировать поведение disabled опций в разных браузерах
- **Переключение:** Переключение между режимами должно быть плавным с показом прелоадера
- **Тестирование:** Протестировать переключение между режимами, визуальное выделение, работу в разных браузерах

