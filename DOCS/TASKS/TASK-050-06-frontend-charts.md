# TASK-050-06: Frontend графики (опционально)

**Дата создания:** 2025-12-17 09:30 (UTC+3, Брест)  
**Статус:** 📋 Новая  
**Приоритет:** Низкий (опционально)  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связь с задачей:** Этап 6 из TASK-050  
**Зависимости:** TASK-050-03 (требуется завершение структуры компонентов)

---

## Цель этапа

Реализовать компонент графиков для визуализации трудозатрат. Графики опциональны и могут быть добавлены для улучшения UX.

---

## Контекст

- Модуль «Трудозатраты на Тикеты сектора 1С» (TASK-050) — 4-й модуль в дашборде сектора 1С
- Графики помогают визуализировать динамику трудозатрат
- Использовать те же библиотеки графиков, что и в других модулях (Chart.js или аналогичные)

---

## Задачи этапа

### 1. Создание компонента графиков

**Файл:** `vue-app/src/components/tickets-time-tracking/TicketsTimeTrackingChart.vue`

**Типы графиков:**
1. **Линейный график:** Динамика трудозатрат по неделям
2. **Столбчатый график:** Сравнение по сотрудникам

### 2. Линейный график

**Назначение:** Показать динамику трудозатрат по неделям

**Структура:**
```
   20 ч ┤     ●
   15 ч ┤  ●     ●
   10 ч ┤●         ●
    5 ч ┤─────────────
    0 ч └─────────────────
       48  49  50  51
        ↑   ↑   ↑   ↑
        Недели записи трудозатрат
```

**Данные:**
- Ось X: недели (48, 49, 50, 51)
- Ось Y: трудозатраты в часах
- Линия: сумма трудозатрат всего сектора за каждую неделю

**Интерактивность:**
- Клик на точку → открытие попапа детализации недели
- Tooltip при наведении (показывать неделю и сумму)

### 3. Столбчатый график

**Назначение:** Сравнение трудозатрат между сотрудниками

**Структура:**
```
   20 ч ┤     ███
   15 ч ┤  ███ ███
   10 ч ┤███ ███ ███
    5 ч ┤─────────────────
    0 ч └─────────────────
       Ив  Пет  Сид
       ↑   ↑   ↑
       Сотрудники сектора 1С
```

**Данные:**
- Ось X: сотрудники (сокращённые имена)
- Ось Y: трудозатраты в часах
- Столбцы: сумма трудозатрат каждого сотрудника за период

**Интерактивность:**
- Клик на столбец → открытие попапа детализации сотрудника
- Tooltip при наведении (показывать имя и сумму)

### 4. Интеграция с библиотекой графиков

**Требования:**
- Использовать ту же библиотеку, что и в других модулях (Chart.js или аналогичные)
- Соответствие стилям других графиков
- Адаптивность для мобильных устройств

---

## Технические требования

### Vue.js компонент

- Использовать Composition API (`<script setup>`)
- Props: `data` (данные из API), `type` (тип графика: 'line' | 'bar')
- Emits: `point-click`, `bar-click`

### Библиотека графиков

- Использовать Chart.js или ту же библиотеку, что и в других модулях
- Конфигурация графиков должна соответствовать другим модулям

### Стили

- Адаптивность для мобильных устройств
- Соответствие цветовой схеме других модулей

---

## Критерии приёмки этапа

- [ ] Создан компонент `TicketsTimeTrackingChart.vue`
- [ ] Реализован линейный график (динамика по неделям)
- [ ] Реализован столбчатый график (сравнение по сотрудникам)
- [ ] Графики отображают данные корректно
- [ ] Реализована интерактивность (клик на точку/столбец)
- [ ] Tooltip работает при наведении
- [ ] Графики адаптивны для мобильных устройств
- [ ] Стили соответствуют другим модулям

---

## Дополнительные уточнения

- Этап опционален — графики можно добавить позже или пропустить
- При реализации использовать примеры графиков из других модулей
- Обратить внимание на производительность при большом количестве данных

---

## Следующий этап

После завершения этого этапа переходить к **TASK-050-07: Frontend попап детализации**

---

## Примеры кода

### Использование Chart.js

**Пример из `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`:**

```vue
<script setup>
import { Line, Bar } from 'vue-chartjs';
import { Chart as ChartJS, CategoryScale, LinearScale, PointElement, LineElement, BarElement, Title, Tooltip, Legend } from 'chart.js';

ChartJS.register(
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  BarElement,
  Title,
  Tooltip,
  Legend
);

const chartComponent = computed(() => {
  if (chartType.value === 'line') {
    return Line;
  } else if (chartType.value === 'bar') {
    return Bar;
  }
  return Line;
});

const chartData = computed(() => {
  const weeks = props.data?.weeks || [];
  const weekLabels = weeks.map(w => `Неделя ${w.weekNumber}`);
  const weekValues = weeks.map(w => w.totalElapsedTime || 0);
  
  return {
    labels: weekLabels,
    datasets: [
      {
        label: 'Трудозатраты (часы)',
        data: weekValues,
        borderColor: '#3b82f6',
        backgroundColor: 'rgba(59, 130, 246, 0.1)',
        tension: 0.4
      }
    ]
  };
});

const chartOptions = computed(() => ({
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    legend: {
      display: true
    },
    tooltip: {
      callbacks: {
        label: (context) => {
          return `${context.dataset.label}: ${context.parsed.y.toFixed(1)} ч`;
        }
      }
    }
  },
  scales: {
    y: {
      beginAtZero: true,
      title: {
        display: true,
        text: 'Трудозатраты (часы)'
      }
    }
  },
  onClick: (event, elements) => {
    if (elements.length > 0) {
      const index = elements[0].index;
      const week = props.data?.weeks[index];
      if (week) {
        emit('point-click', week.weekNumber);
      }
    }
  }
}));
</script>
```

## Ссылки на существующие компоненты

**Изучить:**
- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue` — пример использования Chart.js
- `vue-app/src/utils/chart-config.js` — конфигурация графиков

## История правок

- **2025-12-17 09:30 (UTC+3, Брест):** Создан этап frontend графиков
- **2025-12-17 10:40 (UTC+3, Брест):** Добавлены детали:
  - Пример использования Chart.js
  - Пример конфигурации графиков
  - Обработка кликов на графики
  - Ссылки на существующие компоненты

