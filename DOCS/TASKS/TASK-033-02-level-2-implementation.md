# TASK-033-02: Этап 2 - Реализация уровня 2 (временные градации)

**Дата создания:** 2025-12-12 09:09 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-033

---

## 📋 Описание этапа

Реализация второго уровня попапа детализации по сотрудникам с отображением тикетов сотрудника, сгруппированных по временным градациям (СЕГОДНЯ, ВЧЕРА, НА ЭТОЙ НЕДЕЛЕ и т.д.).

**Триггер:** Клик на строку сотрудника в уровне 1

**Результат:** Отображение списка временных градаций с количеством тикетов в каждой категории, прогресс-барами и возможностью перехода на уровень 3.

---

## 🎯 Цель этапа

Добавить второй уровень попапа, который показывает детализацию тикетов выбранного сотрудника по 9 категориям давности, используя те же временные градации, что и в задаче TASK-030.

**Ожидаемый результат:**
- При клике на сотрудника в уровне 1 открывается уровень 2
- Отображаются все временные градации с количеством тикетов
- Каждая градация имеет прогресс-бар с цветом из конфигурации
- Возможен переход на уровень 3 при клике на градацию
- Кнопка "Назад" возвращает на уровень 1

---

## 📊 Временные градации (из TASK-030)

### Категории давности

Используются те же категории, что и в задаче TASK-030:

1. **СЕГОДНЯ** (`today`) — дата создания = текущая дата
2. **ВЧЕРА** (`yesterday`) — дата создания = вчерашняя дата
3. **НА ЭТОЙ НЕДЕЛЕ** (`this_week`) — в течение текущей недели (не сегодня, не вчера)
4. **НА ПРОШЛОЙ НЕДЕЛЕ** (`last_week`) — в течение прошлой недели
5. **БОЛЕЕ ДВУХ НЕДЕЛЬ** (`more_than_two_weeks`) — от 2 недель до 1 месяца
6. **ДО 1 МЕСЯЦА** (`up_to_one_month`) — от 1 месяца до 2 месяцев
7. **БОЛЕЕ 2 МЕСЯЦЕВ** (`more_than_two_months`) — от 2 месяцев до полугода
8. **БОЛЕЕ ПОЛУГОДА** (`more_than_half_year`) — от полугода до года
9. **БОЛЕЕ ГОДА** (`more_than_year`) — более года

### Источники данных

**Файл утилит:** `vue-app/src/services/dashboard-sector-1c/utils/date-utils.js`  
**Функция:** `getDateAccentCategory(createdDate, currentDate = new Date())`

**Файл конфигурации:** `vue-app/src/services/dashboard-sector-1c/utils/date-accent-config.js`  
**Экспорты:** `DATE_ACCENT_CATEGORIES`, `DATE_ACCENT_CONFIG`

---

## 📝 Подзадачи

### Подзадача 2.1: Расширение состояния компонента

**Цель:** Добавить состояние навигации и данные уровня 2 в компонент `EmployeeDetailsModal.vue`.

#### Задачи

1. **Изучить текущую структуру состояния компонента**
   - Открыть `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`
   - Найти текущие refs для состояния попапа
   - Определить, где хранятся данные уровня 1

2. **Добавить состояние навигации**
   - Создать ref `popupLevel` для текущего уровня (1, 2, или 3)
   - Создать ref `level1Data` для сохранения данных уровня 1
   - Создать ref `level2Data` для данных уровня 2

3. **Структура данных уровня 2**
   ```javascript
   level2Data: {
     employeeId: number,           // ID сотрудника
     employeeName: string,         // Имя сотрудника
     stageId: string,              // ID стадии ('formed', 'review', 'execution')
     stageName: string,            // Название стадии
     totalCount: number,           // Общее количество тикетов сотрудника
     dateCategories: [             // Массив категорий давности
       {
         category: string,         // Ключ категории ('today', 'yesterday', ...)
         label: string,            // Название категории ('СЕГОДНЯ', 'ВЧЕРА', ...)
         count: number,            // Количество тикетов в категории
         percentage: number,       // Процент от общего количества
         progressBarWidth: number, // Ширина прогресс-бара в процентах
         progressBarColor: string, // Цвет прогресс-бара (hex)
         tickets: Array            // Массив тикетов в категории (для уровня 3)
       }
     ]
   }
   ```

4. **Инициализация состояния**
   - Инициализировать `popupLevel` значением `1`
   - Инициализировать `level1Data` и `level2Data` как `null`

**Код реализации:**
```javascript
// В <script setup> компонента EmployeeDetailsModal.vue

import { ref } from 'vue';

// Текущий уровень попапа (1, 2, или 3)
const popupLevel = ref(1);

// Данные уровня 1 (сохраняются при переходе на уровень 2)
const level1Data = ref(null);

// Данные уровня 2
const level2Data = ref(null);
```

**Критерии завершения:**
- [ ] Состояние навигации добавлено в компонент
- [ ] Структура данных уровня 2 определена
- [ ] Инициализация состояния выполнена

---

### Подзадача 2.2: Функция группировки тикетов по временным градациям

**Цель:** Создать функцию для группировки тикетов сотрудника по 9 категориям давности.

#### Задачи

1. **Создать файл утилит (если не существует)**
   - Файл: `vue-app/src/utils/graph-state/popupNavigationUtils.js`
   - Или добавить функцию в существующий файл утилит

2. **Импортировать необходимые зависимости**
   ```javascript
   import { getDateAccentCategory } from '@/services/dashboard-sector-1c/utils/date-utils.js';
   import { 
     DATE_ACCENT_CATEGORIES, 
     DATE_ACCENT_CONFIG 
   } from '@/services/dashboard-sector-1c/utils/date-accent-config.js';
   ```

3. **Реализовать функцию группировки**
   ```javascript
   /**
    * Группировать тикеты по временным градациям
    * 
    * @param {Array} tickets - Массив тикетов сотрудника на стадии
    * @param {Date} currentDate - Текущая дата (по умолчанию new Date())
    * @returns {Array} Массив категорий с количеством тикетов
    */
   export function groupTicketsByDateCategory(tickets, currentDate = new Date()) {
     if (!tickets || tickets.length === 0) {
       return [];
     }
     
     const totalCount = tickets.length;
     
     // Инициализация категорий
     const categories = {};
     Object.values(DATE_ACCENT_CATEGORIES).forEach(categoryKey => {
       categories[categoryKey] = {
         category: categoryKey,
         label: DATE_ACCENT_CONFIG[categoryKey].label,
         count: 0,
         tickets: [],
         percentage: 0,
         progressBarWidth: 0,
         progressBarColor: DATE_ACCENT_CONFIG[categoryKey].color
       };
     });
     
     // Группировка тикетов
     tickets.forEach(ticket => {
       if (!ticket.createdAt) {
         // Если нет даты создания, относим к категории "БОЛЕЕ ГОДА"
         categories[DATE_ACCENT_CATEGORIES.MORE_THAN_YEAR].count++;
         categories[DATE_ACCENT_CATEGORIES.MORE_THAN_YEAR].tickets.push(ticket);
         return;
       }
       
       // Определение категории давности
       const category = getDateAccentCategory(ticket.createdAt, currentDate);
       
       if (categories[category]) {
         categories[category].count++;
         categories[category].tickets.push(ticket);
       }
     });
     
     // Расчет процентов и ширины прогресс-баров
     // Фильтруем только категории с тикетами
     const result = Object.values(categories)
       .filter(cat => cat.count > 0)
       .map(cat => {
         const percentage = totalCount > 0 
           ? parseFloat(((cat.count / totalCount) * 100).toFixed(1)) 
           : 0;
         
         return {
           ...cat,
           percentage,
           progressBarWidth: percentage
         };
       })
       .sort((a, b) => {
         // Сортировка по порядку категорий (от свежих к старым)
         const order = [
           DATE_ACCENT_CATEGORIES.TODAY,
           DATE_ACCENT_CATEGORIES.YESTERDAY,
           DATE_ACCENT_CATEGORIES.THIS_WEEK,
           DATE_ACCENT_CATEGORIES.LAST_WEEK,
           DATE_ACCENT_CATEGORIES.MORE_THAN_TWO_WEEKS,
           DATE_ACCENT_CATEGORIES.UP_TO_ONE_MONTH,
           DATE_ACCENT_CATEGORIES.MORE_THAN_TWO_MONTHS,
           DATE_ACCENT_CATEGORIES.MORE_THAN_HALF_YEAR,
           DATE_ACCENT_CATEGORIES.MORE_THAN_YEAR
         ];
         
         return order.indexOf(a.category) - order.indexOf(b.category);
       });
     
     return result;
   }
   ```

4. **Добавить обработку граничных случаев**
   - Тикеты без даты создания → категория "БОЛЕЕ ГОДА"
   - Пустой массив тикетов → возврат пустого массива
   - Некорректные даты → обработка через `getDateAccentCategory()`

5. **Экспортировать функцию**
   - Убедиться, что функция экспортирована
   - Добавить JSDoc комментарии

**Критерии завершения:**
- [ ] Функция `groupTicketsByDateCategory()` создана
- [ ] Группировка работает для всех 9 категорий
- [ ] Проценты и ширина прогресс-баров рассчитываются корректно
- [ ] Сортировка по порядку категорий реализована
- [ ] Граничные случаи обработаны
- [ ] JSDoc комментарии добавлены

---

### Подзадача 2.3: Обновление шаблона компонента (уровень 2)

**Цель:** Добавить отображение уровня 2 в шаблон компонента `EmployeeDetailsModal.vue`.

#### Задачи

1. **Изучить текущий шаблон уровня 1**
   - Найти секцию с отображением списка сотрудников
   - Понять структуру разметки

2. **Добавить условное отображение уровней**
   ```vue
   <template>
     <Teleport to="body">
       <div v-if="isVisible" class="employee-details-modal" ...>
         <div class="modal-content">
           <!-- Уровень 1: Список сотрудников -->
           <div v-if="popupLevel === 1" class="level-1">
             <!-- Текущий контент уровня 1 -->
           </div>
           
           <!-- Уровень 2: Временные градации -->
           <div v-if="popupLevel === 2" class="level-2">
             <!-- Новый контент уровня 2 -->
           </div>
           
           <!-- Уровень 3: Заказчики (будет в следующем этапе) -->
           <div v-if="popupLevel === 3" class="level-3">
             <!-- Контент уровня 3 -->
           </div>
         </div>
       </div>
     </Teleport>
   </template>
   ```

3. **Создать шаблон уровня 2**
   ```vue
   <!-- Уровень 2: Временные градации -->
   <div v-if="popupLevel === 2 && level2Data" class="level-2">
     <div class="modal-header">
       <button class="btn-back" @click="goBack" aria-label="Назад">
         ←
       </button>
       <h3>{{ level2Data.employeeName }}</h3>
       <span class="modal-total">Всего: {{ level2Data.totalCount }} тикетов</span>
       <button class="modal-close" @click="close" aria-label="Закрыть">×</button>
     </div>
     
     <div class="modal-body">
       <div class="stage-info">
         <span class="stage-badge">{{ level2Data.stageName }}</span>
       </div>
       
       <div v-if="level2Data.dateCategories.length === 0" class="no-data">
         Нет данных о тикетах
       </div>
       
       <div v-else class="date-categories-list">
         <div
           v-for="category in level2Data.dateCategories"
           :key="category.category"
           class="category-item"
           @click="goToLevel3(category)"
         >
           <span class="category-label">{{ category.label }}</span>
           <div class="category-progress">
             <div
               class="progress-bar"
               :style="{
                 width: category.progressBarWidth + '%',
                 backgroundColor: category.progressBarColor
               }"
             ></div>
           </div>
           <span class="category-count">
             {{ category.count }} тикетов ({{ category.percentage }}%)
           </span>
         </div>
       </div>
     </div>
   </div>
   ```

4. **Добавить кнопку "Назад"**
   - Кнопка должна быть видна только на уровнях 2 и 3
   - При клике вызывать функцию `goBack()`

**Критерии завершения:**
- [ ] Условное отображение уровней реализовано
- [ ] Шаблон уровня 2 создан
- [ ] Заголовок с именем сотрудника отображается
- [ ] Список временных градаций отображается
- [ ] Прогресс-бары применяются с правильными цветами
- [ ] Кнопка "Назад" работает

---

### Подзадача 2.4: Обработка клика на сотрудника (уровень 1 → 2)

**Цель:** Реализовать переход на уровень 2 при клике на строку сотрудника в уровне 1.

#### Задачи

1. **Изучить текущую структуру уровня 1**
   - Найти элементы списка сотрудников
   - Определить, где добавить обработчик клика

2. **Добавить обработчик клика на строку сотрудника**
   ```vue
   <!-- В уровне 1, в списке сотрудников -->
   <div
     v-for="employee in employees"
     :key="employee.id"
     class="employee-item"
     @click="handleEmployeeClick(employee)"
   >
     <!-- Текущий контент строки сотрудника -->
   </div>
   ```

3. **Создать функцию обработки клика**
   ```javascript
   /**
    * Обработка клика на сотрудника (переход на уровень 2)
    * 
    * @param {Object} employee - Объект сотрудника из уровня 1
    */
   async function handleEmployeeClick(employee) {
     // Сохранить данные уровня 1
     level1Data.value = {
       stageName: props.stageName,
       totalCount: props.totalCount,
       employees: props.employees,
       others: props.others
     };
     
     // Получить ID стадии из props (нужно будет передать из GraphStateChart)
     const stageId = props.stageId; // Добавить в props
     
     // Получить тикеты сотрудника на стадии
     const tickets = await getEmployeeTicketsForStage(
       employee.id,
       stageId,
       props.snapshot, // Передать слепок через props
       props.ticketDetails // Передать детали тикетов через props
     );
     
     // Группировать по временным градациям
     const dateCategories = groupTicketsByDateCategory(tickets);
     
     // Сохранить данные уровня 2
     level2Data.value = {
       employeeId: employee.id,
       employeeName: employee.name,
       stageId: stageId,
       stageName: props.stageName,
       totalCount: tickets.length,
       dateCategories: dateCategories
     };
     
     // Перейти на уровень 2
     popupLevel.value = 2;
   }
   ```

4. **Добавить необходимые props**
   - `stageId` — ID стадии (для фильтрации тикетов)
   - `snapshot` — слепок с данными (для получения тикетов)
   - `ticketDetails` — детали тикетов (если загружены через API)

5. **Импортировать необходимые функции**
   ```javascript
   import { groupTicketsByDateCategory } from '@/utils/graph-state/popupNavigationUtils.js';
   import { getEmployeeTicketsForStage } from '@/utils/graph-state/popupNavigationUtils.js';
   ```

6. **Добавить функцию получения тикетов сотрудника**
   - Создать функцию `getEmployeeTicketsForStage()` в утилитах
   - Фильтровать тикеты по `employeeId` и `stageId`
   - Объединять данные из слепка и загруженных деталей

**Критерии завершения:**
- [ ] Обработчик клика добавлен на строки сотрудников
- [ ] Функция `handleEmployeeClick()` реализована
- [ ] Данные уровня 1 сохраняются при переходе
- [ ] Тикеты сотрудника получаются корректно
- [ ] Группировка по градациям работает
- [ ] Переход на уровень 2 выполняется

---

### Подзадача 2.5: Стилизация уровня 2

**Цель:** Добавить стили для уровня 2, соответствующие дизайну уровня 1 и гайдлайнам Bitrix24.

#### Задачи

1. **Изучить текущие стили уровня 1**
   - Найти стили для `.employee-item`
   - Понять цветовую схему и отступы

2. **Добавить стили для уровня 2**
   ```css
   /* Уровень 2: Временные градации */
   .level-2 {
     /* Наследуем базовые стили модального окна */
   }
   
   .btn-back {
     width: 32px;
     height: 32px;
     border: none;
     background: transparent;
     font-size: 20px;
     line-height: 1;
     color: var(--b24-text-secondary, #6b7280);
     cursor: pointer;
     border-radius: var(--radius-sm, 4px);
     display: flex;
     align-items: center;
     justify-content: center;
     transition: all 0.2s ease;
     margin-right: 8px;
   }
   
   .btn-back:hover {
     background-color: var(--b24-bg-light, #f3f4f6);
     color: var(--b24-text-primary, #1f2937);
   }
   
   .stage-info {
     margin-bottom: 16px;
     padding-bottom: 12px;
     border-bottom: 1px solid var(--b24-border-light, #e5e7eb);
   }
   
   .stage-badge {
     display: inline-block;
     padding: 4px 12px;
     background-color: var(--b24-primary, #007bff);
     color: white;
     border-radius: var(--radius-xl, 9999px);
     font-size: 12px;
     font-weight: 600;
   }
   
   .date-categories-list {
     display: flex;
     flex-direction: column;
     gap: 12px;
   }
   
   .category-item {
     display: flex;
     align-items: center;
     gap: 12px;
     padding: 12px;
     background-color: var(--b24-bg-light, #f3f4f6);
     border-radius: var(--radius-md, 6px);
     border-left: 3px solid transparent;
     transition: all 0.2s ease;
     cursor: pointer;
   }
   
   .category-item:hover {
     background-color: var(--b24-bg, #f9fafb);
     border-left-color: var(--b24-primary, #007bff);
   }
   
   .category-label {
     min-width: 150px;
     font-size: 14px;
     font-weight: 600;
     color: var(--b24-text-primary, #1f2937);
   }
   
   .category-progress {
     flex: 1;
     height: 20px;
     background-color: var(--b24-bg-white, #ffffff);
     border-radius: var(--radius-sm, 4px);
     overflow: hidden;
     margin: 0 12px;
     border: 1px solid var(--b24-border-light, #e5e7eb);
   }
   
   .category-count {
     min-width: 120px;
     font-size: 14px;
     font-weight: 600;
     color: var(--b24-text-secondary, #6b7280);
     text-align: right;
   }
   ```

3. **Адаптивность для мобильных устройств**
   ```css
   @media (max-width: 768px) {
     .category-item {
       flex-direction: column;
       align-items: flex-start;
       gap: 8px;
     }
     
     .category-label {
       min-width: auto;
       width: 100%;
     }
     
     .category-progress {
       width: 100%;
       margin: 0;
     }
     
     .category-count {
       min-width: auto;
       text-align: left;
       width: 100%;
     }
   }
   ```

4. **Применить цвета из конфигурации**
   - Цвета прогресс-баров берутся из `DATE_ACCENT_CONFIG`
   - Уже применяются в функции `groupTicketsByDateCategory()`

**Критерии завершения:**
- [ ] Стили для уровня 2 добавлены
- [ ] Кнопка "Назад" стилизована
- [ ] Список категорий отображается корректно
- [ ] Прогресс-бары имеют правильные цвета
- [ ] Адаптивность для мобильных реализована
- [ ] Стили соответствуют гайдлайнам Bitrix24

---

## 🔗 Интеграция с компонентом GraphStateChart

### Обновление передачи данных в попап

**Файл:** `vue-app/src/components/graph-state/GraphStateChart.vue`

**Изменения:**
1. Передать `stageId` в попап через props
2. Передать `snapshot` в попап (для получения тикетов)
3. Передать `ticketDetails` (если загружены через API)

**Код:**
```vue
<!-- В GraphStateChart.vue -->
<EmployeeDetailsModal
  :is-visible="showEmployeeModal"
  :stage-name="modalStageName"
  :stage-id="modalStageId"  <!-- Добавить -->
  :total-count="modalTotalCount"
  :employees="modalEmployees"
  :others="modalOthers"
  :snapshot="currentSnapshot"  <!-- Добавить -->
  :ticket-details="ticketDetails"  <!-- Добавить (если есть) -->
  @close="closeEmployeeModal"
/>
```

---

## 📚 Связанные файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа
- `vue-app/src/components/graph-state/GraphStateChart.vue` — компонент графика
- `vue-app/src/utils/graph-state/popupNavigationUtils.js` — утилиты навигации (новый файл)
- `vue-app/src/services/dashboard-sector-1c/utils/date-utils.js` — утилиты для работы с датами
- `vue-app/src/services/dashboard-sector-1c/utils/date-accent-config.js` — конфигурация градаций
- `DOCS/TASKS/TASK-030-ticket-card-department-and-date.md` — задача с временными градациями

---

## ✅ Критерии приёмки этапа 2

- [ ] Уровень 2 отображается корректно при клике на сотрудника
- [ ] Группировка по временным градациям работает для всех 9 категорий
- [ ] Прогресс-бары отображаются с правильными цветами из конфигурации
- [ ] Проценты рассчитываются корректно
- [ ] Навигация между уровнями 1 и 2 работает (вперед и назад)
- [ ] Кнопка "Назад" возвращает на уровень 1
- [ ] Стили применены корректно
- [ ] Адаптивность для мобильных устройств реализована
- [ ] Обработка граничных случаев (нет тикетов, нет данных) работает

---

## 🔄 Зависимости

**Зависит от:**
- Этап 1 (TASK-033-01): Подготовка данных и загрузка деталей тикетов
  - Необходима функция `getEmployeeTicketsForStage()` для получения тикетов

**Требует для следующего этапа:**
- Этап 3 (TASK-033-03): Реализация уровня 3 (детализация по заказчикам)
  - Данные уровня 2 должны содержать массив `tickets` в каждой категории

---

## 📝 История правок

- **2025-12-12 09:09 (UTC+3, Брест):** Создан документ этапа 2
  - Добавлены 5 подзадач с детальной реализацией
  - Описаны временные градации и источники данных
  - Добавлены примеры кода и стилей

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Дата:** 2025-12-12 09:09 (UTC+3, Брест)




