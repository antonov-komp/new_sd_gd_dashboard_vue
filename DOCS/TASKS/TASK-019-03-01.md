# TASK-019-03-01: Изучение и анализ существующего сервиса дашборда, создание метода-обёртки для получения данных сектора

**Дата создания:** 2025-12-08 10:00 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 3, часть 1)

---

## 📋 Цель задачи

Изучить существующий сервис `DashboardSector1CService` и композаблы `useDashboardState` и `useDashboardActions`, создать метод-обёртку для получения данных сектора в формате, пригодном для создания слепков. Обеспечить совместимость с существующим кешированием и системой прогресса загрузки.

---

## 🎯 Контекст

Для создания слепков состояния сектора необходимо использовать существующий сервис `DashboardSector1CService.getSectorData()`, который уже реализует:
- Загрузку тикетов из Bitrix24 с пагинацией
- Фильтрацию по сектору 1С
- Получение данных сотрудников
- Группировку тикетов по этапам и сотрудникам
- Кеширование данных
- Систему отслеживания прогресса загрузки

**Задача:** Создать метод-обёртку, который будет использовать существующий сервис и преобразовывать данные в формат, необходимый для создания слепков, сохраняя совместимость с кешированием и системой прогресса.

**Зависимости:**
- Этап 1 (TASK-019-01-01, TASK-019-01-02) должен быть завершён — структура данных слепка определена
- Этап 2 (TASK-019-02-01, TASK-019-02-02) должен быть завершён — `SnapshotService` создан
- Существующий сервис `DashboardSector1CService` работает корректно

---

## 🔍 Анализ существующего сервиса

### Структура `DashboardSector1CService.getSectorData()`

**Расположение:** `vue-app/src/services/dashboard-sector-1c/index.js`

**Сигнатура метода:**
```javascript
static async getSectorData(useCache = true, onProgress = null)
```

**Параметры:**
- `useCache` (boolean, по умолчанию `true`) — использовать кеш данных сектора
- `onProgress` (Function | null) — колбэк для отслеживания прогресса загрузки

**Возвращает:** `Promise<Object>` — объект с данными сектора:
```javascript
{
  stages: [
    {
      id: 'formed', // или 'review', 'execution'
      name: 'Сформировано обращение',
      color: '#007bff',
      employees: [
        {
          id: 456,
          name: 'Петр Петров',
          tickets: [
            {
              id: 789,
              title: 'Название тикета',
              assignedTo: { id: 456, name: 'Петр Петров' },
              createdAt: '2025-12-01T10:00:00+03:00',
              updatedAt: '2025-12-05T14:30:00+03:00',
              stageId: 'DT140_12:UC_0VHWE2', // Bitrix24 stage ID
              // ... другие поля тикета
            }
          ]
        }
      ]
    }
  ],
  employees: [
    {
      id: 456,
      name: 'Петр Петров',
      department: 'Отдел разработки',
      // ... другие поля сотрудника
    }
  ],
  zeroPointTickets: {
    formed: [
      {
        id: 790,
        title: 'Тикет без ответственного',
        // ... поля тикета
      }
    ],
    review: [],
    execution: []
  }
}
```

**Особенности:**
- Использует кеширование через `CacheManager`
- Поддерживает отслеживание прогресса через колбэк `onProgress`
- Фильтрует тикеты по тегу сектора 1С (`UF_CRM_7_TYPE_PRODUCT = '1C'`)
- Загружает только тикеты из трёх целевых этапов:
  - `DT140_12:UC_0VHWE2` — Сформировано обращение (formed)
  - `DT140_12:PREPARATION` — Рассмотрение ТЗ (review)
  - `DT140_12:CLIENT` — Исполнение (execution)

---

## 🔍 Анализ композаблов

### `useDashboardState`

**Расположение:** `vue-app/src/composables/useDashboardState.js`

**Назначение:** Управление реактивным состоянием дашборда сектора 1С.

**Структура состояния:**
```javascript
{
  isLoading: ref(false),
  error: ref(null),
  stages: ref([...]), // Массив этапов с сотрудниками и тикетами
  employees: ref([]), // Список всех сотрудников
  zeroPointTickets: ref({...}), // Тикеты без ответственного
  draggedTicket: ref(null) // Текущий перетаскиваемый тикет
}
```

**Методы:**
- `updateState(data)` — обновление состояния данными сектора
- `resetState()` — сброс состояния

### `useDashboardActions`

**Расположение:** `vue-app/src/composables/useDashboardActions.js`

**Назначение:** Управление действиями дашборда (загрузка данных, назначение тикетов, создание тикетов).

**Методы:**
- `loadSectorData(useCache = true)` — загрузка данных сектора через `DashboardSector1CService.getSectorData()`
- `assignTicket(ticketId, employeeId, stageId)` — назначение тикета сотруднику
- `createTicket(ticketData)` — создание нового тикета
- `loadingProgress` — объект для отслеживания прогресса загрузки

**Особенности:**
- Использует `useNotifications` для уведомлений об ошибках
- Использует `useLoadingProgress` для отслеживания прогресса
- Обрабатывает ошибки через `handleErrorWithNotification`

---

## 📝 Что нужно сделать

### 1. Изучить структуру данных `DashboardSector1CService.getSectorData()`

**Действия:**
- Открыть файл `vue-app/src/services/dashboard-sector-1c/index.js`
- Изучить метод `getSectorData()` и его внутреннюю логику
- Понять структуру возвращаемых данных (stages, employees, zeroPointTickets)
- Изучить структуру тикетов (поля, типы данных)
- Изучить структуру сотрудников (поля, типы данных)
- Изучить структуру этапов (id, name, color, employees)

**Результат:** Понимание структуры данных, возвращаемых `getSectorData()`

---

### 2. Изучить композаблы `useDashboardState` и `useDashboardActions`

**Действия:**
- Открыть файл `vue-app/src/composables/useDashboardState.js`
- Изучить структуру состояния и методы обновления
- Открыть файл `vue-app/src/composables/useDashboardActions.js`
- Изучить метод `loadSectorData()` и его интеграцию с `DashboardSector1CService`
- Понять, как используется кеширование и система прогресса
- Изучить обработку ошибок

**Результат:** Понимание работы композаблов и их интеграции с сервисом

---

### 3. Создать метод-обёртку `getSectorDataForSnapshot()`

**Расположение:** `vue-app/src/services/graph-state/SectorDataAdapter.js` (новый файл)

**Назначение:** Адаптер для получения данных сектора в формате, пригодном для создания слепков.

**Сигнатура:**
```javascript
static async getSectorDataForSnapshot(options = {})
```

**Параметры:**
- `options` (Object, опционально) — опции получения данных
  - `useCache` (boolean, по умолчанию `true`) — использовать кеш
  - `onProgress` (Function | null) — колбэк для отслеживания прогресса
  - `includeTicketDetails` (boolean, по умолчанию `false`) — включать детальную информацию о тикетах

**Возвращает:** `Promise<Object>` — данные сектора в формате, совместимом с `SnapshotService.createSnapshot()`

**Внутренняя логика:**
1. Вызвать `DashboardSector1CService.getSectorData(useCache, onProgress)`
2. Получить данные сектора
3. Вернуть данные в том же формате (без преобразования на этом этапе)
4. Обеспечить совместимость с кешированием (передать параметр `useCache`)
5. Обеспечить совместимость с системой прогресса (передать колбэк `onProgress`)

**Пример использования:**
```javascript
import { SectorDataAdapter } from '@/services/graph-state/SectorDataAdapter';

// Получение данных для создания слепка
const sectorData = await SectorDataAdapter.getSectorDataForSnapshot({
  useCache: true,
  onProgress: (progressInfo) => {
    console.log('Прогресс загрузки:', progressInfo);
  }
});

// Использование данных для создания слепка
const snapshot = await SnapshotService.createSnapshot(
  sectorData,
  'week_start',
  {
    createdBy: { id: 123, name: 'Иван Иванов' }
  }
);
```

---

### 4. Создать документацию по структуре данных

**Расположение:** `DOCS/ARCHITECTURE/graph-state-snapshots.md` (раздел "Структура данных сектора")

**Содержание:**
- Описание структуры данных, возвращаемых `DashboardSector1CService.getSectorData()`
- Описание структуры этапов (id, name, color, employees)
- Описание структуры сотрудников (id, name, department, tickets)
- Описание структуры тикетов (id, title, assignedTo, createdAt, updatedAt, stageId)
- Описание структуры нулевой точки (zeroPointTickets)
- Схема преобразования данных сектора в формат слепка (для следующей задачи)

---

## 🏗️ Архитектура решения

### Структура файлов

```
vue-app/src/services/graph-state/
├── SectorDataAdapter.js          # Новый адаптер для получения данных сектора
└── SnapshotService.js            # Существующий сервис (из этапа 2)
```

### Класс SectorDataAdapter

**Паттерн:** Класс со статическими методами (аналогично `SnapshotService`)

**Методы:**
- `getSectorDataForSnapshot(options)` — основной метод для получения данных сектора

**Зависимости:**
- `DashboardSector1CService` — для получения данных сектора
- `SnapshotService` — для создания слепков (используется в следующей задаче)

---

## 💡 Примеры кода

### Базовая структура класса SectorDataAdapter

```javascript
// vue-app/src/services/graph-state/SectorDataAdapter.js

import { DashboardSector1CService } from '@/services/dashboard-sector-1c/index.js';

/**
 * Адаптер для получения данных сектора в формате, пригодном для создания слепков
 * 
 * Использует существующий сервис DashboardSector1CService для получения данных
 * и обеспечивает совместимость с кешированием и системой прогресса
 * 
 * @class SectorDataAdapter
 */
class SectorDataAdapter {
  /**
   * Получить данные сектора для создания слепка
   * 
   * Использует существующий сервис DashboardSector1CService.getSectorData()
   * и возвращает данные в формате, совместимом с SnapshotService.createSnapshot()
   * 
   * @param {Object} options - Опции получения данных
   * @param {boolean} options.useCache - Использовать кеш (по умолчанию true)
   * @param {Function|null} options.onProgress - Колбэк для отслеживания прогресса
   * @param {boolean} options.includeTicketDetails - Включать детальную информацию о тикетах (по умолчанию false)
   * @returns {Promise<Object>} Данные сектора в формате для создания слепка
   * 
   * @example
   * // Получение данных с кешированием
   * const sectorData = await SectorDataAdapter.getSectorDataForSnapshot({
   *   useCache: true
   * });
   * 
   * @example
   * // Получение данных с отслеживанием прогресса
   * const sectorData = await SectorDataAdapter.getSectorDataForSnapshot({
   *   useCache: true,
   *   onProgress: (progressInfo) => {
   *     console.log('Прогресс:', progressInfo.progress, '%');
   *   }
   * });
   */
  static async getSectorDataForSnapshot(options = {}) {
    const {
      useCache = true,
      onProgress = null,
      includeTicketDetails = false
    } = options;

    try {
      // Вызываем существующий сервис для получения данных сектора
      const sectorData = await DashboardSector1CService.getSectorData(
        useCache,
        onProgress
      );

      // На данном этапе просто возвращаем данные без преобразования
      // Преобразование будет реализовано в следующей задаче (TASK-019-03-02)
      
      // Если требуется детальная информация о тикетах, можно добавить дополнительную обработку
      if (includeTicketDetails) {
        // TODO: Реализовать загрузку детальной информации о тикетах (в будущем)
        console.warn('includeTicketDetails пока не реализовано');
      }

      return sectorData;
    } catch (error) {
      console.error('SectorDataAdapter.getSectorDataForSnapshot error:', error);
      throw error;
    }
  }

  /**
   * Проверить доступность данных сектора
   * 
   * Проверяет, доступен ли сервис DashboardSector1CService и может ли он вернуть данные
   * 
   * @returns {Promise<boolean>} true, если данные доступны
   */
  static async isDataAvailable() {
    try {
      // Пробуем получить данные с кешем (быстрая проверка)
      const data = await DashboardSector1CService.getSectorData(true);
      return data !== null && data !== undefined;
    } catch (error) {
      return false;
    }
  }
}

export default SectorDataAdapter;
```

---

## 🔌 Интеграция с существующими компонентами

### Использование в компонентах

**Пример использования в компоненте создания слепка:**
```javascript
// vue-app/src/components/graph-state/CreateSnapshotButton.vue

import { SectorDataAdapter } from '@/services/graph-state/SectorDataAdapter';
import { SnapshotService } from '@/services/graph-state/SnapshotService';

export default {
  methods: {
    async handleCreateSnapshot() {
      try {
        // Получаем данные сектора через адаптер
        const sectorData = await SectorDataAdapter.getSectorDataForSnapshot({
          useCache: true,
          onProgress: (progressInfo) => {
            // Обновляем прогресс в UI
            this.loadingProgress = progressInfo.progress;
          }
        });

        // Создаём слепок
        const snapshot = await SnapshotService.createSnapshot(
          sectorData,
          'manual',
          {
            createdBy: {
              id: this.currentUser.id,
              name: this.currentUser.name
            }
          }
        );

        // Уведомление об успехе
        this.$notify.success('Слепок успешно создан');
      } catch (error) {
        console.error('Ошибка создания слепка:', error);
        this.$notify.error('Ошибка создания слепка: ' + error.message);
      }
    }
  }
};
```

---

## ✅ Критерии приёмки

- [ ] Файл `vue-app/src/services/graph-state/SectorDataAdapter.js` создан
- [ ] Метод `getSectorDataForSnapshot()` реализован и работает корректно
- [ ] Метод использует `DashboardSector1CService.getSectorData()` для получения данных
- [ ] Параметр `useCache` корректно передаётся в существующий сервис
- [ ] Колбэк `onProgress` корректно передаётся в существующий сервис
- [ ] Метод `isDataAvailable()` реализован для проверки доступности данных
- [ ] Обработка ошибок реализована (try/catch, логирование)
- [ ] Код соответствует стандартам проекта (ESLint, форматирование)
- [ ] JSDoc-комментарии добавлены к методам
- [ ] Документация создана в `DOCS/ARCHITECTURE/graph-state-snapshots.md` (раздел "Структура данных сектора")
- [ ] Примеры использования добавлены в код
- [ ] Метод протестирован вручную (через консоль браузера или временный компонент)

---

## 📝 Действия

1. **Изучить существующий сервис:**
   - Открыть `vue-app/src/services/dashboard-sector-1c/index.js`
   - Изучить метод `getSectorData()` и его структуру данных
   - Понять логику работы с кешированием и системой прогресса

2. **Изучить композаблы:**
   - Открыть `vue-app/src/composables/useDashboardState.js`
   - Открыть `vue-app/src/composables/useDashboardActions.js`
   - Изучить метод `loadSectorData()` и его интеграцию с сервисом

3. **Создать структуру папок:**
   - Убедиться, что папка `vue-app/src/services/graph-state/` существует

4. **Создать файл `SectorDataAdapter.js`:**
   - Создать класс `SectorDataAdapter` со статическими методами
   - Реализовать метод `getSectorDataForSnapshot()`
   - Реализовать метод `isDataAvailable()`
   - Добавить обработку ошибок
   - Добавить JSDoc-комментарии

5. **Протестировать метод:**
   - Создать временный компонент или использовать консоль браузера
   - Проверить получение данных с кешированием
   - Проверить получение данных без кеширования
   - Проверить работу колбэка прогресса
   - Проверить обработку ошибок

6. **Создать документацию:**
   - Открыть или создать `DOCS/ARCHITECTURE/graph-state-snapshots.md`
   - Добавить раздел "Структура данных сектора"
   - Описать структуру данных, возвращаемых `DashboardSector1CService.getSectorData()`
   - Описать структуру этапов, сотрудников, тикетов
   - Добавить примеры данных

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущие задачи:**
  - `DOCS/TASKS/TASK-019-01-01.md` (структура данных слепка)
  - `DOCS/TASKS/TASK-019-01-02.md` (архитектура хранения)
  - `DOCS/TASKS/TASK-019-02-01.md` (SnapshotService)
  - `DOCS/TASKS/TASK-019-02-02.md` (расширение SnapshotService)
- **Следующая задача:** `DOCS/TASKS/TASK-019-03-02.md` (нормализация данных и преобразование в формат слепка)
- **Существующий сервис:** `vue-app/src/services/dashboard-sector-1c/index.js`
- **Композаблы:**
  - `vue-app/src/composables/useDashboardState.js`
  - `vue-app/src/composables/useDashboardActions.js`
- **Архитектурный документ:** `DOCS/ARCHITECTURE/graph-state-snapshots.md` (будет создан или обновлён)

---

## ⏱️ Оценка времени

**1-2 часа**

**Разбивка:**
- Изучение существующего сервиса и композаблов: 30-45 минут
- Создание класса `SectorDataAdapter`: 20-30 минут
- Тестирование и отладка: 15-20 минут
- Создание документации: 15-20 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-03-02: Нормализация данных сектора и преобразование в формат слепка, интеграция с композаблами**.

---

## 💡 Дополнительные замечания

### Совместимость с кешированием

Метод `getSectorDataForSnapshot()` должен полностью поддерживать кеширование, используя параметр `useCache`. Это важно для:
- Быстрого создания слепков (использование кешированных данных)
- Снижения нагрузки на Bitrix24 API
- Согласованности данных (использование тех же данных, что и в дашборде)

### Совместимость с системой прогресса

Метод должен поддерживать отслеживание прогресса через колбэк `onProgress`, что важно для:
- Отображения прогресса загрузки в UI
- Информирования пользователя о процессе создания слепка
- Согласованности с существующим дашбордом

### Обработка ошибок

Все ошибки должны быть обработаны и залогированы, чтобы:
- Пользователь получал понятные сообщения об ошибках
- Разработчики могли отслеживать проблемы в консоли
- Система могла корректно обрабатывать сбои

---

## История правок

- **2025-12-08 10:00 (UTC+3, Брест):** Создана задача

---

**Автор:** Технический писатель  
**Версия документа:** 1.0


