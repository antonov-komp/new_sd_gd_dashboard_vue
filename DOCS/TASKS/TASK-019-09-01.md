# TASK-019-09-01: Добавление маршрута и интеграция дашборда "График состояния" в навигацию

**Дата создания:** 2025-12-08 10:17 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 9, часть 1)

---

## 📋 Цель задачи

Добавить маршрут для дашборда "График состояния" в роутер приложения, интегрировать его в конфигурацию отчётов для отображения на стартовой странице и добавить навигацию из существующих дашбордов (например, из дашборда сектора 1С).

---

## 🎯 Контекст

После создания главного компонента дашборда "График состояния" (`GraphStateDashboard.vue`) необходимо:
- Добавить маршрут в роутер для доступа к дашборду
- Добавить отчёт в конфигурацию отчётов для отображения на стартовой странице
- Добавить ссылки/кнопки навигации из других дашбордов
- Обеспечить корректную работу переходов между страницами

**Требования:**
- Маршрут должен использовать lazy loading для оптимизации
- Отчёт должен отображаться на стартовой странице с правильной иконкой и описанием
- Навигация должна быть интуитивно понятной
- Переходы между страницами должны работать без ошибок

**Зависимости:**
- Этап 8 (TASK-019-08-01, TASK-019-08-02) должен быть завершён — `GraphStateDashboard.vue` создан
- Роутер приложения существует и работает (`vue-app/src/router/index.js`)
- Конфигурация отчётов существует (`vue-app/src/config/reports-config.js`)

---

## 🔍 Анализ существующей структуры

### Роутер (vue-app/src/router/index.js)

**Текущая структура:**
```javascript
const routes = [
  {
    path: '/install',
    name: 'install',
    component: InstallPage
  },
  {
    path: '/',
    name: 'index',
    component: IndexPage
  },
  {
    path: '/admin/webhook-logs',
    name: 'admin-webhook-logs',
    component: WebhookLogsPage,
    meta: {
      requiresAuth: true,
      title: 'Логи вебхуков',
      adminOnly: true
    }
  },
  {
    path: '/dashboard/sector-1c',
    name: 'dashboard-sector-1c',
    component: () => import('@/components/dashboard/DashboardSector1C.vue')
  }
];
```

**Особенности:**
- Используется `createWebHashHistory()` для работы внутри Bitrix24
- Lazy loading для компонентов дашбордов
- Метаданные для маршрутов (requiresAuth, title, adminOnly)

### Конфигурация отчётов (vue-app/src/config/reports-config.js)

**Текущая структура:**
```javascript
export const reportsConfig = {
  reports: [
    {
      id: 5,
      title: 'Дашборд сектора 1С',
      icon: '📋',
      route: '/dashboard/sector-1c',
      description: 'Управление тикетами сектора 1С',
      active: true
    }
  ]
};
```

**Особенности:**
- Массив отчётов с метаданными (id, title, icon, route, description, active)
- Функция `getReports()` возвращает только активные отчёты
- Отчёты отображаются на стартовой странице (`IndexPage.vue`)

---

## 📝 Что нужно сделать

### 1. Добавить маршрут в роутер

**Расположение:** `vue-app/src/router/index.js`

**Действия:**
- Добавить новый маршрут для дашборда "График состояния"
- Использовать lazy loading для оптимизации
- Добавить метаданные маршрута (title, requiresAuth, если нужно)

**Пример:**
```javascript
{
  path: '/dashboard/graph-state',
  name: 'dashboard-graph-state',
  component: () => import('@/components/graph-state/GraphStateDashboard.vue'),
  meta: {
    title: 'График состояния сектора 1С',
    requiresAuth: true, // Если требуется авторизация
    description: 'Визуализация изменений состояния сектора во времени'
  }
}
```

**Требования:**
- Путь должен быть понятным и соответствовать назначению
- Имя маршрута должно быть уникальным
- Использовать lazy loading (`() => import(...)`)
- Добавить метаданные для SEO и навигации

---

### 2. Добавить отчёт в конфигурацию отчётов

**Расположение:** `vue-app/src/config/reports-config.js`

**Действия:**
- Добавить новый объект отчёта в массив `reports`
- Установить `active: true` для отображения на стартовой странице
- Выбрать подходящую иконку и описание

**Пример:**
```javascript
{
  id: 6, // Следующий свободный ID
  title: 'График состояния',
  icon: '📊', // Или другая подходящая иконка (📈, 📉, 📊)
  route: '/dashboard/graph-state',
  description: 'Визуализация изменений состояния сектора 1С во времени',
  active: true,
  category: 'analytics', // Опционально, для группировки
  order: 6 // Опционально, для сортировки
}
```

**Требования:**
- ID должен быть уникальным
- `route` должен соответствовать маршруту в роутере
- `active: true` для отображения на стартовой странице
- Иконка должна быть понятной и отражать назначение

---

### 3. Добавить навигацию из дашборда сектора 1С

**Расположение:** `vue-app/src/components/dashboard/DashboardSector1C.vue`

**Действия:**
- Добавить кнопку/ссылку для перехода к дашборду "График состояния"
- Разместить в удобном месте (например, в заголовке или панели действий)
- Использовать `router.push()` для навигации

**Пример:**
```vue
<template>
  <div class="dashboard-sector-1c">
    <div class="dashboard-header">
      <h1>Дашборд сектора 1С</h1>
      <div class="header-actions">
        <button 
          @click="navigateToGraphState"
          class="btn-navigate-graph-state"
          title="Перейти к графику состояния"
        >
          <span class="icon">📊</span>
          <span>График состояния</span>
        </button>
      </div>
    </div>
    <!-- ... остальной контент ... -->
  </div>
</template>

<script setup>
import { useRouter } from 'vue-router';

const router = useRouter();

function navigateToGraphState() {
  router.push({ name: 'dashboard-graph-state' });
}
</script>
```

**Альтернативный вариант (через ссылку):**
```vue
<router-link 
  :to="{ name: 'dashboard-graph-state' }"
  class="nav-link-graph-state"
>
  <span class="icon">📊</span>
  <span>График состояния</span>
</router-link>
```

---

### 4. Добавить навигацию обратно (из дашборда "График состояния" к дашборду сектора 1С)

**Расположение:** `vue-app/src/components/graph-state/GraphStateDashboard.vue`

**Действия:**
- Добавить кнопку/ссылку для перехода обратно к дашборду сектора 1С
- Разместить в заголовке дашборда
- Использовать `router.push()` или `<router-link>`

**Пример:**
```vue
<template>
  <div class="graph-state-dashboard">
    <div class="dashboard-header">
      <div class="header-content">
        <div class="breadcrumbs">
          <router-link 
            :to="{ name: 'dashboard-sector-1c' }"
            class="breadcrumb-link"
          >
            Дашборд сектора 1С
          </router-link>
          <span class="breadcrumb-separator">/</span>
          <span class="breadcrumb-current">График состояния</span>
        </div>
        <h1 class="dashboard-title">График состояния сектора 1С</h1>
      </div>
      <!-- ... остальные действия ... -->
    </div>
    <!-- ... остальной контент ... -->
  </div>
</template>
```

---

## 🏗️ Архитектура решения

### Структура файлов

```
vue-app/src/
├── router/
│   └── index.js                    # Обновление: добавление маршрута
├── config/
│   └── reports-config.js           # Обновление: добавление отчёта
└── components/
    ├── dashboard/
    │   └── DashboardSector1C.vue   # Обновление: добавление навигации
    └── graph-state/
        └── GraphStateDashboard.vue # Обновление: добавление навигации обратно
```

### Последовательность изменений

1. **Добавить маршрут в роутер** — первичный шаг для доступа к дашборду
2. **Добавить отчёт в конфигурацию** — для отображения на стартовой странице
3. **Добавить навигацию из дашборда сектора 1С** — для удобного перехода
4. **Добавить навигацию обратно** — для возврата к дашборду сектора 1С

---

## 💡 Примеры кода

### Полный пример изменения router/index.js

```javascript
import { createRouter, createWebHashHistory } from 'vue-router';
import InstallPage from '@/components/InstallPage.vue';
import IndexPage from '@/components/IndexPage.vue';
import WebhookLogsPage from '@/pages/WebhookLogsPage.vue';

const routes = [
  {
    path: '/install',
    name: 'install',
    component: InstallPage
  },
  {
    path: '/',
    name: 'index',
    component: IndexPage
  },
  {
    path: '/admin/webhook-logs',
    name: 'admin-webhook-logs',
    component: WebhookLogsPage,
    meta: {
      requiresAuth: true,
      title: 'Логи вебхуков',
      adminOnly: true
    }
  },
  {
    path: '/dashboard/sector-1c',
    name: 'dashboard-sector-1c',
    component: () => import('@/components/dashboard/DashboardSector1C.vue'),
    meta: {
      title: 'Дашборд сектора 1С',
      description: 'Управление тикетами сектора 1С'
    }
  },
  // НОВЫЙ МАРШРУТ:
  {
    path: '/dashboard/graph-state',
    name: 'dashboard-graph-state',
    component: () => import('@/components/graph-state/GraphStateDashboard.vue'),
    meta: {
      title: 'График состояния сектора 1С',
      description: 'Визуализация изменений состояния сектора во времени',
      requiresAuth: true // Если требуется авторизация
    }
  }
];

const router = createRouter({
  // Используем hash mode для работы внутри Bitrix24
  history: createWebHashHistory(),
  routes
});

// Определяем начальный маршрут на основе текущего URL
setTimeout(() => {
  const currentPath = window.location.pathname;
  const hash = window.location.hash;
  
  if (hash && hash !== '#/') {
    return;
  }
  
  if (currentPath.includes('install.php')) {
    router.replace('/install');
  } else {
    router.replace('/');
  }
}, 0);

export default router;
```

### Полный пример изменения reports-config.js

```javascript
/**
 * Конфигурация отчётов для стартовой страницы
 * 
 * Определяет список доступных отчётов с их метаданными
 */

export const reportsConfig = {
  reports: [
    {
      id: 1,
      title: 'Отчёт по тикетам',
      icon: '📊',
      route: '/reports/tickets',
      description: 'Статистика по тикетам и обращениям',
      active: false
    },
    {
      id: 2,
      title: 'Отчёт по производительности',
      icon: '📈',
      route: '/reports/performance',
      description: 'Анализ производительности работы отдела',
      active: false
    },
    {
      id: 3,
      title: 'Отчёт по срокам',
      icon: '📉',
      route: '/reports/deadlines',
      description: 'Анализ соблюдения сроков выполнения задач',
      active: false
    },
    {
      id: 4,
      title: 'Сводный отчёт',
      icon: '📋',
      route: '/reports/summary',
      description: 'Общая сводка по всем показателям',
      active: false
    },
    {
      id: 5,
      title: 'Дашборд сектора 1С',
      icon: '📋',
      route: '/dashboard/sector-1c',
      description: 'Управление тикетами сектора 1С',
      active: true
    },
    // НОВЫЙ ОТЧЁТ:
    {
      id: 6,
      title: 'График состояния',
      icon: '📊',
      route: '/dashboard/graph-state',
      description: 'Визуализация изменений состояния сектора 1С во времени',
      active: true,
      category: 'analytics', // Опционально
      order: 6 // Опционально
    }
  ]
};

/**
 * Получение списка активных отчётов
 * 
 * Возвращает только отчёты с active: true
 * 
 * @returns {Array<Object>} Массив активных отчётов
 */
export function getReports() {
  return reportsConfig.reports.filter(report => report.active);
}

/**
 * Получение отчёта по ID
 * 
 * @param {number} id - ID отчёта
 * @returns {Object|null} Отчёт или null, если не найден
 */
export function getReportById(id) {
  return reportsConfig.reports.find(report => report.id === id) || null;
}

/**
 * Получение отчёта по маршруту
 * 
 * @param {string} route - Маршрут отчёта
 * @returns {Object|null} Отчёт или null, если не найден
 */
export function getReportByRoute(route) {
  return reportsConfig.reports.find(report => report.route === route) || null;
}
```

### Пример добавления навигации в DashboardSector1C.vue

```vue
<template>
  <div class="dashboard-sector-1c">
    <div class="dashboard-header">
      <div class="header-content">
        <h1 class="dashboard-title">Дашборд сектора 1С</h1>
        <p class="dashboard-subtitle">Управление тикетами сектора 1С</p>
      </div>
      <div class="header-actions">
        <!-- Кнопка перехода к графику состояния -->
        <button 
          @click="navigateToGraphState"
          class="btn-navigate-graph-state"
          title="Перейти к графику состояния сектора"
        >
          <span class="icon">📊</span>
          <span>График состояния</span>
        </button>
      </div>
    </div>
    <!-- ... остальной контент дашборда ... -->
  </div>
</template>

<script setup>
import { useRouter } from 'vue-router';

const router = useRouter();

function navigateToGraphState() {
  router.push({ name: 'dashboard-graph-state' });
}
</script>

<style scoped>
.btn-navigate-graph-state {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  background-color: #3b82f6;
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: background-color 0.2s;
}

.btn-navigate-graph-state:hover {
  background-color: #2563eb;
}

.btn-navigate-graph-state .icon {
  font-size: 18px;
}
</style>
```

### Пример добавления навигации обратно в GraphStateDashboard.vue

```vue
<template>
  <div class="graph-state-dashboard">
    <div class="dashboard-header">
      <div class="header-content">
        <!-- Хлебные крошки для навигации -->
        <nav class="breadcrumbs" aria-label="Навигация">
          <router-link 
            :to="{ name: 'dashboard-sector-1c' }"
            class="breadcrumb-link"
          >
            Дашборд сектора 1С
          </router-link>
          <span class="breadcrumb-separator" aria-hidden="true">/</span>
          <span class="breadcrumb-current">График состояния</span>
        </nav>
        
        <h1 class="dashboard-title">График состояния сектора 1С</h1>
        <p class="dashboard-subtitle">
          Визуализация изменений состояния сектора во времени
        </p>
      </div>
      <!-- ... остальные действия ... -->
    </div>
    <!-- ... остальной контент ... -->
  </div>
</template>

<style scoped>
.breadcrumbs {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
  font-size: 14px;
}

.breadcrumb-link {
  color: #3b82f6;
  text-decoration: none;
  transition: color 0.2s;
}

.breadcrumb-link:hover {
  color: #2563eb;
  text-decoration: underline;
}

.breadcrumb-separator {
  color: #6b7280;
}

.breadcrumb-current {
  color: #1f2937;
  font-weight: 600;
}
</style>
```

---

## ✅ Критерии приёмки

- [ ] Маршрут `/dashboard/graph-state` добавлен в `router/index.js`
- [ ] Маршрут использует lazy loading (`() => import(...)`)
- [ ] Метаданные маршрута добавлены (title, description, requiresAuth)
- [ ] Отчёт "График состояния" добавлен в `reports-config.js`
- [ ] Отчёт имеет `active: true` и отображается на стартовой странице
- [ ] Кнопка/ссылка навигации добавлена в `DashboardSector1C.vue`
- [ ] Навигация обратно добавлена в `GraphStateDashboard.vue` (хлебные крошки или кнопка)
- [ ] Переходы между страницами работают без ошибок
- [ ] Навигация работает корректно (router.push или router-link)
- [ ] Код соответствует стандартам проекта (ESLint, форматирование)
- [ ] Комментарии добавлены к основным изменениям
- [ ] Все изменения протестированы (переходы, отображение на стартовой странице)

---

## 📝 Действия

1. **Добавить маршрут в роутер:**
   - Открыть `vue-app/src/router/index.js`
   - Добавить новый маршрут для дашборда "График состояния"
   - Использовать lazy loading
   - Добавить метаданные

2. **Добавить отчёт в конфигурацию:**
   - Открыть `vue-app/src/config/reports-config.js`
   - Добавить новый объект отчёта в массив `reports`
   - Установить `active: true`
   - Выбрать подходящую иконку и описание

3. **Добавить навигацию из дашборда сектора 1С:**
   - Открыть `vue-app/src/components/dashboard/DashboardSector1C.vue`
   - Добавить кнопку/ссылку для перехода к графику состояния
   - Реализовать функцию навигации через `useRouter()`

4. **Добавить навигацию обратно:**
   - Открыть `vue-app/src/components/graph-state/GraphStateDashboard.vue`
   - Добавить хлебные крошки или кнопку для возврата к дашборду сектора 1С
   - Реализовать навигацию через `router-link` или `router.push()`

5. **Протестировать:**
   - Проверить отображение отчёта на стартовой странице
   - Проверить переход по маршруту `/dashboard/graph-state`
   - Проверить навигацию из дашборда сектора 1С
   - Проверить навигацию обратно
   - Проверить работу в разных браузерах

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущие задачи:**
  - `DOCS/TASKS/TASK-019-08-01.md` (базовая структура дашборда)
  - `DOCS/TASKS/TASK-019-08-02.md` (финальная полировка)
- **Следующая задача:** `DOCS/TASKS/TASK-019-09-02.md` (настройка прав доступа и тестирование)
- **Файлы для изменения:**
  - `vue-app/src/router/index.js`
  - `vue-app/src/config/reports-config.js`
  - `vue-app/src/components/dashboard/DashboardSector1C.vue`
  - `vue-app/src/components/graph-state/GraphStateDashboard.vue`
- **Связанные документы:**
  - `DOCS/TASKS/ARCHIVE/TASK-005-01-add-report-config-router.md` (пример добавления отчёта)

---

## ⏱️ Оценка времени

**1-2 часа**

**Разбивка:**
- Добавление маршрута в роутер: 15-20 минут
- Добавление отчёта в конфигурацию: 15-20 минут
- Добавление навигации из дашборда сектора 1С: 15-20 минут
- Добавление навигации обратно: 15-20 минут
- Тестирование и отладка: 20-30 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-09-02: Настройка прав доступа, тестирование переходов и финальная интеграция**.

---

## 💡 Дополнительные замечания

### Lazy Loading

Использование lazy loading для компонентов дашборда важно для:
- Оптимизации начальной загрузки приложения
- Разделения кода (code splitting)
- Улучшения производительности

### Метаданные маршрута

Метаданные маршрута могут использоваться для:
- Установки заголовка страницы
- Проверки прав доступа (через navigation guards)
- SEO оптимизации
- Отображения в навигации

### Навигация

Рекомендуется использовать:
- `<router-link>` для статических ссылок (лучше для SEO и доступности)
- `router.push()` для программной навигации (например, после действий пользователя)

---

## История правок

- **2025-12-08 10:17 (UTC+3, Брест):** Создана задача

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

