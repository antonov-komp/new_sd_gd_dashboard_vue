# TASK-022: Ограничение доступа к диагностике дашборда 1С только для администраторов

**Дата создания:** 2025-12-11 10:36 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js)

---

## Цель
Ограничить доступ к функционалу диагностики дашборда сектора 1С только для администраторов приложения, используя ту же логику проверки прав, что и для кнопки "шестеренка" (администраторская панель).

## Контекст
- В дашборде сектора 1С есть кнопка "Диагностика", которая в данный момент доступна всем пользователям, имеющим доступ к дашборду.
- В приложении уже реализована система проверки прав администратора через функцию `isAdmin()` из `access-config.js`.
- Кнопка "шестеренка" (⚙️) на главной странице (`IndexPage.vue`) использует проверку `v-if="isAdmin"` и отображается только администраторам.
- Необходимо применить ту же логику к кнопке "Диагностика" и панели диагностики в дашборде сектора 1С.

## Требования (функциональные и нефункциональные)

### Функциональные требования:
1. **Ограничение видимости кнопки "Диагностика":**
   - Кнопка "Диагностика" должна отображаться только для администраторов приложения.
   - Для обычных пользователей кнопка должна быть скрыта.

2. **Ограничение доступа к панели диагностики:**
   - Панель диагностики (`DiagnosticsPanel`) должна отображаться только для администраторов.
   - Если не-администратор попытается включить диагностику через URL-параметр или localStorage, панель не должна отображаться.

3. **Проверка прав в функции `isDiagnosticsEnabled()`:**
   - Функция должна дополнительно проверять права администратора перед возвратом `true`.
   - Если пользователь не является администратором, функция должна возвращать `false`, даже если диагностика включена через URL или localStorage.

4. **Получение информации о текущем пользователе:**
   - В компоненте `DashboardSector1C.vue` необходимо получить информацию о текущем пользователе через `AccessControlService.getCurrentUser()`.
   - Использовать computed-свойство для проверки прав администратора, аналогично `IndexPage.vue`.

### Нефункциональные требования:
- Не нарушить текущий UI/UX дашборда для обычных пользователей.
- Не добавить лишних запросов к API (использовать кеширование информации о пользователе, если возможно).
- Сохранить обратную совместимость: если пользователь не определён, диагностика должна быть недоступна (fail-safe).

## API-методы Bitrix24 (для справки)
- `user.current` — получение информации о текущем пользователе (используется через `AccessControlService.getCurrentUser()`)
  - Документация: https://context7.com/bitrix24/rest/user.current

## Модули и компоненты

### Файлы для изменения:
1. **`vue-app/src/components/dashboard/DashboardSector1C.vue`**
   - Добавить получение информации о текущем пользователе через `AccessControlService.getCurrentUser()`.
   - Добавить computed-свойство `isUserAdmin` для проверки прав администратора.
   - Изменить условие отображения кнопки "Диагностика" с `v-if="!isDiagnosticsEnabled"` на `v-if="!isDiagnosticsEnabled && isUserAdmin"`.
   - Изменить условие отображения кнопки "Сбросить кеш" с `v-if="isDiagnosticsEnabled"` на `v-if="isDiagnosticsEnabled && isUserAdmin"`.
   - Передать `isUserAdmin` в компонент `DiagnosticsPanel` через prop.

2. **`vue-app/src/components/dashboard/DiagnosticsPanel.vue`**
   - Добавить prop `isUserAdmin` для проверки прав администратора.
   - Изменить условие отображения панели с `v-if="isEnabled"` на `v-if="isEnabled && isUserAdmin"`.

3. **`vue-app/src/services/dashboard-sector-1c/utils/diagnostics-service.js`**
   - Добавить опциональный параметр `currentUser` в функцию `isDiagnosticsEnabled()`.
   - Добавить проверку прав администратора перед возвратом `true`.
   - Использовать функцию `isAdmin()` из `access-config.js` для проверки прав.

### Файлы для импорта:
- `@/services/access-control-service.js` — для получения информации о пользователе
- `@/config/access-config.js` — для функции `isAdmin()`

## Зависимости
- Использует существующую систему проверки прав администратора (`isAdmin()` из `access-config.js`).
- Зависит от `AccessControlService.getCurrentUser()` для получения информации о текущем пользователе.
- Связана с задачей TASK-021 (реализация диагностики).

## Ступенчатые подзадачи

### 1. Получение информации о пользователе в DashboardSector1C.vue
- Импортировать `AccessControlService` и `isAdmin` из соответствующих модулей.
- Добавить `ref` для хранения информации о текущем пользователе (`currentUser`).
- В `onMounted` получить информацию о пользователе через `AccessControlService.getCurrentUser()`.
- Создать computed-свойство `isUserAdmin`, которое проверяет права администратора через `isAdmin(currentUser.value)`.

### 2. Ограничение видимости кнопки "Диагностика"
- Изменить условие `v-if="!isDiagnosticsEnabled"` на `v-if="!isDiagnosticsEnabled && isUserAdmin"` для кнопки "Диагностика".
- Изменить условие `v-if="isDiagnosticsEnabled"` на `v-if="isDiagnosticsEnabled && isUserAdmin"` для кнопки "Сбросить кеш".

### 3. Передача прав администратора в DiagnosticsPanel
- Добавить prop `isUserAdmin` в компонент `DiagnosticsPanel`.
- Передать `isUserAdmin` из `DashboardSector1C.vue` в `DiagnosticsPanel`.

### 4. Ограничение доступа к панели диагностики
- В `DiagnosticsPanel.vue` изменить условие `v-if="isEnabled"` на `v-if="isEnabled && isUserAdmin"`.
- Добавить проверку прав в computed-свойствах `metrics` и `problematicTickets`, чтобы они возвращали пустые данные, если пользователь не администратор.

### 5. Проверка прав в функции isDiagnosticsEnabled()
- Добавить опциональный параметр `currentUser` в функцию `isDiagnosticsEnabled()`.
- Если `currentUser` передан, проверить права администратора через `isAdmin(currentUser)`.
- Если пользователь не является администратором, вернуть `false`, даже если диагностика включена через URL или localStorage.
- Обновить все вызовы `isDiagnosticsEnabled()` в коде, передавая `currentUser` где это возможно.

### 6. Обновление вызовов isDiagnosticsEnabled()
- В `DashboardSector1C.vue` передавать `currentUser.value` в функцию `isDiagnosticsEnabled()`.
- В других местах кода (если есть) также передавать информацию о пользователе.

### 7. Тестирование
- Проверить, что кнопка "Диагностика" не отображается для обычных пользователей.
- Проверить, что кнопка "Диагностика" отображается для администраторов.
- Проверить, что панель диагностики не отображается для обычных пользователей, даже если диагностика включена через URL.
- Проверить, что панель диагностики отображается для администраторов при включённой диагностике.
- Проверить, что обычные пользователи не могут включить диагностику через localStorage.

## Критерии приёмки
- [x] Кнопка "Диагностика" отображается только для администраторов приложения.
- [x] Кнопка "Сбросить кеш" отображается только для администраторов при включённой диагностике.
- [x] Панель диагностики не отображается для обычных пользователей, даже если диагностика включена через URL или localStorage.
- [x] Функция `isDiagnosticsEnabled()` проверяет права администратора и возвращает `false` для не-администраторов.
- [x] Информация о текущем пользователе получается через `AccessControlService.getCurrentUser()`.
- [x] Проверка прав администратора выполняется через функцию `isAdmin()` из `access-config.js`.
- [x] Нет регрессий в работе дашборда для обычных пользователей.
- [x] Нет регрессий в работе диагностики для администраторов.
- [x] Код соответствует Vue 3 + Composition API.
- [x] Код соответствует стандартам проекта (именование, структура, комментарии).

## Примеры кода

### 1. Получение информации о пользователе в DashboardSector1C.vue

```javascript
import { AccessControlService } from '@/services/access-control-service.js';
import { isAdmin } from '@/config/access-config.js';

// В setup():
const currentUser = ref(null);

// Computed для проверки администратора
const isUserAdmin = computed(() => {
  if (!currentUser.value) {
    return false;
  }
  return isAdmin(currentUser.value);
});

// В onMounted():
onMounted(async () => {
  // Получаем информацию о текущем пользователе
  try {
    currentUser.value = await AccessControlService.getCurrentUser();
  } catch (error) {
    console.error('Error getting current user:', error);
    currentUser.value = null;
  }
  
  // Остальной код загрузки...
  actions.loadSectorData();
});
```

### 2. Изменение условия отображения кнопки "Диагностика"

```vue
<button 
  v-if="!isDiagnosticsEnabled && isUserAdmin"
  @click="enableDiagnostics"
  class="btn-enable-diagnostics"
  title="Включить диагностический режим"
>
  <span class="icon">🔍</span>
  <span>Диагностика</span>
</button>
```

### 3. Обновление функции isDiagnosticsEnabled()

```javascript
/**
 * Проверка, включён ли диагностический режим
 * 
 * @param {object} route - Объект route из Vue Router (опционально)
 * @param {object} currentUser - Объект текущего пользователя (опционально)
 * @returns {boolean} true, если диагностика включена И пользователь является администратором
 */
export function isDiagnosticsEnabled(route = null, currentUser = null) {
  // Сначала проверяем права администратора (если передан currentUser)
  if (currentUser) {
    const { isAdmin } = require('@/config/access-config.js');
    if (!isAdmin(currentUser)) {
      return false; // Не-администраторы не могут использовать диагностику
    }
  }
  
  // Затем проверяем, включена ли диагностика через URL/localStorage
  // ... существующая логика проверки query-параметра, hash, localStorage ...
  
  return false;
}
```

### 4. Передача isUserAdmin в DiagnosticsPanel

```vue
<!-- В DashboardSector1C.vue -->
<DiagnosticsPanel 
  :is-enabled="isDiagnosticsEnabled" 
  :is-user-admin="isUserAdmin"
/>
```

```vue
<!-- В DiagnosticsPanel.vue -->
<div v-if="isEnabled && isUserAdmin" class="diagnostics-panel">
  <!-- ... содержимое панели ... -->
</div>
```

## Тестирование

### Тест 1: Обычный пользователь не видит кнопку "Диагностика"
1. Войти в систему как обычный пользователь (не администратор).
2. Открыть дашборд сектора 1С.
3. **Ожидаемый результат:** Кнопка "Диагностика" не отображается в заголовке.

### Тест 2: Администратор видит кнопку "Диагностика"
1. Войти в систему как администратор.
2. Открыть дашборд сектора 1С.
3. **Ожидаемый результат:** Кнопка "Диагностика" отображается в заголовке.

### Тест 3: Обычный пользователь не может включить диагностику через URL
1. Войти в систему как обычный пользователь.
2. Открыть дашборд сектора 1С с параметром `?diagnostics=true` в URL.
3. **Ожидаемый результат:** Панель диагностики не отображается, кнопка "Диагностика" не отображается.

### Тест 4: Администратор может включить диагностику
1. Войти в систему как администратор.
2. Открыть дашборд сектора 1С.
3. Нажать кнопку "Диагностика".
4. **Ожидаемый результат:** Панель диагностики отображается, кнопка "Диагностика" заменяется на кнопку "Сбросить кеш".

### Тест 5: Обычный пользователь не может включить диагностику через localStorage
1. Войти в систему как обычный пользователь.
2. Установить `localStorage.setItem('dashboard-sector-1c-diagnostics', 'true')` в консоли браузера.
3. Обновить страницу дашборда сектора 1С.
4. **Ожидаемый результат:** Панель диагностики не отображается, кнопка "Диагностика" не отображается.

## История правок
- 2025-12-11 10:36 (UTC+3, Брест): Создана задача.
- 2025-12-11 10:49 (UTC+3, Брест): Задача успешно выполнена. Реализовано ограничение доступа к диагностике дашборда 1С только для администраторов. Обновлены компоненты DashboardSector1C.vue, DiagnosticsPanel.vue и сервис diagnostics-service.js. Все критерии приёмки выполнены.

