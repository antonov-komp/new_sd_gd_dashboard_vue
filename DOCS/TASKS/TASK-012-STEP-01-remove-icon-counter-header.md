# TASK-012-STEP-01: Убрать иконку `[0]` и счётчик из заголовка нулевой точки

**Дата создания:** 2025-12-06 17:21 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-012

---

## Описание

Убрать иконку `[0]` и счётчик `(count)` из заголовка компонента нулевой точки. Заголовок должен отображаться как просто "Нулевая точка" без дополнительных элементов.

**Текущее отображение:**
```vue
<div class="zero-point-header">
  <span class="zero-point-icon">📥</span>
  <h3>[0] Нулевая точка</h3>
  <span class="tickets-count">({{ tickets.length }})</span>
</div>
```

**Новое отображение:**
```vue
<div class="zero-point-header">
  <h3>Нулевая точка</h3>
</div>
```

---

## Контекст

В рамках редизайна нулевой точки необходимо упростить заголовок, убрав избыточные элементы (иконку `[0]` и счётчик тикетов). Это улучшит читаемость и соответствие гайдлайнам Bitrix24.

---

## Модули и компоненты

### Файл для изменения:

**`vue-app/src/components/dashboard/ZeroPoint.vue`**

**Текущая реализация (строки 3-7):**
```vue
<div class="zero-point-header">
  <span class="zero-point-icon">📥</span>
  <h3>[0] Нулевая точка</h3>
  <span class="tickets-count">({{ tickets.length }})</span>
</div>
```

**Новая реализация:**
```vue
<div class="zero-point-header">
  <h3>Нулевая точка</h3>
</div>
```

**Стили для изменения:**
- Убрать стили для `.zero-point-icon` (строки 107-109)
- Убрать стили для `.tickets-count` (строки 119-125)
- Обновить стили для `.zero-point-header` (убрать `gap: 10px`, если не используется)

---

## Ступенчатые подзадачи

1. Убрать элемент `<span class="zero-point-icon">📥</span>` из шаблона
2. Убрать текст `[0]` из заголовка `<h3>`
3. Убрать элемент `<span class="tickets-count">({{ tickets.length }})</span>` из шаблона
4. Убрать стили для `.zero-point-icon` из `<style scoped>`
5. Убрать стили для `.tickets-count` из `<style scoped>`
6. Обновить стили для `.zero-point-header` (убрать `gap`, если не нужен)

---

## Технические требования

- Сохранить структуру компонента
- Сохранить все props и emits
- Сохранить функциональность перетаскивания тикетов
- Стили должны соответствовать гайдлайнам Bitrix24

---

## Критерии приёмки

- [ ] Иконка `📥` убрана из заголовка
- [ ] Текст `[0]` убран из заголовка
- [ ] Счётчик `(count)` убран из заголовка
- [ ] Заголовок отображается как "Нулевая точка"
- [ ] Стили для `.zero-point-icon` удалены
- [ ] Стили для `.tickets-count` удалены
- [ ] Компонент работает корректно на всех этапах дашборда

---

## Тестирование

1. Открыть дашборд сектора 1С
2. Проверить отображение заголовка нулевой точки на каждом этапе:
   - Заголовок должен быть "Нулевая точка" (без иконки, без `[0]`, без счётчика)
3. Проверить, что функциональность перетаскивания тикетов работает корректно

---

## Пример кода

**До:**
```vue
<div class="zero-point-header">
  <span class="zero-point-icon">📥</span>
  <h3>[0] Нулевая точка</h3>
  <span class="tickets-count">({{ tickets.length }})</span>
</div>
```

**После:**
```vue
<div class="zero-point-header">
  <h3>Нулевая точка</h3>
</div>
```

**Стили (убрать):**
```css
.zero-point-icon {
  font-size: 24px;
}

.tickets-count {
  font-size: 14px;
  color: #666;
  background: white;
  padding: 4px 8px;
  border-radius: 12px;
}
```

---

## История правок

- 2025-12-06 17:21 (UTC+3, Брест): Создана подзадача

