# TASK-033-05: Этап 5 - Тестирование и оптимизация

**Дата создания:** 2025-12-12 09:35 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-033

---

## 📋 Описание этапа

Проведение комплексного тестирования всех трёх уровней попапа детализации по сотрудникам, оптимизация производительности, проверка адаптивности и исправление всех найденных проблем.

**Цель:** Обеспечить стабильную работу попапа на всех уровнях, оптимальную производительность и корректное отображение на всех устройствах.

---

## 🎯 Цель этапа

Провести полное тестирование функциональности попапа и оптимизировать его работу:

1. **Функциональное тестирование** — проверить все сценарии использования
2. **Тестирование данных** — проверить корректность группировок и расчетов
3. **Тестирование производительности** — оптимизировать загрузку и рендеринг
4. **Тестирование адаптивности** — проверить работу на всех устройствах
5. **Исправление проблем** — устранить все найденные баги и недочеты

**Ожидаемый результат:**
- Все функциональные тесты пройдены
- Данные отображаются корректно на всех уровнях
- Производительность оптимизирована
- Адаптивность проверена на всех устройствах
- Все найденные проблемы исправлены

---

## 📝 Подзадачи

### Подзадача 5.1: Функциональное тестирование

**Цель:** Провести полное функциональное тестирование всех уровней попапа и всех сценариев навигации.

#### Задачи

1. **Тестирование открытия попапа для разных типов графиков**

   **Тест 1.1: Линейный график**
   ```javascript
   /**
    * Тест: Открытие попапа при клике на точку линейного графика
    * 
    * Шаги:
    * 1. Открыть график состояния сектора 1С
    * 2. Выбрать линейный график
    * 3. Кликнуть на точку графика для этапа "Сформировано обращение"
    * 4. Проверить открытие попапа
    * 5. Проверить отображение уровня 1 (список сотрудников)
    * 
    * Ожидаемый результат:
    * - Попап открывается
    * - Заголовок: "Сформировано обращение"
    * - Отображается список сотрудников с прогресс-барами
    * - Общее количество тикетов отображается корректно
    */
   ```

   **Чек-лист:**
   - [ ] Попап открывается при клике на точку
   - [ ] Заголовок соответствует названию этапа
   - [ ] Список сотрудников отображается
   - [ ] Прогресс-бары отображаются корректно
   - [ ] Общее количество тикетов верное
   - [ ] Группа "Другие" отображается (если сотрудников > 10)

   **Тест 1.2: Круговая диаграмма**
   ```javascript
   /**
    * Тест: Открытие попапа при клике на сектор круговой диаграммы
    * 
    * Шаги:
    * 1. Выбрать круговую диаграмму
    * 2. Кликнуть на сектор "Рассмотрение ТЗ"
    * 3. Проверить открытие попапа
    * 4. Проверить отображение уровня 1
    * 
    * Ожидаемый результат:
    * - Попап открывается
    * - Заголовок: "Рассмотрение ТЗ"
    * - Список сотрудников отображается корректно
    */
   ```

   **Чек-лист:**
   - [ ] Попап открывается при клике на сектор
   - [ ] Заголовок соответствует выбранному этапу
   - [ ] Данные отображаются корректно

2. **Тестирование переходов между уровнями**

   **Тест 2.1: Переход 1 → 2 → 3 → 2 → 1**
   ```javascript
   /**
    * Тест: Полный цикл навигации через все уровни
    * 
    * Шаги:
    * 1. Открыть попап (уровень 1)
    * 2. Кликнуть на сотрудника "Иван Иванов" (переход на уровень 2)
    * 3. Проверить отображение уровня 2 (временные градации)
    * 4. Кликнуть на градацию "СЕГОДНЯ" (переход на уровень 3)
    * 5. Проверить отображение уровня 3 (таблица заказчиков)
    * 6. Кликнуть "Назад" (возврат на уровень 2)
    * 7. Проверить восстановление уровня 2
    * 8. Кликнуть "Назад" (возврат на уровень 1)
    * 9. Проверить восстановление уровня 1
    * 
    * Ожидаемый результат:
    * - Все переходы выполняются плавно
    * - Данные сохраняются при переходах
    * - Возврат работает корректно
    * - Анимации плавные
    */
   ```

   **Чек-лист:**
   - [ ] Переход 1 → 2 работает
   - [ ] Данные уровня 1 сохраняются
   - [ ] Уровень 2 отображается корректно
   - [ ] Переход 2 → 3 работает
   - [ ] Данные уровня 2 сохраняются
   - [ ] Уровень 3 отображается корректно
   - [ ] Возврат 3 → 2 работает
   - [ ] Данные уровня 2 восстанавливаются
   - [ ] Возврат 2 → 1 работает
   - [ ] Данные уровня 1 восстанавливаются

   **Тест 2.2: Прямые переходы через хлебные крошки**
   ```javascript
   /**
    * Тест: Прямой переход на уровень 1 с уровня 3 через хлебные крошки
    * 
    * Шаги:
    * 1. Находиться на уровне 3
    * 2. Кликнуть на "Стадия" в хлебных крошках
    * 3. Проверить переход на уровень 1
    * 
    * Ожидаемый результат:
    * - Переход на уровень 1 выполняется
    * - Данные уровня 1 восстанавливаются
    * - Уровни 2 и 3 очищаются
    */
   ```

   **Чек-лист:**
   - [ ] Прямой переход 3 → 1 работает
   - [ ] Прямой переход 3 → 2 работает
   - [ ] Данные корректно восстанавливаются
   - [ ] Ненужные данные очищаются

3. **Тестирование кнопки "Назад"**

   **Тест 3.1: Кнопка "Назад" на уровне 2**
   ```javascript
   /**
    * Тест: Возврат с уровня 2 на уровень 1
    * 
    * Шаги:
    * 1. Находиться на уровне 2
    * 2. Кликнуть кнопку "Назад"
    * 3. Проверить возврат на уровень 1
    * 
    * Ожидаемый результат:
    * - Возврат на уровень 1 выполняется
    * - Данные уровня 1 отображаются
    * - Данные уровня 2 очищаются
    */
   ```

   **Тест 3.2: Кнопка "Назад" на уровне 3**
   ```javascript
   /**
    * Тест: Возврат с уровня 3 на уровень 2
    * 
    * Шаги:
    * 1. Находиться на уровне 3
    * 2. Кликнуть кнопку "Назад"
    * 3. Проверить возврат на уровень 2
    * 
    * Ожидаемый результат:
    * - Возврат на уровень 2 выполняется
    * - Данные уровня 2 отображаются
    * - Данные уровня 3 очищаются
    */
   ```

   **Чек-лист:**
   - [ ] Кнопка "Назад" отображается на уровнях 2 и 3
   - [ ] Кнопка "Назад" не отображается на уровне 1
   - [ ] Возврат работает корректно
   - [ ] Данные восстанавливаются правильно

4. **Тестирование закрытия попапа**

   **Тест 4.1: Закрытие через кнопку ×**
   ```javascript
   /**
    * Тест: Закрытие попапа через кнопку закрытия
    * 
    * Шаги:
    * 1. Открыть попап
    * 2. Перейти на уровень 2
    * 3. Перейти на уровень 3
    * 4. Кликнуть кнопку ×
    * 5. Проверить закрытие попапа
    * 
    * Ожидаемый результат:
    * - Попап закрывается
    * - Все данные очищаются
    * - Уровень сбрасывается на 1
    */
   ```

   **Тест 4.2: Закрытие через клик вне попапа**
   ```javascript
   /**
    * Тест: Закрытие попапа через клик на затемненную область
    * 
    * Шаги:
    * 1. Открыть попап
    * 2. Кликнуть на затемненную область (вне модального окна)
    * 3. Проверить закрытие попапа
    * 
    * Ожидаемый результат:
    * - Попап закрывается
    * - Все данные очищаются
    */
   ```

   **Тест 4.3: Закрытие через клавишу ESC**
   ```javascript
   /**
    * Тест: Закрытие попапа через клавишу ESC
    * 
    * Шаги:
    * 1. Открыть попап
    * 2. Нажать клавишу ESC
    * 3. Проверить закрытие попапа
    * 
    * Ожидаемый результат:
    * - Попап закрывается
    * - Все данные очищаются
    */
   ```

   **Чек-лист:**
   - [ ] Закрытие через кнопку × работает
   - [ ] Закрытие через клик вне попапа работает
   - [ ] Закрытие через ESC работает
   - [ ] Все данные очищаются при закрытии
   - [ ] Уровень сбрасывается на 1

**Критерии завершения подзадачи 5.1:**
- [ ] Все тесты открытия попапа пройдены
- [ ] Все тесты переходов между уровнями пройдены
- [ ] Тесты кнопки "Назад" пройдены
- [ ] Тесты закрытия попапа пройдены
- [ ] Все чек-листы заполнены
- [ ] Найденные проблемы задокументированы

---

### Подзадача 5.2: Тестирование данных

**Цель:** Проверить корректность группировок, расчетов и отображения данных на всех уровнях.

#### Задачи

1. **Тестирование группировки по временным градациям**

   **Тест 2.1: Корректность категорий давности**
   ```javascript
   /**
    * Тест: Проверка корректности определения категорий давности
    * 
    * Тестовые данные:
    * - Тикет 1: создан сегодня → категория "СЕГОДНЯ"
    * - Тикет 2: создан вчера → категория "ВЧЕРА"
    * - Тикет 3: создан 3 дня назад → категория "НА ЭТОЙ НЕДЕЛЕ"
    * - Тикет 4: создан 10 дней назад → категория "НА ПРОШЛОЙ НЕДЕЛЕ"
    * - Тикет 5: создан 3 недели назад → категория "БОЛЕЕ ДВУХ НЕДЕЛЬ"
    * - Тикет 6: создан 1.5 месяца назад → категория "ДО 1 МЕСЯЦА"
    * - Тикет 7: создан 4 месяца назад → категория "БОЛЕЕ 2 МЕСЯЦЕВ"
    * - Тикет 8: создан 8 месяцев назад → категория "БОЛЕЕ ПОЛУГОДА"
    * - Тикет 9: создан 1.5 года назад → категория "БОЛЕЕ ГОДА"
    * 
    * Шаги:
    * 1. Открыть попап для сотрудника с тестовыми тикетами
    * 2. Перейти на уровень 2
    * 3. Проверить, что все категории определены правильно
    * 
    * Ожидаемый результат:
    * - Все 9 категорий отображаются
    * - Каждая категория содержит правильное количество тикетов
    * - Проценты рассчитываются корректно
    */
   ```

   **Чек-лист:**
   - [ ] Категория "СЕГОДНЯ" определяется правильно
   - [ ] Категория "ВЧЕРА" определяется правильно
   - [ ] Категория "НА ЭТОЙ НЕДЕЛЕ" определяется правильно
   - [ ] Категория "НА ПРОШЛОЙ НЕДЕЛЕ" определяется правильно
   - [ ] Категория "БОЛЕЕ ДВУХ НЕДЕЛЬ" определяется правильно
   - [ ] Категория "ДО 1 МЕСЯЦА" определяется правильно
   - [ ] Категория "БОЛЕЕ 2 МЕСЯЦЕВ" определяется правильно
   - [ ] Категория "БОЛЕЕ ПОЛУГОДА" определяется правильно
   - [ ] Категория "БОЛЕЕ ГОДА" определяется правильно
   - [ ] Проценты рассчитываются корректно (сумма = 100%)
   - [ ] Ширина прогресс-баров соответствует процентам

   **Тест 2.2: Сортировка категорий**
   ```javascript
   /**
    * Тест: Проверка сортировки категорий по порядку (от свежих к старым)
    * 
    * Ожидаемый порядок:
    * 1. СЕГОДНЯ
    * 2. ВЧЕРА
    * 3. НА ЭТОЙ НЕДЕЛЕ
    * 4. НА ПРОШЛОЙ НЕДЕЛЕ
    * 5. БОЛЕЕ ДВУХ НЕДЕЛЬ
    * 6. ДО 1 МЕСЯЦА
    * 7. БОЛЕЕ 2 МЕСЯЦЕВ
    * 8. БОЛЕЕ ПОЛУГОДА
    * 9. БОЛЕЕ ГОДА
    * 
    * Шаги:
    * 1. Открыть попап с тикетами во всех категориях
    * 2. Перейти на уровень 2
    * 3. Проверить порядок категорий
    * 
    * Ожидаемый результат:
    * - Категории отображаются в правильном порядке
    */
   ```

   **Чек-лист:**
   - [ ] Категории отсортированы правильно
   - [ ] Порядок соответствует ожидаемому
   - [ ] Пустые категории не отображаются

2. **Тестирование группировки по заказчикам**

   **Тест 2.3: Корректность группировки по заказчикам**
   ```javascript
   /**
    * Тест: Проверка группировки тикетов по заказчикам
    * 
    * Тестовые данные:
    * - Тикет 1: departmentHead = "Отдел ИТ"
    * - Тикет 2: departmentHead = "Бухгалтерия"
    * - Тикет 3: departmentHead = "Отдел ИТ"
    * - Тикет 4: departmentHead = null (Без заказчика)
    * - Тикет 5: departmentHead = "" (Без заказчика)
    * - Тикет 6: departmentHead = "   " (только пробелы, Без заказчика)
    * 
    * Шаги:
    * 1. Открыть попап для сотрудника с тестовыми тикетами
    * 2. Перейти на уровень 2
    * 3. Выбрать категорию с тестовыми тикетами
    * 4. Перейти на уровень 3
    * 5. Проверить группировку по заказчикам
    * 
    * Ожидаемый результат:
    * - "Отдел ИТ": 2 тикета
    * - "Бухгалтерия": 1 тикет
    * - "Без заказчика": 3 тикета
    * - Сортировка по убыванию количества
    */
   ```

   **Чек-лист:**
   - [ ] Группировка по заказчикам работает корректно
   - [ ] Тикеты с одинаковым заказчиком группируются
   - [ ] Тикеты с null группируются в "Без заказчика"
   - [ ] Тикеты с пустой строкой группируются в "Без заказчика"
   - [ ] Тикеты с только пробелами группируются в "Без заказчика"
   - [ ] Полные названия заказчиков отображаются (без ограничения 17 символов)
   - [ ] Сортировка по убыванию количества работает

   **Тест 2.4: Сортировка заказчиков**
   ```javascript
   /**
    * Тест: Проверка сортировки заказчиков по количеству тикетов
    * 
    * Тестовые данные:
    * - "Отдел ИТ": 5 тикетов
    * - "Бухгалтерия": 3 тикета
    * - "Отдел продаж": 3 тикета
    * - "Без заказчика": 1 тикет
    * 
    * Ожидаемый порядок:
    * 1. "Отдел ИТ" (5)
    * 2. "Бухгалтерия" (3) - перед "Отдел продаж" по алфавиту
    * 3. "Отдел продаж" (3)
    * 4. "Без заказчика" (1)
    * 
    * Шаги:
    * 1. Открыть попап с тестовыми данными
    * 2. Перейти на уровень 3
    * 3. Проверить порядок заказчиков
    * 
    * Ожидаемый результат:
    * - Заказчики отсортированы по убыванию количества
    * - При одинаковом количестве - по алфавиту
    */
   ```

   **Чек-лист:**
   - [ ] Сортировка по убыванию количества работает
   - [ ] Вторичная сортировка по алфавиту работает
   - [ ] Порядок соответствует ожидаемому

3. **Тестирование расчетов процентов**

   **Тест 2.5: Корректность процентов на уровне 2**
   ```javascript
   /**
    * Тест: Проверка расчета процентов для временных градаций
    * 
    * Тестовые данные:
    * - Всего тикетов у сотрудника: 10
    * - СЕГОДНЯ: 2 тикета → 20%
    * - ВЧЕРА: 1 тикет → 10%
    * - НА ЭТОЙ НЕДЕЛЕ: 3 тикета → 30%
    * - НА ПРОШЛОЙ НЕДЕЛЕ: 4 тикета → 40%
    * 
    * Шаги:
    * 1. Открыть попап с тестовыми данными
    * 2. Перейти на уровень 2
    * 3. Проверить проценты для каждой категории
    * 
    * Ожидаемый результат:
    * - Проценты рассчитываются правильно
    * - Сумма процентов = 100% (или близко к 100% с учетом округления)
    * - Ширина прогресс-баров соответствует процентам
    */
   ```

   **Чек-лист:**
   - [ ] Проценты рассчитываются корректно
   - [ ] Округление до 1 знака после запятой работает
   - [ ] Сумма процентов близка к 100%
   - [ ] Ширина прогресс-баров соответствует процентам

   **Тест 2.6: Корректность процентов на уровне 1**
   ```javascript
   /**
    * Тест: Проверка расчета процентов для сотрудников на уровне 1
    * 
    * Тестовые данные:
    * - Всего тикетов на стадии: 25
    * - Иван Иванов: 10 тикетов → 40%
    * - Петр Петров: 5 тикетов → 20%
    * - Другие: 10 тикетов → 40%
    * 
    * Шаги:
    * 1. Открыть попап
    * 2. Проверить проценты для каждого сотрудника
    * 
    * Ожидаемый результат:
    * - Проценты рассчитываются правильно
    * - Сумма процентов = 100%
    */
   ```

   **Чек-лист:**
   - [ ] Проценты на уровне 1 рассчитываются корректно
   - [ ] Сумма процентов = 100%

4. **Тестирование обработки пустых значений**

   **Тест 2.7: Тикеты без даты создания**
   ```javascript
   /**
    * Тест: Обработка тикетов без даты создания
    * 
    * Тестовые данные:
    * - Тикет 1: createdAt = null
    * - Тикет 2: createdAt = undefined
    * - Тикет 3: createdAt = ""
    * 
    * Ожидаемое поведение:
    * - Тикеты без даты относятся к категории "БОЛЕЕ ГОДА"
    * - Не вызывают ошибок
    * 
    * Шаги:
    * 1. Открыть попап с тикетами без даты
    * 2. Перейти на уровень 2
    * 3. Проверить, что тикеты отображаются в категории "БОЛЕЕ ГОДА"
    */
   ```

   **Чек-лист:**
   - [ ] Тикеты с null createdAt обрабатываются
   - [ ] Тикеты с undefined createdAt обрабатываются
   - [ ] Тикеты с пустой строкой createdAt обрабатываются
   - [ ] Ошибки не возникают

   **Тест 2.8: Тикеты без заказчика**
   ```javascript
   /**
    * Тест: Обработка тикетов без заказчика
    * 
    * Тестовые данные:
    * - Тикет 1: departmentHead = null
    * - Тикет 2: departmentHead = undefined
    * - Тикет 3: departmentHead = ""
    * - Тикет 4: departmentHead = "   " (только пробелы)
    * 
    * Ожидаемое поведение:
    * - Все тикеты группируются в "Без заказчика"
    * 
    * Шаги:
    * 1. Открыть попап с тикетами без заказчика
    * 2. Перейти на уровень 3
    * 3. Проверить группировку в "Без заказчика"
    */
   ```

   **Чек-лист:**
   - [ ] Тикеты с null departmentHead группируются в "Без заказчика"
   - [ ] Тикеты с undefined departmentHead группируются в "Без заказчика"
   - [ ] Тикеты с пустой строкой группируются в "Без заказчика"
   - [ ] Тикеты с только пробелами группируются в "Без заказчика"

5. **Тестирование граничных значений**

   **Тест 2.9: Сотрудник без тикетов**
   ```javascript
   /**
    * Тест: Обработка сотрудника без тикетов на стадии
    * 
    * Шаги:
    * 1. Открыть попап
    * 2. Попытаться кликнуть на сотрудника без тикетов
    * 
    * Ожидаемое поведение:
    * - Показывается уведомление "У сотрудника нет тикетов"
    * - Переход на уровень 2 не выполняется
    */
   ```

   **Тест 2.10: Категория без тикетов**
   ```javascript
   /**
    * Тест: Обработка категории без тикетов
    * 
    * Шаги:
    * 1. Открыть попап на уровне 2
    * 2. Попытаться кликнуть на категорию с count = 0
    * 
    * Ожидаемое поведение:
    * - Показывается уведомление "В категории нет тикетов"
    * - Переход на уровень 3 не выполняется
    */
   ```

   **Тест 2.11: Все тикеты без заказчика**
   ```javascript
   /**
    * Тест: Обработка случая, когда все тикеты без заказчика
    * 
    * Шаги:
    * 1. Открыть попап с тикетами, у которых все departmentHead = null
    * 2. Перейти на уровень 3
    * 
    * Ожидаемое поведение:
    * - Таблица отображается
    * - Одна строка: "Без заказчика" с общим количеством
    */
   ```

   **Чек-лист:**
   - [ ] Сотрудник без тикетов обрабатывается корректно
   - [ ] Категория без тикетов обрабатывается корректно
   - [ ] Все тикеты без заказчика обрабатываются корректно
   - [ ] Уведомления показываются пользователю

**Критерии завершения подзадачи 5.2:**
- [ ] Все тесты группировки по градациям пройдены
- [ ] Все тесты группировки по заказчикам пройдены
- [ ] Тесты расчетов процентов пройдены
- [ ] Тесты обработки пустых значений пройдены
- [ ] Тесты граничных значений пройдены
- [ ] Все чек-листы заполнены

---

### Подзадача 5.3: Тестирование производительности

**Цель:** Оптимизировать производительность попапа, особенно загрузку данных и рендеринг.

#### Задачи

1. **Измерение времени загрузки данных**

   **Тест 3.1: Время загрузки деталей тикетов через API**
   ```javascript
   /**
    * Тест: Измерение времени загрузки деталей тикетов
    * 
    * Метод измерения:
    * 1. Открыть DevTools → Network
    * 2. Открыть попап
    * 3. Зафиксировать время загрузки API-запроса
    * 
    * Целевые показатели:
    * - Время загрузки < 1 секунды для 50 тикетов
    * - Время загрузки < 2 секунд для 100 тикетов
    * 
    * Оптимизация:
    * - Использовать кеширование загруженных данных
    * - Загружать только необходимые поля
    * - Использовать батч-запросы (если возможно)
    */
   ```

   **Инструменты для измерения:**
   - Chrome DevTools → Performance
   - Chrome DevTools → Network
   - Vue DevTools → Performance

   **Чек-лист:**
   - [ ] Время загрузки измерено
   - [ ] Время соответствует целевым показателям
   - [ ] Кеширование работает
   - [ ] Оптимизация выполнена (если необходимо)

2. **Оптимизация кеширования**

   **Реализация кеширования:**
   ```javascript
   /**
    * Кеш для деталей тикетов
    * Ключ: ID тикета, Значение: объект тикета с полными данными
    */
   const ticketDetailsCache = new Map();
   
   /**
    * Получить детали тикета из кеша или загрузить
    * 
    * @param {number} ticketId - ID тикета
    * @returns {Promise<Object>} Детали тикета
    */
   async function getTicketDetails(ticketId) {
     // Проверка кеша
     if (ticketDetailsCache.has(ticketId)) {
       return ticketDetailsCache.get(ticketId);
     }
     
     // Загрузка через API
     const details = await TicketDetailsService.getTicketDetails(ticketId);
     
     // Сохранение в кеш
     if (details) {
       ticketDetailsCache.set(ticketId, details);
     }
     
     return details;
   }
   
   /**
    * Получить детали нескольких тикетов (батч)
    * 
    * @param {Array<number>} ticketIds - Массив ID тикетов
    * @returns {Promise<Array>} Массив деталей тикетов
    */
   async function getTicketsDetailsBatch(ticketIds) {
     // Разделить на закешированные и незакешированные
     const cached = [];
     const toLoad = [];
     
     ticketIds.forEach(id => {
       if (ticketDetailsCache.has(id)) {
         cached.push(ticketDetailsCache.get(id));
       } else {
         toLoad.push(id);
       }
     });
     
     // Загрузить незакешированные
     if (toLoad.length > 0) {
       const loaded = await TicketDetailsService.getTicketsDetails(toLoad);
       
       // Сохранить в кеш
       loaded.forEach(details => {
         if (details && details.id) {
           ticketDetailsCache.set(details.id, details);
         }
       });
       
       return [...cached, ...loaded];
     }
     
     return cached;
   }
   
   /**
    * Очистка кеша (при необходимости)
    */
   function clearTicketDetailsCache() {
     ticketDetailsCache.clear();
   }
   ```

   **Чек-лист:**
   - [ ] Кеширование реализовано
   - [ ] Кеш работает корректно
   - [ ] Батч-загрузка реализована
   - [ ] Очистка кеша работает

3. **Оптимизация рендеринга**

   **Тест 3.2: Время рендеринга больших списков**
   ```javascript
   /**
    * Тест: Измерение времени рендеринга списка из 50 сотрудников
    * 
    * Метод измерения:
    * 1. Открыть DevTools → Performance
    * 2. Записать профиль производительности
    * 3. Открыть попап с 50 сотрудниками
    * 4. Остановить запись
    * 5. Проанализировать время рендеринга
    * 
    * Целевые показатели:
    * - Время рендеринга < 100ms для 50 элементов
    * - Плавность анимаций (60 FPS)
    * 
    * Оптимизация:
    * - Использовать v-show вместо v-if (если возможно)
    * - Виртуализация списков (если элементов > 100)
    * - Ленивая загрузка данных
    */
   ```

   **Оптимизация рендеринга:**
   ```vue
   <!-- Использование v-show для быстрого переключения -->
   <div v-show="popupLevel === 1" class="level-1">
     <!-- Контент уровня 1 -->
   </div>
   
   <!-- Использование key для оптимизации Vue -->
   <div
     v-for="employee in employees"
     :key="employee.id"
     class="employee-item"
   >
     <!-- Контент -->
   </div>
   ```

   **Чек-лист:**
   - [ ] Время рендеринга измерено
   - [ ] Время соответствует целевым показателям
   - [ ] Оптимизация рендеринга выполнена
   - [ ] Анимации плавные (60 FPS)

4. **Оптимизация анимаций**

   **Тест 3.3: Производительность анимаций**
   ```javascript
   /**
    * Тест: Проверка плавности анимаций переходов
    * 
    * Метод измерения:
    * 1. Открыть DevTools → Performance
    * 2. Включить "Paint flashing"
    * 3. Выполнить переходы между уровнями
    * 4. Проверить FPS (должен быть ~60)
    * 
    * Оптимизация:
    * - Использовать transform вместо position для анимаций
    * - Использовать will-change для оптимизации
    * - Избегать изменения layout во время анимации
    */
   ```

   **Оптимизированные CSS анимации:**
   ```css
   /* Использование transform вместо position */
   .level-enter-active,
   .level-leave-active {
     transition: transform 0.3s ease, opacity 0.3s ease;
     will-change: transform, opacity; /* Оптимизация */
   }
   
   .level-enter-from {
     opacity: 0;
     transform: translateX(20px); /* Вместо left: 20px */
   }
   
   .level-leave-to {
     opacity: 0;
     transform: translateX(-20px); /* Вместо left: -20px */
   }
   ```

   **Чек-лист:**
   - [ ] FPS анимаций ~60
   - [ ] Анимации используют transform
   - [ ] will-change применен корректно
   - [ ] Нет "jank" (заиканий) при анимациях

5. **Оптимизация памяти**

   **Тест 3.4: Использование памяти**
   ```javascript
   /**
    * Тест: Проверка использования памяти
    * 
    * Метод измерения:
    * 1. Открыть DevTools → Memory
    * 2. Сделать snapshot до открытия попапа
    * 3. Открыть попап, перейти по всем уровням
    * 4. Сделать snapshot после использования
    * 5. Сравнить использование памяти
    * 
    * Целевые показатели:
    * - Увеличение памяти < 5MB для попапа
    * - Память освобождается при закрытии попапа
    * 
    * Оптимизация:
    * - Очистка данных при закрытии попапа
    * - Очистка кеша при необходимости
    * - Избежание утечек памяти
    */
   ```

   **Очистка памяти:**
   ```javascript
   /**
    * Очистка всех данных при закрытии попапа
    */
   function resetPopup() {
     popupLevel.value = 1;
     level1Data.value = null;
     level2Data.value = null;
     level3Data.value = null;
     
     // Очистка кеша (опционально, если кеш слишком большой)
     if (ticketDetailsCache.size > 1000) {
       ticketDetailsCache.clear();
     }
   }
   ```

   **Чек-лист:**
   - [ ] Использование памяти измерено
   - [ ] Память освобождается при закрытии
   - [ ] Нет утечек памяти
   - [ ] Кеш ограничен по размеру

**Критерии завершения подзадачи 5.3:**
- [ ] Время загрузки данных оптимизировано
- [ ] Кеширование работает эффективно
- [ ] Рендеринг оптимизирован
- [ ] Анимации плавные (60 FPS)
- [ ] Использование памяти оптимизировано

---

### Подзадача 5.4: Тестирование адаптивности

**Цель:** Проверить корректное отображение попапа на всех типах устройств и размерах экранов.

#### Задачи

1. **Тестирование на мобильных устройствах**

   **Тест 4.1: Мобильные устройства (320px - 480px)**
   ```javascript
   /**
    * Тест: Отображение попапа на мобильных устройствах
    * 
    * Устройства для тестирования:
    * - iPhone SE (375px)
    * - iPhone 12/13 (390px)
    * - Android (360px - 412px)
    * 
    * Шаги:
    * 1. Открыть DevTools → Toggle device toolbar
    * 2. Выбрать мобильное устройство
    * 3. Открыть попап
    * 4. Проверить отображение всех уровней
    * 
    * Проверки:
    * - Модальное окно занимает всю ширину экрана (с отступами)
    * - Текст читаемый (не слишком мелкий)
    * - Кнопки достаточно большие для нажатия
    * - Таблицы прокручиваются горизонтально (если необходимо)
    * - Прогресс-бары отображаются корректно
    */
   ```

   **Чек-лист для мобильных:**
   - [ ] Модальное окно адаптируется под ширину экрана
   - [ ] Заголовок читаемый
   - [ ] Кнопки "Назад" и "×" достаточно большие
   - [ ] Список сотрудников отображается корректно
   - [ ] Прогресс-бары читаемые
   - [ ] Уровень 2 отображается корректно
   - [ ] Уровень 3 (таблица) прокручивается горизонтально
   - [ ] Хлебные крошки отображаются (если добавлены)
   - [ ] Анимации работают плавно

2. **Тестирование на планшетах**

   **Тест 4.2: Планшеты (768px - 1024px)**
   ```javascript
   /**
    * Тест: Отображение попапа на планшетах
    * 
    * Устройства для тестирования:
    * - iPad (768px)
    * - iPad Pro (1024px)
    * - Android планшеты (800px - 1024px)
    * 
    * Шаги:
    * 1. Открыть DevTools → Toggle device toolbar
    * 2. Выбрать планшет
    * 3. Открыть попап
    * 4. Проверить отображение
    * 
    * Проверки:
    * - Модальное окно имеет оптимальную ширину
    * - Контент не растягивается слишком широко
    * - Таблицы отображаются полностью (без горизонтальной прокрутки)
    */
   ```

   **Чек-лист для планшетов:**
   - [ ] Модальное окно имеет оптимальную ширину
   - [ ] Контент читаемый
   - [ ] Таблицы отображаются полностью
   - [ ] Все элементы доступны

3. **Тестирование на десктопах**

   **Тест 4.3: Десктопы (1024px+)**
   ```javascript
   /**
    * Тест: Отображение попапа на десктопах
    * 
    * Разрешения для тестирования:
    * - 1024px (минимум)
    * - 1280px (стандарт)
    * - 1920px (Full HD)
    * - 2560px (2K)
    * 
    * Шаги:
    * 1. Изменить размер окна браузера
    * 2. Открыть попап
    * 3. Проверить отображение
    * 
    * Проверки:
    * - Модальное окно имеет максимальную ширину (например, 800px)
    * - Контент не растягивается слишком широко
    * - Все элементы отображаются корректно
    */
   ```

   **Чек-лист для десктопов:**
   - [ ] Модальное окно имеет максимальную ширину
   - [ ] Контент центрирован
   - [ ] Все элементы отображаются корректно
   - [ ] Таблицы читаемые

4. **Тестирование ориентации экрана**

   **Тест 4.4: Портретная ориентация**
   ```javascript
   /**
    * Тест: Отображение попапа в портретной ориентации
    * 
    * Шаги:
    * 1. Повернуть устройство в портретную ориентацию
    * 2. Открыть попап
    * 3. Проверить отображение
    * 
    * Проверки:
    * - Модальное окно адаптируется под высоту экрана
    * - Вертикальная прокрутка работает
    * - Все элементы доступны
    */
   ```

   **Тест 4.5: Альбомная ориентация**
   ```javascript
   /**
    * Тест: Отображение попапа в альбомной ориентации
    * 
    * Шаги:
    * 1. Повернуть устройство в альбомную ориентацию
    * 2. Открыть попап
    * 3. Проверить отображение
    * 
    * Проверки:
    * - Модальное окно использует доступную ширину
    * - Таблицы отображаются полностью
    */
   ```

   **Чек-лист:**
   - [ ] Портретная ориентация работает
   - [ ] Альбомная ориентация работает
   - [ ] Вертикальная прокрутка работает
   - [ ] Все элементы доступны

5. **Оптимизация стилей для разных размеров**

   **Медиа-запросы:**
   ```css
   /* Мобильные устройства (до 480px) */
   @media (max-width: 480px) {
     .modal-content {
       width: 95%;
       max-height: 95vh;
       margin: 2.5vh auto;
     }
     
     .modal-header {
       padding: 12px;
       font-size: 14px;
     }
     
     .employee-item,
     .category-item {
       flex-direction: column;
       align-items: flex-start;
       gap: 8px;
     }
     
     .departments-table {
       font-size: 12px;
     }
   }
   
   /* Планшеты (481px - 768px) */
   @media (min-width: 481px) and (max-width: 768px) {
     .modal-content {
       width: 90%;
       max-width: 600px;
     }
   }
   
   /* Десктопы (769px+) */
   @media (min-width: 769px) {
     .modal-content {
       width: 90%;
       max-width: 800px;
     }
   }
   ```

   **Чек-лист:**
   - [ ] Медиа-запросы добавлены
   - [ ] Стили оптимизированы для каждого размера
   - [ ] Адаптивность проверена на всех устройствах

**Критерии завершения подзадачи 5.4:**
- [ ] Тестирование на мобильных выполнено
- [ ] Тестирование на планшетах выполнено
- [ ] Тестирование на десктопах выполнено
- [ ] Тестирование ориентации выполнено
- [ ] Стили оптимизированы для всех размеров
- [ ] Все чек-листы заполнены

---

### Подзадача 5.5: Исправление найденных проблем

**Цель:** Исправить все найденные баги, недочеты и проблемы, выявленные в ходе тестирования.

#### Задачи

1. **Документирование найденных проблем**

   **Формат документации проблемы:**
   ```markdown
   ## Проблема #1: [Название проблемы]
   
   **Уровень:** [1, 2, или 3]
   **Приоритет:** [Критический, Высокий, Средний, Низкий]
   **Шаги для воспроизведения:**
   1. ...
   2. ...
   3. ...
   
   **Ожидаемое поведение:**
   ...
   
   **Фактическое поведение:**
   ...
   
   **Скриншоты/Логи:**
   ...
   
   **Решение:**
   ...
   ```

2. **Приоритизация проблем**

   **Критический приоритет:**
   - Попап не открывается
   - Ошибки JavaScript, блокирующие работу
   - Некорректное отображение данных
   - Потеря данных при переходах

   **Высокий приоритет:**
   - Медленная загрузка данных
   - Некорректная навигация
   - Проблемы с анимациями

   **Средний приоритет:**
   - Мелкие визуальные недочеты
   - Улучшения UX

   **Низкий приоритет:**
   - Косметические улучшения
   - Оптимизации производительности (если не критично)

3. **Исправление критических проблем**

   **Пример исправления:**
   ```javascript
   /**
    * Проблема: Попап не открывается при клике на точку графика
    * 
    * Причина: Отсутствует проверка наличия данных перед открытием
    * 
    * Решение:
    */
   function openEmployeeDetailsModalForLine(stageId, timePoint, employeeData) {
     // Добавить проверку наличия данных
     if (!employeeData || employeeData.length === 0) {
       console.warn('No employee data for stage:', stageId);
       notifications.warning('Нет данных о сотрудниках для этого этапа');
       return;
     }
     
     // ... остальной код ...
   }
   ```

4. **Исправление проблем с данными**

   **Пример исправления:**
   ```javascript
   /**
    * Проблема: Проценты не сходятся к 100%
    * 
    * Причина: Округление приводит к потере точности
    * 
    * Решение: Нормализация процентов
    */
   function normalizePercentages(categories, totalCount) {
     // Расчет процентов
     const percentages = categories.map(cat => ({
       ...cat,
       percentage: parseFloat(((cat.count / totalCount) * 100).toFixed(1))
     }));
     
     // Нормализация: если сумма не равна 100%, скорректировать последний элемент
     const sum = percentages.reduce((acc, cat) => acc + cat.percentage, 0);
     if (Math.abs(sum - 100) > 0.1) {
       const diff = 100 - sum;
       percentages[percentages.length - 1].percentage += diff;
       percentages[percentages.length - 1].percentage = parseFloat(
         percentages[percentages.length - 1].percentage.toFixed(1)
       );
     }
     
     return percentages;
   }
   ```

5. **Исправление проблем с навигацией**

   **Пример исправления:**
   ```javascript
   /**
    * Проблема: При быстром переключении между уровнями данные теряются
    * 
    * Причина: Асинхронные операции не завершаются
    * 
    * Решение: Добавить проверку состояния перед переходом
    */
   let isNavigating = false;
   
   async function goToLevel2(employee) {
     if (isNavigating) {
       console.warn('Navigation in progress, skipping');
       return;
     }
     
     isNavigating = true;
     
     try {
       // ... код перехода ...
     } finally {
       isNavigating = false;
     }
   }
   ```

6. **Исправление проблем с производительностью**

   **Пример оптимизации:**
   ```javascript
   /**
    * Проблема: Медленная загрузка деталей тикетов
    * 
    * Решение: Батч-загрузка и кеширование
    */
   async function loadTicketsDetailsBatch(ticketIds) {
     // Разделить на батчи по 50 тикетов
     const batchSize = 50;
     const batches = [];
     
     for (let i = 0; i < ticketIds.length; i += batchSize) {
       batches.push(ticketIds.slice(i, i + batchSize));
     }
     
     // Загрузить все батчи параллельно
     const results = await Promise.all(
       batches.map(batch => TicketDetailsService.getTicketsDetails(batch))
     );
     
     // Объединить результаты
     return results.flat();
   }
   ```

7. **Исправление проблем с адаптивностью**

   **Пример исправления:**
   ```css
   /* Проблема: Таблица не прокручивается на мобильных
    * Решение: Добавить горизонтальную прокрутку
    */
   @media (max-width: 768px) {
     .departments-table-container {
       overflow-x: auto;
       -webkit-overflow-scrolling: touch; /* Плавная прокрутка на iOS */
       scrollbar-width: thin; /* Тонкий скроллбар на Firefox */
     }
     
     .departments-table {
       min-width: 400px; /* Минимальная ширина для читаемости */
     }
   }
   ```

8. **Регрессионное тестирование**

   **После исправления каждой проблемы:**
   ```javascript
   /**
    * Регрессионный тест: Проверка, что исправление не сломало другие функции
    * 
    * Шаги:
    * 1. Исправить проблему
    * 2. Запустить все тесты из подзадач 5.1 и 5.2
    * 3. Убедиться, что все тесты проходят
    * 4. Проверить работу связанных функций
    */
   ```

   **Чек-лист регрессии:**
   - [ ] Все предыдущие тесты проходят
   - [ ] Связанные функции работают
   - [ ] Новые проблемы не появились

**Критерии завершения подзадачи 5.5:**
- [ ] Все найденные проблемы задокументированы
- [ ] Проблемы приоритизированы
- [ ] Критические проблемы исправлены
- [ ] Проблемы с данными исправлены
- [ ] Проблемы с навигацией исправлены
- [ ] Проблемы с производительностью исправлены
- [ ] Проблемы с адаптивностью исправлены
- [ ] Регрессионное тестирование выполнено

---

## 🔧 Инструменты для тестирования

### Браузерные инструменты

1. **Chrome DevTools**
   - Performance — профилирование производительности
   - Network — анализ загрузки данных
   - Memory — анализ использования памяти
   - Device Toolbar — тестирование адаптивности

2. **Vue DevTools**
   - Компоненты — проверка состояния компонентов
   - Performance — профилирование Vue-компонентов

3. **Lighthouse**
   - Проверка производительности
   - Проверка доступности
   - Проверка лучших практик

### Тестирование на реальных устройствах

1. **Мобильные устройства**
   - iPhone (Safari)
   - Android (Chrome)
   - Проверка touch-событий

2. **Планшеты**
   - iPad (Safari)
   - Android планшеты (Chrome)

---

## 📊 Метрики производительности

### Целевые показатели

**Время загрузки:**
- Загрузка деталей тикетов: < 1 сек (50 тикетов)
- Загрузка деталей тикетов: < 2 сек (100 тикетов)
- Рендеринг уровня 1: < 100ms (50 сотрудников)
- Рендеринг уровня 2: < 100ms (9 категорий)
- Рендеринг уровня 3: < 50ms (20 заказчиков)

**Производительность:**
- FPS анимаций: ~60 FPS
- Время перехода между уровнями: < 300ms
- Использование памяти: < 5MB для попапа

**Адаптивность:**
- Работа на мобильных: 320px+
- Работа на планшетах: 768px+
- Работа на десктопах: 1024px+

---

## 📝 Чек-лист финальной проверки

### Функциональность

- [ ] Попап открывается для всех типов графиков
- [ ] Все три уровня работают корректно
- [ ] Навигация между уровнями работает
- [ ] Кнопка "Назад" работает на всех уровнях
- [ ] Закрытие попапа работает всеми способами
- [ ] Хлебные крошки работают (если добавлены)

### Данные

- [ ] Группировка по временным градациям корректна
- [ ] Группировка по заказчикам корректна
- [ ] Проценты рассчитываются правильно
- [ ] Обработка пустых значений работает
- [ ] Граничные случаи обработаны

### Производительность

- [ ] Время загрузки соответствует целевым показателям
- [ ] Кеширование работает эффективно
- [ ] Рендеринг оптимизирован
- [ ] Анимации плавные (60 FPS)
- [ ] Использование памяти оптимизировано

### Адаптивность

- [ ] Работает на мобильных устройствах
- [ ] Работает на планшетах
- [ ] Работает на десктопах
- [ ] Работает в обеих ориентациях
- [ ] Стили оптимизированы для всех размеров

### Качество кода

- [ ] Код соответствует стандартам проекта
- [ ] JSDoc комментарии добавлены
- [ ] Обработка ошибок реализована
- [ ] Логирование для отладки добавлено
- [ ] Нет console.log в production коде

---

## 📚 Связанные файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа
- `vue-app/src/components/graph-state/GraphStateChart.vue` — компонент графика
- `vue-app/src/utils/graph-state/popupNavigationUtils.js` — утилиты навигации
- `vue-app/src/services/graph-state/TicketDetailsService.js` — сервис загрузки деталей
- `DOCS/TASKS/TASK-033-01-preparation-data.md` — этап 1
- `DOCS/TASKS/TASK-033-02-level-2-implementation.md` — этап 2
- `DOCS/TASKS/TASK-033-03-level-3-implementation.md` — этап 3
- `DOCS/TASKS/TASK-033-04-navigation-integration.md` — этап 4

---

## ✅ Критерии приёмки этапа 5

- [ ] Все функциональные тесты пройдены
- [ ] Все тесты данных пройдены
- [ ] Производительность оптимизирована и соответствует целевым показателям
- [ ] Адаптивность проверена на всех устройствах
- [ ] Все найденные проблемы исправлены
- [ ] Регрессионное тестирование выполнено
- [ ] Финальный чек-лист пройден
- [ ] Код готов к production

---

## 🔄 Зависимости

**Зависит от:**
- Этап 1 (TASK-033-01): Подготовка данных
- Этап 2 (TASK-033-02): Реализация уровня 2
- Этап 3 (TASK-033-03): Реализация уровня 3
- Этап 4 (TASK-033-04): Интеграция навигации

**Финальный этап:**
- После завершения этапа 5 задача TASK-033 считается завершенной

---

## 📝 История правок

- **2025-12-12 09:35 (UTC+3, Брест):** Создан документ этапа 5
  - Добавлены 5 подзадач с детальным тестированием
  - Описаны все тестовые сценарии
  - Добавлены методы оптимизации производительности
  - Описаны инструменты для тестирования
  - Добавлены целевые метрики производительности
  - Создан финальный чек-лист

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Дата:** 2025-12-12 09:35 (UTC+3, Брест)




