# TASK-032-03: Этап 3 - Создание кастомного плагина для индикатора количества

**Дата создания:** 2025-12-12 08:38 (UTC+3, Брест)  
**Дата завершения:** 2025-12-12 09:41 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-032

---

## 📋 Описание этапа

Разработка кастомного плагина Chart.js для отрисовки индикаторов количества перекрывающихся точек на линейном графике. Плагин должен анализировать график, находить позиции с 3+ перекрывающимися точками и отрисовывать бейджи с количеством перекрытий.

---

## 🎯 Цель этапа

Создать переиспользуемый плагин Chart.js, который автоматически находит перекрывающиеся точки и отрисовывает визуальные индикаторы (бейджи) для позиций с множественными перекрытиями (3+ точек).

**Ожидаемый результат:**
Кастомный плагин `overlappingPointsPlugin`, который интегрируется с Chart.js и отрисовывает бейджи с количеством перекрывающихся точек на позициях с 3+ перекрытиями.

---

## 📝 Подзадачи

### Подзадача 3.1: Создание структуры плагина

**Цель:** Создать файл плагина с базовой структурой Chart.js плагина.

**Задачи:**
1. Создать директорию `vue-app/src/components/graph-state/plugins/` (если не существует)
2. Создать файл `overlappingPointsPlugin.js`
3. Добавить базовую структуру плагина Chart.js
4. Экспортировать плагин

**Технические детали:**
- Путь: `vue-app/src/components/graph-state/plugins/overlappingPointsPlugin.js`
- Формат: ES6 модуль с именованным экспортом
- Структура: объект с обязательными полями `id` и методами жизненного цикла

**Базовая структура:**
```javascript
/**
 * Плагин Chart.js для отрисовки индикаторов количества перекрывающихся точек
 * 
 * @module overlappingPointsPlugin
 */

import { findOverlappingPoints } from '@/utils/graph-state/lineChartUtils.js';

export const overlappingPointsPlugin = {
  id: 'overlappingPointsPlugin',
  
  /**
   * Вызывается после отрисовки всех datasets
   * 
   * @param {Object} chart - Экземпляр Chart.js
   */
  afterDatasetsDraw: (chart) => {
    // Реализация отрисовки индикаторов
  }
};
```

**Критерии завершения:**
- [ ] Файл `overlappingPointsPlugin.js` создан
- [ ] Добавлена базовая структура плагина
- [ ] Плагин экспортирован
- [ ] Добавлены JSDoc комментарии

---

### Подзадача 3.2: Реализация логики анализа перекрытий

**Цель:** Интегрировать утилиту `findOverlappingPoints` в плагин для анализа перекрытий.

**Задачи:**
1. Импортировать функцию `findOverlappingPoints` из утилиты
2. Вызвать функцию для получения всех перекрытий
3. Отфильтровать перекрытия с количеством 3+ (порог отображения)
4. Сохранить результаты для отрисовки

**Технические детали:**
- Использовать утилиту из Этапа 2: `findOverlappingPoints(chart, threshold)`
- Порог threshold: 0.5 единицы Y
- Порог отображения: только перекрытия с count >= 3
- Проверка типа графика: только для `'line'`

**Реализация:**
```javascript
afterDatasetsDraw: (chart) => {
  // Проверка типа графика
  if (chart.config.type !== 'line') {
    return;
  }
  
  // Найти все перекрывающиеся точки
  const overlaps = findOverlappingPoints(chart, 0.5);
  
  // Отфильтровать только перекрытия с 3+ точками
  const significantOverlaps = overlaps.filter(overlap => overlap.count >= 3);
  
  // Отрисовка бейджей для каждого значимого перекрытия
  significantOverlaps.forEach(overlap => {
    drawOverlapBadge(chart, overlap);
  });
}
```

**Критерии завершения:**
- [ ] Утилита `findOverlappingPoints` импортирована
- [ ] Функция вызывается для анализа перекрытий
- [ ] Перекрытия фильтруются по порогу (3+)
- [ ] Проверка типа графика реализована

---

### Подзадача 3.3: Реализация функции отрисовки бейджа

**Цель:** Создать функцию для отрисовки бейджа с количеством перекрытий.

**Задачи:**
1. Реализовать функцию `drawOverlapBadge(chart, overlap)`
2. Отрисовать круглый фон бейджа
3. Отрисовать текст с количеством перекрытий
4. Применить стили (цвет, размер, тень)

**Технические детали:**
- Позиция: справа-сверху от точки (x + 8px, y - 8px)
- Размер: радиус 9px (диаметр 18px)
- Цвет фона: `#ef4444` (Bitrix24 danger color)
- Цвет текста: `#ffffff` (белый)
- Шрифт: `bold 10px Arial, sans-serif`
- Тень: `rgba(0, 0, 0, 0.3)`, blur: 4px, offset: (0, 2px)

**Реализация:**
```javascript
/**
 * Отрисовывает бейдж с количеством перекрывающихся точек
 * 
 * Бейдж отрисовывается справа-сверху от точки с перекрытиями.
 * Используется красный цвет (#ef4444) для привлечения внимания.
 * 
 * @param {Object} chart - Экземпляр Chart.js
 * @param {Object} overlap - Объект с информацией о перекрытии
 * @param {number} overlap.x - X-координата точки на canvas
 * @param {number} overlap.y - Y-координата точки на canvas
 * @param {number} overlap.count - Количество перекрывающихся точек
 * 
 * @throws {Error} Если координаты невалидны или canvas недоступен
 */
function drawOverlapBadge(chart, overlap) {
  // Валидация входных данных
  if (!chart || !chart.ctx) {
    console.warn('drawOverlapBadge: chart или ctx недоступен');
    return;
  }
  
  if (!overlap || typeof overlap.x !== 'number' || typeof overlap.y !== 'number') {
    console.warn('drawOverlapBadge: невалидные координаты overlap', overlap);
    return;
  }
  
  const count = overlap.count;
  if (typeof count !== 'number' || count < 2) {
    console.warn('drawOverlapBadge: невалидное количество перекрытий', count);
    return;
  }
  
  const ctx = chart.ctx;
  const { x, y } = overlap;
  
  // Позиция бейджа (справа-сверху от точки)
  // Смещение: 8px вправо, 8px вверх
  const badgeX = x + 8;
  const badgeY = y - 8;
  const badgeRadius = 9; // Радиус бейджа (диаметр 18px)
  
  // Проверка, что координаты в пределах canvas
  const canvas = ctx.canvas;
  if (!canvas) {
    return;
  }
  
  // Сохранение состояния canvas (важно для восстановления после отрисовки)
  ctx.save();
  
  try {
    // Отрисовка тени (под бейджем для лучшей видимости)
    ctx.beginPath();
    ctx.arc(badgeX, badgeY + 2, badgeRadius, 0, 2 * Math.PI);
    ctx.fillStyle = 'rgba(0, 0, 0, 0.3)'; // Полупрозрачная чёрная тень
    ctx.fill();
    
    // Отрисовка фона бейджа (круг)
    ctx.beginPath();
    ctx.arc(badgeX, badgeY, badgeRadius, 0, 2 * Math.PI);
    ctx.fillStyle = '#ef4444'; // Bitrix24 danger color (красный)
    ctx.fill();
    
    // Отрисовка текста с количеством перекрытий
    ctx.fillStyle = '#ffffff'; // Белый текст
    ctx.font = 'bold 10px Arial, sans-serif'; // Жирный шрифт для читаемости
    ctx.textAlign = 'center'; // Горизонтальное выравнивание по центру
    ctx.textBaseline = 'middle'; // Вертикальное выравнивание по центру
    
    // Преобразовать count в строку (на случай, если передано число)
    const countText = String(count);
    ctx.fillText(countText, badgeX, badgeY);
    
  } catch (error) {
    // Логировать ошибку, но не прерывать работу плагина
    console.warn('drawOverlapBadge: ошибка при отрисовке бейджа:', error);
  } finally {
    // Всегда восстанавливать состояние canvas
    ctx.restore();
  }
}
```

**Важные детали:**

1. **Валидация данных:**
   - Проверка наличия `chart.ctx` (контекст canvas)
   - Проверка валидности координат (должны быть числа)
   - Проверка валидности `count` (должно быть число >= 2)

2. **Сохранение/восстановление состояния canvas:**
   - `ctx.save()` сохраняет текущее состояние (цвета, шрифты, трансформации)
   - `ctx.restore()` восстанавливает состояние после отрисовки
   - Важно для предотвращения влияния на остальную отрисовку графика

3. **Отрисовка тени:**
   - Тень отрисовывается на 2px ниже бейджа (`badgeY + 2`)
   - Используется полупрозрачный чёрный цвет для эффекта глубины
   - Тень помогает бейджу выделяться на фоне графика

4. **Отрисовка текста:**
   - Текст центрируется в бейдже (`textAlign: 'center'`, `textBaseline: 'middle'`)
   - Используется белый цвет для контраста с красным фоном
   - Шрифт: `bold 10px Arial` для читаемости на маленьком размере

5. **Обработка ошибок:**
   - Try-catch блок для обработки ошибок отрисовки
   - Ошибки логируются, но не прерывают работу плагина
   - `finally` блок гарантирует восстановление состояния canvas

**Оптимизация:**
- Проверка координат перед отрисовкой (можно добавить проверку границ canvas)
- Кеширование стилей (если отрисовывается много бейджей)

**Критерии завершения:**
- [ ] Функция `drawOverlapBadge` реализована
- [ ] Бейдж отрисовывается с правильными размерами и цветами
- [ ] Текст центрирован в бейдже
- [ ] Тень применяется корректно
- [ ] Позиционирование справа-сверху от точки

---

### Подзадача 3.4: Интеграция функции отрисовки в плагин

**Цель:** Интегрировать функцию отрисовки в метод `afterDatasetsDraw` плагина.

**Задачи:**
1. Добавить функцию `drawOverlapBadge` в плагин
2. Вызвать функцию для каждого значимого перекрытия
3. Обработать ошибки (try-catch)
4. Добавить проверки на валидность данных

**Реализация:**
```javascript
/**
 * Плагин Chart.js для отрисовки индикаторов количества перекрывающихся точек
 * 
 * Плагин анализирует линейный график, находит позиции с 3+ перекрывающимися точками
 * и отрисовывает красные бейджи с количеством перекрытий.
 * 
 * @module overlappingPointsPlugin
 */

import { findOverlappingPoints } from '@/utils/graph-state/lineChartUtils.js';

/**
 * Порог отображения индикатора (минимальное количество перекрытий)
 * Индикатор отображается только при 3+ перекрывающихся точках
 */
const MIN_OVERLAP_COUNT = 3;

/**
 * Порог для определения перекрытия точек (в единицах Y)
 * Точки считаются перекрывающимися, если их значения отличаются менее чем на 0.5
 */
const OVERLAP_THRESHOLD = 0.5;

export const overlappingPointsPlugin = {
  id: 'overlappingPointsPlugin',
  
  /**
   * Вызывается после отрисовки всех datasets графика
   * 
   * В этом методе происходит анализ перекрытий и отрисовка бейджей.
   * Метод вызывается Chart.js автоматически при каждой перерисовке графика.
   * 
   * @param {Object} chart - Экземпляр Chart.js
   */
  afterDatasetsDraw: (chart) => {
    // Проверка типа графика (плагин работает только для линейных графиков)
    if (!chart || !chart.config || chart.config.type !== 'line') {
      return;
    }
    
    // Проверка наличия canvas контекста
    if (!chart.ctx) {
      return;
    }
    
    try {
      // Найти все перекрывающиеся точки на графике
      const overlaps = findOverlappingPoints(chart, OVERLAP_THRESHOLD);
      
      if (!Array.isArray(overlaps) || overlaps.length === 0) {
        return; // Нет перекрытий
      }
      
      // Отфильтровать только перекрытия с достаточным количеством точек
      // и валидными координатами
      const significantOverlaps = overlaps.filter(overlap => {
        // Проверка наличия объекта overlap
        if (!overlap || typeof overlap !== 'object') {
          return false;
        }
        
        // Проверка количества перекрытий (должно быть >= MIN_OVERLAP_COUNT)
        if (typeof overlap.count !== 'number' || overlap.count < MIN_OVERLAP_COUNT) {
          return false;
        }
        
        // Проверка валидности координат
        if (typeof overlap.x !== 'number' || typeof overlap.y !== 'number') {
          return false;
        }
        
        // Проверка, что координаты не NaN и не Infinity
        if (!isFinite(overlap.x) || !isFinite(overlap.y)) {
          return false;
        }
        
        return true;
      });
      
      // Отрисовка бейджей для каждого значимого перекрытия
      significantOverlaps.forEach(overlap => {
        try {
          drawOverlapBadge(chart, overlap);
        } catch (error) {
          // Логировать ошибку для конкретного бейджа, но продолжить отрисовку остальных
          console.warn(`overlappingPointsPlugin: ошибка при отрисовке бейджа для позиции ${overlap.position}:`, error);
        }
      });
      
    } catch (error) {
      // Логирование ошибки без прерывания работы графика
      // Плагин не должен ломать основной график при ошибках
      console.warn('overlappingPointsPlugin: ошибка в afterDatasetsDraw:', error);
    }
  }
};

/**
 * Отрисовывает бейдж с количеством перекрывающихся точек
 * (реализация функции выше)
 */
function drawOverlapBadge(chart, overlap) {
  // ... реализация из предыдущего блока ...
}
```

**Важные детали:**

1. **Константы:**
   - `MIN_OVERLAP_COUNT = 3` - минимальное количество перекрытий для отображения индикатора
   - `OVERLAP_THRESHOLD = 0.5` - порог для определения перекрытия (совпадает с утилитой)

2. **Валидация в afterDatasetsDraw:**
   - Проверка наличия `chart` и `chart.config`
   - Проверка типа графика
   - Проверка наличия `chart.ctx` (контекст canvas)

3. **Фильтрация перекрытий:**
   - Множественные проверки валидности данных
   - Проверка на `isFinite()` для координат (исключает NaN и Infinity)
   - Фильтрация по минимальному количеству перекрытий

4. **Обработка ошибок:**
   - Внешний try-catch для всей логики плагина
   - Внутренний try-catch для каждого бейджа
   - Ошибки логируются, но не прерывают работу

5. **Производительность:**
   - Функция вызывается при каждой перерисовке графика
   - В Этапе 5 будет добавлено кеширование результатов анализа

/**
 * Отрисовывает бейдж с количеством перекрывающихся точек
 */
function drawOverlapBadge(chart, overlap) {
  // ... реализация из Подзадачи 3.3
}
```

**Критерии завершения:**
- [ ] Функция отрисовки интегрирована в плагин
- [ ] Добавлена обработка ошибок
- [ ] Добавлены проверки на валидность данных
- [ ] Плагин готов к использованию

---

### Подзадача 3.5: Тестирование плагина

**Цель:** Протестировать плагин на тестовых данных.

**Задачи:**
1. Создать тестовый график с известными перекрытиями
2. Зарегистрировать плагин
3. Проверить отрисовку бейджей
4. Проверить обработку edge cases

**Тестирование:**
1. Создать график с позициями, имеющими 3+ перекрытия
2. Зарегистрировать плагин: `ChartJS.register(overlappingPointsPlugin)`
3. Проверить, что бейджи отрисовываются только при 3+ перекрытиях
4. Проверить, что бейджи не отрисовываются при 1-2 перекрытиях
5. Проверить стилизацию бейджей

**Критерии завершения:**
- [ ] Плагин корректно анализирует перекрытия
- [ ] Бейджи отрисовываются только при 3+ перекрытиях
- [ ] Бейджи стилизованы в соответствии с требованиями
- [ ] Edge cases обрабатываются корректно
- [ ] Нет ошибок в консоли

---

## 📁 Новые файлы

1. **`vue-app/src/components/graph-state/plugins/overlappingPointsPlugin.js`**
   - Экспорт плагина `overlappingPointsPlugin`
   - Функция `drawOverlapBadge(chart, overlap)`

---

## 🔧 Технические детали

### Структура плагина

```javascript
export const overlappingPointsPlugin = {
  id: 'overlappingPointsPlugin',  // Уникальный ID плагина
  
  afterDatasetsDraw: (chart) => {
    // Вызывается после отрисовки всех datasets
    // Здесь происходит отрисовка бейджей поверх графика
  }
};
```

### Параметры функции drawOverlapBadge

**`drawOverlapBadge(chart, overlap)`**
- `chart` (Object, обязательный) — экземпляр Chart.js
- `overlap` (Object, обязательный) — объект с информацией о перекрытии:
  ```javascript
  {
    position: 0,        // Индекс позиции
    count: 3,           // Количество перекрытий
    x: 100,             // X-координата на canvas
    y: 50,              // Y-координата на canvas
    value: 10,          // Значение Y
    points: [...]       // Массив перекрывающихся точек
  }
  ```

### Стилизация бейджа

- **Форма:** Круг
- **Размер:** Радиус 9px (диаметр 18px)
- **Цвет фона:** `#ef4444` (Bitrix24 danger color - красный)
- **Цвет текста:** `#ffffff` (белый)
- **Шрифт:** `bold 10px Arial, sans-serif`
- **Выравнивание:** По центру (textAlign: 'center', textBaseline: 'middle')
- **Тень:** 
  - Цвет: `rgba(0, 0, 0, 0.3)`
  - Blur: 4px
  - Offset: (0, 2px)

### Позиционирование

- **Смещение от точки:** x + 8px, y - 8px (справа-сверху)
- **Обоснование:** Бейдж не перекрывает саму точку, но находится достаточно близко для визуальной связи

### Порог отображения

- **Минимальное количество перекрытий:** 3
- **Обоснование:** При 1-2 перекрытиях стили точек (из Этапа 1) достаточно для визуального различия. Индикатор нужен только при большом количестве перекрытий.

---

## ✅ Критерии приёмки

### Обязательные:

- [ ] Файл `overlappingPointsPlugin.js` создан
- [ ] Плагин экспортирован и имеет правильную структуру
- [ ] Плагин использует утилиту `findOverlappingPoints` для анализа
- [ ] Бейджи отрисовываются только при 3+ перекрывающихся точках
- [ ] Бейджи стилизованы в соответствии с требованиями (цвет, размер, шрифт)
- [ ] Позиционирование бейджей корректно (справа-сверху от точки)
- [ ] Плагин работает только для линейных графиков
- [ ] Добавлена обработка ошибок
- [ ] Нет ошибок отрисовки

### Дополнительные:

- [ ] Добавлены JSDoc комментарии
- [ ] Код соответствует стандартам проекта
- [ ] Плагин протестирован на различных сценариях
- [ ] Производительность не ухудшилась

---

## 🧪 Тестирование

### Сценарий 1: Базовое тестирование

1. Создать тестовый график с позицией, имеющей 3 перекрывающихся точки
2. Зарегистрировать плагин
3. Проверить, что бейдж отрисовывается

**Ожидаемый результат:** Бейдж с цифрой "3" отображается справа-сверху от точки.

---

### Сценарий 2: Тестирование порога отображения

1. Создать график с позициями, имеющими 1, 2, 3, 4 перекрытия
2. Проверить отображение бейджей

**Ожидаемый результат:** 
- Бейджи отображаются только для позиций с 3+ перекрытиями
- Позиции с 1-2 перекрытиями не имеют бейджей

---

### Сценарий 3: Тестирование стилизации

1. Проверить размер бейджа (18px диаметр)
2. Проверить цвет фона (красный #ef4444)
3. Проверить цвет текста (белый)
4. Проверить тень

**Ожидаемый результат:** Все стили соответствуют требованиям.

---

### Сценарий 4: Тестирование позиционирования

1. Проверить позицию бейджа относительно точки
2. Убедиться, что бейдж не перекрывает точку
3. Убедиться, что бейдж визуально связан с точкой

**Ожидаемый результат:** Бейдж позиционируется справа-сверху от точки с отступом 8px.

---

### Сценарий 5: Тестирование edge cases

1. График без перекрытий
2. График с одним dataset
3. График с пустыми данными

**Ожидаемый результат:** Все edge cases обрабатываются корректно, плагин не выбрасывает ошибки.

---

## ⚠️ Типичные ошибки и решения

### Ошибка 1: Плагин не вызывается

**Симптом:** Бейджи не отрисовываются, нет ошибок в консоли

**Причина:** Плагин не зарегистрирован или зарегистрирован неправильно

**Решение:**
1. Проверить регистрацию плагина:
   ```javascript
   console.log(ChartJS.registry.plugins.get('overlappingPointsPlugin'));
   // Должен вернуть объект плагина
   ```
2. Убедиться, что плагин зарегистрирован до создания графика
3. Проверить, что `chart.config.type === 'line'` (плагин работает только для линейных графиков)

---

### Ошибка 2: "findOverlappingPoints is not a function"

**Симптом:** Ошибка при импорте утилиты

**Причина:** Неправильный путь импорта или функция не экспортирована

**Решение:**
1. Проверить путь импорта:
   ```javascript
   // Правильный путь (если плагин в components/graph-state/plugins/)
   import { findOverlappingPoints } from '@/utils/graph-state/lineChartUtils.js';
   ```
2. Убедиться, что функция экспортирована в утилите:
   ```javascript
   export function findOverlappingPoints(chart, threshold) { ... }
   ```
3. Проверить, что файл утилиты существует

---

### Ошибка 3: Бейджи отрисовываются в неправильных местах

**Симптом:** Бейджи не рядом с точками или вообще не видны

**Причина:** Неправильные координаты или координаты в неправильной системе отсчёта

**Решение:**
1. Проверить координаты перед отрисовкой:
   ```javascript
   console.log('Overlap coordinates:', overlap.x, overlap.y);
   console.log('Badge position:', badgeX, badgeY);
   ```
2. Убедиться, что координаты в пикселях canvas, а не в единицах данных
3. Проверить, что смещение (8px) подходит для размера графика

---

### Ошибка 4: Canvas context lost

**Симптом:** Ошибка "Cannot read property 'fillStyle' of null" или подобная

**Причина:** Canvas контекст потерян или недоступен

**Решение:**
1. Проверить наличие `chart.ctx` перед использованием:
   ```javascript
   if (!chart || !chart.ctx) {
     return;
   }
   ```
2. Убедиться, что canvas элемент существует и валиден
3. Проверить, что график не уничтожен во время отрисовки

---

## 📝 История правок

- **2025-12-12 08:38 (UTC+3, Брест):** Создан документ TASK-032-03 с детальным планом создания плагина для индикатора количества
- **2025-12-12 09:41 (UTC+3, Брест):** Задача успешно завершена - плагин реализован с функцией увеличения размера точек при перекрытии

---

## 🔗 Связанные задачи

- **TASK-032:** Визуализация перекрывающихся точек на линейном графике состояния сектора 1С (родительская задача)
- **TASK-032-02:** Этап 2 - Создание утилиты для анализа перекрытий (предыдущий этап)
- **TASK-032-04:** Этап 4 - Интеграция плагина в компонент GraphStateChart (следующий этап)

---

## 📚 Дополнительная информация

### Использованные технологии:

- JavaScript (ES6+)
- Chart.js 4.x Plugins API
- Canvas API для отрисовки

### Документация:

- Chart.js Plugins: https://www.chartjs.org/docs/latest/developers/plugins.html
- Chart.js Plugin Lifecycle: https://www.chartjs.org/docs/latest/developers/plugins.html#plugin-lifecycle
- Canvas API: https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API

### Best Practices:

- Плагины Chart.js должны иметь уникальный ID
- Использовать `afterDatasetsDraw` для отрисовки поверх графика
- Всегда сохранять и восстанавливать состояние canvas (save/restore)
- Обрабатывать ошибки без прерывания работы графика

---

**Статус:** ✅ Завершена  
**Реализовано:** Плагин overlappingPointsPlugin.js создан, реализовано увеличение размера точек при перекрытии (вариант 3)






