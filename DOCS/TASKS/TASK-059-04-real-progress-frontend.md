# TASK-059-04: Реальный прогресс загрузки (Frontend)

**Дата создания:** 2025-12-18 11:00 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)  
**Подзадача:** Подзадача 4 из TASK-059

---

## 📋 Описание

Улучшить UX загрузки данных в режиме "3 месяца" модуля "График приёма и закрытий сектора 1С". Проблема: прогресс не отражает реальное состояние загрузки, пользователь видит симуляцию прогресса до 95%, затем долгое ожидание без обновления.

**Цель:** Реализовать реальный прогресс загрузки, отражающий этапы выполнения запросов к API.

---

## 🎯 Контекст

### Текущая проблема:

**Симуляция прогресса не отражает реальное состояние:**

```javascript
// Текущая реализация (GraphAdmissionClosureMonthsDashboard.vue)
// Этап 5: Агрегация данных (90-95%)
await updateProgress(95, 'Агрегация данных...', 400);

// Выполняем реальный запрос к API (15-30 секунд, прогресс не обновляется)
const result = await fetchAdmissionClosureStats({...});

// Этап 6: Формирование графиков (95-100%)
await updateProgress(100, 'Формирование графиков...', 300);
```

**Проблемы:**
- Прогресс до 95% — это симуляция (быстро, ~2-3 секунды)
- Реальный запрос к API происходит после 95%, но прогресс не обновляется
- Пользователь видит 95% и ждет, не понимая, что происходит
- Плохой UX

### Решение:

Использовать реальный прогресс на основе этапов загрузки:
- Этап 1: Подготовка данных (0-5%)
- Этап 2: Запрос к API (5-90%)
  - Подэтап 2.1: Запрос 1 — Тикеты, созданные за период (5-35%)
  - Подэтап 2.2: Запрос 2 — Тикеты, закрытые за период (35-65%)
  - Подэтап 2.3: Запрос 3 — Переходящие тикеты (65-85%)
  - Подэтап 2.4: Агрегация данных (85-90%)
- Этап 3: Формирование графиков (90-100%)

**Подход:**
- Использовать промежуточные обновления прогресса на основе времени
- Или добавить поддержку прогресса в API (опционально, более сложно)

---

## 🏗️ Модули и компоненты

### Изменяемые файлы:
- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureMonthsDashboard.vue` — главный компонент
  - Функция `loadData()` — загрузка данных (строки 204-262)
  - Функция `updateProgress()` — обновление прогресса (строки 186-202)
- `vue-app/src/services/graph-admission-closure/admissionClosureService.js` — сервис для работы с API

### Опциональные изменения (Backend):
- `api/graph-1c-admission-closure.php` — добавление поддержки прогресса (опционально)

---

## 📦 Зависимости

- **От задач:** 
  - TASK-059: Оптимизация загрузки режима 3 месяца
- **От модулей:** 
  - Модуль 3: График приёма и закрытий сектора 1С
  - TASK-053: Режим выбора периода 3 месяца

---

## 🎯 Ступенчатые подзадачи

### Подзадача 4.1: Анализ текущей реализации прогресса

**Цель:** Понять текущую логику отображения прогресса.

**Шаги:**

1. **Изучить текущий код прогресса**
   - Файл: `GraphAdmissionClosureMonthsDashboard.vue` (строки 204-262)
   - Понять логику симуляции прогресса
   - Понять этапы загрузки

2. **Изучить компонент прелоадера**
   - Файл: `MonthsModePreloader.vue`
   - Понять, как отображается прогресс
   - Понять, какие этапы показываются

3. **Определить этапы реальной загрузки**
   - Запрос 1: Тикеты, созданные за период
   - Запрос 2: Тикеты, закрытые за период
   - Запрос 3: Переходящие тикеты
   - Агрегация данных
   - Формирование графиков

**Критерии приёмки:**
- [ ] Текущая реализация прогресса изучена
- [ ] Этапы реальной загрузки определены

---

### Подзадача 4.2: Реализация промежуточных обновлений прогресса

**Цель:** Реализовать промежуточные обновления прогресса на основе времени выполнения запросов.

**Шаги:**

1. **Создать функцию для отслеживания прогресса запроса**
   - Параметры: общее время запроса, текущее время
   - Возвращает: процент выполнения
   - Обновляет прогресс через промежуточные интервалы

2. **Обновить функцию `loadData()`**
   - Убрать симуляцию прогресса
   - Добавить промежуточные обновления во время выполнения запроса
   - Обновлять прогресс на основе времени выполнения

3. **Добавить этапы загрузки**
   - Этап 1: Подготовка данных (0-5%)
   - Этап 2: Запрос к API (5-90%)
     - Подэтап 2.1: Запрос 1 (5-35%)
     - Подэтап 2.2: Запрос 2 (35-65%)
     - Подэтап 2.3: Запрос 3 (65-85%)
     - Подэтап 2.4: Агрегация данных (85-90%)
   - Этап 3: Формирование графиков (90-100%)

**Критерии приёмки:**
- [ ] Промежуточные обновления прогресса реализованы
- [ ] Прогресс отражает реальное состояние загрузки
- [ ] Этапы загрузки отображаются корректно

**Пример кода:**

```javascript
/**
 * Загрузка данных с реальным прогрессом
 * 
 * TASK-059-04: Реальный прогресс загрузки
 */
async function loadData() {
  isLoading.value = true;
  error.value = null;
  loadingProgress.value = 0;
  currentLoadingStage.value = 'Подготовка данных...';
  
  try {
    // Этап 1: Подготовка данных (0-5%)
    await updateProgress(5, 'Подготовка данных...', 200);
    
    // Этап 2: Запрос к API с промежуточными обновлениями (5-90%)
    const apiStartTime = Date.now();
    const estimatedApiTime = 20000; // Оценочное время: 20 секунд
    
    // Создаём промис для отслеживания прогресса
    const progressTracker = setInterval(() => {
      const elapsed = Date.now() - apiStartTime;
      const progress = Math.min(5 + (elapsed / estimatedApiTime) * 85, 90);
      loadingProgress.value = progress;
      
      // Обновляем этап на основе прогресса
      if (progress < 35) {
        currentLoadingStage.value = 'Загрузка данных за период (запрос 1)...';
      } else if (progress < 65) {
        currentLoadingStage.value = 'Загрузка данных за период (запрос 2)...';
      } else if (progress < 85) {
        currentLoadingStage.value = 'Загрузка данных за период (запрос 3)...';
      } else {
        currentLoadingStage.value = 'Агрегация данных...';
      }
    }, 500); // Обновление каждые 500ms
    
    // Выполняем реальный запрос к API
    const result = await fetchAdmissionClosureStats({
      product: '1C',
      periodMode: 'months',
      includeTickets: true
    });
    
    // Останавливаем отслеживание прогресса
    clearInterval(progressTracker);
    
    // Устанавливаем прогресс на 90% после завершения запроса
    loadingProgress.value = 90;
    currentLoadingStage.value = 'Агрегация данных завершена';
    
    // Этап 3: Формирование графиков (90-100%)
    await updateProgress(100, 'Формирование графиков...', 300);
    
    const { meta, data } = result;
    chartMeta.value = meta;
    chartData.value = data;
    
    // Небольшая задержка перед скрытием прелоадера
    await new Promise(resolve => setTimeout(resolve, 200));
  } catch (err) {
    console.error('[DEBUG MonthsDashboard] API call failed:', err);
    error.value = err instanceof Error ? err : new Error('Неизвестная ошибка загрузки');
    loadingProgress.value = 100;
  } finally {
    isLoading.value = false;
  }
}
```

---

### Подзадача 4.3: Улучшение отображения этапов загрузки

**Цель:** Улучшить отображение этапов загрузки в компоненте прелоадера.

**Шаги:**

1. **Обновить компонент `MonthsModePreloader.vue`**
   - Добавить больше этапов загрузки
   - Улучшить отображение текущего этапа
   - Добавить индикацию времени выполнения

2. **Обновить список этапов**
   - Подготовка данных
   - Запрос 1: Тикеты, созданные за период
   - Запрос 2: Тикеты, закрытые за период
   - Запрос 3: Переходящие тикеты
   - Агрегация данных
   - Формирование графиков

3. **Добавить индикацию времени**
   - Показывать оценочное время выполнения
   - Показывать прошедшее время

**Критерии приёмки:**
- [ ] Компонент прелоадера обновлён
- [ ] Этапы загрузки отображаются корректно
- [ ] Индикация времени добавлена

**Пример кода:**

```vue
<!-- MonthsModePreloader.vue -->
<template>
  <div class="months-preloader">
    <!-- ... существующий код ... -->
    
    <div class="stages-list">
      <div
        v-for="(stage, index) in stages"
        :key="index"
        class="stage-item"
        :class="{
          'stage-completed': isStageCompleted(index),
          'stage-current': isStageCurrent(index)
        }"
      >
        <span class="stage-icon">
          {{ isStageCompleted(index) ? '✓' : isStageCurrent(index) ? '→' : '○' }}
        </span>
        <span class="stage-text">{{ stage.label }}</span>
        <span v-if="isStageCurrent(index)" class="stage-time">
          {{ elapsedTime }} сек
        </span>
      </div>
    </div>
  </div>
</template>

<script setup>
import { computed, ref, onMounted, onUnmounted } from 'vue';

const props = defineProps({
  progress: {
    type: Number,
    required: true,
    validator: (value) => value >= 0 && value <= 100
  },
  currentStage: {
    type: String,
    default: ''
  }
});

const stages = [
  { label: 'Подготовка данных...', threshold: 5 },
  { label: 'Загрузка данных (запрос 1)...', threshold: 35 },
  { label: 'Загрузка данных (запрос 2)...', threshold: 65 },
  { label: 'Загрузка данных (запрос 3)...', threshold: 85 },
  { label: 'Агрегация данных...', threshold: 90 },
  { label: 'Формирование графиков...', threshold: 100 }
];

const startTime = ref(Date.now());
const elapsedTime = ref(0);

let timeInterval = null;

onMounted(() => {
  timeInterval = setInterval(() => {
    elapsedTime.value = Math.floor((Date.now() - startTime.value) / 1000);
  }, 1000);
});

onUnmounted(() => {
  if (timeInterval) {
    clearInterval(timeInterval);
  }
});

// ... остальной код ...
</script>
```

---

### Подзадача 4.4: Оптимизация обновлений прогресса

**Цель:** Оптимизировать частоту обновлений прогресса для лучшей производительности.

**Шаги:**

1. **Оптимизировать частоту обновлений**
   - Обновлять прогресс не чаще, чем каждые 200-500ms
   - Использовать `requestAnimationFrame` для плавности

2. **Добавить дебаунсинг обновлений**
   - Избегать слишком частых обновлений DOM
   - Группировать обновления прогресса

3. **Оптимизировать вычисления**
   - Кешировать вычисления прогресса
   - Избегать лишних вычислений

**Критерии приёмки:**
- [ ] Частота обновлений оптимизирована
- [ ] Производительность улучшена
- [ ] Плавность анимации сохранена

---

### Подзадача 4.5: Тестирование реального прогресса

**Цель:** Протестировать корректность работы реального прогресса.

**Шаги:**

1. **Функциональное тестирование**
   - Проверить отображение этапов загрузки
   - Проверить обновление прогресса
   - Проверить корректность времени выполнения

2. **UX тестирование**
   - Проверить, понятен ли пользователю прогресс
   - Проверить, улучшился ли UX
   - Собрать обратную связь

3. **Производительность**
   - Проверить влияние обновлений прогресса на производительность
   - Убедиться, что обновления не замедляют загрузку

**Критерии приёмки:**
- [ ] Прогресс отражает реальное состояние загрузки
- [ ] Пользователь видит этапы загрузки
- [ ] UX улучшен
- [ ] Производительность не ухудшилась

---

## 🔧 Технические требования

### Frontend (Vue.js):
- Vue.js 3.x
- Поддержка промежуточных обновлений прогресса
- Оптимизация частоты обновлений

### Производительность:
- **Частота обновлений:** не чаще, чем каждые 200-500ms
- **Плавность анимации:** сохранена

---

## ✅ Критерии приёмки

### Общие критерии:
- [ ] Прогресс отражает реальное состояние загрузки
- [ ] Пользователь видит этапы загрузки (запросы к API, агрегация)
- [ ] UX улучшен (пользователь понимает, что происходит)
- [ ] Код соответствует стандартам ESLint
- [ ] Комментарии в коде добавлены
- [ ] Производительность не ухудшилась

### Детальные критерии:
- [ ] Симуляция прогресса убрана
- [ ] Промежуточные обновления прогресса реализованы
- [ ] Этапы загрузки отображаются корректно
- [ ] Индикация времени добавлена (опционально)

---

## 🧪 Тестирование

### Функциональное тестирование:
1. Проверить отображение этапов загрузки
2. Проверить обновление прогресса во время выполнения запроса
3. Проверить корректность времени выполнения

### UX тестирование:
1. Проверить, понятен ли пользователю прогресс
2. Проверить, улучшился ли UX
3. Собрать обратную связь от пользователей

### Производительность:
1. Проверить влияние обновлений прогресса на производительность
2. Убедиться, что обновления не замедляют загрузку
3. Проверить плавность анимации

---

## 📝 Примечания

### Альтернативный подход (опционально):

Если добавить поддержку прогресса в API сложно, можно использовать:
- Промежуточные обновления на основе времени (реализовано выше)
- WebSocket для реального прогресса (более сложно)
- Server-Sent Events (SSE) для прогресса (более сложно)

### Рекомендации:

1. **Начать с простого подхода:** Промежуточные обновления на основе времени
2. **Улучшить позже:** Добавить поддержку прогресса в API (если нужно)
3. **Собрать обратную связь:** Узнать, улучшился ли UX

---

## 🔗 Связанные документы

- [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)
- [Анализ оптимизации](../../REFACTOR/PERFORMANCE/optimization-3-months-mode-loading.md)
- [Модуль 3: График приёма и закрытий сектора 1С](../../MODULES/module-03-graph-admission-closure/README.md)

---

## 📊 История правок

- **2025-12-18 11:00 (UTC+3, Брест):** Создана подзадача для реального прогресса загрузки

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

