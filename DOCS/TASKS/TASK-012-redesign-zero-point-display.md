# TASK-012: Редизайн отображения нулевой точки в дашборде 1С

**Дата создания:** 2025-12-06 17:21 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js)

---

## Описание

Изменить отображение нулевой точки в дашборде сектора 1С на уровне дизайна. Нулевая точка работает по логике: пользователь 1051 (Хранитель объектов) как ответственный по тикету.

**Текущее отображение:**
- Иконка 📥
- Заголовок: `[0] Нулевая точка`
- Счётчик: `(14)` (количество тикетов)
- Описание: "Входящие тикеты" и "Перетащите тикет на сотрудника"

**Новое отображение:**
- Убрать иконку `[0]` из заголовка
- Убрать счётчик `(count)` из заголовка
- Полностью убрать блок описания "Входящие тикеты. Перетащите тикет на сотрудника"
- Если нет тикетов (на уровне стадии нет тикетов по сотруднику 1051), показать fallback: "Нет неразобранных тикетов в стадии"

---

## Контекст

В дашборде сектора 1С есть блок "Нулевая точка" (Неразобранное), который отображает тикеты с ответственным 1051 (Хранитель объектов). Текущий дизайн содержит избыточную информацию, которую нужно упростить для лучшего UX.

**Бизнес-логика:**
- Нулевая точка = тикеты с ответственным 1051 (Хранитель объектов)
- Тикеты отображаются только в нулевой точке соответствующего этапа
- Если тикетов нет, показывается fallback-сообщение

---

## Модули и компоненты

### Файлы для изменения:

**`vue-app/src/components/dashboard/ZeroPoint.vue`**
- Компонент нулевой точки
- Требуется обновить шаблон и стили

**Структура изменений:**
1. Убрать иконку `[0]` из заголовка
2. Убрать счётчик `(count)` из заголовка
3. Убрать блок `.zero-point-description` полностью
4. Обновить fallback-сообщение в `.empty-state`

---

## Зависимости

- Использует компонент `TicketCard.vue` для отображения тикетов
- Используется в `DashboardStage.vue` для каждого этапа
- Зависит от логики получения тикетов нулевой точки (уже реализована в TASK-006)

---

## Ступенчатые подзадачи

1. **STEP-01:** Убрать иконку `[0]` и счётчик из заголовка нулевой точки
2. **STEP-02:** Убрать блок описания "Входящие тикеты. Перетащите тикет на сотрудника"
3. **STEP-03:** Обновить fallback-сообщение для пустого состояния

---

## Технические требования

- Использовать Vue.js 3.x (Composition API)
- Сохранить функциональность перетаскивания тикетов
- Сохранить обработку событий (`ticket-dragged`, `ticket-assigned`, `ticket-clicked`)
- Стили должны соответствовать гайдлайнам Bitrix24
- Компонент должен оставаться переиспользуемым

---

## Критерии приёмки

- [ ] Иконка `[0]` убрана из заголовка
- [ ] Счётчик `(count)` убран из заголовка
- [ ] Блок описания "Входящие тикеты. Перетащите тикет на сотрудника" полностью удалён
- [ ] Заголовок отображается как "Нулевая точка" (без иконки и счётчика)
- [ ] Если тикетов нет, отображается fallback: "Нет неразобранных тикетов в стадии"
- [ ] Функциональность перетаскивания тикетов работает корректно
- [ ] Стили соответствуют гайдлайнам Bitrix24
- [ ] Компонент протестирован на всех этапах дашборда

---

## Тестирование

1. Открыть дашборд сектора 1С
2. Проверить отображение нулевой точки на каждом этапе:
   - Если есть тикеты: проверить, что заголовок "Нулевая точка" без иконки и счётчика
   - Если нет тикетов: проверить fallback-сообщение "Нет неразобранных тикетов в стадии"
3. Проверить, что блок описания не отображается
4. Проверить функциональность перетаскивания тикетов из нулевой точки на сотрудников

---

## История правок

- 2025-12-06 17:21 (UTC+3, Брест): Создана задача

