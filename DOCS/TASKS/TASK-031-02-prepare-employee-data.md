# TASK-031-02: Этап 2 - Подготовка данных для интеграции в графики

**Дата создания:** 2025-12-11 15:21 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-031

---

## 📋 Описание этапа

Подготовка структуры данных сотрудников для интеграции в графики состояния сектора 1С. Создание функций подготовки данных для каждого типа графика (линейный, столбчатый, круговая) и утилит для форматирования данных сотрудников с учетом выбранных UX-решений.

---

## ✅ Контекст из этапа 1 (TASK-031-01)

**Выбранные решения:**
- **Линейный график:** Комбинированный подход (tooltip + модальное окно при клике)
- **Столбчатый график:** Группированные столбцы (по одному на сотрудника)
- **Круговая диаграмма:** Модальное окно при клике на сектор
- **Формат отображения:** Прогресс-бары для всех типов графиков
- **Ограничение:** Максимум 10 сотрудников глобально (группировка остальных в "Другие")

---

## 🎯 Цель этапа

1. Создать функции подготовки данных сотрудников для каждого типа графика
2. Реализовать утилиты для форматирования данных (прогресс-бары, группировка)
3. Обеспечить обработку ограничения в 10 сотрудников
4. Подготовить данные для разных временных точек (начало недели, конец недели, текущее состояние)

---

## 📝 Подзадача 2.1: Подготовка данных сотрудников для линейного графика

### 2.1.1. Создание функции `prepareLineChartEmployeeData()`

**Файл:** `vue-app/src/components/graph-state/GraphStateChart.vue`  
**Или:** `vue-app/src/utils/graph-state/employeeChartUtils.js` (новый файл)

#### Назначение функции

Подготовка данных сотрудников для каждой точки линейного графика (начало недели, конец недели, текущее состояние).

#### Сигнатура функции

```javascript
/**
 * Подготовка данных сотрудников для линейного графика
 * 
 * @param {string} stageId - ID этапа ('formed', 'review', 'execution')
 * @param {Object} snapshots - Объект с слепками { weekStart, weekEnd, current }
 * @returns {Object} Данные сотрудников для каждой точки графика
 */
function prepareLineChartEmployeeData(stageId, snapshots) {
  // ...
}
```

#### Структура возвращаемых данных

```javascript
{
  weekStart: [
    {
      id: 123,
      name: "Иван Иванов",
      count: 5,
      percentage: 50.0,  // Процент от общего количества этапа
      isKeeper: false
    },
    // ... другие сотрудники (максимум 10)
  ],
  weekEnd: [
    // ... аналогичная структура
  ],
  current: [
    // ... аналогичная структура
  ],
  others: {
    weekStart: { count: 2, employeeCount: 1 },  // Если сотрудников > 10
    weekEnd: { count: 0, employeeCount: 0 },
    current: { count: 0, employeeCount: 0 }
  }
}
```

#### Логика функции

1. **Получение данных для каждой временной точки:**
   - Использовать `getEmployeesForStage(stageId)` для каждого слепка
   - Получить общее количество тикетов этапа через `getStageTotalCount(stageId)`

2. **Обработка ограничения в 10 сотрудников:**
   - Сортировка сотрудников по убыванию количества тикетов
   - Взять топ-10 сотрудников
   - Остальных сгруппировать в объект `others`

3. **Расчет процентов:**
   - Для каждого сотрудника: `(count / totalCount) * 100`
   - Для "Другие": `(others.count / totalCount) * 100`

4. **Обработка отсутствующих данных:**
   - Если слепок отсутствует, вернуть пустой массив
   - Если нет сотрудников, вернуть пустой массив

#### Задачи для реализации

- [ ] Создать функцию `prepareLineChartEmployeeData()`
- [ ] Реализовать получение данных для каждой временной точки
- [ ] Реализовать сортировку и ограничение до 10 сотрудников
- [ ] Реализовать расчет процентов
- [ ] Реализовать группировку остальных сотрудников в "Другие"
- [ ] Добавить обработку отсутствующих данных
- [ ] Добавить комментарии к функции

---

### 2.1.2. Интеграция с существующими функциями

#### Использование существующих функций

**Функции, которые можно использовать:**
- `getEmployeesForStage(stageId)` — получение списка сотрудников для этапа
- `getStageTotalCount(stageId)` — получение общего количества тикетов этапа
- `getKeeperTicketsCountForStage(stageId)` — получение тикетов хранителя

#### Модификация существующих функций (если требуется)

**Возможные изменения:**
- Добавить параметр `snapshot` в `getEmployeesForStage()` для работы с конкретным слепком
- Или создать новые функции с явным указанием слепка

#### Задачи для реализации

- [ ] Определить, нужно ли модифицировать существующие функции
- [ ] Если да — обновить сигнатуры функций
- [ ] Если нет — создать обертки для работы с конкретными слепками
- [ ] Проверить обратную совместимость

---

## 📝 Подзадача 2.2: Подготовка данных сотрудников для столбчатого графика

### 2.2.1. Создание функции `prepareBarChartEmployeeData()`

**Файл:** `vue-app/src/components/graph-state/GraphStateChart.vue`  
**Или:** `vue-app/src/utils/graph-state/employeeChartUtils.js` (новый файл)

#### Назначение функции

Подготовка данных для группированных столбцов (по одному столбцу на сотрудника) с группировкой по этапам.

#### Сигнатура функции

```javascript
/**
 * Подготовка данных сотрудников для столбчатого графика
 * 
 * @param {Object} snapshots - Объект с слепками { weekStart, weekEnd, current }
 * @param {string} snapshotType - Тип слепка для отображения ('weekStart' | 'weekEnd' | 'current')
 * @returns {Object} Данные для Chart.js с группированными столбцами
 */
function prepareBarChartEmployeeData(snapshots, snapshotType = 'current') {
  // ...
}
```

#### Структура возвращаемых данных

```javascript
{
  labels: ['Сформировано обращение', 'Рассмотрение ТЗ', 'Исполнение'],
  datasets: [
    // Каждый dataset — один сотрудник
    {
      label: 'Иван Иванов',
      data: [5, 3, 2],  // Количество тикетов для каждого этапа
      backgroundColor: '#007bff',  // Цвет этапа или оттенок
      borderColor: '#0056b3',
      borderWidth: 1
    },
    {
      label: 'Петр Петров',
      data: [3, 2, 1],
      backgroundColor: '#007bff80',  // Оттенок цвета этапа
      borderColor: '#0056b3',
      borderWidth: 1
    },
    // ... другие сотрудники (максимум 10)
    {
      label: 'Другие (2)',
      data: [2, 1, 0],
      backgroundColor: '#6c757d',  // Серый цвет для "Другие"
      borderColor: '#5a6268',
      borderWidth: 1
    }
  ]
}
```

#### Логика функции

1. **Получение списка всех сотрудников:**
   - Собрать уникальных сотрудников из всех этапов
   - Максимум 10 сотрудников (топ-10 по общему количеству тикетов)

2. **Подготовка данных для каждого сотрудника:**
   - Для каждого сотрудника создать dataset
   - Данные: массив с количеством тикетов для каждого этапа
   - Цвет: оттенок цвета этапа или индивидуальный цвет

3. **Группировка по этапам:**
   - Labels: названия этапов
   - Данные для каждого сотрудника: количество тикетов на каждом этапе

4. **Обработка "Другие":**
   - Если сотрудников > 10, создать dataset для "Другие"
   - Данные: сумма тикетов остальных сотрудников для каждого этапа

#### Задачи для реализации

- [ ] Создать функцию `prepareBarChartEmployeeData()`
- [ ] Реализовать сбор уникальных сотрудников из всех этапов
- [ ] Реализовать сортировку и ограничение до 10 сотрудников
- [ ] Реализовать подготовку datasets для каждого сотрудника
- [ ] Реализовать цветовую схему (оттенки цвета этапа)
- [ ] Реализовать группировку остальных сотрудников в "Другие"
- [ ] Добавить обработку отсутствующих данных
- [ ] Добавить комментарии к функции

---

### 2.2.2. Создание функции `getEmployeeColorForBarChart()`

#### Назначение функции

Получение цвета для столбца сотрудника в столбчатом графике.

#### Сигнатура функции

```javascript
/**
 * Получение цвета для столбца сотрудника
 * 
 * @param {number} employeeId - ID сотрудника
 * @param {string} stageId - ID этапа
 * @param {number} index - Индекс сотрудника в списке (0-9)
 * @returns {string} Цвет в формате hex или rgba
 */
function getEmployeeColorForBarChart(employeeId, stageId, index) {
  // ...
}
```

#### Логика функции

1. **Определение базового цвета этапа:**
   - `formed`: `#007bff` (синий)
   - `review`: `#ffc107` (жёлтый)
   - `execution`: `#28a745` (зелёный)

2. **Создание оттенков:**
   - Для каждого сотрудника использовать оттенок базового цвета
   - Оттенки: от более насыщенного к менее насыщенному
   - Или использовать фиксированную палитру цветов

3. **Специальный случай:**
   - Хранитель объектов (ID: 1051): использовать специальный цвет (например, `#f59e0b`)

#### Задачи для реализации

- [ ] Создать функцию `getEmployeeColorForBarChart()`
- [ ] Реализовать определение базового цвета этапа
- [ ] Реализовать создание оттенков для сотрудников
- [ ] Реализовать обработку хранителя объектов
- [ ] Добавить комментарии к функции

---

## 📝 Подзадача 2.3: Подготовка данных сотрудников для круговой диаграммы

### 2.3.1. Создание функции `prepareDoughnutChartEmployeeData()`

**Файл:** `vue-app/src/components/graph-state/GraphStateChart.vue`  
**Или:** `vue-app/src/utils/graph-state/employeeChartUtils.js` (новый файл)

#### Назначение функции

Подготовка данных сотрудников для модального окна круговой диаграммы (при клике на сектор этапа).

#### Сигнатура функции

```javascript
/**
 * Подготовка данных сотрудников для круговой диаграммы
 * 
 * @param {string} stageId - ID этапа ('formed', 'review', 'execution')
 * @param {Object} snapshot - Слепок с данными (current, weekEnd или weekStart)
 * @returns {Object} Данные сотрудников для модального окна
 */
function prepareDoughnutChartEmployeeData(stageId, snapshot) {
  // ...
}
```

#### Структура возвращаемых данных

```javascript
{
  stageName: 'Сформировано обращение',
  totalCount: 25,
  employees: [
    {
      id: 123,
      name: "Иван Иванов",
      count: 10,
      percentage: 40.0,  // Процент от общего количества этапа
      progressBarWidth: 40.0,  // Ширина прогресс-бара в процентах
      isKeeper: false
    },
    // ... другие сотрудники (максимум 10)
  ],
  others: {
    count: 2,
    employeeCount: 1,
    percentage: 8.0,
    progressBarWidth: 8.0
  }
}
```

#### Логика функции

1. **Получение данных этапа:**
   - Использовать `getEmployeesForStage(stageId)` для получения сотрудников
   - Получить общее количество тикетов через `getStageTotalCount(stageId)`

2. **Обработка ограничения в 10 сотрудников:**
   - Сортировка сотрудников по убыванию количества тикетов
   - Взять топ-10 сотрудников
   - Остальных сгруппировать в объект `others`

3. **Расчет процентов и ширины прогресс-бара:**
   - Для каждого сотрудника: `percentage = (count / totalCount) * 100`
   - `progressBarWidth = percentage` (для CSS)

4. **Получение названия этапа:**
   - Из массива `stages` по `stageId`

#### Задачи для реализации

- [ ] Создать функцию `prepareDoughnutChartEmployeeData()`
- [ ] Реализовать получение данных этапа
- [ ] Реализовать сортировку и ограничение до 10 сотрудников
- [ ] Реализовать расчет процентов и ширины прогресс-бара
- [ ] Реализовать группировку остальных сотрудников в "Другие"
- [ ] Реализовать получение названия этапа
- [ ] Добавить обработку отсутствующих данных
- [ ] Добавить комментарии к функции

---

## 📝 Подзадача 2.4: Создание утилит для форматирования данных сотрудников

### 2.4.1. Создание функции `formatEmployeeProgressBarData()`

**Файл:** `vue-app/src/utils/graph-state/employeeChartUtils.js` (новый файл)

#### Назначение функции

Форматирование данных сотрудников для отображения с прогресс-барами.

#### Сигнатура функции

```javascript
/**
 * Форматирование данных сотрудников для прогресс-баров
 * 
 * @param {Array} employees - Массив сотрудников с количеством тикетов
 * @param {number} totalCount - Общее количество тикетов этапа
 * @param {number} maxVisible - Максимальное количество видимых сотрудников (по умолчанию 10)
 * @returns {Object} Отформатированные данные с прогресс-барами
 */
function formatEmployeeProgressBarData(employees, totalCount, maxVisible = 10) {
  // ...
}
```

#### Структура возвращаемых данных

```javascript
{
  employees: [
    {
      id: 123,
      name: "Иван Иванов",
      count: 10,
      percentage: 40.0,
      progressBarWidth: 40.0,
      progressBarColor: '#007bff',  // Цвет этапа
      isKeeper: false
    },
    // ... другие сотрудники
  ],
  others: {
    count: 2,
    employeeCount: 1,
    percentage: 8.0,
    progressBarWidth: 8.0,
    progressBarColor: '#6c757d'  // Серый для "Другие"
  }
}
```

#### Логика функции

1. **Сортировка сотрудников:**
   - По убыванию количества тикетов

2. **Ограничение до maxVisible:**
   - Взять топ-N сотрудников
   - Остальных сгруппировать в `others`

3. **Расчет процентов и ширины прогресс-бара:**
   - `percentage = (count / totalCount) * 100`
   - `progressBarWidth = percentage`

4. **Определение цвета прогресс-бара:**
   - Обычные сотрудники: цвет этапа
   - Хранитель объектов: специальный цвет
   - "Другие": серый цвет

#### Задачи для реализации

- [ ] Создать функцию `formatEmployeeProgressBarData()`
- [ ] Реализовать сортировку сотрудников
- [ ] Реализовать ограничение до maxVisible
- [ ] Реализовать расчет процентов и ширины прогресс-бара
- [ ] Реализовать определение цвета прогресс-бара
- [ ] Реализовать группировку остальных сотрудников
- [ ] Добавить комментарии к функции

---

### 2.4.2. Создание функции `groupEmployeesByCount()`

#### Назначение функции

Группировка сотрудников при превышении максимального количества.

#### Сигнатура функции

```javascript
/**
 * Группировка сотрудников при превышении максимального количества
 * 
 * @param {Array} employees - Массив сотрудников
 * @param {number} maxVisible - Максимальное количество видимых сотрудников
 * @returns {Object} Объект с видимыми сотрудниками и группой "Другие"
 */
function groupEmployeesByCount(employees, maxVisible = 10) {
  // ...
}
```

#### Структура возвращаемых данных

```javascript
{
  visible: [
    // Топ-N сотрудников
  ],
  others: {
    employees: [
      // Остальные сотрудники
    ],
    count: 5,  // Сумма тикетов остальных сотрудников
    employeeCount: 3  // Количество сотрудников в группе
  }
}
```

#### Логика функции

1. **Сортировка:**
   - По убыванию количества тикетов

2. **Разделение:**
   - Взять первые `maxVisible` сотрудников
   - Остальных поместить в `others`

3. **Расчет суммы:**
   - Подсчитать общее количество тикетов в группе "Другие"
   - Подсчитать количество сотрудников в группе

#### Задачи для реализации

- [ ] Создать функцию `groupEmployeesByCount()`
- [ ] Реализовать сортировку сотрудников
- [ ] Реализовать разделение на видимых и остальных
- [ ] Реализовать расчет суммы тикетов для "Другие"
- [ ] Добавить комментарии к функции

---

### 2.4.3. Создание функции `calculateEmployeePercentages()`

#### Назначение функции

Расчет процентов для каждого сотрудника от общего количества тикетов этапа.

#### Сигнатура функции

```javascript
/**
 * Расчет процентов для сотрудников
 * 
 * @param {Array} employees - Массив сотрудников с количеством тикетов
 * @param {number} totalCount - Общее количество тикетов этапа
 * @returns {Array} Массив сотрудников с добавленными процентами
 */
function calculateEmployeePercentages(employees, totalCount) {
  // ...
}
```

#### Логика функции

1. **Расчет процента для каждого сотрудника:**
   - `percentage = (count / totalCount) * 100`
   - Округление до 1 знака после запятой

2. **Обработка нулевого totalCount:**
   - Если `totalCount === 0`, вернуть `percentage = 0` для всех

#### Задачи для реализации

- [ ] Создать функцию `calculateEmployeePercentages()`
- [ ] Реализовать расчет процентов
- [ ] Реализовать округление до 1 знака после запятой
- [ ] Реализовать обработку нулевого totalCount
- [ ] Добавить комментарии к функции

---

## 📁 Структура файлов

### Новый файл: `vue-app/src/utils/graph-state/employeeChartUtils.js`

**Назначение:** Утилиты для работы с данными сотрудников в графиках

**Содержимое:**
- `prepareLineChartEmployeeData()` — подготовка данных для линейного графика
- `prepareBarChartEmployeeData()` — подготовка данных для столбчатого графика
- `prepareDoughnutChartEmployeeData()` — подготовка данных для круговой диаграммы
- `formatEmployeeProgressBarData()` — форматирование для прогресс-баров
- `groupEmployeesByCount()` — группировка сотрудников
- `calculateEmployeePercentages()` — расчет процентов
- `getEmployeeColorForBarChart()` — получение цвета для столбца

### Модификация существующего файла: `vue-app/src/components/graph-state/GraphStateChart.vue`

**Изменения:**
- Импорт утилит из `employeeChartUtils.js`
- Использование новых функций в компоненте

---

## ✅ Критерии приёмки этапа

### Функции подготовки данных:

- [ ] Создана функция `prepareLineChartEmployeeData()` с корректной логикой
- [ ] Создана функция `prepareBarChartEmployeeData()` с корректной логикой
- [ ] Создана функция `prepareDoughnutChartEmployeeData()` с корректной логикой
- [ ] Все функции обрабатывают ограничение в 10 сотрудников
- [ ] Все функции обрабатывают группировку остальных в "Другие"
- [ ] Все функции обрабатывают отсутствующие данные

### Утилиты форматирования:

- [ ] Создана функция `formatEmployeeProgressBarData()` с корректной логикой
- [ ] Создана функция `groupEmployeesByCount()` с корректной логикой
- [ ] Создана функция `calculateEmployeePercentages()` с корректной логикой
- [ ] Создана функция `getEmployeeColorForBarChart()` с корректной логикой

### Структура данных:

- [ ] Все функции возвращают данные в ожидаемом формате
- [ ] Данные содержат все необходимые поля (id, name, count, percentage, progressBarWidth)
- [ ] Данные корректно обрабатывают случай с отсутствующими сотрудниками

### Тестирование:

- [ ] Протестированы все функции с различными входными данными
- [ ] Протестирована обработка граничных случаев (0 сотрудников, 10 сотрудников, >10 сотрудников)
- [ ] Протестирована обработка отсутствующих данных

### Документация:

- [ ] Все функции имеют JSDoc комментарии
- [ ] Добавлены примеры использования функций
- [ ] Описаны параметры и возвращаемые значения

---

## 🧪 Тестирование

### Тестовые сценарии

#### 1. Тест функции `prepareLineChartEmployeeData()`

**Сценарий 1: Нормальный случай (5 сотрудников)**
```javascript
const snapshots = {
  weekStart: { /* слепок с 5 сотрудниками */ },
  weekEnd: { /* слепок с 5 сотрудниками */ },
  current: { /* слепок с 5 сотрудниками */ }
};
const result = prepareLineChartEmployeeData('formed', snapshots);
// Ожидается: массив из 5 сотрудников для каждой временной точки
```

**Сценарий 2: Максимальное количество (10 сотрудников)**
```javascript
// Ожидается: массив из 10 сотрудников, others пустой
```

**Сценарий 3: Превышение максимума (15 сотрудников)**
```javascript
// Ожидается: массив из 10 сотрудников, others содержит 5 сотрудников
```

**Сценарий 4: Отсутствующие данные**
```javascript
const snapshots = {
  weekStart: null,
  weekEnd: null,
  current: null
};
// Ожидается: пустые массивы для всех временных точек
```

#### 2. Тест функции `prepareBarChartEmployeeData()`

**Сценарий 1: Нормальный случай**
```javascript
// Ожидается: datasets для каждого сотрудника с данными для всех этапов
```

**Сценарий 2: Группировка по этапам**
```javascript
// Ожидается: labels содержат названия всех этапов
```

#### 3. Тест функции `formatEmployeeProgressBarData()`

**Сценарий 1: Расчет процентов**
```javascript
const employees = [{ id: 1, name: "Иван", count: 10 }];
const totalCount = 25;
const result = formatEmployeeProgressBarData(employees, totalCount);
// Ожидается: percentage = 40.0, progressBarWidth = 40.0
```

---

## 📚 Связанные документы

### Исходный код:
- `vue-app/src/components/graph-state/GraphStateChart.vue` — компонент графика
- `vue-app/src/services/graph-state/SectorDataAdapter.js` — адаптер данных сектора

### Документация:
- `DOCS/TASKS/TASK-031-integrate-employee-details-into-charts.md` — родительская задача
- `DOCS/TASKS/TASK-031-01-analysis-and-design.md` — этап 1 (анализ и проектирование)

---

## 🔍 Дополнительная информация

### Зависимости от этапа 1

Этап 2 зависит от результатов этапа 1:
- Выбранные UX-решения (комбинированный подход, группированные столбцы, модальное окно)
- Формат отображения (прогресс-бары)
- Ограничение в 10 сотрудников

### Следующие этапы

После завершения этапа 2:
- **Этап 3:** Интеграция детализации в линейный и столбчатый графики
- **Этап 4:** Интеграция детализации в круговую диаграмму
- **Этап 5:** Удаление блока детализации и финальная полировка

---

## 📝 История правок

- **2025-12-11 15:21 (UTC+3, Брест):** Создана задача TASK-031-02 для этапа 2 "Подготовка данных для интеграции в графики"

---

## 🎯 Следующие шаги

После завершения этапа 2:
1. Переход к этапу 3: Интеграция детализации в линейный и столбчатый графики
2. Использование созданных функций для подготовки данных
3. Интеграция функций в компонент `GraphStateChart.vue`

