# TASK-034-05: Обработка кликов и открытие тикетов

**Дата создания:** 2025-12-12 11:51 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-034 (Этап 5)

---

## 📋 Описание

Реализовать обработку кликов на карточки тикетов в уровне 4 попапа и открытие тикетов в новой вкладке браузера. Обеспечить корректное формирование URL, обработку ошибок и уведомления пользователю.

---

## 🎯 Цель

1. Реализовать обработчик клика на карточку тикета
2. Использовать функцию `getTicketIframeUrl()` для формирования URL
3. Открыть тикет в новой вкладке через `window.open()`
4. Обработать все возможные ошибки
5. Добавить уведомления пользователю при ошибках

---

## 🔍 Контекст задачи

### Компонент TicketCard.vue

**Расположение:** `vue-app/src/components/dashboard/TicketCard.vue`

**Текущая реализация обработки клика:**
```javascript
const handleCardClick = (event) => {
  // Предотвращаем клик, если идёт перетаскивание
  if (isDragging.value) {
    return;
  }
  
  const iframeUrl = getTicketIframeUrl(props.ticket.id);
  
  // Открываем всегда в новой вкладке (по требованию)
  window.open(iframeUrl, '_blank');

  emit('click', props.ticket);
};
```

**Особенности:**
- Компонент уже обрабатывает клик внутри себя
- Открывает тикет в новой вкладке
- Эмитит событие `@click` с объектом тикета

### Функция getTicketIframeUrl()

**Расположение:** `vue-app/src/services/dashboard-sector-1c/utils/constants.js`

**Реализация:**
```javascript
export function getTicketIframeUrl(ticketId) {
  return `${BITRIX24_BASE_URL}/page/${SERVISDESK_SECTION}/servisdesk/type/${ENTITY_TYPE_ID}/details/${ticketId}/`;
}
```

**Параметры:**
- `BITRIX24_BASE_URL` — `'https://bitrix24.kompo.by'`
- `SERVISDESK_SECTION` — `'servisdeskitotdel'`
- `ENTITY_TYPE_ID` — `140` (смарт-процесс сектора 1С)
- `ticketId` — ID тикета

**Формат URL:**
```
https://bitrix24.kompo.by/page/servisdeskitotdel/servisdesk/type/140/details/{ticketId}/
```

### Зависимости от предыдущих этапов

**Этап 4 (TASK-034-04):**
- Компонент `TicketCard` интегрирован в шаблон уровня 4
- Обработчик `@click` добавлен в шаблон

---

## 📝 Задачи

### Задача 5.1: Обработчик клика на карточку

**Цель:** Создать обработчик клика на карточку тикета, который получает объект тикета и открывает его в новой вкладке.

#### 5.1.1. Изучение текущей реализации в шаблоне

**Текущая структура в шаблоне уровня 4:**
```vue
<TicketCard
  v-for="ticket in level4Data.tickets"
  :key="ticket.id"
  :ticket="ticket"
  :draggable="false"
  @click="handleTicketClick(ticket)"
/>
```

**Действия:**
1. Проверить, что обработчик `@click` уже добавлен в шаблон (из этапа 4)
2. Определить, нужно ли создавать функцию `handleTicketClick()` или использовать существующую логику

**Ожидаемый результат:**
- Понимание текущей структуры обработки кликов
- Определено, что нужно реализовать

#### 5.1.2. Создание функции handleTicketClick()

**Функция:**
```javascript
/**
 * Обработка клика на карточку тикета
 * 
 * Открывает тикет в новой вкладке браузера через функцию getTicketIframeUrl()
 * 
 * @param {Object} ticket - Объект тикета
 * @param {Number} ticket.id - ID тикета
 */
function handleTicketClick(ticket) {
  if (!ticket) {
    console.warn('[EmployeeDetailsModal] Ticket is null or undefined');
    notifications.warning('Ошибка: тикет не найден');
    return;
  }

  if (!ticket.id) {
    console.error('[EmployeeDetailsModal] Ticket ID is missing:', ticket);
    notifications.error('Ошибка: ID тикета не указан');
    return;
  }

  try {
    // Импортировать функцию getTicketIframeUrl
    const { getTicketIframeUrl } = await import('@/services/dashboard-sector-1c/utils/constants.js');
    
    // Сформировать URL для открытия тикета
    const iframeUrl = getTicketIframeUrl(ticket.id);
    
    console.log('[EmployeeDetailsModal] Opening ticket:', {
      ticketId: ticket.id,
      iframeUrl: iframeUrl
    });

    // Проверить, что URL сформирован корректно
    if (!iframeUrl || typeof iframeUrl !== 'string' || iframeUrl.length === 0) {
      throw new Error('Не удалось сформировать URL для тикета');
    }

    // Открыть тикет в новой вкладке
    const newWindow = window.open(iframeUrl, '_blank');

    // Проверить, что окно открылось (может быть заблокировано popup blocker)
    if (!newWindow || newWindow.closed || typeof newWindow.closed === 'undefined') {
      throw new Error('Не удалось открыть новую вкладку. Возможно, заблокирован popup blocker');
    }

    console.log('[EmployeeDetailsModal] Ticket opened successfully:', {
      ticketId: ticket.id,
      iframeUrl: iframeUrl
    });
  } catch (error) {
    console.error('[EmployeeDetailsModal] Error opening ticket:', error);
    console.error('[EmployeeDetailsModal] Error details:', {
      message: error.message,
      stack: error.stack,
      ticket: ticket
    });

    // Показать уведомление пользователю
    notifications.error('Ошибка открытия тикета: ' + error.message);
  }
}
```

**Действия:**
1. Создать функцию `handleTicketClick()` в секции `<script setup>`
2. Добавить проверку наличия тикета и его ID
3. Импортировать функцию `getTicketIframeUrl()` (динамический импорт)
4. Сформировать URL для открытия тикета
5. Открыть тикет в новой вкладке через `window.open()`
6. Добавить проверку успешности открытия окна
7. Добавить обработку ошибок

**Ожидаемый результат:**
- Функция `handleTicketClick()` реализована
- URL формируется корректно
- Тикет открывается в новой вкладке

#### 5.1.3. Альтернативный вариант: использование события от TicketCard

**Вариант 1: Прямая обработка в шаблоне (текущий)**
```vue
<TicketCard
  @click="handleTicketClick"
/>
```

**Вариант 2: Обработка с передачей тикета**
```vue
<TicketCard
  @click="(ticket) => handleTicketClick(ticket)"
/>
```

**Примечание:** Компонент `TicketCard` уже эмитит событие `@click` с объектом тикета, поэтому можно использовать оба варианта.

**Действия:**
1. Выбрать вариант обработки (рекомендуется вариант 2 для явности)
2. Обновить шаблон, если нужно

**Ожидаемый результат:**
- Обработчик клика работает корректно
- Объект тикета передается в функцию

**Критерии приёмки задачи 5.1:**
- [ ] Функция `handleTicketClick()` реализована
- [ ] Обработчик `@click` работает корректно
- [ ] URL формируется через `getTicketIframeUrl()`
- [ ] Тикет открывается в новой вкладке
- [ ] Проверка успешности открытия окна работает

---

### Задача 5.2: Эмит события из TicketCard

**Цель:** Обеспечить корректную передачу объекта тикета из компонента `TicketCard` в обработчик попапа.

#### 5.2.1. Изучение эмита события в TicketCard

**Текущая реализация в TicketCard.vue:**
```javascript
const handleCardClick = (event) => {
  // ...
  emit('click', props.ticket);
};
```

**Особенности:**
- Компонент эмитит событие `@click` с объектом тикета
- Событие эмитится после открытия тикета в новой вкладке
- Родительский компонент может обработать событие для дополнительной логики

#### 5.2.2. Обработка события в попапе

**Текущая структура в шаблоне:**
```vue
<TicketCard
  v-for="ticket in level4Data.tickets"
  :key="ticket.id"
  :ticket="ticket"
  :draggable="false"
  @click="handleTicketClick"
/>
```

**Действия:**
1. Проверить, что событие `@click` обрабатывается корректно
2. Убедиться, что объект тикета передается в функцию `handleTicketClick()`
3. Добавить логирование для отладки (если нужно)

**Ожидаемый результат:**
- Событие обрабатывается корректно
- Объект тикета передается в функцию

#### 5.2.3. Дополнительная логика при клике (опционально)

**Возможные действия:**
- Логирование клика для аналитики
- Отслеживание статистики открытий тикетов
- Обновление состояния попапа (если нужно)

**Пример:**
```javascript
function handleTicketClick(ticket) {
  // Основная логика открытия тикета
  // ...
  
  // Дополнительная логика (если нужно)
  console.log('[EmployeeDetailsModal] Ticket clicked for analytics:', {
    ticketId: ticket.id,
    sourceLevel: level4Data.value?.context?.sourceLevel,
    timestamp: new Date().toISOString()
  });
}
```

**Действия:**
1. Определить, нужна ли дополнительная логика
2. Реализовать, если требуется

**Ожидаемый результат:**
- Дополнительная логика реализована (если нужна)

**Критерии приёмки задачи 5.2:**
- [ ] Событие `@click` обрабатывается корректно
- [ ] Объект тикета передается в функцию
- [ ] Дополнительная логика реализована (если нужна)

---

### Задача 5.3: Обработка ошибок

**Цель:** Реализовать обработку всех возможных ошибок при открытии тикета.

#### 5.3.1. Обработка отсутствия тикета

**Ошибка:** Тикет не передан или равен `null`/`undefined`

**Обработка:**
```javascript
if (!ticket) {
  console.warn('[EmployeeDetailsModal] Ticket is null or undefined');
  notifications.warning('Ошибка: тикет не найден');
  return;
}
```

**Действия:**
1. Добавить проверку наличия тикета
2. Показать уведомление пользователю
3. Прервать выполнение функции

**Ожидаемый результат:**
- Ошибка обрабатывается корректно
- Пользователь получает уведомление

#### 5.3.2. Обработка отсутствия ID тикета

**Ошибка:** У тикета отсутствует поле `id`

**Обработка:**
```javascript
if (!ticket.id) {
  console.error('[EmployeeDetailsModal] Ticket ID is missing:', ticket);
  notifications.error('Ошибка: ID тикета не указан');
  return;
}
```

**Действия:**
1. Добавить проверку наличия `ticket.id`
2. Показать уведомление пользователю
3. Залогировать ошибку для отладки

**Ожидаемый результат:**
- Ошибка обрабатывается корректно
- Пользователь получает уведомление

#### 5.3.3. Обработка ошибки формирования URL

**Ошибка:** Функция `getTicketIframeUrl()` не может сформировать URL

**Обработка:**
```javascript
const iframeUrl = getTicketIframeUrl(ticket.id);

if (!iframeUrl || typeof iframeUrl !== 'string' || iframeUrl.length === 0) {
  throw new Error('Не удалось сформировать URL для тикета');
}
```

**Действия:**
1. Проверить результат функции `getTicketIframeUrl()`
2. Убедиться, что URL — непустая строка
3. Выбросить ошибку при проблеме

**Ожидаемый результат:**
- Ошибка формирования URL обрабатывается
- Пользователь получает уведомление

#### 5.3.4. Обработка ошибки открытия окна (popup blocker)

**Ошибка:** Браузер заблокировал открытие новой вкладки (popup blocker)

**Обработка:**
```javascript
const newWindow = window.open(iframeUrl, '_blank');

if (!newWindow || newWindow.closed || typeof newWindow.closed === 'undefined') {
  throw new Error('Не удалось открыть новую вкладку. Возможно, заблокирован popup blocker');
}
```

**Действия:**
1. Проверить результат `window.open()`
2. Проверить, что окно не закрыто сразу
3. Выбросить ошибку при проблеме
4. Показать информативное сообщение пользователю

**Особенности:**
- Popup blocker может блокировать открытие новой вкладки
- Нужно сообщить пользователю о проблеме
- Возможно, потребуется альтернативный способ открытия (например, через ссылку)

**Ожидаемый результат:**
- Ошибка popup blocker обрабатывается
- Пользователь получает информативное сообщение

#### 5.3.5. Общая обработка ошибок (try/catch)

**Структура:**
```javascript
try {
  // Основная логика открытия тикета
  // ...
} catch (error) {
  console.error('[EmployeeDetailsModal] Error opening ticket:', error);
  console.error('[EmployeeDetailsModal] Error details:', {
    message: error.message,
    stack: error.stack,
    ticket: ticket
  });

  // Показать уведомление пользователю
  notifications.error('Ошибка открытия тикета: ' + error.message);
}
```

**Действия:**
1. Обернуть основную логику в `try/catch`
2. Логировать все ошибки для отладки
3. Показывать уведомления пользователю

**Ожидаемый результат:**
- Все ошибки обрабатываются
- Логирование работает корректно
- Пользователь получает уведомления

#### 5.3.6. Альтернативный способ открытия (если popup blocker)

**Вариант 1: Создание ссылки и программный клик**
```javascript
// Если window.open() заблокирован, создать ссылку и кликнуть по ней
if (!newWindow || newWindow.closed) {
  const link = document.createElement('a');
  link.href = iframeUrl;
  link.target = '_blank';
  link.rel = 'noopener noreferrer';
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
}
```

**Вариант 2: Показ уведомления с инструкцией**
```javascript
if (!newWindow || newWindow.closed) {
  notifications.warning(
    'Браузер заблокировал открытие новой вкладки. ' +
    'Пожалуйста, разрешите всплывающие окна для этого сайта или используйте правую кнопку мыши → "Открыть в новой вкладке"'
  );
  return;
}
```

**Действия:**
1. Определить стратегию обработки popup blocker
2. Реализовать альтернативный способ (если нужно)
3. Показать инструкцию пользователю

**Ожидаемый результат:**
- Альтернативный способ реализован (если нужен)
- Пользователь получает инструкцию

**Критерии приёмки задачи 5.3:**
- [ ] Обработка отсутствия тикета реализована
- [ ] Обработка отсутствия ID тикета реализована
- [ ] Обработка ошибки формирования URL реализована
- [ ] Обработка popup blocker реализована
- [ ] Общая обработка ошибок (try/catch) реализована
- [ ] Логирование ошибок работает
- [ ] Уведомления пользователю работают
- [ ] Альтернативный способ открытия реализован (если нужен)

---

### Задача 5.4: Улучшение пользовательского опыта

**Цель:** Добавить визуальную обратную связь и улучшить UX при клике на карточку.

#### 5.4.1. Визуальная обратная связь при клике

**Действия:**
1. Добавить визуальный эффект при клике на карточку (если нужно)
2. Использовать CSS-класс для активного состояния
3. Обеспечить плавный переход

**Стили (если нужно):**
```css
.level-4 .tickets-list .ticket-card:active {
  transform: scale(0.98);
  opacity: 0.8;
  transition: transform 0.1s ease, opacity 0.1s ease;
}
```

**Ожидаемый результат:**
- Визуальная обратная связь работает (если реализована)

#### 5.4.2. Курсор при наведении

**Действия:**
1. Убедиться, что курсор меняется на `pointer` при наведении
2. Проверить, что стили из `TicketCard.vue` применяются

**Ожидаемый результат:**
- Курсор меняется на `pointer` при наведении

#### 5.4.3. Уведомление об успешном открытии (опционально)

**Действия:**
1. Рассмотреть добавление уведомления об успешном открытии (если нужно)
2. Реализовать, если требуется

**Пример:**
```javascript
if (newWindow && !newWindow.closed) {
  notifications.success(`Тикет #${ticket.id} открыт в новой вкладке`);
}
```

**Ожидаемый результат:**
- Уведомление об успешном открытии работает (если реализовано)

**Критерии приёмки задачи 5.4:**
- [ ] Визуальная обратная связь работает (если реализована)
- [ ] Курсор меняется на `pointer` при наведении
- [ ] Уведомление об успешном открытии работает (если реализовано)

---

## 📚 Зависимости

### Существующие файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа (основной файл для изменений)
- `vue-app/src/components/dashboard/TicketCard.vue` — компонент карточки тикета
- `vue-app/src/services/dashboard-sector-1c/utils/constants.js` — функция `getTicketIframeUrl()`

### Импорты

```javascript
// Динамический импорт (в функции handleTicketClick)
import { getTicketIframeUrl } from '@/services/dashboard-sector-1c/utils/constants.js';

// Существующие импорты
import { useNotifications } from '@/composables/useNotifications.js';
```

---

## ✅ Критерии приёмки этапа 5

- [x] Функция `handleTicketClick()` реализована
- [x] Обработчик `@click` работает корректно
- [x] URL формируется через `getTicketIframeUrl()`
- [x] Тикет открывается в новой вкладке
- [x] Обработка всех ошибок реализована
- [x] Логирование ошибок работает
- [x] Уведомления пользователю работают
- [x] Проверка успешности открытия окна работает
- [x] Обработка popup blocker реализована
- [x] Код соответствует стандартам проекта
- [x] JSDoc комментарии добавлены

**Статус:** ✅ Завершена (2025-12-12, UTC+3, Брест)

---

## 🧪 Тестирование

### Тест 1: Успешное открытие тикета

**Сценарий:**
1. Перейти на уровень 4
2. Кликнуть на карточку тикета
3. Проверить, что тикет открылся в новой вкладке

**Ожидаемый результат:**
- Тикет открывается в новой вкладке
- URL формируется корректно
- Новая вкладка содержит детальную информацию о тикете

### Тест 2: Обработка отсутствия тикета

**Сценарий:**
1. Вызвать `handleTicketClick(null)`
2. Проверить, что ошибка обрабатывается

**Ожидаемый результат:**
- Ошибка обрабатывается корректно
- Пользователь получает уведомление
- Функция не падает

### Тест 3: Обработка отсутствия ID тикета

**Сценарий:**
1. Вызвать `handleTicketClick({ title: 'Test' })` (без ID)
2. Проверить, что ошибка обрабатывается

**Ожидаемый результат:**
- Ошибка обрабатывается корректно
- Пользователь получает уведомление
- Функция не падает

### Тест 4: Обработка popup blocker

**Сценарий:**
1. Включить popup blocker в браузере
2. Кликнуть на карточку тикета
3. Проверить, что ошибка обрабатывается

**Ожидаемый результат:**
- Ошибка обрабатывается корректно
- Пользователь получает информативное сообщение
- Альтернативный способ работает (если реализован)

### Тест 5: Открытие нескольких тикетов подряд

**Сценарий:**
1. Перейти на уровень 4
2. Кликнуть на несколько карточек тикетов подряд
3. Проверить, что все тикеты открываются

**Ожидаемый результат:**
- Все тикеты открываются в новых вкладках
- Нет конфликтов или ошибок

### Тест 6: Логирование

**Сценарий:**
1. Открыть консоль браузера
2. Кликнуть на карточку тикета
3. Проверить логирование

**Ожидаемый результат:**
- Логирование работает корректно
- Все действия логируются

---

## 📝 История правок

- **2025-12-12 11:51 (UTC+3, Брест):** Создана задача TASK-034-05
  - Определены 4 подзадачи с детальными шагами
  - Добавлены примеры кода и обработки ошибок
  - Описаны критерии приёмки и тесты

- **2025-12-12 (UTC+3, Брест):** Выполнена задача TASK-034-05
  - Добавлен импорт функции `getTicketIframeUrl` из '@/services/dashboard-sector-1c/utils/constants.js'
  - Реализована полная функция `handleTicketClick()`:
    - Проверка наличия тикета и его ID
    - Формирование URL через `getTicketIframeUrl(ticket.id)`
    - Валидация сформированного URL
    - Открытие тикета в новой вкладке через `window.open()` с параметрами `noopener,noreferrer`
    - Проверка успешности открытия окна (обработка popup blocker)
    - Альтернативный способ открытия через создание ссылки и программный клик (если popup blocker заблокировал)
    - Обработка всех ошибок с логированием и уведомлениями пользователю
  - Добавлена визуальная обратная связь при клике:
    - Стили для `.level-4 .tickets-list .ticket-card` с `cursor: pointer`
    - Стили для `:active` состояния (transform: scale(0.98), opacity: 0.8)
    - Плавные переходы (transition)
  - Все ошибки обрабатываются с информативными сообщениями:
    - Отсутствие тикета → warning уведомление
    - Отсутствие ID тикета → error уведомление
    - Ошибка формирования URL → error уведомление
    - Popup blocker → альтернативный способ + error уведомление (если альтернативный способ тоже не сработал)
  - Добавлено подробное логирование всех действий и ошибок для отладки
  - JSDoc комментарии добавлены к функции

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)



