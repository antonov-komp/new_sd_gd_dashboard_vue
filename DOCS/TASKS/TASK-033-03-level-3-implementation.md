# TASK-033-03: Этап 3 - Реализация уровня 3 (детализация по заказчикам)

**Дата создания:** 2025-12-12 09:14 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-033

---

## 📋 Описание этапа

Реализация третьего уровня попапа детализации по сотрудникам с отображением таблицы заказчиков (отделов) для выбранной временной градации. На этом уровне пользователь видит полное распределение тикетов сотрудника по заказчикам в рамках конкретной категории давности.

**Триггер:** Клик на строку временной градации в уровне 2

**Результат:** Отображение таблицы с заказчиками (отделами), количеством тикетов у каждого заказчика, с возможностью возврата на уровень 2.

---

## 🎯 Цель этапа

Добавить третий уровень попапа, который показывает детальную разбивку тикетов выбранного сотрудника в выбранной временной градации по заказчикам (отделам), используя поле `UF_CRM_7_DEPARTMENT_HEAD` из тикетов.

**Ожидаемый результат:**
- При клике на временную градацию в уровне 2 открывается уровень 3
- Отображается таблица с заказчиками и количеством тикетов
- Заказчики отсортированы по убыванию количества тикетов
- Полные названия заказчиков (без ограничения 17 символов)
- Кнопка "Назад" возвращает на уровень 2
- Обработка тикетов без заказчика (группа "Без заказчика")

---

## 🏢 Заказчик (отдел) - UF_CRM_7_DEPARTMENT_HEAD

### Поле в Bitrix24

**Поле Bitrix24:** `UF_CRM_7_DEPARTMENT_HEAD`  
**Тип поля:** Строка (String)  
**Назначение:** Отдел заказчика тикета

**Расположение в маппере:** `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js`  
**Поле в маппере:** `departmentHead`

### Текущая нормализация (для карточки тикета)

**В маппере тикетов:**
```javascript
// Извлечение пользовательского поля UF_CRM_7_DEPARTMENT_HEAD
const ufDepartmentHead = bitrixTicket.UF_CRM_7_DEPARTMENT_HEAD || 
                         bitrixTicket.uf_crm_7_department_head || 
                         bitrixTicket.ufCrm7DepartmentHead ||
                         bitrixTicket['UF_CRM_7_DEPARTMENT_HEAD'] ||
                         bitrixTicket['uf_crm_7_department_head'] ||
                         null;

// Нормализация: trim и ограничение 17 символов (для карточки тикета)
let departmentHead = null;
if (ufDepartmentHead) {
  const trimmed = String(ufDepartmentHead).trim();
  if (trimmed.length > 0) {
    departmentHead = trimmed.length > 17 ? trimmed.substring(0, 17) : trimmed;
  }
}
```

### Для уровня 3 попапа

**Особенности:**
- Использовать полное значение (без ограничения 17 символов)
- Если значение `null` или пустое → группировать в "Без заказчика"
- Не применять `trim()` для обрезки (только для удаления пробелов в начале/конце)
- Сохранять оригинальное значение из тикета

**Примеры значений:**
- "Отдел информационных технологий" → "Отдел информационных технологий" (полное название)
- "Бухгалтерия" → "Бухгалтерия"
- "Отдел продаж и маркетинга" → "Отдел продаж и маркетинга"
- `null` → "Без заказчика"
- `""` (пустая строка) → "Без заказчика"
- `"   "` (только пробелы) → "Без заказчика"

---

## 📝 Подзадачи

### Подзадача 3.1: Функция группировки тикетов по заказчикам

**Цель:** Создать функцию для группировки тикетов по заказчикам (отделам) с обработкой пустых значений и сортировкой.

#### Задачи

1. **Создать функцию в файле утилит**
   - Файл: `vue-app/src/utils/graph-state/popupNavigationUtils.js`
   - Или добавить в существующий файл утилит

2. **Реализовать функцию группировки**
   ```javascript
   /**
    * Группировать тикеты по заказчикам (отделам)
    * 
    * Группирует тикеты по полю departmentHead (UF_CRM_7_DEPARTMENT_HEAD).
    * Тикеты без заказчика (null, пустая строка) группируются в "Без заказчика".
    * Результат сортируется по убыванию количества тикетов.
    * 
    * @param {Array} tickets - Массив тикетов для группировки
    * @returns {Array} Массив заказчиков с количеством тикетов, отсортированный по убыванию
    * 
    * @example
    * const tickets = [
    *   { id: 1, departmentHead: "Отдел ИТ" },
    *   { id: 2, departmentHead: "Бухгалтерия" },
    *   { id: 3, departmentHead: "Отдел ИТ" },
    *   { id: 4, departmentHead: null }
    * ];
    * 
    * const result = groupTicketsByDepartment(tickets);
    * // [
    * //   { departmentName: "Отдел ИТ", count: 2 },
    * //   { departmentName: "Бухгалтерия", count: 1 },
    * //   { departmentName: "Без заказчика", count: 1 }
    * // ]
    */
   export function groupTicketsByDepartment(tickets) {
     if (!tickets || tickets.length === 0) {
       return [];
     }
     
     // Объект для группировки: ключ - название заказчика, значение - количество
     const departmentsMap = {};
     
     tickets.forEach(ticket => {
       // Извлечение значения заказчика
       let departmentName = ticket.departmentHead;
       
       // Нормализация значения
       if (!departmentName) {
         // Если значение null, undefined, пустая строка или только пробелы
         departmentName = 'Без заказчика';
       } else {
         // Приведение к строке и удаление пробелов в начале/конце
         departmentName = String(departmentName).trim();
         
         // Если после trim осталась пустая строка, относим к "Без заказчика"
         if (departmentName.length === 0) {
           departmentName = 'Без заказчика';
         }
       }
       
       // Группировка: увеличиваем счетчик для заказчика
       if (!departmentsMap[departmentName]) {
         departmentsMap[departmentName] = {
           departmentName: departmentName,
           count: 0
         };
       }
       
       departmentsMap[departmentName].count++;
     });
     
     // Преобразование объекта в массив
     const departments = Object.values(departmentsMap);
     
     // Сортировка по убыванию количества тикетов
     departments.sort((a, b) => {
       // Сначала по количеству (убывание)
       if (b.count !== a.count) {
         return b.count - a.count;
       }
       
       // Если количество одинаковое, сортируем по названию (алфавитный порядок)
       return a.departmentName.localeCompare(b.departmentName, 'ru');
     });
     
     return departments;
   }
   ```

3. **Обработка граничных случаев**
   - `null` → "Без заказчика"
   - `undefined` → "Без заказчика"
   - `""` (пустая строка) → "Без заказчика"
   - `"   "` (только пробелы) → "Без заказчика"
   - Отсутствие поля `departmentHead` в тикете → "Без заказчика"

4. **Сортировка результатов**
   - Первичная сортировка: по убыванию количества тикетов
   - Вторичная сортировка: по алфавиту (если количество одинаковое)
   - Использовать `localeCompare` для корректной сортировки русских названий

5. **Добавить JSDoc комментарии**
   - Описание функции
   - Описание параметров
   - Описание возвращаемого значения
   - Примеры использования

**Критерии завершения:**
- [ ] Функция `groupTicketsByDepartment()` создана
- [ ] Группировка работает корректно
- [ ] Обработка пустых значений реализована
- [ ] Сортировка по убыванию количества работает
- [ ] Вторичная сортировка по алфавиту работает
- [ ] Граничные случаи обработаны
- [ ] JSDoc комментарии добавлены

---

### Подзадача 3.2: Обновление состояния компонента (уровень 3)

**Цель:** Добавить состояние для уровня 3 в компонент `EmployeeDetailsModal.vue` и реализовать функции перехода.

#### Задачи

1. **Добавить ref для данных уровня 3**
   ```javascript
   // В <script setup> компонента EmployeeDetailsModal.vue
   
   // Данные уровня 3
   const level3Data = ref(null);
   ```

2. **Структура данных уровня 3**
   ```javascript
   level3Data: {
     employeeId: number,           // ID сотрудника
     employeeName: string,         // Имя сотрудника
     stageId: string,              // ID стадии ('formed', 'review', 'execution')
     stageName: string,            // Название стадии
     dateCategory: string,         // Ключ категории давности ('today', 'yesterday', ...)
     dateCategoryLabel: string,    // Название категории ('СЕГОДНЯ', 'ВЧЕРА', ...)
     totalCount: number,           // Общее количество тикетов в градации
     departments: [                 // Массив заказчиков
       {
         departmentName: string,    // Название заказчика (полное, без сокращений)
         count: number            // Количество тикетов у заказчика
       }
     ]
   }
   ```

3. **Инициализация состояния**
   - Инициализировать `level3Data` как `null`
   - Убедиться, что состояние сбрасывается при закрытии попапа

4. **Функция перехода на уровень 3**
   ```javascript
   /**
    * Переход на уровень 3 (детализация по заказчикам)
    * 
    * @param {Object} category - Объект категории давности из уровня 2
    * @param {string} category.category - Ключ категории ('today', 'yesterday', ...)
    * @param {string} category.label - Название категории ('СЕГОДНЯ', 'ВЧЕРА', ...)
    * @param {Array} category.tickets - Массив тикетов в категории
    */
   function goToLevel3(category) {
     if (!category || !category.tickets || category.tickets.length === 0) {
       // Если нет тикетов в категории, не переходим на уровень 3
       console.warn('Нет тикетов в выбранной категории');
       return;
     }
     
     // Сохранить данные уровня 2 (если еще не сохранены)
     if (!level2Data.value) {
       // Данные уровня 2 должны быть уже сохранены при переходе на уровень 2
       console.error('Данные уровня 2 не найдены');
       return;
     }
     
     // Группировать тикеты по заказчикам
     const departments = groupTicketsByDepartment(category.tickets);
     
     // Сохранить данные уровня 3
     level3Data.value = {
       employeeId: level2Data.value.employeeId,
       employeeName: level2Data.value.employeeName,
       stageId: level2Data.value.stageId,
       stageName: level2Data.value.stageName,
       dateCategory: category.category,
       dateCategoryLabel: category.label,
       totalCount: category.count,
       departments: departments
     };
     
     // Перейти на уровень 3
     popupLevel.value = 3;
   }
   ```

5. **Обновить функцию сброса состояния**
   ```javascript
   /**
    * Сброс состояния попапа при закрытии
    */
   function resetPopup() {
     popupLevel.value = 1;
     level1Data.value = null;
     level2Data.value = null;
     level3Data.value = null;
   }
   
   /**
    * Закрытие модального окна
    */
   function close() {
     resetPopup();
     emit('close');
   }
   ```

6. **Импортировать функцию группировки**
   ```javascript
   import { groupTicketsByDepartment } from '@/utils/graph-state/popupNavigationUtils.js';
   ```

**Критерии завершения:**
- [ ] Ref `level3Data` добавлен в компонент
- [ ] Структура данных уровня 3 определена
- [ ] Функция `goToLevel3()` реализована
- [ ] Функция `resetPopup()` обновлена
- [ ] Импорты добавлены
- [ ] Обработка граничных случаев реализована

---

### Подзадача 3.3: Обновление шаблона компонента (уровень 3)

**Цель:** Добавить отображение уровня 3 в шаблон компонента с таблицей заказчиков.

#### Задачи

1. **Добавить условное отображение уровня 3**
   ```vue
   <!-- Уровень 3: Детализация по заказчикам -->
   <div v-if="popupLevel === 3 && level3Data" class="level-3">
     <!-- Контент уровня 3 -->
   </div>
   ```

2. **Создать шаблон уровня 3**
   ```vue
   <!-- Уровень 3: Детализация по заказчикам -->
   <div v-if="popupLevel === 3 && level3Data" class="level-3">
     <div class="modal-header">
       <button class="btn-back" @click="goBack" aria-label="Назад">
         ←
       </button>
       <div class="header-info">
         <h3>{{ level3Data.employeeName }}</h3>
         <div class="header-badges">
           <span class="date-badge">{{ level3Data.dateCategoryLabel }}</span>
           <span class="stage-badge">{{ level3Data.stageName }}</span>
         </div>
       </div>
       <span class="modal-total">Всего: {{ level3Data.totalCount }} тикетов</span>
       <button class="modal-close" @click="close" aria-label="Закрыть">×</button>
     </div>
     
     <div class="modal-body">
       <div v-if="level3Data.departments.length === 0" class="no-data">
         Нет данных о заказчиках
       </div>
       
       <div v-else class="departments-table-container">
         <table class="departments-table">
           <thead>
             <tr>
               <th class="col-department">Заказчик</th>
               <th class="col-count">Количество тикетов</th>
             </tr>
           </thead>
           <tbody>
             <tr
               v-for="(department, index) in level3Data.departments"
               :key="index"
               class="department-row"
             >
               <td class="col-department">
                 <span class="department-name">{{ department.departmentName }}</span>
               </td>
               <td class="col-count">
                 <span class="count-value">{{ department.count }}</span>
               </td>
             </tr>
           </tbody>
         </table>
       </div>
     </div>
   </div>
   ```

3. **Добавить хлебные крошки (опционально)**
   ```vue
   <!-- Хлебные крошки для навигации -->
   <div class="breadcrumbs">
     <span class="breadcrumb-item" @click="goToLevel1">
       {{ level1Data?.stageName || 'Стадия' }}
     </span>
     <span class="breadcrumb-separator">→</span>
     <span class="breadcrumb-item" @click="goToLevel2">
       {{ level2Data?.employeeName || 'Сотрудник' }}
     </span>
     <span class="breadcrumb-separator">→</span>
     <span class="breadcrumb-item active">
       {{ level3Data?.dateCategoryLabel || 'Градация' }}
     </span>
   </div>
   ```

4. **Обработка пустых данных**
   - Если нет заказчиков → показать сообщение "Нет данных о заказчиках"
   - Если `totalCount === 0` → показать сообщение "Нет тикетов в этой категории"

**Критерии завершения:**
- [ ] Условное отображение уровня 3 реализовано
- [ ] Шаблон уровня 3 создан
- [ ] Заголовок с информацией о сотруднике, градации и стадии отображается
- [ ] Таблица заказчиков отображается корректно
- [ ] Обработка пустых данных реализована
- [ ] Кнопка "Назад" работает

---

### Подзадача 3.4: Обработка клика на временную градацию (уровень 2 → 3)

**Цель:** Реализовать переход на уровень 3 при клике на строку временной градации в уровне 2.

#### Задачи

1. **Изучить текущую структуру уровня 2**
   - Найти элементы списка временных градаций
   - Определить, где добавить обработчик клика

2. **Добавить обработчик клика на строку градации**
   ```vue
   <!-- В уровне 2, в списке категорий -->
   <div
     v-for="category in level2Data.dateCategories"
     :key="category.category"
     class="category-item"
     @click="handleCategoryClick(category)"
   >
     <!-- Текущий контент строки категории -->
   </div>
   ```

3. **Создать функцию обработки клика**
   ```javascript
   /**
    * Обработка клика на временную градацию (переход на уровень 3)
    * 
    * @param {Object} category - Объект категории давности
    */
   function handleCategoryClick(category) {
     if (!category || category.count === 0) {
       // Если нет тикетов в категории, не переходим
       console.warn('Нет тикетов в категории:', category.label);
       return;
     }
     
     // Переход на уровень 3
     goToLevel3(category);
   }
   ```

4. **Убедиться, что данные уровня 2 содержат тикеты**
   - При создании данных уровня 2 в функции `groupTicketsByDateCategory()` 
   - Убедиться, что массив `tickets` сохраняется в каждой категории
   - Это необходимо для группировки по заказчикам на уровне 3

5. **Обновить функцию `groupTicketsByDateCategory()`**
   - Убедиться, что массив `tickets` сохраняется в каждой категории
   - Это уже должно быть реализовано в этапе 2, но нужно проверить

**Проверка сохранения тикетов в категориях:**
```javascript
// В функции groupTicketsByDateCategory() (этап 2)
categories[category].tickets.push(ticket); // ✅ Должно быть реализовано
```

**Критерии завершения:**
- [ ] Обработчик клика добавлен на строки категорий
- [ ] Функция `handleCategoryClick()` реализована
- [ ] Переход на уровень 3 выполняется корректно
- [ ] Данные уровня 2 содержат массив тикетов в каждой категории
- [ ] Обработка пустых категорий реализована

---

### Подзадача 3.5: Стилизация уровня 3

**Цель:** Добавить стили для уровня 3, соответствующие дизайну предыдущих уровней и гайдлайнам Bitrix24.

#### Задачи

1. **Изучить стили уровней 1 и 2**
   - Найти стили для `.level-1` и `.level-2`
   - Понять цветовую схему и отступы
   - Сохранить единообразие дизайна

2. **Добавить стили для уровня 3**
   ```css
   /* Уровень 3: Детализация по заказчикам */
   .level-3 {
     /* Наследуем базовые стили модального окна */
   }
   
   .header-info {
     display: flex;
     flex-direction: column;
     gap: 8px;
     flex: 1;
   }
   
   .header-info h3 {
     margin: 0;
     font-size: 18px;
     font-weight: 600;
     color: var(--b24-text-primary, #1f2937);
   }
   
   .header-badges {
     display: flex;
     gap: 8px;
     flex-wrap: wrap;
   }
   
   .date-badge {
     display: inline-block;
     padding: 4px 12px;
     background-color: var(--b24-info, #17a2b8);
     color: white;
     border-radius: var(--radius-xl, 9999px);
     font-size: 12px;
     font-weight: 600;
   }
   
   .stage-badge {
     display: inline-block;
     padding: 4px 12px;
     background-color: var(--b24-primary, #007bff);
     color: white;
     border-radius: var(--radius-xl, 9999px);
     font-size: 12px;
     font-weight: 600;
   }
   
   .departments-table-container {
     width: 100%;
     overflow-x: auto;
   }
   
   .departments-table {
     width: 100%;
     border-collapse: collapse;
     background-color: var(--b24-bg-white, #ffffff);
   }
   
   .departments-table thead {
     background-color: var(--b24-bg-light, #f3f4f6);
     border-bottom: 2px solid var(--b24-border-medium, #d1d5db);
   }
   
   .departments-table th {
     padding: 12px 16px;
     text-align: left;
     font-size: 14px;
     font-weight: 600;
     color: var(--b24-text-primary, #1f2937);
     text-transform: uppercase;
     letter-spacing: 0.5px;
   }
   
   .departments-table .col-department {
     width: 70%;
   }
   
   .departments-table .col-count {
     width: 30%;
     text-align: right;
   }
   
   .departments-table tbody tr {
     border-bottom: 1px solid var(--b24-border-light, #e5e7eb);
     transition: background-color 0.2s ease;
   }
   
   .departments-table tbody tr:hover {
     background-color: var(--b24-bg-light, #f3f4f6);
   }
   
   .departments-table tbody tr:last-child {
     border-bottom: none;
   }
   
   .department-row {
     cursor: default; /* Таблица не интерактивна, только для просмотра */
   }
   
   .department-name {
     font-size: 14px;
     font-weight: 500;
     color: var(--b24-text-primary, #1f2937);
     word-break: break-word; /* Перенос длинных названий */
   }
   
   .count-value {
     font-size: 14px;
     font-weight: 600;
     color: var(--b24-text-secondary, #6b7280);
   }
   ```

3. **Стили для хлебных крошек (если добавлены)**
   ```css
   .breadcrumbs {
     display: flex;
     align-items: center;
     gap: 8px;
     padding: 12px 0;
     margin-bottom: 16px;
     border-bottom: 1px solid var(--b24-border-light, #e5e7eb);
     font-size: 13px;
   }
   
   .breadcrumb-item {
     color: var(--b24-text-secondary, #6b7280);
     cursor: pointer;
     transition: color 0.2s ease;
   }
   
   .breadcrumb-item:hover {
     color: var(--b24-primary, #007bff);
     text-decoration: underline;
   }
   
   .breadcrumb-item.active {
     color: var(--b24-text-primary, #1f2937);
     font-weight: 600;
     cursor: default;
   }
   
   .breadcrumb-item.active:hover {
     text-decoration: none;
   }
   
   .breadcrumb-separator {
     color: var(--b24-text-muted, #9ca3af);
   }
   ```

4. **Адаптивность для мобильных устройств**
   ```css
   @media (max-width: 768px) {
     .header-info {
       width: 100%;
     }
     
     .header-badges {
       width: 100%;
     }
     
     .departments-table-container {
       overflow-x: scroll;
       -webkit-overflow-scrolling: touch;
     }
     
     .departments-table {
       min-width: 400px; /* Минимальная ширина для читаемости */
     }
     
     .departments-table th,
     .departments-table td {
       padding: 10px 12px;
       font-size: 13px;
     }
     
     .department-name {
       font-size: 13px;
     }
     
     .count-value {
       font-size: 13px;
     }
   }
   ```

5. **Стили для пустого состояния**
   ```css
   .no-data {
     text-align: center;
     padding: 40px 20px;
     color: var(--b24-text-muted, #9ca3af);
     font-size: 14px;
     font-style: italic;
   }
   ```

6. **Улучшение читаемости таблицы**
   ```css
   /* Чередование цветов строк (опционально) */
   .departments-table tbody tr:nth-child(even) {
     background-color: var(--b24-bg, #f9fafb);
   }
   
   .departments-table tbody tr:nth-child(even):hover {
     background-color: var(--b24-bg-light, #f3f4f6);
   }
   
   /* Выделение первой строки (топ заказчик) */
   .departments-table tbody tr:first-child {
     background-color: var(--b24-primary-lighter, #e7f3ff);
   }
   
   .departments-table tbody tr:first-child:hover {
     background-color: var(--b24-primary-light, #cfe2ff);
   }
   ```

**Критерии завершения:**
- [ ] Стили для уровня 3 добавлены
- [ ] Таблица заказчиков стилизована корректно
- [ ] Заголовок с бейджами отображается правильно
- [ ] Адаптивность для мобильных реализована
- [ ] Стили для пустого состояния добавлены
- [ ] Читаемость таблицы улучшена
- [ ] Стили соответствуют гайдлайнам Bitrix24

---

## 🔄 Интеграция с предыдущими уровнями

### Обновление функции goBack()

**Цель:** Реализовать корректный возврат с уровня 3 на уровень 2.

```javascript
/**
 * Возврат на предыдущий уровень
 */
function goBack() {
  if (popupLevel.value === 3) {
    // Возврат с уровня 3 на уровень 2
    popupLevel.value = 2;
    level3Data.value = null; // Очистить данные уровня 3
  } else if (popupLevel.value === 2) {
    // Возврат с уровня 2 на уровень 1
    popupLevel.value = 1;
    level2Data.value = null; // Очистить данные уровня 2
  }
  // На уровне 1 кнопка "Назад" не отображается
}
```

### Обновление функции goToLevel1() (для хлебных крошек)

```javascript
/**
 * Переход на уровень 1 (через хлебные крошки)
 */
function goToLevel1() {
  if (level1Data.value) {
    popupLevel.value = 1;
    level2Data.value = null;
    level3Data.value = null;
  }
}
```

### Обновление функции goToLevel2() (для хлебных крошек)

```javascript
/**
 * Переход на уровень 2 (через хлебные крошки)
 */
function goToLevel2() {
  if (level2Data.value) {
    popupLevel.value = 2;
    level3Data.value = null;
  }
}
```

---

## 📊 Примеры данных

### Входные данные (тикеты категории)

```javascript
const categoryTickets = [
  {
    id: 1001,
    title: "Тикет 1",
    departmentHead: "Отдел информационных технологий",
    createdAt: "2024-12-12T10:00:00+03:00"
  },
  {
    id: 1002,
    title: "Тикет 2",
    departmentHead: "Бухгалтерия",
    createdAt: "2024-12-12T11:00:00+03:00"
  },
  {
    id: 1003,
    title: "Тикет 3",
    departmentHead: "Отдел информационных технологий",
    createdAt: "2024-12-12T12:00:00+03:00"
  },
  {
    id: 1004,
    title: "Тикет 4",
    departmentHead: null, // Без заказчика
    createdAt: "2024-12-12T13:00:00+03:00"
  }
];
```

### Выходные данные (после группировки)

```javascript
const departments = [
  {
    departmentName: "Отдел информационных технологий",
    count: 2
  },
  {
    departmentName: "Бухгалтерия",
    count: 1
  },
  {
    departmentName: "Без заказчика",
    count: 1
  }
];
```

### Структура level3Data

```javascript
level3Data = {
  employeeId: 123,
  employeeName: "Иван Иванов",
  stageId: "formed",
  stageName: "Сформировано обращение",
  dateCategory: "today",
  dateCategoryLabel: "СЕГОДНЯ",
  totalCount: 4,
  departments: [
    {
      departmentName: "Отдел информационных технологий",
      count: 2
    },
    {
      departmentName: "Бухгалтерия",
      count: 1
    },
    {
      departmentName: "Без заказчика",
      count: 1
    }
  ]
};
```

---

## 🔍 Обработка граничных случаев

### Случай 1: Нет тикетов в категории

```javascript
// В функции handleCategoryClick()
if (!category || category.count === 0) {
  console.warn('Нет тикетов в категории:', category?.label);
  // Можно показать уведомление пользователю
  return;
}
```

### Случай 2: Все тикеты без заказчика

```javascript
// Результат группировки
departments = [
  {
    departmentName: "Без заказчика",
    count: 10
  }
];
// Таблица отобразит одну строку с "Без заказчика"
```

### Случай 3: Одинаковое количество тикетов у заказчиков

```javascript
// Сортировка: сначала по количеству (убывание), затем по алфавиту
departments = [
  { departmentName: "Бухгалтерия", count: 5 },
  { departmentName: "Отдел ИТ", count: 5 },
  { departmentName: "Отдел продаж", count: 3 }
];
// "Бухгалтерия" будет перед "Отдел ИТ" (алфавитный порядок)
```

### Случай 4: Очень длинные названия заказчиков

```css
/* В стилях таблицы */
.department-name {
  word-break: break-word; /* Перенос длинных названий */
  max-width: 500px; /* Ограничение ширины (опционально) */
}
```

---

## 📚 Связанные файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа
- `vue-app/src/utils/graph-state/popupNavigationUtils.js` — утилиты навигации
- `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js` — маппер тикетов
- `DOCS/TASKS/TASK-033-02-level-2-implementation.md` — этап 2 (предыдущий)
- `DOCS/TASKS/TASK-030-ticket-card-department-and-date.md` — задача с полем заказчика

---

## ✅ Критерии приёмки этапа 3

- [ ] Уровень 3 отображается корректно при клике на временную градацию
- [ ] Группировка по заказчикам работает корректно
- [ ] Таблица заказчиков отображается с полными названиями
- [ ] Сортировка по убыванию количества работает
- [ ] Обработка тикетов без заказчика реализована ("Без заказчика")
- [ ] Навигация между уровнями 2 и 3 работает (вперед и назад)
- [ ] Кнопка "Назад" возвращает на уровень 2
- [ ] Стили применены корректно
- [ ] Адаптивность для мобильных устройств реализована
- [ ] Обработка граничных случаев работает
- [ ] Хлебные крошки работают (если добавлены)

---

## 🔄 Зависимости

**Зависит от:**
- Этап 2 (TASK-033-02): Реализация уровня 2 (временные градации)
  - Необходимы данные уровня 2 с массивом `tickets` в каждой категории
  - Функция `groupTicketsByDateCategory()` должна сохранять тикеты

**Требует для следующего этапа:**
- Этап 4 (TASK-033-04): Интеграция навигации и состояния попапа
  - Все три уровня должны работать вместе
  - Навигация между всеми уровнями должна быть реализована

---

## 📝 История правок

- **2025-12-12 09:14 (UTC+3, Брест):** Создан документ этапа 3
  - Добавлены 5 подзадач с детальной реализацией
  - Описана работа с полем заказчика (UF_CRM_7_DEPARTMENT_HEAD)
  - Добавлены примеры кода, стилей и данных
  - Описана обработка граничных случаев

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Дата:** 2025-12-12 09:14 (UTC+3, Брест)




