# TASK-077: Рефакторинг крупных компонентов Vue.js

**Дата создания:** 2025-12-24 17:00 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-065 (Рефакторинг модуля "График приёма и закрытий сектора 1С")

---

## 📌 Краткое резюме

**Что реализуется:**
- Рефакторинг крупных Vue.js компонентов (1000+ строк) в папке `vue-app/src/components/graph-admission-closure/`
- Разбиение монолитных компонентов на меньшие, переиспользуемые части
- Улучшение архитектуры и поддерживаемости кода
- Создание composables для переиспользования логики

**Компоненты для рефакторинга:**
- `GraphAdmissionClosureChart.vue` (2650 строк) - основной компонент графика
- `ResponsibleModal.vue` (1758 строк) - модальное окно с системой вкладок
- `GraphAdmissionClosureDashboard.vue` (1440 строк) - дашборд недельного режима
- `LineChartMonths.vue` (1167 строк) - графики и таблица месячного режима

**Цель:** Снизить сложность компонентов, улучшить их поддерживаемость и создать переиспользуемые части.

---

## 📋 Описание

Анализ показал наличие крупных компонентов с большим количеством строк кода, что затрудняет поддержку и развитие. Необходимо провести рефакторинг для:

### Текущие проблемы:

1. **Монолитные компоненты:** Большие файлы содержат смешанную логику (шаблоны, скрипты, стили)
2. **Дублирование кода:** Повторяющаяся логика в разных компонентах
3. **Сложность поддержки:** Трудно найти и исправить ошибки в больших файлах
4. **Отсутствие переиспользования:** Логика не вынесена в composables

### Цели рефакторинга:

1. **Разбиение компонентов:** Каждый крупный компонент разбивается на 3-6 меньших
2. **Создание composables:** Выделение логики в переиспользуемые composables
3. **Улучшение структуры:** Четкое разделение ответственности
4. **Снижение сложности:** Компоненты становятся проще для понимания и тестирования

---

## 🎯 Контекст

### Текущая структура папки `vue-app/src/components/graph-admission-closure/`:

```
graph-admission-closure/
├── CarryoverDurationModal.vue (870 строк)     ✅ OK
├── GraphAdmissionClosureChart.vue (2650 строк) ❌ РЕФАКТОРИТЬ
├── GraphAdmissionClosureDashboard.vue (1440 строк) ❌ РЕФАКТОРИТЬ
├── GraphAdmissionClosureMonthsDashboard.vue (предположительно 1000+ строк) ❌ РЕФАКТОРИТЬ
├── LineChartMonths.vue (1167 строк) ❌ РЕФАКТОРИТЬ
├── MonthsModePreloader.vue (269 строк)       ✅ OK
├── PeriodModeInfoModal.vue (387 строк)       ✅ OK
├── PeriodModeSelector.vue (230 строк)        ✅ OK
├── ResponsibleModal.vue (1758 строк)          ❌ РЕФАКТОРИТЬ
├── StagesModal.vue (902 строк)               ✅ OK
├── SummaryCardsMonths.vue (610 строк)        ✅ OK
```

### Принципы рефакторинга:

1. **Один файл = один компонент:** Каждый Vue.js файл должен содержать один компонент
2. **Composables для логики:** Бизнес-логика выносится в composables
3. **Базовые компоненты:** Создание переиспользуемых базовых компонентов
4. **Четкое разделение:** UI, логика, данные разделены

---

## 🏗️ Модули и компоненты

### Новые папки и компоненты:

#### 1. Базовые компоненты (shared):
```
vue-app/src/components/graph-admission-closure/shared/
├── BaseModal.vue - базовый модал для всех модальных окон
├── BaseChart.vue - базовый компонент графика
├── MetricCard.vue - базовая карточка метрики
├── ProgressIndicator.vue - индикатор прогресса
```

#### 2. Composables (logic):
```
vue-app/src/composables/graph-admission-closure/
├── useChartData.js - логика работы с данными графиков
├── useModalState.js - управление состоянием модальных окон
├── useMetricsCalculation.js - расчет метрик и процентов
├── useCacheManagement.js - работа с кешем
├── useApiIntegration.js - интеграция с API
```

#### 3. Разбитые компоненты:

##### GraphAdmissionClosureChart.vue →:
```
chart/
├── ChartHeader.vue - заголовок и контролы
├── ChartSummary.vue - сводная информация
├── ChartContainer.vue - контейнер графика
├── ChartFooter.vue - подвал с дополнительной информацией
```

##### ResponsibleModal.vue →:
```
responsible/
├── ResponsibleTabs.vue - система вкладок
├── CategoriesTab.vue - вкладка "По категориям"
├── EmployeesTab.vue - вкладка "По сотрудникам"
├── ResponsibleList.vue - список ответственных
├── EmployeeGradations.vue - градации сотрудника
```

##### GraphAdmissionClosureDashboard.vue →:
```
dashboard/
├── DashboardHeader.vue - заголовок дашборда
├── DashboardSummary.vue - сводные метрики
├── DashboardCharts.vue - контейнер графиков
├── DashboardControls.vue - элементы управления
```

##### LineChartMonths.vue →:
```
months/
├── MonthsChartContainer.vue - контейнер графиков
├── MonthsTable.vue - таблица с детализацией
├── MonthsSummary.vue - сводная информация
├── MonthsAnalysis.vue - анализ трендов
```

---

## 📦 Зависимости

### От задач:
- TASK-065: Рефакторинг модуля "График приёма и закрытий сектора 1С" (родительская задача)

### От модулей:
- `vue-app/src/components/graph-admission-closure/` - текущие компоненты для рефакторинга
- `vue-app/src/composables/` - директория для composables (создать, если не существует)

### От библиотек:
- Vue.js 3.x (Composition API)
- Vue ChartJS - для графиков
- SCSS - для стилей

---

## 🎯 Детальный план реализации

### Обзор этапов

Реализация разделена на **4 основных этапа**, каждый из которых соответствует рефакторингу одного крупного компонента:

1. **Этап 1:** Рефакторинг `GraphAdmissionClosureChart.vue` (2650 строк)
2. **Этап 2:** Рефакторинг `ResponsibleModal.vue` (1758 строк)
3. **Этап 3:** Рефакторинг `GraphAdmissionClosureDashboard.vue` (1440 строк)
4. **Этап 4:** Рефакторинг `LineChartMonths.vue` (1167 строк)

---

## 📝 Этап 1: Рефакторинг GraphAdmissionClosureChart.vue

**Цель:** Разбить основной компонент графика (2650 строк) на меньшие компоненты.

**Текущая структура:**
- Шаблон: заголовок + сводная секция + график + контролы
- Скрипт: логика графиков + расчеты + управление состоянием
- Стили: стилизация всех элементов

**План разбиения:**
1. **ChartHeader.vue** - заголовок, контролы типов графиков
2. **ChartSummary.vue** - сводная информация (текущая/предыдущая неделя)
3. **ChartContainer.vue** - основной контейнер графика
4. **ChartFooter.vue** - дополнительная информация

**Создать composables:**
- `useChartData.js` - логика работы с данными графиков
- `useChartControls.js` - управление типами графиков
- `useSummaryMetrics.js` - расчет сводных метрик

---

## 📝 Этап 2: Рефакторинг ResponsibleModal.vue

**Цель:** Разбить сложное модальное окно с вкладками (1758 строк) на компоненты.

**Текущая структура:**
- Шаблон: система вкладок + многоуровневая навигация
- Скрипт: логика вкладок + API вызовы + обработка данных
- Стили: стилизация вкладок и содержимого

**План разбиения:**
1. **ResponsibleTabs.vue** - система вкладок
2. **CategoriesTab.vue** - содержимое вкладки "По категориям"
3. **EmployeesTab.vue** - содержимое вкладки "По сотрудникам"
4. **ResponsibleList.vue** - список ответственных
5. **EmployeeGradations.vue** - градации тикетов сотрудника

**Создать composables:**
- `useResponsibleData.js` - работа с данными ответственных
- `useModalTabs.js` - логика системы вкладок
- `useEmployeeApi.js` - API для работы с сотрудниками

---

## 📝 Этап 3: Рефакторинг GraphAdmissionClosureDashboard.vue

**Цель:** Разбить дашборд недельного режима (1440 строк) на компоненты.

**Текущая структура:**
- Шаблон: заголовок + метрики + графики + управление
- Скрипт: координация компонентов + управление состоянием
- Стили: стилизация всех элементов

**План разбиения:**
1. **DashboardHeader.vue** - заголовок и навигация
2. **DashboardMetrics.vue** - сводные метрики
3. **DashboardCharts.vue** - контейнер графиков
4. **DashboardControls.vue** - элементы управления

**Создать composables:**
- `useDashboardState.js` - управление состоянием дашборда
- `useMetricsDisplay.js` - отображение метрик
- `useChartsIntegration.js` - интеграция графиков

---

## 📝 Этап 4: Рефакторинг LineChartMonths.vue

**Цель:** Разбить компонент месячных графиков (1167 строк) на компоненты.

**Текущая структура:**
- Шаблон: графики + таблица + анализ
- Скрипт: расчеты + анализ трендов + форматирование
- Стили: стилизация графиков и таблиц

**План разбиения:**
1. **MonthsCharts.vue** - контейнер графиков
2. **MonthsTable.vue** - детальная таблица
3. **MonthsAnalysis.vue** - анализ трендов
4. **MonthsSummary.vue** - сводная информация

**Создать composables:**
- `useMonthsData.js` - работа с месячными данными
- `useTrendAnalysis.js` - анализ трендов
- `useTableFormatting.js` - форматирование таблиц

---

## ✅ Критерии приёмки

### Общие критерии для всех этапов:

- [ ] Каждый крупный компонент разбит на 3-6 меньших компонентов
- [ ] Создан хотя бы один composable для каждого этапа
- [ ] Компоненты имеют четкое разделение ответственности
- [ ] Код стал более читаемым и поддерживаемым
- [ ] Функциональность не нарушена (регрессионное тестирование)

### Специфические критерии:

**Этап 1 (GraphAdmissionClosureChart.vue):**
- [ ] Компонент ChartHeader.vue создан и работает
- [ ] Компонент ChartSummary.vue создан и работает
- [ ] Компонент ChartContainer.vue создан и работает
- [ ] Composable useChartData.js создан и используется

**Этап 2 (ResponsibleModal.vue):**
- [ ] Компонент ResponsibleTabs.vue создан и работает
- [ ] Компоненты CategoriesTab.vue и EmployeesTab.vue созданы
- [ ] Composable useResponsibleData.js создан и используется

**Этап 3 (GraphAdmissionClosureDashboard.vue):**
- [ ] Компонент DashboardHeader.vue создан и работает
- [ ] Компонент DashboardMetrics.vue создан и работает
- [ ] Composable useDashboardState.js создан и используется

**Этап 4 (LineChartMonths.vue):**
- [ ] Компонент MonthsCharts.vue создан и работает
- [ ] Компонент MonthsTable.vue создан и работает
- [ ] Composable useMonthsData.js создан и используется

---

## 📝 Примечания

### Важные замечания:

1. **Последовательность:** Этапы выполняются последовательно, каждый этап зависит от предыдущего
2. **Тестирование:** После каждого этапа проводится регрессионное тестирование
3. **Документация:** Каждый новый компонент и composable документируется
4. **Совместимость:** Существующий API и интерфейсы не меняются

### Риски:

1. **Регрессии:** Возможны ошибки при разбиении компонентов
2. **Производительность:** Увеличение количества компонентов может повлиять на производительность
3. **Сложность:** Переиспользование логики требует дополнительного внимания

### Метрики успеха:

- Снижение количества строк в файлах на 60-80%
- Увеличение количества компонентов для лучшего разделения ответственности
- Создание переиспользуемых composables
- Улучшение поддерживаемости кода

---

## 🔄 История правок

- 2025-12-24 17:00 (UTC+3, Брест): Создана задача TASK-077 для рефакторинга крупных компонентов
- 2025-12-24 17:00 (UTC+3, Брест): Определены 4 этапа рефакторинга по количеству крупных компонентов

---

## 📚 Связанные документы

- `DOCS/TASKS/TASK-065-refactor-graph-1c-admission-closure.md` — родительская задача рефакторинга
- `DOCS/TASKS/ARCHIVE/TASK-065-refactor-graph-1c-admission-closure.md` — архивная версия
- `vue-app/src/components/graph-admission-closure/` — папка с компонентами для рефакторинга
