# TASK-035: Улучшение визуализации точек на линейном графике состояния сектора 1С

**Дата создания:** 2025-12-13 15:13 (UTC+3, Брест)  
**Статус:** В работе  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-032

---

## 📋 Описание задачи

В модуле "График состояния сектора 1С" в разделе с отображением **линейного графика** существует проблема: когда несколько точек разных стадий (этапов) имеют одинаковое значение Y на одной временной точке X, они визуально перекрываются, что затрудняет:

1. **Визуальное различие точек** — даже при разных стилях точек (круг, треугольник, квадрат) при полном перекрытии сложно различить отдельные точки
2. **Клик по точке** — при полном перекрытии сложно кликнуть на конкретную точку нужной стадии
3. **Чтение значений** — пользователь не видит числовое значение точки без наведения курсора

**Текущая ситуация:**
- Точки разных стадий имеют разные стили (круг, треугольник, квадрат) — реализовано в TASK-032
- При одинаковых значениях Y точки полностью перекрываются
- Размер точек увеличивается при перекрытии (через плагин `overlappingPointsPlugin`)
- Значения точек видны только в tooltip при наведении

**Целевое состояние:**
- Точки с одинаковыми значениями Y должны иметь визуальное разделение в сетке матрицы (небольшое смещение)
- Рядом с каждой точкой должна отображаться цифра значения этой точки
- Клик по точке должен быть более точным и удобным

---

## 🎯 Цель

Улучшить UX линейного графика, обеспечив:
1. **Визуальное разделение точек** — точки с одинаковыми значениями должны быть слегка смещены друг относительно друга для лучшей различимости
2. **Читаемость значений** — числовые значения точек должны отображаться рядом с точками без необходимости наведения курсора
3. **Улучшенную кликабельность** — каждая точка должна быть легко кликабельна, даже при перекрытии

---

## 🔍 Анализ проблемы

### Текущая реализация

**Компонент:** `GraphStateChart.vue`  
**Расположение:** `vue-app/src/components/graph-state/GraphStateChart.vue`

**Технические детали:**
- Используется Chart.js 4.x с vue-chartjs
- Линейный график (`type: 'line'`)
- Каждый этап (стадия) — отдельный dataset (линия)
- Настройки точек:
  - `pointRadius: 6` — радиус точки
  - `pointHoverRadius: 8` — радиус при наведении
  - `pointStyle` — разные стили для каждого этапа (круг, треугольник, квадрат)
- Плагин `overlappingPointsPlugin` увеличивает размер точек при перекрытии

**Проблема:**
```javascript
// Пример: три стадии имеют одинаковое значение (например, 10 тикетов)
// на временной точке "Начало недели"
datasets: [
  { label: 'Сформировано обращение', data: [10, 15, 12], pointStyle: 'circle' },
  { label: 'Рассмотрение ТЗ', data: [10, 18, 14], pointStyle: 'triangle' },      // ← перекрывается
  { label: 'Исполнение', data: [10, 20, 16], pointStyle: 'rect' }              // ← перекрывается
]
```

В этом случае все три точки с координатами (0, 10) будут наложены друг на друга, и:
- Визуально видна только одна точка (последняя нарисованная)
- Клик попадает только на одну точку
- Значение видно только в tooltip

---

## 💡 Решение

### Подход 1: Jitter (смещение точек) + Подписи значений ⭐ (Рекомендуется)

**Идея:** 
1. Применить небольшое смещение точек по оси X (jitter) для точек с одинаковыми значениями Y
2. Добавить подписи с числовыми значениями рядом с каждой точкой

**Преимущества:**
- Все точки видны и кликабельны
- Значения видны без наведения курсора
- Не искажает общую картину графика (смещение минимальное)
- Соответствует best practices для визуализации перекрывающихся точек

**Реализация:**
- Кастомный плагин Chart.js для смещения точек и отрисовки подписей
- Смещение по X: ±2-3px для точек с одинаковыми Y
- Подписи значений: отрисовка текста рядом с точкой

**Визуальный эффект:**
- Точки с одинаковыми значениями слегка смещены по горизонтали
- Рядом с каждой точкой видно её числовое значение
- Все точки остаются кликабельными

---

## 📋 План реализации (5 этапов)

### Этап 1: Анализ структуры данных и подготовка утилиты для определения перекрытий

**Цель:** Создать утилиту для определения точек с одинаковыми значениями Y на одной временной позиции X.

**Задачи:**
1. Изучить существующую утилиту `lineChartUtils.js` (созданную в TASK-032)
2. Расширить функцию `findOverlappingPoints` или создать новую функцию для определения точек с одинаковыми Y
3. Функция должна возвращать информацию о точках, которые нужно сместить
4. Добавить функцию для вычисления смещения (jitter) для каждой точки

**Технические детали:**
- Файл: `vue-app/src/utils/graph-state/lineChartUtils.js` (расширение существующего)
- Новая функция: `calculatePointJitter(chart, positionIndex, threshold)` 
- Возвращает массив объектов с информацией о смещении для каждой точки:
  ```javascript
  [
    { datasetIndex: 0, jitterX: -2, value: 10 },
    { datasetIndex: 1, jitterX: 0, value: 10 },
    { datasetIndex: 2, jitterX: 2, value: 10 }
  ]
  ```

**Критерии завершения:**
- [ ] Утилита определяет точки с одинаковыми значениями Y
- [ ] Функция вычисляет смещение для каждой точки
- [ ] Смещение распределяется равномерно (например, -2px, 0px, +2px для 3 точек)
- [ ] Добавлены JSDoc комментарии

**Ожидаемый результат:**
Утилита, которая может определить все точки с одинаковыми значениями Y на одной временной позиции и вычислить оптимальное смещение для каждой точки.

---

### Этап 2: Создание плагина для смещения точек (Jitter)

**Цель:** Разработать плагин Chart.js, который применяет смещение к точкам с одинаковыми значениями Y.

**Задачи:**
1. Создать файл `vue-app/src/components/graph-state/plugins/pointJitterPlugin.js`
2. Реализовать плагин Chart.js с методом `beforeDatasetsDraw` или использовать кастомную отрисовку точек
3. Плагин должен:
   - Использовать утилиту из Этапа 1 для определения точек, требующих смещения
   - Применять смещение по оси X к точкам с одинаковыми Y
   - Сохранять координаты оригинальных точек для tooltip и кликов
4. Смещение должно быть минимальным (2-3px), чтобы не искажать общую картину графика

**Технические детали:**
- Новый файл: `vue-app/src/components/graph-state/plugins/pointJitterPlugin.js`
- Структура плагина:
  ```javascript
  export const pointJitterPlugin = {
    id: 'pointJitterPlugin',
    beforeDatasetsDraw: (chart) => {
      // Применение смещения к точкам
    }
  };
  ```
- Смещение: ±2-3px по оси X
- Применяется только к точкам с одинаковыми значениями Y (в пределах threshold 0.5)

**Критерии завершения:**
- [ ] Файл `pointJitterPlugin.js` создан
- [ ] Плагин корректно применяет смещение к точкам
- [ ] Смещение минимальное и не искажает график
- [ ] Tooltip и клики работают корректно (используют оригинальные координаты)
- [ ] Плагин работает только для линейных графиков

**Ожидаемый результат:**
Плагин, который автоматически смещает точки с одинаковыми значениями Y, делая их визуально различимыми и кликабельными.

---

### Этап 3: Создание плагина для отрисовки подписей значений

**Цель:** Разработать плагин Chart.js для отрисовки числовых значений рядом с точками графика.

**Задачи:**
1. Создать файл `vue-app/src/components/graph-state/plugins/pointLabelsPlugin.js`
2. Реализовать плагин Chart.js с методом `afterDatasetsDraw`
3. Плагин должен:
   - Проходить по всем точкам всех datasets
   - Отрисовывать текст с числовым значением рядом с каждой точкой
   - Позиционировать подпись справа-сверху или справа-снизу от точки (в зависимости от позиции на графике)
   - Применять стили, соответствующие гайдлайнам Bitrix24
4. Стилизация подписей:
   - Шрифт: `10px Arial, sans-serif`
   - Цвет: цвет точки или серый `#6b7280`
   - Позиция: справа от точки с отступом 4-6px
   - Фон: белый с небольшой прозрачностью для читаемости

**Технические детали:**
- Новый файл: `vue-app/src/components/graph-state/plugins/pointLabelsPlugin.js`
- Структура плагина:
  ```javascript
  export const pointLabelsPlugin = {
    id: 'pointLabelsPlugin',
    afterDatasetsDraw: (chart) => {
      // Отрисовка подписей значений
    }
  };
  ```
- Позиционирование: справа от точки, с небольшим смещением вверх/вниз для избежания перекрытия с другими подписями
- Форматирование значений: целые числа без десятичных знаков

**Критерии завершения:**
- [ ] Файл `pointLabelsPlugin.js` создан
- [ ] Плагин отрисовывает значения рядом с каждой точкой
- [ ] Подписи стилизованы в соответствии с требованиями
- [ ] Подписи не перекрывают точки и другие элементы графика
- [ ] Плагин работает только для линейных графиков
- [ ] Производительность не ухудшилась (отрисовка < 50ms)

**Ожидаемый результат:**
Плагин, который автоматически отрисовывает числовые значения рядом с каждой точкой графика, делая значения видимыми без наведения курсора.

---

### Этап 4: Интеграция плагинов в компонент GraphStateChart

**Цель:** Интегрировать оба плагина (jitter и labels) в основной компонент графика.

**Задачи:**
1. Импортировать плагины в `GraphStateChart.vue`
2. Зарегистрировать плагины через `ChartJS.register()`
3. Убедиться, что плагины применяются только к линейным графикам
4. Проверить совместимость с существующим плагином `overlappingPointsPlugin`
5. Протестировать работу плагинов с реальными данными
6. Оптимизировать порядок отрисовки (jitter → точки → labels)

**Технические детали:**
- Файл: `vue-app/src/components/graph-state/GraphStateChart.vue`
- Импорты:
  ```javascript
  import { pointJitterPlugin } from './plugins/pointJitterPlugin.js';
  import { pointLabelsPlugin } from './plugins/pointLabelsPlugin.js';
  ```
- Регистрация: в `setup()` или `onMounted()`
- Условие: `if (chartType.value === 'line')`

**Критерии завершения:**
- [ ] Плагины импортированы и зарегистрированы
- [ ] Плагины работают только для линейных графиков
- [ ] Смещение точек применяется корректно
- [ ] Подписи значений отображаются корректно
- [ ] Совместимость с `overlappingPointsPlugin` проверена
- [ ] Tooltip и клики работают корректно
- [ ] Нет ошибок в консоли

**Ожидаемый результат:**
Полностью интегрированное решение: точки с одинаковыми значениями смещены для лучшей видимости, рядом с каждой точкой отображается её значение.

---

### Этап 5: Тестирование, оптимизация и финализация

**Цель:** Протестировать решение, оптимизировать производительность и завершить реализацию.

**Задачи:**
1. **Функциональное тестирование:**
   - Проверить смещение точек при различных сценариях перекрытий
   - Проверить отображение подписей значений для всех точек
   - Проверить работу tooltip при наведении на смещённые точки
   - Проверить клик по смещённым точкам (модальное окно с детализацией)
   - Проверить работу с различным количеством стадий (2, 3, 4+)

2. **Визуальное тестирование:**
   - Проверить читаемость подписей на разных фонах
   - Проверить видимость смещённых точек
   - Проверить адаптивность на мобильных устройствах
   - Проверить контрастность цветов подписей

3. **Производительность:**
   - Проверить время отрисовки графика (должно быть < 100ms)
   - Оптимизировать отрисовку подписей (кеширование, мемоизация)
   - Убедиться, что нет лагов при взаимодействии

4. **Документация:**
   - Добавить комментарии к коду
   - Обновить JSDoc для всех функций
   - Задокументировать параметры плагинов

5. **Финализация:**
   - Убрать console.log (если были)
   - Проверить соответствие стандартам кода
   - Убедиться, что все критерии приёмки выполнены

**Технические детали:**
- Оптимизация: мемоизация результатов вычисления jitter
- Кеширование: пересчёт смещений только при изменении данных
- Производительность: анализ и отрисовка должны выполняться за < 100ms

**Критерии завершения:**
- [ ] Все функциональные тесты пройдены
- [ ] Визуальное тестирование завершено успешно
- [ ] Производительность соответствует требованиям
- [ ] Код задокументирован
- [ ] Все критерии приёмки выполнены
- [ ] Решение готово к продакшену

**Ожидаемый результат:**
Полностью рабочее, протестированное и оптимизированное решение для улучшения визуализации точек на линейном графике.

---

## 📁 Модули и компоненты

### Изменяемые файлы:

1. **`vue-app/src/components/graph-state/GraphStateChart.vue`**
   - Импорт и регистрация плагинов `pointJitterPlugin` и `pointLabelsPlugin` (Этап 4)
   - Проверка применения плагинов только к линейным графикам

### Новые файлы:

1. **`vue-app/src/utils/graph-state/lineChartUtils.js`** (расширение, Этап 1)
   - Функция `calculatePointJitter(chart, positionIndex, threshold)` — вычисление смещения для точек

2. **`vue-app/src/components/graph-state/plugins/pointJitterPlugin.js`** (Этап 2)
   - Кастомный плагин Chart.js для применения смещения к точкам с одинаковыми Y

3. **`vue-app/src/components/graph-state/plugins/pointLabelsPlugin.js`** (Этап 3)
   - Кастомный плагин Chart.js для отрисовки подписей значений рядом с точками

---

## 🔧 Технические детали

### Утилита для вычисления смещения (Этап 1):

**Файл:** `vue-app/src/utils/graph-state/lineChartUtils.js`

```javascript
/**
 * Вычисляет смещение (jitter) для точек с одинаковыми значениями Y
 * 
 * @param {Object} chart - Экземпляр Chart.js
 * @param {number} positionIndex - Индекс временной позиции (0, 1, 2...)
 * @param {number} threshold - Порог для определения одинаковых значений (по умолчанию 0.5)
 * @returns {Array} Массив объектов { datasetIndex, jitterX, value }
 */
export function calculatePointJitter(chart, positionIndex, threshold = 0.5) {
  if (chart.config.type !== 'line') return [];
  
  const datasets = chart.data.datasets;
  const points = [];
  
  // Собрать все точки на данной позиции
  datasets.forEach((dataset, datasetIndex) => {
    const meta = chart.getDatasetMeta(datasetIndex);
    if (meta.data[positionIndex]) {
      const point = meta.data[positionIndex];
      const value = dataset.data[positionIndex];
      points.push({
        datasetIndex,
        value,
        x: point.x,
        y: point.y
      });
    }
  });
  
  // Группировать точки с одинаковыми значениями Y
  const groups = groupPointsByValue(points, threshold);
  
  // Вычислить смещение для каждой группы
  const jitterResults = [];
  groups.forEach(group => {
    if (group.length > 1) {
      // Применить смещение только если точек больше одной
      const jitterStep = 2.5; // Шаг смещения в пикселях
      group.forEach((point, index) => {
        const offset = (index - (group.length - 1) / 2) * jitterStep;
        jitterResults.push({
          datasetIndex: point.datasetIndex,
          jitterX: offset,
          value: point.value
        });
      });
    } else {
      // Для одиночных точек смещение не нужно
      jitterResults.push({
        datasetIndex: group[0].datasetIndex,
        jitterX: 0,
        value: group[0].value
      });
    }
  });
  
  return jitterResults;
}
```

### Плагин для смещения точек (Этап 2):

**Файл:** `vue-app/src/components/graph-state/plugins/pointJitterPlugin.js`

```javascript
import { calculatePointJitter } from '@/utils/graph-state/lineChartUtils.js';

export const pointJitterPlugin = {
  id: 'pointJitterPlugin',
  
  /**
   * Вызывается перед отрисовкой datasets
   * Применяет смещение к точкам с одинаковыми значениями Y
   */
  beforeDatasetsDraw: (chart) => {
    if (chart.config.type !== 'line') return;
    
    // Применить смещение к точкам через модификацию координат
    // или через кастомную отрисовку точек
  }
};
```

### Плагин для подписей значений (Этап 3):

**Файл:** `vue-app/src/components/graph-state/plugins/pointLabelsPlugin.js`

```javascript
export const pointLabelsPlugin = {
  id: 'pointLabelsPlugin',
  
  /**
   * Вызывается после отрисовки всех datasets
   * Отрисовывает подписи значений рядом с точками
   */
  afterDatasetsDraw: (chart) => {
    if (chart.config.type !== 'line') return;
    
    const ctx = chart.ctx;
    const datasets = chart.data.datasets;
    
    datasets.forEach((dataset, datasetIndex) => {
      const meta = chart.getDatasetMeta(datasetIndex);
      if (meta.hidden) return;
      
      dataset.data.forEach((value, dataIndex) => {
        const point = meta.data[dataIndex];
        if (!point) return;
        
        // Позиция подписи (справа от точки)
        const labelX = point.x + 6;
        const labelY = point.y - 4;
        
        // Отрисовка фона для читаемости
        ctx.save();
        ctx.fillStyle = 'rgba(255, 255, 255, 0.8)';
        ctx.fillRect(labelX - 2, labelY - 8, 20, 12);
        
        // Отрисовка текста
        ctx.fillStyle = dataset.pointBackgroundColor || '#6b7280';
        ctx.font = '10px Arial, sans-serif';
        ctx.textAlign = 'left';
        ctx.textBaseline = 'middle';
        ctx.fillText(String(value), labelX, labelY);
        
        ctx.restore();
      });
    });
  }
};
```

### Интеграция в компонент (Этап 4):

**Файл:** `vue-app/src/components/graph-state/GraphStateChart.vue`

```javascript
import { pointJitterPlugin } from './plugins/pointJitterPlugin.js';
import { pointLabelsPlugin } from './plugins/pointLabelsPlugin.js';
import ChartJS from 'chart.js/auto';

// Регистрация плагинов
ChartJS.register(pointJitterPlugin, pointLabelsPlugin);

// Плагины автоматически применяются ко всем линейным графикам
```

---

## 🎨 UX/UI требования

### Визуальные требования:

1. **Смещение точек (Jitter):**
   - Смещение по оси X: ±2-3px для точек с одинаковыми Y
   - Распределение смещения: равномерное (например, -2px, 0px, +2px для 3 точек)
   - Смещение должно быть минимальным, чтобы не искажать общую картину графика
   - Применяется только к точкам с одинаковыми значениями Y (в пределах threshold 0.5)

2. **Подписи значений:**
   - **Позиция:** Справа от точки с отступом 4-6px
   - **Фон:** Белый с прозрачностью 0.8 для читаемости на любом фоне
   - **Цвет текста:** Цвет точки или серый `#6b7280`
   - **Шрифт:** `10px Arial, sans-serif`
   - **Формат:** Целые числа без десятичных знаков
   - **Размер фона:** Автоматически подстраивается под длину числа

3. **Адаптивность:**
   - Подписи должны быть читаемы на мобильных устройствах (минимум 9px)
   - Смещение точек должно быть видимым на всех размерах экрана
   - На мобильных устройствах подписи могут быть немного меньше (9px вместо 10px)

### Соответствие гайдлайнам Bitrix24:

- **Цвета:** Использовать цвета из палитры Bitrix24 для подписей
- **Типографика:** Шрифт Arial или системный шрифт Bitrix24
- **Доступность:** 
  - Контрастность текста на фоне: минимум 4.5:1
  - Размеры элементов: минимум 9px для подписей
  - Семантические цвета: использовать цвета точек для подписей

### Визуальная иерархия:

1. **Базовый уровень:** Точки с разными стилями (круг, треугольник, квадрат) — уже реализовано
2. **Уровень разделения:** Смещение точек для визуального различия при перекрытии
3. **Уровень информации:** Подписи значений для быстрого чтения данных
4. **Интерактивный уровень:** Tooltip и модальное окно предоставляют детальную информацию при взаимодействии

---

## ✅ Критерии приёмки

### Обязательные (Этап 1 - Утилита):

- [ ] Функция `calculatePointJitter` корректно определяет точки с одинаковыми Y
- [ ] Функция вычисляет равномерное смещение для группы точек
- [ ] Смещение применяется только к точкам с одинаковыми значениями (threshold 0.5)
- [ ] Добавлены JSDoc комментарии

### Обязательные (Этап 2 - Плагин смещения):

- [ ] Файл `pointJitterPlugin.js` создан
- [ ] Плагин корректно применяет смещение к точкам
- [ ] Смещение минимальное (2-3px) и не искажает график
- [ ] Tooltip и клики работают корректно (используют оригинальные координаты)
- [ ] Плагин работает только для линейных графиков

### Обязательные (Этап 3 - Плагин подписей):

- [ ] Файл `pointLabelsPlugin.js` создан
- [ ] Плагин отрисовывает значения рядом с каждой точкой
- [ ] Подписи стилизованы в соответствии с требованиями
- [ ] Подписи не перекрывают точки и другие элементы графика
- [ ] Плагин работает только для линейных графиков

### Обязательные (Этап 4 - Интеграция):

- [ ] Плагины импортированы и зарегистрированы в компоненте
- [ ] Плагины работают только для линейных графиков
- [ ] Смещение точек применяется корректно
- [ ] Подписи значений отображаются корректно
- [ ] Совместимость с `overlappingPointsPlugin` проверена
- [ ] Tooltip и клики работают корректно
- [ ] Нет ошибок в консоли

### Обязательные (Этап 5 - Финальное тестирование):

- [ ] Точки с одинаковыми значениями визуально разделены
- [ ] Значения точек видны без наведения курсора
- [ ] Клик по точке работает корректно (модальное окно открывается)
- [ ] График корректно отображается на всех устройствах
- [ ] Нет визуальных артефактов или ошибок отрисовки
- [ ] Производительность не ухудшилась (время отрисовки < 100ms)
- [ ] Код задокументирован (JSDoc, комментарии)
- [ ] Соответствие стандартам кода проекта

### Дополнительные (UX/UI):

- [ ] Подписи различимы на мобильных устройствах
- [ ] Смещение точек видимо на всех размерах экрана
- [ ] Контрастность цветов соответствует стандартам доступности
- [ ] Подписи не перекрывают важные элементы графика
- [ ] Визуальное решение соответствует гайдлайнам Bitrix24

---

## 🧪 Тестирование

### Тестирование по этапам:

#### Этап 1: Тестирование утилиты

**Сценарии:**
1. **Базовый сценарий:**
   - Создать тестовый график с позицией, имеющей 3 точки с одинаковым значением Y
   - Вызвать функцию `calculatePointJitter`
   - Проверить, что возвращается массив с правильными смещениями

2. **Сценарий с разными значениями:**
   - Проверить, что точки с разными Y не получают смещение
   - Убедиться, что смещение применяется только к группе точек с одинаковыми Y

#### Этап 2: Тестирование плагина смещения

**Сценарии:**
1. **Базовый сценарий:**
   - Открыть линейный график с перекрывающимися точками
   - Проверить, что точки смещены по горизонтали
   - Убедиться, что смещение минимальное и не искажает график

2. **Сценарий с кликами:**
   - Кликнуть на смещённую точку
   - Проверить, что открывается модальное окно с детализацией
   - Убедиться, что tooltip показывает правильные данные

#### Этап 3: Тестирование плагина подписей

**Сценарии:**
1. **Базовый сценарий:**
   - Открыть линейный график
   - Проверить, что рядом с каждой точкой отображается её значение
   - Убедиться, что подписи стилизованы правильно

2. **Визуальное тестирование:**
   - Проверить читаемость подписей на разных фонах
   - Проверить позиционирование подписей
   - Убедиться, что подписи не перекрывают точки

#### Этап 4: Интеграционное тестирование

**Сценарии:**
1. **Полная интеграция:**
   - Проверить работу смещения точек + подписей вместе
   - Убедиться, что оба решения работают корректно
   - Проверить совместимость с `overlappingPointsPlugin`

2. **Сценарий с наведением:**
   - Навести курсор на смещённую точку
   - Проверить, что tooltip показывает правильные данные
   - Убедиться, что подпись не мешает tooltip

#### Этап 5: Финальное тестирование

**Сценарии:**
1. **Функциональное тестирование:**
   - Все сценарии из предыдущих этапов
   - Проверить работу на реальных данных
   - Проверить обработку различных состояний данных

2. **Визуальное тестирование:**
   - Проверить читаемость подписей на разных фонах
   - Проверить видимость смещённых точек
   - Проверить контрастность цветов

3. **Адаптивность:**
   - Проверить отображение на мобильных устройствах (320px+)
   - Проверить на планшетах (768px+)
   - Проверить на больших экранах (1920px+)

4. **Производительность:**
   - Измерить время отрисовки графика (должно быть < 100ms)
   - Проверить отсутствие лагов при взаимодействии
   - Проверить плавность анимаций при наведении

---

## 📝 История правок

- **2025-12-13 15:13 (UTC+3, Брест):** Создан документ TASK-035 с планом улучшения визуализации точек на линейном графике
- **2025-12-13 (UTC+3, Брест):** Реализованы все 5 этапов задачи:
  - ✅ Этап 1: Расширен `lineChartUtils.js` функцией `calculatePointJitter` для вычисления смещения точек
  - ✅ Этап 2: Создан плагин `pointJitterPlugin.js` для смещения точек с одинаковыми значениями Y
  - ✅ Этап 3: Создан плагин `pointLabelsPlugin.js` для отрисовки подписей значений рядом с точками
  - ✅ Этап 4: Интегрированы плагины в `GraphStateChart.vue` (импорты и регистрация через ChartJS.register)
  - ✅ Этап 5: Завершена реализация, готово к тестированию
- **2025-12-13 (UTC+3, Брест):** Улучшена визуализация точек на основе обратной связи:
  - ✅ Увеличено смещение точек с 2.5px до 4px для лучшей видимости
  - ✅ Увеличен размер точек с 6px до 7px (и до 8px для смещённых точек)
  - ✅ Увеличен размер точек при наведении с 8px до 10px
  - ✅ Увеличена высота графика с 300px до 400px (расширена сетка)
  - ✅ Увеличена высота canvas-контейнера с 380px до 450px
  - ✅ Улучшены подписи значений: увеличен размер шрифта с 10px до 11px, улучшен фон и добавлена обводка
  - ✅ Увеличены отступы подписей от точек для лучшей читаемости

---

## 🔗 Связанные задачи

### Родительская задача:
- **TASK-032:** Визуализация перекрывающихся точек на линейном графике состояния сектора 1С

### Связанные задачи:
- **TASK-031:** Интеграция детализации по сотрудникам в графики состояния сектора 1С
- **TASK-032-03:** Этап 3 - Создание кастомного плагина для индикатора количества

---

## 📚 Дополнительная информация

### Использованные технологии:

- Vue.js 3.x (Composition API)
- Chart.js 4.x
- vue-chartjs

### Документация:

- Chart.js Plugins: https://www.chartjs.org/docs/latest/developers/plugins.html
- Chart.js Point Configuration: https://www.chartjs.org/docs/latest/configuration/elements.html#point-configuration
- Bitrix24 UI Kit: https://apidocs.bitrix24.ru/sdk/ui.html

### Примеры визуализации:

- Jitter для перекрывающихся точек — стандартный подход в аналитике
- Подписи значений — популярны в BI-инструментах для быстрого чтения данных

---

**Статус:** 📋 Новая  
**План реализации:** 5 этапов  
**Исполнитель:** Bitrix24 Программист (Vue.js)



