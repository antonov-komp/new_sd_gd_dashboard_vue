# TASK-033-01-01: Анализ текущей структуры данных

**Дата создания:** 2025-12-12 09:06 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-033 (Этап 1)

---

## 📋 Описание подзадачи

Провести детальный анализ текущей структуры данных слепков (snapshots) для определения наличия необходимых полей (`stageId`, `departmentHead`) и разработки стратегии получения детальной информации о тикетах для реализации уровней 2 и 3 попапа.

---

## 🎯 Цель подзадачи

1. Изучить структуру слепков версии 1.0
2. Определить, какие поля отсутствуют в массиве `tickets`
3. Разработать стратегию получения недостающих данных
4. Документировать результаты анализа

---

## 📝 Задачи

### Задача 1: Изучение структуры слепков

**Файлы для изучения:**
- `DOCS/ARCHITECTURE/graph-state-snapshots.md` — документация по структуре слепков
- `vue-app/src/services/graph-state/SnapshotService.js` — сервис работы со слепками
- `vue-app/src/services/graph-state/snapshot-normalizer.js` — нормализация данных в слепки

**Действия:**
1. Открыть документацию по структуре слепков
2. Изучить формат версии 1.0
3. Определить, какие поля содержатся в массиве `tickets`
4. Зафиксировать отсутствующие поля

**Ожидаемый результат:**
- Понимание текущей структуры слепков
- Список полей в массиве `tickets`
- Список отсутствующих полей

---

### Задача 2: Проверка наличия полей в слепках

**Файлы для проверки:**
- `vue-app/src/services/graph-state/snapshot-normalizer.js` — функция `extractTicketsData()`
- Примеры реальных слепков (если доступны)

**Действия:**
1. Найти функцию `extractTicketsData()` в нормализаторе
2. Изучить, какие поля извлекаются из тикетов
3. Проверить, есть ли `stageId` и `departmentHead` в извлечении
4. Создать тестовый слепок и проверить структуру

**Текущая структура (ожидаемая):**
```javascript
tickets: [
  {
    id: number,              // ✅ Есть
    title: string,           // ✅ Есть
    assignedTo: object,      // ✅ Есть
    createdAt: string,       // ✅ Есть
    updatedAt: string        // ✅ Есть
    // ❌ Отсутствуют:
    // stageId: string
    // departmentHead: string
  }
]
```

**Ожидаемый результат:**
- Подтверждение отсутствия `stageId` и `departmentHead`
- Документация текущей структуры

---

### Задача 3: Анализ исходных данных сектора

**Файлы для анализа:**
- `vue-app/src/services/dashboard-sector-1c-service.js` — сервис получения данных сектора
- `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js` — маппер тикетов

**Действия:**
1. Изучить метод `getSectorData()` в `DashboardSector1CService`
2. Проверить, какие поля содержатся в тикетах до нормализации
3. Убедиться, что `stageId` и `departmentHead` доступны в исходных данных
4. Проверить маппер тикетов на наличие `departmentHead`

**Ожидаемый результат:**
- Подтверждение наличия полей в исходных данных
- Понимание, где теряются поля при нормализации

---

### Задача 4: Разработка стратегии получения данных

**Варианты решения:**

**Вариант 1: Расширение структуры слепков (для будущих слепков)**
- Добавить `stageId` и `departmentHead` в функцию `extractTicketsData()`
- Обновить версию формата слепка до 1.1
- Для старых слепков использовать вариант 2

**Вариант 2: Загрузка деталей через API (для текущих слепков)**
- Использовать массив `ticketIds` из слепка
- Загружать детали через Bitrix24 REST API: `crm.item.list`
- Кешировать загруженные данные

**Рекомендуемая стратегия:**
- Для текущих слепков (версия 1.0): использовать вариант 2
- Для будущих слепков: реализовать вариант 1 (в отдельной задаче)

**Действия:**
1. Выбрать стратегию для текущей реализации
2. Документировать выбранный подход
3. Определить необходимые API-методы
4. Спланировать структуру сервиса загрузки деталей

**Ожидаемый результат:**
- Выбранная стратегия получения данных
- План реализации
- Список необходимых API-методов

---

## 🔍 Технические детали

### Структура слепка версии 1.0

```javascript
{
  metadata: {
    version: "1.0",
    createdAt: "2025-12-08T08:00:00+03:00",
    // ...
  },
  statistics: {
    stages: { ... },
    employees: [ ... ],
    zeroPoint: { ... }
  },
  ticketIds: [1001, 1002, 1003],
  tickets: [
    {
      id: 1001,
      title: "Название тикета",
      assignedTo: { id: 123, name: "Иван Иванов" },
      createdAt: "2024-12-12T10:00:00+03:00",
      updatedAt: "2024-12-12T14:00:00+03:00"
      // Отсутствуют: stageId, departmentHead
    }
  ]
}
```

### Необходимые поля для уровней 2 и 3

**Для уровня 2 (временные градации):**
- `createdAt` — ✅ есть (для определения категории давности)
- `stageId` — ❌ отсутствует (для фильтрации по стадии)

**Для уровня 3 (заказчики):**
- `departmentHead` — ❌ отсутствует (для группировки по заказчикам)
- `stageId` — ❌ отсутствует (для фильтрации по стадии)

---

## 📚 Связанные файлы

- `DOCS/ARCHITECTURE/graph-state-snapshots.md` — документация структуры слепков
- `vue-app/src/services/graph-state/SnapshotService.js` — сервис работы со слепками
- `vue-app/src/services/graph-state/snapshot-normalizer.js` — нормализация данных
- `vue-app/src/services/dashboard-sector-1c-service.js` — сервис данных сектора
- `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js` — маппер тикетов
- `DOCS/ANALYSIS/graph-state-popup-interactive-levels-analysis.md` — анализ уровней попапа

---

## ✅ Критерии приёмки

- [ ] Структура слепков версии 1.0 изучена и задокументирована
- [ ] Подтверждено отсутствие полей `stageId` и `departmentHead` в массиве `tickets`
- [ ] Проверено наличие полей в исходных данных сектора
- [ ] Выбрана стратегия получения недостающих данных
- [ ] Документирован план реализации
- [ ] Определены необходимые API-методы Bitrix24

---

## 📝 Результаты анализа (заполняется при выполнении)

### Текущая структура массива `tickets`:

```
[Заполнить при выполнении]
```

### Отсутствующие поля:

```
[Заполнить при выполнении]
```

### Наличие полей в исходных данных:

```
[Заполнить при выполнении]
```

### Выбранная стратегия:

```
[Заполнить при выполнении]
```

---

## 🔗 Следующие шаги

После завершения анализа:
- Переход к подзадаче 1.2: Создание сервиса загрузки деталей тикетов
- Или к подзадаче 1.3: Интеграция с компонентом попапа (если стратегия не требует API)

---

**Автор:** Технический писатель  
**Дата:** 2025-12-12 09:06 (UTC+3, Брест)




