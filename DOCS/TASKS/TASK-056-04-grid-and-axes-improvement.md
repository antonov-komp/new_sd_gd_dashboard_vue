# TASK-056-04: Улучшение сетки и осей графика

**Дата создания:** 2025-12-17 16:42 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-056: Улучшение дизайна линейных графиков](./TASK-056-line-charts-design-improvements.md)  
**Этап:** 4/7

---

## 📋 Описание

Улучшить сетку и оси линейных графиков модуля «График приёма и закрытий сектора 1С»: сделать сетку более тонкой и ненавязчивой, улучшить форматирование подписей осей и добавить разделитель тысяч для больших значений.

---

## 🎯 Контекст

**Текущее состояние:**
- Толщина линий сетки: `1.5px`
- Цвет сетки: по умолчанию Chart.js
- Форматирование подписей осей: базовое
- Нет разделителя тысяч для больших значений

**Проблема:**
- Сетка слишком заметна, отвлекает от данных
- Подписи осей не форматируются для больших значений
- Нет визуального разделения основных делений

**Требуемое состояние:**
- Тонкие линии сетки (`1px`)
- Светлый цвет сетки (`#e5e7eb`)
- Более тёмные линии для основных делений
- Форматирование подписей с разделителем тысяч
- Улучшенная читаемость подписей недель

---

## 🔧 Технические требования

### 1. Улучшение сетки

**Стили сетки:**
- Толщина линий: `1px` (вместо `1.5px`)
- Цвет сетки: `#e5e7eb` (светло-серый)
- Стиль линий: сплошной

**Основные деления:**
- Более тёмные линии для основных делений (каждые 5 единиц на оси Y)
- Цвет основных делений: `#d1d5db` (чуть темнее)
- Толщина основных делений: `1px` (можно `1.5px` для выделения)

### 2. Улучшение оси Y

**Форматирование:**
- Добавить разделитель тысяч (если значения > 1000)
- Использовать функцию `formatNumber()` из TASK-056-02
- Precision: `0` (целые числа)

**Стили:**
- Размер шрифта: `14px`
- Padding: `10px`
- Цвет текста: `#374151`

### 3. Улучшение оси X

**Форматирование:**
- Улучшить читаемость подписей недель
- Можно добавить даты в скобках (если нужно)
- Максимальный угол поворота: `45°`

**Стили:**
- Размер шрифта: `14px`
- Padding: `10px`
- Цвет текста: `#374151`

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Обновить стили сетки

1. **Открыть файл:**
   - `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`

2. **Обновить `chartOptions.computed`:**
   ```javascript
   scales: {
     y: {
       grid: {
         color: '#e5e7eb', // Светло-серый
         lineWidth: 1 // Тонкие линии
       }
     },
     x: {
       grid: {
         color: '#e5e7eb',
         lineWidth: 1
       }
     }
   }
   ```

### Шаг 2: Добавить основные деления

1. **Настроить основные деления для оси Y с обработкой edge cases:**
   ```javascript
   y: {
     beginAtZero: true,
     grid: {
       color: (context) => {
         // Обработка edge cases
         if (!context || context.tick === undefined || context.tick.value === undefined) {
           return '#e5e7eb'; // Fallback
         }
         
         const value = context.tick.value;
         
         // Проверка на NaN или Infinity
         if (isNaN(value) || !isFinite(value)) {
           return '#e5e7eb';
         }
         
         // Более тёмные линии для основных делений (каждые 5 единиц)
         // Также выделяем 0
         if (value === 0 || value % 5 === 0) {
           return '#d1d5db'; // Чуть темнее
         }
         
         return '#e5e7eb'; // Обычный цвет
       },
       lineWidth: (context) => {
         if (!context || context.tick === undefined || context.tick.value === undefined) {
           return 1; // Fallback
         }
         
         const value = context.tick.value;
         
         if (isNaN(value) || !isFinite(value)) {
           return 1;
         }
         
         // Чуть толще для основных делений и 0
         if (value === 0 || value % 5 === 0) {
           return 1.5;
         }
         
         return 1;
       }
     }
   }
   ```

2. **Важно:** Функции `color` и `lineWidth` вызываются для каждого тика, поэтому они должны быть эффективными. Избегать сложных вычислений внутри.

### Шаг 3: Улучшить форматирование оси Y

1. **Добавить форматирование с разделителем тысяч и обработкой ошибок:**
   ```javascript
   y: {
     beginAtZero: true,
     ticks: {
       precision: 0,
       font: {
         size: 14
       },
       padding: 10,
       color: '#374151',
       callback: (value) => {
         // Обработка edge cases
         if (value === null || value === undefined || isNaN(value)) {
           return '0';
         }
         
         // Использовать функцию formatNumber из TASK-056-02
         // Убедиться, что функция доступна в области видимости
         return formatNumber(value);
       },
       // Опционально: настроить шаг для основных делений
       stepSize: undefined, // Автоматический шаг
       // Или явно указать шаг (если нужно)
       // stepSize: 5
     }
   }
   ```

2. **Использовать функцию `formatNumber()` из TASK-056-02:**
   - Переиспользовать существующую логику
   - Убедиться, что функция определена в том же компоненте или импортирована
   - Если функция недоступна, можно использовать inline версию:
   ```javascript
   callback: (value) => {
     if (value === null || value === undefined || isNaN(value)) {
       return '0';
     }
     
     if (value >= 1000) {
       return value.toLocaleString('ru-RU');
     }
     
     return value.toString();
   }
   ```

3. **Обработка отрицательных значений (если возможно):**
   ```javascript
   callback: (value) => {
     if (value === null || value === undefined || isNaN(value)) {
       return '0';
     }
     
     // Отрицательные значения (если возможны)
     const absValue = Math.abs(value);
     const sign = value < 0 ? '-' : '';
     
     if (absValue >= 1000) {
       return sign + absValue.toLocaleString('ru-RU');
     }
     
     return sign + absValue.toString();
   }
   ```

### Шаг 4: Улучшить форматирование оси X

1. **Обновить настройки ticks:**
   ```javascript
   x: {
     ticks: {
       maxRotation: 45,
       minRotation: 45,
       font: {
         size: 14
       },
       padding: 10
     }
   }
   ```

2. **Улучшить читаемость подписей (если нужно):**
   - Можно добавить даты в скобках через кастомный callback

---

## 🔍 Детали реализации

### Файлы для изменения

1. **`vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`**
   - Обновить `chartOptions.computed`
   - Добавить форматирование для осей

### Применение к обоим графикам

**Левый график (Новые и Закрытые):**
- Сетка и оси должны быть одинаковыми
- Форматирование для всех значений

**Правый график (Переходящие):**
- Сетка и оси должны быть одинаковыми
- Форматирование для всех значений

### Определение основных делений

**Логика:**
- Основные деления: каждые 5 единиц (0, 5, 10, 15, 20, ...)
- Можно настроить через `stepSize` или кастомную логику
- Визуально выделять более тёмным цветом

---

## 💻 Примеры кода

### Обновлённая конфигурация сетки и осей

```javascript
const chartOptions = computed(() => ({
  // ... другие настройки
  scales: {
    y: {
      beginAtZero: true,
      ticks: {
        precision: 0,
        font: {
          size: 14
        },
        padding: 10,
        color: '#374151',
        callback: (value) => {
          // Форматирование с разделителем тысяч
          return formatNumber(value);
        }
      },
      grid: {
        color: (context) => {
          // Более тёмные линии для основных делений (каждые 5 единиц)
          if (context.tick.value % 5 === 0) {
            return '#d1d5db'; // Чуть темнее
          }
          return '#e5e7eb'; // Обычный цвет
        },
        lineWidth: (context) => {
          // Чуть толще для основных делений
          if (context.tick.value % 5 === 0) {
            return 1.5;
          }
          return 1;
        }
      }
    },
    x: {
      ticks: {
        maxRotation: 45,
        minRotation: 45,
        font: {
          size: 14
        },
        padding: 10,
        color: '#374151'
      },
      grid: {
        color: '#e5e7eb',
        lineWidth: 1
      }
    }
  }
}));
```

### Альтернативный подход для основных делений

```javascript
// Если нужно более точное управление основными делениями
y: {
  ticks: {
    stepSize: 5, // Шаг для основных делений
    // ... остальные настройки
  },
  grid: {
    color: (context) => {
      const value = context.tick.value;
      // Основные деления (каждые 5 единиц)
      if (value % 5 === 0) {
        return '#d1d5db';
      }
      return '#e5e7eb';
    },
    lineWidth: (context) => {
      const value = context.tick.value;
      if (value % 5 === 0) {
        return 1.5;
      }
      return 1;
    }
  }
}
```

---

## ✅ Критерии приёмки

- [ ] Толщина линий сетки уменьшена до `1px`
- [ ] Цвет сетки изменён на `#e5e7eb` (светло-серый)
- [ ] Основные деления (каждые 5 единиц) имеют более тёмный цвет (`#d1d5db`)
- [ ] Основные деления имеют увеличенную толщину (`1.5px`)
- [ ] Ось Y форматирует числа с разделителем тысяч (если значения > 1000)
- [ ] Размер шрифта подписей осей составляет `14px`
- [ ] Padding подписей осей составляет `10px`
- [ ] Цвет текста подписей осей: `#374151`
- [ ] Ось X имеет угол поворота `45°`
- [ ] Улучшения применены к обоим графикам
- [ ] Сетка не отвлекает от данных графика

---

## 🔗 Зависимости

**Зависит от:**
- [TASK-056-02: Улучшение tooltips](./TASK-056-02-tooltips-improvement.md) — использовать функцию `formatNumber()`

**Зависит от этого этапа:**
- [TASK-056-05: Обновление цветовой схемы](./TASK-056-05-color-scheme-update.md) — может использовать цвета сетки

---

## 📝 История правок

- **2025-12-17 16:42 (UTC+3, Брест):** Создан этап TASK-056-04
  - Определены требования для улучшения сетки и осей
  - Зафиксированы технические детали и примеры кода
  - Добавлены критерии приёмки

---

## 💡 Примечания

- **Производительность:** Кастомные функции для цвета сетки должны быть эффективными
- **Читаемость:** Сетка должна быть ненавязчивой, но видимой
- **Адаптивность:** Проверить отображение сетки на разных размерах экрана
- **Тестирование:** Протестировать форматирование для разных значений (0, 100, 1000, 10000)

## ⚠️ Возможные проблемы и решения

### Проблема 1: Функции color/lineWidth вызываются слишком часто
**Причина:** Chart.js вызывает эти функции для каждого тика при каждом рендере  
**Решение:** 
- Убедиться, что функции простые и быстрые
- Избегать сложных вычислений
- Использовать простые проверки (`value % 5 === 0`)

### Проблема 2: Основные деления не выделяются
**Причина:** Логика `value % 5 === 0` не работает для дробных значений  
**Решение:** 
- Использовать `precision: 0` для целых чисел
- Или округлять значение: `Math.round(value) % 5 === 0`

### Проблема 3: Форматирование чисел не работает
**Причина:** `formatNumber` недоступна или `toLocaleString` не поддерживается  
**Решение:** 
- Убедиться, что функция определена в области видимости
- Добавить fallback для `toLocaleString`:
  ```javascript
  if (value >= 1000) {
    try {
      return value.toLocaleString('ru-RU');
    } catch {
      return value.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ' ');
    }
  }
  ```

### Проблема 4: Сетка слишком тёмная или слишком светлая
**Причина:** Цвета не соответствуют дизайну  
**Решение:** 
- Основные деления: `#d1d5db` (средне-серый)
- Обычные линии: `#e5e7eb` (светло-серый)
- Можно настроить под конкретный дизайн

### Проблема 5: Форматирование работает только для положительных значений
**Причина:** Не обработаны отрицательные значения  
**Решение:** Добавить проверку знака (если отрицательные значения возможны)

## 🔍 Детальная проверка реализации

**После реализации проверить:**
1. Толщина всех линий сетки: `1px`
2. Цвет обычных линий: `#e5e7eb`
3. Основные деления (0, 5, 10, 15, ...) имеют цвет `#d1d5db` и толщину `1.5px`
4. Ось Y форматирует числа с разделителем тысяч (1 234)
5. Ось Y обрабатывает edge cases (null, undefined, NaN)
6. Размер шрифта подписей: `14px`
7. Padding подписей: `10px`
8. Цвет текста: `#374151`
9. Ось X имеет угол поворота `45°`
10. Сетка не отвлекает от данных
11. Работает для обоих графиков
12. Работает в обоих режимах (недели и месяцы)

