# TASK-053-04: Изменения в блоках данных (Summary-карточки) для 3-месячного режима

**Дата создания:** 2025-12-17 13:45 (UTC+3, Брест)  
**Статус:** 📋 Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-053: Изменение формата выбора периода и внедрение 3-месячного режима](./TASK-053-period-selection-3-months-mode.md)  
**Этап:** 2.2 (Часть 2: Внедрение 3-месячного режима)

---

## 📋 Описание

Адаптировать summary-карточки (блоки данных) в модуле «График приёма и закрытий сектора 1С» для отображения данных в 3-месячном режиме. Карточки должны показывать агрегированные данные за 3 месяца с помесячной разбивкой и делением второго уровня (для закрытых и переходящих тикетов).

---

## 🎯 Контекст

### Текущее состояние

**Текущие summary-карточки (режим "4 недели"):**
- "Новые за неделю" — одна цифра
- "Закрытые всего" — три цифры (всего, созданные этой неделей, созданные другой неделей)
- "Переходящие всего" — три цифры (всего, созданные этой неделей, созданные другой неделей)
- "Закрытия по стадиям" — список стадий с количествами

**Проблема:**
- Карточки не адаптированы для 3-месячного режима
- Нет помесячной разбивки данных
- Нет деления второго уровня по месяцам

### Требуемое состояние

**Новые summary-карточки (режим "3 месяца"):**
- "Новые за период" — 4 цифры (сумма за 3 месяца + 3 месяца)
- "Закрытые всего" — иерархическая структура (всего + помесячно + деление второго уровня)
- "Переходящие всего" — иерархическая структура (всего + помесячно)
- "Закрытия по стадиям" — только суммарные количества за 3 месяца (3 стадии)

---

## 🔧 Технические требования

### Компонент: SummaryCardsMonths.vue

**Расположение:** `vue-app/src/components/graph-admission-closure/SummaryCardsMonths.vue`

**Props:**
```typescript
{
  data: {
    newTickets: number,
    newTicketsByMonth: Array<{
      month: number,
      monthName: string,
      count: number
    }>,
    closedTickets: number,
    closedTicketsByMonth: Array<{
      month: number,
      monthName: string,
      count: number,
      closedCreatedThisWeek: number,
      closedCreatedOtherWeek: number,
      weeks: Array<{
        weekNumber: number,
        count: number,
        closedCreatedThisWeek: number,
        closedCreatedOtherWeek: number
      }>
    }>,
    carryoverTickets: number,
    carryoverTicketsByMonth: Array<{
      month: number,
      monthName: string,
      count: number,
      carryoverCreatedThisWeek: number,
      carryoverCreatedOtherWeek: number
    }>,
    stages: Array<{
      stageId: string,
      stageName: string,
      count: number
    }>
  }
}
```

**Функциональность:**
- Отображение 4 summary-карточек с данными за 3 месяца
- Помесячная разбивка данных
- Иерархическая структура для закрытых и переходящих тикетов
- Адаптивность (работа на мобильных устройствах)

### Структура карточек

#### 1. Карточка "Новые за период"

**Структура:**
- Большая цифра: сумма за 3 месяца
- Три маленькие цифры: декабрь, ноябрь, октябрь

**Визуализация:**
```
┌─────────────────────────────────────┐
│  Новые за период                    │
│  ┌──────────┐                       │
│  │   156    │  ← Сумма за 3 месяца │
│  └──────────┘                       │
│    Декабрь: 52  Ноябрь: 48  Октябрь: 56 │
└─────────────────────────────────────┘
```

#### 2. Карточка "Закрытые всего"

**Структура:**
- Большая цифра: сумма за 3 месяца
- Для каждого месяца:
  - Цифра месяца
  - Деление второго уровня:
    - Созданные этой неделей
    - Созданные другой неделей

**Визуализация:**
```
┌─────────────────────────────────────┐
│  Закрытые всего                     │
│  ┌──────────┐                       │
│  │   134    │  ← Сумма за 3 месяца │
│  └──────────┘                       │
│    Декабрь: 45                       │
│      ✓ 28 этой неделей               │
│      ↻ 17 другой недели              │
│    Ноябрь: 42                        │
│      ✓ 25 этой неделей               │
│      ↻ 17 другой недели              │
│    Октябрь: 47                       │
│      ✓ 30 этой неделей               │
│      ↻ 17 другой недели              │
└─────────────────────────────────────┘
```

#### 3. Карточка "Переходящие всего"

**Структура:**
- Большая цифра: сумма за 3 месяца
- Три цифры: декабрь, ноябрь, октябрь

**Визуализация:**
```
┌─────────────────────────────────────┐
│  Переходящие всего                  │
│  ┌──────────┐                       │
│  │    89    │  ← Сумма за 3 месяца │
│  └──────────┘                       │
│    Декабрь: 30  Ноябрь: 28  Октябрь: 31 │
└─────────────────────────────────────┘
```

#### 4. Карточка "Закрытия по стадиям"

**Структура:**
- Только суммарные количества за 3 месяца
- Три стадии:
  - Успешное закрытие
  - Отклонено
  - Закрыли без задачи

**Визуализация:**
```
┌─────────────────────────────────────┐
│  Закрытия по стадиям                │
│  ✓ Успешное закрытие: 98            │
│  ✗ Отклонено: 24                    │
│  ⚠ Закрыли без задачи: 12           │
└─────────────────────────────────────┘
```

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Создание компонента SummaryCardsMonths.vue

1. **Создать файл компонента:**
   ```bash
   touch vue-app/src/components/graph-admission-closure/SummaryCardsMonths.vue
   ```

2. **Реализовать структуру компонента:**
   - Template с 4 summary-карточками
   - Script setup с props
   - Стили в соответствии с гайдлайнами Bitrix24

3. **Реализовать карточку "Новые за период":**
   - Большая цифра (сумма)
   - Три маленькие цифры (по месяцам)
   - Визуальное разделение

4. **Реализовать карточку "Закрытые всего":**
   - Большая цифра (сумма)
   - Иерархическая структура по месяцам
   - Деление второго уровня для каждого месяца

5. **Реализовать карточку "Переходящие всего":**
   - Большая цифра (сумма)
   - Три цифры по месяцам

6. **Реализовать карточку "Закрытия по стадиям":**
   - Список стадий с суммарными количествами
   - Цветовое кодирование стадий

### Шаг 2: Интеграция в GraphAdmissionClosureMonthsDashboard.vue

1. **Импортировать компонент:**
   ```javascript
   import SummaryCardsMonths from './SummaryCardsMonths.vue';
   ```

2. **Добавить компонент в template:**
   - Разместить перед графиками
   - Передать данные из API

3. **Обработать данные:**
   - Нормализовать данные из API
   - Подготовить структуру для компонента

---

## 🔍 Детали реализации

### Обработка данных

**Нормализация данных:**
- Проверять наличие всех обязательных полей
- Устанавливать значения по умолчанию для отсутствующих данных
- Обрабатывать null/undefined значения

**Валидация props:**
- Проверять структуру объекта `data`
- Валидировать массивы `newTicketsByMonth`, `closedTicketsByMonth`, etc.
- Логировать предупреждения при некорректных данных

### Форматирование чисел

**Отображение больших чисел:**
- Числа > 1000: форматировать с разделителями (1,234)
- Числа > 1000000: форматировать с сокращениями (1.2M)
- Использовать функцию форматирования для единообразия

**Обработка нулевых значений:**
- Показывать "0" вместо пустых значений
- Визуально выделять нулевые значения (более светлый цвет)

### Иерархическая структура

**Визуальное представление:**
- Использовать отступы для показа иерархии
- Границы слева для вложенных элементов
- Разные размеры шрифтов для разных уровней

**Адаптивность:**
- На мобильных устройствах уменьшать отступы
- Сворачивать иерархию при необходимости
- Использовать аккордеон для больших блоков (опционально)

### Производительность

**Оптимизация рендеринга:**
- Использовать `v-memo` для тяжёлых вычислений
- Кешировать computed-свойства
- Избегать лишних перерисовок

**Ленивая загрузка:**
- Загружать данные по мере необходимости
- Не загружать все данные сразу, если не нужно

## 💻 Примеры кода

### SummaryCardsMonths.vue

```vue
<template>
  <div class="summary-cards-months">
    <!-- Карточка "Новые за период" -->
    <div class="summary-card">
      <h3 class="card-title">Новые за период</h3>
      <div class="card-content">
        <div class="card-main-value">
          {{ formattedTotalNewTickets }}
        </div>
        <div class="card-month-breakdown">
          <div
            v-for="month in newTicketsByMonth"
            :key="month.month"
            class="month-item"
          >
            <span class="month-name">{{ month.monthName }}:</span>
            <span class="month-value">{{ month.count }}</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Карточка "Закрытые всего" -->
    <div class="summary-card">
      <h3 class="card-title">Закрытые всего</h3>
      <div class="card-content">
        <div class="card-main-value">
          {{ data.closedTickets }}
        </div>
        <div class="card-month-breakdown-hierarchical">
          <div
            v-for="month in data.closedTicketsByMonth"
            :key="month.month"
            class="month-item-hierarchical"
          >
            <div class="month-header">
              <span class="month-name">{{ month.monthName }}:</span>
              <span class="month-value">{{ month.count }}</span>
            </div>
            <div class="month-breakdown">
              <div class="breakdown-item breakdown-this-week">
                <span class="breakdown-icon">✓</span>
                <span class="breakdown-label">этой неделей:</span>
                <span class="breakdown-value">{{ month.closedCreatedThisWeek }}</span>
              </div>
              <div class="breakdown-item breakdown-other-week">
                <span class="breakdown-icon">↻</span>
                <span class="breakdown-label">другой неделей:</span>
                <span class="breakdown-value">{{ month.closedCreatedOtherWeek }}</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Карточка "Переходящие всего" -->
    <div class="summary-card">
      <h3 class="card-title">Переходящие всего</h3>
      <div class="card-content">
        <div class="card-main-value">
          {{ data.carryoverTickets }}
        </div>
        <div class="card-month-breakdown">
          <div
            v-for="month in data.carryoverTicketsByMonth"
            :key="month.month"
            class="month-item"
          >
            <span class="month-name">{{ month.monthName }}:</span>
            <span class="month-value">{{ month.count }}</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Карточка "Закрытия по стадиям" -->
    <div class="summary-card">
      <h3 class="card-title">Закрытия по стадиям</h3>
      <div class="card-content">
        <div class="stages-list">
          <div
            v-for="stage in data.stages"
            :key="stage.stageId"
            class="stage-item"
            :class="getStageClass(stage.stageId)"
          >
            <span class="stage-icon">{{ getStageIcon(stage.stageId) }}</span>
            <span class="stage-name">{{ stage.stageName }}:</span>
            <span class="stage-value">{{ stage.count }}</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';

const props = defineProps({
  data: {
    type: Object,
    required: true
  }
});

/**
 * Форматирование числа с разделителями
 */
function formatNumber(num) {
  if (num === null || num === undefined || isNaN(num)) {
    return '0';
  }
  
  if (num >= 1000000) {
    return (num / 1000000).toFixed(1) + 'M';
  }
  
  if (num >= 1000) {
    return num.toLocaleString('ru-RU');
  }
  
  return num.toString();
}

const totalNewTickets = computed(() => {
  return props.data?.newTickets || 0;
});

const formattedTotalNewTickets = computed(() => {
  return formatNumber(totalNewTickets.value);
});

const newTicketsByMonth = computed(() => {
  return props.data.newTicketsByMonth || [];
});

/**
 * Получить класс для стадии
 */
function getStageClass(stageId) {
  const classes = {
    'DT140_12:SUCCESS': 'stage-success',
    'DT140_12:FAIL': 'stage-fail',
    'DT140_12:UC_0GBU8Z': 'stage-other'
  };
  return classes[stageId] || '';
}

/**
 * Получить иконку для стадии
 */
function getStageIcon(stageId) {
  const icons = {
    'DT140_12:SUCCESS': '✓',
    'DT140_12:FAIL': '✗',
    'DT140_12:UC_0GBU8Z': '⚠'
  };
  return icons[stageId] || '•';
}
</script>

<style scoped>
.summary-cards-months {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: var(--spacing-lg, 20px);
  margin-bottom: var(--spacing-xl, 24px);
}

.summary-card {
  background-color: var(--b24-bg-white, #ffffff);
  border: 1px solid var(--b24-border-light, #e5e7eb);
  border-radius: var(--radius-md, 8px);
  padding: var(--spacing-lg, 20px);
  box-shadow: var(--shadow-sm, 0 1px 2px rgba(0, 0, 0, 0.05));
}

.card-title {
  margin: 0 0 var(--spacing-md, 16px) 0;
  font-size: var(--font-size-lg, 16px);
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.card-content {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md, 16px);
}

.card-main-value {
  font-size: 36px;
  font-weight: 700;
  color: var(--b24-primary, #007bff);
  line-height: 1;
}

.card-month-breakdown {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs, 4px);
}

.month-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm, 8px);
  font-size: var(--font-size-sm, 14px);
  color: var(--b24-text-secondary, #6b7280);
}

.month-name {
  font-weight: 500;
}

.month-value {
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.card-month-breakdown-hierarchical {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md, 16px);
}

.month-item-hierarchical {
  padding-left: var(--spacing-md, 16px);
  border-left: 2px solid var(--b24-border-light, #e5e7eb);
}

.month-header {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm, 8px);
  margin-bottom: var(--spacing-xs, 4px);
  font-size: var(--font-size-base, 14px);
}

.month-breakdown {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs, 4px);
  margin-top: var(--spacing-xs, 4px);
  padding-left: var(--spacing-md, 16px);
}

.breakdown-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs, 4px);
  font-size: var(--font-size-sm, 14px);
}

.breakdown-icon {
  font-size: 16px;
}

.breakdown-this-week {
  color: var(--b24-success, #28a745);
}

.breakdown-other-week {
  color: var(--b24-warning, #ffc107);
}

.breakdown-label {
  color: var(--b24-text-secondary, #6b7280);
}

.breakdown-value {
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.stages-list {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm, 8px);
}

.stage-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm, 8px);
  font-size: var(--font-size-base, 14px);
  padding: var(--spacing-sm, 8px);
  border-radius: var(--radius-sm, 4px);
  background-color: var(--b24-bg-light, #f9fafb);
}

.stage-icon {
  font-size: 18px;
  font-weight: 600;
}

.stage-name {
  flex: 1;
  color: var(--b24-text-primary, #111827);
}

.stage-value {
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.stage-success {
  border-left: 3px solid var(--b24-success, #28a745);
}

.stage-fail {
  border-left: 3px solid var(--b24-danger, #dc3545);
}

.stage-other {
  border-left: 3px solid var(--b24-warning, #ffc107);
}

/* Адаптивность */
@media (max-width: 768px) {
  .summary-cards-months {
    grid-template-columns: 1fr;
  }
  
  .card-main-value {
    font-size: 28px;
  }
}
</style>
```

---

## ✅ Критерии приёмки

- [ ] Компонент `SummaryCardsMonths.vue` создан и работает корректно
- [ ] Карточка "Новые за период" отображает 4 цифры (сумма + 3 месяца)
- [ ] Карточка "Закрытые всего" отображает иерархическую структуру (всего + помесячно + деление второго уровня)
- [ ] Карточка "Переходящие всего" отображает сумму и помесячную разбивку
- [ ] Карточка "Закрытия по стадиям" отображает суммарные количества за 3 месяца
- [ ] Все данные корректно отображаются из API
- [ ] Визуальная структура соответствует требованиям
- [ ] Иерархия данных понятна пользователю
- [ ] Компонент адаптивен (работает на мобильных устройствах)
- [ ] Компонент соответствует гайдлайнам Bitrix24 UI
- [ ] Данные обновляются при переключении режима

---

## 🔗 Зависимости

**Зависит от:**
- [TASK-053-03: Backend API для 3-месячного режима](./TASK-053-03-backend-api-3-months.md) — использует данные от API

**Зависит от этого этапа:**
- [TASK-053-06: Создание отдельного шаблона](./TASK-053-06-months-dashboard-template.md) — использует компонент в дашборде

---

## 📝 История правок

- **2025-12-17 13:45 (UTC+3, Брест):** Создана подзадача TASK-053-04
  - Определены требования для summary-карточек в 3-месячном режиме
  - Зафиксированы структура карточек и примеры кода
  - Добавлены критерии приёмки

---

## 💡 Примечания

- **Визуальный дизайн:** Карточки должны быть информативными, но не перегруженными
- **Иерархия:** Важно визуально показать иерархию данных (отступы, границы)
- **Адаптивность:** На мобильных устройствах карточки должны быть читаемыми
- **Тестирование:** Протестировать отображение данных с разными значениями (нули, большие числа)

