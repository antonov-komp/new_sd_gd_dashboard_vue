# TASK-058-04: Frontend — улучшение графика "Новые и закрытые тикеты"

**Дата создания:** 2025-12-18 08:31 (UTC+3, Брест)  
**Статус:** 📝 Черновик  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-058: Улучшение модуля "График приема и закрытий сектора 1С" — режим 3 месяца](./TASK-058-enrichment-3-months-mode.md)  
**Этап:** 4 из 5  
**Зависимости:** TASK-058-01 (Backend должен возвращать `previousPeriodData`)

---

## 📋 Описание

Улучшить график "Новые и закрытые тикеты" в компоненте `LineChartMonths.vue`, добавив:
1. Цифры на точках графика (даже при перекрытии — двойной чип)
2. Динамичный заголовок с указанием периода (месяца)
3. Сводный итог под графиком с 6 цифрами (2 показателя × 3 месяца) + словесный анализ

---

## 🎯 Контекст

### Текущее состояние

**Файл:** `vue-app/src/components/graph-admission-closure/LineChartMonths.vue`

**Текущее отображение:**
```vue
<div class="chart-section">
  <h3 class="chart-title">Новые и Закрытые тикеты</h3>  <!-- Статичный заголовок -->
  <div class="chart-container">
    <Line :data="newClosedChartData" :options="chartOptions" />
    <!-- Нет цифр на точках -->
  </div>
  <!-- Нет сводного итога -->
</div>
```

**Проблемы:**
- Статичный заголовок не показывает период
- Нет цифр на точках графика
- Нет сводного итога с анализом

### Требуемое состояние

**Новое отображение:**
```vue
<div class="chart-section">
  <h3 class="chart-title">
    Новые и Закрытые тикеты
    <span class="chart-period">(Октябрь — Декабрь 2025)</span>  <!-- Динамичный период -->
  </h3>
  <div class="chart-container">
    <Line :data="newClosedChartData" :options="chartOptions" />
    <!-- Цифры на точках графика -->
  </div>
  <div class="chart-summary">
    <h4 class="summary-title">Сводный итог</h4>
    <div class="summary-numbers">
      <div class="summary-row">
        <span class="summary-label">Новые:</span>
        <span class="summary-values">50 (окт) → 60 (ноя) → 40 (дек)</span>
      </div>
      <div class="summary-row">
        <span class="summary-label">Закрытые:</span>
        <span class="summary-values">80 (окт) → 90 (ноя) → 70 (дек)</span>
      </div>
    </div>
    <div class="summary-analysis">
      <p>Новые тикеты: рост на 20% в ноябре, снижение на 33.3% в декабре</p>
      <p>Закрытые тикеты: рост на 12.5% в ноябре, снижение на 22.2% в декабре</p>
    </div>
  </div>
</div>
```

---

## 🔍 Детализация требований

### 1. Цифры на точках графика

**Задачи:**
1. Установить/настроить Chart.js плагин для отображения значений на точках
2. Добавить конфигурацию в `chartOptions` для плагина `datalabels`
3. Обработать случай перекрытия точек (двойной чип)

**Установка плагина (если требуется):**
```bash
npm install chartjs-plugin-datalabels
```

**Импорт плагина:**
```javascript
import ChartDataLabels from 'chartjs-plugin-datalabels';
import { Chart as ChartJS } from 'chart.js';

ChartJS.register(ChartDataLabels);
```

**Конфигурация плагина:**
```javascript
const chartOptions = {
  // ... существующие настройки ...
  plugins: {
    // ... существующие плагины ...
    datalabels: {
      anchor: 'end',
      align: 'top',
      formatter: (value, context) => {
        // Форматирование значения
        return value !== null && value !== undefined ? value.toString() : '';
      },
      color: '#333',
      font: {
        size: 12,
        weight: 'bold'
      },
      padding: {
        top: 4,
        bottom: 4
      },
      display: function(context) {
        // Показывать только если значение не null/undefined
        return context.dataset.data[context.dataIndex] !== null &&
               context.dataset.data[context.dataIndex] !== undefined;
      }
    }
  }
};
```

**Обработка перекрытия точек:**
- Если две точки перекрываются, Chart.js автоматически покажет оба значения
- Можно настроить смещение через `offset` в конфигурации `datalabels`
- Для разных датасетов можно использовать разные `anchor` и `align`:
  - Новые тикеты: `anchor: 'end', align: 'top'`
  - Закрытые тикеты: `anchor: 'start', align: 'top'` (чтобы не перекрывались)

**Детальная конфигурация для разных датасетов:**
```javascript
// В конфигурации datasets можно переопределить настройки datalabels для каждого датасета
datasets: [
  {
    label: 'Новые тикеты',
    data: newData,
    // ... другие настройки ...
    datalabels: {
      anchor: 'end',
      align: 'top',
      offset: 4,
      color: chartColors.primary
    }
  },
  {
    label: 'Закрытые тикеты',
    data: closedData,
    // ... другие настройки ...
    datalabels: {
      anchor: 'start',
      align: 'top',
      offset: 4,
      color: chartColors.success
    }
  }
]
```

### 2. Динамичный заголовок

**Задачи:**
1. Создать computed-свойство `chartPeriod` для формирования периода
2. Обновить заголовок графика для отображения периода

**Computed-свойство:**
```javascript
const chartPeriod = computed(() => {
  const months = props.data?.newTicketsByMonth || [];
  
  if (months.length === 0) {
    return null;
  }
  
  const firstMonth = months[0];
  const lastMonth = months[months.length - 1];
  
  if (!firstMonth || !lastMonth) {
    return null;
  }
  
  const firstMonthName = firstMonth.monthName || `Месяц ${firstMonth.month}`;
  const lastMonthName = lastMonth.monthName || `Месяц ${lastMonth.month}`;
  const year = firstMonth.year || new Date().getFullYear();
  
  return `${firstMonthName} — ${lastMonthName} ${year}`;
});
```

**Обновление шаблона:**
```vue
<h3 class="chart-title">
  Новые и Закрытые тикеты
  <span v-if="chartPeriod" class="chart-period">({{ chartPeriod }})</span>
</h3>
```

### 3. Сводный итог под графиком

**Задачи:**
1. Создать computed-свойство `summaryNumbers` для формирования строки с цифрами
2. Создать computed-свойство `summaryAnalysis` для расчета анализа
3. Реализовать функцию `generateTextAnalysis()` для словесного анализа
4. Добавить блок "Сводный итог" под графиком

**Computed-свойство для цифр:**
```javascript
const summaryNumbers = computed(() => {
  const months = props.data?.newTicketsByMonth || [];
  const closedMonths = props.data?.closedTicketsByMonth || [];
  
  if (months.length === 0) {
    return { new: '—', closed: '—' };
  }
  
  const newValues = months.map(m => 
    `${formatNumber(m.count || 0)} (${m.monthName || m.month})`
  ).join(' → ');
  
  const closedValues = closedMonths.map(m => 
    `${formatNumber(m.count || 0)} (${m.monthName || m.month})`
  ).join(' → ');
  
  return {
    new: newValues,
    closed: closedValues
  };
});
```

**Computed-свойство для анализа:**
```javascript
const summaryAnalysis = computed(() => {
  const months = props.data?.newTicketsByMonth || [];
  const closedMonths = props.data?.closedTicketsByMonth || [];
  
  if (months.length < 2) {
    return ['Недостаточно данных для анализа'];
  }
  
  const analysis = [];
  
  // Анализ новых тикетов
  if (months.length >= 2) {
    const month1 = months[0];
    const month2 = months[1];
    const month3 = months[2];
    
    // Изменение между первым и вторым месяцем
    if (month2 && month1 && typeof month1.count === 'number' && typeof month2.count === 'number') {
      if (month1.count > 0) {
        const change1 = ((month2.count - month1.count) / month1.count) * 100;
        if (isFinite(change1) && !isNaN(change1)) {
          const trend = change1 >= 0 ? 'рост' : 'снижение';
          const absChange = Math.abs(change1);
          analysis.push(
            `Новые тикеты: ${trend} на ${absChange.toFixed(1)}% в ${month2.monthName || month2.month}`
          );
        }
      } else if (month1.count === 0 && month2.count > 0) {
        analysis.push(
          `Новые тикеты: появление тикетов (${month2.count}) в ${month2.monthName || month2.month}`
        );
      }
    }
    
    // Изменение между вторым и третьим месяцем
    if (month3 && month2 && typeof month2.count === 'number' && typeof month3.count === 'number') {
      if (month2.count > 0) {
        const change2 = ((month3.count - month2.count) / month2.count) * 100;
        if (isFinite(change2) && !isNaN(change2)) {
          const trend = change2 >= 0 ? 'рост' : 'снижение';
          const absChange = Math.abs(change2);
          analysis.push(
            `Новые тикеты: ${trend} на ${absChange.toFixed(1)}% в ${month3.monthName || month3.month}`
          );
        }
      } else if (month2.count === 0 && month3.count > 0) {
        analysis.push(
          `Новые тикеты: появление тикетов (${month3.count}) в ${month3.monthName || month3.month}`
        );
      }
    }
  }
  
  // Анализ закрытых тикетов
  if (closedMonths.length >= 2) {
    const month1 = closedMonths[0];
    const month2 = closedMonths[1];
    const month3 = closedMonths[2];
    
    // Изменение между первым и вторым месяцем
    if (month2 && month1 && typeof month1.count === 'number' && typeof month2.count === 'number') {
      if (month1.count > 0) {
        const change1 = ((month2.count - month1.count) / month1.count) * 100;
        if (isFinite(change1) && !isNaN(change1)) {
          const trend = change1 >= 0 ? 'рост' : 'снижение';
          const absChange = Math.abs(change1);
          analysis.push(
            `Закрытые тикеты: ${trend} на ${absChange.toFixed(1)}% в ${month2.monthName || month2.month}`
          );
        }
      } else if (month1.count === 0 && month2.count > 0) {
        analysis.push(
          `Закрытые тикеты: появление закрытий (${month2.count}) в ${month2.monthName || month2.month}`
        );
      }
    }
    
    // Изменение между вторым и третьим месяцем
    if (month3 && month2 && typeof month2.count === 'number' && typeof month3.count === 'number') {
      if (month2.count > 0) {
        const change2 = ((month3.count - month2.count) / month2.count) * 100;
        if (isFinite(change2) && !isNaN(change2)) {
          const trend = change2 >= 0 ? 'рост' : 'снижение';
          const absChange = Math.abs(change2);
          analysis.push(
            `Закрытые тикеты: ${trend} на ${absChange.toFixed(1)}% в ${month3.monthName || month3.month}`
          );
        }
      } else if (month2.count === 0 && month3.count > 0) {
        analysis.push(
          `Закрытые тикеты: появление закрытий (${month3.count}) в ${month3.monthName || month3.month}`
        );
      }
    }
  }
  
  // Общая тенденция за период (если есть данные за все 3 месяца)
  if (months.length >= 3 && closedMonths.length >= 3) {
    const firstNew = months[0].count || 0;
    const lastNew = months[months.length - 1].count || 0;
    const firstClosed = closedMonths[0].count || 0;
    const lastClosed = closedMonths[closedMonths.length - 1].count || 0;
    
    if (firstNew > 0 && firstClosed > 0) {
      const newChange = ((lastNew - firstNew) / firstNew) * 100;
      const closedChange = ((lastClosed - firstClosed) / firstClosed) * 100;
      
      if (isFinite(newChange) && isFinite(closedChange)) {
        analysis.push(
          `Общая динамика за период: новые тикеты ${newChange >= 0 ? 'выросли' : 'снизились'} на ${Math.abs(newChange).toFixed(1)}%, закрытые тикеты ${closedChange >= 0 ? 'выросли' : 'снизились'} на ${Math.abs(closedChange).toFixed(1)}%`
        );
      }
    }
  }
  
  return analysis.length > 0 ? analysis : ['Недостаточно данных для анализа'];
});
```

**Детали реализации анализа:**

1. **Валидация данных:**
   - Проверка типов данных (`typeof month.count === 'number'`)
   - Проверка на `isFinite()` и `!isNaN()` для результатов расчетов
   - Обработка случая, когда `count` может быть `null` или `undefined`

2. **Обработка граничных случаев:**
   - Если `month1.count === 0` и `month2.count > 0` → "появление тикетов"
   - Если `month1.count > 0` и `month2.count === 0` → "полное исчезновение"
   - Если оба равны 0 → не добавлять в анализ

3. **Форматирование:**
   - Округление до 1 знака после запятой
   - Использование правильных названий месяцев из данных
   - Формирование понятных фраз для пользователя

**Шаблон сводного итога:**
```vue
<div class="chart-summary">
  <h4 class="summary-title">Сводный итог</h4>
  <div class="summary-numbers">
    <div class="summary-row">
      <span class="summary-label">Новые:</span>
      <span class="summary-values">{{ summaryNumbers.new }}</span>
    </div>
    <div class="summary-row">
      <span class="summary-label">Закрытые:</span>
      <span class="summary-values">{{ summaryNumbers.closed }}</span>
    </div>
  </div>
  <div class="summary-analysis">
    <p v-for="(analysis, index) in summaryAnalysis" :key="index">
      {{ analysis }}
    </p>
  </div>
</div>
```

---

## 🔧 Технические требования

### Зависимости

**Установка плагина (если требуется):**
```bash
cd vue-app
npm install chartjs-plugin-datalabels
```

**Импорт в компоненте:**
```javascript
import ChartDataLabels from 'chartjs-plugin-datalabels';
import { Chart as ChartJS } from 'chart.js';

// Регистрация плагина (делать один раз, можно в отдельном файле или в main.js)
ChartJS.register(ChartDataLabels);
```

**Важно:** 
- Плагин должен быть зарегистрирован до использования компонента
- Если плагин уже зарегистрирован в другом месте, не регистрировать повторно
- Проверить версию `chartjs-plugin-datalabels` на совместимость с версией Chart.js

**Проверка версий:**
```bash
# Проверить установленные версии
npm list chart.js chartjs-plugin-datalabels

# Рекомендуемые версии:
# chart.js: ^4.0.0
# chartjs-plugin-datalabels: ^2.2.0
```

### Структура данных

**Ожидаемая структура `props.data`:**
```javascript
{
  newTicketsByMonth: [
    { month: 10, monthName: 'Октябрь', count: 50, year: 2025 },
    { month: 11, monthName: 'Ноябрь', count: 60, year: 2025 },
    { month: 12, monthName: 'Декабрь', count: 40, year: 2025 }
  ],
  closedTicketsByMonth: [
    { month: 10, monthName: 'Октябрь', count: 80, year: 2025 },
    { month: 11, monthName: 'Ноябрь', count: 90, year: 2025 },
    { month: 12, monthName: 'Декабрь', count: 70, year: 2025 }
  ],
  previousPeriodData: {
    newTickets: 133,
    closedTickets: 200
  }
}
```

---

## ✅ Критерии приёмки

- [ ] Плагин `chartjs-plugin-datalabels` установлен и зарегистрирован
- [ ] Цифры отображаются на всех точках графика
- [ ] При перекрытии точек показываются оба значения (двойной чип)
- [ ] Динамичный заголовок показывает период (месяца и год)
- [ ] Сводный итог отображает 6 цифр (2 показателя × 3 месяца)
- [ ] Словесный анализ основан на процентных показателях
- [ ] Анализ использует данные предыдущих месяцев для расчета процентов
- [ ] Стили адаптивны для мобильных устройств
- [ ] Код протестирован с реальными данными

---

## 📚 Примеры реализации

### Пример 1: Полная конфигурация chartOptions с datalabels

```javascript
const chartOptions = {
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    legend: {
      position: 'top',
      labels: {
        font: {
          size: 14,
          weight: '500'
        },
        padding: 16,
        boxWidth: 20,
        boxHeight: 12
      }
    },
    tooltip: {
      enabled: true,
      titleFont: {
        size: 14,
        weight: 'bold'
      },
      bodyFont: {
        size: 13
      },
      padding: 12
    },
    datalabels: {
      anchor: 'end',
      align: 'top',
      formatter: (value, context) => {
        if (value === null || value === undefined || isNaN(value)) {
          return '';
        }
        return value.toString();
      },
      color: '#333',
      font: {
        size: 12,
        weight: 'bold'
      },
      padding: {
        top: 4,
        bottom: 4
      },
      display: function(context) {
        const value = context.dataset.data[context.dataIndex];
        // Показывать только если значение валидное и не равно 0 (опционально)
        // Можно изменить условие, если нужно показывать и нули
        return value !== null && 
               value !== undefined && 
               !isNaN(value) && 
               isFinite(value);
      },
      // Дополнительные настройки для лучшей читаемости
      textStrokeColor: '#fff',
      textStrokeWidth: 2,
      textShadowBlur: 4,
      textShadowColor: 'rgba(0, 0, 0, 0.3)',
      backgroundColor: 'rgba(255, 255, 255, 0.8)',
      borderColor: '#333',
      borderWidth: 1,
      borderRadius: 4,
      padding: 4
    }
  },
  scales: {
    y: {
      beginAtZero: true,
      ticks: {
        precision: 0,
        font: {
          size: 14
        },
        padding: 10
      },
      grid: {
        lineWidth: 1.5
      }
    },
    x: {
      ticks: {
        maxRotation: 0,
        minRotation: 0,
        font: {
          size: 14
        },
        padding: 10
      },
      grid: {
        lineWidth: 1.5
      }
    }
  }
};
```

### Пример 2: Полные стили для сводного итога

```css
.chart-summary {
  margin-top: 24px;
  padding: 20px;
  background-color: var(--b24-bg-light, #f9fafb);
  border-radius: 8px;
  border: 1px solid var(--b24-border-light, #e5e7eb);
}

.summary-title {
  margin: 0 0 16px 0;
  font-size: 16px;
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.summary-numbers {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-bottom: 16px;
}

.summary-row {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 14px;
}

.summary-label {
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
  min-width: 100px;
}

.summary-values {
  color: var(--b24-text-secondary, #6b7280);
  font-family: 'Courier New', monospace;
}

.summary-analysis {
  padding-top: 16px;
  border-top: 1px solid var(--b24-border-light, #e5e7eb);
}

.summary-analysis p {
  margin: 8px 0;
  font-size: 14px;
  color: var(--b24-text-primary, #111827);
  line-height: 1.5;
}

/* Адаптивность */
@media (max-width: 768px) {
  .chart-summary {
    padding: 16px;
  }
  
  .summary-row {
    flex-direction: column;
    align-items: flex-start;
    gap: 4px;
  }
  
  .summary-label {
    min-width: auto;
  }
}
```

---

## 🔗 Зависимости

- **TASK-058-01:** Backend должен возвращать `previousPeriodData`
- **TASK-053-05:** Линейные графики для 3-месячного режима (базовая реализация)

---

## 📝 История правок

- **2025-12-18 08:31 (UTC+3, Брест):** Создан детализированный документ для этапа 4
  - Описаны требования к цифрам на точках
  - Описаны требования к динамичному заголовку
  - Описаны требования к сводному итогу
  - Добавлены примеры реализации
  - Добавлены критерии приёмки

---

## ⚠️ Примечания

- **Важно:** Плагин `chartjs-plugin-datalabels` должен быть установлен и зарегистрирован
- **Производительность:** Анализ должен рассчитываться эффективно (computed-свойства)
- **Адаптивность:** Сводный итог должен корректно отображаться на мобильных устройствах

## 🐛 Типичные ошибки и их решения

### Ошибка 1: Плагин datalabels не работает

**Причина:** Плагин не установлен или не зарегистрирован

**Решение:**
- Проверить установку: `npm list chartjs-plugin-datalabels`
- Проверить регистрацию: `ChartJS.register(ChartDataLabels)`
- Проверить версию Chart.js на совместимость

### Ошибка 2: Цифры не отображаются на точках

**Причина:** Неправильная конфигурация `datalabels` или конфликт с другими плагинами

**Решение:**
- Проверить конфигурацию `display` в `datalabels`
- Проверить, что значения в данных не null/undefined
- Проверить консоль браузера на наличие ошибок

### Ошибка 3: Перекрытие точек не обрабатывается

**Причина:** Неправильная настройка `anchor` и `align` для разных датасетов

**Решение:**
- Использовать разные `anchor` для разных датасетов:
  - Новые тикеты: `anchor: 'end'`
  - Закрытые тикеты: `anchor: 'start'`
- Настроить `offset` для смещения

### Ошибка 4: Анализ показывает неправильные проценты

**Причина:** Неправильная формула расчета или невалидные данные

**Решение:**
- Проверить формулу расчета процентов
- Добавить проверку на `isFinite()` и `!isNaN()`
- Проверить типы данных перед расчетом
- Добавить логирование для отладки

## 🔗 Интеграция с существующим кодом

### Использование существующих функций

**Функция `formatNumber()` уже существует:**
- Использовать для форматирования чисел в сводном итоге
- Не дублировать логику

**Computed-свойство `newClosedChartData`:**
- Уже существует и используется для графика
- Не нужно изменять, только добавить конфигурацию `datalabels`

**Объект `chartOptions`:**
- Уже определен в компоненте
- Добавить конфигурацию `datalabels` в секцию `plugins`

## 📋 Пошаговая инструкция реализации

### Шаг 1: Установка плагина datalabels

1. Открыть терминал в директории `vue-app`
2. Выполнить: `npm install chartjs-plugin-datalabels`
3. Проверить установку: `npm list chartjs-plugin-datalabels`
4. Обновить `package.json` (если требуется)

### Шаг 2: Регистрация плагина

1. Открыть файл `vue-app/src/components/graph-admission-closure/LineChartMonths.vue`
2. В секции импортов добавить:
   ```javascript
   import ChartDataLabels from 'chartjs-plugin-datalabels';
   import { Chart as ChartJS } from 'chart.js';
   ChartJS.register(ChartDataLabels);
   ```
3. Или зарегистрировать в `main.js` (если используется глобально)

### Шаг 3: Добавление конфигурации datalabels

1. Найти объект `chartOptions` в компоненте
2. Добавить конфигурацию `datalabels` в секцию `plugins`
3. Настроить `anchor`, `align`, `formatter`, `color`, `font`
4. Настроить `display` для условного отображения
5. Протестировать отображение цифр на графике

### Шаг 4: Создание динамичного заголовка

1. Создать computed-свойство `chartPeriod`
2. Извлечь первый и последний месяц из данных
3. Сформировать строку периода
4. Обновить шаблон заголовка
5. Протестировать с различными данными

### Шаг 5: Создание сводного итога

1. Создать computed-свойство `summaryNumbers` для цифр
2. Создать computed-свойство `summaryAnalysis` для анализа
3. Реализовать функцию расчета процентов изменений
4. Добавить блок "Сводный итог" в шаблон
5. Добавить стили для блока
6. Протестировать отображение и расчеты

### Шаг 6: Тестирование

1. Проверить отображение цифр на всех точках графика
2. Проверить перекрытие точек (двойной чип)
3. Проверить динамичный заголовок
4. Проверить сводный итог с реальными данными
5. Проверить анализ на корректность расчетов

## 🔍 Дополнительные проверки

### Проверка 1: Корректность работы плагина datalabels

```javascript
// Добавить в код для отладки
const chartOptions = {
  // ...
  plugins: {
    datalabels: {
      // ...
      formatter: (value, context) => {
        console.log('Datalabel value:', value, 'at index:', context.dataIndex);
        return value !== null && value !== undefined ? value.toString() : '';
      }
    }
  }
};
```

### Проверка 2: Проверка корректности анализа

```javascript
// Добавить проверку после расчета анализа
watch(summaryAnalysis, (newAnalysis) => {
  console.log('Summary analysis:', newAnalysis);
  
  // Проверить, что все строки анализа валидны
  newAnalysis.forEach((line, index) => {
    if (!line || typeof line !== 'string') {
      console.error(`Invalid analysis line at index ${index}:`, line);
    }
  });
});
```

### Проверка 3: Визуальная проверка

- Проверить, что цифры видны на всех точках
- Проверить, что при перекрытии обе цифры видны
- Проверить, что динамичный заголовок показывает правильный период
- Проверить, что сводный итог отображает все 6 цифр
- Проверить, что анализ понятен и корректен
- **Валидация:** Всегда проверять типы данных и валидность результатов расчетов
- **Читаемость:** Цифры на точках должны быть хорошо видны на фоне графика

## 🧪 Тестирование

### Тестовые сценарии для цифр на точках

1. **Нормальный случай:**
   - График с 3 точками → все 3 точки должны показывать цифры

2. **Перекрытие точек:**
   - Если две линии пересекаются в одной точке → обе цифры должны быть видны

3. **Нулевые значения:**
   - Если значение равно 0 → цифра "0" должна отображаться (или не отображаться, в зависимости от настройки)

4. **Большие числа:**
   - Если значение > 1000 → должно отображаться с форматированием (например, "1.5K")

### Тестовые сценарии для сводного итога

1. **Нормальный случай:**
   - Данные за 3 месяца → должен показать 6 цифр и анализ

2. **Недостаточно данных:**
   - Данные за 1 месяц → должен показать "Недостаточно данных для анализа"

3. **Нулевые значения:**
   - Один из месяцев имеет count = 0 → должен корректно обработать

4. **Очень большие изменения:**
   - Изменение > 100% → должен корректно отобразить

### Примеры тестовых данных

```javascript
// Тест 1: Нормальные данные
const testData1 = {
  newTicketsByMonth: [
    { month: 10, monthName: 'Октябрь', count: 50, year: 2025 },
    { month: 11, monthName: 'Ноябрь', count: 60, year: 2025 },
    { month: 12, monthName: 'Декабрь', count: 40, year: 2025 }
  ],
  closedTicketsByMonth: [
    { month: 10, monthName: 'Октябрь', count: 80, year: 2025 },
    { month: 11, monthName: 'Ноябрь', count: 90, year: 2025 },
    { month: 12, monthName: 'Декабрь', count: 70, year: 2025 }
  ]
};
// Ожидаемый результат: 
// - Цифры на всех точках графика
// - Сводный итог с 6 цифрами
// - Анализ с процентами изменений

// Тест 2: Недостаточно данных
const testData2 = {
  newTicketsByMonth: [
    { month: 10, monthName: 'Октябрь', count: 50, year: 2025 }
  ],
  closedTicketsByMonth: []
};
// Ожидаемый результат: "Недостаточно данных для анализа"

// Тест 3: Нулевые значения
const testData3 = {
  newTicketsByMonth: [
    { month: 10, monthName: 'Октябрь', count: 0, year: 2025 },
    { month: 11, monthName: 'Ноябрь', count: 50, year: 2025 },
    { month: 12, monthName: 'Декабрь', count: 100, year: 2025 }
  ]
};
// Ожидаемый результат: 
// - Корректная обработка нулевых значений
// - Анализ должен показать "появление тикетов" для ноября
```

