# TASK-059-03: Параллельные запросы к Bitrix24 API

**Дата создания:** 2025-12-18 11:00 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Backend Developer (PHP)  
**Родительская задача:** [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)  
**Подзадача:** Подзадача 3 из TASK-059

---

## 📋 Описание

Оптимизировать загрузку данных из Bitrix24 API в режиме "3 месяца" модуля "График приёма и закрытий сектора 1С". Проблема: запросы 1 и 2 выполняются последовательно, что увеличивает общее время загрузки.

**Цель:** Выполнять запросы 1 и 2 параллельно для сокращения времени загрузки на 30-50%.

---

## 🎯 Контекст

### Текущая проблема:

**Запросы 1 и 2 выполняются последовательно:**

```php
// Текущая реализация (строки 609-714)
// Запрос 1: Тикеты, созданные за период (3-5 секунд)
do {
    $result = CRest::call('crm.item.list', [...]);
    // ...
} while ($hasMore);

// Запрос 2: Тикеты, закрытые за период (3-5 секунд)
do {
    $result = CRest::call('crm.item.list', [...]);
    // ...
} while ($hasMore);
```

**Проблемы:**
- Запросы выполняются последовательно (один за другим)
- Общее время: 6-10 секунд (3-5 секунд × 2)
- Неэффективное использование времени ожидания

### Решение:

Выполнять запросы 1 и 2 параллельно через `curl_multi_exec()`.

**Логика:**
- Создать два параллельных запроса через `curl_multi_init()`
- Выполнить оба запроса одновременно
- Обработать результаты по мере получения

**Ограничения:**
- Batch-запросы Bitrix24 не поддерживают пагинацию напрямую
- Нужно использовать `curl_multi_exec()` для параллельных запросов
- Каждый запрос с пагинацией обрабатывается отдельно

---

## 🏗️ Модули и компоненты

### Изменяемые файлы:
- `api/graph-1c-admission-closure.php` — основной API endpoint
  - Запрос 1: Тикеты, созданные за период (строки 609-660)
  - Запрос 2: Тикеты, закрытые за период (строки 662-714)

### Новые функции:
- `loadTicketsParallel()` — функция для параллельной загрузки тикетов
- `executeParallelQueries()` — функция для выполнения параллельных запросов через curl_multi

---

## 📦 Зависимости

- **От задач:** 
  - TASK-059: Оптимизация загрузки режима 3 месяца
- **От модулей:** 
  - Модуль 3: График приёма и закрытий сектора 1С
  - TASK-053: Режим выбора периода 3 месяца
  - TASK-054: Исправление пагинации

---

## 🎯 Ступенчатые подзадачи

### Подзадача 3.1: Изучение возможностей параллельных запросов

**Цель:** Понять, как реализовать параллельные запросы к Bitrix24 API.

**Шаги:**

1. **Изучить возможности `curl_multi_exec()`**
   - Понять, как создавать параллельные запросы
   - Понять, как обрабатывать результаты

2. **Изучить структуру CRest**
   - Файл: `crest.php`
   - Понять, как CRest выполняет запросы
   - Понять, можно ли использовать CRest для параллельных запросов

3. **Определить подход к реализации**
   - Использовать `curl_multi_exec()` напрямую
   - Или создать обёртку над CRest для параллельных запросов

**Критерии приёмки:**
- [ ] Возможности параллельных запросов изучены
- [ ] Подход к реализации определён

---

### Подзадача 3.2: Создание функции для параллельных запросов

**Цель:** Создать функцию для выполнения параллельных запросов к Bitrix24 API.

**Шаги:**

1. **Создать функцию `executeParallelQueries()`**
   - Параметры: массив запросов (метод, параметры)
   - Возвращает: массив результатов
   - Использует `curl_multi_exec()` для параллельного выполнения

2. **Реализовать обработку результатов**
   - Обработка успешных запросов
   - Обработка ошибок
   - Логирование

3. **Добавить поддержку пагинации**
   - Обработка пагинации для каждого запроса
   - Объединение результатов всех страниц

**Критерии приёмки:**
- [ ] Функция `executeParallelQueries()` создана
- [ ] Параллельные запросы работают корректно
- [ ] Пагинация обрабатывается корректно

**Пример кода:**

```php
/**
 * Выполнение параллельных запросов к Bitrix24 API
 * 
 * TASK-059-03: Функция для параллельного выполнения запросов
 * 
 * @param array $queries Массив запросов [['method' => '...', 'params' => [...]], ...]
 * @return array Массив результатов [0 => [...], 1 => [...]]
 */
function executeParallelQueries(array $queries): array
{
    if (empty($queries)) {
        return [];
    }
    
    $multiHandle = curl_multi_init();
    $handles = [];
    $results = [];
    
    // Создание curl-дескрипторов для каждого запроса
    foreach ($queries as $index => $query) {
        $url = getBitrix24ApiUrl($query['method'], $query['params']);
        
        $ch = curl_init($url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        curl_setopt($ch, CURLOPT_TIMEOUT, 30);
        curl_setopt($ch, CURLOPT_HTTPHEADER, [
            'Content-Type: application/json'
        ]);
        
        curl_multi_add_handle($multiHandle, $ch);
        $handles[$index] = $ch;
    }
    
    // Выполнение параллельных запросов
    $running = null;
    do {
        curl_multi_exec($multiHandle, $running);
        curl_multi_select($multiHandle);
    } while ($running > 0);
    
    // Получение результатов
    foreach ($handles as $index => $ch) {
        $response = curl_multi_getcontent($ch);
        $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
        $error = curl_error($ch);
        
        if ($error) {
            error_log("[ParallelQuery] Error for query {$index}: {$error}");
            $results[$index] = ['error' => $error];
        } elseif ($httpCode !== 200) {
            error_log("[ParallelQuery] HTTP error {$httpCode} for query {$index}");
            $results[$index] = ['error' => "HTTP {$httpCode}"];
        } else {
            $data = json_decode($response, true);
            $results[$index] = $data ?? ['error' => 'Invalid JSON'];
        }
        
        curl_multi_remove_handle($multiHandle, $ch);
        curl_close($ch);
    }
    
    curl_multi_close($multiHandle);
    
    return $results;
}

/**
 * Получение URL для запроса к Bitrix24 API
 * 
 * @param string $method Метод API
 * @param array $params Параметры запроса
 * @return string URL для запроса
 */
function getBitrix24ApiUrl(string $method, array $params): string
{
    // Используем настройки из CRest
    $webhookUrl = defined('C_REST_WEB_HOOK_URL') ? C_REST_WEB_HOOK_URL : '';
    
    if (empty($webhookUrl)) {
        throw new Exception('Bitrix24 webhook URL not configured');
    }
    
    $url = rtrim($webhookUrl, '/') . '/' . $method;
    
    if (!empty($params)) {
        $url .= '?' . http_build_query($params);
    }
    
    return $url;
}
```

---

### Подзадача 3.3: Рефакторинг запросов 1 и 2 для параллельного выполнения

**Цель:** Обновить запросы 1 и 2 для использования параллельного выполнения.

**Шаги:**

1. **Создать функцию `loadTicketsParallel()`**
   - Параметры: период, фильтры для запросов 1 и 2
   - Возвращает: объединённый массив тикетов
   - Использует `executeParallelQueries()` для параллельного выполнения

2. **Обновить логику обработки пагинации**
   - Обработка пагинации для каждого запроса отдельно
   - Объединение результатов всех страниц

3. **Заменить последовательные запросы на параллельные**
   - Заменить код запросов 1 и 2 (строки 609-714)
   - Использовать новую функцию `loadTicketsParallel()`

**Критерии приёмки:**
- [ ] Функция `loadTicketsParallel()` создана
- [ ] Запросы 1 и 2 выполняются параллельно
- [ ] Пагинация обрабатывается корректно
- [ ] Результаты корректны

**Пример кода:**

```php
/**
 * Параллельная загрузка тикетов (запросы 1 и 2)
 * 
 * TASK-059-03: Параллельное выполнение запросов 1 и 2
 * 
 * @param string $periodStartStr Начало периода
 * @param string $periodEndStr Конец периода
 * @param array $closingStages Закрывающие стадии
 * @param int $entityTypeId ID типа сущности
 * @param int $pageSize Размер страницы
 * @return array Объединённый массив тикетов
 */
function loadTicketsParallel(
    string $periodStartStr,
    string $periodEndStr,
    array $closingStages,
    int $entityTypeId,
    int $pageSize = 50
): array {
    $allTicketsMap = [];
    
    // Запрос 1: Тикеты, созданные за период
    $query1Params = [
        'entityTypeId' => $entityTypeId,
        'filter' => [
            '>=createdTime' => $periodStartStr,
            '<=createdTime' => $periodEndStr
        ],
        'select' => [
            'id', 'title', 'stageId', 'assignedById',
            'createdTime', 'updatedTime', 'movedTime',
            'UF_CRM_7_TYPE_PRODUCT', 'ufCrm7TypeProduct'
        ],
        'start' => 0
    ];
    
    // Запрос 2: Тикеты, закрытые за период
    $query2Params = [
        'entityTypeId' => $entityTypeId,
        'filter' => [
            '>=movedTime' => $periodStartStr,
            '<=movedTime' => $periodEndStr,
            'stageId' => $closingStages
        ],
        'select' => [
            'id', 'title', 'stageId', 'assignedById',
            'createdTime', 'updatedTime', 'movedTime',
            'UF_CRM_7_TYPE_PRODUCT', 'ufCrm7TypeProduct'
        ],
        'start' => 0
    ];
    
    // TASK-059-03: Параллельное выполнение запросов 1 и 2
    // Выполняем первые страницы параллельно
    $queries = [
        ['method' => 'crm.item.list', 'params' => $query1Params],
        ['method' => 'crm.item.list', 'params' => $query2Params]
    ];
    
    $results = executeParallelQueries($queries);
    
    // Обработка результатов запроса 1
    if (isset($results[0]['error'])) {
        throw new Exception("Query 1 error: " . $results[0]['error']);
    }
    
    $query1Items = $results[0]['result']['items'] ?? [];
    foreach ($query1Items as $item) {
        $allTicketsMap[$item['id']] = $item;
    }
    
    // Обработка результатов запроса 2
    if (isset($results[1]['error'])) {
        throw new Exception("Query 2 error: " . $results[1]['error']);
    }
    
    $query2Items = $results[1]['result']['items'] ?? [];
    foreach ($query2Items as $item) {
        $allTicketsMap[$item['id']] = $item;
    }
    
    // Обработка пагинации для запроса 1
    $query1Next = $results[0]['result']['next'] ?? null;
    $query1HasMore = count($query1Items) === $pageSize && 
                     $query1Next !== null && 
                     $query1Next !== '' && 
                     $query1Next !== '0' && 
                     (int)$query1Next > 0;
    
    // Обработка пагинации для запроса 2
    $query2Next = $results[1]['result']['next'] ?? null;
    $query2HasMore = count($query2Items) === $pageSize && 
                     $query2Next !== null && 
                     $query2Next !== '' && 
                     $query2Next !== '0' && 
                     (int)$query2Next > 0;
    
    // Продолжаем загрузку страниц последовательно (если нужно)
    // Можно оптимизировать дальше, загружая следующие страницы параллельно
    $query1Start = $pageSize;
    while ($query1HasMore) {
        $query1Params['start'] = $query1Start;
        $result = CRest::call('crm.item.list', $query1Params);
        
        if (isset($result['error'])) {
            throw new Exception($result['error_description'] ?? $result['error']);
        }
        
        $items = $result['result']['items'] ?? [];
        foreach ($items as $item) {
            $allTicketsMap[$item['id']] = $item;
        }
        
        $nextValue = $result['result']['next'] ?? null;
        $hasNext = $nextValue !== null && 
                  $nextValue !== '' && 
                  $nextValue !== '0' && 
                  (int)$nextValue > 0;
        
        $query1HasMore = count($items) === $pageSize && $hasNext;
        $query1Start += $pageSize;
    }
    
    $query2Start = $pageSize;
    while ($query2HasMore) {
        $query2Params['start'] = $query2Start;
        $result = CRest::call('crm.item.list', $query2Params);
        
        if (isset($result['error'])) {
            throw new Exception($result['error_description'] ?? $result['error']);
        }
        
        $items = $result['result']['items'] ?? [];
        foreach ($items as $item) {
            $allTicketsMap[$item['id']] = $item;
        }
        
        $nextValue = $result['result']['next'] ?? null;
        $hasNext = $nextValue !== null && 
                  $nextValue !== '' && 
                  $nextValue !== '0' && 
                  (int)$nextValue > 0;
        
        $query2HasMore = count($items) === $pageSize && $hasNext;
        $query2Start += $pageSize;
    }
    
    return array_values($allTicketsMap);
}
```

---

### Подзадача 3.4: Интеграция параллельных запросов в API endpoint

**Цель:** Интегрировать параллельные запросы в `api/graph-1c-admission-closure.php`.

**Шаги:**

1. **Заменить последовательные запросы 1 и 2**
   - Файл: `api/graph-1c-admission-closure.php` (строки 609-714)
   - Использовать функцию `loadTicketsParallel()`

2. **Обновить логирование**
   - Добавить логирование времени выполнения параллельных запросов
   - Добавить информацию о количестве загруженных тикетов

3. **Добавить обработку ошибок**
   - Обработка ошибок параллельных запросов
   - Fallback на последовательные запросы при ошибке

**Критерии приёмки:**
- [ ] Параллельные запросы интегрированы
- [ ] Логирование обновлено
- [ ] Обработка ошибок добавлена

**Пример кода:**

```php
// TASK-059-03: Параллельная загрузка тикетов (запросы 1 и 2)
$startTime = microtime(true);

try {
    $allTickets = loadTicketsParallel(
        $periodStartStr,
        $periodEndStr,
        $closingStages,
        $entityTypeId,
        $pageSize
    );
    
    $loadTime = microtime(true) - $startTime;
    error_log("[MONTHS-PARALLEL] Loaded " . count($allTickets) . " tickets in {$loadTime} seconds");
    
    // Преобразуем в map для совместимости с существующим кодом
    $allTicketsMap = [];
    foreach ($allTickets as $ticket) {
        $allTicketsMap[$ticket['id']] = $ticket;
    }
} catch (Exception $e) {
    error_log("[MONTHS-PARALLEL] Error: " . $e->getMessage());
    error_log("[MONTHS-PARALLEL] Falling back to sequential queries");
    
    // Fallback на последовательные запросы
    // ... существующий код запросов 1 и 2 ...
}
```

---

### Подзадача 3.5: Тестирование параллельных запросов

**Цель:** Протестировать корректность работы параллельных запросов.

**Шаги:**

1. **Функциональное тестирование**
   - Проверить корректность результатов параллельных запросов
   - Сравнить результаты с последовательными запросами
   - Проверить обработку пагинации

2. **Производительность**
   - Измерить время выполнения параллельных запросов
   - Измерить время выполнения последовательных запросов
   - Сравнить результаты

3. **Обработка ошибок**
   - Проверить обработку ошибок параллельных запросов
   - Проверить fallback на последовательные запросы

**Критерии приёмки:**
- [ ] Результаты параллельных запросов корректны
- [ ] Время загрузки сократилось на 30-50%
- [ ] Обработка ошибок работает корректно

---

## 🔧 Технические требования

### Backend (PHP):
- PHP 8.4+
- Расширение `curl` с поддержкой `curl_multi_exec()`
- Bitrix24 REST API (CRest)

### Производительность:
- **Целевое время загрузки:** сокращение на 30-50%
- **Параллельность:** 2 запроса одновременно

---

## ✅ Критерии приёмки

### Общие критерии:
- [ ] Запросы 1 и 2 выполняются параллельно
- [ ] Результаты запросов корректны
- [ ] Время загрузки сократилось на 30-50%
- [ ] Код соответствует стандартам PSR-12
- [ ] Комментарии в коде добавлены
- [ ] Логирование обновлено
- [ ] Обработка ошибок реализована

### Детальные критерии:
- [ ] Функция `executeParallelQueries()` создана
- [ ] Функция `loadTicketsParallel()` создана
- [ ] Пагинация обрабатывается корректно
- [ ] Fallback на последовательные запросы работает

---

## 🧪 Тестирование

### Функциональное тестирование:
1. Проверить корректность результатов параллельных запросов
2. Сравнить результаты с последовательными запросами
3. Проверить обработку пагинации
4. Проверить объединение результатов запросов 1 и 2

### Производительность:
1. Измерить время выполнения параллельных запросов
2. Измерить время выполнения последовательных запросов
3. Сравнить результаты
4. Проверить нагрузку на Bitrix24 API

### Обработка ошибок:
1. Проверить обработку ошибок параллельных запросов
2. Проверить fallback на последовательные запросы
3. Проверить логирование ошибок

---

## 📝 Примечания

### Ограничения batch-запросов Bitrix24:

Batch-запросы Bitrix24 (`CRest::callBatch()`) не поддерживают пагинацию напрямую. Поэтому используется `curl_multi_exec()` для параллельного выполнения запросов.

### Альтернативный подход:

Если `curl_multi_exec()` не работает корректно, можно использовать:
- Асинхронные запросы через `Promise.all()` на фронтенде (если разделить запросы)
- Очередь запросов с приоритетами
- Оптимизация последовательных запросов (уменьшение времени ожидания)

---

## 🔗 Связанные документы

- [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)
- [Анализ оптимизации](../../REFACTOR/PERFORMANCE/optimization-3-months-mode-loading.md)
- [Модуль 3: График приёма и закрытий сектора 1С](../../MODULES/module-03-graph-admission-closure/README.md)

---

## 📊 История правок

- **2025-12-18 11:00 (UTC+3, Брест):** Создана подзадача для параллельных запросов к Bitrix24 API

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

