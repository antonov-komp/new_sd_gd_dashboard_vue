# TASK-053-05: Линейные графики для 3-месячного режима

**Дата создания:** 2025-12-17 13:50 (UTC+3, Брест)  
**Статус:** 📋 Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-053: Изменение формата выбора периода и внедрение 3-месячного режима](./TASK-053-period-selection-3-months-mode.md)  
**Этап:** 2.3 (Часть 2: Внедрение 3-месячного режима)

---

## 📋 Описание

Реализовать линейные графики для 3-месячного режима в модуле «График приёма и закрытий сектора 1С». Графики должны отображать данные за 3 месяца с разбивкой по неделям внутри каждого месяца. Графики должны быть расположены сверху-снизу (по всей ширине экрана) с табличной частью под каждым графиком.

---

## 🎯 Контекст

### Текущее состояние

**Текущие линейные графики (режим "4 недели"):**
- Графики расположены слева-справа (бок о бок)
- Ось X: 4 точки (по одной на неделю)
- Серии: новые, закрытые, переходящие тикеты

**Проблема:**
- Графики не адаптированы для 3-месячного режима
- Нет разбивки по неделям внутри месяцев
- Нет табличной части с детализацией

### Требуемое состояние

**Новые линейные графики (режим "3 месяца"):**
- Графики расположены сверху-снизу (по всей ширине экрана)
- Ось X: 3 точки (по одной на месяц)
- Внутри каждой точки месяца: разбивка по неделям
- Табличная часть под графиком с детализацией по месяцам и неделям

---

## 🔧 Технические требования

### Компонент: LineChartMonths.vue

**Расположение:** `vue-app/src/components/graph-admission-closure/LineChartMonths.vue`

**Props:**
```typescript
{
  data: {
    newTicketsByMonth: Array<{
      month: number,
      monthName: string,
      count: number,
      weeks: Array<{
        weekNumber: number,
        count: number
      }>
    }>,
    closedTicketsByMonth: Array<{
      month: number,
      monthName: string,
      count: number,
      weeks: Array<{
        weekNumber: number,
        count: number
      }>
    }>,
    carryoverTicketsByMonth: Array<{
      month: number,
      monthName: string,
      count: number,
      weeks: Array<{
        weekNumber: number,
        count: number
      }>
    }>
  }
}
```

**Функциональность:**
- Отображение линейного графика с 3 точками (по месяцам)
- Разбивка по неделям внутри каждого месяца
- Табличная часть с детализацией
- Адаптивность (работа на мобильных устройствах)

### Структура графика

**Ось X:**
- 3 точки: декабрь, ноябрь, октябрь
- Внутри каждой точки: маленькие метки для недель

**Серии данных:**
- Новые тикеты (по месяцам)
- Закрытые тикеты (всего)
- Переходящие тикеты (всего)
- Опционально: разбивка закрытых/переходящих (пунктирные линии)

**Табличная часть:**
- Под графиком: таблица с детализацией
- Структура: месяц → недели → данные
- Данные: новые тикеты, закрытые, переходящие

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Создание компонента LineChartMonths.vue

1. **Создать файл компонента:**
   ```bash
   touch vue-app/src/components/graph-admission-closure/LineChartMonths.vue
   ```

2. **Реализовать структуру компонента:**
   - Template с графиком и таблицей
   - Script setup с props
   - Использование Chart.js через vue-chartjs

3. **Реализовать линейный график:**
   - Настройка Chart.js для 3 точек (месяцы)
   - Добавление меток недель внутри точек
   - Настройка серий данных

4. **Реализовать табличную часть:**
   - Таблица с месяцами в строках
   - Недели в колонках
   - Данные для каждой недели

### Шаг 2: Интеграция в GraphAdmissionClosureMonthsDashboard.vue

1. **Импортировать компонент:**
   ```javascript
   import LineChartMonths from './LineChartMonths.vue';
   ```

2. **Добавить компонент в template:**
   - Разместить после summary-карточек
   - Передать данные из API

3. **Настроить расположение:**
   - Графики сверху-снизу (flex-direction: column)
   - Каждый график на полную ширину

---

## 🔍 Детали реализации

### Настройка Chart.js

**Конфигурация графика:**
- Использовать Chart.js версии 4.x (совместимость с vue-chartjs)
- Настроить responsive: true для адаптивности
- maintainAspectRatio: false для контроля высоты

**Ось X:**
- 3 точки: декабрь, ноябрь, октябрь
- Метки недель внутри точек (кастомные метки)
- Настройка ticks для предотвращения перекрытия

**Ось Y:**
- beginAtZero: true для корректного отображения
- precision: 0 для целых чисел
- Автоматическое масштабирование

### Разбивка по неделям внутри месяцев

**Визуализация недель:**
- Использовать кастомные плагины Chart.js для отображения недель
- Маленькие точки/метки внутри основных точек месяцев
- Пунктирные линии для соединения недель внутри месяца

**Альтернативный подход:**
- Использовать подграфики для каждой недели
- Или использовать tooltip для показа детализации по неделям

### Табличная часть

**Структура таблицы:**
- Заголовок: месяц + все недели
- Строки: по одному месяцу
- Колонки: по одной неделе
- Данные: значения для каждой недели

**Обработка отсутствующих данных:**
- Показывать "-" для отсутствующих недель
- Обрабатывать случаи, когда неделя не входит в месяц полностью

**Адаптивность таблицы:**
- Горизонтальная прокрутка на мобильных устройствах
- Фиксированный первый столбец (месяц)
- Минимальная ширина для колонок

### Производительность

**Оптимизация рендеринга:**
- Использовать `v-memo` для данных графика
- Кешировать вычисления для таблицы
- Ленивая загрузка данных для таблицы (если большая)

**Обработка больших объёмов данных:**
- Виртуализация таблицы при большом количестве недель
- Пагинация или бесконечная прокрутка

## 💻 Примеры кода

### LineChartMonths.vue

```vue
<template>
  <div class="line-chart-months">
    <div class="chart-container">
      <Line
        :data="chartData"
        :options="chartOptions"
      />
    </div>
    
    <div class="chart-table">
      <table class="data-table">
        <thead>
          <tr>
            <th>Месяц</th>
            <th
              v-for="week in allWeeks"
              :key="week"
              class="week-header"
            >
              Неделя {{ week }}
            </th>
          </tr>
        </thead>
        <tbody>
          <tr
            v-for="month in data.newTicketsByMonth"
            :key="month.month"
          >
            <td class="month-cell">{{ month.monthName }}</td>
            <td
              v-for="week in allWeeks"
              :key="week"
              class="week-cell"
            >
              {{ getWeekData(month, week) }}
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';
import { Line } from 'vue-chartjs';
import {
  Chart as ChartJS,
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend
} from 'chart.js';

ChartJS.register(
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend
);

const props = defineProps({
  data: {
    type: Object,
    required: true
  }
});

const chartData = computed(() => {
  const labels = props.data.newTicketsByMonth.map(m => m.monthName);
  
  return {
    labels,
    datasets: [
      {
        label: 'Новые тикеты',
        data: props.data.newTicketsByMonth.map(m => m.count),
        borderColor: '#007bff',
        backgroundColor: 'rgba(0, 123, 255, 0.1)',
        tension: 0.4
      },
      {
        label: 'Закрытые тикеты',
        data: props.data.closedTicketsByMonth.map(m => m.count),
        borderColor: '#28a745',
        backgroundColor: 'rgba(40, 167, 69, 0.1)',
        tension: 0.4
      },
      {
        label: 'Переходящие тикеты',
        data: props.data.carryoverTicketsByMonth.map(m => m.count),
        borderColor: '#ff9800',
        backgroundColor: 'rgba(255, 152, 0, 0.1)',
        tension: 0.4
      }
    ]
  };
});

const chartOptions = {
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    legend: {
      position: 'top'
    },
    tooltip: {
      callbacks: {
        title: (items) => {
          return items[0].label;
        }
      }
    }
  },
  scales: {
    y: {
      beginAtZero: true,
      ticks: {
        precision: 0
      }
    },
    x: {
      ticks: {
        maxRotation: 0,
        minRotation: 0
      }
    }
  }
};

const allWeeks = computed(() => {
  const weeks = new Set();
  props.data.newTicketsByMonth.forEach(month => {
    month.weeks.forEach(week => {
      weeks.add(week.weekNumber);
    });
  });
  return Array.from(weeks).sort((a, b) => a - b);
});

/**
 * Получить данные для недели в месяце
 * 
 * @param {Object} month - Объект месяца
 * @param {number} weekNumber - Номер недели
 * @returns {string|number} Значение или '-'
 */
function getWeekData(month, weekNumber) {
  if (!month || !month.weeks || !Array.isArray(month.weeks)) {
    return '-';
  }
  
  const week = month.weeks.find(w => w && w.weekNumber === weekNumber);
  
  if (!week || week.count === null || week.count === undefined) {
    return '-';
  }
  
  return formatNumber(week.count);
}

/**
 * Форматирование числа
 */
function formatNumber(num) {
  if (num === null || num === undefined || isNaN(num)) {
    return '0';
  }
  
  if (num >= 1000) {
    return num.toLocaleString('ru-RU');
  }
  
  return num.toString();
}
</script>

<style scoped>
.line-chart-months {
  width: 100%;
  margin-bottom: var(--spacing-xl, 24px);
}

.chart-container {
  width: 100%;
  height: 400px;
  margin-bottom: var(--spacing-lg, 20px);
}

.chart-table {
  width: 100%;
  overflow-x: auto;
}

.data-table {
  width: 100%;
  border-collapse: collapse;
  font-size: var(--font-size-sm, 14px);
}

.data-table th,
.data-table td {
  padding: var(--spacing-sm, 8px);
  text-align: center;
  border: 1px solid var(--b24-border-light, #e5e7eb);
}

.data-table th {
  background-color: var(--b24-bg-light, #f9fafb);
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.month-cell {
  font-weight: 600;
  text-align: left;
  background-color: var(--b24-bg-light, #f9fafb);
}

.week-header {
  min-width: 80px;
}

.week-cell {
  min-width: 80px;
}

/* Адаптивность */
@media (max-width: 768px) {
  .chart-container {
    height: 300px;
  }
  
  .data-table {
    font-size: var(--font-size-xs, 12px);
  }
}
</style>
```

---

## ✅ Критерии приёмки

- [ ] Компонент `LineChartMonths.vue` создан и работает корректно
- [ ] Графики расположены сверху-снизу (по ширине экрана)
- [ ] Ось X содержит 3 точки (по месяцам)
- [ ] Внутри каждой точки месяца есть разбивка по неделям
- [ ] Табличная часть отображает детализацию по месяцам и неделям
- [ ] График адаптивен (работает на мобильных устройствах)
- [ ] Серии данных отображаются корректно
- [ ] Tooltips работают корректно
- [ ] Компонент соответствует гайдлайнам Bitrix24 UI

---

## 🔗 Зависимости

**Зависит от:**
- [TASK-053-03: Backend API для 3-месячного режима](./TASK-053-03-backend-api-3-months.md) — использует данные от API

**Зависит от этого этапа:**
- [TASK-053-06: Создание отдельного шаблона](./TASK-053-06-months-dashboard-template.md) — использует компонент в дашборде

---

## 📝 История правок

- **2025-12-17 13:50 (UTC+3, Брест):** Создана подзадача TASK-053-05
  - Определены требования для линейных графиков в 3-месячном режиме
  - Зафиксированы структура графика и примеры кода
  - Добавлены критерии приёмки

---

## 💡 Примечания

- **Chart.js:** Использовать Chart.js через vue-chartjs для совместимости
- **Производительность:** Оптимизировать рендеринг графика при больших объёмах данных
- **Адаптивность:** На мобильных устройствах график должен быть читаемым
- **Табличная часть:** Таблица должна быть прокручиваемой на мобильных устройствах

