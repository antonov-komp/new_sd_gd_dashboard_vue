# TASK-047: Разбивка закрытых тикетов на три категории в модуле «График приёма и закрытий сектора 1С»

**Дата создания:** 2025-12-16 18:55 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer) + Backend (PHP)  
**Связь с задачей:** Расширение функциональности TASK-041

---

## 📋 Описание

Добавить три цифры над графиками в модуле «График приёма и закрытий сектора 1С», которые показывают разбивку закрытых тикетов по критерию создания, и обновить попап «Закрытые за неделю» с новым первым уровнем. Также добавить разбивку для переходящих тикетов по критерию создания.

1. **Три цифры над графиками (закрытые тикеты):**
   - **Первая цифра** — все закрытые тикеты за неделю (как есть, текущая реализация)
   - **Вторая цифра** — тикеты, закрытые на этой неделе И созданные этой неделей
   - **Третья цифра** — тикеты, закрытые на этой неделе, но созданные другой неделей

2. **Разбивка переходящих тикетов:**
   - **Основная цифра** — все переходящие тикеты (не закрытые на данный момент)
   - **Вторая цифра** — переходящие тикеты, созданные этой неделей (но ещё не закрытые)
   - **Третья цифра** — переходящие тикеты, созданные прошлыми неделями (но ещё не закрытые) — **жёсткий остаток предыдущего долга**

3. **Попап «Закрытые за неделю»:**
   - **Вкладки на первом уровне:**
     - **Вкладка "По категориям":** Две категории закрытых тикетов с количеством
       - Тикеты закрытые этой неделью и созданные этой неделью
       - Тикеты закрытые этой неделью и созданные ранее
       - При клике на категорию → переход к списку сотрудников этой категории
     - **Вкладка "По сотрудникам":** Список всех сотрудников (объединённый из обеих категорий)
       - При клике на сотрудника → показ градации:
         - "На этой неделе" (тикеты сотрудника, созданные этой неделей)
         - "На другой неделе" (тикеты сотрудника, созданные другой неделей)
       - При клике на градацию → список тикетов сотрудника выбранной градации
   - **Уровень 1 (для вкладки "По категориям"):** Список сотрудников выбранной категории
   - **Уровень 2 (для вкладки "По категориям"):** Список тикетов сотрудника
   - **Уровень 1 (для вкладки "По сотрудникам"):** Градация тикетов выбранного сотрудника
   - **Уровень 2 (для вкладки "По сотрудникам"):** Список тикетов сотрудника выбранной градации

---

## 🎯 Контекст

### Текущее состояние модуля

В модуле «График приёма и закрытий сектора 1С» (`GraphAdmissionClosureChart.vue`) отображается одна цифра «Закрытые за неделю» (`closedTickets`), которая показывает общее количество закрытых тикетов за неделю.

**Текущая логика:**
- Закрытые тикеты: тикеты с `stageId` в закрывающих стадиях (`DT140_12:SUCCESS`, `DT140_12:FAIL`, `DT140_12:UC_0GBU8Z`) и `movedTime` в диапазоне `[weekStartUtc, weekEndUtc]`
- API возвращает только общее количество `closedTickets`

**Проблема:**
- Нет разбивки закрытых тикетов по критерию создания
- Не видно, сколько тикетов было закрыто из тех, что созданы в эту же неделю
- Не видно, сколько тикетов было закрыто из тех, что созданы в другие недели

### Требуется

1. **Расширить API** для возврата дополнительных метрик и данных по категориям:
   - **Для закрытых тикетов:**
     - `closedTicketsCreatedThisWeek` — закрытые тикеты, созданные в эту же неделю
     - `closedTicketsCreatedOtherWeek` — закрытые тикеты, созданные в другие недели
     - `responsibleCreatedThisWeek[]` — список сотрудников для категории "созданные этой неделей"
     - `responsibleCreatedOtherWeek[]` — список сотрудников для категории "созданные ранее"
   - **Для переходящих тикетов:**
     - `carryoverTicketsCreatedThisWeek` — переходящие тикеты, созданные этой неделей (но ещё не закрытые)
     - `carryoverTicketsCreatedOtherWeek` — переходящие тикеты, созданные прошлыми неделями (но ещё не закрытые) — жёсткий остаток предыдущего долга

2. **Обновить фронтенд** для отображения разбивки:
   - **Для закрытых тикетов:** три цифры над графиками с визуальным разделением и пояснениями
   - **Для переходящих тикетов:** три цифры с разбивкой по критерию создания
   - Первая цифра: `closedTickets` (как есть) — отображается крупно в заголовке карточки с пояснением
   - Вторая цифра: `closedTicketsCreatedThisWeek` — отображается в отдельной секции с иконкой и пояснением
   - Третья цифра: `closedTicketsCreatedOtherWeek` — отображается в отдельной секции с иконкой и пояснением
   - Визуальное разделение: разделитель между основной цифрой и разбивкой, разделитель между двумя секциями разбивки

3. **Обновить попап `ResponsibleModal.vue`** для добавления нового первого уровня:
   - Уровень 0: Две категории закрытых тикетов с количеством
   - Уровень 1: Список сотрудников выбранной категории (как сейчас)
   - Уровень 2: Список тикетов сотрудника (как сейчас)

---

## 📊 Логика фильтрации

### Определение закрытых тикетов

**Базовый критерий (как есть):**
- `stageId` ∈ {`DT140_12:SUCCESS`, `DT140_12:FAIL`, `DT140_12:UC_0GBU8Z`}
- `movedTime` в диапазоне `[weekStartUtc, weekEndUtc]`

### Разбивка на категории

**Первая цифра (все закрытые):**
```php
// Все тикеты, удовлетворяющие базовому критерию
$closedTickets = count($tickets where stageId in closingStages AND movedTime in [weekStart, weekEnd])
```

**Вторая цифра (закрытые + созданные этой неделей):**
```php
// Тикеты, которые закрыты на этой неделе И созданы этой неделей
$closedTicketsCreatedThisWeek = count($tickets where 
    stageId in closingStages 
    AND movedTime in [weekStart, weekEnd]
    AND createdTime in [weekStart, weekEnd]
)
```

**Третья цифра (закрытые + созданные другой неделей):**
```php
// Тикеты, которые закрыты на этой неделе, но созданы другой неделей
$closedTicketsCreatedOtherWeek = count($tickets where 
    stageId in closingStages 
    AND movedTime in [weekStart, weekEnd]
    AND createdTime NOT in [weekStart, weekEnd]
)
```

**Проверка:**
```php
$closedTickets === $closedTicketsCreatedThisWeek + $closedTicketsCreatedOtherWeek
```

---

## 🔧 Технические требования

### API (бэкенд)

**Файл:** `api/graph-1c-admission-closure.php`

**Структура ответа API (расширенная):**
```json
{
  "success": true,
  "meta": {
    "weekNumber": 50,
    "weekStartUtc": "2025-12-08T00:00:00Z",
    "weekEndUtc": "2025-12-15T23:59:59Z"
  },
  "data": {
    "newTickets": 14,
    "closedTickets": 34,
    "closedTicketsCreatedThisWeek": 20,
    "closedTicketsCreatedOtherWeek": 14,
    "responsibleCreatedThisWeek": [
      {
        "id": 1006,
        "name": "ID 1006",
        "count": 12,
        "tickets": [ ... ]
      },
      {
        "id": 994,
        "name": "ID 994",
        "count": 8,
        "tickets": [ ... ]
      }
    ],
    "responsibleCreatedOtherWeek": [
      {
        "id": 1006,
        "name": "ID 1006",
        "count": 6,
        "tickets": [ ... ]
      },
      {
        "id": 993,
        "name": "ID 993",
        "count": 8,
        "tickets": [ ... ]
      }
    ],
    "carryoverTickets": 25,
    "carryoverTicketsCreatedThisWeek": 10,
    "carryoverTicketsCreatedOtherWeek": 15,
    "series": { ... },
    "stages": [ ... ],
    "responsible": [ ... ]
  }
}
```

**Изменения:**

1. **Добавить подсчёт двух новых метрик** в цикле обработки тикетов (после строки 542, где считается `closedTickets`):

```php
// Закрытые за неделю
if ($stageId && in_array($stageId, $closingStages, true) && isInRange($movedTime, $weekStart, $weekEnd)) {
    $closedCount++;
    
    // НОВОЕ: Разбивка по критерию создания
    $createdInThisWeek = isInRange($createdTime, $weekStart, $weekEnd);
    
    // Нормализация assignedById (как для закрытых тикетов)
    $responsibleId = $assignedRaw;
    if (is_array($responsibleId)) {
        $responsibleId = $responsibleId['id'] ?? $responsibleId['ID'] ?? $responsibleId['value'] ?? null;
    }
    $responsibleId = $responsibleId ? (int)$responsibleId : null;
    $responsibleKey = ($responsibleId === null || $responsibleId === $keeperId) ? 'unassigned' : (string)$responsibleId;
    
    if ($createdInThisWeek) {
        $closedTicketsCreatedThisWeek++;
        
        // Агрегация по сотрудникам для категории "созданные этой неделей"
        if (!isset($responsibleCreatedThisWeekAgg[$responsibleKey])) {
            $responsibleCreatedThisWeekAgg[$responsibleKey] = [
                'id' => $responsibleId,
                'name' => ($responsibleId === null || $responsibleId === $keeperId) ? 'Не назначен' : ('ID ' . $responsibleId),
                'count' => 0,
                'tickets' => []
            ];
        }
        $responsibleCreatedThisWeekAgg[$responsibleKey]['count']++;
        
        // Если нужно включить тикеты, сохранить данные тикета
        if ($includeTickets) {
            $responsibleCreatedThisWeekAgg[$responsibleKey]['tickets'][] = [
                'id' => (int)$ticket['id'],
                'title' => $ticket['title'] ?? 'Без названия',
                'createdTime' => $ticket['createdTime'] ?? null,
                'movedTime' => $movedTime,
                'stageId' => $stageId,
                'assignedById' => $responsibleId
            ];
        }
    } else {
        $closedTicketsCreatedOtherWeek++;
        
        // Агрегация по сотрудникам для категории "созданные ранее"
        if (!isset($responsibleCreatedOtherWeekAgg[$responsibleKey])) {
            $responsibleCreatedOtherWeekAgg[$responsibleKey] = [
                'id' => $responsibleId,
                'name' => ($responsibleId === null || $responsibleId === $keeperId) ? 'Не назначен' : ('ID ' . $responsibleId),
                'count' => 0,
                'tickets' => []
            ];
        }
        $responsibleCreatedOtherWeekAgg[$responsibleKey]['count']++;
        
        // Если нужно включить тикеты, сохранить данные тикета
        if ($includeTickets) {
            $responsibleCreatedOtherWeekAgg[$responsibleKey]['tickets'][] = [
                'id' => (int)$ticket['id'],
                'title' => $ticket['title'] ?? 'Без названия',
                'createdTime' => $ticket['createdTime'] ?? null,
                'movedTime' => $movedTime,
                'stageId' => $stageId,
                'assignedById' => $responsibleId
            ];
        }
    }
    
    // ... остальная логика агрегации по стадиям и ответственным (для общего списка)
}
```

2. **Добавить новые поля в ответ API:**

```php
$response = [
    'success' => true,
    'meta' => $meta,
    'data' => [
        'newTickets' => $newCount,
        'closedTickets' => $closedCount,
        'closedTicketsCreatedThisWeek' => $closedTicketsCreatedThisWeek,  // НОВОЕ
        'closedTicketsCreatedOtherWeek' => $closedTicketsCreatedOtherWeek, // НОВОЕ
        'responsibleCreatedThisWeek' => array_values($responsibleCreatedThisWeekAgg), // НОВОЕ
        'responsibleCreatedOtherWeek' => array_values($responsibleCreatedOtherWeekAgg), // НОВОЕ
        'series' => $series,
        'stages' => $stages,
        'responsible' => array_values($responsibleAgg), // Общий список (для обратной совместимости)
        // ... остальные поля
    ]
];
```

3. **Инициализация переменных:**

```php
// В начале функции, где инициализируются счётчики
$closedTicketsCreatedThisWeek = 0;
$closedTicketsCreatedOtherWeek = 0;
$responsibleCreatedThisWeekAgg = []; // НОВОЕ: агрегация по сотрудникам для категории "созданные этой неделей"
$responsibleCreatedOtherWeekAgg = []; // НОВОЕ: агрегация по сотрудникам для категории "созданные ранее"
```

### Фронтенд (Vue.js)

**Файл:** `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`

**Изменения:**

1. **Обновить секцию summary для отображения трёх цифр с визуальным разделением:**

```vue
<section class="ac-chart__summary">
  <!-- Существующие карточки: Новые, Переходящие, Закрытия по стадиям -->
  <div class="summary-card summary-card--new" @click="handleSummaryClick('new')">
    <div class="summary-card__label">Новые за неделю</div>
    <div class="summary-card__value">{{ data.newTickets ?? 0 }}</div>
  </div>
  
  <div class="summary-card summary-card--carryover" @click="handleSummaryClick('carryover')">
    <div class="summary-card__label">Переходящие</div>
    <div class="summary-card__value">{{ data.carryoverTickets ?? 0 }}</div>
  </div>
  
  <!-- НОВОЕ: Три цифры для закрытых тикетов с визуальным разделением -->
  <div class="summary-card summary-card--closed-breakdown">
    <div class="summary-card__header">
      <div class="summary-card__label">Закрытые за неделю</div>
      <div class="summary-card__value-main">{{ data.closedTickets ?? 0 }}</div>
      <div class="summary-card__description">
        Всего закрытых тикетов за выбранную неделю
      </div>
    </div>
    
    <div class="summary-card__divider"></div>
    
    <div class="summary-card__breakdown">
      <div class="breakdown-section breakdown-section--this-week">
        <div class="breakdown-section__header">
          <div class="breakdown-section__icon">✓</div>
          <div class="breakdown-section__label">Созданы этой неделей</div>
        </div>
        <div class="breakdown-section__value">{{ data.closedTicketsCreatedThisWeek ?? 0 }}</div>
        <div class="breakdown-section__description">
          Тикеты, которые были закрыты на этой неделе и созданы в эту же неделю
        </div>
      </div>
      
      <div class="breakdown-section__separator"></div>
      
      <div class="breakdown-section breakdown-section--other-week">
        <div class="breakdown-section__header">
          <div class="breakdown-section__icon">↻</div>
          <div class="breakdown-section__label">Созданы другой неделей</div>
        </div>
        <div class="breakdown-section__value">{{ data.closedTicketsCreatedOtherWeek ?? 0 }}</div>
        <div class="breakdown-section__description">
          Тикеты, которые были закрыты на этой неделе, но созданы в предыдущие недели
        </div>
      </div>
    </div>
  </div>
  
  <div class="summary-card summary-card--stages">
    <div class="summary-card__label">Закрытия по стадиям</div>
    <div class="summary-card__tags">
      <!-- ... существующий код ... -->
    </div>
  </div>
</section>
```

2. **Обновить props компонента:**

```javascript
const props = defineProps({
  meta: {
    type: Object,
    default: null
  },
  data: {
    type: Object,
    default: () => ({
      newTickets: 0,
      closedTickets: 0,
      closedTicketsCreatedThisWeek: 0,  // НОВОЕ
      closedTicketsCreatedOtherWeek: 0, // НОВОЕ
      series: { new: [0], closed: [0] },
      stages: [],
      responsible: []
    })
  }
});
```

3. **Добавить стили для новых элементов с визуальным разделением:**

```css
/* Карточка с разбивкой закрытых тикетов */
.summary-card--closed-breakdown {
  grid-column: span 2; /* Занимает две колонки для лучшей читаемости */
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* Заголовок карточки с основной цифрой */
.summary-card__header {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding-bottom: 16px;
}

.summary-card__value-main {
  font-size: 36px;
  font-weight: 700;
  color: var(--b24-primary, #007bff);
  line-height: 1.2;
}

.summary-card__description {
  font-size: 12px;
  color: var(--b24-text-secondary, #6b7280);
  line-height: 1.4;
}

/* Визуальный разделитель */
.summary-card__divider {
  height: 1px;
  background: linear-gradient(
    to right,
    transparent,
    var(--b24-border-light, #e5e7eb) 20%,
    var(--b24-border-light, #e5e7eb) 80%,
    transparent
  );
  margin: 0 -16px; /* Учитываем padding карточки */
}

/* Контейнер для разбивки */
.summary-card__breakdown {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  gap: 16px;
  align-items: start;
}

/* Секция разбивки */
.breakdown-section {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 12px;
  border-radius: var(--radius-md, 8px);
  background: var(--b24-bg, #f9fafb);
  transition: background-color 0.2s ease;
}

.breakdown-section:hover {
  background: var(--b24-bg-hover, #f3f4f6);
}

.breakdown-section__header {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 4px;
}

.breakdown-section__icon {
  font-size: 18px;
  line-height: 1;
}

.breakdown-section--this-week .breakdown-section__icon {
  color: var(--b24-success, #28a745);
}

.breakdown-section--other-week .breakdown-section__icon {
  color: var(--b24-warning, #ffc107);
}

.breakdown-section__label {
  font-size: 14px;
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.breakdown-section__value {
  font-size: 28px;
  font-weight: 700;
  color: var(--b24-text-primary, #111827);
  line-height: 1.2;
}

.breakdown-section--this-week .breakdown-section__value {
  color: var(--b24-success, #28a745);
}

.breakdown-section--other-week .breakdown-section__value {
  color: var(--b24-warning, #ffc107);
}

.breakdown-section__description {
  font-size: 12px;
  color: var(--b24-text-secondary, #6b7280);
  line-height: 1.4;
  margin-top: 4px;
}

/* Разделитель между секциями */
.breakdown-section__separator {
  width: 1px;
  background: var(--b24-border-light, #e5e7eb);
  margin: 0 8px;
  align-self: stretch;
}

/* Адаптивность для мобильных устройств */
@media (max-width: 768px) {
  .summary-card--closed-breakdown {
    grid-column: span 1; /* На мобильных занимает одну колонку */
  }
  
  .summary-card__breakdown {
    grid-template-columns: 1fr;
    gap: 12px;
  }
  
  .breakdown-section__separator {
    display: none; /* Скрываем разделитель на мобильных */
  }
  
  .summary-card__value-main {
    font-size: 32px;
  }
  
  .breakdown-section__value {
    font-size: 24px;
  }
}
```

---

## 🚀 Этапы реализации

### Этап 1: Расширение API (бэкенд)

**Цель:** Добавить подсчёт двух новых метрик в API.

**Задачи:**
1. Инициализировать переменные `$closedTicketsCreatedThisWeek` и `$closedTicketsCreatedOtherWeek`
2. Инициализировать массивы для агрегации по сотрудникам для каждой категории:
   - `$responsibleCreatedThisWeekAgg[]` — сотрудники для категории "созданные этой неделей"
   - `$responsibleCreatedOtherWeekAgg[]` — сотрудники для категории "созданные ранее"
3. В цикле обработки тикетов добавить проверку `createdTime` для закрытых тикетов
4. Разделить закрытые тикеты на две категории по критерию создания
5. Агрегировать тикеты по сотрудникам для каждой категории отдельно
6. Добавить новые поля в ответ API:
   - `closedTicketsCreatedThisWeek` — количество
   - `closedTicketsCreatedOtherWeek` — количество
   - `responsibleCreatedThisWeek[]` — список сотрудников с количеством тикетов
   - `responsibleCreatedOtherWeek[]` — список сотрудников с количеством тикетов
7. Проверить, что `closedTickets === closedTicketsCreatedThisWeek + closedTicketsCreatedOtherWeek`

**Критерии приёмки этапа 1:**
- [ ] API возвращает `closedTicketsCreatedThisWeek` и `closedTicketsCreatedOtherWeek`
- [ ] API возвращает `responsibleCreatedThisWeek[]` и `responsibleCreatedOtherWeek[]`
- [ ] Сумма двух новых метрик равна `closedTickets`
- [ ] Сумма тикетов в `responsibleCreatedThisWeek` равна `closedTicketsCreatedThisWeek`
- [ ] Сумма тикетов в `responsibleCreatedOtherWeek` равна `closedTicketsCreatedOtherWeek`
- [ ] Логика фильтрации работает корректно
- [ ] Тесты проходят успешно

---

### Этап 2: Обновление фронтенда (Vue.js)

**Цель:** Отобразить три цифры над графиками с визуальным разделением и пояснениями.

**Визуальная структура:**
```
┌─────────────────────────────────────────┐
│ Закрытые за неделю                      │
│                                         │
│            [Основная цифра]             │
│                                         │
│  Всего закрытых тикетов за выбранную   │
│  неделю                                 │
│                                         │
│ ─────────────────────────────────────── │
│                                         │
│ ┌──────────────┐    ┌──────────────┐  │
│ │ ✓ Созданы    │    │ ↻ Созданы    │  │
│ │   этой       │    │   другой     │  │
│ │   неделей    │    │   неделей    │  │
│ │              │    │              │  │
│ │   [Цифра]    │    │   [Цифра]    │  │
│ │              │    │              │  │
│ │ Тикеты,      │    │ Тикеты,      │  │
│ │ которые были │    │ которые были │  │
│ │ закрыты...   │    │ закрыты...   │  │
│ └──────────────┘    └──────────────┘  │
└─────────────────────────────────────────┘
```

**Задачи:**
1. Обновить props компонента `GraphAdmissionClosureChart.vue` для поддержки новых полей
2. Добавить разметку для отображения трёх цифр с визуальным разделением
3. Добавить пояснения к каждой цифре (описания)
4. Добавить стили для новых элементов с разделителями и иконками
5. Обеспечить адаптивность для мобильных устройств
6. Протестировать отображение на разных размерах экрана

**Критерии приёмки этапа 2:**
- [ ] Три цифры отображаются над графиками в одной карточке
- [ ] Первая цифра (`closedTickets`) отображается крупно в заголовке карточки
- [ ] Под первой цифрой есть пояснение "Всего закрытых тикетов за выбранную неделю"
- [ ] Визуальный разделитель отделяет основную цифру от разбивки
- [ ] Вторая цифра (`closedTicketsCreatedThisWeek`) отображается в отдельной секции с иконкой ✓
- [ ] Под второй цифрой есть пояснение "Тикеты, которые были закрыты на этой неделе и созданы в эту же неделю"
- [ ] Третья цифра (`closedTicketsCreatedOtherWeek`) отображается в отдельной секции с иконкой ↻
- [ ] Под третьей цифрой есть пояснение "Тикеты, которые были закрыты на этой неделе, но созданы в предыдущие недели"
- [ ] Визуальный разделитель между двумя секциями разбивки
- [ ] Сумма второй и третьей цифры равна первой
- [ ] Стили соответствуют дизайну модуля (цвета, отступы, закругления)
- [ ] Адаптивность работает корректно (на мобильных секции располагаются вертикально)
- [ ] Иконки и цвета помогают визуально различать секции

---

### Этап 3: Обновление попапа «Закрытые за неделю» (Vue.js)

**Цель:** Добавить систему вкладок в попап `ResponsibleModal.vue` с двумя способами навигации: "По категориям" и "По сотрудникам".

**Структура попапа (обновлённая):**

**Вкладка "По категориям":**
```
Уровень 0: Две категории закрытых тикетов
├── Тикеты закрытые этой неделью и созданные этой неделью [X тикетов]
└── Тикеты закрытые этой неделью и созданные ранее [Y тикетов]

Уровень 1: Список сотрудников выбранной категории
├── Иванов Иван [Z тикетов]
├── Петров Пётр [W тикетов]
└── ...

Уровень 2: Список тикетов сотрудника
├── [TicketCard] Тикет #12345
├── [TicketCard] Тикет #12346
└── ...
```

**Вкладка "По сотрудникам":**
```
Уровень 0: Список всех сотрудников (объединённый)
├── Иванов Иван [A тикетов всего]
├── Петров Пётр [B тикетов всего]
└── ...

Уровень 1: Градация тикетов выбранного сотрудника
├── На этой неделе [C тикетов] (тикеты, созданные этой неделей)
└── На другой неделе [D тикетов] (тикеты, созданные другой неделей)

Уровень 2: Список тикетов сотрудника выбранной градации
├── [TicketCard] Тикет #12345
├── [TicketCard] Тикет #12346
└── ...
```

**Задачи:**
1. Добавить систему вкладок в попап:
   - Вкладка "По категориям" (активна по умолчанию)
   - Вкладка "По сотрудникам"
   - Переключение между вкладками без потери состояния
2. Реализовать вкладку "По категориям":
   - Уровень 0: Две категории закрытых тикетов с количеством
   - Уровень 1: Список сотрудников выбранной категории
   - Уровень 2: Список тикетов сотрудника
3. Реализовать вкладку "По сотрудникам":
   - Уровень 0: Объединённый список всех сотрудников (из `responsibleCreatedThisWeek` и `responsibleCreatedOtherWeek`)
     - Для каждого сотрудника показывать общее количество тикетов (сумма из обеих категорий)
   - Уровень 1: Градация тикетов выбранного сотрудника:
     - "На этой неделе" — тикеты, созданные этой неделей (из `responsibleCreatedThisWeek`)
     - "На другой неделе" — тикеты, созданные другой неделей (из `responsibleCreatedOtherWeek`)
   - Уровень 2: Список тикетов сотрудника выбранной градации
4. Обновить логику перехода между уровнями для обеих вкладок
5. Обновить загрузку данных:
   - Для вкладки "По категориям": использовать данные из выбранной категории
   - Для вкладки "По сотрудникам": объединять данные из обеих категорий для сотрудника
6. Обновить заголовки попапа для каждого уровня и вкладки
7. Добавить анимации переходов между уровнями и вкладками

**Структура данных для уровня 0:**
```javascript
const categories = [
  {
    id: 'created-this-week',
    label: 'Тикеты закрытые этой неделью и созданные этой неделью',
    count: props.data.closedTicketsCreatedThisWeek ?? 0,
    responsible: props.data.responsibleCreatedThisWeek ?? []
  },
  {
    id: 'created-other-week',
    label: 'Тикеты закрытые этой неделью и созданные ранее',
    count: props.data.closedTicketsCreatedOtherWeek ?? 0,
    responsible: props.data.responsibleCreatedOtherWeek ?? []
  }
];
```

**Обновление компонента ResponsibleModal.vue:**
```vue
<template>
  <div v-if="isVisible" class="modal-backdrop">
    <div class="modal">
      <Transition name="level" mode="out-in">
        <!-- Уровень 0: Две категории закрытых тикетов -->
        <div v-if="popupLevel === 0" key="level-0" class="level-0">
          <header class="modal__header">
            <h3 class="modal__title">Закрытые за неделю</h3>
          </header>
          
          <section class="modal__body">
            <ul class="categories-list">
              <li
                v-for="category in categories"
                :key="category.id"
                class="categories-list__item"
                :class="{ 'categories-list__item--clickable': category.count > 0 }"
                @click="handleCategoryClick(category)"
              >
                <div class="category-item">
                  <div class="category-item__icon">
                    <span v-if="category.id === 'created-this-week'">✓</span>
                    <span v-else>↻</span>
                  </div>
                  <div class="category-item__content">
                    <div class="category-item__label">{{ category.label }}</div>
                    <div class="category-item__count">{{ category.count }} тикетов</div>
                  </div>
                  <span v-if="category.count > 0" class="category-item__arrow">→</span>
                </div>
              </li>
            </ul>
          </section>
          
          <footer class="modal__footer">
            <button class="btn" @click="$emit('close')">Закрыть</button>
          </footer>
        </div>
        
        <!-- Уровень 1: Список сотрудников выбранной категории -->
        <div v-else-if="popupLevel === 1" key="level-1" class="level-1">
          <!-- ... существующий код уровня 1 ... -->
        </div>
        
        <!-- Уровень 2: Список тикетов сотрудника -->
        <div v-else-if="popupLevel === 2" key="level-2" class="level-2">
          <!-- ... существующий код уровня 2 ... -->
        </div>
      </Transition>
    </div>
  </div>
</template>

<script setup>
// ... существующий код ...

const popupLevel = ref(0); // Изменить начальное значение с 1 на 0
const selectedCategory = ref(null); // Новое состояние для выбранной категории

const categories = computed(() => [
  {
    id: 'created-this-week',
    label: 'Тикеты закрытые этой неделью и созданные этой неделью',
    count: props.data.closedTicketsCreatedThisWeek ?? 0,
    responsible: props.data.responsibleCreatedThisWeek ?? []
  },
  {
    id: 'created-other-week',
    label: 'Тикеты закрытые этой неделью и созданные ранее',
    count: props.data.closedTicketsCreatedOtherWeek ?? 0,
    responsible: props.data.responsibleCreatedOtherWeek ?? []
  }
]);

/**
 * Обработка клика на категорию
 * Переход на уровень 1 и загрузка сотрудников выбранной категории
 */
function handleCategoryClick(category) {
  if (!category || category.count === 0) {
    return;
  }
  
  selectedCategory.value = category;
  popupLevel.value = 1;
  
  // Обогатить именами сотрудников выбранной категории
  enrichResponsibleWithNames(category.responsible);
}

/**
 * Обновление функции goBack для поддержки уровня 0
 */
function goBack() {
  if (popupLevel.value === 2) {
    // Возврат с уровня 2 на уровень 1
    popupLevel.value = 1;
    selectedEmployee.value = null;
    tickets.value = [];
    error.value = null;
  } else if (popupLevel.value === 1) {
    // Возврат с уровня 1 на уровень 0
    popupLevel.value = 0;
    selectedCategory.value = null;
    enrichedResponsible.value = [];
  }
}

// Обновить watch для сброса состояния
watch(() => props.isVisible, (newValue) => {
  if (!newValue) {
    popupLevel.value = 0; // Изменить с 1 на 0
    selectedCategory.value = null;
    selectedEmployee.value = null;
    tickets.value = [];
    error.value = null;
    enrichedResponsible.value = [];
  }
});
</script>
```

**Критерии приёмки этапа 3:**
- [ ] Попап открывается с двумя вкладками: "По категориям" и "По сотрудникам"
- [ ] Вкладка "По категориям" работает как раньше:
  - [ ] Уровень 0: Две категории закрытых тикетов с количеством
  - [ ] При клике на категорию открывается уровень 1 со списком сотрудников этой категории
  - [ ] При клике на сотрудника открывается уровень 2 со списком тикетов
  - [ ] Кнопка "Назад" работает корректно на всех уровнях
- [ ] Вкладка "По сотрудникам" работает корректно:
  - [ ] Уровень 0: Показывается объединённый список всех сотрудников с общим количеством тикетов
  - [ ] При клике на сотрудника открывается уровень 1 с градацией:
    - [ ] "На этой неделе" — показывает количество тикетов, созданных этой неделей
    - [ ] "На другой неделе" — показывает количество тикетов, созданных другой неделей
  - [ ] При клике на градацию открывается уровень 2 со списком тикетов выбранной градации
  - [ ] Кнопка "Назад" работает корректно на всех уровнях
- [ ] Переключение между вкладками работает без потери состояния
- [ ] Заголовки попапа обновляются для каждого уровня и вкладки
- [ ] Анимации переходов работают плавно
- [ ] Имена сотрудников загружаются корректно
- [ ] При отсутствии тикетов элементы не кликабельны

---

## 📝 Модули и компоненты

### Бэкенд (PHP)

- `api/graph-1c-admission-closure.php` — расширение логики подсчёта закрытых тикетов

### Фронтенд (Vue.js)

- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue` — обновление секции summary
- `vue-app/src/components/graph-admission-closure/ResponsibleModal.vue` — добавление уровня 0 с категориями
- `vue-app/src/services/admissionClosureService.js` — возможно, обновление типов данных (если используется TypeScript)

---

## 🔗 API-методы Bitrix24

**Используемые методы:**
- `crm.item.list` — получение тикетов (уже используется)
  - Документация: https://context7.com/bitrix24/rest/crm.item.list

**Поля тикетов:**
- `createdTime` — дата создания тикета (используется для фильтрации)
- `movedTime` — дата перемещения в стадию (используется для определения закрытия)
- `stageId` — ID стадии (используется для определения закрывающих стадий)

---

## ✅ Критерии приёмки

### Общие критерии

- [ ] API возвращает `closedTicketsCreatedThisWeek` и `closedTicketsCreatedOtherWeek`
- [ ] API возвращает `responsibleCreatedThisWeek[]` и `responsibleCreatedOtherWeek[]`
- [ ] Сумма двух новых метрик равна `closedTickets`
- [ ] Три цифры отображаются над графиками
- [ ] Первая цифра показывает все закрытые тикеты (как есть)
- [ ] Вторая цифра показывает закрытые тикеты, созданные этой неделей
- [ ] Третья цифра показывает закрытые тикеты, созданные другой неделей
- [ ] Попап «Закрытые за неделю» открывается с двумя вкладками
- [ ] Вкладка "По категориям": работает как описано выше
- [ ] Вкладка "По сотрудникам": работает как описано выше
- [ ] Переключение между вкладками работает корректно
- [ ] Кнопка "Назад" работает корректно на всех уровнях для обеих вкладок
- [ ] Логика фильтрации работает корректно для всех случаев
- [ ] Стили соответствуют дизайну модуля
- [ ] Адаптивность работает на всех устройствах
- [ ] Код соответствует стандартам проекта

### Функциональные проверки

- [ ] При загрузке модуля отображаются три цифры
- [ ] Значения цифр соответствуют данным из API
- [ ] При изменении недели (если будет добавлено) значения обновляются
- [ ] При отсутствии данных отображается 0
- [ ] Обработка ошибок работает корректно

---

## 🧪 Тестирование

### Тестовые сценарии

1. **Сценарий 1: Все тикеты закрыты в ту же неделю, что и созданы**
   - Ожидаемый результат: `closedTicketsCreatedThisWeek === closedTickets`, `closedTicketsCreatedOtherWeek === 0`

2. **Сценарий 2: Все тикеты закрыты в неделю, но созданы в другие недели**
   - Ожидаемый результат: `closedTicketsCreatedThisWeek === 0`, `closedTicketsCreatedOtherWeek === closedTickets`

3. **Сценарий 3: Смешанный случай**
   - Ожидаемый результат: `closedTickets === closedTicketsCreatedThisWeek + closedTicketsCreatedOtherWeek`

4. **Сценарий 4: Нет закрытых тикетов**
   - Ожидаемый результат: все три цифры равны 0

### Проверка данных

- Проверить корректность фильтрации по `createdTime` и `movedTime`
- Проверить, что учитываются только закрывающие стадии
- Проверить граничные случаи (тикеты на границе недели)

---

## 📚 Связанные документы

- `DOCS/TASKS/TASK-041-graph-1c-admission-closure.md` — основная задача модуля
- `DOCS/TASKS/TASK-042-popup-level2-tickets-list.md` — попап второго уровня
- `DOCS/TASKS/TASK-043-new-tickets-stages-popup.md` — попап по стадиям для новых тикетов
- `api/graph-1c-admission-closure.php` — API endpoint
- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue` — компонент графика
- `vue-app/src/components/graph-admission-closure/ResponsibleModal.vue` — попап «Закрытые за неделю»

---

## 📝 История правок

- **2025-12-16 18:55 (UTC+3, Брест):** Создана задача TASK-047
  - Определены требования к разбивке закрытых тикетов
  - Описаны изменения в API и фронтенде
  - Добавлены этапы реализации и критерии приёмки
- **2025-12-16 19:00 (UTC+3, Брест):** Обновлена задача
  - Добавлен вариант 1 с визуальным разделением и пояснениями для трёх цифр
  - Добавлен этап 3: обновление попапа «Закрытые за неделю» с новым уровнем 0
  - Расширен API для возврата данных по категориям (`responsibleCreatedThisWeek[]`, `responsibleCreatedOtherWeek[]`)
  - Обновлены критерии приёмки для всех этапов
- **2025-12-16 20:30 (UTC+3, Брест):** Добавлено требование по вкладкам в попапе
  - Добавлена вкладка "По категориям" (существующая функциональность)
  - Добавлена вкладка "По сотрудникам" с новой навигацией:
    - Уровень 0: Список всех сотрудников (объединённый)
    - Уровень 1: Градация тикетов сотрудника ("На этой неделе" / "На другой неделе")
    - Уровень 2: Список тикетов выбранной градации
  - Обновлены критерии приёмки для этапа 3

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer) + Backend (PHP)

