# TASK-058-06: Frontend — исправление перекрытия подсказок на графике "Новые и закрытые тикеты"

**Дата создания:** 2025-12-18 09:49 (UTC+3, Брест)  
**Статус:** 📝 Черновик  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-058: Улучшение модуля "График приема и закрытий сектора 1С" — режим 3 месяца](./TASK-058-enrichment-3-months-mode.md)  
**Этап:** 6 из 6  
**Зависимости:** TASK-058-04 (Цифры на точках графика уже реализованы)

---

## 📋 Описание

Исправить проблему перекрытия подсказок (data labels) с цифрами на графике "Новые и закрытые тикеты" в компоненте `LineChartMonths.vue`. Когда две линии пересекаются в одной точке (например, обе имеют одинаковое значение в одном месяце), подсказки с цифрами перекрывают друг друга, что ухудшает читаемость графика.

---

## 🎯 Контекст

### Текущее состояние

**Файл:** `vue-app/src/components/graph-admission-closure/LineChartMonths.vue`

**Текущая конфигурация datalabels:**
- Новые тикеты: `anchor: 'end', align: 'top'` (справа от точки, сверху)
- Закрытые тикеты: `anchor: 'start', align: 'top'` (слева от точки, сверху)

**Проблема:**
- Когда две линии пересекаются в одной точке (например, обе имеют значение 79 в Октябре), подсказки перекрываются
- Подсказки расположены слишком близко друг к другу
- Читаемость графика снижается

**Пример проблемы:**
```
Октябрь: Новые = 79, Закрытые = 79
→ Обе подсказки "79" перекрываются в одной точке
```

### Требуемое состояние

- Подсказки не должны перекрываться, даже когда линии пересекаются
- Подсказки должны быть хорошо видны и читаемы
- Решение должно работать для всех случаев пересечения линий

---

## 🔍 Детализация требований

### 1. Анализ проблемы

**Текущая ситуация:**
- Две линии на графике: "Новые тикеты" (синяя) и "Закрытые тикеты" (зеленая)
- Три точки на каждой линии (по одной на месяц)
- Когда значения совпадают или близки, подсказки перекрываются

**Варианты решения:**

1. **Увеличение offset для разных датасетов:**
   - Новые тикеты: `offset: 8` (больше смещение вверх)
   - Закрытые тикеты: `offset: -8` (смещение вниз)
   - Разделение по вертикали

2. **Использование разных align для разных датасетов:**
   - Новые тикеты: `align: 'top'`
   - Закрытые тикеты: `align: 'bottom'`
   - Размещение подсказок сверху и снизу от точки

3. **Динамическое позиционирование на основе значений:**
   - Если значения близки (разница < 5), использовать разные позиции
   - Если значения далеки, использовать стандартные позиции

4. **Комбинированная подсказка при совпадении:**
   - Если значения совпадают, показывать одну подсказку с двумя значениями
   - Формат: "79 / 79" или "Новые: 79, Закрытые: 79"

### 2. Рекомендуемое решение

**Вариант: Комбинированный подход с динамическим позиционированием**

1. **Базовое позиционирование:**
   - Новые тикеты: `anchor: 'end', align: 'top', offset: 8`
   - Закрытые тикеты: `anchor: 'start', align: 'bottom', offset: 8`
   - Разделение по вертикали и горизонтали

2. **Динамическая проверка пересечения:**
   - Перед отображением подсказки проверять, не совпадают ли значения
   - Если значения совпадают или очень близки (разница < 3), использовать специальное позиционирование

3. **Специальная обработка совпадений:**
   - Если значения совпадают, показывать подсказки с большим смещением
   - Или использовать комбинированную подсказку

### 3. Техническая реализация

**Файл:** `vue-app/src/components/graph-admission-closure/LineChartMonths.vue`

**Изменения в конфигурации datalabels:**

```javascript
// В newClosedChartData для датасета "Новые тикеты"
datalabels: {
  anchor: 'end',
  align: 'top',
  offset: 8,  // Увеличенное смещение
  color: chartColors.primary,
  // Динамическое позиционирование при совпадении
  display: function(context) {
    const value = context.dataset.data[context.dataIndex];
    if (value === null || value === undefined || isNaN(value)) {
      return false;
    }
    
    // Проверка на совпадение с другим датасетом
    const otherDataset = context.chart.data.datasets.find(d => d.label === 'Закрытые тикеты');
    if (otherDataset) {
      const otherValue = otherDataset.data[context.dataIndex];
      // Если значения совпадают или очень близки, увеличиваем offset
      if (otherValue !== null && Math.abs(value - otherValue) < 3) {
        context.offset = 12; // Большее смещение при совпадении
      }
    }
    
    return true;
  }
}

// В newClosedChartData для датасета "Закрытые тикеты"
datalabels: {
  anchor: 'start',
  align: 'bottom',  // Изменено с 'top' на 'bottom'
  offset: 8,  // Увеличенное смещение
  color: chartColors.success,
  // Аналогичная логика для закрытых тикетов
  display: function(context) {
    const value = context.dataset.data[context.dataIndex];
    if (value === null || value === undefined || isNaN(value)) {
      return false;
    }
    
    // Проверка на совпадение с другим датасетом
    const otherDataset = context.chart.data.datasets.find(d => d.label === 'Новые тикеты');
    if (otherDataset) {
      const otherValue = otherDataset.data[context.dataIndex];
      // Если значения совпадают или очень близки, увеличиваем offset
      if (otherValue !== null && Math.abs(value - otherValue) < 3) {
        context.offset = 12; // Большее смещение при совпадении
      }
    }
    
    return true;
  }
}
```

**Альтернативный вариант (проще):**

```javascript
// Простое решение: разные позиции и увеличенные offset
datalabels: {
  anchor: 'end',
  align: 'top',
  offset: 10,  // Увеличенное смещение
  color: chartColors.primary
}

// Для закрытых тикетов
datalabels: {
  anchor: 'start',
  align: 'bottom',  // Снизу от точки
  offset: 10,  // Увеличенное смещение
  color: chartColors.success
}
```

---

## 🔧 Технические требования

### Конфигурация datalabels

**Требования:**
- Подсказки не должны перекрываться
- Подсказки должны быть хорошо видны на фоне графика
- Решение должно работать для всех случаев (совпадение, близкие значения, разные значения)

### Производительность

- Проверка на совпадение не должна замедлять отрисовку графика
- Использовать кеширование результатов проверки, если возможно

### Обратная совместимость

- Изменения не должны ломать существующий функционал
- График должен работать корректно даже если данные отсутствуют

---

## ✅ Критерии приёмки

- [ ] Подсказки не перекрываются, даже когда линии пересекаются в одной точке
- [ ] Подсказки хорошо видны и читаемы
- [ ] Решение работает для всех случаев:
  - [ ] Совпадающие значения (например, оба 79)
  - [ ] Близкие значения (разница < 5)
  - [ ] Разные значения (разница > 5)
- [ ] График отображается корректно на всех размерах экрана
- [ ] Производительность не ухудшилась
- [ ] Код протестирован с реальными данными

---

## 📚 Примеры реализации

### Пример 1: Простое решение с разными позициями

```javascript
const newClosedChartData = computed(() => {
  const months = props.data.newTicketsByMonth || [];
  const labels = months.map(m => m.monthName || `Месяц ${m.month}`);
  
  const newData = months.map(m => m.count || 0);
  const closedData = (props.data.closedTicketsByMonth || []).map(m => m.count || 0);
  
  return {
    labels,
    datasets: [
      {
        label: 'Новые тикеты',
        data: newData,
        borderColor: chartColors.primary,
        backgroundColor: 'rgba(0, 123, 255, 0.1)',
        tension: 0.4,
        fill: false,
        // TASK-058-06: Настройка datalabels для избежания перекрытия
        datalabels: {
          anchor: 'end',
          align: 'top',
          offset: 10,  // Увеличенное смещение
          color: chartColors.primary,
          font: {
            size: 12,
            weight: 'bold'
          },
          backgroundColor: 'rgba(255, 255, 255, 0.9)',
          borderColor: chartColors.primary,
          borderWidth: 1,
          borderRadius: 4,
          padding: 4
        }
      },
      {
        label: 'Закрытые тикеты',
        data: closedData,
        borderColor: chartColors.success,
        backgroundColor: 'rgba(40, 167, 69, 0.1)',
        tension: 0.4,
        fill: false,
        // TASK-058-06: Настройка datalabels для избежания перекрытия
        datalabels: {
          anchor: 'start',
          align: 'bottom',  // Снизу от точки, чтобы не перекрываться с новыми
          offset: 10,  // Увеличенное смещение
          color: chartColors.success,
          font: {
            size: 12,
            weight: 'bold'
          },
          backgroundColor: 'rgba(255, 255, 255, 0.9)',
          borderColor: chartColors.success,
          borderWidth: 1,
          borderRadius: 4,
          padding: 4
        }
      }
    ]
  };
});
```

### Пример 2: Динамическое позиционирование при совпадении

```javascript
// Более сложное решение с проверкой на совпадение
function getDatalabelsConfig(datasetIndex, datasets) {
  const isNewTickets = datasetIndex === 0;
  const baseConfig = {
    anchor: isNewTickets ? 'end' : 'start',
    align: isNewTickets ? 'top' : 'bottom',
    offset: 8,
    color: isNewTickets ? chartColors.primary : chartColors.success,
    font: {
      size: 12,
      weight: 'bold'
    },
    backgroundColor: 'rgba(255, 255, 255, 0.9)',
    borderColor: isNewTickets ? chartColors.primary : chartColors.success,
    borderWidth: 1,
    borderRadius: 4,
    padding: 4,
    display: function(context) {
      const value = context.dataset.data[context.dataIndex];
      if (value === null || value === undefined || isNaN(value)) {
        return false;
      }
      
      // Проверка на совпадение с другим датасетом
      const otherDataset = datasets.find((d, idx) => idx !== datasetIndex);
      if (otherDataset) {
        const otherValue = otherDataset.data[context.dataIndex];
        if (otherValue !== null && Math.abs(value - otherValue) < 3) {
          // Если значения совпадают или очень близки, увеличиваем offset
          context.offset = isNewTickets ? 12 : -12;
        }
      }
      
      return true;
    }
  };
  
  return baseConfig;
}

// Использование в newClosedChartData
const newClosedChartData = computed(() => {
  const months = props.data.newTicketsByMonth || [];
  const labels = months.map(m => m.monthName || `Месяц ${m.month}`);
  
  const newData = months.map(m => m.count || 0);
  const closedData = (props.data.closedTicketsByMonth || []).map(m => m.count || 0);
  
  const datasets = [
    {
      label: 'Новые тикеты',
      data: newData,
      borderColor: chartColors.primary,
      backgroundColor: 'rgba(0, 123, 255, 0.1)',
      tension: 0.4,
      fill: false
    },
    {
      label: 'Закрытые тикеты',
      data: closedData,
      borderColor: chartColors.success,
      backgroundColor: 'rgba(40, 167, 69, 0.1)',
      tension: 0.4,
      fill: false
    }
  ];
  
  // Добавляем конфигурацию datalabels для каждого датасета
  datasets[0].datalabels = getDatalabelsConfig(0, datasets);
  datasets[1].datalabels = getDatalabelsConfig(1, datasets);
  
  return {
    labels,
    datasets
  };
});
```

---

## 🔗 Зависимости

- **TASK-058-04:** Цифры на точках графика уже реализованы
- **chartjs-plugin-datalabels:** Плагин уже установлен

---

## 📝 История правок

- **2025-12-18 09:49 (UTC+3, Брест):** Создана задача для исправления перекрытия подсказок
  - Описана проблема перекрытия data labels
  - Предложены варианты решения
  - Добавлены примеры реализации

---

## ⚠️ Примечания

- **Важно:** Решение должно работать для всех случаев пересечения линий
- **UX:** Подсказки должны быть хорошо видны и не мешать восприятию графика
- **Производительность:** Проверка на совпадение не должна замедлять отрисовку

## 🐛 Типичные ошибки и их решения

### Ошибка 1: Подсказки все еще перекрываются

**Причина:** Недостаточное смещение или неправильное позиционирование

**Решение:**
- Увеличить `offset` до 10-12 пикселей
- Использовать разные `align` для разных датасетов (top/bottom)
- Добавить проверку на совпадение значений

### Ошибка 2: Подсказки слишком далеко от точек

**Причина:** Слишком большое смещение

**Решение:**
- Уменьшить `offset` до 8-10 пикселей
- Использовать динамическое позиционирование только при совпадении

### Ошибка 3: Подсказки не отображаются

**Причина:** Неправильная логика в `display` функции

**Решение:**
- Проверить, что функция `display` возвращает `true` для валидных значений
- Проверить, что значения не `null` или `undefined`

