# TASK-058-03: Проверка логики "Закрытые всего"

**Дата создания:** 2025-12-18 08:31 (UTC+3, Брест)  
**Статус:** 📝 Черновик  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Backend/Frontend Developer)  
**Родительская задача:** [TASK-058: Улучшение модуля "График приема и закрытий сектора 1С" — режим 3 месяца](./TASK-058-enrichment-3-months-mode.md)  
**Этап:** 3 из 5

---

## 📋 Описание

Проверить и уточнить логику расчета показателей "Закрытые всего" в режиме 3 месяца, особенно показателей "этой неделей" и "другой неделей". Убедиться, что логика работает корректно и соответствует бизнес-требованиям.

---

## 🎯 Контекст

### Текущее состояние

**Вопрос от заказчика:**
> Как именно считаются показатели "этой неделей" и "другой неделей" в месячном режиме?
> 
> ПРИМЕР:
> Октябрь общее число 80
> Этой неделей 60
> Другой неделей 20
> 
> Вторая и третья цифра это наверное Месяц?
> То есть Если в октября Закрыто 80 всего, то из этого числа 60 закрыто, как тикеты созданные этим же месяцем, а остальные 20, это закрытые тикеты числом Октября, но созданные месяцами ранее, так?

**Гипотеза:**
- "Этой неделей" = закрытые тикеты октября, которые были созданы в октябре
- "Другой неделей" = закрытые тикеты октября, которые были созданы в предыдущих месяцах

### Требуемое состояние

- Логика расчета проверена и задокументирована
- Показатели "этой неделей" и "другой неделей" работают корректно
- При необходимости — логика исправлена
- Документация обновлена

---

## 🔍 Детализация требований

### 1. Изучение текущей логики расчета

**Файлы для проверки:**
- `api/graph-1c-admission-closure.php` — Backend логика расчета
- `vue-app/src/components/graph-admission-closure/SummaryCardsMonths.vue` — Frontend отображение

**Задачи:**
1. Найти код, который рассчитывает показатели "этой неделей" и "другой неделей"
2. Понять логику расчета:
   - Как определяется "этой неделей"?
   - Как определяется "другой неделей"?
   - Это месячные данные или недельные?
3. Создать документацию текущей логики

**Места для проверки в Backend:**
- Строка ~850-900: расчет `closedTicketsByMonth`
- Логика разбивки закрытых тикетов по неделям внутри месяца
- Логика определения "создан этой неделей" vs "создан другой неделей"

### 2. Проверка логики с тестовыми данными

**Задачи:**
1. Создать тестовые данные:
   - Тикеты, созданные в октябре и закрытые в октябре
   - Тикеты, созданные в сентябре и закрытые в октябре
   - Тикеты, созданные в августе и закрытые в октябре
2. Проверить расчет показателей:
   - Общее число закрытых тикетов октября
   - "Этой неделей" — должно быть равно тикетам, созданным в октябре
   - "Другой неделей" — должно быть равно тикетам, созданным ранее
3. Сравнить результаты с ожидаемыми значениями

**Пример тестовых данных:**
```
Октябрь 2025:
- Тикет #1: создан 2025-10-05, закрыт 2025-10-15 → должен попасть в "этой неделей"
- Тикет #2: создан 2025-10-10, закрыт 2025-10-20 → должен попасть в "этой неделей"
- Тикет #3: создан 2025-09-20, закрыт 2025-10-10 → должен попасть в "другой неделей"
- Тикет #4: создан 2025-08-15, закрыт 2025-10-25 → должен попасть в "другой неделей"

Ожидаемый результат:
- Общее число: 4
- Этой неделей: 2 (тикеты #1, #2)
- Другой неделей: 2 (тикеты #3, #4)
```

### 3. Уточнение логики у заказчика

**Вопросы для уточнения:**
1. **"Этой неделей"** — это:
   - Закрытые тикеты месяца, созданные в этом же месяце?
   - Или закрытые тикеты месяца, созданные в определенной неделе месяца?
2. **"Другой неделей"** — это:
   - Закрытые тикеты месяца, созданные в предыдущих месяцах?
   - Или закрытые тикеты месяца, созданные в других неделях месяца?
3. **В месячном режиме** — это месячные данные или недельные?

**Гипотеза для подтверждения:**
- "Этой неделей" = закрытые тикеты октября, которые были созданы в октябре
- "Другой неделей" = закрытые тикеты октября, которые были созданы в предыдущих месяцах

### 4. Исправление логики (если необходимо)

**Задачи:**
1. Если логика не соответствует требованиям — исправить
2. Обновить код расчета показателей
3. Протестировать исправленную логику
4. Обновить документацию

**Возможные исправления:**
- Изменение логики определения "этой неделей" vs "другой неделей"
- Исправление фильтров по датам создания
- Исправление группировки данных

### 5. Обновление документации

**Задачи:**
1. Создать/обновить документацию логики расчета
2. Описать:
   - Как считаются показатели "этой неделей"
   - Как считаются показатели "другой неделей"
   - Примеры расчета
3. Добавить комментарии в код

---

## 🔧 Технические требования

### Логика расчета (текущая гипотеза)

**Для каждого месяца:**
1. Получить все тикеты, закрытые в этом месяце (`movedTime` в диапазоне месяца)
2. Разделить на две группы:
   - **"Этой неделей"**: тикеты, у которых `createdTime` находится в том же месяце, что и `movedTime`
   - **"Другой неделей"**: тикеты, у которых `createdTime` находится в предыдущих месяцах

**Псевдокод:**
```php
foreach ($closedTickets as $ticket) {
    $closedMonth = getMonthFromDate($ticket['movedTime']);
    $createdMonth = getMonthFromDate($ticket['createdTime']);
    
    if ($closedMonth === $createdMonth) {
        // "Этой неделей"
        $closedCreatedThisMonth++;
    } else {
        // "Другой неделей"
        $closedCreatedOtherMonth++;
    }
}
```

### Проверка корректности

**Критерии корректности:**
- Общее число закрытых тикетов = "этой неделей" + "другой неделей"
- "Этой неделей" ≤ общее число закрытых тикетов
- "Другой неделей" ≤ общее число закрытых тикетов

---

## ✅ Критерии приёмки

- [ ] Текущая логика расчета изучена и задокументирована
- [ ] Логика проверена с тестовыми данными
- [ ] Логика уточнена у заказчика
- [ ] Показатели "этой неделей" и "другой неделей" работают корректно
- [ ] Общее число = "этой неделей" + "другой неделей" (для каждого месяца)
- [ ] При необходимости — логика исправлена
- [ ] Документация обновлена
- [ ] Комментарии добавлены в код

---

## 📚 Примеры проверки

### Пример 1: Проверка логики с тестовыми данными

```php
// Тестовые данные
$testTickets = [
    [
        'id' => 1,
        'createdTime' => '2025-10-05T10:00:00Z',
        'movedTime' => '2025-10-15T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ],
    [
        'id' => 2,
        'createdTime' => '2025-10-10T10:00:00Z',
        'movedTime' => '2025-10-20T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ],
    [
        'id' => 3,
        'createdTime' => '2025-09-20T10:00:00Z',
        'movedTime' => '2025-10-10T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ],
    [
        'id' => 4,
        'createdTime' => '2025-08-15T10:00:00Z',
        'movedTime' => '2025-10-25T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ]
];

// Октябрь 2025
$octoberStart = new DateTimeImmutable('2025-10-01T00:00:00Z');
$octoberEnd = new DateTimeImmutable('2025-10-31T23:59:59Z');

$closedThisMonth = 0;  // "Этой неделей"
$closedOtherMonth = 0; // "Другой неделей"

foreach ($testTickets as $ticket) {
    $movedTime = new DateTimeImmutable($ticket['movedTime']);
    $createdTime = new DateTimeImmutable($ticket['createdTime']);
    
    // Тикет закрыт в октябре?
    if ($movedTime >= $octoberStart && $movedTime <= $octoberEnd) {
        // Создан в октябре?
        if ($createdTime >= $octoberStart && $createdTime <= $octoberEnd) {
            $closedThisMonth++;
        } else {
            $closedOtherMonth++;
        }
    }
}

// Проверка
assert($closedThisMonth === 2, "Этой неделей должно быть 2");
assert($closedOtherMonth === 2, "Другой неделей должно быть 2");
assert($closedThisMonth + $closedOtherMonth === 4, "Общее число должно быть 4");
```

### Пример 2: Документация логики

```php
/**
 * Рассчитывает показатели закрытых тикетов для месяца
 * 
 * TASK-058-03: Логика расчета "этой неделей" и "другой неделей"
 * 
 * "Этой неделей" = закрытые тикеты месяца, которые были созданы в этом же месяце
 * "Другой неделей" = закрытые тикеты месяца, которые были созданы в предыдущих месяцах
 * 
 * @param array $tickets Массив тикетов
 * @param DateTimeImmutable $monthStart Начало месяца
 * @param DateTimeImmutable $monthEnd Конец месяца
 * @return array ['total' => int, 'thisMonth' => int, 'otherMonth' => int]
 */
function calculateClosedTicketsForMonth(array $tickets, DateTimeImmutable $monthStart, DateTimeImmutable $monthEnd): array
{
    $total = 0;
    $thisMonth = 0;
    $otherMonth = 0;
    
    foreach ($tickets as $ticket) {
        $movedTime = new DateTimeImmutable($ticket['movedTime']);
        
        // Тикет закрыт в этом месяце?
        if ($movedTime >= $monthStart && $movedTime <= $monthEnd) {
            $total++;
            
            $createdTime = new DateTimeImmutable($ticket['createdTime']);
            
            // Создан в этом же месяце?
            if ($createdTime >= $monthStart && $createdTime <= $monthEnd) {
                $thisMonth++;
            } else {
                $otherMonth++;
            }
        }
    }
    
    return [
        'total' => $total,
        'thisMonth' => $thisMonth,
        'otherMonth' => $otherMonth
    ];
}
```

---

## 🔗 Зависимости

- **TASK-053-03:** Backend API для 3-месячного режима
- **TASK-053-04:** Summary-карточки для 3-месячного режима

---

## 📝 История правок

- **2025-12-18 08:31 (UTC+3, Брест):** Создан детализированный документ для этапа 3
  - Описана задача проверки логики
  - Добавлены вопросы для уточнения
  - Добавлены примеры проверки
  - Добавлены критерии приёмки

---

## ⚠️ Примечания

- **Важно:** Требуется уточнение логики у заказчика перед исправлениями
- **Тестирование:** Обязательно протестировать с реальными данными
- **Документация:** После уточнения логики — обновить документацию

