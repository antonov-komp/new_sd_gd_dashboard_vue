# TASK-058-03: Проверка логики "Закрытые всего"

**Дата создания:** 2025-12-18 08:31 (UTC+3, Брест)  
**Статус:** 📝 Черновик  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Backend/Frontend Developer)  
**Родительская задача:** [TASK-058: Улучшение модуля "График приема и закрытий сектора 1С" — режим 3 месяца](./TASK-058-enrichment-3-months-mode.md)  
**Этап:** 3 из 5

---

## 📋 Описание

Проверить и уточнить логику расчета показателей "Закрытые всего" в режиме 3 месяца, особенно показателей "этой неделей" и "другой неделей". Убедиться, что логика работает корректно и соответствует бизнес-требованиям.

---

## 🎯 Контекст

### Текущее состояние

**Вопрос от заказчика:**
> Как именно считаются показатели "этой неделей" и "другой неделей" в месячном режиме?
> 
> ПРИМЕР:
> Октябрь общее число 80
> Этой неделей 60
> Другой неделей 20
> 
> Вторая и третья цифра это наверное Месяц?
> То есть Если в октября Закрыто 80 всего, то из этого числа 60 закрыто, как тикеты созданные этим же месяцем, а остальные 20, это закрытые тикеты числом Октября, но созданные месяцами ранее, так?

**Гипотеза:**
- "Этой неделей" = закрытые тикеты октября, которые были созданы в октябре
- "Другой неделей" = закрытые тикеты октября, которые были созданы в предыдущих месяцах

### Требуемое состояние

- Логика расчета проверена и задокументирована
- Показатели "этой неделей" и "другой неделей" работают корректно
- При необходимости — логика исправлена
- Документация обновлена

---

## 🔍 Детализация требований

### 1. Изучение текущей логики расчета

**Файлы для проверки:**
- `api/graph-1c-admission-closure.php` — Backend логика расчета
- `vue-app/src/components/graph-admission-closure/SummaryCardsMonths.vue` — Frontend отображение

**Задачи:**
1. Найти код, который рассчитывает показатели "этой неделей" и "другой неделей"
2. Понять логику расчета:
   - Как определяется "этой неделей"?
   - Как определяется "другой неделей"?
   - Это месячные данные или недельные?
3. Создать документацию текущей логики

**Места для проверки в Backend:**
- Строка ~795-815: расчет `closedTicketsByMonth` с разбивкой на "этой неделей" и "другой неделей"
- Строка 808: проверка `isInRange($createdTime, $monthStart, $monthEnd)` — определяет "этой неделей"
- Строка 811: else-блок — определяет "другой неделей"
- Функция `isInRange()` (строка 200) — проверка попадания даты в диапазон

**Текущая логика (найдена в коде):**

**Строка 806-813:**
```php
if (isInRange($movedTime, $monthStart, $monthEnd)) {
    $monthClosedCount++;
    if (isInRange($createdTime, $monthStart, $monthEnd)) {
        $monthClosedCreatedThisWeek++;  // "Этой неделей" = создан в этом же месяце
    } else {
        $monthClosedCreatedOtherWeek++; // "Другой неделей" = создан в другом месяце
    }
}
```

**Вывод:** 
- **"Этой неделей"** = закрытые тикеты месяца, которые были **созданы в этом же месяце**
- **"Другой неделей"** = закрытые тикеты месяца, которые были **созданы в предыдущих месяцах**
- Это **месячные данные**, а не недельные (название "этой неделей" — историческое, но логика месячная)

**Важно:** Название "этой неделей" и "другой неделей" в месячном режиме может вводить в заблуждение. Фактически это "этим месяцем" и "другим месяцем".

### 2. Проверка логики с тестовыми данными

**Задачи:**
1. Создать тестовые данные:
   - Тикеты, созданные в октябре и закрытые в октябре
   - Тикеты, созданные в сентябре и закрытые в октябре
   - Тикеты, созданные в августе и закрытые в октябре
2. Проверить расчет показателей:
   - Общее число закрытых тикетов октября
   - "Этой неделей" — должно быть равно тикетам, созданным в октябре
   - "Другой неделей" — должно быть равно тикетам, созданным ранее
3. Сравнить результаты с ожидаемыми значениями
4. Проверить граничные случаи (тикеты на границе месяцев)

**Пример тестовых данных:**
```
Октябрь 2025 (месяц: 2025-10-01 00:00:00 UTC до 2025-10-31 23:59:59 UTC):
- Тикет #1: создан 2025-10-05, закрыт 2025-10-15 → должен попасть в "этой неделей" (создан в октябре)
- Тикет #2: создан 2025-10-10, закрыт 2025-10-20 → должен попасть в "этой неделей" (создан в октябре)
- Тикет #3: создан 2025-09-20, закрыт 2025-10-10 → должен попасть в "другой неделей" (создан в сентябре)
- Тикет #4: создан 2025-08-15, закрыт 2025-10-25 → должен попасть в "другой неделей" (создан в августе)
- Тикет #5: создан 2025-09-30 23:59:59, закрыт 2025-10-01 00:00:01 → должен попасть в "другой неделей" (создан в сентябре)
- Тикет #6: создан 2025-10-31 23:59:59, закрыт 2025-10-31 23:59:59 → должен попасть в "этой неделей" (создан в октябре)

Ожидаемый результат для октября:
- Общее число: 6
- Этой неделей: 3 (тикеты #1, #2, #6)
- Другой неделей: 3 (тикеты #3, #4, #5)
```

**Проверка граничных случаев:**

1. **Тикет создан в последний момент предыдущего месяца, закрыт в первый момент текущего:**
   - Создан: 2025-09-30 23:59:59
   - Закрыт: 2025-10-01 00:00:00
   - Ожидаемый результат: "другой неделей" (создан в сентябре)

2. **Тикет создан в последний момент текущего месяца, закрыт в последний момент текущего:**
   - Создан: 2025-10-31 23:59:59
   - Закрыт: 2025-10-31 23:59:59
   - Ожидаемый результат: "этой неделей" (создан в октябре)

3. **Тикет создан в первый момент текущего месяца, закрыт в первый момент текущего:**
   - Создан: 2025-10-01 00:00:00
   - Закрыт: 2025-10-01 00:00:00
   - Ожидаемый результат: "этой неделей" (создан в октябре)

### 3. Уточнение логики у заказчика

**Текущая логика (найдена в коде):**
- **"Этой неделей"** = закрытые тикеты месяца, которые были **созданы в этом же месяце**
- **"Другой неделей"** = закрытые тикеты месяца, которые были **созданы в предыдущих месяцах**

**Вопросы для уточнения у заказчика:**

1. **Подтверждение текущей логики:**
   - Подтверждает ли заказчик, что "этой неделей" = создан в этом же месяце?
   - Подтверждает ли заказчик, что "другой неделей" = создан в предыдущих месяцах?

2. **Название показателей:**
   - Название "этой неделей" и "другой неделей" в месячном режиме может вводить в заблуждение
   - Предложить переименование на "этим месяцем" и "другим месяцем"?
   - Или оставить текущие названия для консистентности с недельным режимом?

3. **Пример для подтверждения:**
   ```
   Октябрь: закрыто 80 тикетов
   - 60 тикетов созданы в октябре → "этой неделей" = 60
   - 20 тикетов созданы в сентябре/августе → "другой неделей" = 20
   ```
   - Подтверждает ли заказчик эту логику?

**Гипотеза (требует подтверждения):**
- "Этой неделей" = закрытые тикеты октября, которые были созданы в октябре
- "Другой неделей" = закрытые тикеты октября, которые были созданы в предыдущих месяцах
- Название "этой неделей" — историческое (из недельного режима), но логика месячная

### 4. Исправление логики (если необходимо)

**Задачи:**
1. Если логика не соответствует требованиям — исправить
2. Обновить код расчета показателей
3. Протестировать исправленную логику
4. Обновить документацию

**Возможные исправления:**
- Изменение логики определения "этой неделей" vs "другой неделей"
- Исправление фильтров по датам создания
- Исправление группировки данных

### 5. Обновление документации

**Задачи:**
1. Создать/обновить документацию логики расчета
2. Описать:
   - Как считаются показатели "этой неделей"
   - Как считаются показатели "другой неделей"
   - Примеры расчета
3. Добавить комментарии в код

---

## 🔧 Технические требования

### Логика расчета (текущая гипотеза)

**Для каждого месяца:**
1. Получить все тикеты, закрытые в этом месяце (`movedTime` в диапазоне месяца)
2. Разделить на две группы:
   - **"Этой неделей"**: тикеты, у которых `createdTime` находится в том же месяце, что и `movedTime`
   - **"Другой неделей"**: тикеты, у которых `createdTime` находится в предыдущих месяцах

**Псевдокод:**
```php
foreach ($closedTickets as $ticket) {
    $closedMonth = getMonthFromDate($ticket['movedTime']);
    $createdMonth = getMonthFromDate($ticket['createdTime']);
    
    if ($closedMonth === $createdMonth) {
        // "Этой неделей"
        $closedCreatedThisMonth++;
    } else {
        // "Другой неделей"
        $closedCreatedOtherMonth++;
    }
}
```

### Проверка корректности

**Критерии корректности:**
- Общее число закрытых тикетов = "этой неделей" + "другой неделей"
- "Этой неделей" ≤ общее число закрытых тикетов
- "Другой неделей" ≤ общее число закрытых тикетов

---

## ✅ Критерии приёмки

- [ ] Текущая логика расчета изучена и задокументирована
- [ ] Логика проверена с тестовыми данными
- [ ] Логика уточнена у заказчика
- [ ] Показатели "этой неделей" и "другой неделей" работают корректно
- [ ] Общее число = "этой неделей" + "другой неделей" (для каждого месяца)
- [ ] При необходимости — логика исправлена
- [ ] Документация обновлена
- [ ] Комментарии добавлены в код

---

## 📚 Примеры проверки

### Пример 1: Проверка логики с тестовыми данными

```php
// Тестовые данные
$testTickets = [
    [
        'id' => 1,
        'createdTime' => '2025-10-05T10:00:00Z',
        'movedTime' => '2025-10-15T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ],
    [
        'id' => 2,
        'createdTime' => '2025-10-10T10:00:00Z',
        'movedTime' => '2025-10-20T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ],
    [
        'id' => 3,
        'createdTime' => '2025-09-20T10:00:00Z',
        'movedTime' => '2025-10-10T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ],
    [
        'id' => 4,
        'createdTime' => '2025-08-15T10:00:00Z',
        'movedTime' => '2025-10-25T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS'
    ]
];

// Октябрь 2025
$octoberStart = new DateTimeImmutable('2025-10-01T00:00:00Z');
$octoberEnd = new DateTimeImmutable('2025-10-31T23:59:59Z');

$closedThisMonth = 0;  // "Этой неделей"
$closedOtherMonth = 0; // "Другой неделей"

foreach ($testTickets as $ticket) {
    $movedTime = new DateTimeImmutable($ticket['movedTime']);
    $createdTime = new DateTimeImmutable($ticket['createdTime']);
    
    // Тикет закрыт в октябре?
    if ($movedTime >= $octoberStart && $movedTime <= $octoberEnd) {
        // Создан в октябре?
        if ($createdTime >= $octoberStart && $createdTime <= $octoberEnd) {
            $closedThisMonth++;
        } else {
            $closedOtherMonth++;
        }
    }
}

// Проверка
assert($closedThisMonth === 2, "Этой неделей должно быть 2");
assert($closedOtherMonth === 2, "Другой неделей должно быть 2");
assert($closedThisMonth + $closedOtherMonth === 4, "Общее число должно быть 4");
```

### Пример 2: Документация текущей логики (найдена в коде)

**Текущая реализация (строки 795-815 в `api/graph-1c-admission-closure.php`):**

```php
// Подсчёт закрытых тикетов за месяц (закрытых в этом месяце)
$monthClosedCount = 0;
$monthClosedCreatedThisWeek = 0;  // Название "thisWeek", но логика месячная
$monthClosedCreatedOtherWeek = 0; // Название "otherWeek", но логика месячная

foreach ($tickets as $ticket) {
    $movedTime = $ticket['movedTime'] ?? $ticket['updatedTime'] ?? null;
    $createdTime = $ticket['createdTime'] ?? null;
    $stageId = $ticket['stageId'] ?? null;
    $stageId = $stageId ? strtoupper($stageId) : null;
    
    // Тикет закрыт в этом месяце?
    if ($stageId && in_array($stageId, $closingStages, true)) {
        if (isInRange($movedTime, $monthStart, $monthEnd)) {
            $monthClosedCount++;
            
            // TASK-058-03: Логика определения "этой неделей" vs "другой неделей"
            // "Этой неделей" = создан в этом же месяце (название историческое, но логика месячная)
            if (isInRange($createdTime, $monthStart, $monthEnd)) {
                $monthClosedCreatedThisWeek++;  // Создан в этом же месяце
            } else {
                $monthClosedCreatedOtherWeek++; // Создан в другом месяце
            }
        }
    }
}
```

**Документация логики:**

```php
/**
 * Рассчитывает показатели закрытых тикетов для месяца
 * 
 * TASK-058-03: Логика расчета "этой неделей" и "другой неделей"
 * 
 * ВАЖНО: Названия "этой неделей" и "другой неделей" — исторические (из недельного режима),
 * но в месячном режиме логика работает с месяцами, а не неделями.
 * 
 * Логика:
 * - "Этой неделей" = закрытые тикеты месяца, которые были созданы в этом же месяце
 * - "Другой неделей" = закрытые тикеты месяца, которые были созданы в предыдущих месяцах
 * 
 * Пример для октября:
 * - Тикет создан 2025-10-05, закрыт 2025-10-15 → "этой неделей" (создан в октябре)
 * - Тикет создан 2025-09-20, закрыт 2025-10-10 → "другой неделей" (создан в сентябре)
 * 
 * @param array $tickets Массив тикетов (уже отфильтрован по product=1C)
 * @param DateTimeImmutable $monthStart Начало месяца
 * @param DateTimeImmutable $monthEnd Конец месяца
 * @param array $closingStages Массив закрывающих стадий
 * @return array [
 *   'total' => int,              // Общее число закрытых тикетов месяца
 *   'thisMonth' => int,          // Закрытые тикеты, созданные в этом же месяце
 *   'otherMonth' => int          // Закрытые тикеты, созданные в предыдущих месяцах
 * ]
 */
function calculateClosedTicketsForMonth(
    array $tickets, 
    DateTimeImmutable $monthStart, 
    DateTimeImmutable $monthEnd,
    array $closingStages
): array {
    $total = 0;
    $thisMonth = 0;  // "Этой неделей" (историческое название)
    $otherMonth = 0; // "Другой неделей" (историческое название)
    
    foreach ($tickets as $ticket) {
        $movedTime = $ticket['movedTime'] ?? $ticket['updatedTime'] ?? null;
        $createdTime = $ticket['createdTime'] ?? null;
        $stageId = $ticket['stageId'] ?? null;
        $stageId = $stageId ? strtoupper($stageId) : null;
        
        // Тикет закрыт в этом месяце?
        if ($stageId && in_array($stageId, $closingStages, true)) {
            if (isInRange($movedTime, $monthStart, $monthEnd)) {
                $total++;
                
                // Создан в этом же месяце?
                if (isInRange($createdTime, $monthStart, $monthEnd)) {
                    $thisMonth++;  // "Этой неделей" = создан в этом же месяце
                } else {
                    $otherMonth++; // "Другой неделей" = создан в другом месяце
                }
            }
        }
    }
    
    // Проверка корректности
    if ($total !== ($thisMonth + $otherMonth)) {
        error_log("[MONTHS] Warning: Total closed tickets ({$total}) != thisMonth ({$thisMonth}) + otherMonth ({$otherMonth})");
    }
    
    return [
        'total' => $total,
        'thisMonth' => $thisMonth,
        'otherMonth' => $otherMonth
    ];
}
```

**Важные детали:**

1. **Использование функции `isInRange()`:**
   - Функция обрабатывает null и различные форматы дат
   - Не нужно создавать объекты DateTimeImmutable вручную
   - Функция уже определена в файле (строка 200)

2. **Проверка стадий:**
   - Тикет должен быть в закрывающей стадии (`$closingStages`)
   - Стадия проверяется через `in_array($stageId, $closingStages, true)`

3. **Проверка корректности:**
   - `total` должно равняться `thisMonth + otherMonth`
   - Добавить проверку и логирование несоответствий

---

## 🔗 Зависимости

- **TASK-053-03:** Backend API для 3-месячного режима
- **TASK-053-04:** Summary-карточки для 3-месячного режима

---

## 📝 История правок

- **2025-12-18 08:31 (UTC+3, Брест):** Создан детализированный документ для этапа 3
  - Описана задача проверки логики
  - Добавлены вопросы для уточнения
  - Добавлены примеры проверки
  - Добавлены критерии приёмки

---

## ⚠️ Примечания

- **Важно:** Требуется уточнение логики у заказчика перед исправлениями
- **Тестирование:** Обязательно протестировать с реальными данными
- **Документация:** После уточнения логики — обновить документацию
- **Названия:** Названия "этой неделей" и "другой неделей" в месячном режиме могут вводить в заблуждение
- **Консистентность:** Логика должна быть консистентной с недельным режимом (где названия имеют смысл)

## 📊 Текущая логика (найдена в коде)

**Выводы из анализа кода:**

1. **Логика работает корректно:**
   - "Этой неделей" = закрытые тикеты месяца, созданные в этом же месяце
   - "Другой неделей" = закрытые тикеты месяца, созданные в предыдущих месяцах
   - Это месячные данные, а не недельные

2. **Названия могут вводить в заблуждение:**
   - В месячном режиме названия "этой неделей" и "другой неделей" не соответствуют логике
   - Фактически это "этим месяцем" и "другим месяцем"
   - Названия исторические (из недельного режима)

3. **Рекомендации:**
   - Уточнить у заказчика, нужно ли переименовать показатели в месячном режиме
   - Или оставить текущие названия для консистентности
   - Добавить комментарии в код для ясности

## 🔍 Детальная проверка логики

### Шаг 1: Проверка функции isInRange()

**Файл:** `api/graph-1c-admission-closure.php`, строка 200

**Проверить:**
- Корректно ли функция обрабатывает граничные случаи (начало/конец месяца)
- Корректно ли функция обрабатывает null значения
- Корректно ли функция обрабатывает различные форматы дат

### Шаг 2: Проверка расчета для конкретного месяца

**Создать тестовый скрипт:**
```php
// test-closed-logic.php
require_once __DIR__ . '/api/graph-1c-admission-closure.php';

// Тестовые данные для октября 2025
$testTickets = [
    [
        'id' => 1,
        'createdTime' => '2025-10-05T10:00:00Z',
        'movedTime' => '2025-10-15T14:00:00Z',
        'stageId' => 'DT140_12:SUCCESS',
        'UF_CRM_7_TYPE_PRODUCT' => '1C'
    ],
    // ... остальные тестовые тикеты
];

$octoberStart = new DateTimeImmutable('2025-10-01T00:00:00Z', new DateTimeZone('UTC'));
$octoberEnd = new DateTimeImmutable('2025-10-31T23:59:59Z', new DateTimeZone('UTC'));
$closingStages = ['DT140_12:SUCCESS', 'DT140_12:FAIL', 'DT140_12:UC_0GBU8Z'];

$result = calculateClosedTicketsForMonth($testTickets, $octoberStart, $octoberEnd, $closingStages);

echo "Total: {$result['total']}\n";
echo "This month: {$result['thisMonth']}\n";
echo "Other month: {$result['otherMonth']}\n";
echo "Check: " . ($result['total'] === ($result['thisMonth'] + $result['otherMonth']) ? 'OK' : 'ERROR') . "\n";
```

### Шаг 3: Сравнение с реальными данными

**Задачи:**
1. Выбрать реальный месяц с известными данными
2. Вручную посчитать ожидаемые значения
3. Сравнить с результатами API
4. Найти расхождения (если есть)

