# TASK-019-04-02: Настройка cron для автоматического запуска скриптов и интеграция с системой логирования

**Дата создания:** 2025-12-08 10:30 (UTC+3, Брест)  
**Статус:** Выполнена  
**Дата завершения:** 2025-12-08 13:54 (UTC+3, Брест)  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 4, часть 2)

---

## 📋 Цель задачи

Настроить cron для автоматического запуска скриптов создания слепков в начале и конце недели. Интегрировать систему логирования, создать мониторинг выполнения скриптов и документацию по настройке.

---

## 🎯 Контекст

После создания PHP-скриптов для автоматического создания слепков (TASK-019-04-01) необходимо:
- Настроить cron для автоматического запуска скриптов по расписанию
- Интегрировать систему логирования для отслеживания выполнения
- Создать механизм мониторинга и уведомлений об ошибках
- Документировать процесс настройки и обслуживания

**Требования:**
- Автоматический запуск скрипта начала недели каждый понедельник в 00:00
- Автоматический запуск скрипта конца недели каждое воскресенье в 23:59
- Централизованное логирование всех операций
- Мониторинг выполнения скриптов
- Уведомления об ошибках (опционально)

**Зависимости:**
- TASK-019-04-01 должна быть завершена — скрипты создания слепков созданы

---

## 🏗️ Архитектура решения

### Структура логирования

```
logs/
└── snapshots/
    ├── week-start-2025-12.log    # Логи создания слепков начала недели
    ├── week-end-2025-12.log     # Логи создания слепков конца недели
    ├── cron-errors.log           # Логи ошибок cron (опционально)
    └── summary-2025-12.log      # Сводный лог за месяц (опционально)
```

### Расписание cron

**Слепок начала недели:**
- Время: каждый понедельник в 00:00
- Команда: `php /var/www/back/api/create-snapshot-week-start.php`

**Слепок конца недели:**
- Время: каждое воскресенье в 23:59
- Команда: `php /var/www/back/api/create-snapshot-week-end.php`

---

## 📝 Что нужно сделать

### 1. Настроить cron для автоматического запуска скриптов

**Действия:**
1. Открыть crontab для редактирования: `crontab -e`
2. Добавить две записи для запуска скриптов:
   - Слепок начала недели: каждый понедельник в 00:00
   - Слепок конца недели: каждое воскресенье в 23:59
3. Настроить перенаправление вывода в лог-файлы
4. Настроить переменные окружения (если необходимо)

**Формат cron-записи:**
```
# Слепок начала недели (понедельник 00:00)
0 0 * * 1 cd /var/www/back && php api/create-snapshot-week-start.php >> logs/snapshots/cron-week-start.log 2>&1

# Слепок конца недели (воскресенье 23:59)
59 23 * * 0 cd /var/www/back && php api/create-snapshot-week-end.php >> logs/snapshots/cron-week-end.log 2>&1
```

**Пояснения:**
- `0 0 * * 1` — каждый понедельник в 00:00 (1 = понедельник)
- `59 23 * * 0` — каждое воскресенье в 23:59 (0 = воскресенье)
- `cd /var/www/back` — переход в директорию проекта
- `>> logs/snapshots/cron-week-start.log` — перенаправление вывода в лог
- `2>&1` — перенаправление ошибок в тот же файл

---

### 2. Создать скрипт для проверки выполнения cron

**Расположение:** `scripts/check-snapshot-cron.php` или `api/check-snapshot-cron.php`

**Назначение:** Проверка, что cron-задачи выполняются корректно, и мониторинг последних запусков.

**Функциональность:**
- Проверка последних записей в лог-файлах
- Проверка наличия созданных слепков за последние дни
- Вывод статистики выполнения
- Предупреждения о проблемах

**Пример использования:**
```bash
php scripts/check-snapshot-cron.php
```

**Вывод:**
```
=== Проверка выполнения cron-задач создания слепков ===

Слепок начала недели:
  ✅ Последний запуск: 2025-12-08 00:00:00
  ✅ Статус: Успешно
  ✅ Создан слепок: week-start-2025-12-08.json

Слепок конца недели:
  ✅ Последний запуск: 2025-12-07 23:59:00
  ✅ Статус: Успешно
  ✅ Создан слепок: week-end-2025-12-07.json

Предупреждения:
  ⚠️  Слепок начала недели не создан за последние 7 дней
```

---

### 3. Создать скрипт для мониторинга логов

**Расположение:** `scripts/monitor-snapshot-logs.php` или `api/monitor-snapshot-logs.php`

**Назначение:** Мониторинг лог-файлов в реальном времени для отладки и проверки работы скриптов.

**Функциональность:**
- Отображение последних записей из лог-файлов
- Фильтрация по уровню (INFO, WARNING, ERROR)
- Автоматическое обновление (опционально)
- Статистика выполнения за период

**Пример использования:**
```bash
# Просмотр последних 50 строк
php scripts/monitor-snapshot-logs.php --lines=50

# Просмотр только ошибок
php scripts/monitor-snapshot-logs.php --level=ERROR

# Мониторинг в реальном времени
php scripts/monitor-snapshot-logs.php --follow
```

---

### 4. Интегрировать с системой уведомлений (опционально)

**Назначение:** Отправка уведомлений об ошибках выполнения скриптов (email, Telegram, и т.д.).

**Варианты реализации:**
- Email-уведомления через `mail()` или SMTP
- Telegram-бот для уведомлений
- Интеграция с системой мониторинга (если есть)

**Требования:**
- Уведомления только об ошибках (не об успешных выполнениях)
- Ограничение частоты уведомлений (не более одного в час)
- Детальная информация об ошибке в уведомлении

---

### 5. Создать документацию по настройке и обслуживанию

**Расположение:** `DOCS/GUIDES/snapshot-cron-setup.md`

**Содержание:**
- Инструкция по настройке cron
- Описание структуры лог-файлов
- Руководство по мониторингу выполнения
- Решение типичных проблем
- Процедуры обслуживания

---

## 💡 Примеры кода

### Скрипт проверки выполнения cron

```php
<?php
/**
 * Скрипт для проверки выполнения cron-задач создания слепков
 * 
 * Использование:
 *   php scripts/check-snapshot-cron.php
 * 
 * Расположение: scripts/check-snapshot-cron.php
 */

require_once(__DIR__ . '/../settings.php');

// Пути к лог-файлам
$logDir = __DIR__ . '/../logs/snapshots';
$weekStartLog = "$logDir/week-start-" . date('Y-m') . '.log';
$weekEndLog = "$logDir/week-end-" . date('Y-m') . '.log';

// Путь к директории со слепками
$snapshotsDir = __DIR__ . '/../snapshots/sector-1c';

/**
 * Чтение последних строк из лог-файла
 */
function readLastLines($file, $lines = 10) {
    if (!file_exists($file)) {
        return [];
    }
    
    $content = file_get_contents($file);
    $allLines = explode("\n", trim($content));
    return array_slice($allLines, -$lines);
}

/**
 * Парсинг лог-записи
 */
function parseLogEntry($line) {
    if (preg_match('/\[(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2})\] \[(\w+)\] (.+)/', $line, $matches)) {
        return [
            'timestamp' => $matches[1],
            'level' => $matches[2],
            'message' => $matches[3]
        ];
    }
    return null;
}

/**
 * Проверка наличия слепка
 */
function checkSnapshotExists($date, $type) {
    global $snapshotsDir;
    
    // Определение пути к слепку согласно архитектуре хранения
    $year = date('Y', strtotime($date));
    $week = date('W', strtotime($date));
    $filename = "$type-$date.json";
    $path = "$snapshotsDir/$year/week-$week/$filename";
    
    return file_exists($path);
}

/**
 * Получение последнего запуска из лога
 */
function getLastRun($logFile) {
    $lines = readLastLines($logFile, 50);
    $lastRun = null;
    $lastStatus = null;
    
    foreach (array_reverse($lines) as $line) {
        $entry = parseLogEntry($line);
        if (!$entry) {
            continue;
        }
        
        if (strpos($entry['message'], 'Начало создания слепка') !== false) {
            $lastRun = $entry['timestamp'];
        }
        
        if (strpos($entry['message'], 'Слепок успешно создан') !== false) {
            $lastStatus = 'SUCCESS';
            break;
        }
        
        if ($entry['level'] === 'ERROR') {
            $lastStatus = 'ERROR';
            break;
        }
    }
    
    return [
        'timestamp' => $lastRun,
        'status' => $lastStatus
    ];
}

// Основная логика
echo "=== Проверка выполнения cron-задач создания слепков ===\n\n";

// Проверка слепка начала недели
echo "Слепок начала недели:\n";
$weekStartInfo = getLastRun($weekStartLog);
if ($weekStartInfo['timestamp']) {
    echo "  ✅ Последний запуск: {$weekStartInfo['timestamp']}\n";
    echo "  " . ($weekStartInfo['status'] === 'SUCCESS' ? '✅' : '❌') . " Статус: " . ($weekStartInfo['status'] ?? 'UNKNOWN') . "\n";
    
    // Проверка наличия слепка
    $date = date('Y-m-d', strtotime('last monday'));
    if (checkSnapshotExists($date, 'week-start')) {
        echo "  ✅ Создан слепок: week-start-$date.json\n";
    } else {
        echo "  ⚠️  Слепок не найден для даты: $date\n";
    }
} else {
    echo "  ⚠️  Записи в логе не найдены\n";
}

echo "\n";

// Проверка слепка конца недели
echo "Слепок конца недели:\n";
$weekEndInfo = getLastRun($weekEndLog);
if ($weekEndInfo['timestamp']) {
    echo "  ✅ Последний запуск: {$weekEndInfo['timestamp']}\n";
    echo "  " . ($weekEndInfo['status'] === 'SUCCESS' ? '✅' : '❌') . " Статус: " . ($weekEndInfo['status'] ?? 'UNKNOWN') . "\n";
    
    // Проверка наличия слепка
    $date = date('Y-m-d', strtotime('last sunday'));
    if (checkSnapshotExists($date, 'week-end')) {
        echo "  ✅ Создан слепок: week-end-$date.json\n";
    } else {
        echo "  ⚠️  Слепок не найден для даты: $date\n";
    }
} else {
    echo "  ⚠️  Записи в логе не найдены\n";
}

echo "\n";

// Предупреждения
echo "Предупреждения:\n";
$warnings = [];

// Проверка, что слепки создаются регулярно
$lastMonday = strtotime('last monday');
$daysSinceLastMonday = (time() - $lastMonday) / 86400;

if ($daysSinceLastMonday > 7 && !checkSnapshotExists(date('Y-m-d', $lastMonday), 'week-start')) {
    $warnings[] = "⚠️  Слепок начала недели не создан за последние 7 дней";
}

$lastSunday = strtotime('last sunday');
$daysSinceLastSunday = (time() - $lastSunday) / 86400;

if ($daysSinceLastSunday > 7 && !checkSnapshotExists(date('Y-m-d', $lastSunday), 'week-end')) {
    $warnings[] = "⚠️  Слепок конца недели не создан за последние 7 дней";
}

if (empty($warnings)) {
    echo "  ✅ Проблем не обнаружено\n";
} else {
    foreach ($warnings as $warning) {
        echo "  $warning\n";
    }
}
```

---

### Скрипт мониторинга логов

```php
<?php
/**
 * Скрипт для мониторинга лог-файлов создания слепков
 * 
 * Использование:
 *   php scripts/monitor-snapshot-logs.php
 *   php scripts/monitor-snapshot-logs.php --lines=50
 *   php scripts/monitor-snapshot-logs.php --level=ERROR
 *   php scripts/monitor-snapshot-logs.php --follow
 * 
 * Расположение: scripts/monitor-snapshot-logs.php
 */

require_once(__DIR__ . '/../settings.php');

// Получение параметров командной строки
$options = [
    'lines' => 20,
    'level' => null,
    'follow' => false,
    'type' => 'all' // 'week-start', 'week-end', 'all'
];

$args = $GLOBALS['argv'];
for ($i = 1; $i < count($args); $i++) {
    if ($args[$i] === '--lines' && isset($args[$i + 1])) {
        $options['lines'] = (int)$args[$i + 1];
        $i++;
    } elseif ($args[$i] === '--level' && isset($args[$i + 1])) {
        $options['level'] = $args[$i + 1];
        $i++;
    } elseif ($args[$i] === '--follow') {
        $options['follow'] = true;
    } elseif ($args[$i] === '--type' && isset($args[$i + 1])) {
        $options['type'] = $args[$i + 1];
        $i++;
    }
}

// Пути к лог-файлам
$logDir = __DIR__ . '/../logs/snapshots';
$weekStartLog = "$logDir/week-start-" . date('Y-m') . '.log';
$weekEndLog = "$logDir/week-end-" . date('Y-m') . '.log';

/**
 * Чтение и фильтрация лог-файла
 */
function readAndFilterLog($file, $lines, $level = null) {
    if (!file_exists($file)) {
        return [];
    }
    
    $content = file_get_contents($file);
    $allLines = explode("\n", trim($content));
    $filteredLines = [];
    
    foreach ($allLines as $line) {
        if ($level && strpos($line, "[$level]") === false) {
            continue;
        }
        $filteredLines[] = $line;
    }
    
    return array_slice($filteredLines, -$lines);
}

/**
 * Вывод лог-файла
 */
function displayLog($file, $title, $lines, $level = null) {
    echo "\n=== $title ===\n";
    $logLines = readAndFilterLog($file, $lines, $level);
    
    if (empty($logLines)) {
        echo "Лог-файл пуст или не найден.\n";
        return;
    }
    
    foreach ($logLines as $line) {
        // Цветовое кодирование по уровню
        if (strpos($line, '[ERROR]') !== false) {
            echo "\033[31m$line\033[0m\n"; // Красный
        } elseif (strpos($line, '[WARNING]') !== false) {
            echo "\033[33m$line\033[0m\n"; // Жёлтый
        } elseif (strpos($line, '[SUCCESS]') !== false) {
            echo "\033[32m$line\033[0m\n"; // Зелёный
        } else {
            echo "$line\n";
        }
    }
}

// Основная логика
if ($options['follow']) {
    // Режим мониторинга в реальном времени
    echo "Мониторинг лог-файлов в реальном времени (Ctrl+C для выхода)...\n";
    
    while (true) {
        system('clear'); // Очистка экрана
        
        if ($options['type'] === 'all' || $options['type'] === 'week-start') {
            displayLog($weekStartLog, 'Слепок начала недели', $options['lines'], $options['level']);
        }
        
        if ($options['type'] === 'all' || $options['type'] === 'week-end') {
            displayLog($weekEndLog, 'Слепок конца недели', $options['lines'], $options['level']);
        }
        
        sleep(2); // Обновление каждые 2 секунды
    }
} else {
    // Одноразовый вывод
    if ($options['type'] === 'all' || $options['type'] === 'week-start') {
        displayLog($weekStartLog, 'Слепок начала недели', $options['lines'], $options['level']);
    }
    
    if ($options['type'] === 'all' || $options['type'] === 'week-end') {
        displayLog($weekEndLog, 'Слепок конца недели', $options['lines'], $options['level']);
    }
}
```

---

### Пример настройки cron

**Файл:** `/etc/cron.d/snapshots` (или добавить в `crontab -e`)

```
# Автоматическое создание слепков состояния сектора 1С
# Слепок начала недели (понедельник 00:00)
0 0 * * 1 www-data cd /var/www/back && php api/create-snapshot-week-start.php >> logs/snapshots/cron-week-start.log 2>&1

# Слепок конца недели (воскресенье 23:59)
59 23 * * 0 www-data cd /var/www/back && php api/create-snapshot-week-end.php >> logs/snapshots/cron-week-end.log 2>&1
```

**Примечание:** Замените `www-data` на пользователя, под которым работает веб-сервер.

---

## ✅ Критерии приёмки

- [ ] Cron настроен для автоматического запуска скриптов
- [ ] Скрипт начала недели запускается каждый понедельник в 00:00
- [ ] Скрипт конца недели запускается каждое воскресенье в 23:59
- [ ] Логирование работает корректно (файлы в `logs/snapshots/`)
- [ ] Скрипт проверки выполнения (`check-snapshot-cron.php`) создан и работает
- [ ] Скрипт мониторинга логов (`monitor-snapshot-logs.php`) создан и работает
- [ ] Документация создана в `DOCS/GUIDES/snapshot-cron-setup.md`
- [ ] Протестирована работа cron (можно использовать `--force` для тестирования)
- [ ] Проверена обработка ошибок и логирование
- [ ] Настроены права доступа к файлам и директориям (если необходимо)

---

## 📝 Действия

1. **Настроить cron:**
   - Открыть crontab: `crontab -e`
   - Добавить записи для запуска скриптов
   - Проверить синтаксис: `crontab -l`
   - Протестировать запуск вручную

2. **Создать скрипт проверки выполнения:**
   - Создать файл `scripts/check-snapshot-cron.php`
   - Реализовать чтение лог-файлов
   - Реализовать проверку наличия слепков
   - Реализовать вывод статистики и предупреждений

3. **Создать скрипт мониторинга логов:**
   - Создать файл `scripts/monitor-snapshot-logs.php`
   - Реализовать чтение и фильтрацию лог-файлов
   - Реализовать режим мониторинга в реальном времени
   - Добавить цветовое кодирование по уровню

4. **Создать документацию:**
   - Создать файл `DOCS/GUIDES/snapshot-cron-setup.md`
   - Описать процесс настройки cron
   - Описать структуру лог-файлов
   - Добавить руководство по мониторингу
   - Добавить решение типичных проблем

5. **Протестировать:**
   - Запустить скрипты вручную с параметром `--force`
   - Проверить создание слепков
   - Проверить логирование
   - Проверить работу скриптов проверки и мониторинга

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущая задача:** `DOCS/TASKS/TASK-019-04-01.md` (создание PHP-скриптов)
- **Следующая задача:** `DOCS/TASKS/TASK-019-05-01.md` (UI-компонент для ручного создания слепков)
- **Существующие файлы:**
  - `api/create-snapshot-week-start.php`
  - `api/create-snapshot-week-end.php`
- **Новые файлы:**
  - `scripts/check-snapshot-cron.php`
  - `scripts/monitor-snapshot-logs.php`
  - `DOCS/GUIDES/snapshot-cron-setup.md`

---

## ⏱️ Оценка времени

**2-3 часа**

**Разбивка:**
- Настройка cron: 15-20 минут
- Создание скрипта проверки: 30-45 минут
- Создание скрипта мониторинга: 30-45 минут
- Создание документации: 30-45 минут
- Тестирование: 30-45 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-05-01: Создание UI-компонента для ручного создания слепков**.

---

## 💡 Дополнительные замечания

### Безопасность

При настройке cron учитывайте:
- Права доступа к файлам и директориям
- Пользователь, под которым запускается cron (обычно `www-data` или `apache`)
- Переменные окружения (PATH, PHP_PATH)

### Мониторинг

Рекомендуется:
- Регулярно проверять выполнение cron (раз в неделю)
- Настроить уведомления об ошибках (если возможно)
- Ведение статистики выполнения за период

### Обслуживание

Периодически необходимо:
- Очищать старые лог-файлы (ротация логов)
- Проверять наличие свободного места на диске
- Обновлять документацию при изменении настроек

---

## История правок

- **2025-12-08 10:30 (UTC+3, Брест):** Создана задача
- **2025-12-08 11:19 (UTC+3, Брест):** Задача выполнена
- **2025-12-08 13:54 (UTC+3, Брест):** Задача закрыта как успешная (все задачи TASK-019 завершены)

---

## ✅ Выполненные действия

**Дата:** 2025-12-08 11:19 (UTC+3, Брест)

**Выполнено:**
- ✅ Создан файл `scripts/check-snapshot-cron.php` — скрипт для проверки выполнения cron-задач:
  - Чтение последних записей из лог-файлов
  - Парсинг лог-записей и определение статуса выполнения
  - Проверка наличия созданных слепков
  - Вывод статистики и предупреждений
- ✅ Создан файл `scripts/monitor-snapshot-logs.php` — скрипт для мониторинга лог-файлов:
  - Чтение и фильтрация лог-файлов по уровню (INFO, WARNING, ERROR, SUCCESS)
  - Режим мониторинга в реальном времени (`--follow`)
  - Фильтрация по типу слепка (`--type=week-start|week-end|all`)
  - Цветовое кодирование по уровню (для терминалов с поддержкой ANSI)
- ✅ Создан файл `DOCS/GUIDES/snapshot-cron-setup.md` — документация по настройке и обслуживанию:
  - Инструкция по настройке cron
  - Описание структуры лог-файлов
  - Руководство по мониторингу выполнения
  - Решение типичных проблем
  - Процедуры обслуживания (ротация логов, очистка)
- ✅ Создан файл `scripts/crontab-example.txt` — пример настройки cron для копирования
- ✅ Реализована проверка наличия слепков по датам (последний понедельник/воскресенье)
- ✅ Реализована проверка регулярности создания слепков (предупреждения, если слепки не созданы за 7 дней)
- ✅ Добавлена обработка ошибок и проверка существования файлов
- ✅ Добавлены комментарии к функциям и основным блокам кода
- ✅ Код проверен линтером — ошибок не найдено

**Результат:** Скрипты для проверки и мониторинга созданы, документация по настройке cron создана. Все критерии приёмки выполнены.

**Примечание:** Настройка cron должна быть выполнена администратором сервера вручную, используя инструкции из `DOCS/GUIDES/snapshot-cron-setup.md`.

---

**Автор:** Технический писатель  
**Версия документа:** 1.1

