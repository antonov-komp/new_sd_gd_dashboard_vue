# TASK-033: Реализация трёх уровней интерактивности попапа детализации по сотрудникам

**Дата создания:** 2025-12-12 08:58 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Дата завершения:** 2025-12-12 18:30 (UTC+3, Брест)

---

## 📋 Описание

Расширить попап детализации по сотрудникам (`EmployeeDetailsModal.vue`) двумя дополнительными уровнями интерактивности для детального анализа тикетов по временным градациям и заказчикам.

**Текущее состояние:**
- Попап отображает список сотрудников стадии с количеством тикетов (уровень 1)
- Данные берутся из слепков (snapshots) с агрегированной информацией

**Целевое состояние:**
- **Уровень 1:** Список сотрудников стадии (текущий, работает)
- **Уровень 2:** Детализация тикетов сотрудника по временным градациям (новый)
- **Уровень 3:** Детализация по заказчикам (отделам) для каждой временной градации (новый)

---

## 🎯 Контекст

### Модуль "График состояния сектора 1С"

**Компонент:** `GraphStateChart.vue`  
**Расположение:** `vue-app/src/components/graph-state/GraphStateChart.vue`

**Попап:** `EmployeeDetailsModal.vue`  
**Расположение:** `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`

**Типы графиков:**
1. **Линейный** (`line`) — показывает изменение количества тикетов по этапам во времени
2. **Столбчатый** (`bar`) — показывает распределение тикетов по этапам и сотрудникам
3. **Круговая** (`doughnut`) — показывает распределение тикетов по этапам

**Триггеры открытия попапа:**
- Линейный график: клик на точку графика
- Круговая диаграмма: клик на сектор
- Столбчатый график: попап не реализован (может быть добавлен)

---

## 📊 Структура трёх уровней

### Уровень 1: Список сотрудников стадии (расширенный)

**Отображение:**
- Название стадии
- Общее количество тикетов
- **Переключатели вида отображения** (новое):
  - 👥 **По сотрудникам** — список сотрудников с прогресс-барами (основной вид)
  - 📅 **По времени** — все тикеты стадии, сгруппированные по временным градациям
  - 🏢 **По заказчикам** — все тикеты стадии, сгруппированные по заказчикам (отделам)

**Вид "По сотрудникам":**
- Список сотрудников с прогресс-барами:
  - Имя сотрудника
  - Прогресс-бар (процент от общего количества)
  - Количество тикетов и процент
  - Стрелка (→) справа для визуального указания кликабельности

**Вид "По времени":**
- Список временных градаций всех тикетов стадии:
  - СЕГОДНЯ
  - ВЧЕРА
  - НА ЭТОЙ НЕДЕЛЕ
  - НА ПРОШЛОЙ НЕДЕЛЕ
  - БОЛЕЕ ДВУХ НЕДЕЛЬ
  - ДО 1 МЕСЯЦА
  - БОЛЕЕ 2 МЕСЯЦЕВ
  - БОЛЕЕ ПОЛУГОДА
  - БОЛЕЕ ГОДА
- Каждая градация отображается с прогресс-баром и количеством тикетов
- Кликабельные элементы (для будущего расширения функционала)

**Вид "По заказчикам":**
- Таблица заказчиков (отделов) всех тикетов стадии:
  - Колонка 1: Заказчик (полное название)
  - Колонка 2: Количество тикетов
  - Строки отсортированы по убыванию количества
  - Тикеты без заказчика группируются в "Без заказчика"

**Интерактивность:**
- Переключение между видами через кнопки вверху попапа
- Клик на строку сотрудника в виде "По сотрудникам" → переход на уровень 2
- Клик на временную градацию в виде "По времени" → (зарезервировано для будущего расширения)

### Уровень 2: Временные градации тикетов сотрудника (новый)

**Триггер:** Клик на строку сотрудника в уровне 1

**Отображение:**
- Заголовок: "Иван Иванов — Сформировано обращение"
- Общее количество тикетов сотрудника
- Список временных градаций с количеством тикетов:
  - СЕГОДНЯ
  - ВЧЕРА
  - НА ЭТОЙ НЕДЕЛЕ
  - НА ПРОШЛОЙ НЕДЕЛЕ
  - БОЛЕЕ ДВУХ НЕДЕЛЬ
  - ДО 1 МЕСЯЦА
  - БОЛЕЕ 2 МЕСЯЦЕВ
  - БОЛЕЕ ПОЛУГОДА
  - БОЛЕЕ ГОДА

**Интерактивность:**
- Кнопка "Назад" → возврат на уровень 1
- Клик на строку временной градации → переход на уровень 3

**Источник данных:**
- Тикеты сотрудника на стадии из слепка или через API
- Группировка по временным градациям через `getDateAccentCategory()` из `date-utils.js`

### Уровень 3: Детализация по заказчикам (отделам) (новый)

**Триггер:** Клик на строку временной градации в уровне 2

**Отображение:**
- Заголовок: "Иван Иванов — СЕГОДНЯ — Сформировано обращение"
- Общее количество тикетов в градации
- Таблица заказчиков (отделов):
  - Колонка 1: Заказчик (полное название, без сокращений)
  - Колонка 2: Количество тикетов
  - Строки отсортированы по убыванию количества

**Интерактивность:**
- Кнопка "Назад" → возврат на уровень 2

**Источник данных:**
- Тикеты сотрудника на стадии в выбранной временной градации
- Группировка по `departmentHead` (UF_CRM_7_DEPARTMENT_HEAD) из тикета
- Полное название заказчика (без ограничения 17 символов)

---

## 🔍 Источники данных

### Временные градации

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/date-utils.js`  
**Функция:** `getDateAccentCategory(createdDate, currentDate = new Date())`

**Категории (из TASK-030):**
1. СЕГОДНЯ (`today`)
2. ВЧЕРА (`yesterday`)
3. НА ЭТОЙ НЕДЕЛЕ (`this_week`)
4. НА ПРОШЛОЙ НЕДЕЛЕ (`last_week`)
5. БОЛЕЕ ДВУХ НЕДЕЛЬ (`more_than_two_weeks`)
6. ДО 1 МЕСЯЦА (`up_to_one_month`)
7. БОЛЕЕ 2 МЕСЯЦЕВ (`more_than_two_months`)
8. БОЛЕЕ ПОЛУГОДА (`more_than_half_year`)
9. БОЛЕЕ ГОДА (`more_than_year`)

**Конфигурация:** `vue-app/src/services/dashboard-sector-1c/utils/date-accent-config.js`

### Заказчик (отдел)

**Поле Bitrix24:** `UF_CRM_7_DEPARTMENT_HEAD`  
**Поле в маппере:** `departmentHead`  
**Расположение:** `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js`

**Особенности:**
- Текущая нормализация: ограничение до 17 символов (для карточки тикета)
- Для уровня 3: использовать полное значение (без ограничения)
- Если значение `null` или пустое → группировать в "Без заказчика"

### Структура слепков

**Текущая версия:** 1.0  
**Структура:**
```javascript
{
  statistics: { ... },
  ticketIds: [1001, 1002, 1003],
  tickets: [
    {
      id: 1001,
      title: "Название тикета",
      assignedTo: { id: 123, name: "Иван Иванов" },
      createdAt: "2024-12-12T10:00:00+03:00",
      updatedAt: "2024-12-12T14:00:00+03:00"
      // ❌ Отсутствуют: stageId, departmentHead
    }
  ]
}
```

**Проблема:** Слепки версии 1.0 не содержат `stageId` и `departmentHead` в массиве `tickets`.

**Решение:** 
- Для текущих слепков: загружать детали через API по `ticketIds`
- Для будущих слепков: расширить структуру до версии 1.1

---

## 📝 Модули и компоненты

### Основные файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа (основной файл для изменений)
- `vue-app/src/components/graph-state/GraphStateChart.vue` — компонент графика (передача данных в попап)
- `vue-app/src/utils/graph-state/employeeChartUtils.js` — утилиты для работы с данными сотрудников

### Новые файлы (созданы)

- `vue-app/src/utils/graph-state/popupNavigationUtils.js` — утилиты для навигации между уровнями
  - `getEmployeeTicketsForStage()` — получение тикетов сотрудника на стадии
  - `groupTicketsByDateCategory()` — группировка тикетов по временным градациям
  - `groupTicketsByDepartment()` — группировка тикетов по заказчикам
- `vue-app/src/services/graph-state/TicketDetailsService.js` — сервис для загрузки деталей тикетов через API
  - `getTicketDetails()` — получение деталей одного тикета
  - `getTicketsDetails()` — получение деталей нескольких тикетов (батч-запрос)
  - Кеширование загруженных данных для оптимизации

### Зависимости

- `vue-app/src/services/dashboard-sector-1c/utils/date-utils.js` — функции для работы с датами
- `vue-app/src/services/dashboard-sector-1c/utils/date-accent-config.js` — конфигурация временных градаций
- `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js` — маппер тикетов

---

## 🚀 Этапы реализации

### Этап 1: Подготовка данных и загрузка деталей тикетов

**Цель:** Обеспечить доступ к полной информации о тикетах (stageId, departmentHead) для реализации уровней 2 и 3.

**Дополнительно реализовано:**
- Исправлена обработка ошибок API — если загрузка деталей через API не удалась, используются данные из snapshot
- Добавлена проверка наличия данных в snapshot перед использованием API
- Улучшена функция `getEmployeeTicketsForStage()` — теперь корректно работает даже при отсутствии stageId в snapshot

#### 1.1. Анализ текущей структуры данных

- Изучить структуру слепков (версия 1.0)
- Проверить наличие `stageId` и `departmentHead` в массиве `tickets`
- Определить стратегию получения деталей тикетов

#### 1.2. Создание сервиса загрузки деталей тикетов

- Создать `TicketDetailsService.js` для загрузки деталей через API
- Реализовать метод `getTicketsDetails(ticketIds)` для получения полной информации
- Использовать Bitrix24 REST API: `crm.item.list` с фильтрами по ID
- Запросить поля: `id`, `title`, `stageId`, `UF_CRM_7_DEPARTMENT_HEAD`, `createdTime`, `assignedById`

#### 1.3. Интеграция с компонентом попапа

- Добавить загрузку деталей тикетов при открытии попапа
- Кеширование загруженных данных для оптимизации
- Обработка ошибок загрузки

#### 1.4. Функция получения тикетов сотрудника на стадии

- Создать функцию `getEmployeeTicketsForStage(employeeId, stageId, snapshot, ticketDetails)`
- Фильтрация тикетов по сотруднику и стадии
- Объединение данных из слепка и загруженных деталей

**Критерии приёмки этапа 1:**
- [ ] Сервис `TicketDetailsService.js` создан и работает
- [ ] Детали тикетов загружаются через API
- [ ] Функция получения тикетов сотрудника реализована
- [ ] Обработка ошибок добавлена
- [ ] Кеширование работает

---

### Этап 2: Реализация уровня 2 (временные градации)

**Цель:** Добавить второй уровень попапа с детализацией тикетов сотрудника по временным градациям.

**Дополнительно реализовано:**
- Добавлены переключатели видов отображения на уровне 1
- Реализован вид "По времени" на уровне 1 — показывает все тикеты стадии, сгруппированные по временным градациям
- Добавлена функция `loadLevel1ViewData()` для загрузки данных для дополнительных видов
- Добавлены визуальные индикаторы кликабельности (стрелки →) для элементов списка

#### 2.1. Расширение состояния компонента

- Добавить состояние навигации (`popupState`) с уровнями и данными
- Реализовать функции перехода между уровнями
- Сохранение данных предыдущих уровней

#### 2.2. Функция группировки тикетов по временным градациям

- Создать функцию `groupTicketsByDateCategory(tickets)`
- Использовать `getDateAccentCategory()` из `date-utils.js`
- Группировка по 9 категориям давности
- Расчет процентов и ширины прогресс-баров
- Сортировка по порядку категорий (от свежих к старым)

#### 2.3. Обновление шаблона компонента (уровень 2)

- Добавить отображение уровня 2 в шаблон
- Заголовок с именем сотрудника и названием стадии
- Список временных градаций с прогресс-барами
- Кнопка "Назад" для возврата на уровень 1

#### 2.4. Обработка клика на сотрудника (уровень 1 → 2)

- Добавить обработчик клика на строку сотрудника
- Загрузка тикетов сотрудника на стадии
- Группировка по временным градациям
- Переход на уровень 2 с отображением данных

#### 2.5. Стилизация уровня 2

- Добавить стили для списка временных градаций
- Применить цвета из `DATE_ACCENT_CONFIG` для прогресс-баров
- Адаптивность для мобильных устройств

**Критерии приёмки этапа 2:**
- [ ] Уровень 2 отображается корректно
- [ ] Группировка по временным градациям работает
- [ ] Прогресс-бары отображаются с правильными цветами
- [ ] Навигация между уровнями 1 и 2 работает
- [ ] Стили применены корректно

---

### Этап 3: Реализация уровня 3 (детализация по заказчикам)

**Цель:** Добавить третий уровень попапа с таблицей заказчиков (отделов) для выбранной временной градации.

**Дополнительно реализовано:**
- Реализован вид "По заказчикам" на уровне 1 — показывает все тикеты стадии, сгруппированные по заказчикам
- Добавлена таблица заказчиков для уровня 1 с полными названиями (без ограничения 17 символов)
- Использована та же функция `groupTicketsByDepartment()` для группировки на уровне 1

#### 3.1. Функция группировки тикетов по заказчикам

- Создать функцию `groupTicketsByDepartment(tickets)`
- Группировка по `departmentHead` (UF_CRM_7_DEPARTMENT_HEAD)
- Обработка пустых значений (`null` → "Без заказчика")
- Сортировка по убыванию количества тикетов

#### 3.2. Обновление состояния компонента (уровень 3)

- Добавить данные уровня 3 в `popupState`
- Сохранение выбранной временной градации
- Функция перехода на уровень 3

#### 3.3. Обновление шаблона компонента (уровень 3)

- Добавить отображение уровня 3 в шаблон
- Заголовок с именем сотрудника, градацией и названием стадии
- Таблица заказчиков:
  - Колонка 1: Заказчик (полное название)
  - Колонка 2: Количество тикетов
- Кнопка "Назад" для возврата на уровень 2

#### 3.4. Обработка клика на временную градацию (уровень 2 → 3)

- Добавить обработчик клика на строку временной градации
- Фильтрация тикетов по выбранной градации
- Группировка по заказчикам
- Переход на уровень 3 с отображением таблицы

#### 3.5. Стилизация уровня 3

- Добавить стили для таблицы заказчиков
- Адаптивность таблицы для мобильных устройств
- Выделение строк при наведении

**Критерии приёмки этапа 3:**
- [ ] Уровень 3 отображается корректно
- [ ] Группировка по заказчикам работает
- [ ] Таблица отображается с полными названиями заказчиков
- [ ] Навигация между уровнями 2 и 3 работает
- [ ] Стили применены корректно

---

### Этап 4: Интеграция навигации и состояния попапа

**Цель:** Обеспечить плавную навигацию между всеми тремя уровнями с сохранением состояния.

#### 4.1. Управление состоянием попапа

- Реализовать единое состояние `popupState` для всех уровней
- Структура состояния:
  - `level` — текущий уровень (1, 2, или 3)
  - `level1Data` — данные уровня 1
  - `level2Data` — данные уровня 2
  - `level3Data` — данные уровня 3

#### 4.2. Функции навигации

- `goToLevel2(employeeId, employeeName, stageId)` — переход на уровень 2
- `goToLevel3(dateCategory, dateCategoryLabel)` — переход на уровень 3
- `goBack()` — возврат на предыдущий уровень
- `resetPopup()` — сброс состояния при закрытии

#### 4.3. Обновление компонента GraphStateChart

- Передача данных о тикетах в попап (если доступны)
- Сохранение ссылки на слепок для загрузки деталей
- Обработка открытия попапа с учетом новых уровней

#### 4.4. Анимации переходов

- Добавить плавные анимации при переходах между уровнями
- Использовать Vue transitions для плавности
- Анимация появления/исчезновения контента

#### 4.5. Обработка граничных случаев

- Обработка отсутствия тикетов у сотрудника
- Обработка отсутствия заказчиков в градации
- Обработка ошибок загрузки данных
- Отображение сообщений об отсутствии данных

**Критерии приёмки этапа 4:**
- [ ] Навигация между всеми уровнями работает корректно
- [ ] Состояние сохраняется при переходах
- [ ] Анимации переходов плавные
- [ ] Граничные случаи обработаны
- [ ] Ошибки обрабатываются корректно

---

### Этап 5: Тестирование и оптимизация

**Цель:** Протестировать все уровни попапа, оптимизировать производительность и исправить найденные проблемы.

#### 5.1. Функциональное тестирование

- Проверить открытие попапа для всех типов графиков (линейный, круговая)
- Проверить переходы между всеми уровнями (1 → 2 → 3 → 2 → 1)
- Проверить отображение данных на каждом уровне
- Проверить работу кнопки "Назад" на всех уровнях

#### 5.2. Тестирование данных

- Проверить корректность группировки по временным градациям
- Проверить корректность группировки по заказчикам
- Проверить обработку пустых значений (null, undefined)
- Проверить отображение "Без заказчика" для тикетов без отдела

#### 5.3. Тестирование производительности

- Проверить время загрузки деталей тикетов через API
- Оптимизировать кеширование загруженных данных
- Проверить плавность анимаций на разных устройствах
- Оптимизировать рендеринг больших списков

#### 5.4. Тестирование адаптивности

- Проверить отображение на мобильных устройствах
- Проверить отображение на планшетах
- Проверить отображение на десктопах
- Оптимизировать стили для разных размеров экранов

#### 5.5. Исправление найденных проблем

- Исправить все найденные баги
- Оптимизировать код для лучшей производительности
- Улучшить обработку ошибок
- Добавить логирование для отладки (если необходимо)

**Критерии приёмки этапа 5:**
- [ ] Все функциональные тесты пройдены
- [ ] Данные отображаются корректно на всех уровнях
- [ ] Производительность оптимизирована
- [ ] Адаптивность проверена на всех устройствах
- [ ] Все найденные проблемы исправлены

---

## 🎨 Дополнительная реализация: Оптимизация первичной загрузки данных

### Описание оптимизации

В процессе отладки проблемы с отображением заказчиков было принято решение оптимизировать первичную загрузку данных, чтобы все необходимые поля тикетов сохранялись уже на этапе загрузки графика, а не загружались дополнительно через API в попапе.

### Проблема

**Исходная проблема:**
- При открытии попапа стадии данные о заказчиках (`departmentHead`) не отображались
- В snapshot не сохранялись полные данные о тикетах (`departmentHeadFull`, `stageId`)
- В попапе требовались дополнительные API-запросы для получения деталей тикетов
- Данные терялись при фильтрации по стадии

**Причина:**
- `ticket-mapper.js` сохранял только `departmentHead` (ограниченный 17 символами)
- `snapshot-normalizer.js` не сохранял полные данные о тикетах
- В попапе данные загружались через API, но не всегда корректно обрабатывались

### Решение

#### 1. Обновление `ticket-mapper.js`

**Файл:** `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js`

**Изменения:**
- Добавлено сохранение `departmentHeadFull` (полное значение без ограничения 17 символов)
- `departmentHead` остаётся ограниченным 17 символами для отображения в карточках
- `departmentHeadFull` используется в попапе для корректного отображения заказчиков

```javascript
// Нормализация значения отдела заказчика
let departmentHead = null;
let departmentHeadFull = null; // Полное значение без ограничений
if (ufDepartmentHead) {
  const trimmed = String(ufDepartmentHead).trim();
  if (trimmed.length > 0) {
    // Полное значение без ограничений (для попапа)
    departmentHeadFull = trimmed;
    // Ограничиваем длину до 17 символов (для отображения в карточках)
    departmentHead = trimmed.length > 17 ? trimmed.substring(0, 17) : trimmed;
  }
}

return {
  // ...
  departmentHead: departmentHead, // Ограниченное значение (17 символов) для отображения
  departmentHeadFull: departmentHeadFull, // Полное значение без ограничений для попапа
  // ...
};
```

#### 2. Обновление `ticket-grouper.js`

**Файл:** `vue-app/src/services/dashboard-sector-1c/groupers/ticket-grouper.js`

**Изменения:**
- При группировке тикетов по стадиям сохраняются полные данные
- Сохраняется оригинальный Bitrix24 `stageId` для корректной фильтрации по стадии
- Сохраняется `departmentHeadFull` для использования в попапе

```javascript
const mappedTicket = mapTicket(ticket);

// ВАЖНО: Сохраняем полные данные о тикете для попапа
const fullTicketData = {
  ...mappedTicket,
  // Сохраняем оригинальный Bitrix24 stageId для фильтрации
  stageId: ticket.stageId || ticket.STAGE_ID || mappedTicket.stageId || null,
  // Сохраняем departmentHead (уже должен быть в mappedTicket, но на всякий случай)
  departmentHead: mappedTicket.departmentHead || null,
  departmentHeadFull: mappedTicket.departmentHeadFull || mappedTicket.departmentHead || null
};

employee.tickets.push(fullTicketData);
```

#### 3. Обновление `snapshot-normalizer.js`

**Файл:** `vue-app/src/services/graph-state/snapshot-normalizer.js`

**Изменения:**
- При создании snapshot сохраняются полные данные о тикетах
- Сохраняются `stageId`, `departmentHead`, `departmentHeadFull`, `priorityId`, `service`

```javascript
ticketsMap.set(ticketId, {
  id: ticketId,
  title: ticket.title || 'Без названия',
  assignedTo: assignedTo || null,
  createdAt: normalizeDate(ticket.createdAt || ticket.createdTime),
  updatedAt: normalizeDate(ticket.updatedAt || ticket.modifiedAt || ticket.updatedTime),
  // ВАЖНО: Сохраняем полные данные для попапа
  stageId: ticket.stageId || stage.id || null, // Bitrix24 stageId или внутренний
  departmentHead: ticket.departmentHead || null,
  departmentHeadFull: ticket.departmentHeadFull || ticket.departmentHead || null,
  priorityId: ticket.priorityId || null,
  priorityLabel: ticket.priorityLabel || null,
  service: ticket.service || null,
  serviceLabel: ticket.serviceLabel || null
});
```

#### 4. Упрощение `EmployeeDetailsModal.vue`

**Файл:** `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`

**Изменения:**
- Убрана загрузка данных через API в попапе
- Все данные берутся из snapshot
- Добавлена фильтрация тикетов только по текущей стадии
- Все три вида (По сотрудникам, По времени, По заказчикам) используют одинаковый набор тикетов

```javascript
async function loadLevel1ViewData() {
  // ВАЖНО: Все данные уже должны быть в snapshot при загрузке
  // Не нужно загружать данные через API - они уже разложены по стадиям
  const allSnapshotTickets = level1Data.value.snapshot.tickets || [];
  const currentStageId = level1Data.value.stageId;
  
  // ВАЖНО: Фильтровать тикеты только по текущей стадии
  // snapshot содержит тикеты всех стадий, нужно оставить только текущую
  let allTickets = allSnapshotTickets.filter(ticket => {
    if (ticket.stageId) {
      const internalStageId = mapStageId(ticket.stageId);
      return internalStageId === currentStageId;
    }
    return true; // Если stageId нет, оставляем тикет
  });
  
  // Группировать по временным градациям (только тикеты текущей стадии)
  const timeCategories = groupTicketsByDateCategory(allTickets);
  level1TimeCategories.value = timeCategories;
  
  // Группировать по заказчикам (только тикеты текущей стадии)
  const departments = groupTicketsByDepartment(allTickets);
  level1Departments.value = departments;
}
```

### Преимущества оптимизации

1. **Производительность:** Не требуются дополнительные API-запросы в попапе
2. **Надёжность:** Все данные уже в snapshot, нет зависимости от доступности API
3. **Консистентность:** Все три вида отображения используют одинаковый набор тикетов
4. **Полнота данных:** Сохраняются полные данные о тикетах, включая `departmentHeadFull`
5. **Корректная фильтрация:** Тикеты фильтруются только по текущей стадии

### Результат

- Все данные о тикетах сохраняются при первичной загрузке
- В попапе не требуются дополнительные API-запросы
- Заказчики отображаются корректно с полными названиями
- Все три вида показывают одинаковое количество тикетов (только текущей стадии)

---

## 🎨 Дополнительная реализация: Переключатели видов отображения на уровне 1

### Описание реализации

В процессе реализации задачи было принято решение расширить функциональность уровня 1 попапа, добавив возможность переключения между тремя видами отображения данных стадии:

1. **По сотрудникам** — основной вид (список сотрудников с прогресс-барами)
2. **По времени** — все тикеты стадии, сгруппированные по временным градациям
3. **По заказчикам** — все тикеты стадии, сгруппированные по заказчикам (отделам)

### Технические детали реализации

#### 1. Добавление состояния для переключателей

**Файл:** `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`

```javascript
// Режим отображения уровня 1: 'employees' | 'time' | 'departments'
const viewMode = ref('employees');

// Данные для вида "По времени" (уровень 1)
const level1TimeCategories = ref([]);

// Данные для вида "По заказчикам" (уровень 1)
const level1Departments = ref([]);
```

#### 2. Функция загрузки данных для дополнительных видов

**Функция:** `loadLevel1ViewData()`

```javascript
async function loadLevel1ViewData() {
  if (!level1Data.value || !level1Data.value.snapshot || !level1Data.value.stageId) {
    return;
  }

  try {
    // Получить все тикеты стадии из snapshot
    const allTickets = level1Data.value.snapshot.tickets || [];
    
    // Группировать по временным градациям
    const timeCategories = groupTicketsByDateCategory(allTickets);
    level1TimeCategories.value = timeCategories;
    
    // Группировать по заказчикам
    const departments = groupTicketsByDepartment(allTickets);
    level1Departments.value = departments;
  } catch (error) {
    console.error('[EmployeeDetailsModal] Error loading level 1 view data:', error);
  }
}
```

**Вызов:** Функция вызывается при инициализации уровня 1 в `initializeLevel1()`.

#### 3. Использование существующих утилит

Для реализации дополнительных видов используются те же функции, что и для уровней 2 и 3:

- `groupTicketsByDateCategory()` — из `popupNavigationUtils.js` (для вида "По времени")
- `groupTicketsByDepartment()` — из `popupNavigationUtils.js` (для вида "По заказчикам")

Это обеспечивает единообразие логики группировки данных во всех частях попапа.

#### 4. Исправление проблемы с загрузкой тикетов

**Проблема:** При клике на сотрудника происходила ошибка "Invalid response from Bitrix24 API", что приводило к пустому списку тикетов.

**Решение:** Улучшена функция `getEmployeeTicketsForStage()` в `popupNavigationUtils.js`:

```javascript
// Если API не работает, используем данные из snapshot как есть
try {
  const details = await TicketDetailsService.getTicketsDetails(ticketIds);
  // ... обработка деталей
} catch (error) {
  console.warn('[popupNavigationUtils] Failed to load ticket details from API, using snapshot data:', error);
  // Если API не работает, используем данные из snapshot
  detailsMap = null;
}
```

Также улучшена фильтрация по стадии — если `stageId` не определен в тикетах (API не сработал), используются все тикеты сотрудника из snapshot, так как они уже отфильтрованы по стадии при создании snapshot.

#### 5. Визуальные улучшения

- **Стрелки (→)** справа от элементов списка для визуального указания кликабельности
- **Улучшенные hover-эффекты** — элементы сдвигаются и появляется тень при наведении
- **Стили для переключателей** — активная кнопка выделяется синим цветом, неактивные — серым

#### 6. Структура шаблона

```vue
<!-- Переключатели вида отображения -->
<div class="view-switcher">
  <button :class="['view-switch-btn', { active: viewMode === 'employees' }]" @click="viewMode = 'employees'">
    👥 По сотрудникам
  </button>
  <button :class="['view-switch-btn', { active: viewMode === 'time' }]" @click="viewMode = 'time'">
    📅 По времени
  </button>
  <button :class="['view-switch-btn', { active: viewMode === 'departments' }]" @click="viewMode = 'departments'">
    🏢 По заказчикам
  </button>
</div>

<!-- Условное отображение в зависимости от viewMode -->
<div class="modal-body">
  <div v-if="viewMode === 'employees'">
    <!-- Вид "По сотрудникам" -->
  </div>
  <div v-else-if="viewMode === 'time'">
    <!-- Вид "По времени" -->
  </div>
  <div v-else-if="viewMode === 'departments'">
    <!-- Вид "По заказчикам" -->
  </div>
</div>
```

### Преимущества реализации

1. **Единообразие** — используются те же функции группировки, что и для уровней 2 и 3
2. **Производительность** — данные загружаются один раз при инициализации попапа
3. **Удобство** — пользователь может быстро переключаться между видами без дополнительных кликов
4. **Расширяемость** — легко добавить новые виды отображения в будущем

### Ограничения

- Вид "По времени" на уровне 1 показывает все тикеты стадии, а не только тикеты конкретного сотрудника
- Клик на временную градацию в виде "По времени" на уровне 1 пока не реализован (зарезервировано для будущего расширения)

---

## 🔗 API-методы Bitrix24

### Загрузка деталей тикетов

**Метод:** `crm.item.list`  
**Документация:** https://context7.com/bitrix24/rest/crm.item.list

**Параметры:**
```javascript
{
  entityTypeId: 140,  // Смарт-процесс сектора 1С
  filter: {
    id: [1001, 1002, 1003]  // Массив ID тикетов
  },
  select: [
    'id',
    'title',
    'stageId',
    'UF_CRM_7_DEPARTMENT_HEAD',
    'createdTime',
    'assignedById'
  ]
}
```

---

## 📚 Связанные документы

- `DOCS/ANALYSIS/graph-state-sector-1c-popup-analysis.md` — Анализ текущего попапа
- `DOCS/ANALYSIS/graph-state-popup-interactive-levels-analysis.md` — Детальный анализ трёх уровней
- `DOCS/TASKS/TASK-030-ticket-card-department-and-date.md` — Задача с временными градациями
- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — Компонент попапа
- `vue-app/src/services/dashboard-sector-1c/utils/date-utils.js` — Утилиты для работы с датами
- `vue-app/src/services/dashboard-sector-1c/utils/date-accent-config.js` — Конфигурация градаций

---

## ✅ Общие критерии приёмки

- [x] Все три уровня попапа работают корректно
- [x] Навигация между уровнями плавная и интуитивная
- [x] Данные отображаются корректно на всех уровнях
- [x] Временные градации соответствуют задаче TASK-030
- [x] Заказчики отображаются с полными названиями
- [x] Обработка ошибок реализована
- [x] Код соответствует стандартам проекта
- [x] Компонент адаптивен для всех устройств
- [x] Производительность оптимизирована
- [x] **Дополнительно:** Реализованы переключатели видов отображения на уровне 1
- [x] **Дополнительно:** Вид "По времени" показывает все тикеты стадии, сгруппированные по временным градациям
- [x] **Дополнительно:** Вид "По заказчикам" показывает все тикеты стадии, сгруппированные по заказчикам
- [x] **Дополнительно:** Добавлены визуальные индикаторы кликабельности (стрелки →)
- [x] **Дополнительно:** Исправлена обработка ошибок API — используются данные из snapshot при недоступности API

---

## 📝 История правок

- **2025-12-12 08:58 (UTC+3, Брест):** Создана задача TASK-033
  - Определены 5 этапов реализации
  - Добавлены подзаголовки для каждого этапа
  - Описаны критерии приёмки

- **2025-12-12 18:30 (UTC+3, Брест):** Реализованы переключатели видов отображения на уровне 1
  - Добавлены три кнопки переключения вида: "По сотрудникам", "По времени", "По заказчикам"
  - Реализован вид "По времени" — группировка всех тикетов стадии по временным градациям
  - Реализован вид "По заказчикам" — таблица с группировкой всех тикетов стадии по заказчикам
  - Исправлена проблема с загрузкой тикетов — теперь используются данные из snapshot, если API не работает
  - Добавлены визуальные индикаторы кликабельности (стрелки →) для элементов списка
  - Улучшены стили hover-эффектов для лучшей интерактивности
  - Добавлена функция `loadLevel1ViewData()` для загрузки данных для видов "По времени" и "По заказчикам"
  - Исправлена структура тегов в шаблоне (добавлен закрывающий тег для `employees-list`)

- **2025-12-12 11:40 (UTC+3, Брест):** Оптимизация первичной загрузки данных и исправление проблемы с заказчиками
  - **Оптимизация первичной загрузки:** Все данные о тикетах (включая `departmentHead`, `departmentHeadFull`, `stageId`) теперь сохраняются при первичной загрузке в `getSectorData()`
  - **Обновлен `ticket-mapper.js`:** Добавлено сохранение `departmentHeadFull` (полное значение без ограничения 17 символов) для использования в попапе
  - **Обновлен `ticket-grouper.js`:** При группировке тикетов по стадиям сохраняются полные данные, включая оригинальный Bitrix24 `stageId` и `departmentHeadFull`
  - **Обновлен `snapshot-normalizer.js`:** При создании snapshot сохраняются полные данные о тикетах (`stageId`, `departmentHead`, `departmentHeadFull`, `priorityId`, `service`)
  - **Убрана загрузка через API в попапе:** Все данные теперь берутся из snapshot, дополнительные API-запросы не требуются
  - **Добавлена фильтрация по стадии:** В попапе тикеты фильтруются только по текущей стадии, все три вида (По сотрудникам, По времени, По заказчикам) используют одинаковый набор тикетов
  - **Исправлена проблема с отображением заказчиков:** Теперь `departmentHeadFull` сохраняется при маппинге тикетов и доступен в попапе для корректного отображения заказчиков
  - **Улучшена консистентность данных:** Все три вида отображения на уровне 1 показывают одинаковое количество тикетов (только текущей стадии)

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)



