# TASK-008: Разделение тикетов по принадлежности к сектору в дашборде 1С

**Дата создания:** 2025-12-06 13:22 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)

---

## 📋 Описание

Реализовать разделение тикетов в дашборде сектора 1С на две группы:
1. **Тикеты внутри сектора** — тикеты, назначенные сотрудникам сектора 1С (отдел 366)
2. **Тикеты вне сектора** — тикеты, назначенные сотрудникам других секторов (Железо 368, Битрикс24 369, PDM 367), но относящиеся к сектору 1С по тегу `UF_CRM_7_TYPE_PRODUCT = '1C'`

Тикеты вне сектора должны отображаться в конце рендера стадии и отделяться визуально. В заголовке стадии должно отображаться количество тикетов с разделением (например: "Сформировано обращение (15 / 3)", где 15 — внутри сектора, 3 — вне сектора).

---

## 🎯 Контекст

В дашборде сектора 1С загружаются тикеты из смарт-процесса 140, отфильтрованные по тегу `UF_CRM_7_TYPE_PRODUCT = '1C'`. Однако ответственные по тикетам могут быть из разных секторов:

- **Сектор 1С (366)** — основные сотрудники сектора
- **Хранитель (1051)** — хранит нулевую очередь (не отображается в колонках)
- **Сектор Железо (368)** — сотрудники из другого сектора
- **Сектор Битрикс24 (369)** — сотрудники из другого сектора
- **Сектор PDM (367)** — сотрудники из другого сектора

Необходимо визуально разделить тикеты, чтобы было понятно, какие тикеты обрабатываются сотрудниками сектора 1С, а какие — сотрудниками других секторов.

---

## 🏗️ Модули и компоненты

### Новые файлы:
- `vue-app/src/services/dashboard-sector-1c/utils/sector-constants.js` — константы секторов
- `vue-app/src/services/dashboard-sector-1c/utils/sector-helper.js` — утилиты для определения сектора сотрудника

### Изменяемые файлы:
- `vue-app/src/services/dashboard-sector-1c/utils/constants.js` — добавление констант секторов
- `vue-app/src/services/dashboard-sector-1c/mappers/employee-mapper.js` — добавление поля `sectorId` в маппинг сотрудника
- `vue-app/src/services/dashboard-sector-1c/groupers/ticket-grouper.js` — разделение тикетов на группы внутри/вне сектора
- `vue-app/src/components/dashboard/DashboardStage.vue` — обновление отображения количества тикетов с разделением
- `vue-app/src/components/dashboard/EmployeeColumn.vue` — разделение тикетов внутри колонки сотрудника

---

## 🔗 Зависимости

- **От:** TASK-005 (базовая реализация дашборда)
- **От:** TASK-006 (исключение хранителя из колонок)
- **Использует:** Bitrix24 REST API `user.get` для получения данных сотрудников (поле `UF_DEPARTMENT`)

---

## 📝 Ступенчатые подзадачи

### Подзадача 1: Создание констант секторов

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/sector-constants.js`

**Описание:** Создать файл с константами секторов и хранителя.

**Требования:**
- Определить константы для всех секторов:
  - `SECTOR_1C_ID = 366` — сектор 1С
  - `SECTOR_HARDWARE_ID = 368` — сектор Железо
  - `SECTOR_BITRIX24_ID = 369` — сектор Битрикс24
  - `SECTOR_PDM_ID = 367` — сектор PDM
  - `KEEPER_USER_ID = 1051` — хранитель нулевой очереди
- Экспортировать константы для использования в других модулях
- Добавить комментарии с описанием каждого сектора

**Критерии приёмки:**
- [ ] Файл создан с константами всех секторов
- [ ] Константы экспортированы
- [ ] Добавлены комментарии с описанием

---

### Подзадача 2: Создание утилиты для определения сектора сотрудника

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/sector-helper.js`

**Описание:** Создать утилиту для определения, к какому сектору принадлежит сотрудник.

**Требования:**
- Создать функцию `getEmployeeSectorId(employee)`:
  - Принимает объект сотрудника из Bitrix24 (с полем `UF_DEPARTMENT`)
  - Возвращает ID сектора (366, 368, 369, 367) или `null`, если сектор не определён
  - Учитывает, что `UF_DEPARTMENT` может быть массивом или числом
- Создать функцию `isEmployeeFromSector1C(employee)`:
  - Проверяет, принадлежит ли сотрудник к сектору 1С (366)
  - Возвращает `true` или `false`
- Создать функцию `isEmployeeFromOtherSector(employee)`:
  - Проверяет, принадлежит ли сотрудник к другому сектору (не 1С и не хранитель)
  - Возвращает `true` или `false`

**Пример использования:**
```javascript
import { getEmployeeSectorId, isEmployeeFromSector1C } from './sector-helper.js';

const employee = { ID: 123, UF_DEPARTMENT: [366] };
const sectorId = getEmployeeSectorId(employee); // 366
const isFrom1C = isEmployeeFromSector1C(employee); // true
```

**Критерии приёмки:**
- [ ] Функция `getEmployeeSectorId()` корректно определяет сектор
- [ ] Функция `isEmployeeFromSector1C()` корректно проверяет принадлежность к сектору 1С
- [ ] Функция `isEmployeeFromOtherSector()` корректно определяет сотрудников других секторов
- [ ] Обработаны случаи, когда `UF_DEPARTMENT` — массив или число
- [ ] Добавлены комментарии JSDoc

---

### Подзадача 3: Обновление маппера сотрудников

**Файл:** `vue-app/src/services/dashboard-sector-1c/mappers/employee-mapper.js`

**Описание:** Добавить поле `sectorId` в маппинг сотрудника для хранения информации о секторе.

**Требования:**
- Импортировать `getEmployeeSectorId` из `sector-helper.js`
- В функции `mapEmployee()` добавить поле `sectorId`:
  ```javascript
  sectorId: getEmployeeSectorId(bitrixUser)
  ```
- Сохранить исходные данные `UF_DEPARTMENT` в поле `departmentId` (для отладки)

**Пример результата:**
```javascript
{
  id: 123,
  name: 'Иван Иванов',
  position: 'Разработчик',
  email: 'ivan@example.com',
  sectorId: 366, // ID сектора
  departmentId: [366], // Исходные данные отдела
  tickets: []
}
```

**Критерии приёмки:**
- [ ] Поле `sectorId` добавлено в маппинг сотрудника
- [ ] Поле `departmentId` сохранено для отладки
- [ ] Импортирована функция `getEmployeeSectorId`
- [ ] Тестирование на сотрудниках из разных секторов

---

### Подзадача 4: Обновление группера тикетов

**Файл:** `vue-app/src/services/dashboard-sector-1c/groupers/ticket-grouper.js`

**Описание:** Модифицировать функцию `groupTicketsByStages()` для разделения тикетов на группы внутри/вне сектора.

**Требования:**
- Импортировать `isEmployeeFromSector1C` из `sector-helper.js`
- В функции `groupTicketsByStages()` для каждого сотрудника разделить тикеты на две группы:
  - `tickets` — тикеты внутри сектора (сотрудник из сектора 1С)
  - `ticketsOutsideSector` — тикеты вне сектора (сотрудник из другого сектора)
- Для каждого сотрудника добавить поле `isFromSector1C` (boolean)
- Сохранить обратную совместимость: поле `tickets` должно содержать все тикеты (для существующего кода)

**Структура результата:**
```javascript
{
  id: 'formed',
  name: 'Сформировано обращение',
  color: '#007bff',
  employees: [
    {
      id: 123,
      name: 'Иван Иванов',
      sectorId: 366,
      isFromSector1C: true,
      tickets: [...], // Все тикеты (для обратной совместимости)
      ticketsInsideSector: [...], // Тикеты внутри сектора
      ticketsOutsideSector: [] // Тикеты вне сектора (пусто, т.к. сотрудник из 1С)
    },
    {
      id: 456,
      name: 'Петр Петров',
      sectorId: 368,
      isFromSector1C: false,
      tickets: [...], // Все тикеты
      ticketsInsideSector: [], // Пусто (сотрудник не из 1С)
      ticketsOutsideSector: [...] // Тикеты вне сектора
    }
  ]
}
```

**Критерии приёмки:**
- [ ] Тикеты разделены на группы внутри/вне сектора
- [ ] Добавлено поле `isFromSector1C` для каждого сотрудника
- [ ] Сохранена обратная совместимость (поле `tickets` содержит все тикеты)
- [ ] Логика работает корректно для всех секторов

---

### Подзадача 5: Обновление компонента стадии

**Файл:** `vue-app/src/components/dashboard/DashboardStage.vue`

**Описание:** Обновить отображение количества тикетов в заголовке стадии с разделением на внутри/вне сектора.

**Требования:**
- Создать computed-свойство `ticketsCountInsideSector`:
  - Подсчитывает тикеты внутри сектора (нулевая точка + сотрудники сектора 1С)
- Создать computed-свойство `ticketsCountOutsideSector`:
  - Подсчитывает тикеты вне сектора (сотрудники других секторов)
- Обновить computed-свойство `stageNameWithCount`:
  - Формат: `"Название стадии (N / M)"`, где N — внутри сектора, M — вне сектора
  - Если тикетов вне сектора нет, формат: `"Название стадии (N)"`

**Примеры:**
- `"Сформировано обращение (15 / 3)"` — 15 внутри сектора, 3 вне сектора
- `"Рассмотрение ТЗ (8)"` — 8 внутри сектора, 0 вне сектора

**Критерии приёмки:**
- [ ] Количество тикетов разделено на внутри/вне сектора
- [ ] Формат отображения корректный
- [ ] Если тикетов вне сектора нет, отображается только одно число
- [ ] Тестирование на разных стадиях

---

### Подзадача 6: Обновление компонента колонки сотрудника

**Файл:** `vue-app/src/components/dashboard/EmployeeColumn.vue`

**Описание:** Разделить отображение тикетов в колонке сотрудника на группы внутри/вне сектора.

**Требования:**
- Если сотрудник из сектора 1С (`isFromSector1C === true`):
  - Отображать все тикеты в обычном порядке
  - Не показывать разделение
- Если сотрудник из другого сектора (`isFromSector1C === false`):
  - Отображать тикеты из `ticketsOutsideSector`
  - Добавить визуальное разделение (например, заголовок "Тикеты вне сектора" или другой стиль)
  - Тикеты должны отображаться в конце колонки

**Визуальное решение:**
- Добавить заголовок "Тикеты вне сектора" перед тикетами вне сектора
- Использовать другой цвет фона или границы для визуального разделения
- Или добавить иконку/бейдж "Вне сектора"

**Критерии приёмки:**
- [ ] Тикеты внутри сектора отображаются в обычном порядке
- [ ] Тикеты вне сектора визуально отделены
- [ ] Добавлен заголовок или визуальное разделение
- [ ] Тестирование на сотрудниках из разных секторов

---

### Подзадача 7: Обновление констант

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/constants.js`

**Описание:** Добавить экспорт констант секторов из `sector-constants.js` для удобства использования.

**Требования:**
- Импортировать константы из `sector-constants.js`
- Реэкспортировать их для использования в других модулях
- Добавить комментарии с описанием

**Критерии приёмки:**
- [ ] Константы импортированы и реэкспортированы
- [ ] Добавлены комментарии

---

## 🔧 Технические требования

### Используемые методы Bitrix24 REST API:
- `user.get` — получение данных сотрудников (поле `UF_DEPARTMENT`)
  - Документация: https://context7.com/bitrix24/rest/user.get

### Структура данных сотрудника из Bitrix24:
```javascript
{
  ID: 123,
  NAME: 'Иван',
  LAST_NAME: 'Иванов',
  EMAIL: 'ivan@example.com',
  WORK_POSITION: 'Разработчик',
  UF_DEPARTMENT: [366] // Массив ID отделов или одно число
}
```

### Структура данных тикета:
```javascript
{
  id: 12345,
  assignedById: 123, // ID ответственного сотрудника
  stageId: 'DT140_12:UC_0VHWE2',
  // ... другие поля
}
```

---

## ✅ Критерии приёмки (общие)

- [ ] Все подзадачи выполнены последовательно
- [ ] Константы секторов определены и используются
- [ ] Тикеты корректно разделяются на группы внутри/вне сектора
- [ ] В заголовке стадии отображается количество с разделением
- [ ] Тикеты вне сектора визуально отделены в колонках сотрудников
- [ ] Обратная совместимость сохранена (поле `tickets` содержит все тикеты)
- [ ] Код соответствует стандартам проекта
- [ ] Добавлены комментарии JSDoc
- [ ] Протестировано на сотрудниках из разных секторов
- [ ] Нет ошибок в консоли браузера

---

## 🧪 Тестирование

### Тест 1: Сотрудник из сектора 1С
1. Открыть дашборд
2. Проверить, что тикеты сотрудника из сектора 1С отображаются в обычном порядке
3. Проверить, что в заголовке стадии отображается только одно число (если нет тикетов вне сектора)

### Тест 2: Сотрудник из другого сектора
1. Открыть дашборд
2. Проверить, что тикеты сотрудника из другого сектора отображаются с визуальным разделением
3. Проверить, что в заголовке стадии отображается два числа (N / M)

### Тест 3: Смешанная ситуация
1. Открыть дашборд с тикетами от сотрудников разных секторов
2. Проверить, что тикеты внутри сектора отображаются первыми
3. Проверить, что тикеты вне сектора отображаются в конце
4. Проверить корректность подсчёта в заголовке стадии

### Тест 4: Нулевая точка
1. Проверить, что тикеты в нулевой точке учитываются в подсчёте внутри сектора
2. Проверить, что хранитель (1051) не отображается в колонках

---

## 📚 Связанные документы

- `DOCS/ARCHITECTURE/cache-mechanism-dashboard-sector-1c.md` — механизм кеширования
- `DOCS/ARCHITECTURE/dashboard-sector-1c.md` — архитектура дашборда
- `DOCS/TASKS/TASK-005-dashboard-sector-1c.md` — базовая реализация дашборда
- `DOCS/TASKS/TASK-006-zero-point-keeper-objects.md` — исключение хранителя из колонок

---

## 📝 История правок

- **2025-12-06 13:22 (UTC+3, Брест):** Создана задача TASK-008

---

**Автор:** Технический писатель  
**Статус:** Новая

