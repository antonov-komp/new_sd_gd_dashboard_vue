# TASK-034-01: Анализ структуры данных и подготовка контекста перехода

**Дата создания:** 2025-12-12 11:51 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-034 (Этап 1)

---

## 📋 Описание

Провести детальный анализ структуры данных для уровня 4 попапа, определить контекст перехода из предыдущих уровней и создать функцию фильтрации тикетов по контексту. Эта задача является первым этапом реализации уровня 4 — списка тикетов в попапе детализации.

---

## 🎯 Цель

1. Изучить структуру данных уровней 1, 2 и 3
2. Определить контекст перехода на уровень 4 из всех возможных источников
3. Создать функцию фильтрации тикетов по контексту перехода
4. Обеспечить корректную передачу данных между уровнями

---

## 🔍 Контекст задачи

### Текущая структура попапа

**Компонент:** `EmployeeDetailsModal.vue`  
**Расположение:** `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`

**Существующие уровни:**
- **Уровень 1:** Список сотрудников стадии (с переключателями: По сотрудникам, По времени, По заказчикам)
- **Уровень 2:** Временные градации тикетов сотрудника
- **Уровень 3:** Детализация по заказчикам (отделам) для каждой временной градации

**Новый уровень:**
- **Уровень 4:** Список тикетов в виде таблицы с карточками (идентично канбан-режиму)

### Источники данных

**Слепок (snapshot):**
- Содержит все тикеты стадии с полными данными
- Структура: `snapshot.tickets` — массив тикетов
- Поля тикета: `id`, `title`, `assignedTo`, `createdAt`, `updatedAt`, `stageId`, `departmentHead`, `departmentHeadFull`, `priorityId`, `priorityLabel`, `service`, `serviceLabel`

**Утилиты навигации:**
- `popupNavigationUtils.js` — функции для работы с данными попапа
- `getEmployeeTicketsForStage()` — получение тикетов сотрудника на стадии
- `groupTicketsByDateCategory()` — группировка по временным градациям
- `groupTicketsByDepartment()` — группировка по заказчикам

---

## 📝 Задачи

### Задача 1.1: Анализ существующих данных

**Цель:** Изучить структуру данных уровней 1, 2 и 3 для понимания, какие тикеты доступны в каждом контексте.

#### 1.1.1. Изучение структуры уровня 1

**Файлы для изучения:**
- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа
- `vue-app/src/utils/graph-state/popupNavigationUtils.js` — утилиты навигации

**Действия:**
1. Изучить структуру `level1Data`:
   ```javascript
   {
     stageName: String,              // Название стадии
     stageId: String,                // ID стадии ('formed', 'review', 'execution')
     totalCount: Number,              // Общее количество тикетов стадии
     employees: Array,                // Массив сотрудников
     others: Object | null,           // Группа "Другие"
     snapshot: Object,                // Слепок с данными
     ticketDetails: Object | null     // Детали тикетов (если загружены)
   }
   ```

2. Изучить структуру данных для видов отображения:
   - `level1TimeCategories` — категории по времени (все тикеты стадии)
   - `level1Departments` — заказчики (все тикеты стадии)

3. Определить, какие тикеты доступны на уровне 1:
   - Все тикеты стадии из `snapshot.tickets`
   - Фильтрация по `stageId` (текущая стадия)

**Ожидаемый результат:**
- Понимание структуры данных уровня 1
- Список доступных тикетов на уровне 1
- Документация структуры `level1Data`

#### 1.1.2. Изучение структуры уровня 2

**Действия:**
1. Изучить структуру `level2Data`:
   ```javascript
   {
     employeeId: Number,             // ID сотрудника
     employeeName: String,           // Имя сотрудника
     stageId: String,                // ID стадии
     stageName: String,              // Название стадии
     totalCount: Number,             // Общее количество тикетов сотрудника
     dateCategories: Array,           // Массив категорий по времени
     snapshot: Object,                // Слепок с данными
     ticketDetails: Object | null    // Детали тикетов
   }
   ```

2. Изучить структуру категории по времени:
   ```javascript
   {
     category: String,               // Ключ категории ('today', 'yesterday', ...)
     label: String,                  // Название категории ('СЕГОДНЯ', 'ВЧЕРА', ...)
     count: Number,                  // Количество тикетов
     tickets: Array,                 // Массив тикетов в категории
     percentage: Number,             // Процент от общего количества
     progressBarWidth: Number,       // Ширина прогресс-бара
     progressBarColor: String        // Цвет прогресс-бара
   }
   ```

3. Определить, какие тикеты доступны на уровне 2:
   - Тикеты сотрудника на стадии (через `getEmployeeTicketsForStage()`)
   - Группировка по временным градациям (через `groupTicketsByDateCategory()`)

**Ожидаемый результат:**
- Понимание структуры данных уровня 2
- Список доступных тикетов на уровне 2
- Документация структуры `level2Data` и категорий

#### 1.1.3. Изучение структуры уровня 3

**Действия:**
1. Изучить структуру `level3Data`:
   ```javascript
   {
     employeeId: Number,             // ID сотрудника
     employeeName: String,           // Имя сотрудника
     stageId: String,                // ID стадии
     stageName: String,              // Название стадии
     dateCategory: String,           // Ключ категории по времени
     dateCategoryLabel: String,      // Название категории по времени
     totalCount: Number,             // Общее количество тикетов
     departments: Array,             // Массив заказчиков
     snapshot: Object,                // Слепок с данными
     ticketDetails: Object | null    // Детали тикетов
   }
   ```

2. Изучить структуру заказчика:
   ```javascript
   {
     departmentName: String,        // Название заказчика
     count: Number                  // Количество тикетов
   }
   ```

3. Определить, какие тикеты доступны на уровне 3:
   - Тикеты сотрудника в выбранной временной градации
   - Группировка по заказчикам (через `groupTicketsByDepartment()`)

**Ожидаемый результат:**
- Понимание структуры данных уровня 3
- Список доступных тикетов на уровне 3
- Документация структуры `level3Data` и заказчиков

#### 1.1.4. Проверка доступности полей тикетов

**Действия:**
1. Проверить структуру тикета в snapshot:
   ```javascript
   {
     id: Number,                    // ✅ Есть
     title: String,                 // ✅ Есть
     assignedTo: Object,             // ✅ Есть
     createdAt: String,              // ✅ Есть
     updatedAt: String,              // ✅ Есть
     stageId: String | null,        // ✅ Есть (после оптимизации TASK-033)
     departmentHead: String | null,  // ✅ Есть (после оптимизации TASK-033)
     departmentHeadFull: String | null, // ✅ Есть (после оптимизации TASK-033)
     priorityId: String | null,     // ✅ Есть (после оптимизации TASK-033)
     priorityLabel: String | null,  // ✅ Есть (после оптимизации TASK-033)
     service: Object | null,        // ✅ Есть (после оптимизации TASK-033)
     serviceLabel: String | null    // ✅ Есть (после оптимизации TASK-033)
   }
   ```

2. Проверить, какие поля требуются для `TicketCard.vue`:
   - Изучить props компонента `TicketCard.vue`
   - Составить список обязательных и опциональных полей
   - Определить, какие поля могут отсутствовать

**Ожидаемый результат:**
- Список полей тикета в snapshot
- Список полей, требуемых для `TicketCard.vue`
- Список отсутствующих полей (если есть)

**Критерии приёмки задачи 1.1:**
- [ ] Структура данных уровня 1 изучена и задокументирована
- [ ] Структура данных уровня 2 изучена и задокументирована
- [ ] Структура данных уровня 3 изучена и задокументирована
- [ ] Проверена доступность всех полей тикетов
- [ ] Составлен список полей, требуемых для `TicketCard.vue`

---

### Задача 1.2: Определение контекста перехода

**Цель:** Создать структуру контекста перехода на уровень 4 из всех возможных источников.

#### 1.2.1. Анализ вариантов перехода на уровень 4

**Варианты перехода:**

1. **Из уровня 2 (Временные градации):**
   - Триггер: Клик на строку временной градации
   - Контекст: Тикеты сотрудника в выбранной временной градации
   - Данные: `level2Data.dateCategories[selectedCategory].tickets`

2. **Из уровня 3 (Заказчики):**
   - Триггер: Клик на строку заказчика
   - Контекст: Тикеты сотрудника у выбранного заказчика в выбранной временной градации
   - Данные: Нужно фильтровать тикеты из `level3Data` по заказчику

3. **Из уровня 1 (По времени):**
   - Триггер: Клик на временную градацию в виде "По времени"
   - Контекст: Все тикеты стадии в выбранной временной градации
   - Данные: `level1TimeCategories[selectedCategory].tickets`

4. **Из уровня 1 (По заказчикам):**
   - Триггер: Клик на строку заказчика в виде "По заказчикам"
   - Контекст: Все тикеты стадии у выбранного заказчика
   - Данные: Нужно фильтровать тикеты стадии по заказчику

#### 1.2.2. Создание структуры контекста перехода

**Файл:** `vue-app/src/utils/graph-state/ticketListUtils.js` (создать новый)

**Структура контекста:**
```javascript
/**
 * Контекст перехода на уровень 4 (список тикетов)
 * 
 * @typedef {Object} Level4Context
 * @property {1|2|3} sourceLevel - Уровень, с которого переходим (1, 2, или 3)
 * @property {Number|null} employeeId - ID сотрудника (если есть)
 * @property {String|null} employeeName - Имя сотрудника (если есть)
 * @property {String} stageId - ID стадии ('formed', 'review', 'execution')
 * @property {String} stageName - Название стадии
 * @property {String|null} dateCategory - Ключ категории давности (если есть)
 * @property {String|null} dateCategoryLabel - Название категории давности (если есть)
 * @property {String|null} departmentName - Название заказчика (если есть)
 * @property {Array} tickets - Массив тикетов для отображения
 * @property {Object} snapshot - Слепок с данными
 * @property {Object|null} ticketDetails - Детали тикетов (если загружены)
 */
```

**Примеры контекстов:**

1. **Из уровня 2:**
```javascript
{
  sourceLevel: 2,
  employeeId: 456,
  employeeName: 'Иван Иванов',
  stageId: 'formed',
  stageName: 'Сформировано обращение',
  dateCategory: 'today',
  dateCategoryLabel: 'СЕГОДНЯ',
  departmentName: null,
  tickets: [...], // Тикеты сотрудника в категории 'today'
  snapshot: {...},
  ticketDetails: null
}
```

2. **Из уровня 3:**
```javascript
{
  sourceLevel: 3,
  employeeId: 456,
  employeeName: 'Иван Иванов',
  stageId: 'formed',
  stageName: 'Сформировано обращение',
  dateCategory: 'today',
  dateCategoryLabel: 'СЕГОДНЯ',
  departmentName: 'Отдел продаж',
  tickets: [...], // Тикеты сотрудника у заказчика 'Отдел продаж' в категории 'today'
  snapshot: {...},
  ticketDetails: null
}
```

3. **Из уровня 1 (По времени):**
```javascript
{
  sourceLevel: 1,
  employeeId: null,
  employeeName: null,
  stageId: 'formed',
  stageName: 'Сформировано обращение',
  dateCategory: 'today',
  dateCategoryLabel: 'СЕГОДНЯ',
  departmentName: null,
  tickets: [...], // Все тикеты стадии в категории 'today'
  snapshot: {...},
  ticketDetails: null
}
```

4. **Из уровня 1 (По заказчикам):**
```javascript
{
  sourceLevel: 1,
  employeeId: null,
  employeeName: null,
  stageId: 'formed',
  stageName: 'Сформировано обращение',
  dateCategory: null,
  dateCategoryLabel: null,
  departmentName: 'Отдел продаж',
  tickets: [...], // Все тикеты стадии у заказчика 'Отдел продаж'
  snapshot: {...},
  ticketDetails: null
}
```

#### 1.2.3. Функции создания контекста

**Функция 1: Создание контекста из уровня 2**
```javascript
/**
 * Создать контекст перехода на уровень 4 из уровня 2
 * 
 * @param {Object} level2Data - Данные уровня 2
 * @param {Object} category - Объект категории по времени
 * @returns {Level4Context} Контекст перехода
 */
export function createContextFromLevel2(level2Data, category) {
  return {
    sourceLevel: 2,
    employeeId: level2Data.employeeId,
    employeeName: level2Data.employeeName,
    stageId: level2Data.stageId,
    stageName: level2Data.stageName,
    dateCategory: category.category,
    dateCategoryLabel: category.label,
    departmentName: null,
    tickets: category.tickets || [],
    snapshot: level2Data.snapshot,
    ticketDetails: level2Data.ticketDetails
  };
}
```

**Функция 2: Создание контекста из уровня 3**
```javascript
/**
 * Создать контекст перехода на уровень 4 из уровня 3
 * 
 * @param {Object} level3Data - Данные уровня 3
 * @param {Object} department - Объект заказчика
 * @returns {Level4Context} Контекст перехода
 */
export function createContextFromLevel3(level3Data, department) {
  // Нужно получить тикеты сотрудника у выбранного заказчика
  // в выбранной временной градации
  // Это будет реализовано в функции фильтрации
  
  return {
    sourceLevel: 3,
    employeeId: level3Data.employeeId,
    employeeName: level3Data.employeeName,
    stageId: level3Data.stageId,
    stageName: level3Data.stageName,
    dateCategory: level3Data.dateCategory,
    dateCategoryLabel: level3Data.dateCategoryLabel,
    departmentName: department.departmentName,
    tickets: [], // Будет заполнено в функции фильтрации
    snapshot: level3Data.snapshot,
    ticketDetails: level3Data.ticketDetails
  };
}
```

**Функция 3: Создание контекста из уровня 1 (По времени)**
```javascript
/**
 * Создать контекст перехода на уровень 4 из уровня 1 (По времени)
 * 
 * @param {Object} level1Data - Данные уровня 1
 * @param {Object} category - Объект категории по времени
 * @returns {Level4Context} Контекст перехода
 */
export function createContextFromLevel1Time(level1Data, category) {
  return {
    sourceLevel: 1,
    employeeId: null,
    employeeName: null,
    stageId: level1Data.stageId,
    stageName: level1Data.stageName,
    dateCategory: category.category,
    dateCategoryLabel: category.label,
    departmentName: null,
    tickets: category.tickets || [],
    snapshot: level1Data.snapshot,
    ticketDetails: level1Data.ticketDetails
  };
}
```

**Функция 4: Создание контекста из уровня 1 (По заказчикам)**
```javascript
/**
 * Создать контекст перехода на уровень 4 из уровня 1 (По заказчикам)
 * 
 * @param {Object} level1Data - Данные уровня 1
 * @param {Object} department - Объект заказчика
 * @returns {Level4Context} Контекст перехода
 */
export function createContextFromLevel1Department(level1Data, department) {
  // Нужно получить тикеты стадии у выбранного заказчика
  // Это будет реализовано в функции фильтрации
  
  return {
    sourceLevel: 1,
    employeeId: null,
    employeeName: null,
    stageId: level1Data.stageId,
    stageName: level1Data.stageName,
    dateCategory: null,
    dateCategoryLabel: null,
    departmentName: department.departmentName,
    tickets: [], // Будет заполнено в функции фильтрации
    snapshot: level1Data.snapshot,
    ticketDetails: level1Data.ticketDetails
  };
}
```

**Критерии приёмки задачи 1.2:**
- [ ] Структура контекста перехода определена
- [ ] Функции создания контекста реализованы
- [ ] Все варианты перехода обработаны
- [ ] Документация структуры контекста создана

---

### Задача 1.3: Функция фильтрации тикетов по контексту

**Цель:** Создать функцию фильтрации тикетов по контексту перехода для всех вариантов.

#### 1.3.1. Основная функция фильтрации

**Файл:** `vue-app/src/utils/graph-state/ticketListUtils.js`

**Функция:**
```javascript
/**
 * Фильтровать тикеты по контексту перехода на уровень 4
 * 
 * Объединяет данные из snapshot и загруженных деталей, затем фильтрует
 * тикеты по параметрам контекста (сотрудник, стадия, временная градация, заказчик).
 * 
 * @param {Level4Context} context - Контекст перехода
 * @returns {Promise<Array>} Массив отфильтрованных тикетов
 * 
 * @example
 * const context = {
 *   sourceLevel: 2,
 *   employeeId: 456,
 *   stageId: 'formed',
 *   dateCategory: 'today',
 *   departmentName: null,
 *   tickets: [],
 *   snapshot: {...}
 * };
 * const tickets = await filterTicketsByContext(context);
 */
export async function filterTicketsByContext(context) {
  if (!context || !context.snapshot) {
    console.warn('[ticketListUtils] Invalid context or missing snapshot');
    return [];
  }

  const { snapshot, stageId, employeeId, dateCategory, departmentName } = context;

  // Получить все тикеты стадии из snapshot
  let tickets = snapshot.tickets || [];
  
  // Фильтровать по стадии
  if (stageId) {
    tickets = filterTicketsByStage(tickets, stageId);
  }

  // Фильтровать по сотруднику (если указан)
  if (employeeId) {
    tickets = filterTicketsByEmployee(tickets, employeeId);
  }

  // Фильтровать по временной градации (если указана)
  if (dateCategory) {
    tickets = filterTicketsByDateCategory(tickets, dateCategory);
  }

  // Фильтровать по заказчику (если указан)
  if (departmentName) {
    tickets = filterTicketsByDepartment(tickets, departmentName);
  }

  return tickets;
}
```

#### 1.3.2. Вспомогательные функции фильтрации

**Функция 1: Фильтрация по стадии**
```javascript
/**
 * Фильтровать тикеты по стадии
 * 
 * @param {Array} tickets - Массив тикетов
 * @param {String} stageId - ID стадии ('formed', 'review', 'execution')
 * @returns {Array} Отфильтрованные тикеты
 */
function filterTicketsByStage(tickets, stageId) {
  if (!tickets || tickets.length === 0) {
    return [];
  }

  return tickets.filter(ticket => {
    // Если stageId не определен в тикете, пропускаем его
    if (!ticket.stageId) {
      return false;
    }

    // Преобразовать Bitrix24 stageId во внутренний формат
    const internalStageId = mapStageId(ticket.stageId);
    
    // Сравнить с переданным stageId (уже во внутреннем формате)
    return internalStageId === stageId;
  });
}
```

**Функция 2: Фильтрация по сотруднику**
```javascript
/**
 * Фильтровать тикеты по сотруднику
 * 
 * @param {Array} tickets - Массив тикетов
 * @param {Number} employeeId - ID сотрудника
 * @returns {Array} Отфильтрованные тикеты
 */
function filterTicketsByEmployee(tickets, employeeId) {
  if (!tickets || tickets.length === 0) {
    return [];
  }

  return tickets.filter(ticket => {
    const assignedTo = ticket.assignedTo;
    if (!assignedTo) {
      return false;
    }
    
    const assignedToId = typeof assignedTo === 'object' ? assignedTo.id : assignedTo;
    return assignedToId === employeeId;
  });
}
```

**Функция 3: Фильтрация по временной градации**
```javascript
/**
 * Фильтровать тикеты по временной градации
 * 
 * @param {Array} tickets - Массив тикетов
 * @param {String} dateCategory - Ключ категории давности ('today', 'yesterday', ...)
 * @returns {Array} Отфильтрованные тикеты
 */
function filterTicketsByDateCategory(tickets, dateCategory) {
  if (!tickets || tickets.length === 0) {
    return [];
  }

  const currentDate = new Date();

  return tickets.filter(ticket => {
    if (!ticket.createdAt) {
      // Если нет даты создания, относим к категории "БОЛЕЕ ГОДА"
      return dateCategory === DATE_ACCENT_CATEGORIES.MORE_THAN_YEAR;
    }
    
    // Определение категории давности
    const ticketCategory = getDateAccentCategory(ticket.createdAt, currentDate);
    
    return ticketCategory === dateCategory;
  });
}
```

**Функция 4: Фильтрация по заказчику**
```javascript
/**
 * Фильтровать тикеты по заказчику
 * 
 * @param {Array} tickets - Массив тикетов
 * @param {String} departmentName - Название заказчика
 * @returns {Array} Отфильтрованные тикеты
 */
function filterTicketsByDepartment(tickets, departmentName) {
  if (!tickets || tickets.length === 0) {
    return [];
  }

  return tickets.filter(ticket => {
    // Извлечение значения заказчика
    // Используем departmentHeadFull (полное значение) или departmentHead
    let ticketDepartmentName = ticket.departmentHeadFull || ticket.departmentHead;
    
    // Нормализация значения
    if (!ticketDepartmentName) {
      ticketDepartmentName = 'Без заказчика';
    } else {
      ticketDepartmentName = String(ticketDepartmentName).trim();
      if (ticketDepartmentName.length === 0) {
        ticketDepartmentName = 'Без заказчика';
      }
    }
    
    // Сравнение с переданным названием заказчика
    return ticketDepartmentName === departmentName;
  });
}
```

#### 1.3.3. Обработка случаев, когда тикеты уже переданы в контексте

**Улучшение функции `filterTicketsByContext`:**
```javascript
export async function filterTicketsByContext(context) {
  if (!context || !context.snapshot) {
    console.warn('[ticketListUtils] Invalid context or missing snapshot');
    return [];
  }

  // Если тикеты уже переданы в контексте (из уровня 2 или уровня 1 По времени),
  // используем их напрямую, но проверяем дополнительные фильтры
  if (context.tickets && context.tickets.length > 0) {
    let tickets = context.tickets;

    // Если указан заказчик, дополнительно фильтруем по нему
    if (context.departmentName) {
      tickets = filterTicketsByDepartment(tickets, context.departmentName);
    }

    return tickets;
  }

  // Если тикеты не переданы, фильтруем из snapshot
  const { snapshot, stageId, employeeId, dateCategory, departmentName } = context;

  let tickets = snapshot.tickets || [];
  
  // Применяем фильтры последовательно
  if (stageId) {
    tickets = filterTicketsByStage(tickets, stageId);
  }

  if (employeeId) {
    tickets = filterTicketsByEmployee(tickets, employeeId);
  }

  if (dateCategory) {
    tickets = filterTicketsByDateCategory(tickets, dateCategory);
  }

  if (departmentName) {
    tickets = filterTicketsByDepartment(tickets, departmentName);
  }

  return tickets;
}
```

#### 1.3.4. Обновление функций создания контекста

**Обновление функции `createContextFromLevel3`:**
```javascript
export async function createContextFromLevel3(level3Data, department) {
  // Получить тикеты сотрудника в выбранной временной градации
  // из snapshot или из level3Data (если доступны)
  
  // Сначала получаем тикеты сотрудника на стадии
  const employeeTickets = await getEmployeeTicketsForStage(
    level3Data.employeeId,
    level3Data.stageId,
    level3Data.snapshot,
    level3Data.ticketDetails
  );

  // Фильтруем по временной градации
  const dateCategoryTickets = filterTicketsByDateCategory(
    employeeTickets,
    level3Data.dateCategory
  );

  // Фильтруем по заказчику
  const departmentTickets = filterTicketsByDepartment(
    dateCategoryTickets,
    department.departmentName
  );

  return {
    sourceLevel: 3,
    employeeId: level3Data.employeeId,
    employeeName: level3Data.employeeName,
    stageId: level3Data.stageId,
    stageName: level3Data.stageName,
    dateCategory: level3Data.dateCategory,
    dateCategoryLabel: level3Data.dateCategoryLabel,
    departmentName: department.departmentName,
    tickets: departmentTickets,
    snapshot: level3Data.snapshot,
    ticketDetails: level3Data.ticketDetails
  };
}
```

**Обновление функции `createContextFromLevel1Department`:**
```javascript
export async function createContextFromLevel1Department(level1Data, department) {
  // Получить все тикеты стадии из snapshot
  const allStageTickets = (level1Data.snapshot?.tickets || []).filter(ticket => {
    if (!ticket.stageId) {
      return false;
    }
    const internalStageId = mapStageId(ticket.stageId);
    return internalStageId === level1Data.stageId;
  });

  // Фильтруем по заказчику
  const departmentTickets = filterTicketsByDepartment(
    allStageTickets,
    department.departmentName
  );

  return {
    sourceLevel: 1,
    employeeId: null,
    employeeName: null,
    stageId: level1Data.stageId,
    stageName: level1Data.stageName,
    dateCategory: null,
    dateCategoryLabel: null,
    departmentName: department.departmentName,
    tickets: departmentTickets,
    snapshot: level1Data.snapshot,
    ticketDetails: level1Data.ticketDetails
  };
}
```

**Критерии приёмки задачи 1.3:**
- [ ] Основная функция фильтрации реализована
- [ ] Все вспомогательные функции фильтрации реализованы
- [ ] Обработка случаев с предзагруженными тикетами работает
- [ ] Функции создания контекста обновлены
- [ ] Все варианты перехода обработаны корректно
- [ ] Тесты для всех функций написаны

---

## 📚 Зависимости

### Существующие файлы

- `vue-app/src/utils/graph-state/popupNavigationUtils.js` — утилиты навигации
- `vue-app/src/services/dashboard-sector-1c/utils/date-utils.js` — функции для работы с датами
- `vue-app/src/services/dashboard-sector-1c/utils/date-accent-config.js` — конфигурация временных градаций
- `vue-app/src/services/dashboard-sector-1c/mappers/stage-mapper.js` — маппер стадий

### Новые файлы

- `vue-app/src/utils/graph-state/ticketListUtils.js` — утилиты для работы со списком тикетов (создать)

### Импорты

```javascript
import { getDateAccentCategory } from '@/services/dashboard-sector-1c/utils/date-utils.js';
import { DATE_ACCENT_CATEGORIES } from '@/services/dashboard-sector-1c/utils/date-accent-config.js';
import { mapStageId } from '@/services/dashboard-sector-1c/mappers/stage-mapper.js';
import { getEmployeeTicketsForStage } from '@/utils/graph-state/popupNavigationUtils.js';
```

---

## ✅ Критерии приёмки этапа 1

- [x] Структура данных уровней 1, 2 и 3 изучена и задокументирована
- [x] Проверена доступность всех полей тикетов в snapshot
- [x] Структура контекста перехода определена и задокументирована
- [x] Функции создания контекста реализованы для всех вариантов перехода
- [x] Основная функция фильтрации тикетов реализована
- [x] Все вспомогательные функции фильтрации реализованы
- [x] Обработка всех вариантов перехода работает корректно
- [x] Код соответствует стандартам проекта
- [x] Добавлены комментарии JSDoc для всех функций
- [x] Логирование добавлено для отладки

**Статус:** ✅ Завершена (2025-12-12, UTC+3, Брест)

---

## 🧪 Тестирование

### Тест 1: Фильтрация из уровня 2

**Сценарий:**
1. Создать контекст из уровня 2 с категорией 'today'
2. Вызвать `filterTicketsByContext()`
3. Проверить, что возвращаются только тикеты сотрудника в категории 'today'

**Ожидаемый результат:**
- Массив тикетов содержит только тикеты сотрудника
- Все тикеты имеют `createdAt` в категории 'today'
- Количество тикетов соответствует ожидаемому

### Тест 2: Фильтрация из уровня 3

**Сценарий:**
1. Создать контекст из уровня 3 с заказчиком 'Отдел продаж'
2. Вызвать `filterTicketsByContext()`
3. Проверить, что возвращаются только тикеты сотрудника у заказчика в выбранной градации

**Ожидаемый результат:**
- Массив тикетов содержит только тикеты сотрудника
- Все тикеты имеют `departmentHead` или `departmentHeadFull` = 'Отдел продаж'
- Все тикеты в выбранной временной градации

### Тест 3: Фильтрация из уровня 1 (По времени)

**Сценарий:**
1. Создать контекст из уровня 1 с категорией 'today'
2. Вызвать `filterTicketsByContext()`
3. Проверить, что возвращаются все тикеты стадии в категории 'today'

**Ожидаемый результат:**
- Массив тикетов содержит тикеты всех сотрудников стадии
- Все тикеты в категории 'today'
- Количество тикетов соответствует ожидаемому

### Тест 4: Фильтрация из уровня 1 (По заказчикам)

**Сценарий:**
1. Создать контекст из уровня 1 с заказчиком 'Отдел продаж'
2. Вызвать `filterTicketsByContext()`
3. Проверить, что возвращаются все тикеты стадии у заказчика

**Ожидаемый результат:**
- Массив тикетов содержит тикеты всех сотрудников стадии
- Все тикеты имеют `departmentHead` или `departmentHeadFull` = 'Отдел продаж'
- Количество тикетов соответствует ожидаемому

---

## 📝 История правок

- **2025-12-12 11:51 (UTC+3, Брест):** Создана задача TASK-034-01
  - Определены 3 подзадачи с детальными шагами
  - Добавлены примеры кода и структуры данных
  - Описаны критерии приёмки и тесты

- **2025-12-12 (UTC+3, Брест):** Выполнена задача TASK-034-01
  - Создан файл `ticketListUtils.js` с функциями создания контекста и фильтрации
  - Реализованы функции создания контекста для всех вариантов перехода:
    - `createContextFromLevel2()` - из уровня 2
    - `createContextFromLevel3()` - из уровня 3 (асинхронная, с фильтрацией)
    - `createContextFromLevel1Time()` - из уровня 1 (По времени)
    - `createContextFromLevel1Department()` - из уровня 1 (По заказчикам)
  - Реализована основная функция `filterTicketsByContext()` с обработкой предзагруженных тикетов
  - Реализованы вспомогательные функции фильтрации:
    - `filterTicketsByStage()` - по стадии
    - `filterTicketsByEmployee()` - по сотруднику
    - `filterTicketsByDateCategory()` - по временной градации
    - `filterTicketsByDepartment()` - по заказчику
  - Добавлены JSDoc комментарии для всех функций
  - Добавлено логирование для отладки
  - Обновлены вызовы функций в компоненте `EmployeeDetailsModal.vue`

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)



