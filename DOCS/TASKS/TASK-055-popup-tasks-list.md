# TASK-055: Добавление кнопки "Список задач" и уровня 2 в попап детализации трудозатрат

**Дата создания:** 2025-12-17 16:42 (UTC+3, Брест)  
**Дата обновления:** 2025-12-17 17:30 (UTC+3, Брест)  
**Статус:** Детализирован  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связь с задачей:** Расширение функциональности TASK-050-07 (попап детализации)

**Ключевые решения:**
- ✅ Формат отображения: **карточки** (вместо таблицы) для лучшей масштабируемости
- ✅ Статус задачи: поле `STATUS` будет добавлено **позже** (отдельное поле с числовыми значениями)
- ✅ Пагинация: реализована для случаев, когда задач **больше 10**
- ✅ Backend расширение: обязательно расширить backend API
- ⏳ Открытие задачи: клик на карточку будет открывать задачу в Bitrix24 (реализуем позже)

---

## Цель

Добавить кнопку "Список задач" в попап детализации трудозатрат (`TimeTrackingDetailModal.vue`) и реализовать переход на уровень 2 со списком задач. В списке задач отображать детальную информацию о каждой задаче: название, номер, даты (начала, дедлайна, завершения) и статус.

---

## Контекст

- **Текущее состояние:**
  - В попапе `TimeTrackingDetailModal.vue` отображается краткая информация о задачах и связанных тикетах
  - Задачи получаются на уровне трудозатраты (из `cellData.week.employees[].tasks[]`)
  - Текущий попап показывает только: номер задачи, трудозатрату и связанный тикет
  - Нет детальной информации о задачах (даты, статусы)

- **Проблема:**
  - Недостаточно информации о задачах для детального анализа
  - Нет возможности просмотреть все задачи в отдельном списке
  - Отсутствуют важные поля: даты начала, дедлайна, завершения, статус

- **Требуется:**
  1. Добавить кнопку "Список задач" в попап уровня 1 (детализация по ячейке)
  2. Реализовать переход на уровень 2 со списком всех задач
  3. В списке задач отображать полную информацию:
     - Название задачи
     - Номер задачи
     - Дата начала задачи
     - Дедлайн задачи
     - Дата завершения
     - Статус задачи (анализ от текущей даты в интерфейсе)

---

## Анализ текущей реализации

### Компонент попапа

**Файл:** `vue-app/src/components/tickets-time-tracking/TimeTrackingDetailModal.vue`

**Текущая структура попапа по ячейке:**
```vue
<div v-if="cellData.type === 'cell' || (!cellData.type && cellData.employee && cellData.week)">
  <div class="detail-info">
    <!-- Информация о сотруднике, неделе, трудозатратах -->
  </div>
  
  <div class="tasks-list">
    <h3>Задачи и связанные тикеты:</h3>
    <!-- Краткая информация о задачах -->
    <div class="task-item" v-for="(task, index) in employee.tasks">
      <div class="task-header">
        <span>Задача #{{ task.id }}</span>
        <span>{{ formatElapsedTime(task.elapsedTime) }}</span>
      </div>
      <!-- Информация о тикете -->
    </div>
  </div>
  
  <div class="detail-total">
    <!-- Итого -->
  </div>
</div>
```

**Данные о задачах (текущие):**
```javascript
// Из cellData.week.employees[].tasks[]
{
  id: 1001,                    // ID задачи
  elapsedTime: 5.0,            // Трудозатрата в часах
  ticket: {                    // Связанный тикет (опционально)
    id: 12345,
    title: "Название тикета",
    createdWeek: 48
  }
}
```

**Проблема:** В текущих данных нет полей:
- Название задачи (`title`, `name`)
- Дата начала (`startDate`, `createdDate`)
- Дедлайн (`deadline`, `endDate`)
- Дата завершения (`closedDate`, `completedDate`)
- Статус (`status`, `stageId`)

---

## Требования

### 1. Добавление кнопки "Список задач"

**Расположение:**
- В попапе уровня 1 (детализация по ячейке)
- После блока "Итого" или в отдельной секции действий
- Визуально выделенная кнопка (primary стиль)

**Условия отображения:**
- Кнопка показывается только если есть задачи (`tasksCount > 0`)
- Кнопка доступна только для попапа по ячейке (`type === 'cell'`)

**Пример расположения:**
```vue
<div class="detail-total">
  <p><strong>Итого:</strong> ...</p>
</div>

<div class="detail-actions" v-if="tasksCount > 0">
  <button class="btn btn-primary" @click="goToTasksList">
    Список задач
  </button>
</div>
```

### 2. Реализация уровня 2 (список задач)

**Структура уровня 2:**
- Заголовок: "Список задач" или "Задачи: [Сотрудник], Неделя [N]"
- Кнопка "Назад" для возврата на уровень 1
- Таблица или список задач с детальной информацией
- Плавный переход между уровнями (использовать `<Transition>`)

**Пример структуры:**
```vue
<Transition name="level" mode="out-in">
  <!-- Уровень 1: Детализация -->
  <div v-if="popupLevel === 1" key="level-1">
    <!-- Текущий контент -->
  </div>
  
  <!-- Уровень 2: Список задач -->
  <div v-else-if="popupLevel === 2" key="level-2">
    <header>
      <button @click="goBack">← Назад</button>
      <h3>Список задач</h3>
    </header>
    <section>
      <!-- Таблица задач -->
    </section>
  </div>
</Transition>
```

### 3. Структура данных задач

**Анализ полей задач из Bitrix24 API (`tasks.task.list` / `tasks.task.get`):**

**Документация:** https://apidocs.bitrix24.ru/rest/tasks.task.list

**Основные поля для отображения:**

| Поле | Название в API | Тип | Описание | Приоритет |
|------|----------------|-----|----------|-----------|
| ID задачи | `ID` | integer | Уникальный идентификатор задачи | ✅ Обязательно |
| Название задачи | `TITLE` | string | Название задачи | ✅ Обязательно |
| Дата создания | `CREATED_DATE` | string (datetime) | Дата создания задачи (используем как дату начала) | ✅ Обязательно |
| Дата начала (план) | `START_DATE_PLAN` | string (datetime) | Планируемая дата начала задачи | ⚠️ Опционально |
| Дата завершения (план) | `END_DATE_PLAN` | string (datetime) | Планируемая дата завершения задачи | ⚠️ Опционально |
| Дедлайн | `DEADLINE` | string (datetime) | Крайний срок выполнения задачи | ✅ Обязательно |
| Дата завершения | `CLOSED_DATE` | string (datetime) | Фактическая дата завершения задачи | ✅ Обязательно |
| Статус задачи | `STATUS` | integer | Статус задачи (2=новая, 3=в работе, 4=ожидает, 5=завершена, 6=отложена, 7=отклонена) | ⏳ Добавим позже |
| ID стадии | `STAGE_ID` | integer | ID стадии задачи (зависит от проекта) | ⏳ Добавим позже |
| Ответственный | `RESPONSIBLE_ID` | integer | ID ответственного сотрудника | ⚠️ Опционально |
| Создатель | `CREATED_BY` | integer | ID создателя задачи | ⚠️ Опционально |
| Трудозатрата | `timeSpentInLogs` | integer | Общее время в секундах (агрегированное) | ✅ Обязательно |

**Приоритеты:**
- ✅ **Обязательно** — поля, которые нужно отобразить в карточке задачи
- ⚠️ **Опционально** — поля, которые можно добавить позже
- ⏳ **Добавим позже** — поля, которые будут реализованы в следующих этапах

**Логика выбора дат:**
- **Дата начала:** Используем `CREATED_DATE` (если `START_DATE_PLAN` отсутствует)
- **Дедлайн:** Используем `DEADLINE` (если отсутствует, то `END_DATE_PLAN`)
- **Дата завершения:** Используем `CLOSED_DATE` (если задача завершена)

**Примечание:** Статус задачи (`STATUS`) — это отдельное поле с числовыми значениями. Расшифровка статусов находится в документации Bitrix24. Реализацию отображения статуса добавим в следующих этапах.

### 4. Отображение списка задач

**Формат: Карточки задач**

**Решение:** Использовать формат карточек вместо таблицы для лучшей масштабируемости и возможности добавления дополнительных данных в будущем.

**Преимущества карточек:**
- Легче добавлять новые поля в будущем
- Лучшая читаемость на мобильных устройствах
- Возможность группировки информации по блокам
- Аналогично `TicketCard.vue` из других модулей

**Структура карточки задачи:**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Список задач                                                               │
│  ← Назад                                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1001                                    [5.0 ч]              │ │
│  │ ─────────────────────────────────────────────────────────────────── │ │
│  │ Название: Исправление ошибки в модуле обработки данных              │ │
│  │                                                                     │ │
│  │ 📅 Начало:    15.12.2025                                           │ │
│  │ ⏰ Дедлайн:   20.12.2025                                           │ │
│  │ ✓ Завершено: 18.12.2025                                           │ │
│  │                                                                     │ │
│  │ [Статус будет добавлен позже]                                      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1002                                    [7.5 ч]              │ │
│  │ ─────────────────────────────────────────────────────────────────── │ │
│  │ Название: Добавление новой функции экспорта данных                 │ │
│  │                                                                     │ │
│  │ 📅 Начало:    16.12.2025                                           │ │
│  │ ⏰ Дедлайн:   25.12.2025                                           │ │
│  │ ✓ Завершено: -                                                    │ │
│  │                                                                     │ │
│  │ [Статус будет добавлен позже]                                      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1003                                    [3.0 ч]              │ │
│  │ ─────────────────────────────────────────────────────────────────── │ │
│  │ Название: Оптимизация запросов к базе данных                        │ │
│  │                                                                     │ │
│  │ 📅 Начало:    17.12.2025                                           │ │
│  │ ⏰ Дедлайн:   22.12.2025 [ПРОСРОЧЕНО]                              │ │
│  │ ✓ Завершено: -                                                    │ │
│  │                                                                     │ │
│  │ [Статус будет добавлен позже]                                      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  [Пагинация: < 1 2 3 >] (если задач больше 10)                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Пагинация:**
- Если задач **больше 10**, отображать пагинацию
- По умолчанию показывать **10 задач на страницу**
- Элементы пагинации: `< Предыдущая | 1 2 3 | Следующая >`
- При клике на страницу загружать соответствующую порцию задач

**Клик на карточку задачи:**
- При клике на карточку задачи будет открываться задача в Bitrix24
- **Реализуем позже** — в следующих этапах
- Пока клик на карточку не обрабатывается (или обрабатывается минимально, только логирование)
- В будущем: открытие задачи через URL `https://{domain}/company/personal/user/{responsibleId}/tasks/task/view/{taskId}/`

### 5. Статус задачи

**Важно:** Статус задачи — это отдельное поле `STATUS` в Bitrix24 API с числовыми значениями.

**Значения поля `STATUS` (из документации Bitrix24):**
- `2` — Новая (New)
- `3` — В работе (In Progress)
- `4` — Ожидает выполнения (Pending)
- `5` — Завершена (Completed)
- `6` — Отложена (Deferred)
- `7` — Отклонена (Declined)

**Расшифровка статусов:** Находится в официальной документации Bitrix24 API.

**Реализация:** Отображение статуса задачи будет добавлено в следующих этапах. На текущем этапе статус не отображается, но поле `STATUS` будет загружаться из API для будущего использования.

**Визуальное выделение просроченных задач:**
- Если текущая дата > дедлайна и задача не завершена → выделить дедлайн красным цветом
- Если есть дата завершения → показать зелёным цветом
- Визуальная индикация через цветовое выделение дат, без отдельного бейджа статуса

---

## Технические требования

### 1. Модификация компонента попапа

**Файл:** `vue-app/src/components/tickets-time-tracking/TimeTrackingDetailModal.vue`

**Изменения:**
1. Добавить состояние `popupLevel` (1 или 2)
2. Добавить кнопку "Список задач" в уровень 1
3. Реализовать уровень 2 со списком задач
4. Добавить функции навигации: `goToTasksList()`, `goBack()`
5. Использовать `<Transition>` для плавных переходов

**Пример структуры:**
```vue
<script setup>
import { ref, computed } from 'vue';

const popupLevel = ref(1);

const goToTasksList = () => {
  popupLevel.value = 2;
};

const goBack = () => {
  popupLevel.value = 1;
};
</script>
```

### 2. Получение данных о задачах

**Текущие данные:**
- Задачи уже есть в `cellData.week.employees[].tasks[]`
- Но в них нет полной информации (название, даты, статус)

**Требуется:**
- Расширить API endpoint для получения полной информации о задачах
- Или загружать данные о задачах через Bitrix24 API на фронтенде

**Решение:** Расширить backend API

**Обоснование:**
- Единообразие с другими модулями (все данные через backend API)
- Оптимизация запросов (батч-запросы на backend)
- Кеширование данных на backend
- Безопасность (не раскрывать вебхуки Bitrix24 на фронтенде)

**Реализация:**
- Добавить параметр `includeTaskDetails: true` в API endpoint
- При запросе загружать полную информацию о задачах через `tasks.task.get` (батч-запросы)
- Возвращать расширенные данные в `tasks[]` с полями: `id`, `title`, `startDate`, `deadline`, `closedDate`, `status`, `elapsedTime`
- Поддержка пагинации: параметры `page` и `perPage` (по умолчанию 10 задач на страницу)

### 3. API методы Bitrix24

**Метод получения задачи:**
- `tasks.task.get` — получение одной задачи
- `tasks.task.list` — получение списка задач

**Документация:**
- https://context7.com/bitrix24/rest/tasks.task.get
- https://context7.com/bitrix24/rest/tasks.task.list

**Поля для запроса (из документации Bitrix24 API):**
```javascript
{
  taskId: 1001,
  select: [
    'ID',                    // ID задачи
    'TITLE',                 // Название задачи
    'CREATED_DATE',           // Дата создания (используем как дату начала)
    'START_DATE_PLAN',        // Планируемая дата начала (опционально)
    'END_DATE_PLAN',          // Планируемая дата завершения (опционально)
    'DEADLINE',               // Дедлайн задачи
    'CLOSED_DATE',            // Дата завершения
    'STATUS',                 // Статус задачи (числовое значение)
    'STAGE_ID',               // ID стадии (для будущего использования)
    'RESPONSIBLE_ID',         // ID ответственного
    'CREATED_BY',             // ID создателя
    'timeSpentInLogs'         // Трудозатрата в секундах
  ]
}
```

**Документация:**
- https://apidocs.bitrix24.ru/rest/tasks.task.list
- https://apidocs.bitrix24.ru/rest/tasks.task.get
- https://context7.com/bitrix24/rest/tasks.task.list
- https://context7.com/bitrix24/rest/tasks.task.get

### 4. Форматирование дат

**Требования:**
- Формат отображения: `DD.MM.YYYY` (например, "15.12.2025")
- Для мобильных: более компактный формат `DD.MM`
- Время не отображается (только дата)

**Пример функции:**
```javascript
const formatDate = (dateString) => {
  if (!dateString) return '-';
  const date = new Date(dateString);
  return date.toLocaleDateString('ru-RU', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  });
};
```

### 5. Стилизация

**Требования:**
- Соответствие стилям других попапов (ResponsibleModal, StagesModal)
- Адаптивность для мобильных устройств
- Читаемость таблицы/списка задач
- Визуальное выделение статусов (цветные бейджи)

**Пример стилей для карточек:**
```css
/* См. раздел "Детальные стили CSS" ниже для полных стилей карточек */
```

---

## Этапы реализации

> **Примечание:** Детальный структурный план реализации находится в разделе "Структурный план реализации" ниже.

### Краткий обзор этапов

1. **Этап 1: Анализ и подготовка (Backend)** — Расширение backend API
2. **Этап 2: Расширение сервиса (Frontend)** — Добавление метода загрузки задач
3. **Этап 3: Модификация компонента попапа (Frontend)** — Кнопка и уровень 2 с карточками
4. **Этап 4: Стилизация и UX (Frontend)** — Стили карточек и пагинации
5. **Этап 5: Обработка ошибок и состояний (Frontend)** — Все состояния компонента
6. **Этап 6: Тестирование** — Проверка всех функций

**Детали каждого этапа:** См. раздел "Структурный план реализации" ниже.

---

## Критерии приёмки

### Функциональность
- [ ] Кнопка "Список задач" отображается в попапе уровня 1 (только если есть задачи)
- [ ] При клике на кнопку происходит переход на уровень 2
- [ ] В уровне 2 отображается список задач в виде карточек
- [ ] В карточках задач отображаются все требуемые поля:
  - [ ] Название задачи
  - [ ] Номер задачи
  - [ ] Трудозатрата задачи
  - [ ] Дата начала задачи
  - [ ] Дедлайн задачи
  - [ ] Дата завершения
- [ ] Кнопка "Назад" возвращает на уровень 1
- [ ] Плавные переходы между уровнями работают корректно

### Пагинация
- [ ] Пагинация отображается только если задач больше 10
- [ ] По умолчанию показывается 10 задач на страницу
- [ ] Переключение страниц работает корректно
- [ ] При смене страницы загружаются соответствующие задачи

### Визуальное оформление
- [ ] Просроченные задачи визуально выделены (красный цвет дедлайна)
- [ ] Завершённые задачи визуально выделены (зелёная граница)
- [ ] Карточки задач имеют единообразный стиль
- [ ] Адаптивность для мобильных устройств работает
- [ ] Стили соответствуют другим попапам в проекте

### Backend API
- [ ] API endpoint расширен для получения детальной информации о задачах
- [ ] API поддерживает пагинацию (параметры `page` и `perPage`)
- [ ] API возвращает метаданные пагинации
- [ ] Поля задач загружаются корректно из Bitrix24 API

### Обработка состояний
- [ ] Состояние загрузки отображается корректно
- [ ] Состояние ошибки обрабатывается с кнопкой "Повторить"
- [ ] Пустое состояние отображается при отсутствии задач
- [ ] Обработка отсутствующих полей (fallback значения)

### Будущие улучшения (не входят в текущую задачу)
- [ ] Статус задачи (`STATUS`) — будет добавлен позже
- [ ] Открытие задачи в Bitrix24 при клике — будет реализовано позже

---

## Уточнения и решения

### ✅ Решено

1. **Структура данных задач:**
   - ✅ Поля определены из документации Bitrix24 API (`tasks.task.list`)
   - ✅ Основные поля: `ID`, `TITLE`, `CREATED_DATE`, `START_DATE_PLAN`, `END_DATE_PLAN`, `DEADLINE`, `CLOSED_DATE`, `STATUS`, `STAGE_ID`
   - ✅ Документация: https://apidocs.bitrix24.ru/rest/tasks.task.list

2. **Формат отображения:**
   - ✅ Используются **карточки** вместо таблицы (для лучшей масштабируемости)
   - ⏳ Сортировка/фильтрация — в будущих этапах

3. **Статусы задач:**
   - ✅ Статус задачи — отдельное поле `STATUS` с числовыми значениями (2-7)
   - ✅ Расшифровка статусов находится в документации Bitrix24
   - ⏳ Отображение статуса будет добавлено в следующих этапах

4. **API:**
   - ✅ Расширяем **backend API** (единообразие, оптимизация, безопасность)
   - ✅ Пагинация реализована: если задач **больше 10**, показывается пагинация

5. **Интерактивность:**
   - ⏳ Клик на карточку задачи будет открывать задачу в Bitrix24 (реализуем позже)
   - ⏳ Ссылка на задачу в Bitrix24 будет добавлена в следующих этапах

### ⏳ Будущие улучшения

- Отображение статуса задачи (поле `STATUS`)
- Открытие задачи в Bitrix24 при клике на карточку
- Дополнительные поля (ответственный, создатель)
- Фильтрация и сортировка задач

---

## История правок

- **2025-12-17 16:42 (UTC+3, Брест):** Создан черновик задачи
  - Описана цель добавления кнопки "Список задач"
  - Зафиксированы требования к уровню 2
  - Определены этапы реализации и критерии приёмки
  - Добавлены вопросы для уточнения

---

## Связанные документы

- [TASK-050-07: Frontend попап детализации](./TASK-050-07-frontend-detail-modal.md) — базовый попап
- [TASK-042: Попап второго уровня — список тикетов](./TASK-042-popup-level2-tickets-list.md) — пример многоуровневого попапа
- [TASK-043: Попап «По стадиям»](./TASK-043-new-tickets-stages-popup.md) — пример попапа с уровнями
- [Анализ API трудозатрат](../ANALYSIS/time-tracking-api-analysis.md) — структура данных

---

## Детальная структура компонента

### Полная структура TimeTrackingDetailModal.vue

**Файл:** `vue-app/src/components/tickets-time-tracking/TimeTrackingDetailModal.vue`

**Полный пример реализации:**

```vue
<template>
  <Teleport to="body">
    <div
      v-if="cellData"
      class="modal-backdrop"
      role="dialog"
      aria-modal="true"
      @click.self="close"
      @keydown.esc="close"
    >
      <div class="modal-content" @click.stop>
        <div class="modal-header">
          <h2>{{ modalTitle }}</h2>
          <button class="close-button" @click="close" aria-label="Закрыть">×</button>
        </div>
        
        <div class="modal-body">
          <!-- Transition для плавной анимации между уровнями -->
          <Transition name="level" mode="out-in">
            <!-- Уровень 1: Детализация по ячейке -->
            <div 
              v-if="popupLevel === 1 && (cellData.type === 'cell' || (!cellData.type && cellData.employee && cellData.week))" 
              key="level-1"
              class="detail-cell"
            >
              <div class="detail-info">
                <p><strong>Сотрудник:</strong> {{ cellData.employee?.name || 'Неизвестно' }}</p>
                <p><strong>Неделя:</strong> {{ cellData.week?.weekNumber || '?' }}</p>
                <p><strong>Трудозатраты:</strong> {{ formatElapsedTime(cellData.elapsedTime || 0) }}</p>
              </div>
              
              <div v-if="cellData.week?.employees" class="tasks-list">
                <h3>Задачи и связанные тикеты:</h3>
                <div 
                  v-for="employee in cellData.week.employees.filter(e => e.id === cellData.employee?.id)" 
                  :key="employee.id"
                  class="employee-tasks"
                >
                  <div class="task-item" v-for="(task, index) in employee.tasks" :key="task.id || index">
                    <div class="task-header">
                      <span class="task-label">Задача #{{ task.id || index + 1 }}</span>
                      <span class="task-time">{{ formatElapsedTime(task.elapsedTime || 0) }}</span>
                    </div>
                    <div v-if="task.ticket" class="ticket-info">
                      <span class="ticket-label">Тикет #{{ task.ticket.id }}</span>
                      <span 
                        v-if="task.ticket.createdWeek && task.ticket.createdWeek !== cellData.week.weekNumber"
                        class="ticket-week-badge"
                        :title="`Тикет создан в неделе ${task.ticket.createdWeek}, трудозатрата записана в неделе ${cellData.week.weekNumber}`"
                      >
                        (создан в неделе {{ task.ticket.createdWeek }})
                      </span>
                      <div v-if="task.ticket.title" class="ticket-title">{{ task.ticket.title }}</div>
                    </div>
                    <div v-else class="ticket-info ticket-info--no-ticket">
                      <span class="no-ticket-label">Тикет не связан</span>
                    </div>
                  </div>
                </div>
                
                <div class="detail-total">
                  <p>
                    <strong>Итого:</strong> 
                    {{ formatElapsedTime(cellData.elapsedTime || 0) }} 
                    ({{ tasksCount }} задач, {{ ticketsCount }} тикетов)
                  </p>
                </div>
              </div>
              
              <!-- Кнопка "Список задач" -->
              <div class="detail-actions" v-if="tasksCount > 0">
                <button class="btn btn-primary btn-tasks-list" @click="goToTasksList">
                  📋 Список задач
                </button>
              </div>
            </div>
            
            <!-- Уровень 2: Список задач -->
            <div v-else-if="popupLevel === 2" key="level-2" class="tasks-list-level">
              <div class="tasks-list-header">
                <button class="btn-back" @click="goBack" aria-label="Назад">
                  ← Назад
                </button>
                <h3 class="tasks-list-title">
                  Список задач: {{ cellData.employee?.name || 'Сотрудник' }}, Неделя {{ cellData.week?.weekNumber || '?' }}
                </h3>
              </div>
              
              <div class="tasks-list-content">
                <!-- Состояние загрузки -->
                <Transition name="loading" mode="out-in">
                  <div v-if="isLoadingTasks" key="loading" class="loading-state">
                    <div class="loading-spinner"></div>
                    <p>Загрузка задач...</p>
                  </div>
                  
                  <!-- Состояние ошибки -->
                  <div v-else-if="tasksError" key="error" class="error-state">
                    <div class="error-icon">⚠️</div>
                    <p class="error-title">Ошибка загрузки</p>
                    <p class="error-message">{{ tasksError }}</p>
                    <button class="btn btn-retry" @click="retryLoadTasks">Повторить</button>
                  </div>
                  
                  <!-- Пустое состояние -->
                  <div v-else-if="enrichedTasks.length === 0" key="empty" class="empty-state">
                    <div class="empty-state-icon">📋</div>
                    <p class="empty-state-message">Нет задач для отображения</p>
                  </div>
                  
                  <!-- Список задач (карточки) -->
                  <div v-else key="tasks" class="tasks-cards-container">
                    <TransitionGroup name="task-card" tag="div" class="tasks-cards-list">
                      <div
                        v-for="task in paginatedTasks"
                        :key="task.id"
                        class="task-card"
                        :class="getTaskCardClass(task)"
                        @click="handleTaskClick(task)"
                      >
                        <div class="task-card__header">
                          <span class="task-card__number">Задача #{{ task.id }}</span>
                          <span v-if="task.elapsedTime" class="task-card__time">
                            {{ formatElapsedTime(task.elapsedTime) }}
                          </span>
                        </div>
                        
                        <div class="task-card__title">
                          {{ task.title || 'Без названия' }}
                        </div>
                        
                        <div class="task-card__dates">
                          <div class="task-card__date-item">
                            <span class="date-icon">📅</span>
                            <span class="date-label">Начало:</span>
                            <span class="date-value">{{ formatDate(task.startDate) }}</span>
                          </div>
                          
                          <div class="task-card__date-item">
                            <span class="date-icon">⏰</span>
                            <span class="date-label">Дедлайн:</span>
                            <span 
                              class="date-value"
                              :class="{ 'date-value--overdue': isOverdue(task.deadline, task.closedDate) }"
                            >
                              {{ formatDate(task.deadline) || '-' }}
                            </span>
                          </div>
                          
                          <div class="task-card__date-item">
                            <span class="date-icon">✓</span>
                            <span class="date-label">Завершено:</span>
                            <span class="date-value">{{ formatDate(task.closedDate) || '-' }}</span>
                          </div>
                        </div>
                        
                        <!-- Статус будет добавлен позже -->
                        <div class="task-card__status-placeholder">
                          <!-- Статус задачи будет отображаться здесь в следующих этапах -->
                        </div>
                      </div>
                    </TransitionGroup>
                    
                    <!-- Пагинация (если задач больше 10) -->
                    <div v-if="totalPages > 1" class="tasks-pagination">
                      <button 
                        class="pagination-btn"
                        :disabled="currentPage === 1"
                        @click="goToPage(currentPage - 1)"
                      >
                        ← Предыдущая
                      </button>
                      
                      <div class="pagination-pages">
                        <button
                          v-for="page in visiblePages"
                          :key="page"
                          class="pagination-page"
                          :class="{ 'pagination-page--active': page === currentPage }"
                          @click="goToPage(page)"
                        >
                          {{ page }}
                        </button>
                      </div>
                      
                      <button 
                        class="pagination-btn"
                        :disabled="currentPage === totalPages"
                        @click="goToPage(currentPage + 1)"
                      >
                        Следующая →
                      </button>
                    </div>
                  </div>
                </Transition>
              </div>
            </div>
            
            <!-- Попап по сотруднику (без изменений) -->
            <div v-else-if="cellData.type === 'employee'" key="employee" class="detail-employee">
              <!-- ... существующий код ... -->
            </div>
            
            <!-- Попап по неделе (без изменений) -->
            <div v-else-if="cellData.type === 'week'" key="week" class="detail-week">
              <!-- ... существующий код ... -->
            </div>
          </Transition>
        </div>
        
        <div class="modal-footer">
          <button class="close-btn" @click="close">Закрыть</button>
        </div>
      </div>
    </div>
  </Teleport>
</template>

<script setup>
import { ref, computed, watch } from 'vue';
import { formatElapsedTime, getWeekLabel } from '@/services/tickets-time-tracking/timeTrackingUtils.js';
import { timeTrackingService } from '@/services/tickets-time-tracking/timeTrackingService.js';

const props = defineProps({
  cellData: {
    type: Object,
    default: null
  }
});

const emit = defineEmits(['close']);

// Состояния
const popupLevel = ref(1);
const enrichedTasks = ref([]);
const isLoadingTasks = ref(false);
const tasksError = ref(null);

// Пагинация
const currentPage = ref(1);
const perPage = ref(10); // По умолчанию 10 задач на страницу

// Computed
const modalTitle = computed(() => {
  if (!props.cellData) return 'Детализация трудозатрат';
  
  if (popupLevel.value === 2) {
    return `Список задач: ${props.cellData.employee?.name || 'Сотрудник'}, Неделя ${props.cellData.week?.weekNumber || '?'}`;
  }
  
  if (props.cellData.type === 'employee') {
    return `Детализация: ${props.cellData.employee?.name || 'Сотрудник'}`;
  }
  
  if (props.cellData.type === 'week') {
    return `Детализация: Неделя ${props.cellData.week?.weekNumber || '?'}`;
  }
  
  // По умолчанию - попап по ячейке
  const employee = props.cellData.employee?.name || 'Сотрудник';
  const week = props.cellData.week?.weekNumber || '?';
  const time = formatElapsedTime(props.cellData.elapsedTime || 0);
  return `Детализация: ${employee}, Неделя ${week} (${time})`;
});

const tasksCount = computed(() => {
  if (!props.cellData?.week?.employees) return 0;
  const employee = props.cellData.week.employees.find(e => e.id === props.cellData.employee?.id);
  return employee?.tasksCount || 0;
});

const ticketsCount = computed(() => {
  if (!props.cellData?.week?.employees) return 0;
  const employee = props.cellData.week.employees.find(e => e.id === props.cellData.employee?.id);
  return employee?.ticketsCount || 0;
});

// Пагинация
const totalPages = computed(() => {
  if (enrichedTasks.value.length <= perPage.value) {
    return 1;
  }
  return Math.ceil(enrichedTasks.value.length / perPage.value);
});

const paginatedTasks = computed(() => {
  const start = (currentPage.value - 1) * perPage.value;
  const end = start + perPage.value;
  return enrichedTasks.value.slice(start, end);
});

const visiblePages = computed(() => {
  const pages = [];
  const maxVisible = 5; // Максимум видимых страниц
  let start = Math.max(1, currentPage.value - Math.floor(maxVisible / 2));
  let end = Math.min(totalPages.value, start + maxVisible - 1);
  
  if (end - start < maxVisible - 1) {
    start = Math.max(1, end - maxVisible + 1);
  }
  
  for (let i = start; i <= end; i++) {
    pages.push(i);
  }
  
  return pages;
});

// Методы
const formatPeriod = (weeks) => {
  if (!weeks || weeks.length === 0) return 'Период не указан';
  if (weeks.length === 1) {
    return getWeekLabel(weeks[0].week.weekNumber, weeks[0].week.weekStartUtc);
  }
  const firstWeek = weeks[0].week;
  const lastWeek = weeks[weeks.length - 1].week;
  return `${getWeekLabel(firstWeek.weekNumber, firstWeek.weekStartUtc)} - ${getWeekLabel(lastWeek.weekNumber, lastWeek.weekStartUtc)}`;
};

const formatWeekPeriod = (week) => {
  if (!week) return 'Период не указан';
  return getWeekLabel(week.weekNumber, week.weekStartUtc);
};

/**
 * Форматирование даты для отображения
 */
const formatDate = (dateString) => {
  if (!dateString) return '-';
  try {
    const date = new Date(dateString);
    return date.toLocaleDateString('ru-RU', {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric'
    });
  } catch (e) {
    return '-';
  }
};

/**
 * Проверка просроченности задачи
 */
const isOverdue = (deadline, closedDate) => {
  // Если задача уже завершена, не считаем просроченной
  if (closedDate) return false;
  
  if (!deadline) return false;
  try {
    const deadlineDate = new Date(deadline);
    const now = new Date();
    return now > deadlineDate;
  } catch (e) {
    return false;
  }
};

/**
 * Получение класса карточки задачи для стилизации
 */
const getTaskCardClass = (task) => {
  const closedDate = task.closedDate ? new Date(task.closedDate) : null;
  const isOverdueTask = isOverdue(task.deadline, task.closedDate);
  
  return {
    'task-card--completed': !!closedDate,
    'task-card--overdue': isOverdueTask,
    'task-card--in-progress': !closedDate && !isOverdueTask
  };
};

/**
 * Переход на уровень 2 (список задач)
 */
const goToTasksList = async () => {
  popupLevel.value = 2;
  await loadTasksDetails();
};

/**
 * Возврат на уровень 1
 */
const goBack = () => {
  popupLevel.value = 1;
  enrichedTasks.value = [];
  tasksError.value = null;
  currentPage.value = 1; // Сброс пагинации
};

/**
 * Загрузка детальной информации о задачах
 */
const loadTasksDetails = async () => {
  if (!props.cellData?.week?.employees || !props.cellData.employee) {
    tasksError.value = 'Недостаточно данных для загрузки задач';
    return;
  }
  
  isLoadingTasks.value = true;
  tasksError.value = null;
  
  try {
    const employee = props.cellData.week.employees.find(e => e.id === props.cellData.employee?.id);
    if (!employee || !employee.tasks || employee.tasks.length === 0) {
      enrichedTasks.value = [];
      return;
    }
    
    // Получить ID всех задач
    const taskIds = employee.tasks.map(task => task.id).filter(id => id);
    
    if (taskIds.length === 0) {
      enrichedTasks.value = [];
      return;
    }
    
    // Загрузить детальную информацию о задачах через расширенный API endpoint
    const response = await timeTrackingService.getTasksDetails({
      taskIds: taskIds,
      employeeId: props.cellData.employee.id,
      weekNumber: props.cellData.week.weekNumber,
      page: currentPage.value,
      perPage: perPage.value
    });
    
    enrichedTasks.value = response.tasks || [];
    
    // Обновить метаданные пагинации (если нужно)
    // totalPages уже вычисляется в computed свойстве на основе enrichedTasks.value.length
    
  } catch (err) {
    console.error('[TimeTrackingDetailModal] Error loading tasks details:', err);
    tasksError.value = err.message || 'Ошибка загрузки задач';
    enrichedTasks.value = [];
  } finally {
    isLoadingTasks.value = false;
  }
};

/**
 * Повторная загрузка задач
 */
const retryLoadTasks = () => {
  currentPage.value = 1; // Сброс на первую страницу
  loadTasksDetails();
};

// Перезагрузка при смене страницы
watch(currentPage, () => {
  if (popupLevel.value === 2) {
    loadTasksDetails();
  }
});

/**
 * Переход на страницу пагинации
 */
const goToPage = (page) => {
  if (page >= 1 && page <= totalPages.value) {
    currentPage.value = page;
    // Прокрутка вверх списка задач
    const container = document.querySelector('.tasks-cards-container');
    if (container) {
      container.scrollTop = 0;
    }
  }
};

/**
 * Обработка клика на задачу
 * 
 * Примечание: Открытие задачи в Bitrix24 будет реализовано в следующих этапах.
 * Пока клик не обрабатывается или обрабатывается минимально.
 */
const handleTaskClick = (task) => {
  // TODO: Реализовать открытие задачи в Bitrix24 в следующих этапах
  // const taskUrl = `https://${window.location.hostname}/company/personal/user/${task.responsibleId}/tasks/task/view/${task.id}/`;
  // window.open(taskUrl, '_blank');
  
  console.log('[TimeTrackingDetailModal] Task clicked:', task.id);
};

const close = () => {
  emit('close');
};

// Сброс состояния при закрытии попапа
watch(() => props.cellData, (newValue) => {
  if (!newValue) {
    popupLevel.value = 1;
    enrichedTasks.value = [];
    tasksError.value = null;
    currentPage.value = 1;
  }
});
</script>
```

### Расширение сервиса timeTrackingService.js

**Файл:** `vue-app/src/services/tickets-time-tracking/timeTrackingService.js`

**Добавить метод:**

```javascript
/**
 * Получение детальной информации о задачах с поддержкой пагинации
 * 
 * @param {Object} params Параметры запроса
 * @param {Array<number>} params.taskIds Массив ID задач
 * @param {number} params.employeeId ID сотрудника
 * @param {number} params.weekNumber Номер недели
 * @param {number} params.page Номер страницы (по умолчанию 1)
 * @param {number} params.perPage Количество задач на страницу (по умолчанию 10)
 * @returns {Promise<Object>} Объект с массивом задач и метаданными пагинации
 */
export async function getTasksDetails({ 
  taskIds, 
  employeeId, 
  weekNumber, 
  page = 1, 
  perPage = 10 
}) {
  const response = await fetch('/api/tickets-time-tracking-sector-1c.php', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      product: '1C',
      includeTaskDetails: true,
      taskIds: taskIds,
      employeeId: employeeId,
      weekNumber: weekNumber,
      page: page,
      perPage: perPage
    })
  });
  
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  
  const result = await response.json();
  
  if (!result.success) {
    throw new Error(result.error || 'Ошибка получения данных о задачах');
  }
  
  return {
    tasks: result.data.tasks || [],
    pagination: result.data.pagination || {
      totalTasks: 0,
      currentPage: 1,
      perPage: perPage,
      totalPages: 0
    }
  };
}
```

---

## Визуальная схема структуры попапа

### Уровень 1: Детализация по ячейке

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Детализация: Иванов И.И., Неделя 50 (15.5 ч)                    [×]       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Сотрудник: Иванов И.И.                                                    │
│  Неделя: 50                                                                │
│  Трудозатраты: 15.5 ч                                                      │
│                                                                             │
│  ────────────────────────────────────────────────────────────────────────  │
│                                                                             │
│  Задачи и связанные тикеты:                                                │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1001                                   5.0 ч                 │ │
│  │   └─ Тикет #12345 (создан в неделе 48)                              │ │
│  │      Название: "Исправление ошибки в модуле"                          │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1002                                   7.5 ч                 │ │
│  │   └─ Тикет #12346                                                      │ │
│  │      Название: "Добавление новой функции"                              │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ────────────────────────────────────────────────────────────────────────  │
│                                                                             │
│  Итого: 15.5 ч (3 задачи, 3 тикета)                                       │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                    [📋 Список задач]                                │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│                                                      [Закрыть]             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Уровень 2: Список задач

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Список задач: Иванов И.И., Неделя 50                          [×]         │
│  [← Назад]                                                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1001                                    [5.0 ч]              │ │
│  │ ─────────────────────────────────────────────────────────────────── │ │
│  │ Исправление ошибки в модуле обработки данных                        │ │
│  │                                                                     │ │
│  │ 📅 Начало:    15.12.2025                                           │ │
│  │ ⏰ Дедлайн:   20.12.2025                                           │ │
│  │ ✓ Завершено: 18.12.2025                                           │ │
│  │                                                                     │ │
│  │ [Статус будет добавлен позже]                                      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1002                                    [7.5 ч]              │ │
│  │ ─────────────────────────────────────────────────────────────────── │ │
│  │ Добавление новой функции экспорта данных                            │ │
│  │                                                                     │ │
│  │ 📅 Начало:    16.12.2025                                           │ │
│  │ ⏰ Дедлайн:   25.12.2025                                           │ │
│  │ ✓ Завершено: -                                                    │ │
│  │                                                                     │ │
│  │ [Статус будет добавлен позже]                                      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ Задача #1003                                    [3.0 ч]              │ │
│  │ ─────────────────────────────────────────────────────────────────── │ │
│  │ Оптимизация запросов к базе данных                                  │ │
│  │                                                                     │ │
│  │ 📅 Начало:    17.12.2025                                           │ │
│  │ ⏰ Дедлайн:   22.12.2025 [КРАСНЫЙ - ПРОСРОЧЕНО]                   │ │
│  │ ✓ Завершено: -                                                    │ │
│  │                                                                     │ │
│  │ [Статус будет добавлен позже]                                      │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                    [← Предыдущая] [1] [2] [3] [Следующая →]        │ │
│  │                    (Пагинация, если задач > 10)                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│                                                      [Закрыть]             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Детальная структура данных задач

### Текущая структура (из API)

**Источник:** `cellData.week.employees[].tasks[]`

```javascript
{
  id: 1001,                    // ID задачи (TASK_ID)
  elapsedTime: 5.0,            // Трудозатрата в часах
  ticket: {                    // Связанный тикет (опционально)
    id: 12345,
    title: "Название тикета",
    createdWeek: 48
  }
}
```

### Расширенная структура (после загрузки деталей)

**Источник:** Bitrix24 API `tasks.task.get` или расширенный backend API

```javascript
{
  id: 1001,                     // ID задачи
  title: "Исправление ошибки в модуле",  // Название задачи (TITLE)
  startDate: "2025-12-15T10:00:00+03:00",  // Дата начала (CREATED_DATE или START_DATE_PLAN)
  deadline: "2025-12-20T18:00:00+03:00",   // Дедлайн (DEADLINE или END_DATE_PLAN)
  closedDate: "2025-12-18T15:30:00+03:00", // Дата завершения (CLOSED_DATE)
  status: 5,                    // Статус задачи (STATUS: 2=новая, 3=в работе, 4=ожидает, 5=завершена)
  stageId: 5,                   // ID стадии (STAGE_ID)
  elapsedTime: 5.0,             // Трудозатрата в часах
  responsibleId: 123,           // ID ответственного (RESPONSIBLE_ID)
  createdBy: 123,               // ID создателя (CREATED_BY)
  ticket: {                     // Связанный тикет (опционально)
    id: 12345,
    title: "Название тикета",
    createdWeek: 48
  }
}
```

### Статусы задач в Bitrix24

**Значения поля `STATUS`:**
- `2` — Новая (New)
- `3` — В работе (In Progress)
- `4` — Ожидает выполнения (Pending)
- `5` — Завершена (Completed)
- `6` — Отложена (Deferred)
- `7` — Отклонена (Declined)

**Значения поля `STAGE_ID`:**
- Зависит от настройки проекта в Bitrix24
- Обычно соответствует статусам, но может быть более детальным

---

## Расширение backend API

### Параметры запроса

**Файл:** `api/tickets-time-tracking-sector-1c.php`

**Новый параметр:**

```json
{
  "product": "1C",
  "weeksCount": 4,
  "includeTaskDetails": true,  // новый параметр
  "taskIds": [1001, 1002, 1003],  // опционально, для фильтрации
  "employeeId": 123,  // опционально, для фильтрации
  "weekNumber": 50,  // опционально, для фильтрации
  "page": 1,  // номер страницы (по умолчанию 1)
  "perPage": 10  // количество задач на страницу (по умолчанию 10)
}
```

**Логика пагинации:**
- Если `includeTaskDetails: true` и задач больше 10, применять пагинацию
- По умолчанию: `page = 1`, `perPage = 10`
- Возвращать метаданные пагинации в ответе

### Логика в API

```php
/**
 * Получение детальной информации о задачах с поддержкой пагинации
 * 
 * @param array $taskIds Массив ID задач
 * @param int $page Номер страницы (по умолчанию 1)
 * @param int $perPage Количество задач на страницу (по умолчанию 10)
 * @return array Массив с задачами и метаданными пагинации
 */
function getTasksDetails(array $taskIds, int $page = 1, int $perPage = 10): array
{
    if (empty($taskIds)) {
        return [
            'tasks' => [],
            'pagination' => [
                'totalTasks' => 0,
                'currentPage' => 1,
                'perPage' => $perPage,
                'totalPages' => 0
            ]
        ];
    }
    
    $allTasks = [];
    $batchSize = 50; // Bitrix24 ограничение на батч-запросы
    
    // Разбиваем на батчи
    $batches = array_chunk($taskIds, $batchSize);
    
    foreach ($batches as $batch) {
        $batchData = [];
        
        foreach ($batch as $taskId) {
            $batchData["task_{$taskId}"] = [
                'method' => 'tasks.task.get',
                'params' => [
                    'taskId' => $taskId,
                    'select' => [
                        'ID',
                        'TITLE',
                        'CREATED_DATE',
                        'START_DATE_PLAN',
                        'END_DATE_PLAN',
                        'DEADLINE',
                        'CLOSED_DATE',
                        'STATUS',  // Загружаем, но не отображаем пока
                        'STAGE_ID',  // Загружаем, но не отображаем пока
                        'RESPONSIBLE_ID',
                        'CREATED_BY',
                        'timeSpentInLogs'
                    ]
                ]
            ];
        }
        
        $result = CRest::callBatch($batchData);
        
        if (isset($result['result']['result'])) {
            foreach ($result['result']['result'] as $key => $taskResult) {
                if (isset($taskResult['error'])) {
                    error_log(sprintf(
                        "[TimeTracking] Error loading task %s: %s",
                        str_replace('task_', '', $key),
                        $taskResult['error_description'] ?? 'Unknown error'
                    ));
                    continue;
                }
                
                $task = $taskResult['task'] ?? null;
                if ($task) {
                    // Определяем дату начала (приоритет: START_DATE_PLAN, затем CREATED_DATE)
                    $startDate = $task['startDatePlan'] ?? $task['START_DATE_PLAN'] ?? 
                                 $task['createdDate'] ?? $task['CREATED_DATE'] ?? null;
                    
                    // Определяем дедлайн (приоритет: DEADLINE, затем END_DATE_PLAN)
                    $deadline = $task['deadline'] ?? $task['DEADLINE'] ?? 
                                $task['endDatePlan'] ?? $task['END_DATE_PLAN'] ?? null;
                    
                    $allTasks[] = [
                        'id' => (int)($task['id'] ?? $task['ID'] ?? 0),
                        'title' => $task['title'] ?? $task['TITLE'] ?? 'Без названия',
                        'startDate' => $startDate,
                        'deadline' => $deadline,
                        'closedDate' => $task['closedDate'] ?? $task['CLOSED_DATE'] ?? null,
                        'status' => (int)($task['status'] ?? $task['STATUS'] ?? 0),  // Для будущего использования
                        'stageId' => (int)($task['stageId'] ?? $task['STAGE_ID'] ?? 0),  // Для будущего использования
                        'responsibleId' => (int)($task['responsibleId'] ?? $task['RESPONSIBLE_ID'] ?? 0),
                        'createdBy' => (int)($task['createdBy'] ?? $task['CREATED_BY'] ?? 0),
                        'elapsedTime' => isset($task['timeSpentInLogs']) ? ($task['timeSpentInLogs'] / 3600) : 0 // секунды -> часы
                    ];
                }
            }
        }
    }
    
    // Применяем пагинацию
    $totalTasks = count($allTasks);
    $totalPages = $totalTasks > 0 ? (int)ceil($totalTasks / $perPage) : 0;
    $currentPage = max(1, min($page, $totalPages)); // Ограничиваем текущую страницу
    
    $start = ($currentPage - 1) * $perPage;
    $end = $start + $perPage;
    $paginatedTasks = array_slice($allTasks, $start, $perPage);
    
    return [
        'tasks' => $paginatedTasks,
        'pagination' => [
            'totalTasks' => $totalTasks,
            'currentPage' => $currentPage,
            'perPage' => $perPage,
            'totalPages' => $totalPages
        ]
    ];
}
```

### Формат ответа API

**Без пагинации (если задач <= 10):**
```json
{
  "success": true,
  "data": {
    "tasks": [
      {
        "id": 1001,
        "title": "Исправление ошибки в модуле",
        "startDate": "2025-12-15T10:00:00+03:00",
        "deadline": "2025-12-20T18:00:00+03:00",
        "closedDate": "2025-12-18T15:30:00+03:00",
        "status": 5,
        "stageId": 5,
        "responsibleId": 123,
        "createdBy": 123,
        "elapsedTime": 5.0
      }
    ],
    "pagination": {
      "totalTasks": 8,
      "currentPage": 1,
      "perPage": 10,
      "totalPages": 1
    }
  }
}
```

**С пагинацией (если задач > 10):**
```json
{
  "success": true,
  "data": {
    "tasks": [
      {
        "id": 1001,
        "title": "Исправление ошибки в модуле",
        "startDate": "2025-12-15T10:00:00+03:00",
        "deadline": "2025-12-20T18:00:00+03:00",
        "closedDate": "2025-12-18T15:30:00+03:00",
        "status": 5,
        "stageId": 5,
        "responsibleId": 123,
        "createdBy": 123,
        "elapsedTime": 5.0
      }
      // ... еще 9 задач на странице
    ],
    "pagination": {
      "totalTasks": 25,
      "currentPage": 1,
      "perPage": 10,
      "totalPages": 3
    }
  }
}
```

**Примечания:**
- Поле `status` загружается из API, но не отображается в текущем этапе (добавим позже)
- Поле `stageId` загружается из API, но не отображается в текущем этапе (добавим позже)
- Пагинация применяется только если `totalTasks > perPage`

---

## Детальные стили CSS

**Добавить в `TimeTrackingDetailModal.vue`:**

```css
/* Кнопка "Список задач" */
.detail-actions {
  margin-top: 20px;
  padding-top: 20px;
  border-top: 1px solid #e5e7eb;
  display: flex;
  justify-content: center;
}

.btn-tasks-list {
  padding: 12px 24px;
  background-color: #007bff;
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 16px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.btn-tasks-list:hover {
  background-color: #0056b3;
  transform: translateY(-1px);
  box-shadow: 0 2px 8px rgba(0, 123, 255, 0.3);
}

.btn-tasks-list:active {
  transform: translateY(0);
}

/* Уровень 2: Список задач */
.tasks-list-level {
  width: 100%;
}

.tasks-list-header {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 20px;
  padding-bottom: 16px;
  border-bottom: 1px solid #e5e7eb;
}

.btn-back {
  padding: 8px 16px;
  background-color: #f3f4f6;
  color: #374151;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.btn-back:hover {
  background-color: #e5e7eb;
  border-color: #9ca3af;
}

.tasks-list-title {
  margin: 0;
  font-size: 18px;
  font-weight: 600;
  color: #111827;
}

.tasks-list-content {
  width: 100%;
}

/* Карточки задач */
.tasks-cards-container {
  width: 100%;
}

.tasks-cards-list {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.task-card {
  background-color: #ffffff;
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  padding: 16px;
  transition: all 0.2s ease;
  cursor: pointer;
}

.task-card:hover {
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  transform: translateY(-2px);
  border-color: #d1d5db;
}

.task-card--overdue {
  border-left: 4px solid #dc2626;
  background-color: #fef2f2;
}

.task-card--completed {
  border-left: 4px solid #10b981;
  background-color: #f0fdf4;
}

.task-card--in-progress {
  border-left: 4px solid #f59e0b;
  background-color: #fffbeb;
}

.task-card__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
  padding-bottom: 12px;
  border-bottom: 1px solid #e5e7eb;
}

.task-card__number {
  font-weight: 600;
  font-size: 16px;
  color: #007bff;
}

.task-card__time {
  font-weight: 600;
  font-size: 14px;
  color: #059669;
  background-color: #d1fae5;
  padding: 4px 8px;
  border-radius: 4px;
}

.task-card__title {
  font-size: 16px;
  font-weight: 500;
  color: #111827;
  margin-bottom: 16px;
  line-height: 1.5;
}

.task-card__dates {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 12px;
}

.task-card__date-item {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 14px;
}

.date-icon {
  font-size: 16px;
}

.date-label {
  color: #6b7280;
  min-width: 80px;
}

.date-value {
  color: #374151;
  font-weight: 500;
}

.date-value--overdue {
  color: #dc2626;
  font-weight: 600;
}

.task-card__status-placeholder {
  padding-top: 12px;
  border-top: 1px solid #e5e7eb;
  font-size: 12px;
  color: #9ca3af;
  font-style: italic;
  min-height: 20px;
}

/* Пагинация */
.tasks-pagination {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 8px;
  margin-top: 24px;
  padding-top: 24px;
  border-top: 1px solid #e5e7eb;
}

.pagination-btn {
  padding: 8px 16px;
  background-color: #f3f4f6;
  color: #374151;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.pagination-btn:hover:not(:disabled) {
  background-color: #e5e7eb;
  border-color: #9ca3af;
}

.pagination-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.pagination-pages {
  display: flex;
  gap: 4px;
}

.pagination-page {
  min-width: 36px;
  height: 36px;
  padding: 0 12px;
  background-color: #ffffff;
  color: #374151;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  justify-content: center;
}

.pagination-page:hover {
  background-color: #f9fafb;
  border-color: #9ca3af;
}

.pagination-page--active {
  background-color: #007bff;
  color: #ffffff;
  border-color: #007bff;
}

.pagination-page--active:hover {
  background-color: #0056b3;
  border-color: #0056b3;
}

/* Transition для карточек */
.task-card-enter-active,
.task-card-leave-active {
  transition: all 0.3s ease;
}

.task-card-enter-from {
  opacity: 0;
  transform: translateY(10px);
}

.task-card-leave-to {
  opacity: 0;
  transform: translateY(-10px);
}

/* Статус бейдж */
/* Стили для статус-бейджей (для будущего использования) */
/* Статус задачи будет отображаться в следующих этапах */
/*
.status-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 500;
  white-space: nowrap;
}

.status-icon {
  font-size: 14px;
}

.status-badge--success {
  background-color: #d1fae5;
  color: #065f46;
}

.status-badge--danger {
  background-color: #fee2e2;
  color: #991b1b;
}

.status-badge--warning {
  background-color: #fef3c7;
  color: #92400e;
}

.status-badge--secondary {
  background-color: #f3f4f6;
  color: #6b7280;
}
*/

/* Состояния загрузки, ошибки, пустое */
.loading-state,
.error-state,
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40px 20px;
  text-align: center;
}

.loading-spinner {
  width: 40px;
  height: 40px;
  border: 4px solid #e5e7eb;
  border-top-color: #007bff;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 16px;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.error-icon,
.empty-state-icon {
  font-size: 48px;
  margin-bottom: 16px;
}

.error-title,
.empty-state-message {
  font-size: 16px;
  font-weight: 600;
  color: #111827;
  margin-bottom: 8px;
}

.error-message {
  font-size: 14px;
  color: #6b7280;
  margin-bottom: 16px;
}

.btn-retry {
  padding: 8px 16px;
  background-color: #007bff;
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.btn-retry:hover {
  background-color: #0056b3;
}

/* Transition анимации */
.level-enter-active,
.level-leave-active {
  transition: all 0.3s ease;
}

.level-enter-from {
  opacity: 0;
  transform: translateX(20px);
}

.level-leave-to {
  opacity: 0;
  transform: translateX(-20px);
}

.loading-enter-active,
.loading-leave-active {
  transition: all 0.2s ease;
}

.loading-enter-from,
.loading-leave-to {
  opacity: 0;
}

/* Адаптивность для мобильных */
@media (max-width: 768px) {
  .task-card {
    padding: 12px;
  }
  
  .task-card__header {
    flex-direction: column;
    align-items: flex-start;
    gap: 8px;
  }
  
  .task-card__title {
    font-size: 14px;
  }
  
  .task-card__dates {
    gap: 6px;
  }
  
  .task-card__date-item {
    font-size: 13px;
  }
  
  .date-label {
    min-width: 70px;
    font-size: 12px;
  }
  
  .tasks-pagination {
    flex-wrap: wrap;
    gap: 4px;
  }
  
  .pagination-btn {
    padding: 6px 12px;
    font-size: 12px;
  }
  
  .pagination-page {
    min-width: 32px;
    height: 32px;
    padding: 0 8px;
    font-size: 12px;
  }
}
```

---

## Обработка ошибок и состояний

### Состояния компонента

1. **Загрузка задач:**
   - Показывать спиннер и текст "Загрузка задач..."
   - Блокировать взаимодействие с попапом

2. **Ошибка загрузки:**
   - Показывать иконку ошибки, заголовок и сообщение
   - Кнопка "Повторить" для повторной попытки
   - Логирование ошибки в консоль

3. **Пустое состояние:**
   - Показывать иконку и сообщение "Нет задач для отображения"
   - Не показывать таблицу

4. **Успешная загрузка:**
   - Отображать таблицу с задачами
   - Подсветка просроченных задач

### Примеры обработки ошибок

```javascript
// В методе loadTasksDetails
try {
  // ... загрузка данных ...
} catch (err) {
  console.error('[TimeTrackingDetailModal] Error loading tasks details:', err);
  
  // Определить тип ошибки
  if (err.message?.includes('network') || err.message?.includes('fetch')) {
    tasksError.value = 'Ошибка сети. Проверьте подключение к интернету.';
  } else if (err.message?.includes('timeout')) {
    tasksError.value = 'Превышено время ожидания. Попробуйте позже.';
  } else if (err.message?.includes('403') || err.message?.includes('401')) {
    tasksError.value = 'Ошибка доступа. Проверьте права доступа.';
  } else {
    tasksError.value = err.message || 'Ошибка загрузки задач';
  }
  
  enrichedTasks.value = [];
} finally {
  isLoadingTasks.value = false;
}
```

---

## Интеграция с существующим кодом

### Изменения в TicketsTimeTrackingDashboard.vue

**Файл:** `vue-app/src/components/tickets-time-tracking/TicketsTimeTrackingDashboard.vue`

**Изменения не требуются** — попап уже подключен через:

```vue
<TimeTrackingDetailModal
  v-if="selectedCell"
  :cell-data="selectedCell"
  @close="closeDetailModal"
/>
```

### Расширение timeTrackingService.js

**Файл:** `vue-app/src/services/tickets-time-tracking/timeTrackingService.js`

**Добавить метод `getTasksDetails`** (см. раздел "Расширение сервиса" выше).

---

## Тестирование

### Чек-лист тестирования

1. **Функциональное тестирование:**
   - [ ] Кнопка "Список задач" отображается только при наличии задач
   - [ ] При клике на кнопку происходит переход на уровень 2
   - [ ] Кнопка "Назад" возвращает на уровень 1
   - [ ] Все поля задач отображаются корректно
   - [ ] Статусы задач определяются правильно
   - [ ] Просроченные задачи выделены визуально

2. **Тестирование данных:**
   - [ ] Задачи загружаются корректно
   - [ ] Даты форматируются правильно
   - [ ] Обработка отсутствующих полей (null, undefined)
   - [ ] Обработка пустого списка задач

3. **Тестирование ошибок:**
   - [ ] Ошибка сети отображается корректно
   - [ ] Кнопка "Повторить" работает
   - [ ] Ошибка API обрабатывается правильно

4. **Тестирование UI/UX:**
   - [ ] Плавные переходы между уровнями
   - [ ] Адаптивность на мобильных устройствах
   - [ ] Читаемость таблицы на разных размерах экрана
   - [ ] Соответствие стилям других попапов

5. **Производительность:**
   - [ ] Загрузка задач не блокирует интерфейс
   - [ ] Большое количество задач (50+) отображается корректно
   - [ ] Нет утечек памяти при переключении уровней

---

## Структурный план реализации

### Этап 1: Анализ и подготовка (Backend)

**Цель:** Расширить backend API для получения детальной информации о задачах.

**Задачи:**
1. Изучить документацию Bitrix24 API (`tasks.task.list`, `tasks.task.get`)
   - Документация: https://apidocs.bitrix24.ru/rest/tasks.task.list
   - Определить точные названия полей
   - Проверить доступность полей: `TITLE`, `CREATED_DATE`, `START_DATE_PLAN`, `END_DATE_PLAN`, `DEADLINE`, `CLOSED_DATE`, `STATUS`, `STAGE_ID`

2. Расширить API endpoint `api/tickets-time-tracking-sector-1c.php`
   - Добавить параметр `includeTaskDetails: true`
   - Добавить параметры пагинации: `page` (по умолчанию 1), `perPage` (по умолчанию 10)
   - Реализовать функцию `getTasksDetails()` для получения детальной информации о задачах
   - Использовать батч-запросы `CRest::callBatch()` для оптимизации
   - Обработать ошибки при загрузке задач

3. Обновить формат ответа API
   - Добавить поле `tasks[]` с детальной информацией
   - Добавить метаданные пагинации: `totalTasks`, `currentPage`, `perPage`, `totalPages`

**Результат:** Backend API возвращает детальную информацию о задачах с поддержкой пагинации.

---

### Этап 2: Расширение сервиса (Frontend)

**Цель:** Добавить метод загрузки детальной информации о задачах в сервис.

**Задачи:**
1. Расширить `timeTrackingService.js`
   - Добавить метод `getTasksDetails()` с параметрами: `taskIds`, `employeeId`, `weekNumber`, `page`, `perPage`
   - Обработать ответ API с пагинацией
   - Обработать ошибки запроса

2. Обновить структуру данных
   - Определить интерфейс задачи с полями: `id`, `title`, `startDate`, `deadline`, `closedDate`, `status`, `elapsedTime`
   - Добавить типизацию (JSDoc комментарии)

**Результат:** Сервис готов к загрузке детальной информации о задачах.

---

### Этап 3: Модификация компонента попапа (Frontend)

**Цель:** Добавить кнопку "Список задач" и уровень 2 с карточками задач.

**Задачи:**
1. Добавить состояние навигации
   - Добавить `popupLevel` (1 или 2)
   - Добавить состояния: `enrichedTasks`, `isLoadingTasks`, `tasksError`
   - Добавить состояния пагинации: `currentPage`, `perPage`

2. Добавить кнопку "Список задач"
   - Разместить в попапе уровня 1 после блока "Итого"
   - Условие отображения: только если `tasksCount > 0`
   - Стилизация: primary кнопка с иконкой

3. Реализовать уровень 2 (список задач)
   - Добавить `<Transition>` для плавных переходов
   - Создать структуру уровня 2 с заголовком и кнопкой "Назад"
   - Реализовать функции: `goToTasksList()`, `goBack()`

4. Реализовать отображение карточек задач
   - Создать компонент карточки задачи (или inline в шаблоне)
   - Отобразить поля: номер, название, трудозатрата, даты (начало, дедлайн, завершение)
   - Добавить визуальное выделение просроченных задач
   - Использовать `<TransitionGroup>` для анимации появления карточек

5. Реализовать пагинацию
   - Добавить computed свойства: `totalPages`, `paginatedTasks`, `visiblePages`
   - Отобразить пагинацию только если `totalPages > 1`
   - Реализовать функцию `goToPage(page)`
   - При смене страницы перезагружать данные (или использовать кеш)

**Результат:** Попап поддерживает два уровня, отображает задачи в виде карточек с пагинацией.

---

### Этап 4: Стилизация и UX (Frontend)

**Цель:** Обеспечить единообразный стиль и хороший UX.

**Задачи:**
1. Стилизация карточек задач
   - Базовые стили карточек (фон, границы, отступы)
   - Стили для разных состояний (просрочено, завершено, в работе)
   - Hover-эффекты и переходы

2. Стилизация пагинации
   - Кнопки "Предыдущая"/"Следующая"
   - Номера страниц с активным состоянием
   - Адаптивность для мобильных устройств

3. Адаптивность
   - Оптимизация для мобильных устройств (< 768px)
   - Компактное отображение карточек
   - Адаптивная пагинация

4. Анимации и переходы
   - Плавные переходы между уровнями (`<Transition>`)
   - Анимация появления карточек (`<TransitionGroup>`)
   - Индикаторы загрузки

**Результат:** Попап имеет единообразный стиль и хороший UX на всех устройствах.

---

### Этап 5: Обработка ошибок и состояний (Frontend)

**Цель:** Обеспечить корректную обработку всех состояний.

**Задачи:**
1. Состояние загрузки
   - Показывать спиннер при загрузке задач
   - Блокировать взаимодействие во время загрузки

2. Состояние ошибки
   - Отображать сообщение об ошибке
   - Кнопка "Повторить" для повторной попытки
   - Логирование ошибок в консоль

3. Пустое состояние
   - Сообщение "Нет задач для отображения"
   - Иконка для визуального обозначения

4. Обработка отсутствующих данных
   - Fallback для отсутствующих полей (например, `-` для дат)
   - Валидация данных перед отображением

**Результат:** Все состояния обрабатываются корректно, пользователь получает понятную обратную связь.

---

### Этап 6: Тестирование

**Цель:** Убедиться в корректной работе всех функций.

**Задачи:**
1. Функциональное тестирование
   - Кнопка "Список задач" отображается и работает
   - Переходы между уровнями работают корректно
   - Карточки задач отображаются с правильными данными
   - Пагинация работает (если задач > 10)

2. Тестирование данных
   - Корректное отображение всех полей
   - Форматирование дат работает правильно
   - Визуальное выделение просроченных задач

3. Тестирование ошибок
   - Обработка ошибок сети
   - Обработка ошибок API
   - Повторная попытка загрузки

4. Тестирование UI/UX
   - Адаптивность на разных устройствах
   - Плавность анимаций
   - Читаемость карточек

**Результат:** Все функции работают корректно, готово к использованию.

---

### Этап 7: Будущие улучшения (не входит в текущую задачу)

**Что будет реализовано позже:**
1. **Отображение статуса задачи**
   - Добавить поле `STATUS` в карточку
   - Расшифровка статусов из документации Bitrix24
   - Цветные бейджи для статусов

2. **Открытие задачи в Bitrix24**
   - При клике на карточку открывать задачу в Bitrix24
   - Использовать URL задачи из Bitrix24 API

3. **Дополнительные поля**
   - Ответственный за задачу
   - Создатель задачи
   - Связанный тикет (если есть)

4. **Фильтрация и сортировка**
   - Фильтр по статусу
   - Сортировка по дате, названию, трудозатрате

---

## Примечания

- **Формат отображения:** Используются карточки вместо таблицы для лучшей масштабируемости
- **Статус задачи:** Поле `STATUS` будет добавлено в следующих этапах, сейчас не отображается
- **Пагинация:** Реализована для случаев, когда задач больше 10
- **Открытие задачи:** Клик на карточку будет открывать задачу в Bitrix24 (реализуем позже)
- **Backend расширение:** Обязательно расширить backend API для получения детальной информации
- **Аналоги:** Использовать примеры из TASK-042 и TASK-043 для реализации уровней
- **Производительность:** При большом количестве задач пагинация обеспечивает оптимальную загрузку
- **Кеширование:** Рассмотреть кеширование детальной информации о задачах для оптимизации

