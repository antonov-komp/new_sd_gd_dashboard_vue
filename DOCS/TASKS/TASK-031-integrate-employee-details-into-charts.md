# TASK-031: Интеграция детализации по сотрудникам в графики состояния сектора 1С

**Дата создания:** 2025-12-11 14:55 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js) + UX/UI-дизайнер

---

## 📋 Описание

Убрать отдельный блок детализации по сотрудникам и интегрировать данные сотрудников непосредственно в графики состояния сектора 1С. Суммарное количество тикетов по этапам остается как есть, но при взаимодействии с графиками (наведение, клик) должно отображаться разбиение на составляющие каждого сотрудника.

**Текущее состояние:**
- Графики состояния (линейный, столбчатый, круговая) показывают суммарное количество тикетов по этапам
- Отдельный блок "Детализация по сотрудникам" под графиками показывает разбивку по сотрудникам для каждого этапа

**Целевое состояние:**
- Графики показывают суммарное количество тикетов по этапам (как сейчас)
- Данные по сотрудникам интегрированы в сами графики (tooltips, легенда, интерактивные элементы)
- Отдельный блок детализации удален

---

## 🎯 Контекст

### Модуль "График состояния сектора 1С"

**Компонент:** `GraphStateChart.vue`  
**Расположение:** `vue-app/src/components/graph-state/GraphStateChart.vue`

**Типы графиков:**
1. **Линейный** (`line`) — показывает изменение количества тикетов по этапам во времени
2. **Столбчатый** (`bar`) — столбчатая диаграмма с количеством тикетов по этапам
3. **Круговая** (`doughnut`) — круговая диаграмма с распределением тикетов по этапам

**Этапы:**
- `formed` — Сформировано обращение (синий `#007bff`)
- `review` — Рассмотрение ТЗ (жёлтый `#ffc107`)
- `execution` — Исполнение (зелёный `#28a745`)

**Текущая детализация:**
- Блок `employees-details` (строки 109-143 в `GraphStateChart.vue`)
- Показывает для каждого этапа:
  - Общее количество тикетов
  - Список сотрудников с количеством их тикетов на этапе
  - Тикеты из "Неразобранного" (Хранитель объектов, ID: 1051)

**Данные доступны через:**
- `getStageTotalCount(stageId)` — общее количество тикетов на этапе
- `getEmployeesForStage(stageId)` — список сотрудников с количеством тикетов на этапе
- `snapshots.value.current/weekEnd/weekStart` — слепки с данными

---

## 📝 Модули и компоненты

### Файлы для изменения:

1. **`vue-app/src/components/graph-state/GraphStateChart.vue`**
   - Удалить блок `employees-details` (строки 109-143)
   - Обновить `chartOptions` для интеграции данных сотрудников в tooltips
   - Добавить интерактивные элементы для отображения детализации
   - Обновить функции подготовки данных для графиков

2. **`vue-app/src/services/graph-state/SectorDataAdapter.js`** (если требуется)
   - Проверить структуру данных для доступа к информации о сотрудниках

3. **`vue-app/src/utils/chart-config.js`** (если требуется)
   - Обновить конфигурацию Chart.js для поддержки детализации

### Файлы для создания (если требуется):

1. **`vue-app/src/components/graph-state/EmployeeTooltip.vue`** (опционально)
   - Компонент для отображения детализации по сотрудникам в tooltip

2. **`vue-app/src/utils/graph-state/employeeChartUtils.js`** (опционально)
   - Утилиты для подготовки данных сотрудников для графиков

---

## 🔗 Зависимости

### От каких задач зависит:
- Нет зависимостей (самостоятельная задача)

### Какие задачи зависят от этой:
- Возможные будущие задачи по улучшению визуализации данных сотрудников

### Связанные документы:
- `DOCS/GUIDES/ui-analysis-graph-state-sector-1c.md` — анализ дизайна модуля
- `DOCS/TASKS/ARCHIVE/TASK-019-11-01.md` — добавление детализации по сотрудникам
- `vue-app/src/components/graph-state/GraphStateChart.vue` — текущая реализация

---

## 📋 Ступенчатые подзадачи

### Этап 1: Анализ и проектирование интеграции детализации в графики

**Цель:** Определить оптимальный способ отображения данных сотрудников в каждом типе графика.

#### 1.1. Анализ текущей реализации детализации
- Изучить структуру данных сотрудников в `getEmployeesForStage()`
- Проанализировать формат данных в `snapshots.value`
- Определить, какие данные доступны для каждого этапа

#### 1.2. Проектирование UX для каждого типа графика
- **Линейный график:** Определить способ отображения детализации (tooltip при наведении на точку, легенда с разбивкой)
- **Столбчатый график:** Определить способ отображения детализации (stacked bars для сотрудников, tooltip с детализацией)
- **Круговая диаграмма:** Определить способ отображения детализации (вложенные сектора, tooltip с разбивкой)

#### 1.3. Определение требований к tooltips
- Структура tooltip для каждого типа графика
- Формат отображения данных сотрудников (таблица, список, прогресс-бары)
- Обработка большого количества сотрудников (группировка, "и другие")

#### 1.4. Создание макетов/прототипов (UX/UI-дизайнер)
- Визуальные макеты для каждого типа графика с интегрированной детализацией
- Примеры tooltips с данными сотрудников
- Адаптивные версии для мобильных устройств

---

### Этап 2: Подготовка данных для интеграции в графики

**Цель:** Подготовить структуру данных сотрудников для каждого типа графика.

#### 2.1. Создание функции подготовки данных сотрудников для линейного графика
- Функция `prepareLineChartEmployeeData(stageId, snapshot)`
- Структура данных: массив сотрудников с количеством тикетов для каждой точки графика
- Обработка данных для разных временных точек (начало недели, конец недели, текущее состояние)

#### 2.2. Создание функции подготовки данных сотрудников для столбчатого графика
- Функция `prepareBarChartEmployeeData(stageId, snapshot)`
- Структура данных для stacked bars (каждый сотрудник — отдельный слой столбца)
- Обработка цветов для каждого сотрудника (оттенки цвета этапа)

#### 2.3. Создание функции подготовки данных сотрудников для круговой диаграммы
- Функция `prepareDoughnutChartEmployeeData(stageId, snapshot)`
- Структура данных для вложенных секторов или отдельной детализации
- Обработка группировки сотрудников при большом количестве

#### 2.4. Создание утилит для форматирования данных сотрудников
- Функция `formatEmployeeTooltipData(employees, stageId)` — форматирование для tooltip
- Функция `getEmployeeColor(employeeId, stageColor)` — получение цвета для сотрудника
- Функция `groupEmployeesByCount(employees, maxVisible)` — группировка при большом количестве

---

### Этап 3: Интеграция детализации в линейный и столбчатый графики

**Цель:** Реализовать отображение данных сотрудников в линейном и столбчатом графиках.

#### 3.1. Обновление функции `prepareChartData()` для линейного графика
- Добавить данные сотрудников в структуру datasets
- Интегрировать информацию о сотрудниках для каждой точки графика
- Сохранить обратную совместимость с текущим отображением суммарных значений

#### 3.2. Обновление функции `prepareChartData()` для столбчатого графика
- Реализовать stacked bars для отображения сотрудников внутри каждого столбца
- Настроить цвета для каждого сотрудника (оттенки цвета этапа)
- Обработать случай, когда сотрудников больше, чем видимых слоев

#### 3.3. Обновление `chartOptions` для tooltips
- Обновить callback `tooltip.callbacks.label` для отображения детализации по сотрудникам
- Добавить callback `tooltip.callbacks.afterBody` для дополнительной информации о сотрудниках
- Реализовать форматирование данных сотрудников в tooltip (таблица, список)

#### 3.4. Обновление легенды графика
- Добавить интерактивность: клик по легенде этапа показывает/скрывает детализацию по сотрудникам
- Обновить отображение легенды для stacked bars (если применимо)

---

### Этап 4: Интеграция детализации в круговую диаграмму

**Цель:** Реализовать отображение данных сотрудников в круговой диаграмме.

#### 4.1. Обновление функции `prepareDoughnutData()` для детализации
- Добавить данные сотрудников в структуру данных круговой диаграммы
- Определить способ отображения (вложенные сектора или отдельная детализация при клике)

#### 4.2. Реализация интерактивности круговой диаграммы
- Обработка клика по сектору этапа для отображения детализации по сотрудникам
- Реализация модального окна или выпадающего блока с детализацией
- Альтернатива: вложенная круговая диаграмма (donut chart) с детализацией

#### 4.3. Обновление tooltip для круговой диаграммы
- Обновить callback `tooltip.callbacks.label` для отображения детализации по сотрудникам
- Форматирование: показывать общее количество и разбивку по сотрудникам

#### 4.4. Обработка большого количества сотрудников
- Группировка сотрудников с малым количеством тикетов в "Другие"
- Ограничение количества отображаемых сотрудников в tooltip

---

### Этап 5: Удаление блока детализации и финальная полировка

**Цель:** Удалить отдельный блок детализации и завершить интеграцию.

#### 5.1. Удаление блока `employees-details`
- Удалить HTML-разметку блока детализации (строки 109-143)
- Удалить связанные стили `.employees-details`, `.stage-details`, `.employee-detail-item`
- Удалить переменную `showEmployeesDetails` (если больше не используется)

#### 5.2. Удаление неиспользуемых функций (опционально)
- Проверить использование функций `getStageTotalCount()`, `getEmployeesForStage()`, `getKeeperTicketsCountForStage()`
- Если функции используются только в удаленном блоке, перенести их логику в новые функции подготовки данных
- Если функции используются в других местах, оставить их

#### 5.3. Тестирование всех типов графиков
- Проверить отображение детализации в линейном графике
- Проверить отображение детализации в столбчатом графике
- Проверить отображение детализации в круговой диаграмме
- Проверить работу tooltips на разных устройствах

#### 5.4. Адаптивность и производительность
- Проверить отображение tooltips на мобильных устройствах
- Оптимизировать производительность при большом количестве сотрудников
- Проверить работу с разными разрешениями экрана

#### 5.5. Обновление документации
- Обновить `DOCS/GUIDES/ui-analysis-graph-state-sector-1c.md` с описанием новой реализации
- Добавить примеры использования интегрированной детализации
- Обновить скриншоты/описания интерфейса

---

## 💻 Технические требования

### Vue.js специфика:

- **Версия Vue.js:** 3.x (Composition API)
- **Chart.js:** Использовать существующую библиотеку `vue-chartjs`
- **Computed properties:** Использовать `computed()` для реактивности данных
- **Props validation:** Сохранить текущую структуру props

### Chart.js специфика:

- **Tooltips:** Использовать кастомные callbacks для форматирования
- **Legends:** Обновить обработку кликов для интерактивности
- **Datasets:** Поддержка stacked bars для столбчатого графика
- **Responsive:** Сохранить адаптивность графиков

### Обратная совместимость:

- Суммарные значения по этапам должны отображаться как сейчас
- Графики должны работать без данных о сотрудниках (fallback)
- Сохранить все существующие функции сравнения слепков

### Производительность:

- Оптимизировать подготовку данных сотрудников (кеширование, мемоизация)
- Ограничить количество отображаемых сотрудников в tooltip (например, максимум 10)
- Группировать сотрудников с малым количеством тикетов

---

## 📝 Примеры кода

### 1. Подготовка данных сотрудников для tooltip

```javascript
/**
 * Форматирование данных сотрудников для tooltip
 * 
 * @param {Array} employees - Массив сотрудников с количеством тикетов
 * @param {string} stageId - ID этапа
 * @param {number} maxVisible - Максимальное количество видимых сотрудников
 * @returns {string} HTML-строка для tooltip
 */
function formatEmployeeTooltipData(employees, stageId, maxVisible = 10) {
  if (!employees || employees.length === 0) {
    return '<div>Нет данных о сотрудниках</div>';
  }
  
  // Сортировка по убыванию количества тикетов
  const sorted = [...employees].sort((a, b) => b.count - a.count);
  
  // Группировка при большом количестве
  const visible = sorted.slice(0, maxVisible);
  const others = sorted.slice(maxVisible);
  const othersCount = others.reduce((sum, emp) => sum + emp.count, 0);
  
  let html = '<div class="employee-tooltip">';
  html += '<div class="employee-tooltip-header">Детализация по сотрудникам:</div>';
  html += '<div class="employee-tooltip-list">';
  
  visible.forEach(emp => {
    html += `<div class="employee-tooltip-item">`;
    html += `<span class="employee-name">${emp.name}</span>`;
    html += `<span class="employee-count">${emp.count} тикетов</span>`;
    html += `</div>`;
  });
  
  if (othersCount > 0) {
    html += `<div class="employee-tooltip-item employee-tooltip-others">`;
    html += `<span class="employee-name">Другие (${others.length})</span>`;
    html += `<span class="employee-count">${othersCount} тикетов</span>`;
    html += `</div>`;
  }
  
  html += '</div></div>';
  return html;
}
```

### 2. Обновление tooltip callback для линейного графика

```javascript
tooltip: {
  callbacks: {
    label: (context) => {
      const label = context.dataset.label || '';
      const value = context.parsed.y || 0;
      
      // Основная информация
      let result = `${label}: ${value} тикетов`;
      
      // Добавление детализации по сотрудникам
      const stageKey = getStageKeyFromLabel(label);
      const snapshot = snapshots.value.current || snapshots.value.weekEnd;
      const employees = getEmployeesForStage(stageKey, snapshot);
      
      if (employees && employees.length > 0) {
        result += '\n\n' + formatEmployeeTooltipData(employees, stageKey);
      }
      
      return result;
    },
    afterBody: (tooltipItems) => {
      // Дополнительная информация о сотрудниках
      // (если требуется более детальное отображение)
      return [];
    }
  }
}
```

### 3. Подготовка данных для stacked bars (столбчатый график)

```javascript
/**
 * Подготовка данных для столбчатого графика с детализацией по сотрудникам
 * 
 * @param {string} stageId - ID этапа
 * @param {Object} snapshot - Слепок с данными
 * @returns {Array} Массив datasets для stacked bars
 */
function prepareStackedBarData(stageId, snapshot) {
  const employees = getEmployeesForStage(stageId, snapshot);
  if (!employees || employees.length === 0) {
    return null;
  }
  
  const stage = stages.find(s => s.id === stageId);
  const datasets = [];
  
  // Ограничение количества сотрудников для визуализации
  const maxEmployees = 5; // Максимум 5 слоев в столбце
  const visibleEmployees = employees.slice(0, maxEmployees);
  const others = employees.slice(maxEmployees);
  const othersCount = others.reduce((sum, emp) => sum + emp.count, 0);
  
  visibleEmployees.forEach((emp, index) => {
    const color = getEmployeeColor(emp.id, stage.color, index, maxEmployees);
    datasets.push({
      label: emp.name,
      data: [emp.count], // Количество тикетов сотрудника
      backgroundColor: color,
      borderColor: color,
      borderWidth: 1
    });
  });
  
  // Слой "Другие" для остальных сотрудников
  if (othersCount > 0) {
    datasets.push({
      label: `Другие (${others.length})`,
      data: [othersCount],
      backgroundColor: stage.color + '40', // Прозрачный цвет этапа
      borderColor: stage.color,
      borderWidth: 1
    });
  }
  
  return datasets;
}
```

---

## ✅ Критерии приёмки

### Функциональные требования:

- [ ] Отдельный блок детализации удален из интерфейса
- [ ] Данные сотрудников отображаются в tooltips линейного графика
- [ ] Данные сотрудников отображаются в tooltips столбчатого графика
- [ ] Данные сотрудников отображаются в tooltips/интерактивных элементах круговой диаграммы
- [ ] Суммарные значения по этапам отображаются как сейчас (без изменений)
- [ ] Детализация по сотрудникам доступна для всех временных точек (начало недели, конец недели, текущее состояние)

### Технические требования:

- [ ] Удален блок `employees-details` из HTML
- [ ] Удалены связанные стили детализации
- [ ] Обновлены функции подготовки данных для графиков
- [ ] Обновлены tooltip callbacks для отображения детализации
- [ ] Реализована обработка большого количества сотрудников (группировка)
- [ ] Сохранена обратная совместимость с текущим отображением

### UX/UI требования:

- [ ] Tooltips читаемы и информативны
- [ ] Детализация не перегружает интерфейс
- [ ] Адаптивность сохранена для всех типов графиков
- [ ] Интерактивность интуитивно понятна пользователю

### Производительность:

- [ ] Графики загружаются без задержек
- [ ] Tooltips отображаются быстро при наведении
- [ ] Нет проблем с производительностью при большом количестве сотрудников

---

## 🧪 Тестирование

### Шаги для тестирования:

1. **Проверка линейного графика:**
   - Открыть график состояния сектора 1С
   - Выбрать линейный график
   - Навести на точку графика
   - Проверить, что tooltip показывает суммарное количество и детализацию по сотрудникам

2. **Проверка столбчатого графика:**
   - Выбрать столбчатый график
   - Проверить отображение stacked bars (если реализовано)
   - Навести на столбец
   - Проверить tooltip с детализацией по сотрудникам

3. **Проверка круговой диаграммы:**
   - Выбрать круговую диаграмму
   - Навести на сектор этапа
   - Проверить tooltip с детализацией по сотрудникам
   - Проверить интерактивность (клик для детализации, если реализовано)

4. **Проверка удаления блока детализации:**
   - Убедиться, что блок "Детализация по сотрудникам" не отображается под графиком
   - Проверить, что все данные доступны через tooltips

5. **Проверка адаптивности:**
   - Проверить отображение tooltips на мобильных устройствах
   - Проверить работу на разных разрешениях экрана

### Ожидаемые результаты:

- ✅ Детализация по сотрудникам доступна через tooltips/интерактивные элементы
- ✅ Отдельный блок детализации удален
- ✅ Суммарные значения отображаются как сейчас
- ✅ Интерфейс не перегружен информацией
- ✅ Нет ошибок в консоли браузера

---

## 📚 Связанные документы

### Документация модуля:
- `DOCS/GUIDES/ui-analysis-graph-state-sector-1c.md` — анализ дизайна модуля
- `DOCS/TASKS/ARCHIVE/TASK-019-11-01.md` — добавление детализации по сотрудникам

### Исходный код:
- `vue-app/src/components/graph-state/GraphStateChart.vue` — компонент графика
- `vue-app/src/services/graph-state/SectorDataAdapter.js` — адаптер данных сектора
- `vue-app/src/utils/chart-config.js` — конфигурация Chart.js

---

## 🔍 Дополнительная информация

### Почему это важно:

1. **Улучшение UX:**
   - Детализация доступна непосредственно в графиках, не нужно прокручивать страницу
   - Более компактный интерфейс без отдельного блока детализации
   - Интерактивность делает данные более доступными

2. **Оптимизация пространства:**
   - Освобождение места на странице за счет удаления блока детализации
   - Более фокусный интерфейс на самих графиках

3. **Современный подход:**
   - Интеграция данных в визуализацию соответствует современным практикам UX
   - Tooltips и интерактивные элементы — стандартный способ отображения детализации

### Ограничения и соображения:

- **Большое количество сотрудников:** Требуется группировка и ограничение отображения
- **Мобильные устройства:** Tooltips могут быть менее удобны на маленьких экранах
- **Производительность:** Подготовка данных для всех сотрудников может замедлить отрисовку

### Будущие улучшения:

1. **Расширенная интерактивность:**
   - Клик по сектору/столбцу для открытия модального окна с детализацией
   - Фильтрация графиков по сотрудникам

2. **Визуализация трендов сотрудников:**
   - Отображение изменения количества тикетов сотрудника во времени
   - Сравнение производительности сотрудников

---

## 📝 История правок

- **2025-12-11 14:55 (UTC+3, Брест):** Создана задача TASK-031 с 5 этапами для интеграции детализации по сотрудникам в графики состояния сектора 1С

---

## 🎯 Этапы задачи (краткое резюме)

### Этап 1: Анализ и проектирование
- Анализ текущей реализации
- Проектирование UX для каждого типа графика
- Создание макетов/прототипов

### Этап 2: Подготовка данных
- Функции подготовки данных сотрудников для каждого типа графика
- Утилиты для форматирования и группировки

### Этап 3: Интеграция в линейный и столбчатый графики
- Обновление функций подготовки данных
- Обновление tooltips и легенды

### Этап 4: Интеграция в круговую диаграмму
- Обновление функции подготовки данных
- Реализация интерактивности
- Обновление tooltips

### Этап 5: Удаление блока детализации и финальная полировка
- Удаление блока `employees-details`
- Тестирование всех типов графиков
- Обновление документации

---

**Примечание:** Каждый этап будет детализирован в отдельных подзадачах по мере необходимости.

