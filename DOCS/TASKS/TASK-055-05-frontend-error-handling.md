# TASK-055-05: Обработка ошибок и состояний в попапе со списком задач

**Дата создания:** 2025-12-17 17:04 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связь с задачей:** Этап 5 из TASK-055  
**Зависимости:** TASK-055-03, TASK-055-04

---

## Цель этапа

Обеспечить корректную обработку всех состояний компонента (загрузка, ошибка, пустое состояние) и отсутствующих данных для улучшения UX.

---

## Контекст

- **Текущее состояние:**
  - Базовые состояния реализованы, но обработка ошибок может быть неполной
  - Нет обработки отсутствующих полей данных
  - Нет валидации данных перед отображением

- **Требуется:**
  1. Улучшить обработку состояния загрузки
  2. Реализовать детальную обработку ошибок
  3. Добавить пустое состояние
  4. Обработать отсутствующие данные

---

## Задачи этапа

### 1) Состояние загрузки

**Цель:** Показывать понятный индикатор загрузки и блокировать взаимодействие.

**Реализация:**

```vue
<Transition name="loading" mode="out-in">
  <div v-if="isLoadingTasks" key="loading" class="loading-state">
    <div class="loading-spinner"></div>
    <p>Загрузка задач...</p>
  </div>
  
  <!-- Остальные состояния -->
</Transition>
```

**Требования:**
- Спиннер отображается во время загрузки
- Текст "Загрузка задач..." для понятности
- Блокировка взаимодействия (можно добавить overlay)

---

### 2) Состояние ошибки

**Цель:** Отображать понятное сообщение об ошибке с возможностью повтора.

**Реализация:**

```vue
<div v-else-if="tasksError" key="error" class="error-state">
  <div class="error-icon">⚠️</div>
  <p class="error-title">Ошибка загрузки</p>
  <p class="error-message">{{ tasksError }}</p>
  <button class="btn btn-retry" @click="retryLoadTasks">Повторить</button>
</div>
```

**Обработка различных типов ошибок:**

```javascript
const loadTasksDetails = async () => {
  // ... код загрузки ...
  
  try {
    // ... загрузка данных ...
  } catch (err) {
    console.error('[TimeTrackingDetailModal] Error loading tasks details:', err);
    
    // Определить тип ошибки
    if (err.message?.includes('network') || err.message?.includes('fetch')) {
      tasksError.value = 'Ошибка сети. Проверьте подключение к интернету.';
    } else if (err.message?.includes('timeout')) {
      tasksError.value = 'Превышено время ожидания. Попробуйте позже.';
    } else if (err.message?.includes('403') || err.message?.includes('401')) {
      tasksError.value = 'Ошибка доступа. Проверьте права доступа.';
    } else if (err.message?.includes('404')) {
      tasksError.value = 'Данные не найдены.';
    } else {
      tasksError.value = err.message || 'Ошибка загрузки задач';
    }
    
    enrichedTasks.value = [];
  } finally {
    isLoadingTasks.value = false;
  }
};

const retryLoadTasks = () => {
  currentPage.value = 1; // Сброс на первую страницу
  loadTasksDetails();
};
```

**Логирование ошибок:**

```javascript
// В catch блоке
console.error('[TimeTrackingDetailModal] Error loading tasks details:', {
  error: err,
  taskIds: taskIds,
  employeeId: props.cellData.employee?.id,
  weekNumber: props.cellData.week?.weekNumber,
  timestamp: new Date().toISOString()
});
```

---

### 3) Пустое состояние

**Цель:** Показывать понятное сообщение при отсутствии задач.

**Реализация:**

```vue
<div v-else-if="enrichedTasks.length === 0" key="empty" class="empty-state">
  <div class="empty-state-icon">📋</div>
  <p class="empty-state-message">Нет задач для отображения</p>
</div>
```

**Условия отображения:**
- Массив `enrichedTasks` пустой
- Нет ошибки загрузки
- Загрузка завершена

---

### 4) Обработка отсутствующих данных

**Цель:** Обеспечить корректное отображение при отсутствии полей.

**Функции валидации и форматирования:**

```javascript
/**
 * Форматирование даты с обработкой отсутствующих значений
 */
const formatDate = (dateString) => {
  if (!dateString) return '-';
  
  try {
    const date = new Date(dateString);
    
    // Проверка на валидную дату
    if (isNaN(date.getTime())) {
      return '-';
    }
    
    return date.toLocaleDateString('ru-RU', {
      day: '2-digit',
      month: '2-digit',
      year: 'numeric'
    });
  } catch (e) {
    console.warn('[TimeTrackingDetailModal] Error formatting date:', dateString, e);
    return '-';
  }
};

/**
 * Проверка просроченности с обработкой отсутствующих значений
 */
const isOverdue = (deadline, closedDate) => {
  // Если задача уже завершена, не считаем просроченной
  if (closedDate) {
    try {
      const closed = new Date(closedDate);
      if (!isNaN(closed.getTime())) {
        return false;
      }
    } catch (e) {
      // Игнорируем ошибку парсинга
    }
  }
  
  if (!deadline) return false;
  
  try {
    const deadlineDate = new Date(deadline);
    if (isNaN(deadlineDate.getTime())) {
      return false;
    }
    
    const now = new Date();
    return now > deadlineDate;
  } catch (e) {
    console.warn('[TimeTrackingDetailModal] Error checking overdue:', deadline, e);
    return false;
  }
};

/**
 * Валидация данных задачи перед отображением
 */
const validateTask = (task) => {
  if (!task || typeof task !== 'object') {
    return false;
  }
  
  // Минимальные требования: должен быть ID
  if (!task.id || typeof task.id !== 'number') {
    console.warn('[TimeTrackingDetailModal] Invalid task (no ID):', task);
    return false;
  }
  
  return true;
};
```

**Использование в шаблоне:**

```vue
<div
  v-for="task in paginatedTasks.filter(validateTask)"
  :key="task.id"
  class="task-card"
>
  <!-- Использование formatDate для всех дат -->
  <span class="date-value">{{ formatDate(task.startDate) }}</span>
  <span 
    class="date-value"
    :class="{ 'date-value--overdue': isOverdue(task.deadline, task.closedDate) }"
  >
    {{ formatDate(task.deadline) || '-' }}
  </span>
</div>
```

---

### 5) Обработка граничных случаев

**Случаи:**

1. **Пустой массив taskIds:**
   ```javascript
   if (taskIds.length === 0) {
     enrichedTasks.value = [];
     return;
   }
   ```

2. **Отсутствие сотрудника или недели:**
   ```javascript
   if (!props.cellData?.week?.employees || !props.cellData.employee) {
     tasksError.value = 'Недостаточно данных для загрузки задач';
     return;
   }
   ```

3. **Задача без связанного сотрудника:**
   ```javascript
   const employee = props.cellData.week.employees.find(
     e => e.id === props.cellData.employee?.id
   );
   
   if (!employee || !employee.tasks || employee.tasks.length === 0) {
     enrichedTasks.value = [];
     return;
   }
   ```

4. **Некорректные данные из API:**
   ```javascript
   // В loadTasksDetails
   const response = await timeTrackingService.getTasksDetails({...});
   
   // Валидация ответа
   if (!response || !Array.isArray(response.tasks)) {
     console.error('[TimeTrackingDetailModal] Invalid response format:', response);
     tasksError.value = 'Некорректный формат данных от сервера';
     enrichedTasks.value = [];
     return;
   }
   
   // Фильтрация некорректных задач
   enrichedTasks.value = response.tasks.filter(validateTask);
   ```

---

## Критерии приёмки

- [ ] Состояние загрузки отображается корректно
- [ ] Состояние ошибки обрабатывается с понятным сообщением
- [ ] Кнопка "Повторить" работает корректно
- [ ] Пустое состояние отображается при отсутствии задач
- [ ] Отсутствующие поля обрабатываются (fallback значения)
- [ ] Валидация данных работает перед отображением
- [ ] Ошибки логируются в консоль с контекстом
- [ ] Граничные случаи обрабатываются корректно

---

## Тестирование

### 1. Тестирование состояний

**Тест 1: Состояние загрузки**
- Открыть попап уровня 2
- Проверить отображение спиннера и текста "Загрузка задач..."

**Тест 2: Состояние ошибки**
- Симулировать ошибку сети (отключить интернет)
- Проверить отображение сообщения об ошибке
- Проверить работу кнопки "Повторить"

**Тест 3: Пустое состояние**
- Открыть попап для сотрудника без задач
- Проверить отображение сообщения "Нет задач для отображения"

---

### 2. Тестирование обработки данных

**Тест 4: Отсутствующие поля**
- Загрузить задачу без дедлайна
- Проверить отображение "-" вместо даты

**Тест 5: Некорректные данные**
- Симулировать некорректный ответ API
- Проверить обработку ошибки

---

### 3. Тестирование граничных случаев

**Тест 6: Пустой массив taskIds**
- Открыть попап для сотрудника с пустым списком задач
- Проверить отсутствие ошибок

**Тест 7: Отсутствие данных о сотруднике**
- Симулировать отсутствие `cellData.employee`
- Проверить обработку ошибки

---

## Примечания

- **Логирование:** Все ошибки логируются с контекстом для отладки
- **Fallback значения:** Используем "-" для отсутствующих дат, "Без названия" для отсутствующих названий
- **Валидация:** Проверяем данные перед отображением для предотвращения ошибок

---

## Следующий этап

После завершения этого этапа переходим к **TASK-055-06: Тестирование**.


