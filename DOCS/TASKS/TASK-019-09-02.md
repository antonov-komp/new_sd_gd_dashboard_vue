# TASK-019-09-02: Настройка прав доступа, тестирование переходов и финальная интеграция дашборда

**Дата создания:** 2025-12-08 10:17 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 9, часть 2)

---

## 📋 Цель задачи

Настроить права доступа к дашборду "График состояния" (просмотр для всех, создание слепков — только администраторы), протестировать все переходы между страницами, провести финальную интеграцию и убедиться, что всё работает корректно.

---

## 🎯 Контекст

После добавления маршрута и интеграции в навигацию (TASK-019-09-01) необходимо:
- Настроить права доступа к дашборду (просмотр для всех, создание слепков — только администраторы)
- Протестировать все переходы между страницами
- Проверить работу навигации в разных сценариях
- Убедиться, что права доступа работают корректно
- Провести финальную проверку интеграции

**Требования:**
- Просмотр дашборда доступен всем авторизованным пользователям
- Создание слепков доступно только администраторам
- Навигация работает корректно во всех направлениях
- Ошибки доступа обрабатываются корректно
- Все переходы протестированы

**Зависимости:**
- Этап 9, часть 1 (TASK-019-09-01) должна быть завершена — маршрут и навигация добавлены
- Система проверки прав доступа существует (`access-config.js` или `AccessControlService`)
- Компонент `CreateSnapshotButton.vue` уже имеет проверку прав администратора

---

## 🔍 Анализ существующей системы прав доступа

### Access Control Service

**Расположение:** `vue-app/src/services/AccessControlService.js` или `vue-app/src/config/access-config.js`

**Типичные функции:**
- `isAdmin()` — проверка, является ли пользователь администратором
- `hasAccess(permission)` — проверка наличия конкретного права
- `checkAccess(permission)` — проверка с выбрасыванием ошибки

**Пример структуры:**
```javascript
export function isAdmin() {
  // Проверка прав администратора
  const user = getCurrentUser();
  return user?.role === 'admin' || user?.isAdmin === true;
}

export function hasAccess(permission) {
  // Проверка конкретного права
  const user = getCurrentUser();
  return user?.permissions?.includes(permission) || false;
}
```

### Navigation Guards в роутере

**Назначение:** Проверка прав доступа перед переходом на маршрут.

**Пример:**
```javascript
router.beforeEach((to, from, next) => {
  if (to.meta.requiresAuth && !isAuthenticated()) {
    next('/login');
  } else if (to.meta.adminOnly && !isAdmin()) {
    next('/');
    // Показать уведомление об отсутствии прав
  } else {
    next();
  }
});
```

---

## 📝 Что нужно сделать

### 1. Настроить права доступа к маршруту

**Расположение:** `vue-app/src/router/index.js`

**Действия:**
- Добавить navigation guard для проверки прав доступа
- Настроить метаданные маршрута для контроля доступа
- Обработать случаи отсутствия прав доступа

**Пример:**
```javascript
import { createRouter, createWebHashHistory } from 'vue-router';
import { isAdmin, isAuthenticated } from '@/config/access-config.js';
import { useNotifications } from '@/composables/useNotifications.js';

const router = createRouter({
  history: createWebHashHistory(),
  routes: [
    // ... другие маршруты ...
    {
      path: '/dashboard/graph-state',
      name: 'dashboard-graph-state',
      component: () => import('@/components/graph-state/GraphStateDashboard.vue'),
      meta: {
        title: 'График состояния сектора 1С',
        description: 'Визуализация изменений состояния сектора во времени',
        requiresAuth: true, // Требуется авторизация
        // adminOnly: false — просмотр доступен всем авторизованным
      }
    }
  ]
});

// Navigation guard для проверки прав доступа
router.beforeEach((to, from, next) => {
  // Проверка авторизации
  if (to.meta.requiresAuth && !isAuthenticated()) {
    // Перенаправление на страницу входа или стартовую страницу
    next({ name: 'index' });
    return;
  }

  // Проверка прав администратора (если требуется)
  if (to.meta.adminOnly && !isAdmin()) {
    // Показать уведомление об отсутствии прав
    const notifications = useNotifications();
    notifications.error('У вас нет прав доступа к этой странице');
    next({ name: 'index' });
    return;
  }

  next();
});

export default router;
```

---

### 2. Настроить права доступа в компоненте CreateSnapshotButton

**Расположение:** `vue-app/src/components/graph-state/CreateSnapshotButton.vue`

**Проверка:** Убедиться, что компонент уже имеет проверку прав администратора (из TASK-019-05-01).

**Если проверка отсутствует, добавить:**
```vue
<template>
  <div v-if="isUserAdmin" class="create-snapshot-button">
    <button 
      @click="handleCreateSnapshot"
      class="btn-create-snapshot"
      :disabled="isCreating"
    >
      <span v-if="!isCreating">Создать слепок</span>
      <span v-else>Создание...</span>
    </button>
  </div>
</template>

<script setup>
import { computed } from 'vue';
import { isAdmin } from '@/config/access-config.js';

const isUserAdmin = computed(() => isAdmin());
</script>
```

---

### 3. Добавить проверку прав в GraphStateDashboard

**Расположение:** `vue-app/src/components/graph-state/GraphStateDashboard.vue`

**Действия:**
- Проверить права пользователя при монтировании компонента
- Скрыть/показать элементы в зависимости от прав
- Обработать случаи отсутствия прав доступа

**Пример:**
```vue
<template>
  <div class="graph-state-dashboard">
    <div class="dashboard-header">
      <div class="header-content">
        <h1 class="dashboard-title">График состояния сектора 1С</h1>
      </div>
      <div class="header-actions">
        <!-- Кнопка создания слепка видна только администраторам -->
        <CreateSnapshotButton 
          v-if="isUserAdmin"
          :user="currentUser"
          @snapshot-created="handleSnapshotCreated"
        />
      </div>
    </div>
    <!-- ... остальной контент ... -->
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue';
import { isAdmin, getCurrentUser } from '@/config/access-config.js';
import CreateSnapshotButton from './CreateSnapshotButton.vue';

const currentUser = ref(null);
const isUserAdmin = computed(() => isAdmin());

onMounted(() => {
  // Получение текущего пользователя
  currentUser.value = getCurrentUser();
  
  // Проверка прав доступа (опционально, если нужна дополнительная проверка)
  if (!isUserAdmin.value) {
    console.log('Пользователь не является администратором. Создание слепков недоступно.');
  }
});
</script>
```

---

### 4. Протестировать переходы между страницами

**Сценарии для тестирования:**

#### 4.1. Переход со стартовой страницы
- Открыть стартовую страницу (`/`)
- Найти отчёт "График состояния" в списке отчётов
- Кликнуть на отчёт
- Проверить, что произошёл переход на `/dashboard/graph-state`
- Проверить, что дашборд загрузился корректно

#### 4.2. Переход из дашборда сектора 1С
- Открыть дашборд сектора 1С (`/dashboard/sector-1c`)
- Найти кнопку/ссылку "График состояния"
- Кликнуть на кнопку/ссылку
- Проверить, что произошёл переход на `/dashboard/graph-state`
- Проверить, что дашборд загрузился корректно

#### 4.3. Переход обратно к дашборду сектора 1С
- Находясь на дашборде "График состояния"
- Кликнуть на хлебные крошки или кнопку возврата
- Проверить, что произошёл переход на `/dashboard/sector-1c`
- Проверить, что дашборд сектора 1С загрузился корректно

#### 4.4. Прямой переход по URL
- Открыть браузер и перейти на `/dashboard/graph-state`
- Проверить, что дашборд загрузился корректно
- Проверить, что навигация работает

#### 4.5. Переход через кнопку "Назад" браузера
- Перейти на дашборд "График состояния"
- Нажать кнопку "Назад" браузера
- Проверить, что произошёл возврат на предыдущую страницу
- Проверить, что состояние предыдущей страницы сохранено

---

### 5. Протестировать права доступа

**Сценарии для тестирования:**

#### 5.1. Просмотр дашборда (все авторизованные пользователи)
- Войти как обычный пользователь (не администратор)
- Перейти на дашборд "График состояния"
- Проверить, что дашборд отображается
- Проверить, что график загружается и отображается
- Проверить, что кнопка "Создать слепок" не отображается (или скрыта)

#### 5.2. Создание слепков (только администраторы)
- Войти как администратор
- Перейти на дашборд "График состояния"
- Проверить, что кнопка "Создать слепок" отображается
- Кликнуть на кнопку "Создать слепок"
- Проверить, что модальное окно открывается
- Создать слепок
- Проверить, что слепок создан успешно

#### 5.3. Попытка доступа без авторизации
- Выйти из системы (если возможно)
- Попытаться перейти на `/dashboard/graph-state`
- Проверить, что произошло перенаправление на стартовую страницу или страницу входа
- Проверить, что отображается сообщение об отсутствии прав (если реализовано)

#### 5.4. Попытка создания слепка не администратором
- Войти как обычный пользователь
- Попытаться создать слепок через прямой вызов API (если возможно)
- Проверить, что запрос отклонён с ошибкой доступа
- Проверить, что UI не позволяет создать слепок

---

### 6. Провести финальную проверку интеграции

**Чек-лист финальной проверки:**

- [ ] Дашборд отображается на стартовой странице в списке отчётов
- [ ] Иконка и описание отчёта корректны
- [ ] Переход по клику на отчёт работает
- [ ] Маршрут `/dashboard/graph-state` работает корректно
- [ ] Навигация из дашборда сектора 1С работает
- [ ] Навигация обратно к дашборду сектора 1С работает
- [ ] Хлебные крошки отображаются корректно
- [ ] Права доступа работают (просмотр для всех, создание для администраторов)
- [ ] Кнопка "Создать слепок" отображается только для администраторов
- [ ] Ошибки доступа обрабатываются корректно
- [ ] Все переходы работают без ошибок в консоли
- [ ] Дашборд работает в разных браузерах (Chrome, Firefox, Safari)
- [ ] Дашборд работает на мобильных устройствах (если применимо)

---

## 🏗️ Архитектура решения

### Структура проверки прав доступа

```
┌─────────────────────────────────────┐
│  Navigation Guard (router/index.js) │
│  - Проверка requiresAuth            │
│  - Проверка adminOnly (если нужно)  │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  GraphStateDashboard.vue            │
│  - Проверка isAdmin()               │
│  - Условное отображение элементов    │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  CreateSnapshotButton.vue           │
│  - Проверка isAdmin()               │
│  - Скрытие для не-администраторов   │
└─────────────────────────────────────┘
```

### Последовательность проверки прав

1. **Navigation Guard** — проверка при переходе на маршрут
2. **Компонент дашборда** — проверка при монтировании
3. **Компонент кнопки** — проверка перед отображением

---

## 💡 Примеры кода

### Полный пример navigation guard

```javascript
// vue-app/src/router/index.js

import { createRouter, createWebHashHistory } from 'vue-router';
import { isAdmin, isAuthenticated } from '@/config/access-config.js';

const router = createRouter({
  history: createWebHashHistory(),
  routes: [
    // ... другие маршруты ...
    {
      path: '/dashboard/graph-state',
      name: 'dashboard-graph-state',
      component: () => import('@/components/graph-state/GraphStateDashboard.vue'),
      meta: {
        title: 'График состояния сектора 1С',
        requiresAuth: true
      }
    }
  ]
});

// Navigation guard
router.beforeEach((to, from, next) => {
  // Проверка авторизации
  if (to.meta.requiresAuth && !isAuthenticated()) {
    console.warn('Unauthorized access attempt to:', to.path);
    next({ name: 'index' });
    return;
  }

  // Проверка прав администратора (если требуется)
  if (to.meta.adminOnly && !isAdmin()) {
    console.warn('Admin access required for:', to.path);
    // Можно показать уведомление через глобальный сервис
    next({ name: 'index' });
    return;
  }

  // Установка заголовка страницы (если есть)
  if (to.meta.title) {
    document.title = `${to.meta.title} - Bitrix24 Dashboard`;
  }

  next();
});

export default router;
```

### Пример проверки прав в GraphStateDashboard

```vue
<template>
  <div class="graph-state-dashboard">
    <!-- Проверка доступа -->
    <div v-if="!hasAccess" class="access-denied">
      <h2>Доступ запрещён</h2>
      <p>У вас нет прав для просмотра этого дашборда.</p>
      <router-link :to="{ name: 'index' }" class="btn-back">
        Вернуться на главную
      </router-link>
    </div>

    <!-- Основной контент (если есть доступ) -->
    <template v-else>
      <div class="dashboard-header">
        <div class="header-content">
          <h1 class="dashboard-title">График состояния сектора 1С</h1>
        </div>
        <div class="header-actions">
          <!-- Кнопка создания слепка только для администраторов -->
          <CreateSnapshotButton 
            v-if="isUserAdmin"
            :user="currentUser"
            @snapshot-created="handleSnapshotCreated"
          />
        </div>
      </div>
      <!-- ... остальной контент ... -->
    </template>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue';
import { isAdmin, isAuthenticated, getCurrentUser } from '@/config/access-config.js';
import CreateSnapshotButton from './CreateSnapshotButton.vue';

const currentUser = ref(null);
const hasAccess = computed(() => isAuthenticated());
const isUserAdmin = computed(() => isAdmin());

onMounted(() => {
  // Получение текущего пользователя
  currentUser.value = getCurrentUser();
  
  // Дополнительная проверка доступа (если нужна)
  if (!hasAccess.value) {
    console.warn('User does not have access to graph state dashboard');
  }
});
</script>
```

### Пример тестового скрипта для проверки переходов

```javascript
// test-navigation.js (для ручного тестирования в консоли браузера)

/**
 * Тестирование навигации дашборда "График состояния"
 * 
 * Запустить в консоли браузера на странице приложения
 */

async function testNavigation() {
  const router = window.__VUE_ROUTER__; // Если доступен глобально
  const tests = [];

  // Тест 1: Переход на дашборд
  try {
    await router.push({ name: 'dashboard-graph-state' });
    tests.push({ name: 'Переход на дашборд', passed: true });
  } catch (error) {
    tests.push({ name: 'Переход на дашборд', passed: false, error });
  }

  // Тест 2: Переход обратно
  try {
    await router.push({ name: 'dashboard-sector-1c' });
    tests.push({ name: 'Переход обратно', passed: true });
  } catch (error) {
    tests.push({ name: 'Переход обратно', passed: false, error });
  }

  // Тест 3: Проверка прав доступа
  const isAdmin = window.isAdmin?.() || false;
  tests.push({ 
    name: 'Проверка прав администратора', 
    passed: typeof isAdmin === 'boolean',
    value: isAdmin
  });

  // Вывод результатов
  console.table(tests);
  return tests;
}

// Запуск тестов
// testNavigation();
```

---

## ✅ Критерии приёмки

- [ ] Navigation guard добавлен в роутер для проверки прав доступа
- [ ] Метаданные маршрута настроены (requiresAuth, adminOnly если нужно)
- [ ] Проверка прав администратора работает в `CreateSnapshotButton.vue`
- [ ] Проверка прав работает в `GraphStateDashboard.vue`
- [ ] Кнопка "Создать слепок" отображается только для администраторов
- [ ] Все переходы между страницами протестированы и работают
- [ ] Права доступа протестированы для разных ролей пользователей
- [ ] Ошибки доступа обрабатываются корректно
- [ ] Навигация работает в разных браузерах
- [ ] Финальная проверка интеграции пройдена (все пункты чек-листа)
- [ ] Код соответствует стандартам проекта (ESLint, форматирование)
- [ ] Комментарии добавлены к основным блокам кода
- [ ] Документация обновлена (если нужно)

---

## 📝 Действия

1. **Настроить navigation guard:**
   - Открыть `vue-app/src/router/index.js`
   - Добавить `beforeEach` guard для проверки прав доступа
   - Настроить обработку ошибок доступа

2. **Проверить права в компонентах:**
   - Убедиться, что `CreateSnapshotButton.vue` проверяет права администратора
   - Добавить проверку прав в `GraphStateDashboard.vue` (если нужно)
   - Скрыть/показать элементы в зависимости от прав

3. **Протестировать переходы:**
   - Протестировать все сценарии переходов между страницами
   - Проверить работу навигации в разных направлениях
   - Проверить работу кнопки "Назад" браузера

4. **Протестировать права доступа:**
   - Протестировать просмотр для обычных пользователей
   - Протестировать создание слепков для администраторов
   - Протестировать попытки доступа без прав

5. **Провести финальную проверку:**
   - Пройти по всем пунктам чек-листа финальной проверки
   - Исправить найденные проблемы
   - Убедиться, что всё работает корректно

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущие задачи:**
  - `DOCS/TASKS/TASK-019-09-01.md` (добавление маршрута и навигации)
- **Следующая задача:** `DOCS/TASKS/TASK-019-10-01.md` (тестирование, оптимизация и документация)
- **Файлы для изменения:**
  - `vue-app/src/router/index.js`
  - `vue-app/src/components/graph-state/GraphStateDashboard.vue`
  - `vue-app/src/components/graph-state/CreateSnapshotButton.vue`
- **Связанные файлы:**
  - `vue-app/src/config/access-config.js` (или `AccessControlService.js`)

---

## ⏱️ Оценка времени

**1-2 часа**

**Разбивка:**
- Настройка navigation guard: 20-30 минут
- Проверка прав в компонентах: 15-20 минут
- Тестирование переходов: 20-30 минут
- Тестирование прав доступа: 15-20 минут
- Финальная проверка интеграции: 15-20 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-10-01: Тестирование, оптимизация производительности и документация дашборда "График состояния"**.

---

## 💡 Дополнительные замечания

### Безопасность

Важно помнить, что проверка прав на клиенте не является достаточной мерой безопасности:
- Всегда проверяйте права на сервере (в API endpoints)
- Клиентская проверка — только для UX (скрытие элементов)
- Серверная проверка — для реальной безопасности

### Обработка ошибок

При отсутствии прав доступа:
- Показывать понятные сообщения пользователю
- Логировать попытки несанкционированного доступа
- Перенаправлять на безопасную страницу

### Тестирование

Рекомендуется создать автоматические тесты для:
- Проверки прав доступа
- Навигации между страницами
- Отображения элементов в зависимости от прав

---

## История правок

- **2025-12-08 10:17 (UTC+3, Брест):** Создана задача

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

