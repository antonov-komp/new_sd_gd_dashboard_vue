# TASK-049: Возврат к отображению одной недели в линейном графике модуля «График приёма и закрытий сектора 1С»

**Дата создания:** 2025-12-17 08:27 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer) + Backend (PHP)  
**Связь с задачей:** Откат изменений TASK-048, возврат к логике TASK-041

---

## 📋 Описание

Вернуть отображение данных на линейном графике за одну неделю (выбранную в фильтре при старте модуля), убрав формат вывода 4 недель. График должен отображать данные только для той недели, которая была выбрана в фильтре при загрузке модуля.

**Основные требования:**
1. **Линейный график на одну неделю:** Отображать данные только за выбранную в фильтре неделю
2. **Убрать формат 4 недель:** Удалить логику отображения данных за 4 недели
3. **Сохранить разбивку закрытых тикетов:** На графике должны остаться линии для:
   - Все закрытые тикеты (`closedTickets`)
   - Закрытые тикеты, созданные этой неделей (`closedTicketsCreatedThisWeek`)
   - Закрытые тикеты, созданные другой неделей (`closedTicketsCreatedOtherWeek`)
4. **Сохранить разбивку переходящих тикетов:** На графике должны остаться линии для:
   - Все переходящие тикеты (`carryoverTickets`)
   - Переходящие тикеты, созданные этой неделей (`carryoverTicketsCreatedThisWeek`)
   - Переходящие тикеты, созданные другой неделей (`carryoverTicketsCreatedOtherWeek`)
5. **Без попапов на точках:** Точки графика не должны быть кликабельными (как в TASK-048)

---

## 🎯 Контекст

### Текущее состояние модуля (после TASK-048)

В модуле «График приёма и закрытий сектора 1С» (`GraphAdmissionClosureChart.vue`):

**Линейный график:**
- Отображает 4 точки на оси X (4 недели: текущая + 3 предыдущие)
- 7 линий: «Новые», «Закрытые (все)», «Закрытые (созданы этой неделей)», «Закрытые (созданы другой неделей)», «Переходящие (все)», «Переходящие (созданы этой неделей)», «Переходящие (созданы другой неделей)»
- Точки графика не кликабельны (попапы не открываются)

**Разбивка над графиками (TASK-047):**

**Закрытые тикеты:**
- Первая цифра: все закрытые тикеты (`closedTickets`)
- Вторая цифра: закрытые тикеты, созданные этой неделей (`closedTicketsCreatedThisWeek`)
- Третья цифра: закрытые тикеты, созданные другой неделей (`closedTicketsCreatedOtherWeek`)

**Переходящие тикеты:**
- Первая цифра: все переходящие тикеты (`carryoverTickets`)
- Вторая цифра: переходящие тикеты, созданные этой неделей (`carryoverTicketsCreatedThisWeek`)
- Третья цифра: переходящие тикеты, созданные другой неделей (`carryoverTicketsCreatedOtherWeek`)

**API:**
- Возвращает данные за 4 недели
- Структура: `series.new[]`, `series.closed[]` (массивы с 4 элементами)
- Метаданные: `meta.weeks[]` (массив из 4 недель)

### Проблема

- График показывает 4 недели, но требуется показывать только одну неделю (выбранную в фильтре)
- API возвращает избыточные данные (4 недели вместо одной)
- Пользователю не нужна динамика за 4 недели, нужна только текущая выбранная неделя

### Требуется

1. **Вернуть API** к возврату данных только за одну неделю (выбранную в фильтре)
2. **Обновить линейный график** для отображения одной точки (одна неделя)
3. **Сохранить все линии разбивки** закрытых и переходящих тикетов
4. **Сохранить отсутствие попапов** на точках графика (как в TASK-048)
5. **Использовать неделю из фильтра** при старте модуля

---

## 🎨 UX/UI Схема линейного графика

### Визуальная структура графика

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    График приёма и закрытий сектора 1С                      │
│                    Неделя 50 · 2025-12-08 — 2025-12-15 (UTC)                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  [📈 Линейный] [📊 Столбчатый] [🍩 Круговая]                                │
│                                                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│  Summary-карточки (выбранная неделя):                                        │
│  ┌──────────┐  ┌──────────────────────────┐  ┌──────────────────────────┐   │
│  │ Новые    │  │ Закрытые за неделю        │  │ Переходящие              │   │
│  │    14    │  │        34                 │  │        25                │   │
│  └──────────┘  │  ✓ 20 этой неделей        │  │  ✓ 10 этой недели        │   │
│                │  ↻ 14 другой недели      │  │  ↻ 15 другой недели      │   │
│                └──────────────────────────┘  └──────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  Легенда:                                                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ ● Новые                                                             │   │
│  │ ● Закрытые (все)                                                    │   │
│  │ ┅ Закрытые (созданы этой неделей)        [пунктир]                 │   │
│  │ ┅ Закрытые (созданы другой неделей)      [пунктир]                 │   │
│  │ ● Переходящие (все)                                                 │   │
│  │ ┅ Переходящие (созданы этой неделей)     [пунктир]                 │   │
│  │ ┅ Переходящие (созданы другой неделей)   [пунктир]                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                               │
│  График (одна неделя):                                                        │
│                                                                               │
│  40 │                                                                         │
│     │                                                                         │
│  35 │                                                                         │
│     │                                                                         │
│  30 │                                                                         │
│     │                                                                         │
│  25 │                                                                         │
│     │                                                                         │
│  20 │                                                                         │
│     │                                                                         │
│  15 │                                                                         │
│     │                                                                         │
│  10 │                                                                         │
│     │                                                                         │
│   5 │                                                                         │
│     │                                                                         │
│   0 └───────────────────────────────────────────────────────────────────────│   │
│      Неделя 50                                                               │   │
│      (2025-12-08 — 2025-12-15)                                              │   │
│                                                                               │
│  Примечания:                                                                 │
│  • График показывает одну точку (одна неделя, выбранная в фильтре)          │
│  • Все 7 линий отображаются на одной точке                                   │
│  • Точка графика НЕ кликабельна (попап не открывается)                       │
│  • Tooltip показывает данные при наведении на точку                           │
│  • Пунктирные линии (┅) для разбивки закрытых и переходящих тикетов        │
│  • Сплошные линии (─) для основных метрик                                    │
│                                                                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Детальная схема линий графика

```
Ось Y (количество тикетов)
    │
 40 │
    │
 35 │
    │
 30 │
    │
 25 │
    │
 20 │
    │
 15 │
    │
 10 │
    │
  5 │
    │
  0 └───────────────────────────────────────────────────────────────────────
      │
    Неделя 50
    (2025-12-08 — 2025-12-15)
    
    Все 7 линий сходятся в одной точке:
    ● Новые (сплошная синяя)
    ● Закрытые (все) (сплошная зелёная)
    ┅ Закрытые (созданы этой неделей) (пунктир светло-зелёный)
    ┅ Закрытые (созданы другой неделей) (пунктир жёлтый)
    ● Переходящие (все) (сплошная оранжевая)
    ┅ Переходящие (созданы этой неделей) (пунктир светло-оранжевый)
    ┅ Переходящие (созданы другой неделей) (пунктир тёмно-оранжевый)
```

### Цветовая схема линий

| Линия | Тип | Цвет | Стиль |
|-------|-----|------|-------|
| Новые | Основная | Синий (`#007bff`) | Сплошная |
| Закрытые (все) | Основная | Зелёный (`#28a745`) | Сплошная |
| Закрытые (созданы этой неделей) | Разбивка | Светло-зелёный (`#6cbf47`) | Пунктир `[5, 5]` |
| Закрытые (созданы другой неделей) | Разбивка | Жёлтый (`#ffc107`) | Пунктир `[5, 5]` |
| Переходящие (все) | Основная | Оранжевый (`#ff9800`) | Сплошная |
| Переходящие (созданы этой неделей) | Разбивка | Светло-оранжевый (`#ffb74d`) | Пунктир `[5, 5]` |
| Переходящие (созданы другой неделей) | Разбивка | Тёмно-оранжевый (`#f57c00`) | Пунктир `[5, 5]` |

### Структура tooltip при наведении на точку

```
┌─────────────────────────────────────────┐
│ Неделя 50 (08 Dec — 15 Dec)             │
├─────────────────────────────────────────┤
│ Новые: 14                               │
│ Закрытые (все): 34                      │
│   └─ Созданы этой неделей: 20          │
│   └─ Созданы другой неделей: 14         │
│ Переходящие (все): 25                   │
│   └─ Созданы этой неделей: 10          │
│   └─ Созданы другой неделей: 15         │
└─────────────────────────────────────────┘
```

### Интерактивность

**Отключено:**
- ❌ Клик на точку графика → попап не открывается
- ❌ Клик на линию графика → попап не открывается

**Включено:**
- ✅ Наведение на точку → tooltip с данными недели
- ✅ Клик на summary-карточку → открытие попапа
- ✅ Переключение типа графика (линейный/столбчатый/круговая)

---

## 📊 Логика работы

### Определение недели

**Неделя (выбранная в фильтре):**
- Номер недели: `meta.weekNumber` (из фильтра при старте)
- Границы: `meta.weekStartUtc` — `meta.weekEndUtc` (из фильтра при старте)
- Неделя определяется по ISO-8601 (понедельник–воскресенье)

### Структура данных для графика

**Для выбранной недели:**
- `newTickets` — новые тикеты
- `closedTickets` — все закрытые тикеты
- `closedTicketsCreatedThisWeek` — закрытые, созданные этой неделей
- `closedTicketsCreatedOtherWeek` — закрытые, созданные другой неделей
- `carryoverTickets` — все переходящие тикеты
- `carryoverTicketsCreatedThisWeek` — переходящие, созданные этой неделей
- `carryoverTicketsCreatedOtherWeek` — переходящие, созданные другой неделей

---

## 🔧 Технические требования

### API (бэкенд)

**Файл:** `api/graph-1c-admission-closure.php`

**Изменения:**

1. **Вернуть логику для получения данных за одну неделю:**
   - Использовать неделю из фильтра (параметры запроса)
   - Вычислить границы одной недели (ISO-8601)
   - Выполнить подсчёт метрик только для этой недели
   - Вернуть данные в структурированном виде

2. **Обновить структуру ответа API:**

```json
{
  "success": true,
  "meta": {
    "weekNumber": 50,
    "weekStartUtc": "2025-12-08T00:00:00Z",
    "weekEndUtc": "2025-12-15T23:59:59Z"
  },
  "data": {
    "newTickets": 14,
    "closedTickets": 34,
    "closedTicketsCreatedThisWeek": 20,
    "closedTicketsCreatedOtherWeek": 14,
    "carryoverTickets": 25,
    "carryoverTicketsCreatedThisWeek": 10,
    "carryoverTicketsCreatedOtherWeek": 15,
    "series": {
      "new": [14],
      "closed": [34],
      "closedCreatedThisWeek": [20],
      "closedCreatedOtherWeek": [14],
      "carryover": [25],
      "carryoverCreatedThisWeek": [10],
      "carryoverCreatedOtherWeek": [15]
    },
    "stages": [ ... ],
    "responsible": [ ... ]
  }
}
```

**Логика на бэкенде:**

```php
// Использовать неделю из фильтра (параметры запроса)
$weekStartUtc = $_GET['weekStartUtc'] ?? null;
$weekEndUtc = $_GET['weekEndUtc'] ?? null;

// Если неделя не указана, использовать текущую неделю
if (!$weekStartUtc || !$weekEndUtc) {
    $currentWeekStart = new DateTime('now', new DateTimeZone('UTC'));
    $currentWeekStart->modify('monday this week');
    $currentWeekStart->setTime(0, 0, 0);
    
    $currentWeekEnd = clone $currentWeekStart;
    $currentWeekEnd->modify('sunday this week');
    $currentWeekEnd->setTime(23, 59, 59);
    
    $weekStartUtc = $currentWeekStart->format('Y-m-d\TH:i:s\Z');
    $weekEndUtc = $currentWeekEnd->format('Y-m-d\TH:i:s\Z');
}

$weekStart = new DateTime($weekStartUtc);
$weekEnd = new DateTime($weekEndUtc);

// Подсчёт метрик для одной недели
$weekData = calculateWeekMetrics($weekStart, $weekEnd, $tickets);

// Формирование ответа
$response = [
    'success' => true,
    'meta' => [
        'weekNumber' => (int)date('W', $weekStart->getTimestamp()),
        'weekStartUtc' => $weekStartUtc,
        'weekEndUtc' => $weekEndUtc
    ],
    'data' => [
        'newTickets' => $weekData['newTickets'],
        'closedTickets' => $weekData['closedTickets'],
        'closedTicketsCreatedThisWeek' => $weekData['closedTicketsCreatedThisWeek'],
        'closedTicketsCreatedOtherWeek' => $weekData['closedTicketsCreatedOtherWeek'],
        'carryoverTickets' => $weekData['carryoverTickets'],
        'carryoverTicketsCreatedThisWeek' => $weekData['carryoverTicketsCreatedThisWeek'],
        'carryoverTicketsCreatedOtherWeek' => $weekData['carryoverTicketsCreatedOtherWeek'],
        'series' => [
            'new' => [$weekData['newTickets']],
            'closed' => [$weekData['closedTickets']],
            'closedCreatedThisWeek' => [$weekData['closedTicketsCreatedThisWeek']],
            'closedCreatedOtherWeek' => [$weekData['closedTicketsCreatedOtherWeek']],
            'carryover' => [$weekData['carryoverTickets']],
            'carryoverCreatedThisWeek' => [$weekData['carryoverTicketsCreatedThisWeek']],
            'carryoverCreatedOtherWeek' => [$weekData['carryoverTicketsCreatedOtherWeek']]
        ],
        'stages' => $stages,
        'responsible' => $responsible
    ]
];
```

### Фронтенд (Vue.js)

**Файл:** `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`

**Изменения:**

1. **Обновить `lineBarData` для отображения одной недели:**

```javascript
const lineBarData = computed(() => {
  // Получить метаданные о неделе
  const weekNumber = props.meta?.weekNumber;
  const weekLabel = weekNumber ? `Неделя ${weekNumber}` : 'Неделя';
  const labels = [weekLabel];
  
  // Получить данные для одной недели
  const newSeries = [props.data.newTickets || 0];
  const closedSeries = [props.data.closedTickets || 0];
  const closedCreatedThisWeekSeries = [props.data.closedTicketsCreatedThisWeek || 0];
  const closedCreatedOtherWeekSeries = [props.data.closedTicketsCreatedOtherWeek || 0];
  const carryoverSeries = [props.data.carryoverTickets || 0];
  const carryoverCreatedThisWeekSeries = [props.data.carryoverTicketsCreatedThisWeek || 0];
  const carryoverCreatedOtherWeekSeries = [props.data.carryoverTicketsCreatedOtherWeek || 0];

  return {
    labels,
    datasets: [
      {
        label: 'Новые',
        data: newSeries,
        backgroundColor: chartColors.primary,
        borderColor: chartColors.primary,
        tension: 0.3,
        fill: false
      },
      {
        label: 'Закрытые (все)',
        data: closedSeries,
        backgroundColor: chartColors.success,
        borderColor: chartColors.success,
        tension: 0.3,
        fill: false
      },
      {
        label: 'Закрытые (созданы этой неделей)',
        data: closedCreatedThisWeekSeries,
        backgroundColor: chartColors.successLight,
        borderColor: chartColors.successLight,
        tension: 0.3,
        fill: false,
        borderDash: [5, 5] // Пунктирная линия
      },
      {
        label: 'Закрытые (созданы другой неделей)',
        data: closedCreatedOtherWeekSeries,
        backgroundColor: chartColors.warning,
        borderColor: chartColors.warning,
        tension: 0.3,
        fill: false,
        borderDash: [5, 5] // Пунктирная линия
      },
      {
        label: 'Переходящие (все)',
        data: carryoverSeries,
        backgroundColor: chartColors.carryover,
        borderColor: chartColors.carryover,
        tension: 0.3,
        fill: false
      },
      {
        label: 'Переходящие (созданы этой неделей)',
        data: carryoverCreatedThisWeekSeries,
        backgroundColor: chartColors.carryoverLight,
        borderColor: chartColors.carryoverLight,
        tension: 0.3,
        fill: false,
        borderDash: [5, 5] // Пунктирная линия
      },
      {
        label: 'Переходящие (созданы другой неделей)',
        data: carryoverCreatedOtherWeekSeries,
        backgroundColor: chartColors.carryoverDark,
        borderColor: chartColors.carryoverDark,
        tension: 0.3,
        fill: false,
        borderDash: [5, 5] // Пунктирная линия
      }
    ]
  };
});
```

2. **Сохранить отсутствие обработчика `onClick` в `chartOptions`:**

```javascript
const chartOptions = computed(() => ({
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    tooltip: { 
      enabled: true,
      callbacks: {
        title: (items) => {
          const weekNumber = props.meta?.weekNumber;
          const weekStartUtc = props.meta?.weekStartUtc;
          const weekEndUtc = props.meta?.weekEndUtc;
          if (weekNumber && weekStartUtc && weekEndUtc) {
            return `Неделя ${weekNumber} (${formatDate(weekStartUtc)} — ${formatDate(weekEndUtc)})`;
          }
          return items[0].label;
        }
      }
    },
    legend: { position: 'top' }
  },
  // НЕТ onClick обработчика (как в TASK-048)
  scales: chartType.value === 'doughnut'
    ? {}
    : {
        y: {
          beginAtZero: true,
          ticks: { precision: 0 }
        },
        x: {
          ticks: {
            maxRotation: 45,
            minRotation: 45
          }
        }
      }
}));
```

3. **Обновить props для поддержки структуры данных одной недели:**

```javascript
const props = defineProps({
  meta: {
    type: Object,
    default: () => ({
      weekNumber: null,
      weekStartUtc: null,
      weekEndUtc: null
    })
  },
  data: {
    type: Object,
    default: () => ({
      newTickets: 0,
      closedTickets: 0,
      closedTicketsCreatedThisWeek: 0,
      closedTicketsCreatedOtherWeek: 0,
      carryoverTickets: 0,
      carryoverTicketsCreatedThisWeek: 0,
      carryoverTicketsCreatedOtherWeek: 0,
      series: {
        new: [0],
        closed: [0],
        closedCreatedThisWeek: [0],
        closedCreatedOtherWeek: [0],
        carryover: [0],
        carryoverCreatedThisWeek: [0],
        carryoverCreatedOtherWeek: [0]
      },
      stages: [],
      responsible: []
    })
  }
});
```

4. **Обновить отображение summary-карточек:**

```vue
<div class="summary-card summary-card--new" @click="handleSummaryClick('new')">
  <div class="summary-card__label">Новые за неделю</div>
  <div class="summary-card__value">{{ data.newTickets ?? 0 }}</div>
</div>

<div class="summary-card summary-card--closed-breakdown" @click="handleSummaryClick('closed')">
  <div class="summary-card__label">Закрытые за неделю</div>
  <div class="summary-card__value-main">{{ data.closedTickets ?? 0 }}</div>
  <div class="summary-card__breakdown">
    <div class="breakdown-item breakdown-item--this-week">
      <span class="breakdown-item__icon">✓</span>
      <span class="breakdown-item__value">{{ data.closedTicketsCreatedThisWeek ?? 0 }}</span>
      <span class="breakdown-item__label">этой неделей</span>
    </div>
    <div class="breakdown-item breakdown-item--other-week">
      <span class="breakdown-item__icon">↻</span>
      <span class="breakdown-item__value">{{ data.closedTicketsCreatedOtherWeek ?? 0 }}</span>
      <span class="breakdown-item__label">другой неделей</span>
    </div>
  </div>
</div>

<div class="summary-card summary-card--carryover-breakdown" @click="handleSummaryClick('carryover')">
  <div class="summary-card__label">Переходящие</div>
  <div class="summary-card__value-main">{{ data.carryoverTickets ?? 0 }}</div>
  <div class="summary-card__breakdown">
    <div class="breakdown-item breakdown-item--this-week">
      <span class="breakdown-item__icon">✓</span>
      <span class="breakdown-item__value">{{ data.carryoverTicketsCreatedThisWeek ?? 0 }}</span>
      <span class="breakdown-item__label">этой недели</span>
    </div>
    <div class="breakdown-item breakdown-item--other-week">
      <span class="breakdown-item__icon">↻</span>
      <span class="breakdown-item__value">{{ data.carryoverTicketsCreatedOtherWeek ?? 0 }}</span>
      <span class="breakdown-item__label">предыдущих</span>
    </div>
  </div>
</div>
```

5. **Обновить заголовок для отображения выбранной недели:**

```vue
<p class="ac-chart__subtitle">
  Неделя {{ meta?.weekNumber ?? '—' }} · 
  {{ meta?.weekStartUtc || '—' }} — {{ meta?.weekEndUtc || '—' }} (UTC)
</p>
```

---

## 🚀 Этапы реализации

### Этап 1: Откат API к одной неделе (бэкенд)

**Цель:** Вернуть логику получения данных за одну неделю (выбранную в фильтре).

**Задачи:**
1. Удалить логику вычисления 4 недель
2. Использовать неделю из фильтра (параметры запроса `weekStartUtc`, `weekEndUtc`)
3. Если неделя не указана, использовать текущую неделю
4. Выполнить подсчёт метрик только для одной недели:
   - `newTickets`
   - `closedTickets`
   - `closedTicketsCreatedThisWeek`
   - `closedTicketsCreatedOtherWeek`
   - `carryoverTickets`
   - `carryoverTicketsCreatedThisWeek`
   - `carryoverTicketsCreatedOtherWeek`
5. Сформировать массивы `series` с одним элементом для каждой метрики
6. Вернуть структурированный ответ с метаданными о неделе

**Критерии приёмки этапа 1:**
- [ ] API возвращает данные за одну неделю
- [ ] Массивы `series` содержат по одному элементу
- [ ] Метаданные `meta` содержат информацию об одной неделе
- [ ] Неделя определяется из фильтра (параметры запроса)
- [ ] Если фильтр не указан, используется текущая неделя

---

### Этап 2: Обновление линейного графика (фронтенд)

**Цель:** Отобразить данные за одну неделю на линейном графике.

**Задачи:**
1. Обновить `lineBarData` для работы с одной неделей
2. Сохранить все 7 линий разбивки:
   - «Новые»
   - «Закрытые (все)»
   - «Закрытые (созданы этой неделей)»
   - «Закрытые (созданы другой неделей)»
   - «Переходящие (все)»
   - «Переходящие (созданы этой неделей)»
   - «Переходящие (созданы другой неделей)»
3. Обновить labels для отображения номера недели
4. Обновить tooltip для отображения информации о неделе
5. Обновить props для поддержки структуры данных одной недели

**Критерии приёмки этапа 2:**
- [ ] График отображает одну точку на оси X (одна неделя)
- [ ] Отображаются все 7 линий:
  - Новые
  - Закрытые (все)
  - Закрытые (созданы этой неделей)
  - Закрытые (созданы другой неделей)
  - Переходящие (все)
  - Переходящие (созданы этой неделей)
  - Переходящие (созданы другой неделей)
- [ ] Label показывает номер недели
- [ ] Tooltip показывает информацию о неделе (номер, границы)
- [ ] Все линии сходятся в одной точке

---

### Этап 3: Сохранение отсутствия попапов

**Цель:** Убедиться, что попапы не открываются при клике на точку графика.

**Задачи:**
1. Проверить, что обработчик `onClick` отсутствует в `chartOptions`
2. Убедиться, что клики на точки не вызывают никаких действий
3. Проверить, что попапы открываются только через summary-карточки

**Критерии приёмки этапа 3:**
- [ ] Клик на точку графика не открывает попап
- [ ] Попапы открываются только через summary-карточки
- [ ] График остаётся интерактивным (tooltip работает)

---

### Этап 4: Обновление summary-карточек

**Цель:** Обновить отображение summary-карточек для использования данных выбранной недели.

**Задачи:**
1. Обновить отображение значений в summary-карточках для использования `data.newTickets`, `data.closedTickets`, и т.д.
2. Обновить заголовок для отображения выбранной недели
3. Проверить, что клики на карточки работают корректно

**Критерии приёмки этапа 4:**
- [ ] Summary-карточки показывают данные выбранной недели
- [ ] Заголовок показывает номер и границы выбранной недели
- [ ] Клики на карточки открывают соответствующие попапы

---

## 📝 Модули и компоненты

### Бэкенд (PHP)

- `api/graph-1c-admission-closure.php` — откат логики к получению данных за одну неделю

### Фронтенд (Vue.js)

- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue` — обновление линейного графика для одной недели
- `vue-app/src/services/graph-admission-closure/admissionClosureService.js` — возможно, обновление типов данных

---

## 🔗 API-методы Bitrix24

**Используемые методы:**
- `crm.item.list` — получение тикетов (уже используется)
  - Документация: https://context7.com/bitrix24/rest/crm.item.list

**Поля тикетов:**
- `createdTime` — дата создания тикета (используется для фильтрации)
- `movedTime` — дата перемещения в стадию (используется для определения закрытия)
- `stageId` — ID стадии (используется для определения закрывающих стадий)

---

## ✅ Критерии приёмки

### Общие критерии

- [ ] API возвращает данные за одну неделю (выбранную в фильтре)
- [ ] Линейный график отображает одну точку на оси X
- [ ] Отображаются 7 линий на графике:
  - Новые
  - Закрытые (все)
  - Закрытые (созданы этой неделей)
  - Закрытые (созданы другой неделей)
  - Переходящие (все)
  - Переходящие (созданы этой неделей)
  - Переходящие (созданы другой неделей)
- [ ] Клик на точку графика не открывает попап
- [ ] Попапы открываются только через summary-карточки
- [ ] Summary-карточки показывают данные выбранной недели
- [ ] Tooltip показывает информацию о неделе
- [ ] Все линии сходятся в одной точке

### Функциональные проверки

- [ ] При загрузке модуля отображаются данные за выбранную в фильтре неделю
- [ ] Значения на графике соответствуют данным из API
- [ ] Линии разбивки закрытых тикетов отображаются корректно
- [ ] Линии разбивки переходящих тикетов отображаются корректно
- [ ] При отсутствии данных отображается 0
- [ ] Обработка ошибок работает корректно
- [ ] Если фильтр не указан, используется текущая неделя

### Визуальные проверки

- [ ] График читаем (все линии видны на одной точке)
- [ ] Легенда отображает все линии
- [ ] Цвета линий различимы
- [ ] Пунктирные линии для разбивки закрытых тикетов отображаются корректно
- [ ] Пунктирные линии для разбивки переходящих тикетов отображаются корректно

---

## 🧪 Тестирование

### Тестовые сценарии

1. **Сценарий 1: Неделя указана в фильтре**
   - Ожидаемый результат: График отображает одну точку с данными выбранной недели

2. **Сценарий 2: Неделя не указана в фильтре**
   - Ожидаемый результат: График отображает одну точку с данными текущей недели

3. **Сценарий 3: Клик на точку графика**
   - Ожидаемый результат: Попап не открывается

4. **Сценарий 4: Клик на summary-карточку**
   - Ожидаемый результат: Попап открывается корректно

5. **Сценарий 5: Все линии на одной точке**
   - Ожидаемый результат: Все 7 линий сходятся в одной точке, значения корректны

### Проверка данных

- Проверить корректность вычисления границ недели (ISO-8601)
- Проверить, что данные соответствуют выбранной неделе из фильтра
- Проверить корректность подсчёта метрик для недели
- Проверить граничные случаи (переход года, високосный год)

---

## 📚 Связанные документы

- `DOCS/TASKS/TASK-041-graph-1c-admission-closure.md` — основная задача модуля
- `DOCS/TASKS/TASK-047-closed-tickets-breakdown.md` — разбивка закрытых тикетов
- `DOCS/TASKS/TASK-048-line-chart-4-weeks-no-popups.md` — задача с 4 неделями (откат)
- `api/graph-1c-admission-closure.php` — API endpoint
- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue` — компонент графика

---

## 📝 История правок

- **2025-12-17 08:27 (UTC+3, Брест):** Создана задача TASK-049
  - Определено требование вернуть отображение одной недели (выбранной в фильтре)
  - Описаны изменения в API и фронтенде для отката к одной неделе
  - Добавлены этапы реализации и критерии приёмки
  - Сохранено требование убрать клики на попапы точек графика (из TASK-048)
  - Сохранены все линии разбивки закрытых и переходящих тикетов

- **2025-12-17 (UTC+3, Брест):** Задача выполнена
  - ✅ Откат API к возврату данных за одну неделю (убрана логика 4 недель)
  - ✅ Обновлён линейный график во Vue.js для отображения одной точки
  - ✅ Сохранены все 7 линий разбивки на графике
  - ✅ Сохранено отсутствие попапов на точках графика (как в TASK-048)
  - ✅ Обновлены summary-карточки для использования данных одной недели

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer) + Backend (PHP)

