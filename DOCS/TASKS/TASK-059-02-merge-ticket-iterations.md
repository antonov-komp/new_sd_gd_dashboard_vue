# TASK-059-02: Объединение проходов по массиву тикетов

**Дата создания:** 2025-12-18 11:00 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Backend Developer (PHP)  
**Родительская задача:** [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)  
**Подзадача:** Подзадача 2 из TASK-059

---

## 📋 Описание

Оптимизировать агрегацию данных по месяцам в режиме "3 месяца" модуля "График приёма и закрытий сектора 1С". Проблема: множественные проходы по массиву тикетов для агрегации (72-90 проходов), что значительно увеличивает время обработки.

**Цель:** Объединить все проходы по массиву тикетов в один цикл для сокращения времени агрегации на 80-90%.

---

## 🎯 Контекст

### Текущая проблема:

**Множественные проходы по массиву тикетов** для агрегации данных:

```php
// Текущая реализация (строки 808-979)
foreach ($months as $month) {
    // Проход 1: calculateWeekMetrics для каждой недели (проходит по всем тикетам)
    foreach ($month['weeks'] as $week) {
        $weekMetrics = calculateWeekMetrics(...);  // Проход по всем тикетам
    }
    
    // Проход 2: Подсчёт новых тикетов за месяц (строки 855-860)
    foreach ($tickets as $ticket) {
        // ...
    }
    
    // Проход 3: Подсчёт закрытых тикетов за месяц (строки 877-897)
    foreach ($tickets as $ticket) {
        // ...
    }
    
    // Проход 4: Подсчёт переходящих тикетов (строки 909-919)
    foreach ($tickets as $ticket) {
        // ...
    }
    
    // Проход 5: Агрегация по стадиям (строки 922-935)
    foreach ($tickets as $ticket) {
        // ...
    }
}
```

**Проблемы:**
- Для 3 месяцев × 4-5 недель × 6 проходов = **72-90 проходов** по массиву тикетов
- Каждый проход обрабатывает все тикеты (может быть 1000-5000 тикетов)
- Увеличивается время обработки на 5-10 секунд
- Неэффективное использование CPU

### Решение:

Объединить все проходы в один цикл по тикетам, агрегируя все метрики одновременно.

**Логика:**
- Один проход по тикетам
- Для каждого тикета определяем, к каким месяцам/неделям он относится
- Агрегируем все метрики одновременно

---

## 🏗️ Модули и компоненты

### Изменяемые файлы:
- `api/graph-1c-admission-closure.php` — основной API endpoint
  - Агрегация данных по месяцам (строки 808-979)
  - Функция `calculateWeekMetrics()` — агрегация по неделям (строки 286-429)

### Используемые функции:
- `isInRange()` — проверка попадания даты в интервал (строки 248-272)
- `isCarryoverTicket()` — проверка переходящего тикета (строки 492-533)
- `calculateWeekMetrics()` — агрегация по неделям (строки 286-429)

---

## 📦 Зависимости

- **От задач:** 
  - TASK-059: Оптимизация загрузки режима 3 месяца
  - TASK-059-01: Оптимизация запроса переходящих тикетов (рекомендуется выполнить сначала)
- **От модулей:** 
  - Модуль 3: График приёма и закрытий сектора 1С
  - TASK-053: Режим выбора периода 3 месяца
  - TASK-058: Обогащение в режиме 3 месяцев

---

## 🎯 Ступенчатые подзадачи

### Подзадача 2.1: Анализ текущей логики агрегации

**Цель:** Понять текущую логику агрегации данных по месяцам.

**Шаги:**

1. **Изучить текущий код агрегации**
   - Файл: `api/graph-1c-admission-closure.php` (строки 808-979)
   - Понять все проходы по массиву тикетов
   - Понять структуру данных для агрегации

2. **Изучить функцию `calculateWeekMetrics()`**
   - Файл: `api/graph-1c-admission-closure.php` (строки 286-429)
   - Понять логику агрегации по неделям
   - Понять, какие метрики вычисляются

3. **Определить структуру данных для объединённой агрегации**
   - Структура для метрик по месяцам
   - Структура для метрик по неделям
   - Структура для метрик по стадиям

**Критерии приёмки:**
- [ ] Текущая логика агрегации изучена
- [ ] Все проходы по массиву тикетов идентифицированы
- [ ] Структура данных для объединённой агрегации определена

---

### Подзадача 2.2: Создание структуры данных для объединённой агрегации

**Цель:** Создать единую структуру данных для агрегации всех метрик.

**Шаги:**

1. **Создать структуру для метрик по месяцам**
   ```php
   $monthMetrics = [];
   foreach ($months as $month) {
       $monthMetrics[$month['monthNumber']] = [
           'newTickets' => 0,
           'closedTickets' => 0,
           'closedTicketsCreatedThisWeek' => 0,
           'closedTicketsCreatedOtherWeek' => 0,
           'carryoverTickets' => 0,
           'carryoverTicketsCreatedThisWeek' => 0,
           'carryoverTicketsCreatedOtherWeek' => 0,
           'weeks' => []
       ];
   }
   ```

2. **Создать структуру для метрик по неделям**
   ```php
   foreach ($months as $month) {
       foreach ($month['weeks'] as $week) {
           $monthMetrics[$month['monthNumber']]['weeks'][$week['weekNumber']] = [
               'newTickets' => 0,
               'closedTickets' => 0,
               'closedTicketsCreatedThisWeek' => 0,
               'closedTicketsCreatedOtherWeek' => 0,
               'carryoverTickets' => 0,
               'carryoverTicketsCreatedThisWeek' => 0,
               'carryoverTicketsCreatedOtherWeek' => 0
           ];
       }
   }
   ```

3. **Создать структуру для метрик по стадиям**
   ```php
   $stageAgg = [];
   ```

**Критерии приёмки:**
- [ ] Структура данных для метрик по месяцам создана
- [ ] Структура данных для метрик по неделям создана
- [ ] Структура данных для метрик по стадиям создана

---

### Подзадача 2.3: Рефакторинг агрегации в один цикл

**Цель:** Объединить все проходы по массиву тикетов в один цикл.

**Шаги:**

1. **Создать единый цикл по тикетам**
   - Заменить все отдельные циклы одним циклом
   - Для каждого тикета определять, к каким месяцам/неделям он относится

2. **Агрегировать метрики для каждого месяца/недели**
   - Новые тикеты
   - Закрытые тикеты
   - Переходящие тикеты
   - Агрегация по стадиям

3. **Сохранить логику из `calculateWeekMetrics()`**
   - Перенести логику определения метрик в единый цикл
   - Сохранить все условия и проверки

**Критерии приёмки:**
- [ ] Все проходы объединены в один цикл
- [ ] Логика агрегации сохранена
- [ ] Все метрики вычисляются корректно

**Пример кода:**

```php
// Инициализация структуры данных для агрегации
$monthMetrics = [];
$stageAgg = [];

foreach ($months as $month) {
    $monthMetrics[$month['monthNumber']] = [
        'newTickets' => 0,
        'closedTickets' => 0,
        'closedTicketsCreatedThisWeek' => 0,
        'closedTicketsCreatedOtherWeek' => 0,
        'carryoverTickets' => 0,
        'carryoverTicketsCreatedThisWeek' => 0,
        'carryoverTicketsCreatedOtherWeek' => 0,
        'weeks' => []
    ];
    
    foreach ($month['weeks'] as $week) {
        $monthMetrics[$month['monthNumber']]['weeks'][$week['weekNumber']] = [
            'newTickets' => 0,
            'closedTickets' => 0,
            'closedTicketsCreatedThisWeek' => 0,
            'closedTicketsCreatedOtherWeek' => 0,
            'carryoverTickets' => 0,
            'carryoverTicketsCreatedThisWeek' => 0,
            'carryoverTicketsCreatedOtherWeek' => 0
        ];
    }
}

// TASK-059-02: Объединённая агрегация в один цикл по тикетам
foreach ($tickets as $ticket) {
    $createdTime = $ticket['createdTime'] ?? null;
    $movedTime = $ticket['movedTime'] ?? $ticket['updatedTime'] ?? null;
    $stageId = $ticket['stageId'] ?? null;
    $stageId = $stageId ? strtoupper($stageId) : null;
    
    // Для каждого месяца определяем метрики
    foreach ($months as $month) {
        $monthStart = $month['monthStart'];
        $monthEnd = $month['monthEnd'];
        $monthNumber = $month['monthNumber'];
        
        // Новые тикеты за месяц
        if (isInRange($createdTime, $monthStart, $monthEnd)) {
            $monthMetrics[$monthNumber]['newTickets']++;
        }
        
        // Закрытые тикеты за месяц
        if ($stageId && in_array($stageId, $closingStages, true)) {
            if (isInRange($movedTime, $monthStart, $monthEnd)) {
                $monthMetrics[$monthNumber]['closedTickets']++;
                
                // Разбивка по критерию создания
                if (isInRange($createdTime, $monthStart, $monthEnd)) {
                    $monthMetrics[$monthNumber]['closedTicketsCreatedThisWeek']++;
                } else {
                    $monthMetrics[$monthNumber]['closedTicketsCreatedOtherWeek']++;
                }
                
                // Агрегация по стадиям
                if (!isset($stageAgg[$stageId])) {
                    $stageAgg[$stageId] = 0;
                }
                $stageAgg[$stageId]++;
            }
        }
        
        // Переходящие тикеты на конец месяца
        if (isCarryoverTicket($ticket, $monthEnd, $monthEnd, $targetStages, $closingStages)) {
            $monthMetrics[$monthNumber]['carryoverTickets']++;
            
            // Разбивка по критерию создания
            if (isInRange($createdTime, $monthStart, $monthEnd)) {
                $monthMetrics[$monthNumber]['carryoverTicketsCreatedThisWeek']++;
            } else {
                $monthMetrics[$monthNumber]['carryoverTicketsCreatedOtherWeek']++;
            }
        }
        
        // Агрегация по неделям внутри месяца
        foreach ($month['weeks'] as $week) {
            $weekStart = $week['weekStart'];
            $weekEnd = $week['weekEnd'];
            $weekNumber = $week['weekNumber'];
            
            // Новые тикеты за неделю
            if (isInRange($createdTime, $weekStart, $weekEnd)) {
                $monthMetrics[$monthNumber]['weeks'][$weekNumber]['newTickets']++;
            }
            
            // Закрытые тикеты за неделю
            if ($stageId && in_array($stageId, $closingStages, true)) {
                if (isInRange($movedTime, $weekStart, $weekEnd)) {
                    $monthMetrics[$monthNumber]['weeks'][$weekNumber]['closedTickets']++;
                    
                    // Разбивка по критерию создания
                    if (isInRange($createdTime, $weekStart, $weekEnd)) {
                        $monthMetrics[$monthNumber]['weeks'][$weekNumber]['closedTicketsCreatedThisWeek']++;
                    } else {
                        $monthMetrics[$monthNumber]['weeks'][$weekNumber]['closedTicketsCreatedOtherWeek']++;
                    }
                }
            }
            
            // Переходящие тикеты за неделю
            if (isCarryoverTicket($ticket, $weekStart, $weekEnd, $targetStages, $closingStages)) {
                $monthMetrics[$monthNumber]['weeks'][$weekNumber]['carryoverTickets']++;
                
                // Разбивка по критерию создания
                if (isInRange($createdTime, $weekStart, $weekEnd)) {
                    $monthMetrics[$monthNumber]['weeks'][$weekNumber]['carryoverTicketsCreatedThisWeek']++;
                } else {
                    $monthMetrics[$monthNumber]['weeks'][$weekNumber]['carryoverTicketsCreatedOtherWeek']++;
                }
            }
        }
    }
}

// Формирование ответа из агрегированных данных
$newTicketsByMonth = [];
$closedTicketsByMonth = [];
$carryoverTicketsByMonth = [];
$totalNewTickets = 0;
$totalClosedTickets = 0;
$totalCarryoverTickets = 0;

foreach ($months as $month) {
    $monthNumber = $month['monthNumber'];
    $metrics = $monthMetrics[$monthNumber];
    
    $totalNewTickets += $metrics['newTickets'];
    $totalClosedTickets += $metrics['closedTickets'];
    $totalCarryoverTickets += $metrics['carryoverTickets'];
    
    // Формирование данных для ответа
    $weeksData = [];
    foreach ($month['weeks'] as $week) {
        $weekNumber = $week['weekNumber'];
        $weekMetrics = $metrics['weeks'][$weekNumber] ?? [];
        
        $weeksData[] = [
            'weekNumber' => $weekNumber,
            'count' => $weekMetrics['newTickets'] ?? 0,
            'closedCount' => $weekMetrics['closedTickets'] ?? 0,
            'closedCreatedThisWeek' => $weekMetrics['closedTicketsCreatedThisWeek'] ?? 0,
            'closedCreatedOtherWeek' => $weekMetrics['closedTicketsCreatedOtherWeek'] ?? 0,
            'carryoverCount' => $weekMetrics['carryoverTickets'] ?? 0,
            'carryoverCreatedThisWeek' => $weekMetrics['carryoverTicketsCreatedThisWeek'] ?? 0,
            'carryoverCreatedOtherWeek' => $weekMetrics['carryoverTicketsCreatedOtherWeek'] ?? 0
        ];
    }
    
    $newTicketsByMonth[] = [
        'month' => $monthNumber,
        'monthName' => $month['monthName'],
        'count' => $metrics['newTickets'],
        'weeks' => $weeksData
    ];
    
    $closedTicketsByMonth[] = [
        'month' => $monthNumber,
        'monthName' => $month['monthName'],
        'count' => $metrics['closedTickets'],
        'closedCreatedThisWeek' => $metrics['closedTicketsCreatedThisWeek'],
        'closedCreatedOtherWeek' => $metrics['closedTicketsCreatedOtherWeek'],
        'weeks' => array_map(function($week) {
            return [
                'weekNumber' => $week['weekNumber'],
                'count' => $week['closedCount'],
                'closedCreatedThisWeek' => $week['closedCreatedThisWeek'],
                'closedCreatedOtherWeek' => $week['closedCreatedOtherWeek']
            ];
        }, $weeksData)
    ];
    
    $carryoverTicketsByMonth[] = [
        'month' => $monthNumber,
        'monthName' => $month['monthName'],
        'count' => $metrics['carryoverTickets'],
        'carryoverCreatedThisWeek' => $metrics['carryoverTicketsCreatedThisWeek'],
        'carryoverCreatedOtherWeek' => $metrics['carryoverTicketsCreatedOtherWeek'],
        'weeks' => array_map(function($week) {
            return [
                'weekNumber' => $week['weekNumber'],
                'count' => $week['carryoverCount'],
                'carryoverCreatedThisWeek' => $week['carryoverCreatedThisWeek'],
                'carryoverCreatedOtherWeek' => $week['carryoverCreatedOtherWeek']
            ];
        }, $weeksData)
    ];
}
```

---

### Подзадача 2.4: Тестирование оптимизированной агрегации

**Цель:** Протестировать корректность работы оптимизированной агрегации.

**Шаги:**

1. **Функциональное тестирование**
   - Сравнить результаты агрегации до и после оптимизации
   - Проверить корректность данных для каждого месяца
   - Проверить корректность данных для каждой недели

2. **Производительность**
   - Измерить время агрегации до оптимизации
   - Измерить время агрегации после оптимизации
   - Сравнить результаты

3. **Логирование**
   - Добавить логирование времени выполнения агрегации
   - Добавить логирование количества обработанных тикетов

**Критерии приёмки:**
- [ ] Результаты агрегации не изменились (корректность данных)
- [ ] Время агрегации сократилось на 80-90%
- [ ] Логирование добавлено

---

### Подзадача 2.5: Оптимизация функции `calculateWeekMetrics()` (опционально)

**Цель:** Оптимизировать функцию `calculateWeekMetrics()`, если она используется в других местах.

**Шаги:**

1. **Проверить использование `calculateWeekMetrics()`**
   - Найти все места использования функции
   - Определить, можно ли её оптимизировать

2. **Оптимизировать функцию (если нужно)**
   - Улучшить алгоритм агрегации
   - Уменьшить количество проверок

**Критерии приёмки:**
- [ ] Использование функции проверено
- [ ] Функция оптимизирована (если нужно)

---

## 🔧 Технические требования

### Backend (PHP):
- PHP 8.4+
- Оптимизация алгоритма агрегации
- Эффективное использование памяти

### Производительность:
- **Целевое время агрегации:** сокращение на 80-90%
- **Целевое количество проходов:** 1 проход вместо 72-90

---

## ✅ Критерии приёмки

### Общие критерии:
- [ ] Все метрики агрегируются в одном проходе по тикетам
- [ ] Результаты агрегации не изменились (корректность данных)
- [ ] Время агрегации сократилось на 80-90%
- [ ] Код соответствует стандартам PSR-12
- [ ] Комментарии в коде добавлены
- [ ] Логирование обновлено

### Детальные критерии:
- [ ] Структура данных для агрегации создана
- [ ] Все проходы объединены в один цикл
- [ ] Логика агрегации сохранена
- [ ] Все метрики вычисляются корректно

---

## 🧪 Тестирование

### Функциональное тестирование:
1. Сравнить результаты агрегации до и после оптимизации
2. Проверить данные для каждого месяца (Октябрь, Ноябрь, Декабрь)
3. Проверить данные для каждой недели
4. Проверить агрегацию по стадиям

### Производительность:
1. Измерить время агрегации до оптимизации
2. Измерить время агрегации после оптимизации
3. Сравнить количество проходов по массиву тикетов
4. Сравнить результаты

---

## 🔗 Связанные документы

- [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)
- [Анализ оптимизации](../../REFACTOR/PERFORMANCE/optimization-3-months-mode-loading.md)
- [Модуль 3: График приёма и закрытий сектора 1С](../../MODULES/module-03-graph-admission-closure/README.md)

---

## 📊 История правок

- **2025-12-18 11:00 (UTC+3, Брест):** Создана подзадача для объединения проходов по массиву тикетов

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

