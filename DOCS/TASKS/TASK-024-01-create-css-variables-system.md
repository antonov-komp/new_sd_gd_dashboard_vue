# TASK-024-01: Создание системы CSS-переменных Bitrix24 UI Kit

**Дата создания:** 2025-12-11 11:22 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-024 (Этап 1)

---

## 📋 Цель задачи

Создать централизованную систему CSS-переменных для унификации дизайна с Bitrix24 UI Kit. Система переменных должна быть доступна во всём приложении и использоваться всеми компонентами модуля "График состояния сектор 1С" и другими модулями приложения.

---

## 🎯 Контекст

В настоящее время компоненты модуля "График состояния" используют кастомные цвета и значения, которые не соответствуют гайдлайнам Bitrix24 UI Kit. Для унификации дизайна необходимо создать централизованную систему CSS-переменных, которая будет:

- Соответствовать стандартам Bitrix24 UI Kit
- Обеспечивать единообразие дизайна во всём приложении
- Упрощать поддержку и обновление стилей
- Поддерживать модульную систему отступов (кратные 8px)
- Обеспечивать консистентность скруглений углов

**Зависимости:**
- Родительская задача TASK-024 создана
- Анализ дизайна завершён (`DOCS/GUIDES/ui-analysis-graph-state-sector-1c.md`)
- Структура проекта Vue.js существует

**Влияет на:**
- Все последующие этапы TASK-024 (этапы 2-10 зависят от этой задачи)
- Все компоненты модуля "График состояния"
- Потенциально другие модули приложения

---

## 🏗️ Архитектура решения

### Структура файлов

```
vue-app/src/
├── styles/
│   ├── main.css                    # Существующий файл (обновить)
│   └── bitrix24-ui-variables.css   # Новый файл (создать)
└── main.js                         # Обновить импорт
```

### Подход к реализации

1. **Создать отдельный файл** `bitrix24-ui-variables.css` для изоляции переменных
2. **Импортировать файл** в `main.js` или в `main.css` для глобальной доступности
3. **Использовать CSS-переменные** (`:root` селектор) для обеспечения глобальной доступности
4. **Документировать все переменные** в комментариях для удобства использования

---

## 📝 Детальные шаги реализации

### Шаг 1: Создание файла CSS-переменных

**Действие:** Создать файл `vue-app/src/styles/bitrix24-ui-variables.css`

**Структура файла:**
```css
/**
 * Bitrix24 UI Kit - CSS Variables
 * 
 * Централизованная система CSS-переменных для унификации дизайна
 * с гайдлайнами Bitrix24 UI Kit.
 * 
 * Использование:
 *   background-color: var(--b24-primary);
 *   padding: var(--spacing-md);
 *   border-radius: var(--radius-md);
 * 
 * Дата создания: 2025-12-11
 * Версия: 1.0
 */

:root {
  /* ============================================
     ЦВЕТА BITRIX24 UI KIT
     ============================================ */
  
  /* Основные цвета */
  --b24-primary: #007bff;
  --b24-primary-hover: #0056b3;
  --b24-primary-active: #004085;
  --b24-primary-light: #cfe2ff;
  --b24-primary-lighter: #e7f3ff;
  
  --b24-success: #28a745;
  --b24-success-hover: #218838;
  --b24-success-active: #1e7e34;
  --b24-success-light: #d4edda;
  --b24-success-lighter: #e8f5e9;
  
  --b24-danger: #dc3545;
  --b24-danger-hover: #c82333;
  --b24-danger-active: #bd2130;
  --b24-danger-light: #f8d7da;
  --b24-danger-lighter: #ffeaea;
  
  --b24-warning: #ffc107;
  --b24-warning-hover: #e0a800;
  --b24-warning-active: #d39e00;
  --b24-warning-light: #fff3cd;
  --b24-warning-lighter: #fffbf0;
  
  --b24-info: #17a2b8;
  --b24-info-hover: #138496;
  --b24-info-active: #117a8b;
  --b24-info-light: #d1ecf1;
  --b24-info-lighter: #e8f4f8;
  
  /* ============================================
     ЦВЕТА ТЕКСТА
     ============================================ */
  
  --b24-text-primary: #333333;
  --b24-text-secondary: #666666;
  --b24-text-muted: #999999;
  --b24-text-disabled: #cccccc;
  --b24-text-inverse: #ffffff;
  
  /* ============================================
     ЦВЕТА ГРАНИЦ
     ============================================ */
  
  --b24-border: #dee2e6;
  --b24-border-light: #e5e7eb;
  --b24-border-medium: #ced4da;
  --b24-border-dark: #adb5bd;
  --b24-border-focus: #007bff;
  
  /* ============================================
     ЦВЕТА ФОНОВ
     ============================================ */
  
  --b24-bg: #f5f5f5;
  --b24-bg-light: #f9fafb;
  --b24-bg-lighter: #fafbfc;
  --b24-bg-white: #ffffff;
  --b24-bg-dark: #343a40;
  
  /* ============================================
     ОТСТУПЫ (МОДУЛЬНАЯ СИСТЕМА 8PX)
     ============================================ */
  
  --spacing-xs: 4px;    /* 0.5 * 8px */
  --spacing-sm: 8px;    /* 1 * 8px */
  --spacing-md: 16px;   /* 2 * 8px */
  --spacing-lg: 24px;   /* 3 * 8px */
  --spacing-xl: 32px;   /* 4 * 8px */
  --spacing-2xl: 40px;  /* 5 * 8px */
  --spacing-3xl: 48px;  /* 6 * 8px */
  
  /* ============================================
     СКРУГЛЕНИЯ (BORDER-RADIUS)
     ============================================ */
  
  --radius-sm: 4px;     /* Мелкие элементы (badges, индикаторы) */
  --radius-md: 6px;     /* Кнопки, поля ввода */
  --radius-lg: 8px;     /* Карточки, панели */
  --radius-xl: 12px;    /* Большие карточки, модальные окна */
  --radius-full: 9999px; /* Круглые элементы (аватары) */
  
  /* ============================================
     ТЕНИ
     ============================================ */
  
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 2px 4px 0 rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 4px 8px 0 rgba(0, 0, 0, 0.15);
  --shadow-xl: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
  
  /* ============================================
     ПЕРЕХОДЫ (TRANSITIONS)
     ============================================ */
  
  --transition-fast: 0.15s ease;
  --transition-base: 0.2s ease;
  --transition-slow: 0.3s ease;
  
  /* ============================================
     РАЗМЕРЫ ШРИФТОВ
     ============================================ */
  
  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-base: 16px;
  --font-size-lg: 18px;
  --font-size-xl: 20px;
  --font-size-2xl: 24px;
  --font-size-3xl: 28px;
  
  /* ============================================
     ВЫСОТА СТРОКИ (LINE-HEIGHT)
     ============================================ */
  
  --line-height-tight: 1.2;
  --line-height-normal: 1.5;
  --line-height-relaxed: 1.75;
  
  /* ============================================
     ШИРИНА КОНТЕЙНЕРОВ
     ============================================ */
  
  --container-sm: 640px;
  --container-md: 768px;
  --container-lg: 1024px;
  --container-xl: 1280px;
  --container-2xl: 1400px;
  
  /* ============================================
     Z-INDEX СЛОИ
     ============================================ */
  
  --z-index-dropdown: 1000;
  --z-index-sticky: 1020;
  --z-index-fixed: 1030;
  --z-index-modal-backdrop: 1040;
  --z-index-modal: 1050;
  --z-index-popover: 1060;
  --z-index-tooltip: 1070;
}
```

### Шаг 2: Подключение файла в приложении

**Действие:** Импортировать файл переменных в `main.js` или `main.css`

**Вариант 1: Импорт в main.js (рекомендуется)**

Обновить `vue-app/src/main.js`:
```javascript
import { createApp } from 'vue';
import App from './App.vue';
import router from './router';
import './styles/bitrix24-ui-variables.css'; // Добавить эту строку
import './styles/main.css';
// Импорт конфигурации Chart.js для глобальной регистрации
import '@/utils/chart-config.js';

const app = createApp(App);
// ... остальной код
```

**Вариант 2: Импорт в main.css**

Обновить `vue-app/src/styles/main.css`:
```css
/* Импорт переменных Bitrix24 UI Kit */
@import './bitrix24-ui-variables.css';

/* Глобальные стили для Vue.js приложения */
/* ... остальной код */
```

**Рекомендация:** Использовать Вариант 1 (импорт в `main.js`), так как это обеспечивает более раннюю загрузку переменных.

### Шаг 3: Документирование переменных

**Действие:** Создать документацию по использованию переменных

**Создать файл:** `DOCS/GUIDES/bitrix24-ui-variables-reference.md` (опционально, но рекомендуется)

**Содержание документации:**
- Описание всех категорий переменных
- Примеры использования
- Рекомендации по применению
- Связь с гайдлайнами Bitrix24

### Шаг 4: Проверка доступности переменных

**Действие:** Убедиться, что переменные доступны во всех компонентах

**Тест:** Создать простой тестовый компонент или обновить существующий для проверки:

```vue
<template>
  <div class="test-variables">
    <div class="test-box primary">Primary</div>
    <div class="test-box success">Success</div>
    <div class="test-box danger">Danger</div>
  </div>
</template>

<style scoped>
.test-box {
  padding: var(--spacing-md);
  border-radius: var(--radius-md);
  margin: var(--spacing-sm);
  color: var(--b24-text-inverse);
}

.test-box.primary {
  background-color: var(--b24-primary);
}

.test-box.success {
  background-color: var(--b24-success);
}

.test-box.danger {
  background-color: var(--b24-danger);
}
</style>
```

---

## 🎨 Технические требования

### CSS-переменные

**Требования к переменным:**
- Все переменные должны быть определены в `:root` для глобальной доступности
- Имена переменных должны следовать конвенции: `--b24-*` для цветов Bitrix24, `--spacing-*` для отступов, `--radius-*` для скруглений
- Все значения должны быть в пикселях (px) для отступов и скруглений
- Цвета должны соответствовать стандартам Bitrix24 UI Kit

### Совместимость

**Требования:**
- Поддержка всех современных браузеров (Chrome, Firefox, Safari, Edge)
- Поддержка CSS-переменных (все современные браузеры поддерживают)
- Обратная совместимость: если переменная не поддерживается, должны быть fallback значения (но это не требуется для современных браузеров)

### Производительность

**Требования:**
- Файл переменных должен быть минимальным по размеру
- Импорт не должен замедлять загрузку приложения
- Переменные должны загружаться синхронно (до загрузки компонентов)

---

## ✅ Критерии приёмки

### Обязательные требования:

- [ ] Файл `vue-app/src/styles/bitrix24-ui-variables.css` создан
- [ ] Все переменные определены в `:root` селекторе
- [ ] Переменные для цветов Bitrix24 определены (primary, success, danger, warning, info) с состояниями hover/active
- [ ] Переменные для текста определены (primary, secondary, muted, disabled, inverse)
- [ ] Переменные для границ определены (border, border-light, border-medium, border-dark, border-focus)
- [ ] Переменные для фонов определены (bg, bg-light, bg-lighter, bg-white, bg-dark)
- [ ] Переменные для отступов определены (spacing-xs до spacing-3xl, кратные 8px)
- [ ] Переменные для скруглений определены (radius-sm, radius-md, radius-lg, radius-xl, radius-full)
- [ ] Переменные для теней определены (shadow-sm, shadow-md, shadow-lg, shadow-xl)
- [ ] Переменные для переходов определены (transition-fast, transition-base, transition-slow)
- [ ] Переменные для размеров шрифтов определены (font-size-xs до font-size-3xl)
- [ ] Переменные для высоты строки определены (line-height-tight, line-height-normal, line-height-relaxed)
- [ ] Переменные для ширины контейнеров определены (container-sm до container-2xl)
- [ ] Переменные для z-index определены (z-index-dropdown до z-index-tooltip)
- [ ] Файл импортирован в `main.js` или `main.css`
- [ ] Все переменные документированы в комментариях
- [ ] Переменные доступны во всех компонентах (проверено тестом)

### Дополнительные требования (опционально):

- [ ] Создана документация по использованию переменных (`DOCS/GUIDES/bitrix24-ui-variables-reference.md`)
- [ ] Добавлены примеры использования в документации
- [ ] Проверена работа переменных в разных браузерах

---

## 📝 Примеры использования

### Пример 1: Использование цветов

```vue
<style scoped>
.button-primary {
  background-color: var(--b24-primary);
  color: var(--b24-text-inverse);
  border: none;
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--radius-md);
  transition: background-color var(--transition-base);
}

.button-primary:hover {
  background-color: var(--b24-primary-hover);
}

.button-primary:active {
  background-color: var(--b24-primary-active);
}
</style>
```

### Пример 2: Использование отступов

```vue
<style scoped>
.card {
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-lg);
  background-color: var(--b24-bg-white);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
}

.card-header {
  margin-bottom: var(--spacing-sm);
  padding-bottom: var(--spacing-sm);
  border-bottom: 1px solid var(--b24-border-light);
}
</style>
```

### Пример 3: Использование скруглений

```vue
<style scoped>
.badge {
  display: inline-block;
  padding: var(--spacing-xs) var(--spacing-sm);
  background-color: var(--b24-primary);
  color: var(--b24-text-inverse);
  border-radius: var(--radius-sm);
  font-size: var(--font-size-xs);
}

.card {
  border-radius: var(--radius-lg);
}

.button {
  border-radius: var(--radius-md);
}
</style>
```

### Пример 4: Использование теней

```vue
<style scoped>
.modal {
  background-color: var(--b24-bg-white);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-xl);
  padding: var(--spacing-lg);
}

.dropdown {
  background-color: var(--b24-bg-white);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-md);
  border: 1px solid var(--b24-border);
}
</style>
```

### Пример 5: Комплексное использование

```vue
<template>
  <div class="dashboard-card">
    <div class="card-header">
      <h3 class="card-title">Заголовок</h3>
    </div>
    <div class="card-body">
      <p class="card-text">Содержимое карточки</p>
      <button class="btn-primary">Действие</button>
    </div>
  </div>
</template>

<style scoped>
.dashboard-card {
  background-color: var(--b24-bg-white);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
  box-shadow: var(--shadow-md);
  border: 1px solid var(--b24-border-light);
}

.card-header {
  margin-bottom: var(--spacing-md);
  padding-bottom: var(--spacing-sm);
  border-bottom: 1px solid var(--b24-border-light);
}

.card-title {
  font-size: var(--font-size-xl);
  font-weight: 600;
  color: var(--b24-text-primary);
  margin: 0;
  line-height: var(--line-height-tight);
}

.card-body {
  color: var(--b24-text-secondary);
  line-height: var(--line-height-normal);
}

.btn-primary {
  background-color: var(--b24-primary);
  color: var(--b24-text-inverse);
  border: none;
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--radius-md);
  font-size: var(--font-size-sm);
  cursor: pointer;
  transition: background-color var(--transition-base);
}

.btn-primary:hover {
  background-color: var(--b24-primary-hover);
}
</style>
```

---

## 🧪 Тестирование

### Функциональное тестирование:

1. **Проверка доступности переменных:**
   - [ ] Создать тестовый компонент с использованием переменных
   - [ ] Убедиться, что переменные применяются корректно
   - [ ] Проверить, что переменные доступны в scoped стилях компонентов

2. **Проверка значений переменных:**
   - [ ] Проверить, что цвета соответствуют стандартам Bitrix24
   - [ ] Проверить, что отступы кратны 8px
   - [ ] Проверить, что скругления соответствуют стандартам

3. **Проверка импорта:**
   - [ ] Убедиться, что файл импортирован корректно
   - [ ] Проверить, что переменные доступны до загрузки компонентов
   - [ ] Проверить отсутствие ошибок в консоли браузера

### Визуальное тестирование:

1. **Проверка в разных браузерах:**
   - [ ] Chrome (последняя версия)
   - [ ] Firefox (последняя версия)
   - [ ] Safari (последняя версия)
   - [ ] Edge (последняя версия)

2. **Проверка на разных устройствах:**
   - [ ] Десктоп
   - [ ] Планшет
   - [ ] Мобильное устройство

### Инструменты для тестирования:

- **DevTools браузера:** Проверить, что переменные определены в `:root`
- **Console:** Выполнить `getComputedStyle(document.documentElement).getPropertyValue('--b24-primary')` для проверки значения
- **Визуальная проверка:** Создать тестовую страницу с использованием всех переменных

---

## 📚 Связанная документация

- **Родительская задача:** `DOCS/TASKS/TASK-024-improve-graph-state-ui-design.md`
- **Анализ дизайна:** `DOCS/GUIDES/ui-analysis-graph-state-sector-1c.md`
- **Bitrix24 UI Kit:** https://apidocs.bitrix24.ru/sdk/ui.html
- **CSS Custom Properties (MDN):** https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties

---

## 🔗 Зависимости

- **Зависит от:**
  - Родительская задача TASK-024 создана
  - Структура проекта Vue.js существует
  - Файл `vue-app/src/main.js` существует

- **Влияет на:**
  - Все последующие этапы TASK-024 (этапы 2-10)
  - Все компоненты модуля "График состояния"
  - Потенциально другие модули приложения

---

## 📝 История правок

- 2025-12-11 11:22 (UTC+3, Брест): Создана подзадача TASK-024-01 для создания системы CSS-переменных Bitrix24 UI Kit

---

## 💡 Дополнительные заметки

### Рекомендации:

1. **Именование переменных:**
   - Использовать префикс `--b24-` для цветов Bitrix24
   - Использовать префикс `--spacing-` для отступов
   - Использовать префикс `--radius-` для скруглений
   - Использовать префикс `--shadow-` для теней
   - Использовать префикс `--transition-` для переходов
   - Использовать префикс `--font-size-` для размеров шрифтов
   - Использовать префикс `--z-index-` для z-index

2. **Организация переменных:**
   - Группировать переменные по категориям
   - Использовать комментарии для разделения категорий
   - Документировать каждую категорию

3. **Расширение системы:**
   - При необходимости можно добавлять новые переменные
   - Важно сохранять консистентность именования
   - Обновлять документацию при добавлении новых переменных

### Потенциальные проблемы:

1. **Совместимость с существующими стилями:**
   - Существующие компоненты могут использовать кастомные цвета
   - Это нормально, так как этап 2-3 будут обновлять компоненты
   - Переменные не должны конфликтовать с существующими стилями

2. **Производительность:**
   - CSS-переменные имеют минимальное влияние на производительность
   - Файл переменных должен быть небольшим (< 10KB)
   - Импорт в `main.js` обеспечивает раннюю загрузку

3. **Поддержка браузеров:**
   - Все современные браузеры поддерживают CSS-переменные
   - Если требуется поддержка старых браузеров, можно использовать полифиллы (но это не требуется для Bitrix24)

