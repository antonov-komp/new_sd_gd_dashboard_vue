# TASK-046: Навигация между модулями — хлебные крошки и кнопки перехода

**Дата создания:** 2025-12-16 12:18 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связь с задачами:** Расширение навигации для модулей TASK-041, TASK-027

## Цель

Реализовать единую систему навигации между тремя модулями:
1. **Первый модуль:** Дашборд сектора 1С (`DashboardSector1C.vue`)
2. **Второй модуль:** График состояния сектора 1С (`GraphStateDashboard.vue`)
3. **Третий модуль:** График приёма и закрытий сектора 1С (`GraphAdmissionClosureDashboard.vue`)

## Контекст

**Текущее состояние:**
- Первый модуль (`DashboardSector1C.vue`) имеет кнопку "График состояния" для перехода ко второму модулю
- Второй модуль (`GraphStateDashboard.vue`) имеет хлебные крошки: "Дашборд сектора 1С / График состояния"
- Третий модуль (`GraphAdmissionClosureDashboard.vue`) **не имеет хлебных крошек** и кнопок навигации

**Проблема:**
- Нет навигации из первого модуля в третий модуль
- Нет навигации из второго модуля в третий модуль
- Третий модуль не показывает путь навигации (хлебные крошки)
- Второй модуль не показывает связь с третьим модулем в хлебных крошках

**Требуется:**
1. В первом модуле добавить кнопку для перехода в третий модуль (рядом с кнопкой "График состояния")
2. Во втором модуле обновить хлебные крошки: добавить третий модуль в путь
3. В третьем модуле добавить хлебные крошки на том же уровне, как во втором модуле
4. В третьем модуле хлебные крошки должны показывать путь: "Дашборд сектора 1С / График состояния / График приёма и закрытий"

## Структура навигации

### Иерархия модулей

```
Дашборд сектора 1С (первый модуль)
├── График состояния сектора 1С (второй модуль)
└── График приёма и закрытий сектора 1С (третий модуль)
```

### Хлебные крошки

**Второй модуль (текущие):**
```
Дашборд сектора 1С / График состояния
```

**Второй модуль (требуемые):**
```
Дашборд сектора 1С / График состояния / График приёма и закрытий
```
*Примечание: Третий модуль должен быть кликабельным в хлебных крошках второго модуля*

**Третий модуль (требуемые):**
```
Дашборд сектора 1С / График состояния / График приёма и закрытий
```

## Модули и компоненты

### 1. Первый модуль (`DashboardSector1C.vue`)

**Файл:** `vue-app/src/components/dashboard/DashboardSector1C.vue`

**Текущее состояние:**
- Имеет кнопку "График состояния" (строки 27-33)
- Кнопка переходит на маршрут `dashboard-graph-state`

**Требуется:**
- Добавить кнопку "График приёма и закрытий" рядом с кнопкой "График состояния"
- Кнопка должна переходить на маршрут `dashboard-graph-admission-closure`
- Стилизация должна соответствовать существующей кнопке

**Структура кнопки:**
```vue
<button 
  @click="navigateToAdmissionClosure"
  class="btn-navigate-admission-closure"
  title="Перейти к графику приёма и закрытий сектора"
>
  <span class="icon">📈</span>
  <span>График приёма и закрытий</span>
</button>
```

### 2. Второй модуль (`GraphStateDashboard.vue`)

**Файл:** `vue-app/src/components/graph-state/GraphStateDashboard.vue`

**Текущее состояние:**
- Хлебные крошки (строки 43-52):
  ```vue
  <nav class="breadcrumbs" aria-label="Навигация">
    <router-link 
      :to="{ name: 'dashboard-sector-1c' }"
      class="breadcrumb-link"
    >
      Дашборд сектора 1С
    </router-link>
    <span class="breadcrumb-separator" aria-hidden="true">/</span>
    <span class="breadcrumb-current">График состояния</span>
  </nav>
  ```

**Требуется:**
- Добавить третий модуль в хлебные крошки
- Третий модуль должен быть кликабельным (router-link)
- Структура: "Дашборд сектора 1С / График состояния / График приёма и закрытий"

**Обновлённая структура:**
```vue
<nav class="breadcrumbs" aria-label="Навигация">
  <router-link 
    :to="{ name: 'dashboard-sector-1c' }"
    class="breadcrumb-link"
  >
    Дашборд сектора 1С
  </router-link>
  <span class="breadcrumb-separator" aria-hidden="true">/</span>
  <span class="breadcrumb-current">График состояния</span>
  <span class="breadcrumb-separator" aria-hidden="true">/</span>
  <router-link 
    :to="{ name: 'dashboard-graph-admission-closure' }"
    class="breadcrumb-link"
  >
    График приёма и закрытий
  </router-link>
</nav>
```

### 3. Третий модуль (`GraphAdmissionClosureDashboard.vue`)

**Файл:** `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureDashboard.vue`

**Текущее состояние:**
- Нет хлебных крошек
- Заголовок находится в `dashboard-header` (строки 6-13)

**Требуется:**
- Добавить хлебные крошки на том же уровне, как во втором модуле
- Использовать ту же структуру и стили, что и во втором модуле
- Хлебные крошки должны показывать полный путь: "Дашборд сектора 1С / График состояния / График приёма и закрытий"
- Добавить кнопку "Назад" (как во втором модуле)

**Структура хлебных крошек:**
```vue
<div class="dashboard-header">
  <div class="header-content">
    <div class="breadcrumbs-row">
      <button
        class="btn-back-link"
        type="button"
        @click="handleBack"
        :aria-label="backAriaLabel"
        :aria-disabled="!hasHistory"
        :data-fallback="!hasHistory"
        :disabled="isNavigatingBack"
        title="Назад"
      >
        ←
      </button>
      <nav class="breadcrumbs" aria-label="Навигация">
        <router-link 
          :to="{ name: 'dashboard-sector-1c' }"
          class="breadcrumb-link"
        >
          Дашборд сектора 1С
        </router-link>
        <span class="breadcrumb-separator" aria-hidden="true">/</span>
        <router-link 
          :to="{ name: 'dashboard-graph-state' }"
          class="breadcrumb-link"
        >
          График состояния
        </router-link>
        <span class="breadcrumb-separator" aria-hidden="true">/</span>
        <span class="breadcrumb-current">График приёма и закрытий</span>
      </nav>
    </div>
    <h1 class="dashboard-title">График приёма и закрытий сектора 1С</h1>
    <p class="dashboard-subtitle">
      Доступы и фильтры аналогичны «Графику состояния», селектор этапов скрыт.
    </p>
  </div>
</div>
```

## Ступенчатые подзадачи

### 1. Добавление кнопки в первый модуль

**Файл:** `vue-app/src/components/dashboard/DashboardSector1C.vue`

1. Добавить метод `navigateToAdmissionClosure` в `setup()`:
   ```javascript
   const navigateToAdmissionClosure = () => {
     router.push({ name: 'dashboard-graph-admission-closure' });
   };
   ```

2. Добавить кнопку в `header-actions` (после кнопки "График состояния"):
   ```vue
   <button 
     @click="navigateToAdmissionClosure"
     class="btn-navigate-admission-closure"
     title="Перейти к графику приёма и закрытий сектора"
   >
     <span class="icon">📈</span>
     <span>График приёма и закрытий</span>
   </button>
   ```

3. Добавить стили для кнопки (аналогично `.btn-navigate-graph-state`):
   ```css
   .btn-navigate-admission-closure {
     display: flex;
     align-items: center;
     gap: 8px;
     padding: 10px 16px;
     background-color: #10b981; /* Зелёный цвет для отличия */
     color: white;
     border: none;
     border-radius: 6px;
     font-size: 14px;
     font-weight: 600;
     cursor: pointer;
     transition: background-color 0.2s;
   }
   
   .btn-navigate-admission-closure:hover {
     background-color: #059669;
   }
   
   .btn-navigate-admission-closure .icon {
     font-size: 18px;
   }
   ```

4. Вернуть метод в `return`:
   ```javascript
   return {
     // ... существующие свойства
     navigateToAdmissionClosure
   };
   ```

### 2. Обновление хлебных крошек во втором модуле

**Файл:** `vue-app/src/components/graph-state/GraphStateDashboard.vue`

1. Обновить структуру хлебных крошек (строки 43-52):
   - Добавить разделитель после "График состояния"
   - Добавить router-link на третий модуль

2. Проверить, что стили поддерживают дополнительные элементы (уже должны работать)

### 3. Добавление хлебных крошек в третий модуль

**Файл:** `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureDashboard.vue`

1. Импортировать необходимые композаблы и утилиты:
   ```javascript
   import { useRouter } from 'vue-router';
   import { ref, computed } from 'vue';
   ```

2. Добавить логику для кнопки "Назад":
   ```javascript
   const router = useRouter();
   const isNavigatingBack = ref(false);
   
   const hasHistory = computed(() => {
     return window.history.length > 1;
   });
   
   const backAriaLabel = computed(() => {
     return hasHistory.value ? 'Вернуться на предыдущую страницу' : 'Вернуться на главную страницу';
   });
   
   const handleBack = () => {
     if (isNavigatingBack.value) return;
     
     isNavigatingBack.value = true;
     
     if (hasHistory.value) {
       router.back();
     } else {
       router.push({ name: 'dashboard-sector-1c' });
     }
     
     // Сброс флага после небольшой задержки
     setTimeout(() => {
       isNavigatingBack.value = false;
     }, 300);
   };
   ```

3. Обновить структуру `dashboard-header`:
   - Добавить `breadcrumbs-row` с кнопкой "Назад" и хлебными крошками
   - Обернуть заголовок и подзаголовок в `header-content`

4. Скопировать стили хлебных крошек из второго модуля:
   - `.breadcrumbs-row`
   - `.btn-back-link`
   - `.breadcrumbs`
   - `.breadcrumb-link`
   - `.breadcrumb-separator`
   - `.breadcrumb-current`

5. Обновить структуру `dashboard-header` для соответствия второму модулю

## Технические требования

### Маршруты

**Проверка существующих маршрутов:**
- `dashboard-sector-1c` — первый модуль
- `dashboard-graph-state` — второй модуль
- `dashboard-graph-admission-closure` — третий модуль

**Файл:** `vue-app/src/router/index.js`

Все маршруты уже должны быть определены. Проверить наличие:
```javascript
{
  path: '/dashboard/graph-admission-closure',
  name: 'dashboard-graph-admission-closure',
  component: () => import('@/components/graph-admission-closure/GraphAdmissionClosureDashboard.vue'),
  meta: {
    title: 'График приёма и закрытий сектора 1С',
    description: 'Недельные агрегаты новых и закрытых тикетов сектора 1С',
    requiresAuth: true
  }
}
```

### Стили

**Стили хлебных крошек должны быть идентичны во втором и третьем модулях.**

**Файл для справки:** `vue-app/src/components/graph-state/GraphStateDashboard.vue` (строки 496-574)

**Ключевые стили:**
- `.breadcrumbs-row` — контейнер для кнопки "Назад" и хлебных крошек
- `.btn-back-link` — кнопка "Назад"
- `.breadcrumbs` — навигационный контейнер
- `.breadcrumb-link` — кликабельная ссылка
- `.breadcrumb-separator` — разделитель "/"
- `.breadcrumb-current` — текущая страница (не кликабельная)

### Доступность

**ARIA-атрибуты:**
- `aria-label="Навигация"` для `<nav class="breadcrumbs">`
- `aria-label` для кнопки "Назад" (динамический)
- `aria-disabled` для кнопки "Назад" (если нет истории)
- `aria-hidden="true"` для разделителей

## Критерии приёмки

- [ ] В первом модуле добавлена кнопка "График приёма и закрытий" рядом с кнопкой "График состояния"
- [ ] Кнопка в первом модуле корректно переходит на третий модуль
- [ ] Стилизация кнопки соответствует существующим кнопкам
- [ ] Во втором модуле хлебные крошки обновлены: добавлен третий модуль
- [ ] Третий модуль в хлебных крошках второго модуля кликабелен
- [ ] В третьем модуле добавлены хлебные крошки на том же уровне, как во втором модуле
- [ ] Хлебные крошки третьего модуля показывают полный путь: "Дашборд сектора 1С / График состояния / График приёма и закрытий"
- [ ] В третьем модуле добавлена кнопка "Назад" (как во втором модуле)
- [ ] Кнопка "Назад" в третьем модуле работает корректно (возврат на предыдущую страницу или на главную)
- [ ] Стили хлебных крошек идентичны во втором и третьем модулях
- [ ] Все ссылки в хлебных крошках кликабельны и ведут на правильные маршруты
- [ ] ARIA-атрибуты добавлены для доступности
- [ ] Навигация работает корректно на мобильных устройствах

## Тестирование

### Функциональное тестирование

1. **Первый модуль:**
   - Открыть первый модуль
   - Проверить наличие кнопки "График приёма и закрытий"
   - Кликнуть на кнопку — должен произойти переход на третий модуль

2. **Второй модуль:**
   - Открыть второй модуль
   - Проверить хлебные крошки: должны быть "Дашборд сектора 1С / График состояния / График приёма и закрытий"
   - Кликнуть на "График приёма и закрытий" в хлебных крошках — должен произойти переход на третий модуль

3. **Третий модуль:**
   - Открыть третий модуль
   - Проверить наличие хлебных крошек: "Дашборд сектора 1С / График состояния / График приёма и закрытий"
   - Кликнуть на "Дашборд сектора 1С" — должен произойти переход на первый модуль
   - Кликнуть на "График состояния" — должен произойти переход на второй модуль
   - Кликнуть на кнопку "Назад" — должен произойти возврат на предыдущую страницу

### Визуальное тестирование

1. Проверить, что стили хлебных крошек идентичны во втором и третьем модулях
2. Проверить, что кнопки в первом модуле выровнены и имеют одинаковый стиль
3. Проверить адаптивность на мобильных устройствах

### Тестирование доступности

1. Проверить ARIA-атрибуты через инструменты разработчика
2. Проверить навигацию с клавиатуры (Tab, Enter)
3. Проверить работу с screen reader

## Дополнительные уточнения

### Единообразие стилей

**Важно:** Стили хлебных крошек должны быть идентичны во втором и третьем модулях. Рекомендуется:
- Скопировать стили из второго модуля в третий
- Или вынести общие стили в отдельный файл (если планируется переиспользование)

### Производительность

- Использовать `router-link` вместо программной навигации для лучшей производительности
- Кнопка "Назад" должна использовать `router.back()` для сохранения истории браузера

### Совместимость

- Проверить работу на всех поддерживаемых браузерах
- Убедиться, что навигация работает корректно при использовании истории браузера

## История правок

- 2025-12-16 12:18 (UTC+3, Брест): Создана задача
- 2025-12-16 (UTC+3, Брест): Задача завершена
  - Добавлена кнопка "График приёма и закрытий" в первый модуль (DashboardSector1C.vue)
  - Обновлены хлебные крошки во втором модуле (GraphStateDashboard.vue) - добавлен третий модуль
  - Добавлены хлебные крошки и кнопка "Назад" в третий модуль (GraphAdmissionClosureDashboard.vue)
  - Все навигационные элементы работают корректно, стили идентичны

