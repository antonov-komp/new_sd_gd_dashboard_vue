# TASK-058: Улучшение модуля "График приема и закрытий сектора 1С" — режим 3 месяца

**Дата создания:** 2025-12-18 08:31 (UTC+3, Брест)  
**Статус:** 📝 Черновик (требует детализации)  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связь с задачами:** 
- TASK-053 (Режим 3 месяца) — базовый функционал
- TASK-053-04 (Summary-карточки для 3-месячного режима)
- TASK-053-05 (Линейные графики для 3-месячного режима)

---

## 📋 Описание

Улучшить модуль «График приема и закрытий сектора 1С» в режиме 3 месяца, добавив:
1. Процентные составляющие в секции чипов (Новые за период, Закрытые всего, Переходящие всего)
2. Проверку и уточнение логики расчета показателей "Закрытые всего"
3. Улучшение линейных графиков: цифры на точках, динамичные заголовки, сводные итоги с анализом

---

## 🎯 Контекст

### Текущее состояние

**Модуль:** График приема и закрытий сектора 1С  
**Режим:** 3 месяца  
**Компоненты:**
- `SummaryCardsMonths.vue` — карточки с чипами (Новые за период, Закрытые всего, Переходящие всего)
- `LineChartMonths.vue` — линейные графики (Новые и закрытые тикеты, Переходящие тикеты)

**Текущие данные:**
- Отображаются данные за 3 месяца
- В чипах показываются абсолютные значения
- В графиках нет цифр на точках
- Нет динамичных заголовков с периодом
- Нет сводных итогов с анализом

### Проблемы и требования

1. **Процентные составляющие в чипах:**
   - Нужно добавить процентные показатели относительно предыдущего периода
   - Для расчета процентов требуется захватить 4-й месяц данных (под капотом)
   - Проценты должны отображаться рядом с абсолютными значениями

2. **Логика "Закрытые всего":**
   - Требуется проверить и уточнить логику расчета показателей
   - Вопрос: "Этой неделей" и "другой неделей" — это месячные данные?
   - Пример: Октябрь — 80 всего, этой неделей — 60, другой неделей — 20
   - Гипотеза: 60 — закрытые тикеты, созданные в октябре; 20 — закрытые тикеты октября, но созданные ранее

3. **Улучшение графиков:**
   - Добавить цифры на точках графика (даже при перекрытии — двойные чипы)
   - Динамичные заголовки с указанием периода (какие месяца показывает)
   - Сводные итоги под графиками с словесным анализом на основе процентов

---

## 🔍 Детализация требований

### 1. Процентные составляющие в чипах

**Местоположение:** `SummaryCardsMonths.vue`

**Требования:**
- В каждой карточке (Новые за период, Закрытые всего, Переходящие всего) добавить процентные показатели
- Проценты рассчитываются относительно предыдущего периода (4-й месяц)
- Данные 4-го месяца должны быть получены под капотом (не отображаться в интерфейсе)
- Формат отображения: абсолютное значение + процент изменения

**Пример отображения:**
```
Новые за период
150 (+12.5%)
Октябрь: 50
Ноябрь: 60
Декабрь: 40
```

**Технические детали:**
- Backend должен возвращать данные за 4 месяца (3 для отображения + 1 для расчета процентов)
- Проценты рассчитываются на фронтенде: `((текущий - предыдущий) / предыдущий) * 100`
- Отрицательные проценты отображаются красным цветом, положительные — зеленым

### 2. Проверка логики "Закрытые всего"

**Вопрос для уточнения:**
- Как именно считаются показатели "этой неделей" и "другой неделей" в месячном режиме?
- Это месячные данные или недельные?

**Гипотеза (требует подтверждения):**
- "Этой неделей" — закрытые тикеты октября, которые были созданы в октябре
- "Другой неделей" — закрытые тикеты октября, которые были созданы в предыдущих месяцах

**Задачи:**
1. Проверить текущую логику расчета в Backend API
2. Уточнить у заказчика точную логику
3. При необходимости — исправить логику расчета
4. Обновить документацию

### 3. Улучшение графика "Новые и закрытые тикеты"

**Местоположение:** `LineChartMonths.vue`

**Требования:**

#### 3.1. Цифры на точках графика
- Добавить отображение числовых значений на каждой точке графика
- Если точки перекрываются — показывать двойной чип (оба значения)
- Использовать Chart.js плагин для отображения значений на точках

#### 3.2. Динамичный заголовок
- Заменить статичный заголовок "Новые и Закрытые тикеты" на динамичный
- Формат: "Новые и Закрытые тикеты (Октябрь — Декабрь 2025)"
- Период формируется из данных `meta.months` или `data.newTicketsByMonth`

#### 3.3. Сводный итог под графиком
- Добавить блок "Сводный итог" под графиком
- Отображать 6 цифр (2 показателя × 3 месяца):
  - Новые: октябрь, ноябрь, декабрь
  - Закрытые: октябрь, ноябрь, декабрь
- Добавить словесный анализ на основе процентных показателей
- Использовать данные 4-го месяца для расчета процентов

**Пример сводного итога:**
```
Сводный итог
Новые тикеты: 50 (окт) → 60 (ноя) → 40 (дек)
Закрытые тикеты: 80 (окт) → 90 (ноя) → 70 (дек)

Анализ:
- Новые тикеты: рост на 20% в ноябре, снижение на 33.3% в декабре
- Закрытые тикеты: рост на 12.5% в ноябре, снижение на 22.2% в декабре
- Общая динамика: положительная тенденция закрытия тикетов
```

### 4. Улучшение графика "Переходящие тикеты"

**Местоположение:** `LineChartMonths.vue`

**Требования:**

#### 4.1. Динамичный заголовок
- Заменить статичный заголовок "Переходящие тикеты" на динамичный
- Формат: "Переходящие тикеты (Октябрь — Декабрь 2025)"

#### 4.2. Цифры на точках графика
- Добавить отображение числовых значений на каждой точке графика
- Аналогично графику "Новые и закрытые тикеты"

#### 4.3. Словесный отчет под графиком
- Добавить блок "Анализ" под графиком
- Отображать словесный анализ на основе процентных показателей
- Использовать данные 4-го месяца для расчета процентов

**Пример анализа:**
```
Анализ:
- Переходящие тикеты: 30 (окт) → 35 (ноя) → 25 (дек)
- Динамика: рост на 16.7% в ноябре, снижение на 28.6% в декабре
- Тенденция: снижение переходящих тикетов в конце периода
```

---

## 📝 Ступенчатые подзадачи

### Порядок выполнения этапов

**Рекомендуемый порядок:**
1. **Этап 1 (Backend)** — должен быть выполнен первым, так как предоставляет данные для остальных этапов
2. **Этап 3 (Проверка логики)** — можно выполнять параллельно с этапом 1, не блокирует другие этапы
3. **Этап 2 (Frontend чипы)** — зависит от этапа 1, можно выполнять после него
4. **Этап 4 (График новые/закрытые)** — зависит от этапа 1, можно выполнять параллельно с этапом 2
5. **Этап 5 (График переходящие)** — зависит от этапа 4 (переиспользование логики), выполняется последним

**Критический путь:**
```
Этап 1 (Backend) → Этап 2 (Чипы) → Готово
                → Этап 4 (График новые/закрытые) → Этап 5 (График переходящие) → Готово
```

**Параллельное выполнение:**
- Этап 1 и Этап 3 можно выполнять параллельно (независимы)
- Этап 2 и Этап 4 можно выполнять параллельно после этапа 1

### Этап 1: Backend — получение данных 4-го месяца

**Детализированный документ:** [TASK-058-01: Backend — получение данных 4-го месяца для расчета процентов](./TASK-058-01-backend-4th-month-data.md)

**Краткое описание:**
- Модифицировать API для получения данных за 4 месяца (вместо 3)
- Добавить в ответ API поле `previousPeriodData` с данными 4-го месяца
- Данные 4-го месяца не должны влиять на отображение, но должны быть доступны для расчета процентов

**Критичность:** 🔴 Высокая — блокирует этапы 2, 4, 5

**Ориентировочное время:** 2-3 часа

### Этап 2: Frontend — процентные составляющие в чипах

**Детализированный документ:** [TASK-058-02: Frontend — процентные составляющие в чипах](./TASK-058-02-frontend-percentage-chips.md)

**Краткое описание:**
- Добавить computed-свойства для расчета процентов в `SummaryCardsMonths.vue`
- Отобразить проценты рядом с абсолютными значениями
- Добавить стили для положительных/отрицательных процентов

**Критичность:** 🟡 Средняя — зависит от этапа 1

**Ориентировочное время:** 2-3 часа

### Этап 3: Проверка логики "Закрытые всего"

**Детализированный документ:** [TASK-058-03: Проверка логики "Закрытые всего"](./TASK-058-03-check-closed-logic.md)

**Краткое описание:**
- Изучить текущую логику расчета показателей "этой неделей" и "другой неделей"
- Проверить логику с тестовыми данными
- Уточнить логику у заказчика
- При необходимости — исправить логику расчета

**Критичность:** 🟢 Низкая — не блокирует другие этапы, можно выполнять параллельно

**Ориентировочное время:** 1-2 часа (проверка) + время на уточнение у заказчика

**Примечание:** Может потребоваться исправление логики, что увеличит время выполнения

### Этап 4: Улучшение графика "Новые и закрытые тикеты"

**Детализированный документ:** [TASK-058-04: Frontend — улучшение графика "Новые и закрытые тикеты"](./TASK-058-04-frontend-chart-new-closed-improvements.md)

**Краткое описание:**
- Добавить цифры на точках графика (Chart.js плагин `datalabels`)
- Добавить динамичный заголовок с периодом
- Добавить сводный итог под графиком с анализом

**Критичность:** 🟡 Средняя — зависит от этапа 1, блокирует этап 5

**Ориентировочное время:** 3-4 часа

### Этап 5: Улучшение графика "Переходящие тикеты"

**Детализированный документ:** [TASK-058-05: Frontend — улучшение графика "Переходящие тикеты"](./TASK-058-05-frontend-chart-carryover-improvements.md)

**Краткое описание:**
- Добавить динамичный заголовок с периодом
- Добавить цифры на точках графика
- Добавить словесный отчет под графиком с анализом

**Критичность:** 🟢 Низкая — зависит от этапа 4 (переиспользование логики)

**Ориентировочное время:** 2-3 часа

---

## 🔧 Технические требования

### Backend (PHP)

**Расширение API:**
- Добавить получение данных за 4-й месяц (предыдущий период)
- Включить данные 4-го месяца в ответ API (поле `previousPeriodData`)
- Не изменять существующую логику расчета для 3 месяцев

**Структура данных:**
```php
$response = [
    'data' => [
        // Существующие данные за 3 месяца
        'newTickets' => 150,
        'newTicketsByMonth' => [...], // 3 месяца
        'closedTickets' => 240,
        'closedTicketsByMonth' => [...], // 3 месяца
        'carryoverTickets' => 90,
        'carryoverTicketsByMonth' => [...], // 3 месяца
        
        // Новые данные для расчета процентов
        'previousPeriodData' => [
            'newTickets' => 133, // 4-й месяц
            'closedTickets' => 200,
            'carryoverTickets' => 75
        ]
    ]
];
```

### Frontend (Vue.js)

**Зависимости:**
- Chart.js (уже используется)
- chartjs-plugin-datalabels (для отображения значений на точках) — возможно потребуется установка

**Компоненты для изменения:**
- `SummaryCardsMonths.vue` — добавление процентных показателей
- `LineChartMonths.vue` — улучшение графиков

**Новые computed-свойства:**
- `newTicketsPercentage` — процент изменения новых тикетов
- `closedTicketsPercentage` — процент изменения закрытых тикетов
- `carryoverTicketsPercentage` — процент изменения переходящих тикетов
- `chartPeriod` — период для динамичного заголовка
- `summaryAnalysis` — анализ для сводного итога
- `carryoverAnalysis` — анализ для переходящих тикетов

**Новые функции:**
- `formatPercentage(value)` — форматирование процента (+12.5%, -5.2%)
- `generateTextAnalysis(data)` — генерация словесного анализа
- `generateCarryoverAnalysis(data)` — генерация анализа переходящих тикетов

---

## ✅ Критерии приёмки

### Общие требования
- [ ] Все изменения работают в режиме 3 месяца
- [ ] Не нарушена работа режима 4 недели
- [ ] Код соответствует стандартам проекта
- [ ] Компоненты адаптивны для мобильных устройств

### Процентные составляющие в чипах
- [ ] Проценты отображаются во всех трех карточках (Новые, Закрытые, Переходящие)
- [ ] Проценты рассчитываются относительно 4-го месяца
- [ ] Положительные проценты отображаются зеленым цветом
- [ ] Отрицательные проценты отображаются красным цветом
- [ ] Обрабатывается случай отсутствия данных предыдущего периода

### Проверка логики "Закрытые всего"
- [ ] Логика расчета проверена и задокументирована
- [ ] Показатели "этой неделей" и "другой неделей" работают корректно
- [ ] При необходимости — логика исправлена

### График "Новые и закрытые тикеты"
- [ ] Цифры отображаются на всех точках графика
- [ ] При перекрытии точек показываются оба значения (двойной чип)
- [ ] Динамичный заголовок показывает период (месяца)
- [ ] Сводный итог отображает 6 цифр (2 показателя × 3 месяца)
- [ ] Словесный анализ основан на процентных показателях
- [ ] Анализ использует данные 4-го месяца для расчета процентов

### График "Переходящие тикеты"
- [ ] Динамичный заголовок показывает период (месяца)
- [ ] Цифры отображаются на всех точках графика
- [ ] Словесный отчет под графиком отображает анализ
- [ ] Анализ основан на процентных показателях

---

## 📚 Примеры реализации

### Пример 1: Процентные составляющие в чипах

```vue
<!-- SummaryCardsMonths.vue -->
<template>
  <div class="summary-card summary-card--new">
    <h3 class="card-title">Новые за период</h3>
    <div class="card-content">
      <div class="card-main-value">
        {{ formattedTotalNewTickets }}
        <span 
          v-if="newTicketsPercentage !== null"
          :class="['percentage-badge', newTicketsPercentage >= 0 ? 'positive' : 'negative']"
        >
          {{ formatPercentage(newTicketsPercentage) }}
        </span>
      </div>
      <!-- ... остальной код ... -->
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';

const newTicketsPercentage = computed(() => {
  const current = props.data?.newTickets || 0;
  const previous = props.data?.previousPeriodData?.newTickets;
  
  if (previous === null || previous === undefined || previous === 0) {
    return null;
  }
  
  return ((current - previous) / previous) * 100;
});

function formatPercentage(value) {
  if (value === null || value === undefined) {
    return '';
  }
  
  const sign = value >= 0 ? '+' : '';
  return `${sign}${value.toFixed(1)}%`;
}
</script>

<style scoped>
.percentage-badge {
  font-size: 16px;
  font-weight: 600;
  margin-left: 8px;
}

.percentage-badge.positive {
  color: var(--b24-success, #28a745);
}

.percentage-badge.negative {
  color: var(--b24-danger, #dc3545);
}
</style>
```

### Пример 2: Динамичный заголовок графика

```vue
<!-- LineChartMonths.vue -->
<template>
  <div class="chart-section">
    <h3 class="chart-title">
      Новые и Закрытые тикеты
      <span v-if="chartPeriod" class="chart-period">({{ chartPeriod }})</span>
    </h3>
    <!-- ... остальной код ... -->
  </div>
</template>

<script setup>
const chartPeriod = computed(() => {
  const months = props.data?.newTicketsByMonth || [];
  
  if (months.length === 0) {
    return null;
  }
  
  const firstMonth = months[0];
  const lastMonth = months[months.length - 1];
  
  if (!firstMonth || !lastMonth) {
    return null;
  }
  
  const firstMonthName = firstMonth.monthName || `Месяц ${firstMonth.month}`;
  const lastMonthName = lastMonth.monthName || `Месяц ${lastMonth.month}`;
  
  return `${firstMonthName} — ${lastMonthName}`;
});
</script>
```

### Пример 3: Сводный итог под графиком

```vue
<!-- LineChartMonths.vue -->
<template>
  <div class="chart-section">
    <!-- ... график ... -->
    
    <!-- Сводный итог -->
    <div class="chart-summary">
      <h4 class="summary-title">Сводный итог</h4>
      <div class="summary-numbers">
        <div class="summary-row">
          <span class="summary-label">Новые:</span>
          <span class="summary-values">{{ summaryNumbers.new }}</span>
        </div>
        <div class="summary-row">
          <span class="summary-label">Закрытые:</span>
          <span class="summary-values">{{ summaryNumbers.closed }}</span>
        </div>
      </div>
      <div class="summary-analysis">
        <p v-for="(analysis, index) in summaryAnalysis" :key="index">
          {{ analysis }}
        </p>
      </div>
    </div>
  </div>
</template>

<script setup>
const summaryNumbers = computed(() => {
  const months = props.data?.newTicketsByMonth || [];
  const closedMonths = props.data?.closedTicketsByMonth || [];
  
  if (months.length === 0) {
    return { new: '—', closed: '—' };
  }
  
  const newValues = months.map(m => `${formatNumber(m.count || 0)} (${m.monthName || m.month})`).join(' → ');
  const closedValues = closedMonths.map(m => `${formatNumber(m.count || 0)} (${m.monthName || m.month})`).join(' → ');
  
  return {
    new: newValues,
    closed: closedValues
  };
});

const summaryAnalysis = computed(() => {
  const months = props.data?.newTicketsByMonth || [];
  const closedMonths = props.data?.closedTicketsByMonth || [];
  const previousData = props.data?.previousPeriodData;
  
  if (months.length < 2) {
    return ['Недостаточно данных для анализа'];
  }
  
  const analysis = [];
  
  // Анализ новых тикетов
  if (months.length >= 2) {
    const month1 = months[0];
    const month2 = months[1];
    const month3 = months[2];
    
    if (month2 && month1) {
      const change1 = ((month2.count - month1.count) / month1.count) * 100;
      analysis.push(`Новые тикеты: ${change1 >= 0 ? 'рост' : 'снижение'} на ${Math.abs(change1).toFixed(1)}% в ${month2.monthName}`);
    }
    
    if (month3 && month2) {
      const change2 = ((month3.count - month2.count) / month2.count) * 100;
      analysis.push(`Новые тикеты: ${change2 >= 0 ? 'рост' : 'снижение'} на ${Math.abs(change2).toFixed(1)}% в ${month3.monthName}`);
    }
  }
  
  // Анализ закрытых тикетов
  if (closedMonths.length >= 2) {
    const month1 = closedMonths[0];
    const month2 = closedMonths[1];
    const month3 = closedMonths[2];
    
    if (month2 && month1) {
      const change1 = ((month2.count - month1.count) / month1.count) * 100;
      analysis.push(`Закрытые тикеты: ${change1 >= 0 ? 'рост' : 'снижение'} на ${Math.abs(change1).toFixed(1)}% в ${month2.monthName}`);
    }
    
    if (month3 && month2) {
      const change2 = ((month3.count - month2.count) / month2.count) * 100;
      analysis.push(`Закрытые тикеты: ${change2 >= 0 ? 'рост' : 'снижение'} на ${Math.abs(change2).toFixed(1)}% в ${month3.monthName}`);
    }
  }
  
  return analysis.length > 0 ? analysis : ['Недостаточно данных для анализа'];
});
</script>
```

---

## 🔗 Зависимости

### Внешние зависимости

- **TASK-053:** Режим 3 месяца (базовый функционал)
- **TASK-053-04:** Summary-карточки для 3-месячного режима
- **TASK-053-05:** Линейные графики для 3-месячного режима

### Внутренние зависимости (между этапами)

- **Этап 1 → Этап 2:** Backend должен возвращать `previousPeriodData` для расчета процентов в чипах
- **Этап 1 → Этап 4:** Backend должен возвращать `previousPeriodData` для анализа в графике
- **Этап 1 → Этап 5:** Backend должен возвращать `previousPeriodData` для анализа в графике
- **Этап 4 → Этап 5:** Переиспользование логики динамичного заголовка и цифр на точках

### Технические зависимости

- **Chart.js:** Версия 4.0.0 или выше
- **chartjs-plugin-datalabels:** Версия 2.2.0 или выше (для этапов 4 и 5)
- **Vue.js:** Версия 3.x (уже используется в проекте)

---

## ❓ Вопросы для уточнения

1. **Логика "Закрытые всего":**
   - Как именно считаются показатели "этой неделей" и "другой неделей" в месячном режиме?
   - Это месячные данные или недельные?
   - Подтвердить гипотезу: "этой неделей" = закрытые тикеты месяца, созданные в этом же месяце?

2. **Формат отображения процентов:**
   - Какой формат предпочтительнее: "+12.5%" или "↑ 12.5%"?
   - Нужны ли иконки для визуального выделения?

3. **Словесный анализ:**
   - Какой уровень детализации нужен в анализе?
   - Нужны ли рекомендации на основе анализа?

4. **Данные 4-го месяца:**
   - Должны ли данные 4-го месяца быть доступны в интерфейсе (например, в tooltip)?
   - Или они используются только для расчета процентов?

---

## 📝 История правок

- **2025-12-18 08:31 (UTC+3, Брест):** Создан черновик задачи TASK-058
  - Добавлено описание требований
  - Описаны этапы реализации
  - Добавлены примеры кода
  - Добавлены вопросы для уточнения

- **2025-12-18 08:31 (UTC+3, Брест):** Детализированы все этапы задачи
  - Созданы отдельные документы для каждого этапа (TASK-058-01 до TASK-058-05)
  - Добавлены пошаговые инструкции реализации
  - Добавлены разделы с типичными ошибками и их решениями
  - Добавлены разделы тестирования
  - Добавлены детальные примеры кода
  - Добавлена информация о порядке выполнения этапов
  - Добавлена оценка времени выполнения

---

## ⚠️ Примечания

- **Статус:** 📝 Черновик — задача детализирована, готова к реализации
- **Приоритет:** Высокий — улучшение важного модуля
- **Сложность:** Средняя — требует изменений в Backend и Frontend
- **Тестирование:** Требуется тестирование с реальными данными за 3-4 месяца
- **Общее время выполнения:** Ориентировочно 10-15 часов (с учетом тестирования и уточнений)

## 📊 Оценка времени выполнения

**По этапам:**
- Этап 1 (Backend): 2-3 часа
- Этап 2 (Frontend чипы): 2-3 часа
- Этап 3 (Проверка логики): 1-2 часа + время на уточнение
- Этап 4 (График новые/закрытые): 3-4 часа
- Этап 5 (График переходящие): 2-3 часа
- Тестирование и отладка: 2-3 часа

**Итого:** 12-18 часов (1.5-2 рабочих дня)

**С учетом параллельного выполнения:**
- Минимум: 10-12 часов (если этапы 1 и 3 выполняются параллельно)
- Максимум: 15-18 часов (с учетом уточнений и исправлений)

## 🎯 Критерии успешного завершения задачи

- [ ] Все 5 этапов выполнены и протестированы
- [ ] Проценты отображаются во всех чипах
- [ ] Графики показывают цифры на точках
- [ ] Динамичные заголовки работают корректно
- [ ] Сводные итоги и анализ отображаются под графиками
- [ ] Логика "Закрытые всего" проверена и задокументирована
- [ ] Код протестирован с реальными данными
- [ ] Нет регрессий в существующем функционале

