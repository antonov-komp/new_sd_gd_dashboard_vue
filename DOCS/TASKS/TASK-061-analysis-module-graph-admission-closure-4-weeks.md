# TASK-061: Детальный анализ модуля «График приёма и закрытий 1С» — режим 4 недели

**Дата создания:** 2025-12-23 14:30 (UTC+3, Брест)  
**Статус:** ✅ Анализ завершён  
**Приоритет:** Высокий  
**Исполнитель:** Технический писатель  
**Тип задачи:** Анализ и документация  
**Связанные задачи:** TASK-041, TASK-048, TASK-053

---

## 📋 Описание

Детальный анализ модуля «График приёма и закрытий сектора 1С» в режиме отображения данных за 4 недели. Анализ включает архитектуру, компоненты, API контракт, логику работы, зависимости, производительность и рекомендации по улучшению.

---

## 🎯 Цель анализа

1. **Документирование текущего состояния модуля** в режиме 4 недели
2. **Выявление архитектурных решений** и паттернов
3. **Анализ производительности** и оптимизаций
4. **Определение зависимостей** между компонентами
5. **Рекомендации по улучшению** и развитию модуля

---

## 📊 Обзор модуля

### Назначение

Модуль «График приёма и закрытий сектора 1С» предназначен для визуализации динамики поступления и закрытия тикетов сектора 1С по неделям. Режим 4 недели позволяет анализировать краткосрочные тренды за последние 4 недели (текущая неделя + 3 предыдущие).

### Ключевые особенности режима 4 недели

- **Недельная агрегация:** данные агрегируются по неделям ISO-8601 (понедельник–воскресенье, UTC)
- **4 недели:** текущая неделя + 3 предыдущие недели
- **7 линий на графике:**
  - Новые тикеты
  - Закрытые (все)
  - Закрытые (созданы этой неделей) — пунктир
  - Закрытые (созданы другой неделей) — пунктир
  - Переходящие (все)
  - Переходящие (созданы этой неделей) — пунктир
  - Переходящие (созданы другой неделей) — пунктир
- **Без попапов на точках:** клики на точки графика отключены
- **Summary-карточки:** показывают данные текущей недели

### Связанные модули

- **Модуль 1:** Дашборд сектора 1С (`DashboardSector1C.vue`)
- **Модуль 2:** График состояния сектора 1С (`GraphStateDashboard.vue`)
- **Модуль 3:** График приёма и закрытий сектора 1С (текущий модуль)
- **Модуль 4:** Трудозатраты на тикеты сектора 1С (`TicketsTimeTrackingDashboard.vue`)

---

## 🏗️ Архитектура модуля

### Структура файлов

```
vue-app/src/
├── components/
│   └── graph-admission-closure/
│       ├── GraphAdmissionClosureDashboard.vue    # Главный компонент дашборда
│       ├── GraphAdmissionClosureChart.vue        # Компонент графиков (линейный, столбчатый, круговая)
│       ├── ResponsibleModal.vue                  # Модальное окно ответственных
│       ├── StagesModal.vue                       # Модальное окно стадий
│       ├── CarryoverDurationModal.vue             # Модальное окно переходящих тикетов
│       ├── PeriodModeSelector.vue                 # Селектор режима (4 недели / 3 месяца)
│       └── PeriodModeInfoModal.vue               # Информационное модальное окно о режимах
├── services/
│   └── graph-admission-closure/
│       └── admissionClosureService.js            # Сервис для работы с API
└── components/
    └── filters/
        ├── FiltersPanel.vue                      # Панель фильтров (общий компонент)
        └── WeekPicker.vue                        # Компонент выбора недели (барабан прокрутки)

api/
└── graph-1c-admission-closure.php                # Backend API endpoint
```

### Поток данных (режим 4 недели)

```
1. GraphAdmissionClosureDashboard (инициализация)
   ↓
2. PeriodModeSelector (выбор режима: 'weeks' = 4 недели)
   ↓
3. FiltersPanel (фильтры, если нужны)
   ↓
4. admissionClosureService.fetchAdmissionClosureStats({
     periodMode: 'weeks',
     weekStartUtc: ...,
     weekEndUtc: ...
   })
   ↓
5. api/graph-1c-admission-closure.php (обработка на бэкенде)
   ↓
6. getFourWeeksBounds() — вычисление границ 4 недель
   ↓
7. Bitrix24 REST API (получение тикетов за весь период 4 недель)
   ↓
8. calculateWeekMetrics() — подсчёт метрик для каждой недели (цикл по 4 неделям)
   ↓
9. Формирование ответа:
   - meta.weeks[] — массив с информацией о 4 неделях
   - data.series.*[] — массивы с 4 элементами (по одному на неделю)
   - data.weeksData[] — детальные данные для каждой недели
   - data.currentWeek — данные текущей недели (для summary-карточек)
   ↓
10. GraphAdmissionClosureChart (отображение графиков)
    ↓
11. Линейный график с 4 точками на оси X и 7 линиями
```

---

## 🔧 Технические детали реализации

### Backend (PHP)

#### Файл: `api/graph-1c-admission-closure.php`

**Ключевые функции для режима 4 недели:**

1. **`getFourWeeksBounds(DateTimeImmutable $currentWeekStart, DateTimeImmutable $currentWeekEnd): array`**
   - Вычисляет границы 4 недель (текущая + 3 предыдущие)
   - Возвращает массив с информацией о каждой неделе:
     ```php
     [
       'weekNumber' => int,
       'weekStartUtc' => string (ISO-8601),
       'weekEndUtc' => string (ISO-8601),
       'weekStart' => DateTimeImmutable,
       'weekEnd' => DateTimeImmutable
     ]
     ```
   - Недели упорядочены от старых к новым (неделя 48, 49, 50, 51)

2. **`calculateWeekMetrics(...)`**
   - Подсчитывает метрики для одной недели:
     - `newTickets` — новые тикеты (по `createdTime`)
     - `closedTickets` — все закрытые тикеты (по `movedTime` и закрывающим стадиям)
     - `closedTicketsCreatedThisWeek` — закрытые, созданные этой неделей
     - `closedTicketsCreatedOtherWeek` — закрытые, созданные другой неделей
     - `carryoverTickets` — переходящие тикеты (накопление)
     - `carryoverTicketsCreatedThisWeek` — переходящие, созданные этой неделей
     - `carryoverTicketsCreatedOtherWeek` — переходящие, созданные другой неделей

3. **Логика обработки запроса:**
   ```php
   // Определение режима периода
   $periodMode = $body['periodMode'] ?? 'weeks';
   
   if ($periodMode === 'weeks') {
       // Получение границ 4 недель
       [$weekStart, $weekEnd] = getWeekBounds($weekStartParam, $weekEndParam);
       $weeks = getFourWeeksBounds($weekStart, $weekEnd);
       
       // Запрос тикетов за весь период (от начала самой старой недели до конца текущей)
       $firstWeekStart = $weeks[0]['weekStart'];
       $lastWeekEnd = $weeks[count($weeks) - 1]['weekEnd'];
       
       // Получение тикетов из Bitrix24
       $tickets = fetchTicketsFromBitrix24($firstWeekStart, $lastWeekEnd, $product);
       
       // Подсчёт метрик для каждой недели
       $series = [
           'new' => [],
           'closed' => [],
           'closedCreatedThisWeek' => [],
           'closedCreatedOtherWeek' => [],
           'carryover' => [],
           'carryoverCreatedThisWeek' => [],
           'carryoverCreatedOtherWeek' => []
       ];
       
       $weeksData = [];
       
       foreach ($weeks as $week) {
           $weekMetrics = calculateWeekMetrics(
               $tickets,
               $week['weekStart'],
               $week['weekEnd'],
               $targetStages,
               $closingStages
           );
           
           // Добавление в series
           $series['new'][] = $weekMetrics['newTickets'];
           $series['closed'][] = $weekMetrics['closedTickets'];
           // ... остальные метрики
           
           // Добавление в weeksData
           $weeksData[] = [
               'weekNumber' => $week['weekNumber'],
               ...$weekMetrics
           ];
       }
       
       // Формирование currentWeek из последней недели
       $currentWeekData = $weeksData[count($weeksData) - 1];
       
       // Формирование ответа
       return [
           'success' => true,
           'meta' => [
               'weekNumber' => $currentWeekData['weekNumber'],
               'weekStartUtc' => $weeks[count($weeks) - 1]['weekStartUtc'],
               'weekEndUtc' => $weeks[count($weeks) - 1]['weekEndUtc'],
               'currentWeek' => [
                   'weekNumber' => $currentWeekData['weekNumber'],
                   'weekStartUtc' => $weeks[count($weeks) - 1]['weekStartUtc'],
                   'weekEndUtc' => $weeks[count($weeks) - 1]['weekEndUtc']
               ],
               'weeks' => array_map(function($week) {
                   return [
                       'weekNumber' => $week['weekNumber'],
                       'weekStartUtc' => $week['weekStartUtc'],
                       'weekEndUtc' => $week['weekEndUtc']
                   ];
               }, $weeks)
           ],
           'data' => [
               'currentWeek' => $currentWeekData,
               'series' => $series,
               'weeksData' => $weeksData,
               // ... остальные данные
           ]
       ];
   }
   ```

**Особенности реализации:**

- **Оптимизация запросов:** Один запрос к Bitrix24 за весь период 4 недель вместо 4 отдельных запросов
- **Накопление переходящих тикетов:** Логика `isCarryoverTicket()` учитывает тикеты, созданные до конца недели и находящиеся в рабочих стадиях
- **Нормализация времени:** Все расчёты в UTC, недели по ISO-8601 (понедельник–воскресенье)

### Frontend (Vue.js)

#### Файл: `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureDashboard.vue`

**Ключевые функции для режима 4 недели:**

1. **Инициализация:**
   ```javascript
   const periodMode = ref('weeks'); // Режим по умолчанию
   
   async function loadData() {
     if (periodMode.value !== 'weeks') {
       return; // Загрузка только для недельного режима
     }
     
     const currentWeek = getCurrentWeekBounds();
     const { meta, data } = await fetchAdmissionClosureStats({
       product: '1C',
       periodMode: 'weeks',
       weekStartUtc: currentWeek.weekStartUtc,
       weekEndUtc: currentWeek.weekEndUtc,
       includeTickets: true
     });
     
     chartMeta.value = meta;
     chartData.value = data;
   }
   ```

2. **Обработка изменения режима:**
   ```javascript
   function handlePeriodModeChange(mode) {
     periodMode.value = mode;
     if (mode === 'weeks') {
       loadData(); // Загрузка данных для 4 недель
     } else {
       // Переключение на компонент для 3 месяцев
       // GraphAdmissionClosureMonthsDashboard
     }
   }
   ```

#### Файл: `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`

**Ключевые computed-свойства:**

1. **`lineBarData` — данные для линейного/столбчатого графика:**
   ```javascript
   const lineBarData = computed(() => {
     // Получение метаданных о 4 неделях
     const weeks = props.meta?.weeks || [];
     const labels = weeks.length > 0
       ? weeks.map(week => `Неделя ${week.weekNumber}`)
       : [props.meta?.weekNumber ? `Неделя ${props.meta.weekNumber}` : 'Неделя'];
     
     // Получение серий данных (массивы с 4 элементами)
     const newSeries = Array.isArray(props.data.series?.new) && props.data.series.new.length > 0
       ? props.data.series.new
       : [props.data.newTickets ?? 0];
     
     const closedSeries = Array.isArray(props.data.series?.closed) && props.data.series.closed.length > 0
       ? props.data.series.closed
       : [props.data.closedTickets ?? 0];
     
     // ... остальные серии
     
     return {
       labels,
       datasets: [
         {
           label: 'Новые',
           data: newSeries,
           backgroundColor: chartColors.primary,
           borderColor: chartColors.primary,
           tension: 0.3,
           fill: false
         },
         {
           label: 'Закрытые (все)',
           data: closedSeries,
           backgroundColor: chartColors.success,
           borderColor: chartColors.success,
           tension: 0.3,
           fill: false
         },
         {
           label: 'Закрытые (созданы этой неделей)',
           data: closedCreatedThisWeekSeries,
           backgroundColor: chartColors.successLight,
           borderColor: chartColors.successLight,
           tension: 0.3,
           fill: false,
           borderDash: [5, 5] // Пунктирная линия
         },
         // ... остальные линии
       ]
     };
   });
   ```

2. **`currentWeekData` — данные текущей недели для summary-карточек:**
   ```javascript
   const currentWeekData = computed(() => {
     // Приоритет 1: series[последний] (гарантирует соответствие графику)
     if (props.data?.series) {
       const lastIndex = Math.max(
         ...Object.values(props.data.series)
           .filter(Array.isArray)
           .map(arr => arr.length - 1)
       );
       
       if (lastIndex >= 0) {
         return {
           newTickets: props.data.series.new?.[lastIndex] ?? 0,
           closedTickets: props.data.series.closed?.[lastIndex] ?? 0,
           // ... остальные метрики
         };
       }
     }
     
     // Приоритет 2: currentWeek
     if (props.data?.currentWeek) {
       return props.data.currentWeek;
     }
     
     // Приоритет 3: weeksData[последний]
     if (Array.isArray(props.data?.weeksData) && props.data.weeksData.length > 0) {
       return props.data.weeksData[props.data.weeksData.length - 1];
     }
     
     // Приоритет 4: data (fallback)
     return {
       newTickets: props.data?.newTickets ?? 0,
       closedTickets: props.data?.closedTickets ?? 0,
       // ... остальные метрики
     };
   });
   ```

3. **`chartOptions` — настройки графика:**
   ```javascript
   const chartOptions = computed(() => ({
     responsive: true,
     maintainAspectRatio: false,
     plugins: {
       tooltip: {
         enabled: true,
         callbacks: {
           title: (items) => {
             const index = items[0].dataIndex;
             const week = props.meta?.weeks?.[index];
             if (week) {
               return `Неделя ${week.weekNumber} (${formatDate(week.weekStartUtc)} — ${formatDate(week.weekEndUtc)})`;
             }
             return items[0].label;
           }
         }
       },
       legend: { position: 'top' }
     },
     // НЕТ onClick обработчика (клики на точки отключены)
     scales: chartType.value === 'doughnut'
       ? {}
       : {
           y: {
             beginAtZero: true,
             ticks: { precision: 0 }
           },
           x: {
             ticks: {
               maxRotation: 45,
               minRotation: 45
             }
           }
         }
   }));
   ```

---

## 📡 API контракт

### Endpoint

**URL:** `/api/graph-1c-admission-closure.php`  
**Method:** `POST`  
**Content-Type:** `application/json`

### Запрос (режим 4 недели)

```json
{
  "product": "1C",
  "periodMode": "weeks",
  "weekStartUtc": "2025-12-15T00:00:00Z",
  "weekEndUtc": "2025-12-21T23:59:59Z",
  "useCache": true,
  "forceRefresh": false,
  "includeTickets": false,
  "includeNewTicketsByStages": false,
  "includeCarryoverTickets": true,
  "includeCarryoverTicketsByDuration": false,
  "debug": false
}
```

**Параметры:**
- `product` (string, обязательный) — фильтр по продукту ("1C")
- `periodMode` (string, default: "weeks") — режим периода ("weeks" | "months")
- `weekStartUtc` (string, опциональный) — начало текущей недели в UTC (ISO-8601)
- `weekEndUtc` (string, опциональный) — конец текущей недели в UTC (ISO-8601)
- `useCache` (boolean, default: true) — использовать кеш
- `forceRefresh` (boolean, default: false) — принудительная перезагрузка
- `includeTickets` (boolean, default: false) — включить тикеты в ответ
- `includeNewTicketsByStages` (boolean, default: false) — включить новые тикеты по стадиям
- `includeCarryoverTickets` (boolean, default: true) — включить переходящие тикеты
- `includeCarryoverTicketsByDuration` (boolean, default: false) — включить переходящие тикеты по срокам
- `debug` (boolean, default: false) — режим отладки

**Примечание:** Если `weekStartUtc` и `weekEndUtc` не указаны, бэкенд вычисляет текущую неделю автоматически.

### Ответ (режим 4 недели)

```json
{
  "success": true,
  "meta": {
    "weekNumber": 51,
    "weekStartUtc": "2025-12-15T00:00:00Z",
    "weekEndUtc": "2025-12-21T23:59:59Z",
    "currentWeek": {
      "weekNumber": 51,
      "weekStartUtc": "2025-12-15T00:00:00Z",
      "weekEndUtc": "2025-12-21T23:59:59Z"
    },
    "weeks": [
      {
        "weekNumber": 48,
        "weekStartUtc": "2025-11-24T00:00:00Z",
        "weekEndUtc": "2025-11-30T23:59:59Z"
      },
      {
        "weekNumber": 49,
        "weekStartUtc": "2025-12-01T00:00:00Z",
        "weekEndUtc": "2025-12-07T23:59:59Z"
      },
      {
        "weekNumber": 50,
        "weekStartUtc": "2025-12-08T00:00:00Z",
        "weekEndUtc": "2025-12-14T23:59:59Z"
      },
      {
        "weekNumber": 51,
        "weekStartUtc": "2025-12-15T00:00:00Z",
        "weekEndUtc": "2025-12-21T23:59:59Z"
      }
    ]
  },
  "data": {
    "currentWeek": {
      "weekNumber": 51,
      "newTickets": 12,
      "closedTickets": 8,
      "closedTicketsCreatedThisWeek": 5,
      "closedTicketsCreatedOtherWeek": 3,
      "carryoverTickets": 97,
      "carryoverTicketsCreatedThisWeek": 5,
      "carryoverTicketsCreatedOtherWeek": 92
    },
    "series": {
      "new": [14, 14, 14, 12],
      "closed": [21, 21, 33, 8],
      "closedCreatedThisWeek": [10, 7, 9, 2],
      "closedCreatedOtherWeek": [11, 14, 24, 6],
      "carryover": [63, 67, 78, 97],
      "carryoverCreatedThisWeek": [2, 3, 5, 5],
      "carryoverCreatedOtherWeek": [61, 64, 73, 92]
    },
    "weeksData": [
      {
        "weekNumber": 48,
        "newTickets": 14,
        "closedTickets": 21,
        "closedTicketsCreatedThisWeek": 10,
        "closedTicketsCreatedOtherWeek": 11,
        "carryoverTickets": 63,
        "carryoverTicketsCreatedThisWeek": 2,
        "carryoverTicketsCreatedOtherWeek": 61
      },
      {
        "weekNumber": 49,
        "newTickets": 14,
        "closedTickets": 21,
        "closedTicketsCreatedThisWeek": 7,
        "closedTicketsCreatedOtherWeek": 14,
        "carryoverTickets": 67,
        "carryoverTicketsCreatedThisWeek": 3,
        "carryoverTicketsCreatedOtherWeek": 64
      },
      {
        "weekNumber": 50,
        "newTickets": 14,
        "closedTickets": 33,
        "closedTicketsCreatedThisWeek": 9,
        "closedTicketsCreatedOtherWeek": 24,
        "carryoverTickets": 78,
        "carryoverTicketsCreatedThisWeek": 5,
        "carryoverTicketsCreatedOtherWeek": 73
      },
      {
        "weekNumber": 51,
        "newTickets": 12,
        "closedTickets": 8,
        "closedTicketsCreatedThisWeek": 5,
        "closedTicketsCreatedOtherWeek": 3,
        "carryoverTickets": 97,
        "carryoverTicketsCreatedThisWeek": 5,
        "carryoverTicketsCreatedOtherWeek": 92
      }
    ],
    "stages": [
      {
        "stageId": "DT140_12:SUCCESS",
        "stageName": "Успешное закрытие",
        "count": 5
      },
      {
        "stageId": "DT140_12:FAIL",
        "stageName": "Отклонено",
        "count": 2
      },
      {
        "stageId": "DT140_12:UC_0GBU8Z",
        "stageName": "Закрыли без задачи",
        "count": 1
      }
    ],
    "responsible": [
      {
        "id": 123,
        "name": "Иванов Иван",
        "count": 4,
        "tickets": []
      }
    ]
  }
}
```

**Структура ответа:**
- `meta.weeks[]` — массив с информацией о 4 неделях (от старых к новым)
- `meta.currentWeek` — информация о текущей неделе
- `data.currentWeek` — данные текущей недели (для summary-карточек)
- `data.series.*[]` — массивы с 4 элементами (по одному на неделю, от старых к новым)
- `data.weeksData[]` — детальные данные для каждой недели

---

## 🔄 Логика работы с данными

### Определение 4 недель

**Неделя 1 (текущая):**
- Номер недели: `meta.currentWeek.weekNumber`
- Границы: `meta.currentWeek.weekStartUtc` — `meta.currentWeek.weekEndUtc`
- Данные: `data.currentWeek` или `data.series.*[3]` или `data.weeksData[3]`

**Неделя 2 (1 неделя назад):**
- Номер недели: `meta.weeks[2].weekNumber`
- Границы: `meta.weeks[2].weekStartUtc` — `meta.weeks[2].weekEndUtc`
- Данные: `data.series.*[2]` или `data.weeksData[2]`

**Неделя 3 (2 недели назад):**
- Номер недели: `meta.weeks[1].weekNumber`
- Границы: `meta.weeks[1].weekStartUtc` — `meta.weeks[1].weekEndUtc`
- Данные: `data.series.*[1]` или `data.weeksData[1]`

**Неделя 4 (3 недели назад):**
- Номер недели: `meta.weeks[0].weekNumber`
- Границы: `meta.weeks[0].weekStartUtc` — `meta.weeks[0].weekEndUtc`
- Данные: `data.series.*[0]` или `data.weeksData[0]`

### Накопление переходящих тикетов

**Логика накопления:**
- Неделя 48: все тикеты, созданные до конца недели 48, в рабочих стадиях сейчас
- Неделя 49: все тикеты, созданные до конца недели 49, в рабочих стадиях сейчас
- Неделя 50: все тикеты, созданные до конца недели 50, в рабочих стадиях сейчас
- Неделя 51: все тикеты, созданные до конца недели 51, в рабочих стадиях сейчас

**Пример:**
- Неделя 48: 63 переходящих
- Неделя 49: 67 переходящих (63 + новые, которые не закрылись)
- Неделя 50: 78 переходящих (67 + новые, которые не закрылись)
- Неделя 51: 97 переходящих (78 + новые, которые не закрылись)

### Нормализация данных

**Файл:** `vue-app/src/services/graph-admission-closure/admissionClosureService.js`

**Функция:** `normalizeResponse(raw)`

**Назначение:** Гарантирует наличие всех полей в ответе, даже если бэкенд вернул неполные данные.

**Особенности:**
- Обработка разных форматов ответа (`raw.data`, `raw.result`, `raw`)
- Установка значений по умолчанию для всех полей
- Поддержка обратной совместимости (старые форматы ответа)
- Нормализация `meta.weeks[]` и `data.weeksData[]`

---

## 🎨 Визуализация

### Линейный график

**Структура:**
- **Ось X:** 4 точки (неделя 48, 49, 50, 51)
- **Ось Y:** количество тикетов
- **7 линий:**
  1. Новые (синяя, сплошная)
  2. Закрытые (все) (зелёная, сплошная)
  3. Закрытые (созданы этой неделей) (светло-зелёная, пунктир)
  4. Закрытые (созданы другой неделей) (жёлтая, пунктир)
  5. Переходящие (все) (оранжевая, сплошная)
  6. Переходящие (созданы этой неделей) (светло-оранжевая, пунктир)
  7. Переходящие (созданы другой неделей) (тёмно-оранжевая, пунктир)

**Цветовая схема:**
- Новые: `#007bff` (синий)
- Закрытые (все): `#28a745` (зелёный)
- Закрытые (созданы этой неделей): `#6cbf47` (светло-зелёный)
- Закрытые (созданы другой неделей): `#ffc107` (жёлтый)
- Переходящие (все): `#ff9800` (оранжевый)
- Переходящие (созданы этой неделей): `#ffb74d` (светло-оранжевый)
- Переходящие (созданы другой неделей): `#f57c00` (тёмно-оранжевый)

**Интерактивность:**
- ✅ Наведение на точку → tooltip с данными недели
- ❌ Клик на точку → попап не открывается
- ✅ Клик на summary-карточку → открытие попапа

### Summary-карточки

**Отображают данные текущей недели (неделя 51):**

1. **Новые за неделю:**
   - Значение: `currentWeekData.newTickets`
   - Клик → открытие попапа стадий (StagesModal)

2. **Закрытые за неделю:**
   - Основное значение: `currentWeekData.closedTickets`
   - Разбивка:
     - ✓ `currentWeekData.closedTicketsCreatedThisWeek` этой неделей
     - ↻ `currentWeekData.closedTicketsCreatedOtherWeek` другой неделей
   - Клик → открытие попапа ответственных (ResponsibleModal)

3. **Переходящие:**
   - Основное значение: `currentWeekData.carryoverTickets`
   - Разбивка:
     - ✓ `currentWeekData.carryoverTicketsCreatedThisWeek` этой неделей
     - ↻ `currentWeekData.carryoverTicketsCreatedOtherWeek` другой неделей
   - Клик → открытие попапа переходящих (CarryoverDurationModal)

---

## ⚡ Производительность

### Оптимизации

1. **Один запрос к Bitrix24:**
   - Вместо 4 отдельных запросов выполняется один запрос за весь период (от начала самой старой недели до конца текущей)
   - Уменьшение количества запросов к API в 4 раза

2. **Кеширование:**
   - Используется файловый кеш (`GraphAdmissionClosureCache`)
   - Кеш хранится в `logs/cache/graph-admission-closure/`
   - TTL кеша: 1 час (настраивается)

3. **Ленивая загрузка:**
   - Данные загружаются только при необходимости
   - Попапы загружают данные только при открытии

### Потенциальные узкие места

1. **Большой объём данных:**
   - При большом количестве тикетов за 4 недели запрос к Bitrix24 может быть медленным
   - **Рекомендация:** Использовать пагинацию или увеличить TTL кеша

2. **Обработка на бэкенде:**
   - Цикл по 4 неделям с подсчётом метрик для каждой может быть медленным
   - **Рекомендация:** Оптимизировать логику `calculateWeekMetrics()` или использовать параллельную обработку

3. **Рендеринг графика:**
   - 7 линий на графике могут замедлять рендеринг
   - **Рекомендация:** Использовать виртуализацию или упростить график

---

## 🔗 Зависимости

### Внешние зависимости

1. **Bitrix24 REST API:**
   - Метод: `crm.item.list`
   - Документация: https://context7.com/bitrix24/rest/crm.item.list
   - Используется для получения тикетов

2. **Chart.js (через vue-chartjs):**
   - Компоненты: `Line`, `Bar`, `Doughnut`
   - Используется для отображения графиков

3. **Vue.js 3:**
   - Composition API
   - Reactivity API

### Внутренние зависимости

1. **Модуль 1 (Дашборд сектора 1С):**
   - Навигация через кнопку "График приёма и закрытий"
   - Использует те же данные о тикетах

2. **Модуль 2 (График состояния):**
   - Использует аналогичную структуру компонентов
   - Общие фильтры и доступы

3. **Сервисы:**
   - `admissionClosureService.js` — сервис для работы с API
   - `ticket-utils.js` — утилиты для работы с тикетами

---

## 🐛 Известные проблемы и ограничения

### 1. История изменений стадий

**Проблема:** Логика переходящих тикетов использует текущее состояние тикетов, так как история изменений стадий недоступна.

**Влияние:** Если тикет был закрыт после недели, он всё равно учитывается как переходящий для этой недели (если сейчас находится в рабочих стадиях).

**Решение:** Использовать историю изменений стадий из Bitrix24 (если доступна) или кешировать состояние тикетов на момент каждой недели.

### 2. Производительность при большом объёме данных

**Проблема:** При большом количестве тикетов за 4 недели запрос к Bitrix24 может быть медленным.

**Решение:**
- Увеличить TTL кеша
- Использовать пагинацию
- Оптимизировать запросы (выбирать только нужные поля)

### 3. Накопление переходящих тикетов

**Проблема:** Логика накопления может быть неочевидной для пользователей.

**Решение:** Добавить пояснение в tooltip или информационное модальное окно.

---

## 📈 Рекомендации по улучшению

### 1. Оптимизация производительности

- **Параллельная обработка недель:** Использовать `Promise.all()` для параллельного подсчёта метрик для каждой недели
- **Инкрементальное обновление:** Обновлять только изменённые недели вместо полной перезагрузки
- **Виртуализация графика:** Использовать виртуализацию для больших объёмов данных

### 2. Улучшение UX

- **Анимации:** Добавить плавные анимации при переключении между режимами
- **Индикатор загрузки:** Показывать прогресс загрузки данных
- **Обработка ошибок:** Улучшить сообщения об ошибках для пользователей

### 3. Расширение функциональности

- **Экспорт данных:** Добавить возможность экспорта данных в CSV/Excel
- **Сравнение периодов:** Добавить возможность сравнения с предыдущими периодами
- **Фильтры:** Расширить фильтры (по стадиям, по ответственным)

### 4. Документация

- **API документация:** Создать Swagger/OpenAPI документацию
- **Руководство пользователя:** Расширить руководство пользователя с примерами
- **Техническая документация:** Обновить техническую документацию с диаграммами

---

## ✅ Критерии качества

### Функциональность

- [x] API возвращает данные за 4 недели
- [x] Линейный график отображает 4 точки на оси X
- [x] Отображаются все 7 линий на графике
- [x] Клик на точку графика не открывает попап
- [x] Попапы открываются только через summary-карточки
- [x] Summary-карточки показывают данные текущей недели
- [x] Tooltip показывает информацию о неделе
- [x] Данные упорядочены от старых к новым (слева направо)

### Производительность

- [x] Запрос к API выполняется за < 5 секунд
- [x] График рендерится за < 2 секунд
- [x] Кеш работает корректно
- [x] Нет утечек памяти

### Надёжность

- [x] Обработка ошибок работает корректно
- [x] Пустые данные обрабатываются корректно
- [x] Граничные случаи (переход года, високосный год) обрабатываются корректно

---

## 📚 Связанные документы

- [TASK-041: График приёма и закрытий сектора 1С](./TASK-041-graph-1c-admission-closure.md)
- [TASK-048: Линейный график на 4 недели без попапов](./TASK-048-line-chart-4-weeks-no-popups.md)
- [TASK-053: Режим выбора периода 3 месяца](./TASK-053-period-selection-3-months-mode.md)
- [Гайдлайн модуля](../GUIDES/module-graph-admission-closure-guide.md)
- [README модуля](../MODULES/module-03-graph-admission-closure/README.md)

---

## 📝 История правок

- **2025-12-23 14:30 (UTC+3, Брест):** Создан детальный анализ модуля в режиме 4 недели
  - Документирована архитектура модуля
  - Описаны технические детали реализации
  - Проанализированы производительность и зависимости
  - Выявлены известные проблемы и ограничения
  - Предоставлены рекомендации по улучшению

---

**Автор:** Технический писатель  
**Версия документа:** 1.0


