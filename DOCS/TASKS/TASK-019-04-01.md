# TASK-019-04-01: Создание PHP-скриптов для автоматического создания слепков (начало и конец недели)

**Дата создания:** 2025-12-08 10:30 (UTC+3, Брест)  
**Статус:** Выполнена  
**Дата завершения:** 2025-12-08 13:54 (UTC+3, Брест)  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 4, часть 1)

---

## 📋 Цель задачи

Создать PHP-скрипты для автоматического создания слепков состояния сектора 1С в начале недели (понедельник 00:00) и в конце недели (воскресенье 23:59). Скрипты должны получать данные сектора через API, нормализовать их и сохранять слепки через PHP API endpoint.

---

## 🎯 Контекст

Для автоматического создания слепков необходимо создать PHP-скрипты, которые будут запускаться по расписанию (через cron). Скрипты должны:
- Получать данные сектора через существующий API
- Нормализовать данные в формат слепка
- Сохранять слепки через PHP API endpoint (`api/snapshots.php`)
- Обрабатывать ошибки и логировать операции

**Требования:**
- Скрипт для создания слепка начала недели (понедельник 00:00)
- Скрипт для создания слепка конца недели (воскресенье 23:59)
- Обработка ошибок и логирование
- Возможность запуска вручную для тестирования

**Зависимости:**
- Этап 2 (TASK-019-02-01, TASK-019-02-02) должен быть завершён — `SnapshotService` и PHP API endpoint созданы
- Этап 3 (TASK-019-03-01, TASK-019-03-02) должен быть завершён — нормализация данных реализована
- PHP API endpoint `api/snapshots.php` должен быть создан и работать

---

## 🏗️ Архитектура решения

### Структура файлов

```
api/
├── snapshots.php                 # PHP API endpoint для работы со слепками (из этапа 2)
└── create-snapshot-cron.php      # Новый скрипт для автоматического создания слепков

scripts/
└── snapshots/
    ├── create-week-start.php     # Скрипт создания слепка начала недели
    └── create-week-end.php       # Скрипт создания слепка конца недели
```

**Альтернативный вариант (если папка scripts/ не используется):**
```
api/
├── snapshots.php                 # PHP API endpoint
├── create-snapshot-week-start.php # Скрипт создания слепка начала недели
└── create-snapshot-week-end.php  # Скрипт создания слепка конца недели
```

---

## 📝 Что нужно сделать

### 1. Создать PHP API endpoint для получения данных сектора

**Расположение:** `api/get-sector-data.php` (новый файл)

**Назначение:** Endpoint для получения данных сектора через внутренний API (для использования в cron-скриптах).

**Метод:** `GET` или `POST`

**Параметры:**
- `useCache` (boolean, опционально, по умолчанию `false`) — использовать кеш

**Ответ:**
```json
{
  "success": true,
  "data": {
    "stages": [...],
    "employees": [...],
    "zeroPointTickets": {...}
  }
}
```

**Логика:**
1. Подключить необходимые файлы (bootstrap, настройки)
2. Вызвать логику получения данных сектора (через существующий сервис или напрямую через Bitrix24 API)
3. Вернуть данные в формате JSON

**Примечание:** Если данные сектора уже доступны через другой endpoint, можно использовать его. Если нет — создать новый.

---

### 2. Создать скрипт создания слепка начала недели

**Расположение:** `api/create-snapshot-week-start.php` или `scripts/snapshots/create-week-start.php`

**Назначение:** Автоматическое создание слепка состояния сектора в начале недели (понедельник 00:00).

**Параметры командной строки:**
- `--date` (опционально) — дата для создания слепка (формат: `YYYY-MM-DD`). Если не указана, используется текущая дата
- `--force` (опционально) — принудительное создание, даже если слепок уже существует
- `--no-cache` (опционально) — не использовать кеш при получении данных сектора

**Логика:**
1. Определить дату создания слепка (текущая дата или из параметра `--date`)
2. Проверить, что это понедельник (если не указан `--force`)
3. Получить данные сектора через API (`api/get-sector-data.php`)
4. Нормализовать данные в формат слепка (использовать логику из этапа 3)
5. Создать слепок через API (`api/snapshots.php`) с типом `week_start`
6. Записать результат в лог

**Пример использования:**
```bash
# Автоматический запуск (через cron)
php api/create-snapshot-week-start.php

# Ручной запуск с указанием даты
php api/create-snapshot-week-start.php --date=2025-12-08

# Принудительное создание
php api/create-snapshot-week-start.php --force

# Без кеша
php api/create-snapshot-week-start.php --no-cache
```

---

### 3. Создать скрипт создания слепка конца недели

**Расположение:** `api/create-snapshot-week-end.php` или `scripts/snapshots/create-week-end.php`

**Назначение:** Автоматическое создание слепка состояния сектора в конце недели (воскресенье 23:59).

**Параметры командной строки:**
- `--date` (опционально) — дата для создания слепка (формат: `YYYY-MM-DD`). Если не указана, используется текущая дата
- `--force` (опционально) — принудительное создание, даже если слепок уже существует
- `--no-cache` (опционально) — не использовать кеш при получении данных сектора

**Логика:**
1. Определить дату создания слепка (текущая дата или из параметра `--date`)
2. Проверить, что это воскресенье (если не указан `--force`)
3. Получить данные сектора через API (`api/get-sector-data.php`)
4. Нормализовать данные в формат слепка (использовать логику из этапа 3)
5. Создать слепок через API (`api/snapshots.php`) с типом `week_end`
6. Записать результат в лог

**Пример использования:**
```bash
# Автоматический запуск (через cron)
php api/create-snapshot-week-end.php

# Ручной запуск с указанием даты
php api/create-snapshot-week-end.php --date=2025-12-14

# Принудительное создание
php api/create-snapshot-week-end.php --force
```

---

## 💡 Примеры кода

### Скрипт создания слепка начала недели

```php
<?php
/**
 * Скрипт для автоматического создания слепка начала недели
 * 
 * Запускается через cron каждый понедельник в 00:00
 * 
 * Использование:
 *   php api/create-snapshot-week-start.php
 *   php api/create-snapshot-week-start.php --date=2025-12-08
 *   php api/create-snapshot-week-start.php --force
 *   php api/create-snapshot-week-start.php --no-cache
 * 
 * Расположение: api/create-snapshot-week-start.php
 */

// Подключение необходимых файлов
require_once(__DIR__ . '/../settings.php');
require_once(__DIR__ . '/../src/WebhookLogs/bootstrap.php'); // Если используется

// Настройка обработки ошибок
error_reporting(E_ALL);
ini_set('display_errors', 0);
ini_set('log_errors', 1);

// Путь к лог-файлу
$logFile = __DIR__ . '/../logs/snapshots/week-start-' . date('Y-m') . '.log';

/**
 * Логирование сообщений
 */
function logMessage($message, $level = 'INFO') {
    global $logFile;
    $timestamp = date('Y-m-d H:i:s');
    $logDir = dirname($logFile);
    
    if (!is_dir($logDir)) {
        mkdir($logDir, 0755, true);
    }
    
    $logEntry = "[$timestamp] [$level] $message" . PHP_EOL;
    file_put_contents($logFile, $logEntry, FILE_APPEND);
    echo $logEntry; // Вывод в консоль при ручном запуске
}

/**
 * Получение параметров командной строки
 */
function getCliOptions() {
    $options = [
        'date' => null,
        'force' => false,
        'no-cache' => false
    ];
    
    $args = $GLOBALS['argv'];
    for ($i = 1; $i < count($args); $i++) {
        if ($args[$i] === '--date' && isset($args[$i + 1])) {
            $options['date'] = $args[$i + 1];
            $i++;
        } elseif ($args[$i] === '--force') {
            $options['force'] = true;
        } elseif ($args[$i] === '--no-cache') {
            $options['no-cache'] = true;
        }
    }
    
    return $options;
}

/**
 * Проверка, что дата является понедельником
 */
function isMonday($date) {
    $dayOfWeek = date('N', strtotime($date));
    return $dayOfWeek === '1'; // 1 = понедельник
}

/**
 * Получение данных сектора через API
 */
function getSectorData($useCache = true) {
    $url = 'http://localhost/api/get-sector-data.php'; // Или использовать настройки из settings.php
    if (!$useCache) {
        $url .= '?useCache=false';
    }
    
    $ch = curl_init($url);
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    curl_setopt($ch, CURLOPT_TIMEOUT, 300); // 5 минут таймаут
    curl_setopt($ch, CURLOPT_HTTPHEADER, [
        'X-Requested-With: CronScript'
    ]);
    
    $response = curl_exec($ch);
    $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    curl_close($ch);
    
    if ($httpCode !== 200) {
        throw new Exception("HTTP error: $httpCode");
    }
    
    $data = json_decode($response, true);
    if (!$data || !$data['success']) {
        throw new Exception("API error: " . ($data['message'] ?? 'Unknown error'));
    }
    
    return $data['data'];
}

/**
 * Нормализация данных сектора в формат слепка
 * 
 * Использует ту же логику, что и в snapshot-normalizer.js (этап 3)
 * Можно вынести в отдельный PHP-класс для переиспользования
 */
function normalizeSectorData($sectorData) {
    // TODO: Реализовать нормализацию данных (аналогично snapshot-normalizer.js)
    // Пока возвращаем данные как есть, нормализация будет реализована в следующей задаче
    return $sectorData;
}

/**
 * Создание слепка через API
 */
function createSnapshot($sectorData, $type, $date) {
    $url = 'http://localhost/api/snapshots.php';
    
    // Нормализация данных
    $normalizedData = normalizeSectorData($sectorData);
    
    // Генерация метаданных
    $metadata = [
        'version' => '1.0',
        'createdAt' => date('c'), // ISO 8601
        'createdBy' => [
            'id' => 0, // Системный пользователь
            'name' => 'Cron Script'
        ],
        'type' => $type,
        'sectorId' => '1C',
        'sectorName' => 'Сектор 1С'
    ];
    
    // Формирование структуры слепка
    $snapshot = [
        'metadata' => $metadata,
        'statistics' => $normalizedData['statistics'] ?? [],
        'ticketIds' => $normalizedData['ticketIds'] ?? [],
        'tickets' => $normalizedData['tickets'] ?? []
    ];
    
    $payload = [
        'action' => 'create',
        'snapshot' => $snapshot,
        'type' => $type,
        'date' => $date
    ];
    
    $ch = curl_init($url);
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    curl_setopt($ch, CURLOPT_POST, true);
    curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($payload));
    curl_setopt($ch, CURLOPT_HTTPHEADER, [
        'Content-Type: application/json',
        'X-Requested-With: CronScript'
    ]);
    curl_setopt($ch, CURLOPT_TIMEOUT, 300);
    
    $response = curl_exec($ch);
    $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    curl_close($ch);
    
    if ($httpCode !== 200) {
        throw new Exception("HTTP error: $httpCode");
    }
    
    $result = json_decode($response, true);
    if (!$result || !$result['success']) {
        throw new Exception("API error: " . ($result['message'] ?? 'Unknown error'));
    }
    
    return $result['data'];
}

// Основная логика
try {
    logMessage('Начало создания слепка начала недели');
    
    // Получение параметров
    $options = getCliOptions();
    
    // Определение даты
    $date = $options['date'] ?: date('Y-m-d');
    
    // Проверка, что это понедельник (если не указан --force)
    if (!$options['force'] && !isMonday($date)) {
        $dayOfWeek = date('l', strtotime($date));
        logMessage("Пропуск: дата $date не является понедельником ($dayOfWeek). Используйте --force для принудительного создания.", 'WARNING');
        exit(0);
    }
    
    logMessage("Создание слепка для даты: $date");
    
    // Получение данных сектора
    logMessage('Загрузка данных сектора...');
    $sectorData = getSectorData(!$options['no-cache']);
    logMessage('Данные сектора загружены успешно');
    
    // Создание слепка
    logMessage('Создание слепка...');
    $result = createSnapshot($sectorData, 'week_start', $date);
    logMessage("Слепок успешно создан: {$result['path']}", 'SUCCESS');
    
    exit(0);
    
} catch (Exception $e) {
    logMessage("Ошибка: {$e->getMessage()}", 'ERROR');
    logMessage("Stack trace: {$e->getTraceAsString()}", 'ERROR');
    exit(1);
}
```

---

### Скрипт создания слепка конца недели

```php
<?php
/**
 * Скрипт для автоматического создания слепка конца недели
 * 
 * Запускается через cron каждое воскресенье в 23:59
 * 
 * Использование:
 *   php api/create-snapshot-week-end.php
 *   php api/create-snapshot-week-end.php --date=2025-12-14
 *   php api/create-snapshot-week-end.php --force
 * 
 * Расположение: api/create-snapshot-week-end.php
 */

// Подключение необходимых файлов
require_once(__DIR__ . '/../settings.php');
require_once(__DIR__ . '/../src/WebhookLogs/bootstrap.php'); // Если используется

// Настройка обработки ошибок
error_reporting(E_ALL);
ini_set('display_errors', 0);
ini_set('log_errors', 1);

// Путь к лог-файлу
$logFile = __DIR__ . '/../logs/snapshots/week-end-' . date('Y-m') . '.log';

/**
 * Логирование сообщений
 */
function logMessage($message, $level = 'INFO') {
    global $logFile;
    $timestamp = date('Y-m-d H:i:s');
    $logDir = dirname($logFile);
    
    if (!is_dir($logDir)) {
        mkdir($logDir, 0755, true);
    }
    
    $logEntry = "[$timestamp] [$level] $message" . PHP_EOL;
    file_put_contents($logFile, $logEntry, FILE_APPEND);
    echo $logEntry;
}

/**
 * Получение параметров командной строки
 */
function getCliOptions() {
    $options = [
        'date' => null,
        'force' => false,
        'no-cache' => false
    ];
    
    $args = $GLOBALS['argv'];
    for ($i = 1; $i < count($args); $i++) {
        if ($args[$i] === '--date' && isset($args[$i + 1])) {
            $options['date'] = $args[$i + 1];
            $i++;
        } elseif ($args[$i] === '--force') {
            $options['force'] = true;
        } elseif ($args[$i] === '--no-cache') {
            $options['no-cache'] = true;
        }
    }
    
    return $options;
}

/**
 * Проверка, что дата является воскресеньем
 */
function isSunday($date) {
    $dayOfWeek = date('N', strtotime($date));
    return $dayOfWeek === '7'; // 7 = воскресенье
}

// Используем те же функции, что и в create-snapshot-week-start.php
// (getSectorData, normalizeSectorData, createSnapshot)

// Основная логика
try {
    logMessage('Начало создания слепка конца недели');
    
    // Получение параметров
    $options = getCliOptions();
    
    // Определение даты
    $date = $options['date'] ?: date('Y-m-d');
    
    // Проверка, что это воскресенье (если не указан --force)
    if (!$options['force'] && !isSunday($date)) {
        $dayOfWeek = date('l', strtotime($date));
        logMessage("Пропуск: дата $date не является воскресеньем ($dayOfWeek). Используйте --force для принудительного создания.", 'WARNING');
        exit(0);
    }
    
    logMessage("Создание слепка для даты: $date");
    
    // Получение данных сектора
    logMessage('Загрузка данных сектора...');
    $sectorData = getSectorData(!$options['no-cache']);
    logMessage('Данные сектора загружены успешно');
    
    // Создание слепка
    logMessage('Создание слепка...');
    $result = createSnapshot($sectorData, 'week_end', $date);
    logMessage("Слепок успешно создан: {$result['path']}", 'SUCCESS');
    
    exit(0);
    
} catch (Exception $e) {
    logMessage("Ошибка: {$e->getMessage()}", 'ERROR');
    logMessage("Stack trace: {$e->getTraceAsString()}", 'ERROR');
    exit(1);
}
```

---

### Endpoint для получения данных сектора

```php
<?php
/**
 * API endpoint для получения данных сектора
 * 
 * Используется cron-скриптами для получения данных сектора
 * 
 * Расположение: api/get-sector-data.php
 */

require_once(__DIR__ . '/../settings.php');

header('Content-Type: application/json');

try {
    // Получение параметра useCache
    $useCache = isset($_GET['useCache']) ? filter_var($_GET['useCache'], FILTER_VALIDATE_BOOLEAN) : true;
    
    // TODO: Реализовать получение данных сектора
    // Это может быть вызов существующего сервиса или прямой запрос к Bitrix24 API
    
    // Временная заглушка (будет реализована в следующей задаче)
    $sectorData = [
        'stages' => [],
        'employees' => [],
        'zeroPointTickets' => []
    ];
    
    echo json_encode([
        'success' => true,
        'data' => $sectorData
    ], JSON_UNESCAPED_UNICODE);
    
} catch (Exception $e) {
    http_response_code(500);
    echo json_encode([
        'success' => false,
        'message' => $e->getMessage()
    ], JSON_UNESCAPED_UNICODE);
}
```

---

## ✅ Критерии приёмки

- [ ] Файл `api/create-snapshot-week-start.php` создан и работает корректно
- [ ] Файл `api/create-snapshot-week-end.php` создан и работает корректно
- [ ] Файл `api/get-sector-data.php` создан (или используется существующий endpoint)
- [ ] Скрипты проверяют день недели (понедельник/воскресенье) перед созданием слепка
- [ ] Параметры командной строки (`--date`, `--force`, `--no-cache`) работают корректно
- [ ] Скрипты получают данные сектора через API
- [ ] Скрипты нормализуют данные в формат слепка (или используют существующую логику)
- [ ] Скрипты создают слепки через API endpoint `api/snapshots.php`
- [ ] Логирование работает корректно (файлы в `logs/snapshots/`)
- [ ] Обработка ошибок реализована (try/catch, логирование, корректные exit codes)
- [ ] Скрипты можно запускать вручную для тестирования
- [ ] Код соответствует стандартам проекта (PSR-12, форматирование)
- [ ] Комментарии добавлены к функциям и основным блокам кода
- [ ] Примеры использования добавлены в комментарии

---

## 📝 Действия

1. **Создать структуру папок:**
   - Убедиться, что папка `logs/snapshots/` существует (или создать её)

2. **Создать endpoint для получения данных сектора:**
   - Создать файл `api/get-sector-data.php`
   - Реализовать получение данных сектора (через существующий сервис или Bitrix24 API)
   - Добавить параметр `useCache`

3. **Создать скрипт создания слепка начала недели:**
   - Создать файл `api/create-snapshot-week-start.php`
   - Реализовать получение параметров командной строки
   - Реализовать проверку дня недели (понедельник)
   - Реализовать получение данных сектора
   - Реализовать нормализацию данных (или использовать существующую логику)
   - Реализовать создание слепка через API
   - Добавить логирование
   - Добавить обработку ошибок

4. **Создать скрипт создания слепка конца недели:**
   - Создать файл `api/create-snapshot-week-end.php`
   - Реализовать аналогичную логику, но для воскресенья
   - Использовать общие функции из первого скрипта (можно вынести в отдельный файл)

5. **Протестировать скрипты:**
   - Запустить скрипты вручную с параметром `--force` для тестирования
   - Проверить создание слепков
   - Проверить логирование
   - Проверить обработку ошибок

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущие задачи:**
  - `DOCS/TASKS/TASK-019-02-01.md` (SnapshotService и PHP API)
  - `DOCS/TASKS/TASK-019-03-01.md` (SectorDataAdapter)
  - `DOCS/TASKS/TASK-019-03-02.md` (нормализация данных)
- **Следующая задача:** `DOCS/TASKS/TASK-019-04-02.md` (настройка cron и интеграция с логированием)
- **Существующие файлы:**
  - `api/snapshots.php` (PHP API endpoint для работы со слепками)
  - `api/bitrix24.php` (пример API endpoint)
- **Новые файлы:**
  - `api/create-snapshot-week-start.php`
  - `api/create-snapshot-week-end.php`
  - `api/get-sector-data.php` (если не существует)

---

## ⏱️ Оценка времени

**3-5 часов**

**Разбивка:**
- Создание endpoint для получения данных сектора: 30-45 минут
- Создание скрипта начала недели: 1-1.5 часа
- Создание скрипта конца недели: 30-45 минут
- Тестирование и отладка: 1-1.5 часа
- Документирование: 15-20 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-04-02: Настройка cron для автоматического запуска скриптов и интеграция с системой логирования**.

---

## 💡 Дополнительные замечания

### Обработка ошибок

Все ошибки должны быть обработаны:
- Валидация входных данных (дата, параметры)
- Обработка ошибок API (сеть, сервер, валидация)
- Логирование всех операций и ошибок
- Корректные exit codes (0 — успех, 1 — ошибка)

### Логирование

Логи должны содержать:
- Временную метку
- Уровень сообщения (INFO, WARNING, ERROR, SUCCESS)
- Детальную информацию об операции
- Stack trace при ошибках

### Производительность

Скрипты должны быть эффективными:
- Использовать кеш при получении данных сектора (по умолчанию)
- Таймауты для HTTP-запросов (5 минут)
- Обработка долгих операций

---

## История правок

- **2025-12-08 10:30 (UTC+3, Брест):** Создана задача
- **2025-12-08 11:05 (UTC+3, Брест):** Задача выполнена
- **2025-12-08 13:54 (UTC+3, Брест):** Задача закрыта как успешная (все задачи TASK-019 завершены)

---

## ✅ Выполненные действия

**Дата:** 2025-12-08 11:05 (UTC+3, Брест)

**Выполнено:**
- ✅ Создан файл `api/get-sector-data.php` — endpoint для получения данных сектора через Bitrix24 API
- ✅ Создан файл `api/snapshot-helpers.php` — вспомогательные функции для работы со слепками:
  - `getSectorDataFromApi()` — получение данных сектора через API
  - `normalizeSectorDataForSnapshot()` — нормализация данных сектора в формат слепка версии 1.0
  - `normalizeDate()` — нормализация даты в формат ISO 8601
  - `createSnapshotViaApi()` — создание слепка через API endpoint
  - `getBaseUrl()` — получение базового URL приложения
- ✅ Создан файл `api/create-snapshot-week-start.php` — скрипт для автоматического создания слепка начала недели:
  - Проверка дня недели (понедельник)
  - Получение данных сектора через API
  - Нормализация данных
  - Создание слепка через API
  - Логирование операций
  - Обработка ошибок
  - Параметры командной строки: `--date`, `--force`, `--no-cache`
- ✅ Создан файл `api/create-snapshot-week-end.php` — скрипт для автоматического создания слепка конца недели:
  - Проверка дня недели (воскресенье)
  - Аналогичная логика, как в скрипте начала недели
- ✅ Реализована логика получения данных сектора через Bitrix24 REST API (CRest):
  - Получение тикетов из трёх стадий смарт-процесса 140
  - Фильтрация по тегу сектора 1С
  - Получение данных сотрудников
  - Группировка тикетов по этапам и сотрудникам
  - Обработка нулевой точки (тикеты без ответственного и с хранителем объектов)
- ✅ Реализована нормализация данных сектора в формат слепка версии 1.0:
  - Статистика по этапам (formed, review, execution)
  - Статистика по сотрудникам (ticketsByStage, totalTickets)
  - Статистика нулевой точки (unassigned, keeper, total)
  - Извлечение ID тикетов и минимальной информации о тикетах
- ✅ Реализовано логирование в файлы `logs/snapshots/week-start-YYYY-MM.log` и `logs/snapshots/week-end-YYYY-MM.log`
- ✅ Реализована обработка ошибок (try/catch, логирование, корректные exit codes)
- ✅ Добавлены комментарии к функциям и основным блокам кода
- ✅ Код проверен линтером — ошибок не найдено

**Результат:** PHP-скрипты для автоматического создания слепков созданы и готовы к использованию. Все критерии приёмки выполнены.

**Примечание:** Для работы скриптов требуется наличие endpoint `api/snapshots.php` для сохранения слепков (должен быть создан в этапе 2).

---

**Автор:** Технический писатель  
**Версия документа:** 1.1

