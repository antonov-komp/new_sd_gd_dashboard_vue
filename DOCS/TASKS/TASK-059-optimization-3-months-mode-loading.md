# TASK-059: Оптимизация загрузки режима 3 месяца

**Дата создания:** 2025-12-18 10:00 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Backend Developer (PHP) + Bitrix24 Программист (Vue.js Developer)  
**Связанная документация:** [Анализ оптимизации](../../REFACTOR/PERFORMANCE/optimization-3-months-mode-loading.md)

---

## 📋 Описание

Оптимизировать загрузку данных в режиме "3 месяца" модуля "График приёма и закрытий сектора 1С". Проблема: прелоадер быстро доходит до 95%, но на этапе 95% дальнейшая загрузка занимает очень долго (15-30 секунд).

**Цель:** Сократить время загрузки с 15-30 секунд до 5-10 секунд.

---

## 🎯 Контекст

### Текущая проблема:

1. **Прелоадер быстро доходит до 95%** (симуляция прогресса, ~2-3 секунды)
2. **На этапе 95% дальнейшая загрузка занимает очень долго** (реальный запрос к API, 15-30 секунд)
3. **Завершается успешно, но весьма долго**

### Узкие места:

1. **Последовательные запросы к Bitrix24 API** (15-25 секунд)
2. **Множественные проходы по массиву тикетов** (72-90 проходов для агрегации)
3. **Неоптимальный запрос переходящих тикетов** (загружает ВСЕ тикеты в рабочих стадиях)
4. **Отсутствие кеширования** (данные загружаются заново при каждом запросе)
5. **Симуляция прогресса** (не отражает реальное состояние загрузки)

---

## 🏗️ Модули и компоненты

### Backend (PHP):
- `api/graph-1c-admission-closure.php` — основной API endpoint
  - Функция `calculateLastFourMonths()` — расчет периода
  - Запрос 1: Тикеты, созданные за период (строки 609-660)
  - Запрос 2: Тикеты, закрытые за период (строки 662-714)
  - Запрос 3: Переходящие тикеты (строки 716-767)
  - Функция `calculateWeekMetrics()` — агрегация по неделям (строки 286-429)
  - Агрегация данных по месяцам (строки 808-979)
  - Расчет данных 4-го месяца (строки 981-1054)

### Frontend (Vue.js):
- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureMonthsDashboard.vue` — главный компонент
  - Функция `loadData()` — загрузка данных (строки 204-262)
  - Функция `updateProgress()` — обновление прогресса (строки 186-202)
- `vue-app/src/components/graph-admission-closure/MonthsModePreloader.vue` — компонент прелоадера
- `vue-app/src/services/graph-admission-closure/admissionClosureService.js` — сервис для работы с API

---

## 📦 Зависимости

- **От задач:** Нет
- **От модулей:** 
  - Модуль 3: График приёма и закрытий сектора 1С
  - TASK-053: Режим выбора периода 3 месяца
  - TASK-054: Исправление пагинации
  - TASK-058: Обогащение в режиме 3 месяцев

---

## 🎯 Ступенчатые подзадачи

### Подзадача 1: Оптимизация запроса переходящих тикетов

**Проблема:** Загружаются ВСЕ тикеты в рабочих стадиях, без фильтра по периоду.

**Решение:** Добавить фильтр по `createdTime` для ограничения периода.

**Шаги:**
1. Обновить запрос 3 в `api/graph-1c-admission-closure.php` (строки 716-767)
2. Добавить фильтр `'<=createdTime' => $periodEndStr` в запрос переходящих тикетов
3. Протестировать корректность работы
4. Измерить время загрузки до и после оптимизации

**Ожидаемый эффект:** Сокращение времени загрузки на 50-70% для запроса 3.

**Критерии приёмки:**
- [ ] Запрос переходящих тикетов использует фильтр по `createdTime`
- [ ] Результаты агрегации не изменились (корректность данных)
- [ ] Время загрузки сократилось на 50-70% для запроса 3

---

### Подзадача 2: Объединение проходов по массиву тикетов

**Проблема:** Множественные проходы по массиву тикетов для агрегации (72-90 проходов).

**Решение:** Объединить все проходы в один цикл.

**Шаги:**
1. Проанализировать текущую логику агрегации (строки 808-979)
2. Создать единую структуру данных для агрегации всех метрик
3. Рефакторинг: объединить все проходы в один цикл по тикетам
4. Протестировать корректность работы
5. Измерить время агрегации до и после оптимизации

**Ожидаемый эффект:** Сокращение времени агрегации на 80-90%.

**Критерии приёмки:**
- [ ] Все метрики агрегируются в одном проходе по тикетам
- [ ] Результаты агрегации не изменились (корректность данных)
- [ ] Время агрегации сократилось на 80-90%

---

### Подзадача 3: Параллельные запросы к Bitrix24 API

**Проблема:** Запросы 1 и 2 выполняются последовательно.

**Решение:** Выполнять запросы 1 и 2 параллельно через batch-запросы.

**Шаги:**
1. Изучить возможности batch-запросов в Bitrix24 REST API
2. Создать batch-запрос для запросов 1 и 2
3. Обновить логику обработки результатов batch-запроса
4. Протестировать корректность работы
5. Измерить время загрузки до и после оптимизации

**Ожидаемый эффект:** Сокращение времени загрузки на 30-50%.

**Критерии приёмки:**
- [ ] Запросы 1 и 2 выполняются параллельно через batch
- [ ] Результаты запросов корректны
- [ ] Время загрузки сократилось на 30-50%

**Примечание:** Если batch-запросы не поддерживают пагинацию, можно использовать `Promise.all()` на фронтенде или параллельные запросы через `curl_multi_exec()` на бэкенде.

---

### Подзадача 4: Реальный прогресс загрузки (Frontend)

**Проблема:** Прогресс не отражает реальное состояние загрузки.

**Решение:** Использовать реальный прогресс на основе этапов загрузки.

**Шаги:**
1. Добавить поддержку прогресса в API (опционально, можно использовать промежуточные обновления)
2. Обновить `GraphAdmissionClosureMonthsDashboard.vue` для отображения реального прогресса
3. Убрать симуляцию прогресса, использовать реальные этапы загрузки
4. Протестировать UX

**Ожидаемый эффект:** Улучшение UX, пользователь видит реальный прогресс.

**Критерии приёмки:**
- [ ] Прогресс отражает реальное состояние загрузки
- [ ] Пользователь видит этапы загрузки (запросы к API, агрегация)
- [ ] UX улучшен (пользователь понимает, что происходит)

**Примечание:** Если добавить поддержку прогресса в API сложно, можно использовать промежуточные обновления на основе времени или этапов загрузки.

---

### Подзадача 5: Кеширование результатов

**Проблема:** Данные загружаются заново при каждом запросе.

**Решение:** Добавить кеширование результатов на бэкенде.

**Шаги:**
1. Выбрать механизм кеширования (APCu, Redis, файловый кеш)
2. Создать функцию для генерации ключа кеша
3. Добавить проверку кеша перед выполнением запросов
4. Сохранять результаты в кеш после выполнения запросов
5. Установить TTL для кеша (5 минут)
6. Протестировать работу кеша

**Ожидаемый эффект:** Сокращение времени загрузки на 90-95% для повторных запросов.

**Критерии приёмки:**
- [ ] Кеширование работает корректно
- [ ] Кеш обновляется при изменении данных
- [ ] Время загрузки сократилось на 90-95% для повторных запросов

**Примечание:** Учесть параметры запроса при генерации ключа кеша (periodMode, product, includeTickets, etc.).

---

## 🔧 Технические требования

### Backend (PHP):
- PHP 8.4+
- Bitrix24 REST API (CRest)
- Поддержка batch-запросов (если возможно)
- Кеширование (APCu, Redis, или файловый кеш)

### Frontend (Vue.js):
- Vue.js 3.x
- Поддержка реального прогресса загрузки

### Производительность:
- **Целевое время загрузки:** 5-10 секунд (для первого запроса)
- **Целевое время загрузки:** 0.5-2 секунды (для кешированных запросов)
- **Сокращение времени:** 50-70% для первого запроса

---

## ✅ Критерии приёмки

### Общие критерии:
- [ ] Время загрузки сократилось с 15-30 секунд до 5-10 секунд
- [ ] Прогресс отражает реальное состояние загрузки
- [ ] Результаты агрегации не изменились (корректность данных)
- [ ] Кеширование работает корректно
- [ ] Код соответствует стандартам PSR-12
- [ ] Логирование добавлено для всех операций
- [ ] Обработка ошибок реализована

### Критерии для каждой подзадачи:
- См. раздел "Ступенчатые подзадачи"

---

## 🧪 Тестирование

### Функциональное тестирование:
1. Проверить корректность агрегации данных (новые, закрытые, переходящие тикеты)
2. Проверить корректность расчета процентов (4-й месяц)
3. Проверить работу кеша (первый запрос vs повторный запрос)
4. Проверить отображение прогресса загрузки

### Производительность:
1. Измерить время загрузки до оптимизации
2. Измерить время загрузки после каждой подзадачи
3. Измерить время загрузки после всех оптимизаций
4. Сравнить результаты

### UX:
1. Проверить отображение прогресса загрузки
2. Проверить сообщения о этапах загрузки
3. Проверить поведение при ошибках

---

## 📝 Примеры кода

### Оптимизация запроса переходящих тикетов:

```php
// До оптимизации (строки 716-767)
foreach ($targetStages as $stageId) {
    $result = CRest::call('crm.item.list', [
        'entityTypeId' => $entityTypeId,
        'filter' => [
            'stageId' => $stageId  // Фильтр только по стадии
        ],
        // ...
    ]);
}

// После оптимизации
foreach ($targetStages as $stageId) {
    $result = CRest::call('crm.item.list', [
        'entityTypeId' => $entityTypeId,
        'filter' => [
            'stageId' => $stageId,
            '<=createdTime' => $periodEndStr  // Фильтр по периоду
        ],
        // ...
    ]);
}
```

### Объединение проходов по массиву тикетов:

```php
// До оптимизации (множественные проходы)
foreach ($months as $month) {
    foreach ($month['weeks'] as $week) {
        $weekMetrics = calculateWeekMetrics(...);  // Проход по всем тикетам
    }
    foreach ($tickets as $ticket) {
        // Подсчёт новых тикетов
    }
    foreach ($tickets as $ticket) {
        // Подсчёт закрытых тикетов
    }
    // ...
}

// После оптимизации (один проход)
$monthMetrics = [];
foreach ($months as $month) {
    $monthMetrics[$month['monthNumber']] = [
        'newTickets' => 0,
        'closedTickets' => 0,
        'carryoverTickets' => 0,
        // ...
    ];
}

// Один проход по тикетам
foreach ($tickets as $ticket) {
    foreach ($months as $month) {
        // Агрегация всех метрик для месяца
    }
}
```

### Реальный прогресс загрузки (Frontend):

```javascript
// До оптимизации (симуляция)
await updateProgress(95, 'Агрегация данных...', 400);
const result = await fetchAdmissionClosureStats({...});
await updateProgress(100, 'Формирование графиков...', 300);

// После оптимизации (реальный прогресс)
const result = await fetchAdmissionClosureStatsWithProgress({
  product: '1C',
  periodMode: 'months',
  includeTickets: true,
  onProgress: (progress, stage) => {
    loadingProgress.value = progress;
    currentLoadingStage.value = stage;
  }
});
```

---

## 🔗 Связанные документы

- [Анализ оптимизации](../../REFACTOR/PERFORMANCE/optimization-3-months-mode-loading.md)
- [Модуль 3: График приёма и закрытий сектора 1С](../../MODULES/module-03-graph-admission-closure/README.md)
- [TASK-053: Режим выбора периода 3 месяца](./TASK-053-period-selection-3-months-mode.md)
- [TASK-054: Исправление пагинации](./TASK-054-pagination-fix.md)
- [TASK-058: Обогащение в режиме 3 месяцев](./TASK-058-months-mode-enrichment.md)

---

## 📊 История правок

- **2025-12-18 10:00 (UTC+3, Брест):** Создана задача на основе анализа оптимизации

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

