# TASK-001: Миграция страниц install.php и index.php на Vue.js

**Дата создания:** 2025-12-05 20:45 (UTC+3, Брест)  
**Дата завершения:** 2025-12-05 21:30 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Критический  
**Исполнитель:** Bitrix24 Программист (Vue.js)

---

## 📋 Описание

Перевести существующие PHP-страницы `install.php` и `index.php` на Vue.js приложение. Это первая задача по внедрению Vue.js в проект и начало нулевого этапа разработки.

**Текущее состояние:**
- `install.php` — страница установки приложения (PHP + HTML + inline JS)
- `index.php` — главная страница с профилем пользователя (PHP + HTML + BX24 API)

**Целевое состояние:**
- Vue.js приложение с компонентами для установки и главной страницы
- Интеграция с Bitrix24 REST API через Vue.js
- Использование Bitrix24 UI Kit для единого стиля

---

## 🎯 Контекст

Это нулевой этап разработки Vue.js приложения. Задача является основой для дальнейшей разработки:
- Создаёт базовую структуру Vue.js приложения
- Настраивает интеграцию с Bitrix24 REST API
- Определяет архитектуру компонентов
- Устанавливает стандарты работы с Bitrix24 API в Vue.js

**Связи с другими задачами:**
- Будет основой для всех последующих задач по Vue.js
- Определит структуру проекта и паттерны работы

---

## 📁 Модули и компоненты

### Структура Vue.js приложения:

```
vue-app/
├── src/
│   ├── main.js                    # Точка входа Vue.js
│   ├── App.vue                    # Корневой компонент
│   ├── components/
│   │   ├── InstallPage.vue        # Компонент страницы установки
│   │   ├── IndexPage.vue         # Компонент главной страницы
│   │   ├── common/
│   │   │   ├── StatusMessage.vue  # Компонент для сообщений (success/error/info)
│   │   │   └── LoadingSpinner.vue # Компонент загрузки
│   ├── services/
│   │   ├── bitrix24-api.js        # Сервис для работы с Bitrix24 REST API
│   │   └── bitrix24-bx-api.js     # Сервис для работы с BX24.* API
│   ├── router/
│   │   └── index.js               # Маршрутизация (Vue Router)
│   ├── utils/
│   │   └── helpers.js             # Вспомогательные функции
│   └── styles/
│       └── main.css               # Глобальные стили
├── public/
│   └── index.html                 # HTML шаблон
├── package.json                    # Зависимости проекта
└── vite.config.js                  # Конфигурация Vite
```

### PHP файлы (точки входа):

```
/var/www/back/
├── install.php                     # Точка входа для установки (рендерит Vue app)
├── index.php                       # Точка входа для главной страницы (рендерит Vue app)
└── vue-app/                        # Папка с Vue.js приложением
```

---

## 🔗 Зависимости

### От других задач:
- Нет (это первая задача)

### От модулей:
- `crest.php` — библиотека CRest для работы с Bitrix24 REST API (уже существует)
- `settings.php` — настройки приложения (уже существует)

### Зависимости задачи:
- Все последующие задачи по Vue.js будут зависеть от этой задачи

---

## 📝 Ступенчатые подзадачи

### Этап 1: Настройка Vue.js проекта
1. Инициализировать Vue.js проект (через `npm create vue@latest` или вручную)
2. Настроить Vite для сборки проекта
3. Установить зависимости:
   - `vue@^3.x`
   - `vue-router@^4.x`
   - `axios` или использовать `fetch`
4. Настроить структуру папок согласно архитектуре
5. Создать базовый `App.vue` и `main.js`

### Этап 2: Создание сервисов для Bitrix24 API
6. Создать `services/bitrix24-api.js`:
   - Метод для вызова Bitrix24 REST API через PHP backend
   - Обработка ошибок
   - Логирование запросов
7. Создать `services/bitrix24-bx-api.js`:
   - Обёртка для работы с BX24.* API (BX24.init, BX24.callMethod)
   - Интеграция с Bitrix24 UI Kit

### Этап 3: Компонент страницы установки (InstallPage.vue)
8. Создать компонент `InstallPage.vue`:
   - Отображение статуса установки
   - Обработка результата установки через PHP backend
   - Интеграция с BX24.installFinish()
   - Использование компонента StatusMessage для отображения сообщений
9. Реализовать логику:
   - Получение результата установки от PHP backend
   - Отображение успеха/ошибки установки
   - Вызов BX24.installFinish() при успешной установке

### Этап 4: Компонент главной страницы (IndexPage.vue)
10. Создать компонент `IndexPage.vue`:
    - Отображение информации о подключении
    - Получение профиля приложения через REST API
    - Получение информации о текущем пользователе через BX24 API
    - Обработка ошибок (включая "no_install_app")
11. Реализовать логику:
    - Загрузка профиля приложения при монтировании компонента
    - Загрузка информации о текущем пользователе через BX24.callMethod('user.current')
    - Отображение ошибок с предложением перейти на install.php

### Этап 5: Общие компоненты
12. Создать `StatusMessage.vue`:
    - Поддержка типов: success, error, info
    - Стилизация согласно Bitrix24 UI Kit
13. Создать `LoadingSpinner.vue`:
    - Компонент загрузки для асинхронных операций

### Этап 6: Маршрутизация
14. Настроить Vue Router:
    - Маршрут `/install` → InstallPage.vue
    - Маршрут `/` → IndexPage.vue
15. Интегрировать роутинг в `App.vue`

### Этап 7: Интеграция с PHP точками входа
16. Обновить `install.php`:
    - Оставить PHP логику установки (CRest::installApp())
    - Передать результат установки в Vue.js приложение
    - Рендерить Vue.js приложение с передачей данных
17. Обновить `index.php`:
    - Убрать PHP логику отображения (оставить только подключение crest.php)
    - Рендерить Vue.js приложение
    - Передать токены/настройки в Vue.js (если нужно)

### Этап 8: Стилизация
18. Создать глобальные стили `styles/main.css`:
    - Базовые стили (сброс, типографика)
    - Стили для компонентов (контейнеры, сообщения)
    - Адаптация стилей Bitrix24 UI Kit
19. Добавить scoped стили в компоненты

### Этап 9: Тестирование и отладка
20. Протестировать установку приложения:
    - Проверить работу install.php
    - Проверить отображение статуса установки
    - Проверить вызов BX24.installFinish()
21. Протестировать главную страницу:
    - Проверить загрузку профиля приложения
    - Проверить загрузку информации о пользователе
    - Проверить обработку ошибок (включая "no_install_app")
22. Проверить интеграцию с Bitrix24:
    - Убедиться, что BX24 API работает корректно
    - Проверить REST API запросы

---

## 🔌 API-методы Bitrix24

### REST API (через PHP backend):
- **Не требуется** — установка и получение профиля уже реализованы в PHP

### BX24.* API (на клиенте):
- `BX24.init(callback)` — инициализация Bitrix24 API
  - Документация: https://dev.1c-bitrix.ru/rest_help/js_library/index.php
- `BX24.installFinish()` — завершение установки приложения
  - Документация: https://dev.1c-bitrix.ru/rest_help/js_library/bx24_installfinish.php
- `BX24.callMethod(method, params, callback)` — вызов методов Bitrix24
  - Метод: `user.current` — получение текущего пользователя
  - Документация: https://dev.1c-bitrix.ru/rest_help/js_library/bx24_callmethod.php
- `BX24.getUser(callback)` — получение информации о пользователе
- `BX24.getAuth(callback)` — получение информации об авторизации

---

## ⚙️ Технические требования

### Версии технологий:
- **Vue.js:** 3.x (Composition API)
- **Vue Router:** 4.x
- **Vite:** последняя стабильная версия
- **Node.js:** 18.x или выше
- **PHP:** 8.3+ (уже используется)

### Зависимости:
- `vue@^3.4.0`
- `vue-router@^4.2.0`
- `axios@^1.6.0` (или использовать fetch)
- `@vitejs/plugin-vue@^5.0.0`

### Ограничения:
- Должна сохраняться совместимость с существующим PHP кодом (crest.php, settings.php)
- Интеграция с Bitrix24 должна работать как в облачной, так и в коробочной версии
- Приложение должно работать внутри Bitrix24 (не отдельный домен)

### Требования к производительности:
- Время загрузки приложения ≤ 2 секунд
- Плавная работа интерфейса без лагов

---

## ✅ Критерии приёмки

### Функциональные требования:
- [ ] Vue.js приложение инициализировано и работает
- [ ] Страница установки (`install.php`) работает через Vue.js компонент
- [ ] Страница установки корректно отображает статус установки
- [ ] Страница установки вызывает `BX24.installFinish()` при успешной установке
- [ ] Главная страница (`index.php`) работает через Vue.js компонент
- [ ] Главная страница загружает и отображает профиль приложения
- [ ] Главная страница загружает и отображает информацию о текущем пользователе
- [ ] Обработка ошибок работает корректно (включая "no_install_app")
- [ ] При ошибке "no_install_app" отображается ссылка на install.php

### Технические требования:
- [ ] Структура проекта соответствует архитектуре
- [ ] Все компоненты созданы и работают
- [ ] Сервисы для работы с Bitrix24 API реализованы
- [ ] Маршрутизация настроена и работает
- [ ] Стили соответствуют Bitrix24 UI Kit
- [ ] Код соответствует стандартам ESLint
- [ ] Нет ошибок в консоли браузера
- [ ] Приложение собирается без ошибок (npm run build)

### Интеграционные требования:
- [ ] Интеграция с Bitrix24 REST API работает
- [ ] Интеграция с BX24.* API работает
- [ ] PHP точки входа (install.php, index.php) корректно рендерят Vue.js приложение
- [ ] Приложение работает внутри Bitrix24

---

## 💻 Примеры кода

### Пример 1: Сервис для Bitrix24 REST API

```javascript
// services/bitrix24-api.js
/**
 * Сервис для работы с Bitrix24 REST API через PHP backend
 */
export class Bitrix24ApiService {
  /**
   * Вызов метода Bitrix24 REST API
   * @param {string} method - Метод API
   * @param {object} params - Параметры запроса
   * @returns {Promise<object>} Результат запроса
   */
  static async call(method, params = {}) {
    try {
      const response = await fetch('/api/bitrix24.php', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          method,
          params,
        }),
      });

      if (!response.ok) {
        throw new Error(`HTTP error! status: ${response.status}`);
      }

      const result = await response.json();

      if (result.error) {
        throw new Error(result.error_description || result.error);
      }

      return result;
    } catch (error) {
      console.error('Bitrix24 API error:', error);
      throw error;
    }
  }

  /**
   * Получение профиля приложения
   */
  static async getProfile() {
    return this.call('profile', {});
  }
}
```

### Пример 2: Компонент InstallPage.vue

```vue
<template>
  <div class="install-page">
    <h1>Установка Bitrix24 приложения</h1>

    <StatusMessage
      v-if="installStatus"
      :type="installStatus.type"
      :message="installStatus.message"
    />

    <div v-if="installInfo" class="info">
      <h3>Информация об установке:</h3>
      <pre>{{ JSON.stringify(installInfo, null, 2) }}</pre>
    </div>
  </div>
</template>

<script>
import { ref, onMounted } from 'vue';
import StatusMessage from '@/components/common/StatusMessage.vue';
import { Bitrix24BxApi } from '@/services/bitrix24-bx-api.js';

export default {
  name: 'InstallPage',
  components: {
    StatusMessage,
  },
  setup() {
    const installStatus = ref(null);
    const installInfo = ref(null);

    onMounted(async () => {
      // Получаем результат установки от PHP backend
      const response = await fetch('/install.php?get_result=1');
      const result = await response.json();

      installInfo.value = result;

      if (result.rest_only === false) {
        if (result.install === true) {
          installStatus.value = {
            type: 'success',
            message: '✓ Установка завершена успешно!',
          };

          // Завершаем установку через BX24 API
          Bitrix24BxApi.init(() => {
            Bitrix24BxApi.installFinish();
          });
        } else {
          installStatus.value = {
            type: 'error',
            message: '✗ Ошибка установки. Не удалось установить приложение. Проверьте настройки.',
          };
        }
      } else {
        installStatus.value = {
          type: 'info',
          message: 'REST-only режим. Это приложение работает только через REST API.',
        };
      }
    });

    return {
      installStatus,
      installInfo,
    };
  },
};
</script>
```

### Пример 3: Компонент IndexPage.vue

```vue
<template>
  <div class="index-page">
    <h1>Bitrix24 REST Application</h1>

    <StatusMessage
      v-if="error"
      type="error"
      :message="error"
    />

    <div v-if="loading" class="loading">
      <LoadingSpinner />
    </div>

    <div v-if="profile && !error" class="info">
      <strong>Приложение успешно подключено!</strong>
    </div>

    <div v-if="profile" class="profile-info">
      <h2>Профиль приложения (админ):</h2>
      <pre>{{ JSON.stringify(profile, null, 2) }}</pre>
    </div>

    <div v-if="currentUser" class="user-info">
      <h2>Текущий пользователь (кто открыл приложение):</h2>
      <pre>{{ JSON.stringify(currentUser, null, 2) }}</pre>
    </div>
  </div>
</template>

<script>
import { ref, onMounted } from 'vue';
import StatusMessage from '@/components/common/StatusMessage.vue';
import LoadingSpinner from '@/components/common/LoadingSpinner.vue';
import { Bitrix24ApiService } from '@/services/bitrix24-api.js';
import { Bitrix24BxApi } from '@/services/bitrix24-bx-api.js';

export default {
  name: 'IndexPage',
  components: {
    StatusMessage,
    LoadingSpinner,
  },
  setup() {
    const loading = ref(true);
    const error = ref(null);
    const profile = ref(null);
    const currentUser = ref(null);

    onMounted(async () => {
      try {
        // Загружаем профиль приложения
        profile.value = await Bitrix24ApiService.getProfile();
      } catch (err) {
        error.value = err.message;

        // Если ошибка "no_install_app" - предлагаем установить
        if (err.message.includes('no_install_app')) {
          error.value += ' Решение: Установите приложение через install.php';
        }
      } finally {
        loading.value = false;
      }

      // Загружаем информацию о текущем пользователе через BX24 API
      Bitrix24BxApi.init(() => {
        Bitrix24BxApi.callMethod('user.current', {}, (result) => {
          if (result.error()) {
            console.error('Error getting user:', result.error());
          } else {
            currentUser.value = result.data();
          }
        });
      });
    });

    return {
      loading,
      error,
      profile,
      currentUser,
    };
  },
};
</script>
```

---

## 🧪 Тестирование

### Шаги для тестирования:

1. **Тестирование установки:**
   - Открыть `install.php` в браузере
   - Проверить отображение статуса установки
   - Проверить вызов `BX24.installFinish()` в консоли
   - Проверить сохранение токенов в `settings.json`

2. **Тестирование главной страницы:**
   - Открыть `index.php` в браузере
   - Проверить загрузку профиля приложения
   - Проверить загрузку информации о пользователе
   - Проверить отображение данных в интерфейсе

3. **Тестирование ошибок:**
   - Удалить `settings.json` (симуляция не установленного приложения)
   - Открыть `index.php`
   - Проверить отображение ошибки "no_install_app"
   - Проверить наличие ссылки на `install.php`

4. **Тестирование интеграции:**
   - Проверить работу в консоли браузера (нет ошибок)
   - Проверить сетевые запросы (Network tab)
   - Проверить работу внутри Bitrix24

---

## 📚 Дополнительные ресурсы

- [Vue.js документация](https://vuejs.org/)
- [Vue Router документация](https://router.vuejs.org/)
- [Vite документация](https://vitejs.dev/)
- [Bitrix24 REST API](../API-REFERENCES/bitrix24-rest-api.md)
- [Bitrix24 JS Library](https://dev.1c-bitrix.ru/rest_help/js_library/index.php)
- [Архитектура фронтенда](../ARCHITECTURE/frontend-architecture.md)

---

## 📝 История правок

- **2025-12-05 20:45 (UTC+3, Брест):** Создана задача TASK-001
- **2025-12-05 21:00 (UTC+3, Брест):** Задача выполнена, все проблемы решены ✅

---

## ✅ Статус выполнения

**Задача выполнена успешно!** 🚀

Все компоненты работают корректно:
- ✅ Vue.js приложение инициализировано и работает
- ✅ Страница установки (`install.php`) работает через Vue.js компонент
- ✅ Главная страница (`index.php`) работает через Vue.js компонент
- ✅ Интеграция с Bitrix24 REST API работает
- ✅ Интеграция с BX24.* API работает
- ✅ Статические файлы доступны через nginx

---

## 🔧 Проблемы, которые были решены

### Проблема 1: 404 ошибки для статических файлов

**Описание:**
- Nginx возвращал 404 для файлов из `/dist/assets/`
- Файлы физически существовали, но не были доступны через веб-сервер

**Причина:**
- Отсутствовал специальный `location /dist/` блок в конфигурации nginx
- Nginx не знал, как обрабатывать запросы к статическим файлам Vue.js приложения

**Решение:**
- Добавлен `location /dist/` блок в конфигурацию nginx (`/etc/nginx/sites-available/back.antonov-mark.ru`)
- Настроен `alias` на `/var/www/back/dist/`
- Добавлены CORS заголовки для работы внутри Bitrix24

### Проблема 2: Неправильное извлечение путей к скриптам

**Описание:**
- PHP код извлекал неправильный путь к JavaScript файлу
- Регулярное выражение находило первый `<script>` тег (Bitrix24 API), а не Vue.js скрипт

**Причина:**
- Регулярное выражение `/<script[^>]+src="([^"]+)"[^>]*>/i` находило любой скрипт
- Bitrix24 API скрипт (`//api.bitrix24.com/api/v1/`) был первым в HTML

**Решение:**
- Изменено регулярное выражение для поиска скрипта с `type="module"` или `crossorigin`
- Теперь правильно находится Vue.js скрипт: `/dist/assets/main-XXXXX.js`

### Проблема 3: Неправильный base путь в Vite

**Описание:**
- Изначально использовался `base: '/back/dist/'`
- Но в конфигурации nginx `root` установлен в `/var/www/back`

**Причина:**
- Несоответствие между базовым путем в Vite и конфигурацией nginx
- Пути в `dist/index.html` были неправильными

**Решение:**
- Изменён `base` в `vite.config.js` на `/dist/`
- Пересобран проект с правильным base
- Пути теперь корректные: `/dist/assets/...`

---

## 🎯 Важные замечания

### Production сборка

**⚠️ ВАЖНО:** Мы работаем **ТОЛЬКО с production сборкой** приложения, так как всё происходит внутри Bitrix24.

- **НЕ используется** Vite dev server (`npm run dev`)
- **Используется только** production сборка (`npm run build`)
- Файлы из `dist/` автоматически подключаются через PHP (`install.php`, `index.php`)

**Процесс работы:**
1. Разработка в `vue-app/`
2. Сборка: `cd vue-app && npm run build`
3. Файлы попадают в `dist/`
4. PHP файлы автоматически подключают собранные файлы
5. Тестирование внутри Bitrix24

### Авторизация и токены

**Текущая реализация:**
- Авторизация происходит через Bitrix24 REST API
- Токены сохраняются в `settings.json` после установки приложения
- Используется библиотека `crest.php` для работы с Bitrix24 API

**Токены клиента приложения:**
- `access_token` — токен доступа к Bitrix24 REST API
- `refresh_token` — токен для обновления доступа
- `application_token` — токен приложения
- Сохраняются в `settings.json` через `CRest::installApp()`

**Работа с токенами:**
- Токены получаются при установке приложения через `install.php`
- Хранятся в `settings.json` (не в git)
- Используются для всех запросов к Bitrix24 REST API
- Обновляются автоматически через `crest.php` при необходимости

**API endpoint для Vue.js:**
- `/api/bitrix24.php` — принимает запросы от Vue.js приложения
- Использует токены из `settings.json` через `CRest::call()`
- Обеспечивает безопасную работу с Bitrix24 API

---

**Задача выполнена и работает корректно!** ✅

