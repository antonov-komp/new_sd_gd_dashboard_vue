# TASK-034: Реализация уровня 4 — список тикетов в попапе детализации

**Дата создания:** 2025-12-12 11:51 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Зависит от:** TASK-033 (завершена)

---

## 📋 Описание

Добавить четвёртый уровень интерактивности в попап детализации по сотрудникам (`EmployeeDetailsModal.vue`). Уровень 4 отображает список тикетов в виде таблицы с карточками, идентичными визуальной структуре канбан-режима основного модуля "Дашборд 1С".

**Текущее состояние:**
- Уровень 1: Список сотрудников стадии (работает)
- Уровень 2: Временные градации тикетов сотрудника (работает)
- Уровень 3: Детализация по заказчикам (отделам) для каждой временной градации (работает)
- Уровень 4: **Отсутствует** — нужно реализовать

**Целевое состояние:**
- **Уровень 4:** Список тикетов в виде таблицы с карточками (новый)
  - Отображение тикетов в той же визуальной структуре, что и в канбан-режиме
  - Использование компонента `TicketCard.vue` для отображения каждого тикета
  - Возможность открыть тикет в новой вкладке (как в канбан-режиме)

---

## 🎯 Контекст

### Модуль "График состояния сектора 1С"

**Компонент:** `GraphStateChart.vue`  
**Расположение:** `vue-app/src/components/graph-state/GraphStateChart.vue`

**Попап:** `EmployeeDetailsModal.vue`  
**Расположение:** `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`

**Компонент карточки тикета:** `TicketCard.vue`  
**Расположение:** `vue-app/src/components/dashboard/TicketCard.vue`

### Триггеры перехода на уровень 4

**Из уровня 2 (Временные градации):**
- Клик на строку временной градации → переход на уровень 4
- Отображаются все тикеты сотрудника в выбранной временной градации

**Из уровня 3 (Заказчики):**
- Клик на строку заказчика → переход на уровень 4
- Отображаются все тикеты сотрудника у выбранного заказчика в выбранной временной градации

**Из уровня 1 (По времени):**
- Клик на временную градацию в виде "По времени" → переход на уровень 4
- Отображаются все тикеты стадии в выбранной временной градации

**Из уровня 1 (По заказчикам):**
- Клик на строку заказчика в виде "По заказчикам" → переход на уровень 4
- Отображаются все тикеты стадии у выбранного заказчика

---

## 📊 Структура уровня 4

### Отображение

**Заголовок:**
- Зависит от контекста перехода:
  - Из уровня 2: "Иван Иванов — СЕГОДНЯ — Сформировано обращение"
  - Из уровня 3: "Иван Иванов — СЕГОДНЯ — Отдел продаж — Сформировано обращение"
  - Из уровня 1 (По времени): "СЕГОДНЯ — Сформировано обращение"
  - Из уровня 1 (По заказчикам): "Отдел продаж — Сформировано обращение"

**Общее количество тикетов:**
- Отображается в заголовке: "Всего: 15 тикетов"

**Список тикетов:**
- Таблица с карточками тикетов
- Каждая карточка использует компонент `TicketCard.vue`
- Визуальная структура идентична канбан-режиму дашборда 1С
- Карточки отображаются в виде списка (вертикальная прокрутка)

**Интерактивность:**
- Клик на карточку тикета → открытие тикета в новой вкладке
- Использование функции `getTicketIframeUrl()` для формирования URL
- Кнопка "Назад" → возврат на предыдущий уровень

---

## 🔍 Источники данных

### Компонент TicketCard.vue

**Файл:** `vue-app/src/components/dashboard/TicketCard.vue`

**Props:**
- `ticket` (Object, required) — объект тикета
- `draggable` (Boolean, default: false) — отключено для попапа

**Структура объекта тикета:**
```javascript
{
  id: Number,                    // ID тикета
  title: String,                 // Название тикета
  ufSubject: String | null,      // Тема тикета (приоритетное)
  priorityId: String,             // ID приоритета
  priorityLabel: String,         // Название приоритета
  priorityColors: Object,         // Цвета приоритета
  service: Object,               // Объект сервиса
  serviceLabel: String,          // Название сервиса
  actionStr: String | null,      // UF_ACTION_STR
  departmentHead: String | null, // Отдел заказчика (ограничено 17 символов)
  description: String | null,   // Описание тикета
  createdAt: String,             // Дата создания (ISO)
  // ... другие поля
}
```

**Функция открытия тикета:**
```javascript
import { getTicketIframeUrl } from '@/services/dashboard-sector-1c/utils/constants.js';

const iframeUrl = getTicketIframeUrl(ticket.id);
window.open(iframeUrl, '_blank');
```

### Данные для уровня 4

**Источник данных:**
- Тикеты уже загружены в snapshot или через API
- Фильтрация по контексту перехода:
  - Из уровня 2: тикеты сотрудника в выбранной временной градации
  - Из уровня 3: тикеты сотрудника у выбранного заказчика в выбранной временной градации
  - Из уровня 1 (По времени): тикеты стадии в выбранной временной градации
  - Из уровня 1 (По заказчикам): тикеты стадии у выбранного заказчика

**Необходимые поля тикета:**
- Все поля, требуемые компонентом `TicketCard.vue`
- Если каких-то полей нет в snapshot, загрузить через API

---

## 📝 Модули и компоненты

### Основные файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа (основной файл для изменений)
- `vue-app/src/components/dashboard/TicketCard.vue` — компонент карточки тикета (используется в уровне 4)

### Новые файлы (если потребуется)

- `vue-app/src/utils/graph-state/ticketListUtils.js` — утилиты для работы со списком тикетов
  - `filterTicketsByContext()` — фильтрация тикетов по контексту перехода
  - `prepareTicketsForDisplay()` — подготовка тикетов для отображения в карточках

### Зависимости

- `vue-app/src/services/dashboard-sector-1c/utils/constants.js` — функция `getTicketIframeUrl()`
- `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js` — маппер тикетов
- `vue-app/src/utils/graph-state/popupNavigationUtils.js` — утилиты навигации (существующие)

---

## 🚀 Этапы реализации

### Этап 1: Анализ структуры данных и подготовка контекста перехода

**Цель:** Определить структуру данных для уровня 4 и контекст перехода из предыдущих уровней.

#### 1.1. Анализ существующих данных

- Изучить структуру данных уровней 2 и 3
- Определить, какие тикеты доступны в каждом контексте
- Проверить наличие всех необходимых полей для `TicketCard.vue`

#### 1.2. Определение контекста перехода

- Создать структуру контекста перехода:
  ```javascript
  {
    sourceLevel: 1 | 2 | 3,        // Уровень, с которого переходим
    employeeId: Number | null,      // ID сотрудника (если есть)
    employeeName: String | null,     // Имя сотрудника (если есть)
    stageId: String,                // ID стадии
    stageName: String,              // Название стадии
    dateCategory: String | null,    // Категория давности (если есть)
    dateCategoryLabel: String | null, // Название категории (если есть)
    departmentName: String | null,  // Название заказчика (если есть)
    tickets: Array                  // Массив тикетов для отображения
  }
  ```

#### 1.3. Функция фильтрации тикетов по контексту

- Создать функцию `filterTicketsByContext(context, snapshot)`
- Фильтрация тикетов в зависимости от контекста перехода
- Обработка всех вариантов перехода (из уровня 1, 2, 3)

**Критерии приёмки этапа 1:**
- [ ] Структура контекста перехода определена
- [ ] Функция фильтрации тикетов реализована
- [ ] Все варианты перехода обработаны
- [ ] Проверена доступность необходимых полей тикетов

---

### Этап 2: Подготовка данных тикетов для отображения

**Цель:** Обеспечить наличие всех необходимых полей тикетов для компонента `TicketCard.vue`.

#### 2.1. Анализ требований TicketCard.vue

- Изучить все поля, требуемые компонентом `TicketCard.vue`
- Определить, какие поля могут отсутствовать в snapshot
- Составить список обязательных и опциональных полей

#### 2.2. Функция подготовки тикетов

- Создать функцию `prepareTicketsForDisplay(tickets, snapshot, ticketDetails)`
- Объединение данных из snapshot и загруженных деталей
- Нормализация данных для соответствия структуре `TicketCard.vue`
- Обработка отсутствующих полей (fallback значения)

#### 2.3. Загрузка недостающих данных

- Проверить наличие всех необходимых полей
- Если полей нет — загрузить через `TicketDetailsService`
- Кеширование загруженных данных

**Критерии приёмки этапа 2:**
- [ ] Функция подготовки тикетов реализована
- [ ] Все обязательные поля тикетов присутствуют
- [ ] Обработка отсутствующих полей работает
- [ ] Загрузка недостающих данных через API реализована

---

### Этап 3: Расширение состояния компонента попапа

**Цель:** Добавить состояние уровня 4 в компонент `EmployeeDetailsModal.vue`.

#### 3.1. Добавление состояния уровня 4

- Добавить `level4Data` в состояние компонента:
  ```javascript
  const level4Data = ref(null);
  ```

#### 3.2. Структура данных уровня 4

- Определить структуру `level4Data`:
  ```javascript
  {
    context: Object,              // Контекст перехода
    tickets: Array,               // Массив тикетов для отображения
    totalCount: Number,            // Общее количество тикетов
    isLoading: Boolean            // Флаг загрузки
  }
  ```

#### 3.3. Обновление функции навигации

- Расширить функцию `goBack()` для обработки уровня 4
- Добавить функцию `goToLevel4(context)` для перехода на уровень 4
- Обновить функцию `resetPopup()` для сброса уровня 4

**Критерии приёмки этапа 3:**
- [ ] Состояние уровня 4 добавлено
- [ ] Структура данных определена
- [ ] Функции навигации обновлены
- [ ] Сброс состояния работает корректно

---

### Этап 4: Реализация шаблона уровня 4

**Цель:** Создать шаблон для отображения списка тикетов на уровне 4.

#### 4.1. Добавление секции уровня 4 в шаблон

- Добавить блок `v-else-if="popupLevel === 4"` в шаблон
- Структура заголовка с контекстом перехода
- Отображение общего количества тикетов

#### 4.2. Контейнер для списка тикетов

- Создать контейнер с вертикальной прокруткой
- Стили для списка карточек тикетов
- Адаптивность для разных размеров экрана

#### 4.3. Интеграция компонента TicketCard

- Импортировать компонент `TicketCard.vue`
- Использовать `v-for` для отображения карточек
- Передать необходимые props в компонент
- Отключить drag & drop (`draggable: false`)

**Критерии приёмки этапа 4:**
- [ ] Шаблон уровня 4 добавлен
- [ ] Заголовок отображается корректно
- [ ] Компонент TicketCard интегрирован
- [ ] Карточки тикетов отображаются в списке

---

### Этап 5: Обработка кликов и открытие тикетов

**Цель:** Реализовать открытие тикетов в новой вкладке при клике на карточку.

#### 5.1. Обработчик клика на карточку

- Добавить обработчик `@click` на компонент `TicketCard`
- Использовать функцию `getTicketIframeUrl()` для формирования URL
- Открытие тикета в новой вкладке через `window.open()`

#### 5.2. Эмит события из TicketCard

- Обработать событие `@click` от компонента `TicketCard`
- Получить объект тикета из события
- Сформировать URL и открыть в новой вкладке

#### 5.3. Обработка ошибок

- Обработка случая, когда URL не может быть сформирован
- Обработка случая, когда тикет не найден
- Уведомления пользователю при ошибках

**Критерии приёмки этапа 5:**
- [ ] Клик на карточку открывает тикет в новой вкладке
- [ ] URL формируется корректно
- [ ] Обработка ошибок реализована
- [ ] Уведомления пользователю работают

---

### Этап 6: Реализация переходов на уровень 4 из всех источников

**Цель:** Обеспечить переход на уровень 4 из уровней 1, 2 и 3.

#### 6.1. Переход из уровня 2 (Временные градации)

- Добавить обработчик клика на строку временной градации
- Фильтрация тикетов сотрудника по выбранной градации
- Переход на уровень 4 с контекстом перехода

#### 6.2. Переход из уровня 3 (Заказчики)

- Добавить обработчик клика на строку заказчика
- Фильтрация тикетов сотрудника у выбранного заказчика
- Переход на уровень 4 с контекстом перехода

#### 6.3. Переход из уровня 1 (По времени)

- Обновить обработчик `handleTimeCategoryClick()`
- Фильтрация тикетов стадии по выбранной градации
- Переход на уровень 4 с контекстом перехода

#### 6.4. Переход из уровня 1 (По заказчикам)

- Добавить обработчик клика на строку заказчика в виде "По заказчикам"
- Фильтрация тикетов стадии у выбранного заказчика
- Переход на уровень 4 с контекстом перехода

**Критерии приёмки этапа 6:**
- [ ] Переход из уровня 2 работает
- [ ] Переход из уровня 3 работает
- [ ] Переход из уровня 1 (По времени) работает
- [ ] Переход из уровня 1 (По заказчикам) работает
- [ ] Контекст перехода сохраняется корректно

---

### Этап 7: Стилизация уровня 4

**Цель:** Применить стили для уровня 4, соответствующие дизайну попапа и канбан-режима.

#### 7.1. Стили контейнера списка тикетов

- Стили для контейнера с вертикальной прокруткой
- Максимальная высота и overflow
- Отступы и закругления

#### 7.2. Стили карточек тикетов

- Использование стилей из `TicketCard.vue`
- Адаптация для попапа (отключение drag & drop)
- Отступы между карточками

#### 7.3. Адаптивность

- Стили для мобильных устройств
- Стили для планшетов
- Стили для десктопов
- Оптимизация прокрутки на touch-устройствах

**Критерии приёмки этапа 7:**
- [ ] Стили применены корректно
- [ ] Карточки отображаются в правильном формате
- [ ] Адаптивность работает на всех устройствах
- [ ] Прокрутка работает плавно

---

### Этап 8: Анимации переходов и загрузки

**Цель:** Добавить плавные анимации при переходах на уровень 4 и загрузке данных.

#### 8.1. Анимация перехода на уровень 4

- Использовать Vue transitions для плавного перехода
- Анимация появления списка тикетов
- Синхронизация с существующими анимациями уровней

#### 8.2. Индикатор загрузки

- Отображение индикатора загрузки при подготовке данных
- Скрытие индикатора после загрузки
- Обработка состояния загрузки

#### 8.3. Анимация появления карточек

- Плавное появление карточек в списке
- Stagger-анимация для последовательного появления
- Оптимизация производительности анимаций

**Критерии приёмки этапа 8:**
- [ ] Анимации переходов плавные
- [ ] Индикатор загрузки работает
- [ ] Анимация появления карточек реализована
- [ ] Производительность анимаций оптимизирована

---

### Этап 9: Обработка граничных случаев и ошибок

**Цель:** Обработать все граничные случаи и ошибки на уровне 4.

#### 9.1. Обработка пустого списка тикетов

- Отображение сообщения "Нет тикетов" при пустом списке
- Стилизация пустого состояния
- Информативное сообщение для пользователя

#### 9.2. Обработка ошибок загрузки данных

- Обработка ошибок при загрузке тикетов через API
- Fallback на данные из snapshot
- Уведомления пользователю при критических ошибках

#### 9.3. Обработка отсутствующих полей

- Обработка отсутствующих обязательных полей
- Fallback значения для опциональных полей
- Логирование отсутствующих полей для отладки

#### 9.4. Обработка больших списков тикетов

- Виртуализация списка при большом количестве тикетов (если необходимо)
- Оптимизация рендеринга
- Пагинация или бесконечная прокрутка (если необходимо)

**Критерии приёмки этапа 9:**
- [ ] Пустой список обрабатывается корректно
- [ ] Ошибки загрузки обрабатываются
- [ ] Отсутствующие поля обрабатываются
- [ ] Большие списки работают плавно

---

### Этап 10: Тестирование и оптимизация

**Цель:** Протестировать уровень 4, оптимизировать производительность и исправить найденные проблемы.

#### 10.1. Функциональное тестирование

- Проверить переходы на уровень 4 из всех источников
- Проверить отображение тикетов в карточках
- Проверить открытие тикетов в новой вкладке
- Проверить навигацию (кнопка "Назад")

#### 10.2. Тестирование данных

- Проверить корректность фильтрации тикетов
- Проверить отображение всех полей в карточках
- Проверить обработку отсутствующих полей
- Проверить работу с разными контекстами перехода

#### 10.3. Тестирование производительности

- Проверить время загрузки уровня 4
- Проверить плавность анимаций
- Проверить работу с большими списками тикетов
- Оптимизировать рендеринг при необходимости

#### 10.4. Тестирование адаптивности

- Проверить отображение на мобильных устройствах
- Проверить отображение на планшетах
- Проверить отображение на десктопах
- Проверить работу прокрутки на touch-устройствах

#### 10.5. Исправление найденных проблем

- Исправить все найденные баги
- Оптимизировать код для лучшей производительности
- Улучшить обработку ошибок
- Добавить логирование для отладки (если необходимо)

**Критерии приёмки этапа 10:**
- [ ] Все функциональные тесты пройдены
- [ ] Данные отображаются корректно
- [ ] Производительность оптимизирована
- [ ] Адаптивность проверена на всех устройствах
- [ ] Все найденные проблемы исправлены

---

## 🔗 API-методы Bitrix24

### Загрузка деталей тикетов (если необходимо)

**Метод:** `crm.item.list`  
**Документация:** https://context7.com/bitrix24/rest/crm.item.list

**Параметры:**
```javascript
{
  entityTypeId: 140,  // Смарт-процесс сектора 1С
  filter: {
    id: [1001, 1002, 1003]  // Массив ID тикетов
  },
  select: [
    'id',
    'title',
    'ufSubject',
    'stageId',
    'UF_CRM_7_DEPARTMENT_HEAD',
    'UF_CRM_7_UF_PRIORITY',
    'UF_CRM_7_UF_SERVICE',
    'UF_ACTION_STR',
    'createdTime',
    'assignedById',
    'description'
  ]
}
```

---

## 📚 Связанные документы

- `DOCS/TASKS/TASK-033-interactive-popup-levels.md` — задача 33 (предыдущая)
- `DOCS/GUIDES/ui-ticket-card-guideline.md` — гайдлайн по карточке тикета
- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа
- `vue-app/src/components/dashboard/TicketCard.vue` — компонент карточки тикета
- `vue-app/src/services/dashboard-sector-1c/utils/constants.js` — константы и утилиты

---

## ✅ Общие критерии приёмки

- [ ] Уровень 4 отображается корректно
- [ ] Переходы на уровень 4 работают из всех источников (уровни 1, 2, 3)
- [ ] Тикеты отображаются в карточках, идентичных канбан-режиму
- [ ] Клик на карточку открывает тикет в новой вкладке
- [ ] Навигация (кнопка "Назад") работает корректно
- [ ] Обработка ошибок реализована
- [ ] Код соответствует стандартам проекта
- [ ] Компонент адаптивен для всех устройств
- [ ] Производительность оптимизирована
- [ ] Анимации плавные и не влияют на производительность

---

## 📝 История правок

- **2025-12-12 11:51 (UTC+3, Брест):** Создана задача TASK-034
  - Определены 10 этапов реализации
  - Добавлены подзаголовки для каждого этапа
  - Описаны критерии приёмки
  - Указаны источники данных и зависимости

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)




