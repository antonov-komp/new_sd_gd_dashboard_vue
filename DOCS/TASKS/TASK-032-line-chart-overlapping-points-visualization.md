# TASK-032: Визуализация перекрывающихся точек на линейном графике состояния сектора 1С

**Дата создания:** 2025-12-12 08:38 (UTC+3, Брест)  
**Дата завершения:** 2025-12-12 09:41 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js) + UX/UI-дизайнер  
**Родительская задача:** TASK-031

**Выбранное решение:** Вариант 6 - Комбинированный подход (стили точек + индикатор количества)  
**План реализации:** 5 этапов (см. раздел "План реализации")

---

## 📋 Описание задачи

В модуле "График состояния сектора 1С" в разделе с отображением **линейного графика** существует проблема визуализации: когда несколько точек разных линий (этапов) имеют одинаковое значение Y на одной временной точке X, они полностью перекрывают друг друга. 

**Текущая ситуация:**
- При наведении на точку в tooltip видно, что там несколько значений (несколько этапов с одинаковым количеством тикетов)
- Визуально на графике это не заметно — видна только одна точка
- Пользователь не понимает, что в этой позиции находится несколько перекрывающихся точек

**Целевое состояние:**
- Визуально должно быть заметно, что в данной позиции находится несколько точек
- Индикация должна быть видна без наведения курсора
- При наведении сохраняется текущая функциональность (tooltip с детализацией)

---

## 🎯 Цель

Улучшить UX линейного графика, сделав перекрывающиеся точки визуально различимыми без необходимости наведения курсора. Пользователь должен сразу видеть, что в данной позиции находится несколько точек с одинаковыми значениями.

---

## 🔍 Анализ проблемы

### Текущая реализация

**Компонент:** `GraphStateChart.vue`  
**Расположение:** `vue-app/src/components/graph-state/GraphStateChart.vue`

**Технические детали:**
- Используется Chart.js 4.x с vue-chartjs
- Линейный график (`type: 'line'`)
- Каждый этап (стадия) — отдельный dataset (линия)
- Настройки точек:
  - `pointRadius: 6` — радиус точки
  - `pointHoverRadius: 8` — радиус при наведении
  - `pointBackgroundColor` — цвет точки (цвет этапа)
  - `pointBorderColor` — цвет границы точки

**Проблема:**
```javascript
// Пример: три этапа имеют одинаковое значение (например, 10 тикетов)
// на временной точке "Начало недели"
datasets: [
  { label: 'Сформировано обращение', data: [10, 15, 12] },
  { label: 'На проверке', data: [10, 18, 14] },      // ← перекрывается
  { label: 'В исполнении', data: [10, 20, 16] }      // ← перекрывается
]
```

В этом случае все три точки с координатами (0, 10) будут наложены друг на друга, и визуально будет видна только одна точка (последняя нарисованная).

---

## 💡 Возможные решения

### Вариант 1: Разные стили точек для каждого этапа ⭐ (Рекомендуется)

**Идея:** Использовать разные формы точек (круг, треугольник, квадрат, ромб) для каждого этапа.

**Преимущества:**
- Простая реализация (встроенная функциональность Chart.js)
- Не искажает данные (точки остаются на правильных координатах)
- Визуально различимо даже при полном перекрытии
- Соответствует best practices для различения множественных линий

**Реализация:**
```javascript
const pointStyles = ['circle', 'triangle', 'rect', 'rectRot', 'star', 'cross', 'crossRot'];
datasets.forEach((dataset, index) => {
  dataset.pointStyle = pointStyles[index % pointStyles.length];
});
```

**Визуальный эффект:**
- Первая линия: круги (○)
- Вторая линия: треугольники (△)
- Третья линия: квадраты (□)
- При перекрытии видны разные формы

---

### Вариант 2: Концентрические круги (кольца)

**Идея:** При перекрытии точек отрисовывать концентрические круги — чем больше точек перекрывается, тем больше внешний круг.

**Преимущества:**
- Визуально показывает количество перекрывающихся точек
- Интуитивно понятно (больше точек = больше круг)

**Недостатки:**
- Требует кастомного плагина Chart.js
- Сложнее реализация
- Может быть менее заметно при малом количестве перекрытий

**Реализация:**
- Кастомный плагин `afterDatasetsDraw`
- Анализ координат всех точек на каждой временной позиции
- Отрисовка концентрических кругов для перекрывающихся точек

---

### Вариант 3: Jitter (небольшое смещение по X-оси)

**Идея:** Слегка смещать точки по оси X, чтобы они не перекрывались полностью.

**Преимущества:**
- Все точки видны
- Простая реализация

**Недостатки:**
- ❌ **Искажает данные** — точки не на своих временных позициях
- Может вводить в заблуждение пользователя
- Не рекомендуется для временных рядов

**Вывод:** Не подходит для данной задачи, так как важно сохранить точность временных позиций.

---

### Вариант 4: Индикатор количества (цифра/бейдж)

**Идея:** При перекрытии точек отображать небольшой бейдж с цифрой, показывающей количество перекрывающихся точек.

**Преимущества:**
- Точная информация о количестве перекрытий
- Компактное решение

**Недостатки:**
- Требует кастомного плагина
- Может быть менее заметно
- Дополнительный визуальный элемент (может загромождать график)

**Реализация:**
- Кастомный плагин `afterDatasetsDraw`
- Анализ перекрытий
- Отрисовка бейджа рядом с точкой

---

### Вариант 5: Увеличение размера точки при перекрытии

**Идея:** Динамически увеличивать размер точки в зависимости от количества перекрывающихся точек.

**Преимущества:**
- Визуально заметно
- Показывает "важность" позиции (больше точек = больше размер)

**Недостатки:**
- Требует кастомного плагина
- Может быть менее точным визуально (большая точка может скрывать детали)

**Реализация:**
- Кастомный плагин для динамического изменения `pointRadius`
- Анализ перекрытий перед отрисовкой

---

### Вариант 6: Комбинированный подход (стили + индикатор)

**Идея:** Комбинация разных стилей точек + небольшой индикатор количества при большом перекрытии (3+ точек).

**Преимущества:**
- Максимальная информативность
- Работает для любого количества перекрытий

**Недостатки:**
- Более сложная реализация
- Может быть избыточно для простых случаев

---

## ✅ Выбранное решение

**Вариант 6: Комбинированный подход (стили точек + индикатор количества)**

### Обоснование:

1. **Разные стили точек** — базовое решение для визуального различия:
   - Реализуется быстро (встроенная функциональность Chart.js)
   - Не искажает данные (точки остаются на правильных координатах)
   - Визуально различимо для 2-3 перекрывающихся точек
   - Соответствует стандартам визуализации данных
   - Каждый этап получает уникальную форму точки

2. **Индикатор количества** — дополнительное улучшение для случаев с большим количеством перекрытий (3+ точек):
   - Показывает точное количество перекрывающихся точек
   - Помогает понять масштаб перекрытия
   - Визуально привлекает внимание к позициям с множественными перекрытиями
   - Улучшает информативность графика

**Преимущества комбинированного подхода:**
- Максимальная информативность для любого количества перекрытий
- Работает как для малых (2-3 точки), так и для больших (4+ точек) перекрытий
- Соответствует best practices визуализации данных
- Улучшает UX без искажения данных

---

## 📋 План реализации (5 этапов)

### Этап 1: Подготовка и настройка стилей точек

**Цель:** Применить разные стили точек для каждого этапа (dataset) в линейном графике.

**Задачи:**
1. Определить массив доступных стилей точек Chart.js
2. Создать функцию для назначения стиля каждому этапу
3. Интегрировать стили в функцию `prepareChartData()`
4. Применить стили к datasets линейного графика
5. Протестировать визуальное различие точек

**Технические детали:**
- Файл: `vue-app/src/components/graph-state/GraphStateChart.vue`
- Функция: `prepareChartData()` (строки ~1150-1243)
- Массив стилей: `['circle', 'triangle', 'rect', 'rectRot', 'star', 'cross', 'crossRot']`
- Применение: `pointStyle: pointStyles[index % pointStyles.length]`

**Критерии завершения:**
- [ ] Каждый этап имеет уникальный стиль точки
- [ ] Стили применяются ко всем точкам графика
- [ ] Визуально точки разных этапов различимы
- [ ] Нет ошибок в консоли браузера

**Ожидаемый результат:**
Точки разных этапов имеют разные формы (круг, треугольник, квадрат, ромб и т.д.), что делает их визуально различимыми даже при перекрытии.

---

### Этап 2: Создание утилиты для анализа перекрытий

**Цель:** Разработать утилиту для определения перекрывающихся точек на графике.

**Задачи:**
1. Создать файл `vue-app/src/utils/graph-state/lineChartUtils.js`
2. Реализовать функцию `findOverlappingPoints(chart, threshold)` для анализа перекрытий
3. Функция должна:
   - Проходить по всем временным позициям (labels)
   - Находить точки с одинаковыми или близкими значениями Y (в пределах threshold)
   - Группировать перекрывающиеся точки по позициям
   - Возвращать массив объектов с информацией о перекрытиях
4. Добавить функцию `getOverlapCount(position, datasets)` для подсчёта перекрытий на позиции
5. Протестировать утилиту на тестовых данных

**Технические детали:**
- Новый файл: `vue-app/src/utils/graph-state/lineChartUtils.js`
- Функция `findOverlappingPoints`:
  ```javascript
  /**
   * Находит перекрывающиеся точки на линейном графике
   * @param {Object} chart - Экземпляр Chart.js
   * @param {number} threshold - Порог для определения перекрытия (по умолчанию 0.5)
   * @returns {Array} Массив объектов { position, count, points }
   */
  ```
- Threshold: 0.5 единицы Y (точки считаются перекрывающимися, если разница < 0.5)

**Критерии завершения:**
- [ ] Файл `lineChartUtils.js` создан
- [ ] Функция `findOverlappingPoints` корректно находит перекрытия
- [ ] Функция `getOverlapCount` корректно подсчитывает количество
- [ ] Утилита протестирована на различных сценариях
- [ ] Добавлены JSDoc комментарии

**Ожидаемый результат:**
Утилита, которая может анализировать график и находить все позиции с перекрывающимися точками, возвращая информацию о количестве перекрытий.

---

### Этап 3: Создание кастомного плагина для индикатора количества

**Цель:** Разработать плагин Chart.js для отрисовки индикатора количества перекрывающихся точек.

**Задачи:**
1. Создать файл `vue-app/src/components/graph-state/plugins/overlappingPointsPlugin.js`
2. Реализовать плагин Chart.js с методом `afterDatasetsDraw`
3. Плагин должен:
   - Использовать утилиту `findOverlappingPoints` для анализа
   - Для каждой позиции с 3+ перекрытиями отрисовывать бейдж
   - Бейдж должен показывать количество перекрывающихся точек
   - Позиционировать бейдж справа-сверху от точки
4. Стилизовать бейдж:
   - Круглая форма, размер 18px
   - Цвет фона: `#ef4444` (красный, Bitrix24 danger)
   - Цвет текста: белый
   - Шрифт: 10px, bold, Arial
   - Тень для лучшей видимости
5. Зарегистрировать плагин в компоненте

**Технические детали:**
- Новый файл: `vue-app/src/components/graph-state/plugins/overlappingPointsPlugin.js`
- Структура плагина:
  ```javascript
  export const overlappingPointsPlugin = {
    id: 'overlappingPointsPlugin',
    afterDatasetsDraw: (chart) => {
      // Анализ перекрытий
      // Отрисовка бейджей
    }
  };
  ```
- Порог отображения: 3+ перекрывающихся точек
- Позиционирование: `x + 8px, y - 8px` (справа-сверху от точки)

**Критерии завершения:**
- [ ] Файл `overlappingPointsPlugin.js` создан
- [ ] Плагин корректно анализирует перекрытия
- [ ] Бейджи отрисовываются только при 3+ перекрытиях
- [ ] Бейджи стилизованы в соответствии с требованиями
- [ ] Плагин зарегистрирован в компоненте
- [ ] Нет ошибок отрисовки

**Ожидаемый результат:**
Кастомный плагин, который автоматически находит перекрывающиеся точки и отрисовывает индикаторы количества для позиций с 3+ перекрытиями.

---

### Этап 4: Интеграция плагина в компонент GraphStateChart

**Цель:** Интегрировать плагин и утилиту в основной компонент графика.

**Задачи:**
1. Импортировать плагин в `GraphStateChart.vue`
2. Импортировать утилиту `lineChartUtils.js`
3. Зарегистрировать плагин через `ChartJS.register(overlappingPointsPlugin)`
4. Убедиться, что плагин применяется только к линейным графикам
5. Добавить проверку типа графика в плагине
6. Протестировать работу плагина с реальными данными
7. Проверить производительность (не должно быть лагов при отрисовке)

**Технические детали:**
- Файл: `vue-app/src/components/graph-state/GraphStateChart.vue`
- Импорты:
  ```javascript
  import { overlappingPointsPlugin } from './plugins/overlappingPointsPlugin.js';
  import { findOverlappingPoints } from '@/utils/graph-state/lineChartUtils.js';
  ```
- Регистрация: в `setup()` или `onMounted()`
- Условие: `if (chartType.value === 'line')`

**Критерии завершения:**
- [ ] Плагин импортирован и зарегистрирован
- [ ] Утилита импортирована
- [ ] Плагин работает только для линейных графиков
- [ ] Индикаторы отображаются корректно
- [ ] Нет ошибок в консоли
- [ ] Производительность не ухудшилась

**Ожидаемый результат:**
Полностью интегрированное решение: стили точек работают для всех этапов, индикаторы количества отображаются для позиций с 3+ перекрытиями.

---

### Этап 5: Тестирование, оптимизация и финализация

**Цель:** Протестировать решение, оптимизировать производительность и завершить реализацию.

**Задачи:**
1. **Функциональное тестирование:**
   - Проверить отображение стилей точек для всех этапов
   - Проверить отображение индикаторов при различных сценариях перекрытий
   - Проверить работу tooltip при наведении
   - Проверить клик по точкам (модальное окно с детализацией)

2. **Визуальное тестирование:**
   - Проверить читаемость стилей точек на разных устройствах
   - Проверить видимость индикаторов на разных фонах
   - Проверить адаптивность на мобильных устройствах
   - Проверить контрастность цветов

3. **Производительность:**
   - Проверить время отрисовки графика
   - Оптимизировать анализ перекрытий (кеширование, мемоизация)
   - Убедиться, что нет лагов при взаимодействии

4. **Документация:**
   - Добавить комментарии к коду
   - Обновить JSDoc для всех функций
   - Задокументировать параметры плагина

5. **Финализация:**
   - Убрать console.log (если были)
   - Проверить соответствие стандартам кода
   - Убедиться, что все критерии приёмки выполнены

**Технические детали:**
- Оптимизация: мемоизация результатов `findOverlappingPoints`
- Кеширование: пересчёт перекрытий только при изменении данных
- Производительность: анализ должен выполняться за < 50ms

**Критерии завершения:**
- [ ] Все функциональные тесты пройдены
- [ ] Визуальное тестирование завершено успешно
- [ ] Производительность соответствует требованиям
- [ ] Код задокументирован
- [ ] Все критерии приёмки выполнены
- [ ] Решение готово к продакшену

**Ожидаемый результат:**
Полностью рабочее, протестированное и оптимизированное решение для визуализации перекрывающихся точек на линейном графике.

---

## 📁 Модули и компоненты

### Изменяемые файлы:

1. **`vue-app/src/components/graph-state/GraphStateChart.vue`**
   - Добавление массива стилей точек (Этап 1)
   - Применение стилей к datasets в функции `prepareChartData()` (Этап 1)
   - Импорт и регистрация плагина `overlappingPointsPlugin` (Этап 4)
   - Импорт утилиты `lineChartUtils` (Этап 4)

### Новые файлы:

1. **`vue-app/src/utils/graph-state/lineChartUtils.js`** (Этап 2)
   - Утилиты для анализа перекрывающихся точек
   - Функция `findOverlappingPoints(chart, threshold)` — анализ перекрытий
   - Функция `getOverlapCount(position, datasets)` — подсчёт перекрытий на позиции

2. **`vue-app/src/components/graph-state/plugins/overlappingPointsPlugin.js`** (Этап 3)
   - Кастомный плагин Chart.js для отрисовки индикатора количества перекрытий
   - Метод `afterDatasetsDraw` для отрисовки бейджей
   - Логика определения позиций с 3+ перекрытиями

---

## 🔧 Технические детали

### Chart.js Point Styles (Этап 1):

Доступные стили точек в Chart.js:
- `'circle'` — круг (по умолчанию)
- `'triangle'` — треугольник
- `'rect'` — квадрат
- `'rectRot'` — повёрнутый квадрат (ромб)
- `'star'` — звезда
- `'cross'` — крест
- `'crossRot'` — повёрнутый крест

**Пример кода для применения стилей:**

```javascript
// В функции prepareChartData() (строки ~1150-1243)
const pointStyles = ['circle', 'triangle', 'rect', 'rectRot', 'star', 'cross', 'crossRot'];

stages.forEach((stage, index) => {
  datasets.push({
    label: stage.name,
    data: data,
    // ... другие настройки
    pointStyle: pointStyles[index % pointStyles.length], // Применение стиля
    pointRadius: 6,
    pointHoverRadius: 8,
    pointBackgroundColor: stage.color,
    pointBorderColor: stage.color,
    pointBorderWidth: 2,
    // ... остальные настройки
  });
});
```

### Утилита для анализа перекрытий (Этап 2):

**Файл:** `vue-app/src/utils/graph-state/lineChartUtils.js`

```javascript
/**
 * Находит перекрывающиеся точки на линейном графике
 * @param {Object} chart - Экземпляр Chart.js
 * @param {number} threshold - Порог для определения перекрытия (по умолчанию 0.5)
 * @returns {Array} Массив объектов { position: number, count: number, points: Array }
 */
export function findOverlappingPoints(chart, threshold = 0.5) {
  if (chart.config.type !== 'line') return [];
  
  const datasets = chart.data.datasets;
  const scales = chart.scales;
  const labels = chart.data.labels;
  const overlaps = [];
  
  // Проход по каждой временной позиции
  labels.forEach((label, positionIndex) => {
    const pointsAtPosition = [];
    
    // Сбор всех точек на этой позиции
    datasets.forEach((dataset, datasetIndex) => {
      const meta = chart.getDatasetMeta(datasetIndex);
      if (meta.data[positionIndex]) {
        const point = meta.data[positionIndex];
        pointsAtPosition.push({
          datasetIndex,
          x: point.x,
          y: point.y,
          value: dataset.data[positionIndex]
        });
      }
    });
    
    // Группировка перекрывающихся точек
    const grouped = groupOverlappingPoints(pointsAtPosition, threshold);
    
    // Добавление позиций с 3+ перекрытиями
    grouped.forEach(group => {
      if (group.count >= 3) {
        overlaps.push({
          position: positionIndex,
          count: group.count,
          x: group.x,
          y: group.y,
          points: group.points
        });
      }
    });
  });
  
  return overlaps;
}

/**
 * Группирует перекрывающиеся точки
 */
function groupOverlappingPoints(points, threshold) {
  // Логика группировки точек с близкими координатами Y
  // ...
}
```

### Кастомный плагин для индикатора (Этап 3):

**Файл:** `vue-app/src/components/graph-state/plugins/overlappingPointsPlugin.js`

```javascript
import { findOverlappingPoints } from '@/utils/graph-state/lineChartUtils.js';

export const overlappingPointsPlugin = {
  id: 'overlappingPointsPlugin',
  afterDatasetsDraw: (chart) => {
    if (chart.config.type !== 'line') return;
    
    const ctx = chart.ctx;
    
    // Найти перекрывающиеся точки
    const overlaps = findOverlappingPoints(chart, 0.5);
    
    // Отрисовка бейджей для каждой позиции с перекрытиями
    overlaps.forEach(overlap => {
      const { x, y, count } = overlap;
      
      // Позиция бейджа (справа-сверху от точки)
      const badgeX = x + 8;
      const badgeY = y - 8;
      const badgeRadius = 9;
      
      // Отрисовка фона бейджа
      ctx.save();
      ctx.beginPath();
      ctx.arc(badgeX, badgeY, badgeRadius, 0, 2 * Math.PI);
      ctx.fillStyle = '#ef4444'; // Bitrix24 danger color
      ctx.fill();
      ctx.shadowColor = 'rgba(0, 0, 0, 0.3)';
      ctx.shadowBlur = 4;
      ctx.shadowOffsetX = 0;
      ctx.shadowOffsetY = 2;
      
      // Отрисовка текста
      ctx.fillStyle = '#ffffff';
      ctx.font = 'bold 10px Arial, sans-serif';
      ctx.textAlign = 'center';
      ctx.textBaseline = 'middle';
      ctx.fillText(count.toString(), badgeX, badgeY);
      
      ctx.restore();
    });
  }
};
```

### Интеграция в компонент (Этап 4):

**Файл:** `vue-app/src/components/graph-state/GraphStateChart.vue`

```javascript
import { overlappingPointsPlugin } from './plugins/overlappingPointsPlugin.js';
import ChartJS from 'chart.js/auto';

// Регистрация плагина
ChartJS.register(overlappingPointsPlugin);

// В setup() или onMounted()
onMounted(() => {
  // Плагин автоматически применяется ко всем линейным графикам
});
```

---

## 🎨 UX/UI требования

### Визуальные требования:

1. **Стили точек (Этап 1):**
   - Разные формы для каждого этапа (круг, треугольник, квадрат, ромб, звезда и т.д.)
   - Сохранение цветов этапов (каждый этап сохраняет свой цвет)
   - Размер точек: 6px (обычное состояние), 8px (при наведении)
   - Толщина границы: 2px
   - Порядок стилей: циклический (если этапов больше, чем стилей, повторяются)

2. **Индикатор количества (Этап 3):**
   - **Форма:** Круглый бейдж
   - **Размер:** 18px (радиус 9px)
   - **Цвет фона:** `#ef4444` (Bitrix24 danger color - красный)
   - **Цвет текста:** `#ffffff` (белый)
   - **Шрифт:** `bold 10px Arial, sans-serif`
   - **Позиция:** Справа-сверху от точки (x + 8px, y - 8px)
   - **Тень:** `rgba(0, 0, 0, 0.3)`, blur: 4px, offset: (0, 2px)
   - **Порог отображения:** Только при 3+ перекрывающихся точках
   - **Выравнивание текста:** По центру бейджа

3. **Адаптивность:**
   - Стили точек должны быть различимы на мобильных устройствах (минимум 6px)
   - Индикатор должен быть читаемым на всех размерах экрана
   - На мобильных устройствах индикатор может быть немного меньше (16px вместо 18px)

### Соответствие гайдлайнам Bitrix24:

- **Цвета:** Использовать цвета из палитры Bitrix24 для индикаторов (`#ef4444` - danger)
- **Типографика:** Шрифт Arial или системный шрифт Bitrix24
- **Доступность:** 
  - Контрастность текста на бейдже: минимум 4.5:1 (белый на красном)
  - Размеры элементов: минимум 6px для точек, 18px для бейджа
  - Семантические цвета: красный для привлечения внимания к перекрытиям

### Визуальная иерархия:

1. **Базовый уровень:** Разные стили точек делают перекрытия визуально различимыми
2. **Дополнительный уровень:** Индикатор количества привлекает внимание к позициям с множественными перекрытиями (3+)
3. **Интерактивный уровень:** Tooltip и модальное окно предоставляют детальную информацию при взаимодействии

---

## ✅ Критерии приёмки

### Обязательные (Этап 1 - Стили точек):

- [ ] Каждый этап имеет уникальный стиль точки (круг, треугольник, квадрат и т.д.)
- [ ] Стили применяются ко всем точкам линейного графика
- [ ] Точки разных этапов визуально различимы
- [ ] Сохранены цвета этапов для точек
- [ ] Размеры точек: 6px (обычное), 8px (при наведении)

### Обязательные (Этап 2 - Утилита анализа):

- [ ] Файл `lineChartUtils.js` создан и экспортирует функции
- [ ] Функция `findOverlappingPoints` корректно находит перекрытия
- [ ] Функция `getOverlapCount` корректно подсчитывает количество
- [ ] Утилита обрабатывает edge cases (пустые данные, одна точка и т.д.)
- [ ] Добавлены JSDoc комментарии

### Обязательные (Этап 3 - Плагин индикатора):

- [ ] Файл `overlappingPointsPlugin.js` создан
- [ ] Плагин корректно анализирует перекрытия через утилиту
- [ ] Индикатор отображается только при 3+ перекрывающихся точках
- [ ] Бейдж стилизован: круглый, 18px, красный фон, белый текст
- [ ] Бейдж позиционируется справа-сверху от точки
- [ ] Плагин работает только для линейных графиков

### Обязательные (Этап 4 - Интеграция):

- [ ] Плагин импортирован и зарегистрирован в компоненте
- [ ] Утилита импортирована в компонент
- [ ] Плагин применяется только к линейным графикам
- [ ] Индикаторы отображаются корректно на реальных данных
- [ ] Нет ошибок в консоли браузера

### Обязательные (Этап 5 - Финальное тестирование):

- [ ] Перекрывающиеся точки визуально различимы без наведения
- [ ] При наведении сохраняется текущая функциональность (tooltip)
- [ ] При клике открывается модальное окно с детализацией (существующая функциональность)
- [ ] График корректно отображается на всех устройствах (мобильные, планшеты, десктопы)
- [ ] Нет визуальных артефактов или ошибок отрисовки
- [ ] Производительность не ухудшилась (время отрисовки < 50ms)
- [ ] Код задокументирован (JSDoc, комментарии)
- [ ] Соответствие стандартам кода проекта

### Дополнительные (UX/UI):

- [ ] Стили точек различимы на мобильных устройствах
- [ ] Индикаторы читаемы на всех размерах экрана
- [ ] Контрастность цветов соответствует стандартам доступности
- [ ] Индикатор не перекрывает важные элементы графика
- [ ] Визуальное решение соответствует гайдлайнам Bitrix24

---

## 🧪 Тестирование

### Тестирование по этапам:

#### Этап 1: Тестирование стилей точек

**Сценарии:**
1. **Базовый сценарий:**
   - Открыть линейный график состояния сектора 1С
   - Проверить, что каждый этап имеет уникальный стиль точки
   - Убедиться, что стили применяются ко всем точкам графика
   - Проверить, что цвета этапов сохранены

2. **Сценарий с перекрытиями:**
   - Найти позицию с перекрывающимися точками (2-3 точки)
   - Убедиться, что точки визуально различимы благодаря разным формам
   - Проверить, что все формы точек видны (круг, треугольник, квадрат и т.д.)

3. **Сценарий с большим количеством этапов:**
   - Проверить график с максимальным количеством этапов (5+)
   - Убедиться, что стили циклически повторяются
   - Проверить, что нет визуальных конфликтов между стилями

#### Этап 2: Тестирование утилиты анализа

**Сценарии:**
1. **Тестирование функции `findOverlappingPoints`:**
   - Создать тестовый график с известными перекрытиями
   - Вызвать функцию и проверить корректность результатов
   - Проверить обработку edge cases (пустые данные, одна точка, нет перекрытий)

2. **Тестирование функции `getOverlapCount`:**
   - Проверить подсчёт перекрытий на различных позициях
   - Убедиться, что функция корректно обрабатывает граничные случаи

#### Этап 3: Тестирование плагина индикатора

**Сценарии:**
1. **Базовый сценарий:**
   - Открыть график с позициями, имеющими 3+ перекрытия
   - Проверить, что индикаторы отображаются только при 3+ перекрытиях
   - Убедиться, что индикаторы не отображаются при 1-2 перекрытиях

2. **Визуальное тестирование:**
   - Проверить стилизацию бейджей (цвет, размер, форма)
   - Проверить позиционирование (справа-сверху от точки)
   - Проверить читаемость текста на бейдже

3. **Сценарий с множественными перекрытиями:**
   - Проверить график с несколькими позициями, имеющими перекрытия
   - Убедиться, что все индикаторы отображаются корректно
   - Проверить, что индикаторы не перекрывают друг друга

#### Этап 4: Интеграционное тестирование

**Сценарии:**
1. **Полная интеграция:**
   - Проверить работу стилей точек + индикаторов вместе
   - Убедиться, что оба решения работают корректно
   - Проверить, что нет конфликтов между стилями и индикаторами

2. **Сценарий с наведением:**
   - Навести курсор на перекрывающиеся точки
   - Проверить, что tooltip показывает все перекрывающиеся значения
   - Убедиться, что индикатор не мешает tooltip
   - Проверить, что функциональность не нарушена

3. **Сценарий с кликом:**
   - Кликнуть на точку с перекрытиями
   - Проверить, что открывается модальное окно с детализацией
   - Убедиться, что индикатор не блокирует клик

#### Этап 5: Финальное тестирование

**Сценарии:**
1. **Функциональное тестирование:**
   - Все сценарии из предыдущих этапов
   - Проверить работу на реальных данных
   - Проверить обработку различных состояний данных (нет данных, одна точка и т.д.)

2. **Визуальное тестирование:**
   - Проверить читаемость стилей точек на разных фонах
   - Проверить видимость индикаторов на разных фонах
   - Проверить контрастность цветов

3. **Адаптивность:**
   - Проверить отображение на мобильных устройствах (320px+)
   - Проверить на планшетах (768px+)
   - Проверить на больших экранах (1920px+)
   - Убедиться, что стили и индикаторы читаемы на всех размерах

4. **Производительность:**
   - Измерить время отрисовки графика (должно быть < 50ms)
   - Проверить отсутствие лагов при взаимодействии
   - Проверить плавность анимаций при наведении

5. **Кросс-браузерное тестирование:**
   - Chrome/Edge (Chromium)
   - Firefox
   - Safari (если доступно)
   - Проверить корректность отрисовки canvas во всех браузерах

---

## 📝 История правок

- **2025-12-12 08:38 (UTC+3, Брест):** Создан документ с анализом проблемы перекрывающихся точек на линейном графике
- **2025-12-12 08:38 (UTC+3, Брест):** Выбран вариант 6 (комбинированный подход: стили точек + индикатор количества)
- **2025-12-12 08:38 (UTC+3, Брест):** Добавлен детальный план реализации в 5 этапов
- **2025-12-12 09:41 (UTC+3, Брест):** Реализованы все этапы задачи
- **2025-12-12 09:41 (UTC+3, Брест):** Финальное решение: комбинация стилей точек + увеличение размера при перекрытии (вариант 3)
- **2025-12-12 09:41 (UTC+3, Брест):** Задача успешно завершена

---

## 🔗 Связанные задачи

### Родительская задача:
- **TASK-031:** Интеграция детализации по сотрудникам в графики состояния сектора 1С

### Подзадачи (этапы реализации):
- **TASK-032-01:** Этап 1 - Подготовка и настройка стилей точек
- **TASK-032-02:** Этап 2 - Создание утилиты для анализа перекрытий
- **TASK-032-03:** Этап 3 - Создание кастомного плагина для индикатора количества
- **TASK-032-04:** Этап 4 - Интеграция плагина в компонент GraphStateChart
- **TASK-032-05:** Этап 5 - Тестирование, оптимизация и финализация

### Связанные задачи:
- **TASK-031-03:** Этап 3 - Интеграция в линейный и столбчатый графики
- **TASK-031-06:** Доработка UI/UX столбчатого графика состояния сектора 1С

---

## 📚 Дополнительная информация

### Использованные технологии:

- Vue.js 3.x (Composition API)
- Chart.js 4.x
- vue-chartjs

### Документация:

- Chart.js Point Styles: https://www.chartjs.org/docs/latest/configuration/elements.html#point-configuration
- Chart.js Plugins: https://www.chartjs.org/docs/latest/developers/plugins.html
- Bitrix24 UI Kit: https://apidocs.bitrix24.ru/sdk/ui.html

### Примеры визуализации перекрывающихся точек:

- Разные стили точек — стандартный подход в аналитике
- Концентрические круги — используется в некоторых дашбордах
- Индикаторы количества — популярны в BI-инструментах

---

**Статус:** ✅ Завершена  
**Выбранное решение:** Вариант 3 - Увеличение размера точки при перекрытии (финальная реализация)  
**План реализации:** 5 этапов (все этапы выполнены)

**Реализовано:**
- ✅ Этап 1: Стили точек (разные формы для каждого этапа) - применены в `GraphStateChart.vue`
- ✅ Этап 2: Утилита для анализа перекрытий - создан `lineChartUtils.js` с функциями `findOverlappingPoints()` и `getOverlapCount()`
- ✅ Этап 3: Плагин для увеличения размера точек при перекрытии - создан `overlappingPointsPlugin.js`
- ✅ Этап 4: Интеграция плагина в компонент - плагин зарегистрирован в `GraphStateChart.vue`
- ✅ Финальное решение: Комбинация стилей точек + увеличение размера при перекрытии

**Созданные файлы:**
- `vue-app/src/utils/graph-state/lineChartUtils.js` - утилита для анализа перекрытий
- `vue-app/src/components/graph-state/plugins/overlappingPointsPlugin.js` - плагин для визуализации перекрытий

**Изменённые файлы:**
- `vue-app/src/components/graph-state/GraphStateChart.vue` - добавлены стили точек и интеграция плагина

---

## 📋 Структура задач

Задача разбита на 5 детализированных этапов:

1. **TASK-032-01:** Подготовка и настройка стилей точек
   - Применение разных стилей точек для каждого этапа
   - Базовое решение для визуального различия

2. **TASK-032-02:** Создание утилиты для анализа перекрытий
   - Разработка утилиты `lineChartUtils.js`
   - Функции для поиска и анализа перекрывающихся точек

3. **TASK-032-03:** Создание кастомного плагина для индикатора количества
   - Разработка плагина `overlappingPointsPlugin.js`
   - Отрисовка бейджей с количеством перекрытий

4. **TASK-032-04:** Интеграция плагина в компонент GraphStateChart
   - Импорт и регистрация плагина
   - Интеграция с существующим компонентом

5. **TASK-032-05:** Тестирование, оптимизация и финализация
   - Комплексное тестирование
   - Оптимизация производительности
   - Документация и финализация

**Рекомендуемый порядок выполнения:** Последовательно, начиная с TASK-032-01


