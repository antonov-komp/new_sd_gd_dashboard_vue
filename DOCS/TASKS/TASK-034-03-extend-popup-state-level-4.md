# TASK-034-03: Расширение состояния компонента попапа для уровня 4

**Дата создания:** 2025-12-12 11:51 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-034 (Этап 3)

---

## 📋 Описание

Добавить состояние уровня 4 в компонент `EmployeeDetailsModal.vue`. Расширить функции навигации для поддержки перехода на уровень 4 и возврата с него. Обновить computed-свойства и функции инициализации.

---

## 🎯 Цель

1. Добавить `level4Data` в состояние компонента
2. Определить структуру данных уровня 4
3. Расширить функции навигации (`goBack()`, `resetPopup()`)
4. Добавить функцию `goToLevel4()` для перехода на уровень 4
5. Обновить computed-свойства для поддержки уровня 4

---

## 🔍 Контекст задачи

### Компонент EmployeeDetailsModal.vue

**Расположение:** `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`

**Текущая структура состояния:**
```javascript
const popupLevel = ref(1);           // Текущий уровень (1, 2, или 3)
const level1Data = ref(null);        // Данные уровня 1
const level2Data = ref(null);        // Данные уровня 2
const level3Data = ref(null);        // Данные уровня 3
const viewMode = ref('employees');  // Режим отображения уровня 1
const level1TimeCategories = ref([]); // Данные для вида "По времени"
const level1Departments = ref([]);   // Данные для вида "По заказчикам"
```

**Текущие функции навигации:**
- `goBack()` — возврат на предыдущий уровень (обрабатывает уровни 2 и 3)
- `resetPopup()` — полный сброс состояния (сбрасывает уровни 1, 2, 3)
- `initializeLevel1()` — инициализация уровня 1 (сбрасывает уровни 2 и 3)

### Зависимости от предыдущих этапов

**Этап 1 (TASK-034-01):**
- Структура контекста перехода (`Level4Context`)
- Функции создания контекста из разных источников
- Функция фильтрации тикетов по контексту

**Этап 2 (TASK-034-02):**
- Функция подготовки тикетов для отображения
- Функция загрузки недостающих данных

---

## 📝 Задачи

### Задача 3.1: Добавление состояния уровня 4

**Цель:** Добавить `level4Data` в состояние компонента и обновить `popupLevel` для поддержки уровня 4.

#### 3.1.1. Изучение текущей структуры состояния

**Действия:**
1. Открыть файл `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`
2. Найти секцию с объявлением refs для состояния
3. Изучить текущую структуру:
   ```javascript
   const popupLevel = ref(1);
   const level1Data = ref(null);
   const level2Data = ref(null);
   const level3Data = ref(null);
   ```

4. Определить место для добавления `level4Data`

**Ожидаемый результат:**
- Понимание текущей структуры состояния
- Определено место для добавления нового состояния

#### 3.1.2. Добавление level4Data

**Действия:**
1. Добавить объявление `level4Data` после `level3Data`:
   ```javascript
   /**
    * Данные уровня 4 (список тикетов)
    * Сохраняются при переходе на уровень 4
    */
   const level4Data = ref(null);
   ```

2. Обновить комментарий к `popupLevel`:
   ```javascript
   /**
    * Текущий уровень попапа (1, 2, 3, или 4)
    */
   const popupLevel = ref(1);
   ```

**Ожидаемый результат:**
- `level4Data` добавлен в состояние компонента
- Комментарии обновлены

**Критерии приёмки задачи 3.1:**
- [ ] `level4Data` добавлен в состояние компонента
- [ ] Комментарии обновлены
- [ ] Код соответствует стилю проекта

---

### Задача 3.2: Структура данных уровня 4

**Цель:** Определить структуру `level4Data` с учетом контекста перехода и подготовленных тикетов.

#### 3.2.1. Изучение структуры контекста перехода

**Действия:**
1. Изучить структуру `Level4Context` из этапа 1:
   ```javascript
   {
     sourceLevel: 1 | 2 | 3,
     employeeId: Number | null,
     employeeName: String | null,
     stageId: String,
     stageName: String,
     dateCategory: String | null,
     dateCategoryLabel: String | null,
     departmentName: String | null,
     tickets: Array,
     snapshot: Object,
     ticketDetails: Object | null
   }
   ```

2. Определить, какие поля нужны для уровня 4

#### 3.2.2. Определение структуры level4Data

**Структура:**
```javascript
/**
 * Структура данных уровня 4
 * 
 * @typedef {Object} Level4Data
 * @property {Object} context - Контекст перехода на уровень 4
 * @property {Number} context.sourceLevel - Уровень, с которого перешли (1, 2, или 3)
 * @property {Number|null} context.employeeId - ID сотрудника (если есть)
 * @property {String|null} context.employeeName - Имя сотрудника (если есть)
 * @property {String} context.stageId - ID стадии
 * @property {String} context.stageName - Название стадии
 * @property {String|null} context.dateCategory - Ключ категории давности (если есть)
 * @property {String|null} context.dateCategoryLabel - Название категории давности (если есть)
 * @property {String|null} context.departmentName - Название заказчика (если есть)
 * @property {Object} context.snapshot - Слепок с данными
 * @property {Object|null} context.ticketDetails - Детали тикетов (если загружены)
 * @property {Array} tickets - Массив подготовленных тикетов для отображения
 * @property {Number} totalCount - Общее количество тикетов
 * @property {Boolean} isLoading - Флаг загрузки данных
 */
```

**Пример структуры:**
```javascript
{
  context: {
    sourceLevel: 2,
    employeeId: 456,
    employeeName: 'Иван Иванов',
    stageId: 'formed',
    stageName: 'Сформировано обращение',
    dateCategory: 'today',
    dateCategoryLabel: 'СЕГОДНЯ',
    departmentName: null,
    snapshot: {...},
    ticketDetails: null
  },
  tickets: [
    {
      id: 1001,
      title: 'Название тикета',
      ufSubject: 'Тема тикета',
      priorityId: 'errors',
      priorityLabel: 'Ошибки/сбои',
      priorityColors: {...},
      service: {...},
      serviceLabel: '1С:Бухгалтерия',
      serviceColors: {...},
      actionStr: 'Требуется срочно',
      departmentHead: 'Отдел продаж',
      description: 'Описание тикета',
      createdAt: '2025-12-12T10:00:00+03:00'
    },
    // ... другие тикеты
  ],
  totalCount: 15,
  isLoading: false
}
```

#### 3.2.3. Документирование структуры

**Действия:**
1. Добавить JSDoc комментарий к структуре `level4Data`
2. Описать назначение каждого поля
3. Указать типы данных

**Ожидаемый результат:**
- Структура `level4Data` определена и задокументирована
- Все поля описаны с типами и назначением

**Критерии приёмки задачи 3.2:**
- [ ] Структура `level4Data` определена
- [ ] Все поля описаны с типами
- [ ] JSDoc комментарии добавлены
- [ ] Примеры структуры созданы

---

### Задача 3.3: Обновление функции навигации

**Цель:** Расширить функции навигации для поддержки уровня 4.

#### 3.3.1. Расширение функции goBack()

**Текущая реализация:**
```javascript
function goBack() {
  if (popupLevel.value === 3) {
    // Возврат с уровня 3 на уровень 2
    popupLevel.value = 2;
    level3Data.value = null;
  } else if (popupLevel.value === 2) {
    // Возврат с уровня 2 на уровень 1
    popupLevel.value = 1;
    level2Data.value = null;
    level3Data.value = null;
  }
}
```

**Обновленная реализация:**
```javascript
/**
 * Возврат на предыдущий уровень
 * 
 * Поддерживает возврат со всех уровней:
 * - Уровень 4 → Уровень 3, 2, или 1 (в зависимости от sourceLevel)
 * - Уровень 3 → Уровень 2
 * - Уровень 2 → Уровень 1
 */
function goBack() {
  if (popupLevel.value === 4) {
    // Возврат с уровня 4 на предыдущий уровень
    const sourceLevel = level4Data.value?.context?.sourceLevel;
    
    if (sourceLevel === 3) {
      // Возврат на уровень 3
      popupLevel.value = 3;
      level4Data.value = null;
    } else if (sourceLevel === 2) {
      // Возврат на уровень 2
      popupLevel.value = 2;
      level4Data.value = null;
    } else if (sourceLevel === 1) {
      // Возврат на уровень 1
      popupLevel.value = 1;
      level4Data.value = null;
    } else {
      // Fallback: возврат на уровень 1
      console.warn('[EmployeeDetailsModal] Unknown sourceLevel, returning to level 1');
      popupLevel.value = 1;
      level4Data.value = null;
    }
  } else if (popupLevel.value === 3) {
    // Возврат с уровня 3 на уровень 2
    popupLevel.value = 2;
    level3Data.value = null;
    level4Data.value = null; // Сброс уровня 4 при возврате
  } else if (popupLevel.value === 2) {
    // Возврат с уровня 2 на уровень 1
    popupLevel.value = 1;
    level2Data.value = null;
    level3Data.value = null;
    level4Data.value = null; // Сброс уровня 4 при возврате
  }
}
```

**Действия:**
1. Обновить функцию `goBack()` для обработки уровня 4
2. Добавить логику определения предыдущего уровня на основе `sourceLevel`
3. Добавить сброс `level4Data` при возврате с других уровней
4. Добавить обработку ошибок (fallback на уровень 1)

**Ожидаемый результат:**
- Функция `goBack()` поддерживает возврат с уровня 4
- Логика определения предыдущего уровня работает корректно
- Сброс состояния работает правильно

#### 3.3.2. Добавление функции goToLevel4()

**Функция:**
```javascript
/**
 * Переход на уровень 4 (список тикетов)
 * 
 * Фильтрует тикеты по контексту перехода, подготавливает их для отображения
 * и сохраняет в level4Data.
 * 
 * @param {Object} context - Контекст перехода на уровень 4 (Level4Context)
 * @returns {Promise<void>}
 * 
 * @example
 * const context = {
 *   sourceLevel: 2,
 *   employeeId: 456,
 *   employeeName: 'Иван Иванов',
 *   stageId: 'formed',
 *   stageName: 'Сформировано обращение',
 *   dateCategory: 'today',
 *   dateCategoryLabel: 'СЕГОДНЯ',
 *   departmentName: null,
 *   tickets: [...],
 *   snapshot: {...},
 *   ticketDetails: null
 * };
 * await goToLevel4(context);
 */
async function goToLevel4(context) {
  if (!context) {
    console.error('[EmployeeDetailsModal] Context is required for level 4');
    notifications.error('Ошибка: контекст перехода не указан');
    return;
  }

  console.log('[EmployeeDetailsModal] Transitioning to level 4:', {
    sourceLevel: context.sourceLevel,
    employeeId: context.employeeId,
    stageId: context.stageId,
    dateCategory: context.dateCategory,
    departmentName: context.departmentName,
    ticketsCount: context.tickets?.length || 0
  });

  try {
    // Установить флаг загрузки
    level4Data.value = {
      context: context,
      tickets: [],
      totalCount: 0,
      isLoading: true
    };

    // Фильтровать тикеты по контексту (если не переданы напрямую)
    let filteredTickets = context.tickets || [];
    
    if (filteredTickets.length === 0 && context.snapshot) {
      // Если тикеты не переданы, фильтруем из snapshot
      const { filterTicketsByContext } = await import('@/utils/graph-state/ticketListUtils.js');
      filteredTickets = await filterTicketsByContext(context);
    }

    // Подготовить тикеты для отображения
    const { prepareTicketsForDisplay } = await import('@/utils/graph-state/ticketListUtils.js');
    const preparedTickets = await prepareTicketsForDisplay(
      filteredTickets,
      context.snapshot,
      context.ticketDetails
    );

    console.log('[EmployeeDetailsModal] Tickets prepared for level 4:', {
      filteredCount: filteredTickets.length,
      preparedCount: preparedTickets.length,
      sampleTicket: preparedTickets[0] ? {
        id: preparedTickets[0].id,
        hasUfSubject: !!preparedTickets[0].ufSubject,
        hasPriorityColors: !!preparedTickets[0].priorityColors,
        hasServiceColors: !!preparedTickets[0].serviceColors
      } : null
    });

    // Сохранить данные уровня 4
    level4Data.value = {
      context: context,
      tickets: preparedTickets,
      totalCount: preparedTickets.length,
      isLoading: false
    };

    // Перейти на уровень 4
    popupLevel.value = 4;

    console.log('[EmployeeDetailsModal] Level 4 initialized:', {
      totalCount: level4Data.value.totalCount,
      ticketsCount: level4Data.value.tickets.length,
      isLoading: level4Data.value.isLoading
    });
  } catch (error) {
    console.error('[EmployeeDetailsModal] Error transitioning to level 4:', error);
    console.error('[EmployeeDetailsModal] Error details:', {
      message: error.message,
      stack: error.stack,
      context: context
    });

    notifications.error('Ошибка загрузки списка тикетов: ' + error.message);

    // Сбросить состояние уровня 4 при ошибке
    level4Data.value = null;
    // Вернуться на предыдущий уровень
    goBack();
  }
}
```

**Действия:**
1. Создать функцию `goToLevel4(context)`
2. Импортировать функции из `ticketListUtils.js` (динамический импорт)
3. Реализовать фильтрацию тикетов по контексту
4. Реализовать подготовку тикетов для отображения
5. Сохранить данные в `level4Data`
6. Установить `popupLevel = 4`
7. Добавить обработку ошибок

**Ожидаемый результат:**
- Функция `goToLevel4()` реализована
- Фильтрация и подготовка тикетов работают корректно
- Обработка ошибок реализована
- Логирование добавлено для отладки

#### 3.3.3. Обновление функции resetPopup()

**Текущая реализация:**
```javascript
function resetPopup() {
  popupLevel.value = 1;
  level1Data.value = null;
  level2Data.value = null;
  level3Data.value = null;
}
```

**Обновленная реализация:**
```javascript
/**
 * Полный сброс состояния попапа
 * 
 * Сбрасывает все уровни и возвращает попап в начальное состояние
 */
function resetPopup() {
  popupLevel.value = 1;
  level1Data.value = null;
  level2Data.value = null;
  level3Data.value = null;
  level4Data.value = null; // Добавить сброс уровня 4
  viewMode.value = 'employees'; // Сброс режима отображения уровня 1
  level1TimeCategories.value = []; // Сброс данных вида "По времени"
  level1Departments.value = []; // Сброс данных вида "По заказчикам"
}
```

**Действия:**
1. Добавить сброс `level4Data` в функцию `resetPopup()`
2. Убедиться, что все состояния сбрасываются корректно

**Ожидаемый результат:**
- Функция `resetPopup()` сбрасывает уровень 4
- Все состояния сбрасываются корректно

#### 3.3.4. Обновление функции initializeLevel1()

**Текущая реализация:**
```javascript
function initializeLevel1() {
  // ...
  // Сброс уровней 2 и 3
  level2Data.value = null;
  level3Data.value = null;
  popupLevel.value = 1;
  viewMode.value = 'employees';
  // ...
}
```

**Обновленная реализация:**
```javascript
function initializeLevel1() {
  // ...
  // Сброс уровней 2, 3 и 4
  level2Data.value = null;
  level3Data.value = null;
  level4Data.value = null; // Добавить сброс уровня 4
  popupLevel.value = 1;
  viewMode.value = 'employees';
  // ...
}
```

**Действия:**
1. Добавить сброс `level4Data` в функцию `initializeLevel1()`
2. Убедиться, что при инициализации уровня 1 все остальные уровни сбрасываются

**Ожидаемый результат:**
- Функция `initializeLevel1()` сбрасывает уровень 4
- Инициализация работает корректно

**Критерии приёмки задачи 3.3:**
- [ ] Функция `goBack()` обновлена для поддержки уровня 4
- [ ] Функция `goToLevel4()` реализована
- [ ] Функция `resetPopup()` обновлена
- [ ] Функция `initializeLevel1()` обновлена
- [ ] Обработка ошибок реализована
- [ ] Логирование добавлено

---

### Задача 3.4: Обновление computed-свойств

**Цель:** Обновить computed-свойства для поддержки уровня 4.

#### 3.4.1. Обновление canGoBack

**Текущая реализация:**
```javascript
const canGoBack = computed(() => {
  return popupLevel.value > 1;
});
```

**Обновленная реализация:**
```javascript
/**
 * Можно ли вернуться на предыдущий уровень
 * 
 * Возвращает true, если текущий уровень > 1
 */
const canGoBack = computed(() => {
  return popupLevel.value > 1;
});
```

**Действия:**
1. Проверить, что `canGoBack` работает корректно для уровня 4
2. Добавить комментарий (если нужно)

**Ожидаемый результат:**
- `canGoBack` работает корректно для всех уровней (включая 4)

#### 3.4.2. Обновление popupTitle

**Текущая реализация:**
```javascript
const popupTitle = computed(() => {
  switch (popupLevel.value) {
    case 1:
      return level1Data.value?.stageName || props.stageName;
    case 2:
      return level2Data.value?.employeeName || '';
    case 3:
      return `${level2Data.value?.employeeName || ''} — ${level3Data.value?.dateCategoryLabel || ''}`;
    default:
      return '';
  }
});
```

**Обновленная реализация:**
```javascript
/**
 * Заголовок попапа в зависимости от текущего уровня
 * 
 * Формирует заголовок на основе контекста уровня
 */
const popupTitle = computed(() => {
  switch (popupLevel.value) {
    case 1:
      return level1Data.value?.stageName || props.stageName;
    case 2:
      return level2Data.value?.employeeName || '';
    case 3:
      return `${level2Data.value?.employeeName || ''} — ${level3Data.value?.dateCategoryLabel || ''}`;
    case 4:
      // Формируем заголовок на основе контекста уровня 4
      if (!level4Data.value?.context) {
        return 'Список тикетов';
      }
      
      const ctx = level4Data.value.context;
      const parts = [];
      
      // Добавить имя сотрудника (если есть)
      if (ctx.employeeName) {
        parts.push(ctx.employeeName);
      }
      
      // Добавить категорию давности (если есть)
      if (ctx.dateCategoryLabel) {
        parts.push(ctx.dateCategoryLabel);
      }
      
      // Добавить заказчика (если есть)
      if (ctx.departmentName) {
        parts.push(ctx.departmentName);
      }
      
      // Добавить название стадии
      if (ctx.stageName) {
        parts.push(ctx.stageName);
      }
      
      return parts.length > 0 ? parts.join(' — ') : 'Список тикетов';
    default:
      return '';
  }
});
```

**Действия:**
1. Добавить обработку `case 4` в `popupTitle`
2. Реализовать формирование заголовка на основе контекста
3. Обработать все варианты контекста (с сотрудником, без сотрудника, с заказчиком, без заказчика)

**Ожидаемый результат:**
- `popupTitle` формирует корректный заголовок для уровня 4
- Все варианты контекста обработаны

#### 3.4.3. Добавление computed-свойства для уровня 4 (опционально)

**Свойство для проверки наличия данных уровня 4:**
```javascript
/**
 * Есть ли данные уровня 4
 */
const hasLevel4Data = computed(() => {
  return level4Data.value !== null && level4Data.value !== undefined;
});

/**
 * Загружаются ли данные уровня 4
 */
const isLevel4Loading = computed(() => {
  return level4Data.value?.isLoading === true;
});

/**
 * Есть ли тикеты на уровне 4
 */
const hasLevel4Tickets = computed(() => {
  return level4Data.value?.tickets?.length > 0;
});
```

**Действия:**
1. Добавить computed-свойства для удобной проверки состояния уровня 4
2. Использовать их в шаблоне (если нужно)

**Ожидаемый результат:**
- Computed-свойства добавлены (если нужны)
- Удобная проверка состояния уровня 4

**Критерии приёмки задачи 3.4:**
- [ ] `canGoBack` работает корректно для уровня 4
- [ ] `popupTitle` обновлен для уровня 4
- [ ] Заголовок формируется корректно для всех вариантов контекста
- [ ] Дополнительные computed-свойства добавлены (если нужны)

---

## 📚 Зависимости

### Существующие файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа (основной файл для изменений)

### Новые файлы (из предыдущих этапов)

- `vue-app/src/utils/graph-state/ticketListUtils.js` — утилиты для работы со списком тикетов
  - `filterTicketsByContext()` — фильтрация тикетов по контексту
  - `prepareTicketsForDisplay()` — подготовка тикетов для отображения

### Импорты

```javascript
// Динамический импорт (в функции goToLevel4)
import { filterTicketsByContext, prepareTicketsForDisplay } from '@/utils/graph-state/ticketListUtils.js';

// Существующие импорты
import { useNotifications } from '@/composables/useNotifications.js';
```

---

## ✅ Критерии приёмки этапа 3

- [x] `level4Data` добавлен в состояние компонента
- [x] Структура данных уровня 4 определена и задокументирована
- [x] Функция `goBack()` обновлена для поддержки уровня 4
- [x] Функция `goToLevel4()` реализована
- [x] Функция `resetPopup()` обновлена
- [x] Функция `initializeLevel1()` обновлена
- [x] Computed-свойства обновлены для уровня 4
- [x] Обработка ошибок реализована
- [x] Логирование добавлено для отладки
- [x] Код соответствует стандартам проекта
- [x] JSDoc комментарии добавлены для всех функций

**Статус:** ✅ Завершена (2025-12-12, UTC+3, Брест)

---

## 🧪 Тестирование

### Тест 1: Добавление level4Data

**Сценарий:**
1. Открыть компонент `EmployeeDetailsModal.vue`
2. Проверить, что `level4Data` объявлен
3. Проверить, что начальное значение `null`

**Ожидаемый результат:**
- `level4Data` объявлен как `ref(null)`
- Комментарий добавлен

### Тест 2: Переход на уровень 4

**Сценарий:**
1. Создать контекст перехода
2. Вызвать `goToLevel4(context)`
3. Проверить, что `popupLevel.value === 4`
4. Проверить, что `level4Data.value` содержит данные

**Ожидаемый результат:**
- `popupLevel` установлен в 4
- `level4Data` содержит контекст и тикеты
- Тикеты подготовлены для отображения

### Тест 3: Возврат с уровня 4

**Сценарий:**
1. Перейти на уровень 4 из уровня 2
2. Вызвать `goBack()`
3. Проверить, что `popupLevel.value === 2`
4. Проверить, что `level4Data.value === null`

**Ожидаемый результат:**
- Возврат на уровень 2 работает корректно
- `level4Data` сброшен

### Тест 4: Возврат с уровня 4 (разные sourceLevel)

**Сценарий:**
1. Перейти на уровень 4 из уровня 1
2. Вызвать `goBack()`
3. Проверить, что возврат на уровень 1 работает

**Ожидаемый результат:**
- Возврат работает для всех вариантов `sourceLevel`

### Тест 5: Сброс состояния

**Сценарий:**
1. Перейти на уровень 4
2. Вызвать `resetPopup()`
3. Проверить, что все уровни сброшены

**Ожидаемый результат:**
- `popupLevel === 1`
- Все `level*Data` равны `null`

### Тест 6: Формирование заголовка уровня 4

**Сценарий:**
1. Перейти на уровень 4 с разными контекстами
2. Проверить формирование `popupTitle`

**Ожидаемый результат:**
- Заголовок формируется корректно для всех вариантов контекста

---

## 📝 История правок

- **2025-12-12 11:51 (UTC+3, Брест):** Создана задача TASK-034-03
  - Определены 4 подзадачи с детальными шагами
  - Добавлены примеры кода и структуры данных
  - Описаны критерии приёмки и тесты

- **2025-12-12 (UTC+3, Брест):** Выполнена задача TASK-034-03
  - Добавлено состояние `level4Data` в компонент `EmployeeDetailsModal.vue`
  - Обновлен комментарий к `popupLevel` для поддержки уровня 4
  - Реализована функция `goToLevel4(context)` для перехода на уровень 4:
    - Фильтрация тикетов по контексту
    - Подготовка тикетов для отображения через `prepareTicketsForDisplay()`
    - Сохранение данных в `level4Data`
    - Установка `popupLevel = 4`
    - Обработка ошибок с возвратом на предыдущий уровень
  - Обновлена функция `goBack()` для поддержки возврата с уровня 4:
    - Определение предыдущего уровня на основе `sourceLevel` из контекста
    - Сброс `level4Data` при возврате
    - Сброс `level4Data` при возврате с уровней 2 и 3
  - Обновлена функция `resetPopup()` для сброса уровня 4
  - Обновлена функция `initializeLevel1()` для сброса уровня 4
  - Обновлено computed-свойство `popupTitle` для формирования заголовка уровня 4:
    - Формирование заголовка на основе контекста (сотрудник, категория, заказчик, стадия)
    - Обработка всех вариантов контекста
  - Добавлены JSDoc комментарии для всех функций
  - Добавлено логирование для отладки

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)


