# TASK-053: Изменение формата выбора периода и внедрение 3-месячного режима в модуле «График приёма и закрытий сектора 1С»

**Дата создания:** 2025-12-17 13:26 (UTC+3, Брест)  
**Статус:** 📋 Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связанные модули:** 
- График приёма и закрытий сектора 1С (TASK-041) — базовый модуль
- TASK-048 — Линейный график за 4 недели
- TASK-051 — Улучшения модуля

---

## 📋 Описание

Реализовать изменение формата выбора периода в модуле «График приёма и закрытий сектора 1С»: вместо выбора конкретных недель через барабан прокрутки внедрить выбор между двумя режимами:
1. **4 последние недели** (текущий режим)
2. **3 последних месяца** (новый режим)

При первом входе в модуль показывать информационный попап с объяснением различий между режимами.

Внедрить полноценный интерфейс для 3-месячного режима с соответствующей логикой агрегации данных, визуализацией и отдельным прелоадером с глубокой детализацией загрузки.

---

## 🎯 Контекст

### Текущее состояние модуля

**Текущий формат выбора периода:**
- Используется компонент `WeekPicker` с барабаном прокрутки
- Выбор конкретной недели из списка (52 недели назад + 4 недели вперёд)
- Загрузка данных за 4 недели (выбранная + 3 предыдущие)
- Данные агрегируются по неделям

**Проблема:**
- Глобально не нужен выбор конкретных недель
- Пользователям нужен выбор между двумя режимами: недельный и месячный
- Текущий подход с барабаном прокрутки избыточен

### Требуемое состояние

**Новый формат выбора периода:**
- Переключатель между двумя режимами:
  - **4 последние недели** — текущий режим (сохранить логику)
  - **3 последних месяца** — новый режим (внедрить)
- При первом входе в модуль показывать попап с объяснением различий
- Сохранять выбранный режим в localStorage

---

## 📊 Часть 1: Изменение формата выбора периода

### Этап 1.1: Создание переключателя режимов

**Задачи:**
1. **Создать компонент переключателя режимов:**
   - Компонент: `PeriodModeSelector.vue`
   - Расположение: `vue-app/src/components/graph-admission-closure/PeriodModeSelector.vue`
   - Два варианта: "4 последние недели" и "3 последних месяца"
   - Визуально: radio buttons или toggle switch
   - Сохранение выбора в localStorage

2. **Интегрировать в `GraphAdmissionClosureDashboard.vue`:**
   - Заменить `WeekPicker` на `PeriodModeSelector`
   - Убрать проп `weekPickerMode` из `FiltersPanel`
   - Добавить состояние `periodMode: 'weeks' | 'months'`
   - Обновить логику загрузки данных в зависимости от режима

3. **Обновить `FiltersPanel.vue`:**
   - Убрать логику `weekPickerMode` (или оставить для обратной совместимости)
   - Добавить поддержку нового переключателя режимов

**Критерии приёмки:**
- [ ] Переключатель режимов отображается в интерфейсе
- [ ] Выбор режима сохраняется в localStorage
- [ ] При переключении режима загружаются соответствующие данные
- [ ] Обратная совместимость: старый режим "4 недели" работает как раньше

### Этап 1.2: Создание информационного попапа

**Задачи:**
1. **Создать компонент попапа:**
   - Компонент: `PeriodModeInfoModal.vue`
   - Расположение: `vue-app/src/components/graph-admission-closure/PeriodModeInfoModal.vue`
   - Содержимое:
     - Заголовок: "Выбор режима отображения"
     - Описание режима "4 последние недели"
     - Описание режима "3 последних месяца"
     - Кнопка "Понятно" (закрывает попап)
     - Чекбокс "Не показывать снова" (сохраняет в localStorage)

2. **Логика показа попапа:**
   - Показывать только при первом входе в модуль
   - Проверять флаг в localStorage: `graph-admission-closure-period-mode-info-shown`
   - Если флаг установлен — не показывать
   - При установке чекбокса "Не показывать снова" — установить флаг

3. **Интегрировать в `GraphAdmissionClosureDashboard.vue`:**
   - Добавить состояние `showPeriodModeInfo: ref(false)`
   - Проверять флаг при монтировании компонента
   - Показывать попап при первом входе

**Критерии приёмки:**
- [ ] Попап показывается при первом входе в модуль
- [ ] Попап содержит описание обоих режимов
- [ ] Чекбокс "Не показывать снова" работает корректно
- [ ] После закрытия попап больше не показывается (если установлен флаг)

**Пример содержимого попапа:**

```
┌─────────────────────────────────────────────────────────┐
│  Выбор режима отображения                                │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  📅 4 последние недели                                    │
│  Отображение данных за последние 4 недели с детализацией│
│  по неделям. Подходит для краткосрочного анализа.        │
│                                                           │
│  📊 3 последних месяца                                    │
│  Отображение данных за последние 3 месяца с детализацией│
│  по месяцам и неделям внутри месяцев. Подходит для      │
│  долгосрочного анализа и трендов.                        │
│                                                           │
│  [☐] Не показывать снова                                  │
│                                                           │
│  [Понятно]                                                │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Часть 2: Внедрение 3-месячного режима

### Общее описание 3-месячного режима

**Логика расчета периода:**
- Отсчёт от текущей даты
- Пример: если сегодня декабрь 2025, то анализируются:
  - Декабрь 2025 (текущий месяц)
  - Ноябрь 2025
  - Октябрь 2025
- Период: с 1-го числа первого месяца до последнего дня текущего месяца (включительно)

**Структура данных:**
- Агрегация по месяцам (3 месяца)
- Внутри каждого месяца — разбивка по неделям
- Для линейных графиков: каждый месяц как точка с внутренними неделями

### Этап 2.1: Backend API для 3-месячного режима

**⚠️ КРИТИЧЕСКИ ВАЖНО: Уроки из TASK-054**

**Проблема пагинации, которая была исправлена:**
- В TASK-054 была обнаружена и исправлена критическая ошибка с пагинацией
- Тикеты не попадали в запросы, потому что загружались только первые 50 результатов
- **ОБЯЗАТЕЛЬНО применить правильную пагинацию** ко всем запросам к Bitrix24 API в 3-месячном режиме

**Правильная пагинация (из TASK-054):**
- Использовать проверку: `count($items) === $pageSize && $hasNext`
- Правильно проверять наличие `next`: не null, не пустая строка, не '0', больше 0
- Логировать количество загруженных страниц для диагностики

**Задачи:**
1. **Расширить API endpoint:**
   - Файл: `api/graph-1c-admission-closure.php`
   - Добавить параметр `periodMode: 'weeks' | 'months'`
   - При `periodMode: 'months'`:
     - Вычислять период: последние 3 месяца от текущей даты
     - Агрегировать данные по месяцам
     - Внутри каждого месяца — разбивка по неделям
     - **ОБЯЗАТЕЛЬНО использовать правильную пагинацию для всех запросов** (см. TASK-054)
     - Возвращать структуру данных для 3 месяцев

2. **Структура ответа API для 3-месячного режима:**
   ```json
   {
     "success": true,
     "meta": {
       "periodMode": "months",
       "periodStartUtc": "2025-10-01T00:00:00Z",
       "periodEndUtc": "2025-12-31T23:59:59Z",
       "months": [
         {
           "monthNumber": 10,
           "monthName": "Октябрь",
           "year": 2025,
           "monthStartUtc": "2025-10-01T00:00:00Z",
           "monthEndUtc": "2025-10-31T23:59:59Z",
           "weeks": [
             {
               "weekNumber": 40,
               "weekStartUtc": "2025-09-29T00:00:00Z",
               "weekEndUtc": "2025-10-05T23:59:59Z"
             }
             // ... остальные недели месяца
           ]
         },
         {
           "monthNumber": 11,
           "monthName": "Ноябрь",
           "year": 2025,
           "monthStartUtc": "2025-11-01T00:00:00Z",
           "monthEndUtc": "2025-11-30T23:59:59Z",
           "weeks": [...]
         },
         {
           "monthNumber": 12,
           "monthName": "Декабрь",
           "year": 2025,
           "monthStartUtc": "2025-12-01T00:00:00Z",
           "monthEndUtc": "2025-12-31T23:59:59Z",
           "weeks": [...]
         }
       ]
     },
     "data": {
       // Данные для 3 месяцев
     }
   }
   ```

3. **Агрегация данных по месяцам:**
   - Для каждого месяца:
     - Новые тикеты (сумма за месяц)
     - Закрытые тикеты (всего + помесячно)
     - Переходящие тикеты (всего + помесячно)
     - Закрытия по стадиям (только количества, 3 стадии)
   - Внутри каждого месяца — разбивка по неделям

**Критерии приёмки:**
- [ ] API возвращает корректную структуру для 3-месячного режима
- [ ] Период рассчитывается правильно (последние 3 месяца)
- [ ] Данные агрегируются по месяцам
- [ ] Внутри каждого месяца есть разбивка по неделям
- [ ] Обратная совместимость: режим "4 недели" работает как раньше

### Этап 2.2: Изменения в блоках данных (Summary-карточки)

**Задачи:**

1. **Блок "Новые за неделю" → "Новые за период":**
   - **4 цифры:**
     1. Сумма всего за 3 месяца
     2. Декабрь (текущий месяц)
     3. Ноябрь
     4. Октябрь
   - Визуально: одна большая цифра (сумма) + три маленькие (по месяцам)

2. **Блок "Закрытые всего":**
   - **Структура:**
     - Закрытые всего (сумма за 3 месяца)
     - Деление помесячно (3 цифры: декабрь, ноябрь, октябрь)
     - Деление второго уровня (как именно закрывались):
       - Созданные этой неделей (для каждого месяца)
       - Созданные другой неделей (для каждого месяца)
   - Визуально: иерархическая структура с отступами

3. **Блок "Переходящие всего":**
   - **Структура:**
     - Переходящие всего (сумма за 3 месяца)
     - Деление помесячно (3 цифры: декабрь, ноябрь, октябрь)
   - Визуально: аналогично блоку "Закрытые"

4. **Блок "Закрытия по стадиям":**
   - **Только количества целиком (3 стадии):**
     - Успешное закрытие
     - Отклонено
     - Закрыли без задачи
   - Сумма за 3 месяца (без помесячной разбивки)

**Критерии приёмки:**
- [ ] Все блоки отображают корректные данные для 3 месяцев
- [ ] Визуальная структура соответствует требованиям
- [ ] Иерархия данных понятна пользователю
- [ ] Данные обновляются при переключении режима

**Пример визуализации:**

```
┌─────────────────────────────────────────────────────────┐
│  Новые за период                                         │
│  ┌──────────┐                                            │
│  │   156    │  ← Сумма за 3 месяца                      │
│  └──────────┘                                            │
│    Декабрь: 52  Ноябрь: 48  Октябрь: 56                 │
├─────────────────────────────────────────────────────────┤
│  Закрытые всего                                          │
│  ┌──────────┐                                            │
│  │   134    │  ← Сумма за 3 месяца                      │
│  └──────────┘                                            │
│    Декабрь: 45                                           │
│      ✓ 28 этой неделей  ↻ 17 другой неделей             │
│    Ноябрь: 42                                            │
│      ✓ 25 этой неделей  ↻ 17 другой недели               │
│    Октябрь: 47                                           │
│      ✓ 30 этой неделей  ↻ 17 другой недели               │
├─────────────────────────────────────────────────────────┤
│  Переходящие всего                                       │
│  ┌──────────┐                                            │
│  │    89    │  ← Сумма за 3 месяца                      │
│  └──────────┘                                            │
│    Декабрь: 30  Ноябрь: 28  Октябрь: 31                 │
├─────────────────────────────────────────────────────────┤
│  Закрытия по стадиям                                     │
│  ✓ Успешное закрытие: 98                                │
│  ✗ Отклонено: 24                                        │
│  ⚠ Закрыли без задачи: 12                               │
└─────────────────────────────────────────────────────────┘
```

### Этап 2.3: Линейные графики для 3-месячного режима

**Задачи:**

1. **Изменение расположения графиков:**
   - **Текущее:** графики слева-справа (бок о бок)
   - **Новое:** графики сверху-снизу (по всей ширине экрана)
   - Каждый график занимает полную ширину контейнера
   - Вертикальное расположение: один под другим

2. **Структура линейного графика:**
   - **Ось X:** 3 точки (по одной на месяц)
   - **Внутри каждой точки месяца:** разбивка по неделям
   - **Визуализация:**
     - Основные точки: декабрь, ноябрь, октябрь
     - Внутри каждой точки: маленькие точки/метки для недель
     - Линии соединяют месяцы, внутри месяцев — пунктирные линии для недель

3. **Табличная часть линейного графика:**
   - Под графиком: таблица с детализацией
   - Структура таблицы:
     ```
     ┌─────────────┬──────────┬──────────┬──────────┐
     │ Месяц       │ Неделя 1 │ Неделя 2 │ Неделя 3 │ ...
     ├─────────────┼──────────┼──────────┼──────────┤
     │ Декабрь     │    12    │    15    │    18    │
     │ Ноябрь      │    14    │    16    │    18    │
     │ Октябрь     │    16    │    18    │    20    │
     └─────────────┴──────────┴──────────┴──────────┘
     ```
   - Каждый месяц — строка
   - Каждая неделя внутри месяца — колонка
   - Данные: новые тикеты, закрытые, переходящие

4. **Серии данных для графика:**
   - Новые тикеты (по месяцам)
   - Закрытые тикеты (всего)
   - Переходящие тикеты (всего)
   - Опционально: разбивка закрытых/переходящих (пунктирные линии)

**Критерии приёмки:**
- [ ] Графики расположены сверху-снизу (по ширине экрана)
- [ ] Ось X содержит 3 точки (по месяцам)
- [ ] Внутри каждой точки месяца есть разбивка по неделям
- [ ] Табличная часть отображает детализацию по месяцам и неделям
- [ ] График адаптивен (работает на мобильных устройствах)

**Примечание:** Столбчатый и круговой графики пока не внедряем для 3-месячного режима.

### Этап 2.4: Создание отдельного шаблона для 3-месячного режима

**Задачи:**

1. **Создать компонент для 3-месячного режима:**
   - Компонент: `GraphAdmissionClosureMonthsDashboard.vue`
   - Расположение: `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureMonthsDashboard.vue`
   - Основа: копия `GraphAdmissionClosureDashboard.vue` с адаптацией
   - Логика: работа с месячными данными вместо недельных

2. **Структура компонента:**
   - Аналогична текущему дашборду, но:
     - Работа с месячными данными
     - Другая структура summary-карточек
     - Другие графики (только линейный, расположение сверху-снизу)
     - Другая табличная часть

3. **Условный рендеринг в главном компоненте:**
   - В `GraphAdmissionClosureDashboard.vue`:
     - Если `periodMode === 'weeks'` → показывать текущий компонент
     - Если `periodMode === 'months'` → показывать `GraphAdmissionClosureMonthsDashboard.vue`

**Критерии приёмки:**
- [ ] Создан отдельный компонент для 3-месячного режима
- [ ] Компонент работает независимо от недельного режима
- [ ] Условный рендеринг работает корректно
- [ ] Переключение между режимами происходит без ошибок

### Этап 2.5: Прелоадер с глубокой детализацией для 3-месячного режима

**Задачи:**

1. **Создать компонент прелоадера:**
   - Компонент: `MonthsModePreloader.vue`
   - Расположение: `vue-app/src/components/graph-admission-closure/MonthsModePreloader.vue`
   - Особенности:
     - Глубокая детализация процесса загрузки
     - Показ этапов загрузки (по месяцам)
     - Прогресс-бар с процентами
     - Информативные сообщения

2. **Этапы загрузки:**
   - Этап 1: "Подготовка данных..." (0-10%)
   - Этап 2: "Загрузка данных за октябрь..." (10-40%)
   - Этап 3: "Загрузка данных за ноябрь..." (40-70%)
   - Этап 4: "Загрузка данных за декабрь..." (70-90%)
   - Этап 5: "Агрегация данных..." (90-95%)
   - Этап 6: "Формирование графиков..." (95-100%)

3. **Визуализация прелоадера:**
   - Спиннер (анимация загрузки)
   - Прогресс-бар с процентами
   - Текущий этап (текст)
   - Список этапов с отметками (✓ для завершённых)

4. **Интеграция с API:**
   - Если возможно — получать прогресс от бэкенда
   - Если нет — симулировать прогресс на фронтенде
   - Обновлять этапы по мере загрузки данных

**Критерии приёмки:**
- [ ] Прелоадер показывает глубокую детализацию загрузки
- [ ] Отображаются все этапы загрузки
- [ ] Прогресс-бар обновляется в реальном времени
- [ ] Сообщения информативны и понятны пользователю

**Пример визуализации прелоадера:**

```
┌─────────────────────────────────────────────────────────┐
│  Загрузка данных за 3 месяца                            │
│                                                           │
│  [████████████████░░░░░░░░] 65%                         │
│                                                           │
│  ✓ Подготовка данных...                                  │
│  ✓ Загрузка данных за октябрь...                         │
│  → Загрузка данных за ноябрь...                          │
│    Загрузка данных за декабрь...                         │
│    Агрегация данных...                                   │
│    Формирование графиков...                              │
│                                                           │
│  Пожалуйста, подождите...                                │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Часть 3: Рефакторинг выбора режима в фильтры

### Этап 3.1: Перенос выбора режима в фильтры с визуальным выделением

**Контекст:**
- На стартовой странице при клике на модуль "График приема и закрытий сектора 1С" показывается попап с выбором формата
- После выбора в попапе показывается прелоадер и рендерится интерфейс
- В самих интерфейсах (недельный и месячный) выбор режима должен быть в фильтрах, а не глобально

**Задачи:**

1. **Убрать глобальный компонент выбора режима:**
   - Удалить `PeriodModeSelector` из `GraphAdmissionClosureDashboard.vue`
   - Убрать отдельный блок с переключателем режимов
   - Удалить импорт компонента

2. **Добавить выбор режима в `FiltersPanel.vue`:**
   - Добавить выпадающий список в секцию "Период"
   - Показывать только если `showPeriodMode === true`
   - Опции списка:
     - "4 последние недели" (value: 'weeks')
     - "3 последних месяца" (value: 'months')
   - Визуальное выделение текущего режима:
     - Текущий режим должен быть подсвечен (например, синий фон или жирный шрифт)
     - Текущий режим должен быть недоступен для выбора (disabled)
     - Можно выбрать только другой режим
   - При выборе другого режима:
     - Запускается прелоадер
     - Перерендеривается интерфейс (переключение между недельным и месячным дашбордом)

3. **Логика работы выпадающего списка:**
   - Если загружен интерфейс "4 последние недели":
     - В списке "4 последние недели" подсвечено и disabled
     - "3 последних месяца" доступно для выбора
     - При выборе "3 последних месяца" → прелоадер → месячный интерфейс
   - Если загружен интерфейс "3 последних месяца":
     - В списке "3 последних месяца" подсвечено и disabled
     - "4 последние недели" доступно для выбора
     - При выборе "4 последние недели" → прелоадер → недельный интерфейс

4. **Обновить логику в дашбордах:**
   - В `GraphAdmissionClosureDashboard.vue`:
     - Передать `showPeriodMode="true"` и `period-mode="periodMode"` в `FiltersPanel`
     - Обработать событие `@update:period-mode` для переключения режима
   - В `GraphAdmissionClosureMonthsDashboard.vue`:
     - Передать `showPeriodMode="true"` и `period-mode="'months'"` в `FiltersPanel`
     - Обработать событие `@update:period-mode` для переключения на недельный режим
     - Использовать глобальное событие для уведомления родительского компонента

5. **Визуальное оформление:**
   - Выпадающий список в стиле Bitrix24
   - Подсветка текущего режима (синий фон или жирный шрифт)
   - Disabled состояние для текущего режима (серый цвет, курсор not-allowed)
   - Hover-эффекты для доступных опций

**Критерии приёмки:**
- [ ] Глобальный компонент `PeriodModeSelector` удалён из интерфейса
- [ ] Выбор режима находится в фильтрах в секции "Период"
- [ ] Выпадающий список показывает оба режима
- [ ] Текущий режим визуально подсвечен и недоступен для выбора
- [ ] При выборе другого режима запускается прелоадер
- [ ] После загрузки перерендеривается соответствующий интерфейс
- [ ] Переключение работает в обе стороны (weeks ↔ months)
- [ ] Режим сохраняется в localStorage при переключении
- [ ] Визуальное оформление соответствует гайдлайнам Bitrix24

**Пример визуализации выпадающего списка:**

```
┌─────────────────────────────────────────────────────────┐
│  Фильтры                                                 │
├─────────────────────────────────────────────────────────┤
│  📅 Период                                               │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ Режим отображения:                                   │ │
│  │ ┌───────────────────────────────────────────────┐ │ │
│  │ │ ▼ 4 последние недели [подсвечено, disabled]   │ │ │
│  │ │   3 последних месяца [доступно для выбора]    │ │ │
│  │ └───────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ Последняя неделя                                     │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

**Технические детали:**

1. **Props для `FiltersPanel.vue`:**
   ```typescript
   {
     showPeriodMode: boolean,  // Показывать выбор режима
     periodMode: 'weeks' | 'months'  // Текущий режим
   }
   ```

2. **Emits для `FiltersPanel.vue`:**
   ```typescript
   {
     'update:periodMode': (mode: 'weeks' | 'months') => void
   }
   ```

3. **Логика disabled состояния:**
   ```vue
   <select>
     <option 
       :value="'weeks'" 
       :disabled="periodMode === 'weeks'"
       :class="{ 'option-current': periodMode === 'weeks' }"
     >
       4 последние недели
     </option>
     <option 
       :value="'months'" 
       :disabled="periodMode === 'months'"
       :class="{ 'option-current': periodMode === 'months' }"
     >
       3 последних месяца
     </option>
   </select>
   ```

4. **Обработка переключения:**
   - При изменении значения в списке:
     - Проверять, что выбранный режим отличается от текущего
     - Если отличается → запустить прелоадер → переключить режим → перерендерить интерфейс
     - Если совпадает → игнорировать (не должно происходить, так как disabled)

---

## 🔧 Технические требования

### Технологии

- **Vue.js:** 3.x (Composition API)
- **Chart.js:** через `vue-chartjs` (для графиков)
- **LocalStorage:** для сохранения выбора режима и флага попапа

### Структура файлов

```
vue-app/src/
├── components/
│   └── graph-admission-closure/
│       ├── GraphAdmissionClosureDashboard.vue          # Главный компонент (обновить)
│       ├── GraphAdmissionClosureMonthsDashboard.vue    # Новый: 3-месячный режим
│       ├── PeriodModeSelector.vue                       # Новый: переключатель режимов
│       ├── PeriodModeInfoModal.vue                      # Новый: информационный попап
│       ├── MonthsModePreloader.vue                      # Новый: прелоадер для 3 месяцев
│       ├── GraphAdmissionClosureChart.vue               # Обновить: поддержка 3 месяцев
│       └── ... (остальные компоненты без изменений)
├── services/
│   └── graph-admission-closure/
│       └── admissionClosureService.js                   # Обновить: поддержка periodMode
└── ...

api/
└── graph-1c-admission-closure.php                      # Обновить: поддержка periodMode
```

### API контракт

**Запрос:**
```json
{
  "product": "1C",
  "periodMode": "months",  // Новый параметр: "weeks" | "months"
  "useCache": true,
  "forceRefresh": false,
  "includeTickets": false,
  // ... остальные параметры
}
```

**Ответ для 3-месячного режима:**
```json
{
  "success": true,
  "meta": {
    "periodMode": "months",
    "periodStartUtc": "2025-10-01T00:00:00Z",
    "periodEndUtc": "2025-12-31T23:59:59Z",
    "months": [...]
  },
  "data": {
    "newTickets": 156,
    "newTicketsByMonth": [
      { "month": 10, "count": 56 },
      { "month": 11, "count": 48 },
      { "month": 12, "count": 52 }
    ],
    "closedTickets": 134,
    "closedTicketsByMonth": [...],
    "carryoverTickets": 89,
    "carryoverTicketsByMonth": [...],
    "stages": [...],
    // ... остальные данные
  }
}
```

### Обратная совместимость

**Важно:** Все изменения должны быть обратно совместимыми:
- Режим "4 недели" должен работать как раньше
- Старые компоненты не должны ломаться
- API должен поддерживать оба режима

---

## 📝 Критерии приёмки (общие)

### Часть 1: Изменение формата выбора периода

- [ ] Переключатель режимов отображается в интерфейсе
- [ ] Выбор режима сохраняется в localStorage
- [ ] Информационный попап показывается при первом входе
- [ ] Попап можно скрыть с флагом "Не показывать снова"
- [ ] Переключение между режимами работает корректно
- [ ] Режим "4 недели" работает как раньше (обратная совместимость)

### Часть 2: Внедрение 3-месячного режима

- [ ] API возвращает корректную структуру для 3-месячного режима
- [ ] Период рассчитывается правильно (последние 3 месяца)
- [ ] **ОБЯЗАТЕЛЬНО: Правильная пагинация реализована для всех запросов к Bitrix24 API** (из TASK-054)
- [ ] Все тикеты загружаются полностью (не только первые 50 результатов)
- [ ] Логирование количества загруженных страниц работает корректно
- [ ] Summary-карточки отображают корректные данные
- [ ] Линейные графики расположены сверху-снизу
- [ ] Графики содержат 3 точки (по месяцам) с разбивкой по неделям
- [ ] Табличная часть отображает детализацию по месяцам и неделям
- [ ] Прелоадер показывает глубокую детализацию загрузки
- [ ] Отдельный компонент для 3-месячного режима работает корректно
- [ ] Все данные агрегируются правильно по месяцам

### Общие критерии

- [ ] Код соответствует стандартам проекта
- [ ] Все компоненты протестированы
- [ ] Обратная совместимость сохранена
- [ ] Документация обновлена
- [ ] Производительность приемлема (загрузка 3 месяцев не должна быть слишком долгой)

---

## 📅 Порядок выполнения

**Рекомендуемый порядок:**

### Фаза 1: Изменение формата выбора периода (Часть 1)

1. **Этап 1.1:** Создание переключателя режимов
2. **Этап 1.2:** Создание информационного попапа

**Оценка:** 2-3 дня

### Фаза 2: Backend для 3-месячного режима (Часть 2, Этап 2.1)

3. **Этап 2.1:** Backend API для 3-месячного режима

**Оценка:** 3-4 дня

### Фаза 3: Frontend для 3-месячного режима (Часть 2, Этапы 2.2-2.5)

4. **Этап 2.2:** Изменения в блоках данных
5. **Этап 2.3:** Линейные графики для 3-месячного режима
6. **Этап 2.4:** Создание отдельного шаблона
7. **Этап 2.5:** Прелоадер с глубокой детализацией

**Оценка:** 5-7 дней

**Общая оценка:** 10-14 дней

---

## 🔗 Связанные документы

- [TASK-041: График приёма и закрытий сектора 1С](./TASK-041-02-backend-contract-and-data.md)
- [TASK-048: Линейный график за 4 недели](./TASK-048-line-chart-4-weeks-no-popups.md)
- [TASK-051: Улучшения модуля](./TASK-051-graph-admission-closure-improvements.md)
- **[TASK-054: Анализ проблемы с отображением тикетов](./TASK-054-analysis-missing-tickets.md)** ⚠️ **КРИТИЧЕСКИ ВАЖНО: Исправление пагинации**
- [Гайдлайн модуля](../GUIDES/module-graph-admission-closure-guide.md)

---

## 📝 История правок

- **2025-12-17 13:26 (UTC+3, Брест):** Создана задача TASK-053
  - Определены требования для изменения формата выбора периода
  - Детализированы этапы внедрения 3-месячного режима
  - Зафиксированы технические требования и критерии приёмки
- **2025-12-17 14:30 (UTC+3, Брест):** Интегрированы уроки из TASK-054
  - ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Добавлены требования о правильной пагинации из TASK-054
  - Обновлены все разделы, связанные с запросами к Bitrix24 API
  - Добавлены предупреждения о необходимости правильной пагинации
  - Обновлены критерии приёмки с требованием проверки пагинации

- **2025-12-17 15:15 (UTC+3, Брест):** Добавлена Часть 3: Рефакторинг выбора режима в фильтры
  - Создан новый этап 3.1: Перенос выбора режима в фильтры с визуальным выделением
  - Определены требования для переноса выбора режима из глобального компонента в панель фильтров
  - Добавлены требования для визуального выделения текущего режима и disabled состояния
  - Создана подзадача TASK-053-08 с детальным описанием реализации

---

## 🔍 Дополнительные детали реализации

### Интеграция компонентов

**Порядок интеграции:**
1. Сначала реализовать переключатель режимов (TASK-053-01)
2. Затем информационный попап (TASK-053-02)
3. После этого Backend API (TASK-053-03)
4. Затем Frontend компоненты (TASK-053-04, 05, 06, 07)
5. Рефакторинг выбора режима в фильтры (TASK-053-08)

**Условный рендеринг:**
- Использовать `v-if` / `v-else` для переключения между режимами
- Не монтировать оба компонента одновременно (экономия ресурсов)
- Сохранять состояние при переключении (опционально)

### Обработка ошибок

**Уровни обработки ошибок:**
1. **API уровень:** Обработка ошибок запросов к Bitrix24
2. **Компонентный уровень:** Обработка ошибок рендеринга
3. **Пользовательский уровень:** Понятные сообщения об ошибках

**Типы ошибок:**
- Ошибки сети (таймауты, недоступность API)
- Ошибки данных (некорректный формат ответа)
- Ошибки валидации (некорректные параметры)

### Производительность

**Оптимизация загрузки:**
- Кеширование данных на фронтенде (sessionStorage)
- Lazy loading компонентов
- Code splitting для уменьшения размера бандла

**Оптимизация рендеринга:**
- Использовать `v-memo` для тяжёлых компонентов
- Виртуализация длинных списков
- Debounce для частых обновлений

**Оптимизация запросов:**
- Batch-запросы к Bitrix24 API
- Пагинация при больших объёмах данных
- Кеширование на бэкенде

### Тестирование

**Unit-тесты:**
- Тесты для компонентов Vue
- Тесты для функций форматирования
- Тесты для логики вычислений

**Интеграционные тесты:**
- Тесты взаимодействия компонентов
- Тесты API endpoints
- Тесты переключения режимов

**E2E тесты:**
- Тесты пользовательских сценариев
- Тесты на реальных данных
- Тесты производительности

## 💡 Примечания

- **Приоритет этапов:** Фаза 1 (изменение формата выбора) имеет высокий приоритет, так как это основа для дальнейшей работы
- **Тестирование:** Все изменения должны быть протестированы на реальных данных
- **Производительность:** Загрузка данных за 3 месяца может быть медленнее, чем за 4 недели — необходимо оптимизировать запросы
- **Обратная совместимость:** Критически важно сохранить работу режима "4 недели" без изменений
- **Документация:** При добавлении новых компонентов необходимо обновлять гайдлайн модуля
- **Мониторинг:** Добавить логирование для отслеживания использования режимов
- **Аналитика:** Собирать метрики использования каждого режима для принятия решений

