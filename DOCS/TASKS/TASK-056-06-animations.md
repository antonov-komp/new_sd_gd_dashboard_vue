# TASK-056-06: Добавление анимаций для графиков

**Дата создания:** 2025-12-17 16:42 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-056: Улучшение дизайна линейных графиков](./TASK-056-line-charts-design-improvements.md)  
**Этап:** 6/7

---

## 📋 Описание

Добавить плавные анимации для линейных графиков модуля «График приёма и закрытий сектора 1С»: анимацию загрузки графика, анимацию hover для точек и линий, и плавное появление tooltip.

---

## 🎯 Контекст

**Текущее состояние:**
- Нет анимации загрузки графика
- Нет анимации hover для точек и линий
- Tooltip появляется сразу без анимации

**Проблема:**
- График появляется резко, нет плавности
- Нет визуальной обратной связи при взаимодействии
- Tooltip появляется слишком быстро

**Требуемое состояние:**
- Плавная анимация загрузки графика (800ms, ease-out)
- Анимация рисования линий от левого края к правому
- Плавное увеличение точек при hover
- Подсветка линии при hover (увеличение толщины на 1px)
- Плавное появление tooltip (300ms, fade-in)
- Задержка появления tooltip (200ms)

---

## 🔧 Технические требования

### 1. Анимация загрузки графика

**Требования:**
- Длительность: `800ms`
- Easing: `ease-out`
- Анимация рисования линий: от левого края к правому
- ✅ **Факт из TASK-053:** В MonthsModePreloader.vue используются плавные CSS transitions — применить аналогичный подход

**Реализация:**
- Использовать настройки Chart.js `animation`
- Настроить `animation.duration` и `animation.easing`

### 2. Анимация hover для точек

**Требования:**
- Плавное увеличение точек при наведении на линию
- Радиус точки увеличивается с `0` до `5px` (основные) или `4px` (вспомогательные)
- Длительность: `200ms`
- Easing: `ease-out`

**Реализация:**
- Использовать `pointHoverRadius` в конфигурации datasets
- Chart.js автоматически анимирует изменение радиуса

### 3. Анимация hover для линий

**Требования:**
- Подсветка линии при hover (увеличение толщины на `1px`)
- Длительность: `200ms`
- Easing: `ease-out`

**Реализация:**
- Использовать кастомный плагин Chart.js для hover-эффекта
- Или использовать события Chart.js

### 4. Анимация tooltip

**Требования:**
- Плавное появление tooltip с fade-in эффектом
- Длительность: `300ms`
- Задержка появления: `200ms` (не сразу при hover)
- Easing: `ease-out`

**Реализация:**
- Использовать настройки Chart.js `tooltip.animation`
- Настроить задержку через CSS или Chart.js

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Настроить анимацию загрузки

1. **Открыть файл:**
   - `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`

2. **Обновить `chartOptions.computed` с полной конфигурацией анимации:**
   ```javascript
   const chartOptions = computed(() => ({
     responsive: true,
     maintainAspectRatio: false,
     resizeDelay: 0,
     
     // Настройки анимации загрузки графика
     animation: {
       duration: 800, // Длительность анимации в миллисекундах
       easing: 'ease-out', // Тип easing (ease-out для плавного завершения)
       // Опционально: можно настроить анимацию для разных элементов
       // x: { duration: 800 }, // Анимация по оси X
       // y: { duration: 800 }, // Анимация по оси Y
     },
     
     // Настройки взаимодействия (важно для hover-эффектов)
     interaction: {
       intersect: false, // Показывать tooltip при приближении к точке
       mode: 'index' // Показывать все серии для текущего индекса
     },
     
     // ... остальные настройки (plugins, scales)
   }));
   ```

3. **Важно:** 
   - Анимация применяется только при первой загрузке графика
   - При обновлении данных анимация не воспроизводится (если не указано иное)
   - Для повторной анимации при обновлении данных использовать `chart.update('active')`

### Шаг 2: Настроить анимацию hover для точек

1. **Точки уже настроены в TASK-056-01:**
   - `pointRadius: 0` (скрыты по умолчанию)
   - `pointHoverRadius: 5` (для основных) или `4` (для вспомогательных)
   - Chart.js автоматически анимирует изменение радиуса

2. **Проверить плавность анимации:**
   - Убедиться, что анимация плавная
   - При необходимости настроить через CSS transitions

### Шаг 3: Реализовать анимацию hover для линий

1. **Создать кастомный плагин для подсветки линий:**
   ```javascript
   // В script setup компонента, перед определением chartOptions
   import { Chart } from 'chart.js';
   
   const hoverLinePlugin = {
     id: 'hoverLine',
     afterEvent: (chart, args) => {
       const { event, inChartArea, changed } = args;
       
       if (!inChartArea || !changed) {
         // Сброс подсветки при уходе курсора
         chart.data.datasets.forEach((dataset, index) => {
           const meta = chart.getDatasetMeta(index);
           if (meta.hoverBorderWidth) {
             meta.hoverBorderWidth = undefined;
           }
         });
         chart.update('none'); // Обновление без анимации
         return;
       }
       
       // Получаем точку под курсором
       const points = chart.getElementsAtEventForMode(event, 'index', { intersect: false });
       
       if (points.length > 0) {
         const point = points[0];
         const datasetIndex = point.datasetIndex;
         const meta = chart.getDatasetMeta(datasetIndex);
         
         // Увеличиваем толщину линии на 1px при hover
         if (!meta.hoverBorderWidth) {
           meta.hoverBorderWidth = meta.borderWidth + 1;
           chart.update('none');
         }
       }
     }
   };
   
   // Зарегистрировать плагин (один раз, при инициализации компонента)
   Chart.register(hoverLinePlugin);
   ```

2. **Альтернативный подход (проще, но менее гибкий):**
   - Использовать CSS hover через глобальные стили
   - Или использовать события Chart.js через `onHover` в конфигурации (если доступно)

3. **Оптимизация производительности:**
   - Использовать `chart.update('none')` для быстрого обновления без анимации
   - Проверять `changed` флаг, чтобы не обновлять график без необходимости
   - Кэшировать состояние hover, чтобы избежать лишних обновлений

### Шаг 4: Настроить анимацию tooltip

1. **Обновить настройки tooltip с анимацией:**
   ```javascript
   tooltip: {
     // ... другие настройки
     animation: {
       duration: 300, // Длительность анимации появления
       easing: 'ease-out'
     },
     // Задержка появления (если поддерживается версией Chart.js)
     // delay: 200, // Задержка в миллисекундах
   }
   ```

2. **Добавить задержку появления через кастомный плагин (если встроенная не работает):**
   ```javascript
   const tooltipDelayPlugin = {
     id: 'tooltipDelay',
     beforeInit: (chart) => {
       let hoverTimeout = null;
       
       // Переопределяем обработчик hover
       const originalOnHover = chart.options.onHover;
       chart.options.onHover = (event, activeElements) => {
         // Очищаем предыдущий таймер
         if (hoverTimeout) {
           clearTimeout(hoverTimeout);
         }
         
         // Устанавливаем задержку
         hoverTimeout = setTimeout(() => {
           if (originalOnHover) {
             originalOnHover.call(chart, event, activeElements);
           }
         }, 200); // Задержка 200ms
       };
     }
   };
   
   Chart.register(tooltipDelayPlugin);
   ```

3. **Альтернатива: CSS для задержки (если плагин не работает):**
   ```css
   /* Глобальные стили для tooltip Chart.js */
   .chartjs-tooltip {
     transition: opacity 0.3s ease-out 0.2s; /* Задержка 200ms */
   }
   ```

4. **Важно:**
   - Задержка tooltip улучшает UX (не появляется при случайном наведении)
   - Анимация должна быть плавной, но не слишком медленной
   - Проверить работу на разных браузерах

---

## 🔍 Детали реализации

### Файлы для изменения

1. **`vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`**
   - Обновить `chartOptions.computed`
   - Добавить настройки анимации

### Применение к обоим графикам

**Левый график (Новые и Закрытые):**
- Анимации должны работать для всех серий
- Плавная загрузка всех линий

**Правый график (Переходящие):**
- Анимации должны работать для всех серий
- Плавная загрузка всех линий

### Оптимизация производительности

**Рекомендации:**
- Использовать CSS transitions вместо JavaScript анимаций (где возможно)
- Использовать `will-change` CSS-свойство для анимируемых элементов
- Избегать лишних перерисовок
- ✅ **Факт из TASK-053:** В MonthsModePreloader.vue используется оптимизация анимаций через CSS transitions

---

## 💻 Примеры кода

### Обновлённая конфигурация анимаций

```javascript
const chartOptions = computed(() => ({
  responsive: true,
  maintainAspectRatio: false,
  resizeDelay: 0,
  animation: {
    duration: 800,
    easing: 'ease-out'
  },
  interaction: {
    intersect: false,
    mode: 'index'
  },
  plugins: {
    tooltip: {
      // ... другие настройки
      animation: {
        duration: 300
      },
      // Задержка появления (через CSS или Chart.js)
    }
  }
}));
```

### Кастомный плагин для hover-эффекта линий

```javascript
// Опционально: кастомный плагин для подсветки линии при hover
const hoverLinePlugin = {
  id: 'hoverLine',
  afterEvent: (chart, args) => {
    const { event, inChartArea } = args;
    
    if (!inChartArea) {
      // Сброс подсветки при уходе курсора
      chart.data.datasets.forEach((dataset, index) => {
        const meta = chart.getDatasetMeta(index);
        if (meta.hoverBorderWidth) {
          meta.hoverBorderWidth = undefined;
        }
      });
      chart.update('none');
      return;
    }
    
    // Логика подсветки линии при hover
    // Можно увеличить толщину линии на 1px
  }
};

// Зарегистрировать плагин
Chart.register(hoverLinePlugin);
```

### CSS для плавных переходов

```css
/* Опционально: добавить CSS transitions для плавности */
.chart-container {
  will-change: transform;
}

.chart-container canvas {
  transition: opacity 0.3s ease-out;
}
```

---

## ✅ Критерии приёмки

- [ ] Анимация загрузки графика работает плавно (800ms, ease-out)
- [ ] Линии рисуются от левого края к правому
- [ ] Точки плавно увеличиваются при hover (200ms)
- [ ] Линии подсвечиваются при hover (увеличение толщины на 1px)
- [ ] Tooltip появляется плавно (300ms, fade-in)
- [ ] Задержка появления tooltip составляет 200ms
- [ ] Анимации не лагают на разных устройствах
- [ ] Производительность не ухудшилась
- [ ] Анимации работают для обоих графиков
- [ ] Анимации работают в обоих режимах (недели и месяцы)

---

## 🔗 Зависимости

**Зависит от:**
- [TASK-056-01: Улучшение визуализации линий](./TASK-056-01-lines-visualization.md) — анимации используют улучшенные линии и точки
- [TASK-056-02: Улучшение tooltips](./TASK-056-02-tooltips-improvement.md) — анимация tooltip
- [TASK-056-03: Оптимизация легенды](./TASK-056-03-legend-optimization.md) — может использовать hover-эффекты
- [TASK-056-05: Обновление цветовой схемы](./TASK-056-05-color-scheme-update.md) — анимации используют обновлённые цвета

**Зависит от этого этапа:**
- [TASK-056-07: Тестирование и финальная проверка](./TASK-056-07-testing-and-final-check.md) — проверка анимаций

---

## 📝 История правок

- **2025-12-17 16:42 (UTC+3, Брест):** Создан этап TASK-056-06
  - Определены требования для добавления анимаций
  - Зафиксированы технические детали и примеры кода
  - Добавлены критерии приёмки

---

## 💡 Примечания

- **Производительность:** Анимации должны быть плавными, но не влиять на производительность
- **Совместимость:** Проверить работу анимаций на разных браузерах
- **Оптимизация:** Использовать CSS transitions где возможно, избегать лишних перерисовок
- **Тестирование:** Протестировать анимации на разных устройствах (десктоп, планшет, мобильный)
- **UX:** Анимации должны улучшать пользовательский опыт, а не отвлекать

## ⚠️ Возможные проблемы и решения

### Проблема 1: Анимация загрузки не работает
**Причина:** Chart.js может не поддерживать `easing: 'ease-out'` в некоторых версиях  
**Решение:** 
- Использовать стандартные значения: `'linear'`, `'easeInOutQuad'`, `'easeInOutCubic'`
- Или использовать числовое значение для кастомного easing
- Проверить версию Chart.js и документацию

### Проблема 2: Анимация hover для линий не работает
**Причина:** Плагин не зарегистрирован или `afterEvent` не вызывается  
**Решение:** 
- Убедиться, что плагин зарегистрирован через `Chart.register()`
- Проверить, что `inChartArea` и `changed` обрабатываются правильно
- Использовать `chart.update('none')` для быстрого обновления

### Проблема 3: Tooltip появляется без задержки
**Причина:** Chart.js не поддерживает встроенную задержку  
**Решение:** 
- Использовать кастомный плагин для задержки
- Или использовать CSS `transition-delay`
- Проверить версию Chart.js и доступные опции

### Проблема 4: Анимации лагают на слабых устройствах
**Причина:** Слишком сложные анимации или много перерисовок  
**Решение:** 
- Уменьшить длительность анимации (например, до 500ms)
- Использовать `will-change` CSS-свойство
- Избегать лишних обновлений графика
- Использовать `chart.update('none')` где возможно

### Проблема 5: Анимация не работает при обновлении данных
**Причина:** Chart.js не анимирует обновления по умолчанию  
**Решение:** 
- Использовать `chart.update('active')` для анимации обновления
- Или `chart.update('default')` для стандартной анимации
- Или `chart.update('none')` для обновления без анимации

## 🔍 Детальная проверка реализации

**После реализации проверить:**
1. Анимация загрузки работает при первой загрузке графика (800ms, ease-out)
2. Линии рисуются от левого края к правому
3. Точки плавно увеличиваются при hover (200ms)
4. Линии подсвечиваются при hover (увеличение толщины на 1px)
5. Tooltip появляется плавно (300ms, fade-in)
6. Задержка появления tooltip работает (200ms)
7. Анимации не лагают на десктопе
8. Анимации не лагают на планшете
9. Анимации не лагают на мобильном
10. Производительность не ухудшилась (FPS >= 60)
11. Анимации работают для обоих графиков
12. Анимации работают в обоих режимах (недели и месяцы)
13. Hover-эффекты работают корректно
14. Нет визуальных артефактов при анимациях

