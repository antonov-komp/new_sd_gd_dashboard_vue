# TASK-005-01: Добавление отчёта в конфигурацию и роутер

**Дата создания:** 2025-12-06 11:18 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-005

---

## Описание

Добавить новый отчёт "Дашборд сектора 1С" в конфигурацию отчётов (`reports-config.js`) и создать соответствующий маршрут в роутере (`router/index.js`). Это позволит отобразить кнопку отчёта на стартовой странице и обеспечить навигацию к дашборду.

---

## Контекст

На стартовой странице (`IndexPage.vue`) уже реализована система отчётов:
- Отчёты загружаются из `reports-config.js` через функцию `getReports()`
- При клике на кнопку отчёта вызывается `navigateToReport(route)`, который выполняет `router.push(route)`
- В роутере пока нет маршрута для дашборда сектора 1С

**Требуется:**
- Добавить новый отчёт в массив `reports` в `reports-config.js`
- Создать маршрут `/dashboard/sector-1c` в `router/index.js`
- Убедиться, что отчёт отображается на стартовой странице

---

## Модули и компоненты

### Файлы для изменения:

1. **`vue-app/src/config/reports-config.js`**
   - Добавить новый отчёт в массив `reports`
   - Установить `active: true` для отображения на стартовой странице

2. **`vue-app/src/router/index.js`**
   - Добавить новый маршрут для дашборда
   - Импортировать компонент `DashboardSector1C.vue` (будет создан в TASK-005-02)

---

## Зависимости

### От других задач:
- **TASK-005-02** — компонент `DashboardSector1C.vue` должен быть создан (можно создать заглушку)

### От модулей:
- `vue-router` — для создания маршрута
- `@/config/reports-config.js` — для добавления отчёта

---

## Ступенчатые подзадачи

### 1. Добавить отчёт в конфигурацию

**Файл:** `vue-app/src/config/reports-config.js`

Добавить новый объект в массив `reports`:

```javascript
{
  id: 5,  // Следующий свободный ID
  title: 'Дашборд сектора 1С',
  icon: '📋',  // Или другая подходящая иконка
  route: '/dashboard/sector-1c',
  description: 'Управление тикетами сектора 1С',
  active: true  // Отчёт активен и будет отображаться
}
```

**Требования:**
- ID должен быть уникальным (следующий после существующих)
- `route` должен соответствовать маршруту в роутере
- `active: true` для отображения на стартовой странице

---

### 2. Создать маршрут в роутере

**Файл:** `vue-app/src/router/index.js`

Добавить новый маршрут в массив `routes`:

```javascript
{
  path: '/dashboard/sector-1c',
  name: 'dashboard-sector-1c',
  component: () => import('@/components/dashboard/DashboardSector1C.vue')
}
```

**Требования:**
- Использовать lazy loading (`() => import(...)`) для оптимизации
- Путь должен соответствовать `route` в конфигурации отчёта
- Имя маршрута должно быть уникальным

**Важно:** Если компонент `DashboardSector1C.vue` ещё не создан, можно временно использовать заглушку:

```javascript
{
  path: '/dashboard/sector-1c',
  name: 'dashboard-sector-1c',
  component: () => import('@/components/dashboard/DashboardSector1C.vue')
  // Или временно:
  // component: () => Promise.resolve({ template: '<div>Дашборд сектора 1С (в разработке)</div>' })
}
```

---

### 3. Проверить отображение на стартовой странице

**Файл:** `vue-app/src/components/IndexPage.vue`

Проверить, что:
- Отчёт загружается из `reports-config.js` через `getReports()`
- Кнопка отчёта отображается в секции `.reports-section`
- При клике происходит переход на маршрут `/dashboard/sector-1c`

**Текущая реализация уже поддерживает это:**
```javascript
// В IndexPage.vue уже есть:
const reportsButtons = ref(getReports());

const navigateToReport = (route) => {
  router.push(route);
};
```

---

## Технические требования

### Версии:
- **Vue Router:** последняя версия
- **Vue.js:** 3.x

### Ограничения:
- Маршрут должен использовать hash mode (так как приложение работает внутри Bitrix24)
- Lazy loading компонента для оптимизации загрузки

---

## Критерии приёмки

- [ ] Отчёт "Дашборд сектора 1С" добавлен в `reports-config.js` с `active: true`
- [ ] Маршрут `/dashboard/sector-1c` добавлен в `router/index.js`
- [ ] Кнопка отчёта отображается на стартовой странице
- [ ] При клике на кнопку происходит переход на маршрут `/dashboard/sector-1c`
- [ ] Компонент `DashboardSector1C.vue` загружается (даже если это заглушка)
- [ ] Нет ошибок в консоли браузера
- [ ] Код соответствует стандартам проекта

---

## Тестирование

### Функциональное тестирование:
1. Открыть стартовую страницу (`/`)
2. Проверить, что кнопка "Дашборд сектора 1С" отображается в секции отчётов
3. Кликнуть на кнопку
4. Проверить, что произошёл переход на `/dashboard/sector-1c`
5. Проверить, что компонент загрузился (даже если это заглушка)
6. Проверить, что в адресной строке отображается правильный hash-маршрут

### Проверка конфигурации:
1. Открыть `reports-config.js`
2. Проверить, что отчёт добавлен с правильными параметрами
3. Проверить, что `active: true`
4. Проверить, что `route` соответствует маршруту в роутере

### Проверка роутера:
1. Открыть `router/index.js`
2. Проверить, что маршрут добавлен
3. Проверить, что используется lazy loading
4. Проверить, что путь соответствует `route` в конфигурации

---

## Примеры кода

### Полный пример изменения `reports-config.js`:

```javascript
export const reportsConfig = {
  reports: [
    // ... существующие отчёты ...
    {
      id: 4,
      title: 'Сводный отчёт',
      icon: '📋',
      route: '/reports/summary',
      description: 'Общая сводка по всем показателям',
      active: false
    },
    // НОВЫЙ ОТЧЁТ:
    {
      id: 5,
      title: 'Дашборд сектора 1С',
      icon: '📋',
      route: '/dashboard/sector-1c',
      description: 'Управление тикетами сектора 1С',
      active: true
    }
  ]
};
```

### Полный пример изменения `router/index.js`:

```javascript
import { createRouter, createWebHashHistory } from 'vue-router';
import InstallPage from '@/components/InstallPage.vue';
import IndexPage from '@/components/IndexPage.vue';
import WebhookLogsPage from '@/pages/WebhookLogsPage.vue';

const routes = [
  {
    path: '/install',
    name: 'install',
    component: InstallPage
  },
  {
    path: '/',
    name: 'index',
    component: IndexPage
  },
  {
    path: '/webhook-logs',
    name: 'webhook-logs',
    component: WebhookLogsPage
  },
  // НОВЫЙ МАРШРУТ:
  {
    path: '/dashboard/sector-1c',
    name: 'dashboard-sector-1c',
    component: () => import('@/components/dashboard/DashboardSector1C.vue')
  }
];

// ... остальной код роутера ...
```

---

## История правок

- **2025-12-06 11:18 (UTC+3, Брест):** Создана подзадача TASK-005-01


