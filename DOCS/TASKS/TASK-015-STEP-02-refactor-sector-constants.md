# TASK-015-STEP-02: Рефакторинг структуры констант и утилит секторов (TASK-008)

**Дата создания:** 2025-12-06 21:30 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Рефактор-менеджер  
**Родительская задача:** TASK-015  
**Связанная задача:** TASK-008

---

## 📋 Описание

Провести рефакторинг структуры констант и утилит секторов, реализованных в TASK-008, с целью улучшения организации, устранения дублирования и создания единой точки управления константами секторов.

**Цель:** Создать централизованную систему управления константами секторов с улучшенной структурой и переиспользуемыми утилитами.

---

## 🎯 Контекст

**Текущее состояние (TASK-008):**

1. **Константы секторов:**
   - Файл: `vue-app/src/services/dashboard-sector-1c/utils/sector-constants.js`
   - Содержит константы: `SECTOR_1C_ID`, `SECTOR_HARDWARE_ID`, `SECTOR_BITRIX24_ID`, `SECTOR_PDM_ID`, `KEEPER_USER_ID`
   - Экспортирует константы напрямую

2. **Утилиты секторов:**
   - Файл: `vue-app/src/services/dashboard-sector-1c/utils/sector-helper.js`
   - Функции: `getEmployeeSectorId()`, `isEmployeeFromSector1C()`, `isEmployeeFromOtherSector()`

3. **Использование:**
   - Константы используются в `ticket-grouper.js`, `employee-mapper.js`, `DashboardStage.vue`, `EmployeeColumn.vue`
   - Утилиты используются в `ticket-grouper.js`, `employee-mapper.js`

**Выявленные проблемы:**
1. Константы могут быть расширены для других модулей
2. Логика определения сектора может быть оптимизирована
3. Нет единой точки управления константами
4. Отсутствует валидация данных сектора

---

## 🏗️ Модули и компоненты

### Файлы для рефакторинга:

1. **`vue-app/src/services/dashboard-sector-1c/utils/sector-constants.js`**
   - Реструктуризация констант
   - Добавление метаданных о секторах
   - Создание единого объекта констант

2. **`vue-app/src/services/dashboard-sector-1c/utils/sector-helper.js`**
   - Оптимизация функций определения сектора
   - Добавление валидации
   - Улучшение производительности

3. **`vue-app/src/services/dashboard-sector-1c/utils/constants.js`**
   - Обновление реэкспорта констант
   - Добавление новых констант (если нужно)

### Новые файлы для создания:

1. **`vue-app/src/services/dashboard-sector-1c/types/sector-types.js`** (опционально)
   - JSDoc типы для секторов
   - Интерфейсы для работы с секторами

---

## 📝 Ступенчатые подзадачи

### 1. Реструктуризация констант секторов

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/sector-constants.js`

**Текущая структура:**
```javascript
export const SECTOR_1C_ID = 366;
export const SECTOR_HARDWARE_ID = 368;
export const SECTOR_BITRIX24_ID = 369;
export const SECTOR_PDM_ID = 367;
export const KEEPER_USER_ID = 1051;
```

**Новая структура:**
```javascript
/**
 * Константы секторов для дашборда сектора 1С
 * 
 * Секторы определяют принадлежность сотрудников к отделам:
 * - Сектор 1С (366) — основной сектор
 * - Сектор Железо (368) — сектор оборудования
 * - Сектор Битрикс24 (369) — сектор Битрикс24
 * - Сектор PDM (367) — сектор PDM
 * - Хранитель (1051) — хранитель нулевой очереди
 */

/**
 * ID секторов
 */
export const SECTOR_IDS = {
  /** Сектор 1С */
  SECTOR_1C: 366,
  /** Сектор Железо */
  SECTOR_HARDWARE: 368,
  /** Сектор Битрикс24 */
  SECTOR_BITRIX24: 369,
  /** Сектор PDM */
  SECTOR_PDM: 367,
  /** Хранитель нулевой очереди */
  KEEPER: 1051
};

/**
 * Метаданные секторов
 */
export const SECTOR_METADATA = {
  [SECTOR_IDS.SECTOR_1C]: {
    id: SECTOR_IDS.SECTOR_1C,
    name: 'Сектор 1С',
    code: '1C',
    isMain: true
  },
  [SECTOR_IDS.SECTOR_HARDWARE]: {
    id: SECTOR_IDS.SECTOR_HARDWARE,
    name: 'Сектор Железо',
    code: 'HARDWARE',
    isMain: false
  },
  [SECTOR_IDS.SECTOR_BITRIX24]: {
    id: SECTOR_IDS.SECTOR_BITRIX24,
    name: 'Сектор Битрикс24',
    code: 'BITRIX24',
    isMain: false
  },
  [SECTOR_IDS.SECTOR_PDM]: {
    id: SECTOR_IDS.SECTOR_PDM,
    name: 'Сектор PDM',
    code: 'PDM',
    isMain: false
  }
};

/**
 * Список всех ID секторов (кроме хранителя)
 */
export const ALL_SECTOR_IDS = [
  SECTOR_IDS.SECTOR_1C,
  SECTOR_IDS.SECTOR_HARDWARE,
  SECTOR_IDS.SECTOR_BITRIX24,
  SECTOR_IDS.SECTOR_PDM
];

/**
 * ID сектора 1С (для обратной совместимости)
 */
export const SECTOR_1C_ID = SECTOR_IDS.SECTOR_1C;

/**
 * ID хранителя (для обратной совместимости)
 */
export const KEEPER_USER_ID = SECTOR_IDS.KEEPER;

/**
 * Проверка, является ли ID сектором
 * 
 * @param {number} sectorId - ID для проверки
 * @returns {boolean} true, если ID является сектором
 */
export function isSectorId(sectorId) {
  return ALL_SECTOR_IDS.includes(sectorId);
}

/**
 * Получение метаданных сектора по ID
 * 
 * @param {number} sectorId - ID сектора
 * @returns {object|null} Метаданные сектора или null
 */
export function getSectorMetadata(sectorId) {
  return SECTOR_METADATA[sectorId] || null;
}
```

**Критерии:**
- [ ] Константы реструктурированы в объект `SECTOR_IDS`
- [ ] Добавлены метаданные секторов
- [ ] Сохранена обратная совместимость (старые константы экспортируются)
- [ ] Добавлены утилиты для работы с секторами
- [ ] Добавлены JSDoc комментарии

---

### 2. Оптимизация утилит секторов

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/sector-helper.js`

**Текущая структура:**
```javascript
export function getEmployeeSectorId(employee) {
  // Логика определения сектора
}

export function isEmployeeFromSector1C(employee) {
  // Проверка принадлежности к сектору 1С
}

export function isEmployeeFromOtherSector(employee) {
  // Проверка принадлежности к другому сектору
}
```

**Улучшения:**
1. Добавить кеширование результатов
2. Оптимизировать проверку `UF_DEPARTMENT`
3. Добавить валидацию входных данных
4. Улучшить обработку edge cases

**Новая структура:**
```javascript
import { SECTOR_IDS, ALL_SECTOR_IDS, isSectorId } from './sector-constants.js';

/**
 * Кеш для результатов определения сектора
 * Ключ: employee.id, Значение: sectorId
 */
const sectorCache = new Map();

/**
 * Очистка кеша секторов
 */
export function clearSectorCache() {
  sectorCache.clear();
}

/**
 * Нормализация поля UF_DEPARTMENT
 * 
 * @param {number|Array<number>|undefined} department - Поле отдела
 * @returns {Array<number>} Массив ID отделов
 */
function normalizeDepartment(department) {
  if (!department) {
    return [];
  }
  
  if (Array.isArray(department)) {
    return department.filter(id => typeof id === 'number');
  }
  
  if (typeof department === 'number') {
    return [department];
  }
  
  return [];
}

/**
 * Получение ID сектора сотрудника
 * 
 * @param {object} employee - Объект сотрудника
 * @param {number} employee.id - ID сотрудника
 * @param {number|Array<number>} employee.UF_DEPARTMENT - ID отдела(ов)
 * @param {boolean} useCache - Использовать кеш (по умолчанию true)
 * @returns {number|null} ID сектора или null, если не определён
 */
export function getEmployeeSectorId(employee, useCache = true) {
  if (!employee || !employee.id) {
    return null;
  }
  
  // Проверка кеша
  if (useCache && sectorCache.has(employee.id)) {
    return sectorCache.get(employee.id);
  }
  
  const departments = normalizeDepartment(employee.UF_DEPARTMENT);
  
  // Поиск первого совпадения с сектором
  for (const deptId of departments) {
    if (isSectorId(deptId)) {
      const sectorId = deptId;
      
      // Сохранение в кеш
      if (useCache) {
        sectorCache.set(employee.id, sectorId);
      }
      
      return sectorId;
    }
  }
  
  // Сохранение null в кеш (не определён сектор)
  if (useCache) {
    sectorCache.set(employee.id, null);
  }
  
  return null;
}

/**
 * Проверка, принадлежит ли сотрудник к сектору 1С
 * 
 * @param {object} employee - Объект сотрудника
 * @returns {boolean} true, если сотрудник из сектора 1С
 */
export function isEmployeeFromSector1C(employee) {
  return getEmployeeSectorId(employee) === SECTOR_IDS.SECTOR_1C;
}

/**
 * Проверка, принадлежит ли сотрудник к другому сектору (не 1С и не хранитель)
 * 
 * @param {object} employee - Объект сотрудника
 * @returns {boolean} true, если сотрудник из другого сектора
 */
export function isEmployeeFromOtherSector(employee) {
  const sectorId = getEmployeeSectorId(employee);
  
  if (!sectorId) {
    return false;
  }
  
  return sectorId !== SECTOR_IDS.SECTOR_1C && sectorId !== SECTOR_IDS.KEEPER;
}

/**
 * Проверка, является ли сотрудник хранителем
 * 
 * @param {object} employee - Объект сотрудника
 * @returns {boolean} true, если сотрудник является хранителем
 */
export function isKeeper(employee) {
  if (!employee || !employee.UF_DEPARTMENT) {
    return false;
  }
  
  const departments = normalizeDepartment(employee.UF_DEPARTMENT);
  return departments.includes(SECTOR_IDS.KEEPER);
}
```

**Критерии:**
- [ ] Добавлено кеширование результатов
- [ ] Оптимизирована нормализация `UF_DEPARTMENT`
- [ ] Добавлена валидация входных данных
- [ ] Улучшена обработка edge cases
- [ ] Добавлены JSDoc комментарии
- [ ] Добавлена функция `isKeeper()`

---

### 3. Обновление использования констант

**Файлы для обновления:**
- `vue-app/src/services/dashboard-sector-1c/groupers/ticket-grouper.js`
- `vue-app/src/services/dashboard-sector-1c/mappers/employee-mapper.js`
- `vue-app/src/components/dashboard/DashboardStage.vue`
- `vue-app/src/components/dashboard/EmployeeColumn.vue`

**Изменения:**
1. Обновить импорты констант (использовать новые константы)
2. Обновить использование утилит (использовать оптимизированные функции)
3. Сохранить обратную совместимость

**Пример обновления:**
```javascript
// Было:
import { SECTOR_1C_ID } from '../utils/sector-constants.js';
import { isEmployeeFromSector1C } from '../utils/sector-helper.js';

// Стало:
import { SECTOR_IDS, SECTOR_1C_ID } from '../utils/sector-constants.js';
import { isEmployeeFromSector1C, isKeeper } from '../utils/sector-helper.js';
```

**Критерии:**
- [ ] Все файлы обновлены для использования новых констант
- [ ] Использование утилит оптимизировано
- [ ] Обратная совместимость сохранена
- [ ] Код компилируется без ошибок

---

## 🔧 Технические требования

### Принципы рефакторинга:

1. **Обратная совместимость**
   - Старые константы должны экспортироваться
   - Существующий код должен работать без изменений

2. **Производительность**
   - Кеширование результатов определения сектора
   - Оптимизация проверок

3. **Расширяемость**
   - Легко добавлять новые секторы
   - Метаданные секторов доступны

4. **Типизация**
   - JSDoc комментарии для всех функций
   - Типизация параметров и возвращаемых значений

---

## ✅ Критерии приёмки

- [ ] Константы реструктурированы в объект `SECTOR_IDS`
- [ ] Добавлены метаданные секторов
- [ ] Утилиты оптимизированы с кешированием
- [ ] Все файлы обновлены для использования новых констант
- [ ] Обратная совместимость сохранена
- [ ] Добавлены JSDoc комментарии
- [ ] Код компилируется без ошибок
- [ ] Функциональность не нарушена
- [ ] Производительность улучшена

---

## 🧪 Тестирование

### Функциональное тестирование:

1. Проверить определение сектора для сотрудников из разных секторов
2. Проверить кеширование результатов
3. Проверить работу с разными форматами `UF_DEPARTMENT`
4. Проверить обратную совместимость

### Производительность:

1. Проверить работу кеша
2. Проверить скорость определения сектора
3. Проверить использование памяти

---

## 📚 Связанные документы

- **Родительская задача:** `DOCS/TASKS/TASK-015-refactor-tasks-008-009-011-012-013-014.md`
- **Связанная задача:** `DOCS/TASKS/TASK-008-separate-tickets-by-sector.md`

---

## 📝 История правок

- **2025-12-06 21:30 (UTC+3, Брест):** Создана подзадача STEP-02 для рефакторинга констант и утилит секторов

---

**Автор:** Рефактор-менеджер  
**Статус:** Новая

