# TASK-032-01: Этап 1 - Подготовка и настройка стилей точек

**Дата создания:** 2025-12-12 08:38 (UTC+3, Брест)  
**Дата завершения:** 2025-12-12 09:41 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-032

---

## 📋 Описание этапа

Применение разных стилей точек для каждого этапа (dataset) в линейном графике состояния сектора 1С. Это базовое решение, которое делает перекрывающиеся точки визуально различимыми благодаря разным формам (круг, треугольник, квадрат, ромб, звезда и т.д.).

---

## 🎯 Цель этапа

Реализовать визуальное различие точек разных этапов на линейном графике через применение различных стилей точек Chart.js, чтобы пользователь мог видеть перекрывающиеся точки без необходимости наведения курсора.

**Ожидаемый результат:**
Точки разных этапов имеют разные формы (круг, треугольник, квадрат, ромб и т.д.), что делает их визуально различимыми даже при полном перекрытии.

---

## 📝 Подзадачи

### Подзадача 1.1: Определение массива стилей точек

**Цель:** Создать массив доступных стилей точек Chart.js для применения к этапам.

**Задачи:**
1. Изучить доступные стили точек в Chart.js
2. Определить порядок применения стилей
3. Создать константу с массивом стилей

**Технические детали:**
- Доступные стили: `'circle'`, `'triangle'`, `'rect'`, `'rectRot'`, `'star'`, `'cross'`, `'crossRot'`
- Массив должен быть циклическим (если этапов больше, чем стилей, повторяются)
- Рекомендуемый порядок: `['circle', 'triangle', 'rect', 'rectRot', 'star', 'cross', 'crossRot']`

**Реализация:**
```javascript
// В начале <script setup>, после импортов, перед определением других констант
// Рекомендуемое место: после строки 256 (после определения stages)

/**
 * Массив стилей точек для линейного графика
 * Используется для визуального различия точек разных этапов
 * @type {Array<string>}
 */
const POINT_STYLES = ['circle', 'triangle', 'rect', 'rectRot', 'star', 'cross', 'crossRot'];
```

**Важно:**
- Константа должна быть определена в области видимости `setup()`
- Разместить после определения `stages` (строка 256), чтобы иметь доступ к структуре этапов
- Использовать `const`, а не `let` или `var` (неизменяемая константа)

**Критерии завершения:**
- [ ] Массив стилей определен
- [ ] Порядок стилей логичен и визуально различим
- [ ] Массив содержит минимум 5 различных стилей

---

### Подзадача 1.2: Интеграция стилей в функцию prepareChartData()

**Цель:** Модифицировать функцию `prepareChartData()` для применения стилей точек к каждому dataset.

**Задачи:**
1. Найти функцию `prepareChartData()` в компоненте
2. Определить место, где создаются datasets
3. Добавить применение `pointStyle` к каждому dataset
4. Использовать индекс этапа для выбора стиля из массива

**Технические детали:**
- Файл: `vue-app/src/components/graph-state/GraphStateChart.vue`
- Функция: `prepareChartData()` (строки 1050-1243)
- Место изменения: внутри цикла `stages.forEach((stage, index) => { ... })` (строка ~1120)
- Точное место: перед строкой 1215, где создается объект dataset
- Применение: `pointStyle: POINT_STYLES[index % POINT_STYLES.length]`
- Условие: применять только для линейных графиков (`chartType.value === 'line'`)

**Контекст существующего кода:**
В функции `prepareChartData()` уже есть логика для определения цветов точек на основе трендов:
- Строки 1192-1204: определение `pointBackgroundColor` на основе трендов или базовых цветов
- Строка 1221: применение `pointBackgroundColor` к dataset
- Строка 1223: `pointRadius: 6` - размер точки
- Строка 1224: `pointHoverRadius: 8` - размер при наведении

**Важно учитывать:**
- `pointBackgroundColor` может быть массивом (если есть тренды) или строкой (базовый цвет)
- Стиль точки (`pointStyle`) должен применяться независимо от цвета
- Не нарушать существующую логику с трендами и цветами

**Текущий код (пример):**
```javascript
datasets.push({
  label: stage.name,
  data: data,
  backgroundColor: backgroundColor,
  borderColor: borderColor,
  borderWidth: 2,
  pointBackgroundColor: pointBackgroundColor,
  pointBorderColor: borderColor,
  pointRadius: 6,
  pointHoverRadius: 8,
  fill: chartType.value === 'line' ? false : true,
  tension: chartType.value === 'line' ? 0.4 : 0,
  meta: meta
});
```

**Изменённый код:**
```javascript
// В функции prepareChartData(), внутри цикла stages.forEach((stage, index) => { ... })
// Место: после строки 1206 (после определения meta), перед строкой 1215 (datasets.push)

datasets.push({
  label: stage.name,
  data: data,
  backgroundColor: Array.isArray(backgroundColor) ? backgroundColor : backgroundColor,
  borderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  borderWidth: 2,
  // Применение стиля точки только для линейных графиков
  ...(chartType.value === 'line' && {
    pointStyle: POINT_STYLES[index % POINT_STYLES.length]
  }),
  pointBackgroundColor: Array.isArray(pointBackgroundColor) ? pointBackgroundColor : pointBackgroundColor,
  pointBorderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  pointRadius: 6,
  pointHoverRadius: 8,
  fill: chartType.value === 'line' ? false : true,
  tension: chartType.value === 'line' ? 0.4 : 0,
  meta: meta // Метаданные о сотрудниках для линейного графика
});
```

**Альтернативный вариант (если spread operator не подходит):**
```javascript
const datasetConfig = {
  label: stage.name,
  data: data,
  backgroundColor: Array.isArray(backgroundColor) ? backgroundColor : backgroundColor,
  borderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  borderWidth: 2,
  pointBackgroundColor: Array.isArray(pointBackgroundColor) ? pointBackgroundColor : pointBackgroundColor,
  pointBorderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  pointRadius: 6,
  pointHoverRadius: 8,
  fill: chartType.value === 'line' ? false : true,
  tension: chartType.value === 'line' ? 0.4 : 0,
  meta: meta
};

// Добавить стиль точки только для линейных графиков
if (chartType.value === 'line') {
  datasetConfig.pointStyle = POINT_STYLES[index % POINT_STYLES.length];
}

datasets.push(datasetConfig);
```

**Почему использовать spread operator:**
- Более лаконичный код
- Условное добавление свойства без лишних проверок
- Соответствует современным практикам JavaScript

**Возможные ошибки и решения:**

1. **Ошибка: "POINT_STYLES is not defined"**
   - Причина: константа определена не в той области видимости
   - Решение: убедиться, что `POINT_STYLES` определена в `setup()` до использования

2. **Ошибка: "Cannot read property 'length' of undefined"**
   - Причина: `POINT_STYLES` не определена или имеет неправильное значение
   - Решение: проверить определение константы

3. **Стили не применяются**
   - Причина: условие `chartType.value === 'line'` не выполняется
   - Решение: проверить значение `chartType.value` в момент создания dataset

4. **Все точки имеют один стиль**
   - Причина: неправильное использование индекса (например, всегда используется 0)
   - Решение: убедиться, что используется `index` из `forEach`, а не фиксированное значение

**Критерии завершения:**
- [ ] Стиль точки применяется к каждому dataset
- [ ] Используется циклический выбор стиля (index % length)
- [ ] Стили применяются только для линейных графиков (проверка `chartType.value === 'line'`)
- [ ] Код не нарушает существующую функциональность

---

### Подзадача 1.3: Проверка применения стилей

**Цель:** Убедиться, что стили применяются корректно и визуально различимы.

**Задачи:**
1. Открыть линейный график в браузере
2. Проверить, что каждый этап имеет уникальный стиль точки
3. Проверить визуальное различие точек разных этапов
4. Проверить работу на графике с перекрывающимися точками

**Тестирование:**
1. Открыть модуль "График состояния сектора 1С"
2. Выбрать тип графика "Линейный"
3. Проверить точки на графике:
   - Первый этап должен иметь круги (○)
   - Второй этап должен иметь треугольники (△)
   - Третий этап должен иметь квадраты (□)
   - И так далее
4. Найти позицию с перекрывающимися точками
5. Убедиться, что точки визуально различимы благодаря разным формам

**Критерии завершения:**
- [ ] Каждый этап имеет уникальный стиль точки
- [ ] Стили визуально различимы на графике
- [ ] Перекрывающиеся точки различимы без наведения
- [ ] Нет ошибок в консоли браузера
- [ ] График корректно отображается на всех устройствах

---

### Подзадача 1.4: Оптимизация и финализация

**Цель:** Оптимизировать код и убедиться в соответствии стандартам.

**Задачи:**
1. Проверить, что стили применяются только для линейных графиков
2. Добавить комментарии к коду
3. Убедиться в соответствии стандартам кода проекта
4. Проверить производительность (не должно быть лагов)

**Технические детали:**
- Добавить проверку: `if (chartType.value === 'line')` перед применением стилей
- Добавить JSDoc комментарий к константе `POINT_STYLES`
- Убедиться, что код не влияет на другие типы графиков (столбчатый, круговая)

**Критерии завершения:**
- [ ] Стили применяются только для линейных графиков
- [ ] Добавлены комментарии к коду
- [ ] Код соответствует стандартам проекта
- [ ] Производительность не ухудшилась
- [ ] Нет побочных эффектов на другие типы графиков

---

## 📁 Изменяемые файлы

1. **`vue-app/src/components/graph-state/GraphStateChart.vue`**
   - Добавление константы `POINT_STYLES` (в начале setup() или как константа)
   - Модификация функции `prepareChartData()` для применения стилей
   - Добавление свойства `pointStyle` к каждому dataset

---

## 🔧 Технические детали

### Chart.js Point Styles

Доступные стили точек в Chart.js 4.x:
- `'circle'` — круг (по умолчанию)
- `'triangle'` — треугольник
- `'rect'` — квадрат
- `'rectRot'` — повёрнутый квадрат (ромб)
- `'star'` — звезда
- `'cross'` — крест
- `'crossRot'` — повёрнутый крест

### Пример полной реализации

**Шаг 1: Определение константы (после строки 256, где определены stages)**
```javascript
// После определения stages (строка 256)
const stages = [
  { id: 'formed', name: 'Сформировано обращение', color: '#3b82f6' },
  // ... остальные этапы
];

/**
 * Массив стилей точек для линейного графика
 * Используется для визуального различия точек разных этапов при перекрытии
 * Порядок: круг, треугольник, квадрат, ромб, звезда, крест, повёрнутый крест
 * @type {Array<string>}
 */
const POINT_STYLES = ['circle', 'triangle', 'rect', 'rectRot', 'star', 'cross', 'crossRot'];
```

**Шаг 2: Модификация функции prepareChartData() (внутри цикла stages.forEach)**

Текущий код (строки 1215-1228):
```javascript
datasets.push({
  label: stage.name,
  data: data,
  backgroundColor: Array.isArray(backgroundColor) ? backgroundColor : backgroundColor,
  borderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  borderWidth: 2,
  pointBackgroundColor: Array.isArray(pointBackgroundColor) ? pointBackgroundColor : pointBackgroundColor,
  pointBorderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  pointRadius: 6,
  pointHoverRadius: 8,
  fill: chartType.value === 'line' ? false : true,
  tension: chartType.value === 'line' ? 0.4 : 0,
  meta: meta
});
```

Изменённый код:
```javascript
datasets.push({
  label: stage.name,
  data: data,
  backgroundColor: Array.isArray(backgroundColor) ? backgroundColor : backgroundColor,
  borderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  borderWidth: 2,
  // Применение стиля точки только для линейных графиков
  // Используется циклический выбор стиля для случаев, когда этапов больше, чем стилей
  ...(chartType.value === 'line' && {
    pointStyle: POINT_STYLES[index % POINT_STYLES.length]
  }),
  pointBackgroundColor: Array.isArray(pointBackgroundColor) ? pointBackgroundColor : pointBackgroundColor,
  pointBorderColor: Array.isArray(borderColor) ? borderColor : borderColor,
  pointRadius: 6,
  pointHoverRadius: 8,
  fill: chartType.value === 'line' ? false : true,
  tension: chartType.value === 'line' ? 0.4 : 0,
  meta: meta
});
```

**Полный контекст функции prepareChartData() (фрагмент):**
```javascript
function prepareChartData() {
  const datasets = [];
  const labels = [];
  
  // ... существующий код подготовки labels и данных ...
  
  stages.forEach((stage, index) => {
    const data = [];
    
    // ... существующий код подготовки data для этапа ...
    
    // Получение трендов (строки 1163-1190)
    const trends = ['stable'];
    // ... логика определения трендов ...
    
    // Цвета на основе трендов (строки 1192-1204)
    let backgroundColor, borderColor, pointBackgroundColor;
    if (comparison.value && chartType.value !== 'doughnut') {
      backgroundColor = trends.map(trend => getTrendColor(trend, 'background') + '40');
      borderColor = trends.map(trend => getTrendColor(trend, 'border'));
      pointBackgroundColor = trends.map(trend => getTrendColor(trend, 'point'));
    } else {
      backgroundColor = stage.color + '40';
      borderColor = stage.color;
      pointBackgroundColor = stage.color;
    }
    
    // Подготовка метаданных о сотрудниках (строки 1206-1213)
    let meta = null;
    if (chartType.value === 'line') {
      const employeeData = prepareLineChartEmployeeData(stage.id, snapshotsData);
      meta = {
        employees: employeeData
      };
    }
    
    // Создание dataset с применением стиля точки
    datasets.push({
      label: stage.name,
      data: data,
      backgroundColor: Array.isArray(backgroundColor) ? backgroundColor : backgroundColor,
      borderColor: Array.isArray(borderColor) ? borderColor : borderColor,
      borderWidth: 2,
      // ← ДОБАВИТЬ ЗДЕСЬ: применение стиля точки
      ...(chartType.value === 'line' && {
        pointStyle: POINT_STYLES[index % POINT_STYLES.length]
      }),
      pointBackgroundColor: Array.isArray(pointBackgroundColor) ? pointBackgroundColor : pointBackgroundColor,
      pointBorderColor: Array.isArray(borderColor) ? borderColor : borderColor,
      pointRadius: 6,
      pointHoverRadius: 8,
      fill: chartType.value === 'line' ? false : true,
      tension: chartType.value === 'line' ? 0.4 : 0,
      meta: meta
    });
  });
  
  return {
    labels: labels,
    datasets: datasets
  };
}
```

**Проверка после реализации:**
1. Открыть DevTools (F12) в браузере
2. Перейти на вкладку "Console"
3. Выбрать линейный график
4. Проверить, что нет ошибок в консоли
5. В консоли выполнить: `document.querySelector('canvas')` - должен вернуть canvas элемент
6. Проверить визуально, что точки имеют разные формы

### Важные замечания

1. **Циклический выбор:** Использовать `index % POINT_STYLES.length` для циклического повторения стилей, если этапов больше, чем стилей
   - Пример: если этапов 10, а стилей 7, то этапы 0-6 получат стили 0-6, этап 7 получит стиль 0 (7 % 7 = 0), этап 8 получит стиль 1 (8 % 7 = 1) и т.д.

2. **Только для линейных графиков:** Применять стили только когда `chartType.value === 'line'`
   - Столбчатый график (`'bar'`) и круговая диаграмма (`'doughnut'`) не используют точки, поэтому стили не нужны
   - Проверка `chartType.value === 'line'` уже используется в других местах функции (строки 1208, 1225, 1226)

3. **Сохранение цветов:** Стили точек не должны влиять на цвета этапов (цвета остаются как есть)
   - `pointBackgroundColor` и `pointBorderColor` остаются без изменений
   - Стиль точки (`pointStyle`) определяет только форму, не цвет

4. **Размеры точек:** Размеры точек (6px обычное, 8px при наведении) остаются без изменений
   - `pointRadius: 6` и `pointHoverRadius: 8` не изменяются
   - Разные стили точек имеют одинаковый размер

5. **Совместимость с трендами:** Стили точек работают независимо от системы трендов
   - Если есть тренды (сравнение), цвета точек могут быть массивами (разные цвета для разных временных точек)
   - Стиль точки применяется ко всем точкам этапа одинаково (не массив)

6. **Порядок применения:** Стиль применяется после определения всех цветов и метаданных
   - Это гарантирует, что стиль не влияет на существующую логику

### Отладка

**Если стили не применяются:**

1. **Проверить определение константы:**
   ```javascript
   // В консоли браузера (после загрузки компонента)
   console.log(POINT_STYLES); // Должен вывести массив стилей
   ```

2. **Проверить значение chartType:**
   ```javascript
   // В консоли браузера
   console.log(chartType.value); // Должно быть 'line' для линейного графика
   ```

3. **Проверить данные dataset:**
   ```javascript
   // В функции prepareChartData(), перед datasets.push()
   console.log('Stage:', stage.name, 'Index:', index, 'Style:', POINT_STYLES[index % POINT_STYLES.length]);
   ```

4. **Проверить в DevTools:**
   - Открыть DevTools → Elements
   - Найти canvas элемент графика
   - Проверить, что график отрисовывается
   - Визуально проверить формы точек на графике

**Частые ошибки:**

1. **"Cannot read property 'length' of undefined"**
   - Решение: убедиться, что `POINT_STYLES` определена до использования

2. **Все точки одного стиля**
   - Решение: проверить, что используется правильный `index` из `forEach`

3. **Стили применяются к другим типам графиков**
   - Решение: проверить условие `chartType.value === 'line'`

---

## ✅ Критерии приёмки

### Обязательные:

- [ ] Константа `POINT_STYLES` определена с массивом стилей
- [ ] Стили применяются к каждому dataset в функции `prepareChartData()`
- [ ] Каждый этап имеет уникальный стиль точки (или циклически повторяется)
- [ ] Стили применяются только для линейных графиков
- [ ] Точки разных этапов визуально различимы на графике
- [ ] Перекрывающиеся точки различимы без наведения курсора
- [ ] Нет ошибок в консоли браузера
- [ ] График корректно отображается на всех устройствах

### Дополнительные:

- [ ] Добавлены комментарии к коду (JSDoc)
- [ ] Код соответствует стандартам проекта
- [ ] Производительность не ухудшилась
- [ ] Нет побочных эффектов на другие типы графиков

---

## 🧪 Тестирование

### Сценарий 1: Базовое тестирование

1. Открыть модуль "График состояния сектора 1С"
2. Выбрать тип графика "Линейный"
3. Проверить точки на графике:
   - Убедиться, что каждый этап имеет свой стиль точки
   - Проверить визуальное различие форм

**Ожидаемый результат:** Точки разных этапов имеют разные формы и визуально различимы.

---

### Сценарий 2: Тестирование перекрытий

1. Найти позицию на графике с перекрывающимися точками (несколько этапов с одинаковым значением)
2. Визуально проверить, что точки различимы благодаря разным формам
3. Убедиться, что не нужно наводить курсор для различения

**Ожидаемый результат:** Перекрывающиеся точки визуально различимы без наведения курсора.

---

### Сценарий 3: Тестирование с большим количеством этапов

1. Проверить график с максимальным количеством этапов (5+)
2. Убедиться, что стили циклически повторяются
3. Проверить, что нет визуальных конфликтов

**Ожидаемый результат:** Все этапы имеют стили точек, стили циклически повторяются при необходимости.

---

### Сценарий 4: Тестирование других типов графиков

1. Переключиться на столбчатый график
2. Переключиться на круговую диаграмму
3. Убедиться, что стили точек не влияют на другие типы графиков

**Ожидаемый результат:** Стили точек применяются только к линейным графикам, другие типы графиков работают как прежде.

---

### Сценарий 5: Адаптивность

1. Проверить отображение на мобильных устройствах (320px+)
2. Проверить на планшетах (768px+)
3. Проверить на больших экранах (1920px+)

**Ожидаемый результат:** Стили точек различимы на всех размерах экрана.

---

## ⚠️ Типичные ошибки и решения

### Ошибка 1: "POINT_STYLES is not defined"

**Симптом:** В консоли браузера ошибка "ReferenceError: POINT_STYLES is not defined"

**Причина:** Константа определена не в той области видимости или после использования

**Решение:**
1. Убедиться, что `POINT_STYLES` определена в `setup()` до функции `prepareChartData()`
2. Проверить, что константа определена как `const`, а не `let` или `var`
3. Убедиться, что нет опечаток в названии константы

**Правильное размещение:**
```javascript
// ✅ ПРАВИЛЬНО: константа определена в начале setup()
const POINT_STYLES = ['circle', 'triangle', ...];

function prepareChartData() {
  // Использование POINT_STYLES
}

// ❌ НЕПРАВИЛЬНО: константа определена после использования
function prepareChartData() {
  // Использование POINT_STYLES - ошибка!
}
const POINT_STYLES = ['circle', 'triangle', ...];
```

---

### Ошибка 2: Все точки имеют один стиль

**Симптом:** Все точки на графике имеют одинаковую форму (например, все круги)

**Причина:** Неправильное использование индекса в `forEach`

**Решение:**
1. Убедиться, что используется `index` из `stages.forEach((stage, index) => { ... })`
2. Проверить, что используется правильная формула: `POINT_STYLES[index % POINT_STYLES.length]`
3. Добавить отладочный вывод для проверки:

```javascript
stages.forEach((stage, index) => {
  const pointStyle = POINT_STYLES[index % POINT_STYLES.length];
  console.log(`Stage: ${stage.name}, Index: ${index}, Style: ${pointStyle}`);
  
  datasets.push({
    // ...
    pointStyle: pointStyle,
    // ...
  });
});
```

---

### Ошибка 3: Стили применяются к другим типам графиков

**Симптом:** Столбчатый или круговой график показывает ошибки или странное поведение

**Причина:** Условие `chartType.value === 'line'` не работает или отсутствует

**Решение:**
1. Убедиться, что используется spread operator с условием:
   ```javascript
   ...(chartType.value === 'line' && {
     pointStyle: POINT_STYLES[index % POINT_STYLES.length]
   })
   ```
2. Проверить значение `chartType.value` в момент создания dataset
3. Добавить проверку перед применением стиля:

```javascript
// Альтернативный вариант с явной проверкой
const datasetConfig = {
  // ... другие свойства
};

if (chartType.value === 'line') {
  datasetConfig.pointStyle = POINT_STYLES[index % POINT_STYLES.length];
}

datasets.push(datasetConfig);
```

---

### Ошибка 4: Стили не видны на графике

**Симптом:** График отображается, но точки выглядят одинаково

**Причина:** 
- Стили не применяются (проверка условия не проходит)
- Chart.js не поддерживает указанный стиль
- Размер точек слишком мал для различия форм

**Решение:**
1. Проверить в DevTools, что свойство `pointStyle` присутствует в dataset:
   ```javascript
   // В консоли браузера
   const chartData = prepareChartData();
   console.log(chartData.datasets[0].pointStyle); // Должен вывести 'circle' или другой стиль
   ```
2. Убедиться, что используется поддерживаемый стиль (см. список доступных стилей)
3. Увеличить размер точек для тестирования:
   ```javascript
   pointRadius: 10, // Временно увеличить для проверки
   pointHoverRadius: 12,
   ```
4. Проверить, что график перерисовывается после изменений

---

## 📝 История правок

- **2025-12-12 08:38 (UTC+3, Брест):** Создан документ TASK-032-01 с детальным планом реализации стилей точек
- **2025-12-12 09:41 (UTC+3, Брест):** Задача успешно завершена - стили точек применены к линейному графику

---

## 🔗 Связанные задачи

- **TASK-032:** Визуализация перекрывающихся точек на линейном графике состояния сектора 1С (родительская задача)
- **TASK-032-02:** Этап 2 - Создание утилиты для анализа перекрытий (следующий этап)
- **TASK-031:** Интеграция детализации по сотрудникам в графики состояния сектора 1С

---

## 📚 Дополнительная информация

### Использованные технологии:

- Vue.js 3.x (Composition API)
- Chart.js 4.x
- vue-chartjs

### Документация:

- Chart.js Point Styles: https://www.chartjs.org/docs/latest/configuration/elements.html#point-configuration
- Chart.js Elements: https://www.chartjs.org/docs/latest/configuration/elements.html

### Примеры визуализации:

- Разные стили точек — стандартный подход в аналитике для различения множественных линий
- Используется в большинстве BI-инструментов (Tableau, Power BI, D3.js)

---

**Статус:** ✅ Завершена  
**Реализовано:** Все подзадачи выполнены, стили точек применены к линейному графику




