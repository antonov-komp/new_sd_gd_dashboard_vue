# TASK-015-STEP-08: Оптимизация производительности и кеширования

**Дата создания:** 2025-12-06 21:30 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Рефактор-менеджер  
**Родительская задача:** TASK-015

---

## 📋 Описание

Провести комплексную оптимизацию производительности дашборда сектора 1С, включая оптимизацию кеширования, рендеринга компонентов и запросов к API.

**Цель:** Улучшить производительность дашборда без нарушения функциональности.

---

## 🎯 Контекст

**Области для оптимизации:**

1. **Кеширование:**
   - Оптимизация кеша данных (в `DashboardSector1CService`)
   - Оптимизация кеша секторов (в `sector-helper.js` после STEP-02)
   - Оптимизация кеша полей тикетов (в `ticket-field-processor.js` после STEP-06)
   - Добавление TTL для кеша (время жизни кеша)

2. **Рендеринг:**
   - Оптимизация computed свойств (в компонентах)
   - Использование `v-memo` для списков (в `ZeroPoint.vue` после STEP-05, в `EmployeeColumn.vue`)
   - Использование `v-once` для статического контента
   - Оптимизация списков тикетов

3. **Запросы к API:**
   - Батчинг запросов (объединение нескольких запросов в один)
   - Оптимизация параметров запросов (выбор только нужных полей)
   - Кеширование ответов (в репозиториях)
   - Оптимизация повторных запросов

**Текущее состояние:**

1. **Кеширование:**
   - Кеш данных в `DashboardSector1CService` (базовый кеш)
   - Кеш секторов будет добавлен в STEP-02
   - Кеш полей тикетов будет добавлен в STEP-06
   - Нет TTL для кеша (кеш не истекает)

2. **Рендеринг:**
   - `ZeroPoint.vue` будет оптимизирован в STEP-05 (`v-memo`, `v-once`)
   - `EmployeeColumn.vue` может использовать `v-memo` для списков тикетов
   - `DashboardStage.vue` может оптимизировать computed свойства

3. **Запросы к API:**
   - Запросы выполняются последовательно
   - Нет батчинга запросов
   - Кеширование ответов базовое (без TTL)

**Метрики для оптимизации:**
- Время загрузки дашборда
- Время рендеринга компонентов
- Количество ререндеров
- Использование памяти
- Количество запросов к API

---

## 🏗️ Модули и компоненты

### Файлы для оптимизации:

1. **Компоненты:**
   - `vue-app/src/components/dashboard/DashboardSector1C.vue`
   - `vue-app/src/components/dashboard/DashboardStage.vue`
   - `vue-app/src/components/dashboard/EmployeeColumn.vue`
   - `vue-app/src/components/dashboard/TicketCard.vue`

2. **Сервисы:**
   - `vue-app/src/services/dashboard-sector-1c/index.js`
   - `vue-app/src/services/dashboard-sector-1c/data/ticket-repository.js`
   - `vue-app/src/services/dashboard-sector-1c/data/employee-repository.js`

3. **Композаблы:**
   - `vue-app/src/composables/useDashboardActions.js`
   - `vue-app/src/composables/useDashboardState.js`

---

## 📝 Ступенчатые подзадачи

### 1. Оптимизация кеширования

**Задачи:**
1. Оптимизировать стратегию кеширования
   - Добавить TTL (Time To Live) для кеша данных
   - Реализовать автоматическую очистку устаревших записей
   - Оптимизировать размер кеша (ограничение количества записей)
2. Добавить TTL для кеша
   - Кеш данных: 5 минут (300000мс)
   - Кеш секторов: без TTL (данные не меняются часто)
   - Кеш полей тикетов: 10 минут (600000мс)
3. Оптимизировать инвалидацию кеша
   - Автоматическая инвалидация при обновлении данных
   - Ручная инвалидация через методы очистки
   - Частичная инвалидация (только изменённые данные)
4. Добавить кеширование на уровне компонентов
   - Кеширование computed свойств (если возможно)
   - Кеширование результатов фильтрации/сортировки

**Файлы для оптимизации:**
- `vue-app/src/services/dashboard-sector-1c/index.js` (кеш данных)
- `vue-app/src/services/dashboard-sector-1c/utils/sector-helper.js` (кеш секторов, после STEP-02)
- `vue-app/src/services/dashboard-sector-1c/utils/ticket-field-processor.js` (кеш полей, после STEP-06)

**Критерии:**
- [ ] Стратегия кеширования оптимизирована (TTL, ограничение размера)
- [ ] Добавлен TTL для кеша (5-10 минут в зависимости от типа данных)
- [ ] Инвалидация кеша оптимизирована (автоматическая и ручная)
- [ ] Кеширование на уровне компонентов работает (если применимо)
- [ ] Производительность улучшена (меньше запросов к API)
- [ ] Функциональность не нарушена (данные актуальны)

---

### 2. Оптимизация рендеринга компонентов

**Задачи:**
1. Использовать `v-memo` для списков
   - `EmployeeColumn.vue`: список тикетов (`ticketsInsideSector`, `ticketsOutsideSector`)
   - `ZeroPoint.vue`: список тикетов (будет добавлен в STEP-05)
   - `DashboardStage.vue`: список сотрудников (если применимо)
2. Использовать `v-once` для статического контента
   - Заголовки этапов (не меняются)
   - Fallback-сообщения (статичны)
   - Иконки и статические элементы
3. Оптимизировать computed свойства
   - Кеширование результатов (Vue делает это автоматически)
   - Минимизация зависимостей
   - Избежание сложных вычислений в computed
4. Оптимизировать watchers
   - Использование `immediate: false` где возможно
   - Использование `deep: false` где возможно
   - Оптимизация колбэков watchers

**Файлы для оптимизации:**
- `vue-app/src/components/dashboard/EmployeeColumn.vue` (списки тикетов)
- `vue-app/src/components/dashboard/ZeroPoint.vue` (будет оптимизирован в STEP-05)
- `vue-app/src/components/dashboard/DashboardStage.vue` (computed свойства)
- `vue-app/src/components/dashboard/TicketCard.vue` (если применимо)

**Примеры оптимизации:**

1. **EmployeeColumn.vue:**
```vue
<!-- Оптимизация списка тикетов -->
<div
  v-memo="[ticketsInsideSector.length, ticketsInsideSector.map(t => t.id).join(',')]"
  class="tickets-inside-sector"
>
  <TicketCard
    v-for="ticket in ticketsInsideSector"
    :key="ticket.id"
    :ticket="ticket"
  />
</div>
```

2. **DashboardStage.vue:**
```vue
<!-- Статический заголовок -->
<h2 v-once class="stage-title">{{ stage.name }}</h2>
```

**Критерии:**
- [ ] Использован `v-memo` для списков (EmployeeColumn, ZeroPoint)
- [ ] Использован `v-once` для статического контента (заголовки, fallback)
- [ ] Computed свойства оптимизированы (минимизация зависимостей)
- [ ] Watchers оптимизированы (immediate, deep)
- [ ] Количество ререндеров уменьшено
- [ ] Производительность улучшена
- [ ] Функциональность не нарушена

---

### 3. Оптимизация запросов к API

**Задачи:**
1. Батчинг запросов
   - Объединение нескольких запросов `crm.item.get` в один батч
   - Использование `batch` метода Bitrix24 REST API
   - Оптимизация загрузки данных тикетов
2. Оптимизация параметров запросов
   - Выбор только нужных полей (параметр `select`)
   - Избежание запроса всех полей (`select: ['*']`)
   - Оптимизация фильтров
3. Кеширование ответов
   - Кеширование в репозиториях (`ticket-repository.js`, `employee-repository.js`)
   - TTL для кеша ответов (5 минут)
   - Инвалидация при обновлении данных
4. Оптимизация повторных запросов
   - Проверка кеша перед запросом
   - Избежание дублирующих запросов
   - Дебаунсинг запросов (если применимо)

**Файлы для оптимизации:**
- `vue-app/src/services/dashboard-sector-1c/data/ticket-repository.js`
- `vue-app/src/services/dashboard-sector-1c/data/employee-repository.js`
- `vue-app/src/services/dashboard-sector-1c/index.js` (батчинг)

**Примеры оптимизации:**

1. **Батчинг запросов:**
```javascript
// Вместо нескольких запросов:
const ticket1 = await TicketRepository.getTicket(1);
const ticket2 = await TicketRepository.getTicket(2);
const ticket3 = await TicketRepository.getTicket(3);

// Один батч-запрос:
const tickets = await TicketRepository.getTicketsBatch([1, 2, 3]);
```

2. **Оптимизация параметров:**
```javascript
// Вместо всех полей:
const ticket = await TicketRepository.getTicket(id, { select: ['*'] });

// Только нужные поля:
const ticket = await TicketRepository.getTicket(id, { 
  select: ['id', 'title', 'stageId', 'assignedById'] 
});
```

**Критерии:**
- [ ] Реализован батчинг запросов (объединение запросов)
- [ ] Параметры запросов оптимизированы (выбор только нужных полей)
- [ ] Кеширование ответов работает (TTL, инвалидация)
- [ ] Повторные запросы оптимизированы (проверка кеша)
- [ ] Количество запросов к API уменьшено
- [ ] Время загрузки данных улучшено
- [ ] Функциональность не нарушена

---

## 🔧 Технические требования

### Принципы оптимизации:

1. **Производительность**
   - Минимизация ререндеров (использование `v-memo`, `v-once`)
   - Оптимизация computed свойств (кеширование результатов)
   - Эффективное использование памяти (очистка кеша при необходимости)

2. **Кеширование**
   - Умная стратегия кеширования (TTL, инвалидация)
   - Своевременная инвалидация (при обновлении данных)
   - Минимизация дублирования (единый кеш для одинаковых данных)

3. **Запросы**
   - Батчинг где возможно (объединение запросов)
   - Кеширование ответов (в репозиториях)
   - Оптимизация параметров (выбор только нужных полей)

### ⚠️ Важно: Сохранение контекста функционала

**Критически важно сохранить следующую функциональность:**

1. **Функциональность:**
   - Все функции должны работать идентично
   - Данные должны отображаться корректно
   - Обновления данных должны работать

2. **Кеширование:**
   - Кеш не должен нарушать актуальность данных
   - Инвалидация кеша должна работать корректно
   - TTL не должен быть слишком коротким или длинным

3. **Рендеринг:**
   - Компоненты должны отображаться корректно
   - Оптимизации не должны нарушать реактивность
   - Списки должны обновляться при изменении данных

**Что НЕЛЬЗЯ изменять:**
- ❌ Функциональность компонентов
- ❌ Структуру данных
- ❌ API методов
- ❌ Логику обновления данных

---

## ✅ Критерии приёмки

- [ ] Производительность улучшена
- [ ] Кеширование оптимизировано
- [ ] Рендеринг оптимизирован
- [ ] Запросы к API оптимизированы
- [ ] Функциональность не нарушена
- [ ] Нет регрессий
- [ ] Код соответствует стандартам проекта

---

## 🧪 Тестирование

### Производительность:

1. **Проверить время загрузки дашборда:**
   - Измерить время загрузки до оптимизации (базовая линия)
   - Измерить время загрузки после оптимизации
   - Ожидаемое улучшение: 20-30% (за счёт кеширования и батчинга)
   - Использовать Performance API для измерения

2. **Проверить время рендеринга компонентов:**
   - Измерить время первого рендера компонентов
   - Измерить время ререндеров (должно быть меньше с `v-memo`)
   - Проверить количество ререндеров (должно быть меньше)
   - Использовать Vue DevTools Performance для анализа

3. **Проверить использование памяти:**
   - Измерить использование памяти до оптимизации
   - Измерить использование памяти после оптимизации
   - Проверить, что кеш не приводит к утечкам памяти
   - Проверить очистку кеша при необходимости

4. **Проверить количество запросов к API:**
   - Подсчитать количество запросов до оптимизации
   - Подсчитать количество запросов после оптимизации (батчинг, кеш)
   - Ожидаемое уменьшение: 30-50% (за счёт батчинга и кеширования)
   - Использовать Network tab в DevTools

5. **Проверить метрики оптимизации:**
   - FCP (First Contentful Paint) — должно улучшиться
   - LCP (Largest Contentful Paint) — должно улучшиться
   - TTI (Time to Interactive) — должно улучшиться
   - Количество ререндеров — должно уменьшиться

### Функциональное тестирование:

1. **Проверить работу кеша:**
   - Загрузить дашборд и проверить, что данные кешируются
   - Обновить данные и проверить, что кеш обновляется
   - Проверить TTL кеша (данные должны обновляться после истечения TTL)
   - Проверить, что кеш не нарушает актуальность данных

2. **Проверить инвалидацию кеша:**
   - Обновить тикет и проверить, что кеш инвалидируется
   - Вызвать метод очистки кеша и проверить, что кеш очищен
   - Проверить частичную инвалидацию (только изменённые данные)
   - Проверить, что после инвалидации данные загружаются заново

3. **Проверить работу оптимизаций:**
   - Проверить, что `v-memo` работает корректно (ререндер только при изменении)
   - Проверить, что `v-once` работает корректно (статический контент не ререндерится)
   - Проверить, что computed свойства кешируются корректно
   - Проверить, что батчинг запросов работает (объединение запросов)

4. **Проверить функциональность:**
   - Проверить, что все функции работают идентично
   - Проверить, что данные отображаются корректно
   - Проверить, что обновления данных работают
   - Проверить, что нет регрессий

---

## 📚 Связанные документы

- **Родительская задача:** `DOCS/TASKS/TASK-015-refactor-tasks-008-009-011-012-013-014.md`

---

## 📝 История правок

- **2025-12-06 21:30 (UTC+3, Брест):** Создана подзадача STEP-08 для оптимизации производительности

---

**Автор:** Рефактор-менеджер  
**Статус:** Новая

