# TASK-030-05: Тестирование и проверка функциональности карточки тикета

**Дата создания:** 2025-12-11 21:00 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js) / Тестировщик  
**Родительская задача:** TASK-030

## Описание

Провести комплексное тестирование всех изменений, внесённых в карточку тикета в рамках задачи TASK-030. Убедиться в корректной работе отображения отдела заказчика, даты создания с визуальными акцентами, обработке граничных случаев, совместимости с браузерами и производительности.

## Контекст

В рамках задачи TASK-030 были внесены следующие изменения:
- **Этап 1 (TASK-030-01):** Добавлено поле `departmentHead` в маппер тикетов
- **Этап 2 (TASK-030-02):** Созданы утилиты для работы с датами и визуальными акцентами
- **Этап 3 (TASK-030-03):** Добавлено отображение отдела заказчика в левом верхнем углу
- **Этап 4 (TASK-030-04):** Добавлено отображение даты создания с визуальными акцентами в левом нижнем углу

Теперь необходимо провести полное тестирование всех изменений, чтобы убедиться в их корректной работе и отсутствии регрессий.

## Модули и компоненты

- `vue-app/src/components/dashboard/TicketCard.vue` — компонент карточки тикета (основной компонент для тестирования)
- `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js` — маппер тикетов (проверка извлечения полей)
- `vue-app/src/services/dashboard-sector-1c/utils/date-utils.js` — утилиты для работы с датами (проверка функций)
- `vue-app/src/services/dashboard-sector-1c/utils/date-accent-config.js` — конфигурация визуальных акцентов (проверка конфигурации)

## Зависимости

- Зависит от этапов 1-4 (TASK-030-01, TASK-030-02, TASK-030-03, TASK-030-04): все изменения должны быть реализованы
- Требует доступа к дашборду сектора 1С для ручного тестирования
- Требует доступа к консоли браузера для проверки ошибок

---

## Детальный план тестирования

### 1. Подготовка к тестированию

#### 1.1. Проверка окружения

**Действие:**
1. Убедиться, что все изменения из этапов 1-4 применены
2. Проверить, что приложение запускается без ошибок
3. Проверить консоль браузера на наличие ошибок при загрузке

**Проверка:**
- [ ] Приложение запускается без ошибок
- [ ] Консоль браузера не содержит ошибок JavaScript
- [ ] Все файлы загружаются корректно (нет 404 ошибок)

#### 1.2. Подготовка тестовых данных

**Действие:**
1. Определить тикеты с разными характеристиками для тестирования:
   - Тикет с заполненным отделом заказчика (короткий текст)
   - Тикет с заполненным отделом заказчика (длинный текст, > 10 символов)
   - Тикет без отдела заказчика
   - Тикет с датой создания сегодня
   - Тикет с датой создания на этой неделе
   - Тикет с датой создания на прошлой неделе
   - Тикет с датой создания более года
   - Тикет без даты создания
   - Тикет с некорректной датой

**Проверка:**
- [ ] Тестовые данные подготовлены
- [ ] Есть доступ к тикетам с разными характеристиками

---

### 2. Проверка отображения отдела заказчика

#### 2.1. Проверка базового отображения

**Тестовый сценарий 1: Тикет с заполненным отделом заказчика (короткий текст)**

**Входные данные:**
```javascript
const ticket = {
  id: 12345,
  departmentHead: 'Отдел'  // 6 символов
};
```

**Действие:**
1. Открыть дашборд сектора 1С
2. Найти тикет с ID 12345
3. Проверить отображение элемента отдела заказчика

**Ожидаемый результат:**
- Элемент `.ticket-department` отображается в левом верхнем углу карточки
- Текст: "Отдел" (без обрезки)
- Позиция: `top: 8px`, `left: 8px`
- Стили: полупрозрачный белый фон, серый текст

**Проверка:**
- [ ] Элемент отображается
- [ ] Текст корректный
- [ ] Позиция корректна
- [ ] Стили применены

**Тестовый сценарий 2: Тикет с заполненным отделом заказчика (длинный текст)**

**Входные данные:**
```javascript
const ticket = {
  id: 12346,
  departmentHead: 'Отдел продаж и маркетинга'  // 28 символов, должно быть обрезано до 10
};
```

**Действие:**
1. Найти тикет с ID 12346
2. Проверить отображение элемента отдела заказчика

**Ожидаемый результат:**
- Элемент отображается
- Текст: "Отдел прод" (10 символов, обрезано в маппере)
- Если текст всё ещё длинный, применяется обрезка с многоточием (`text-overflow: ellipsis`)

**Проверка:**
- [ ] Текст обрезан до 10 символов (в маппере)
- [ ] Если текст длиннее, применяется обрезка с многоточием
- [ ] Текст читаем

**Тестовый сценарий 3: Тикет без отдела заказчика**

**Входные данные:**
```javascript
const ticket = {
  id: 12347,
  departmentHead: null
};
```

**Действие:**
1. Найти тикет с ID 12347
2. Проверить отсутствие элемента отдела заказчика

**Ожидаемый результат:**
- Элемент `.ticket-department` не отображается (условие `v-if="ticket.departmentHead"`)

**Проверка:**
- [ ] Элемент не отображается
- [ ] Нет ошибок в консоли

#### 2.2. Проверка позиционирования

**Тестовый сценарий: Позиция элемента отдела заказчика**

**Действие:**
1. Открыть DevTools браузера
2. Выбрать элемент `.ticket-department` через инспектор
3. Проверить computed-стили

**Ожидаемый результат:**
- `position: absolute`
- `top: 8px`
- `left: 8px`
- `z-index: 1`

**Проверка:**
- [ ] Позиция корректна
- [ ] Элемент не перекрывает иконку тикета (🎫) критично
- [ ] Элемент не перекрывает ID тикета (#12345) критично

#### 2.3. Проверка на разных этапах

**Тестовый сценарий: Отображение на разных этапах**

**Действие:**
1. Проверить отображение элемента на этапе "Сформировано обращение"
2. Проверить отображение элемента на этапе "Рассмотрение ТЗ"
3. Проверить отображение элемента на этапе "Исполнение"

**Ожидаемый результат:**
- Элемент отображается корректно на всех этапах
- Позиция не меняется
- Стили применяются одинаково

**Проверка:**
- [ ] Элемент отображается на всех этапах
- [ ] Позиция стабильна
- [ ] Стили применяются корректно

---

### 3. Проверка отображения даты создания

#### 3.1. Проверка базового отображения

**Тестовый сценарий 1: Тикет с датой создания сегодня**

**Входные данные:**
```javascript
const ticket = {
  id: 12348,
  createdAt: '2024-12-11T15:00:00+02:00'  // Сегодня
};
```

**Действие:**
1. Найти тикет с ID 12348
2. Проверить отображение элемента даты создания

**Ожидаемый результат:**
- Элемент `.ticket-created-date` отображается в левом нижнем углу карточки
- Метка: "СЕГОДНЯ"
- Дата: "11.12.2024" (формат DD.MM.YYYY)
- Цвета: зелёный акцент
  - Фон: `#d4edda`
  - Текст: `#155724`
  - Граница: `#28a745`

**Проверка:**
- [ ] Элемент отображается
- [ ] Метка корректная
- [ ] Дата отформатирована корректно
- [ ] Цвета применены

**Тестовый сценарий 2: Тикет с датой создания на этой неделе**

**Входные данные:**
```javascript
const ticket = {
  id: 12349,
  createdAt: '2024-12-09T10:00:00+02:00'  // 2 дня назад (на этой неделе)
};
```

**Ожидаемый результат:**
- Элемент отображается
- Метка: "НА ЭТОЙ НЕДЕЛЕ"
- Дата: "09.12.2024"
- Цвета: голубой акцент
  - Фон: `#d1ecf1`
  - Текст: `#0c5460`
  - Граница: `#17a2b8`

**Проверка:**
- [ ] Метка корректная
- [ ] Дата корректная
- [ ] Цвета применены

**Тестовый сценарий 3: Тикет с датой создания на прошлой неделе**

**Входные данные:**
```javascript
const ticket = {
  id: 12350,
  createdAt: '2024-12-04T10:00:00+02:00'  // 7 дней назад (на прошлой неделе)
};
```

**Ожидаемый результат:**
- Элемент отображается
- Метка: "НА ПРОШЛОЙ НЕДЕЛЕ"
- Дата: "04.12.2024"
- Цвета: жёлтый акцент
  - Фон: `#fff3cd`
  - Текст: `#856404`
  - Граница: `#ffc107`

**Проверка:**
- [ ] Метка корректная
- [ ] Дата корректная
- [ ] Цвета применены

**Тестовый сценарий 4: Тикет с датой создания более двух недель**

**Входные данные:**
```javascript
const ticket = {
  id: 12351,
  createdAt: '2024-11-25T10:00:00+02:00'  // 16 дней назад
};
```

**Ожидаемый результат:**
- Элемент отображается
- Метка: "БОЛЕЕ ДВУХ НЕДЕЛЬ"
- Дата: "25.11.2024"
- Цвета: оранжевый акцент
  - Фон: `#ffe5d0`
  - Текст: `#7d3f00`
  - Граница: `#fd7e14`

**Проверка:**
- [ ] Метка корректная
- [ ] Дата корректная
- [ ] Цвета применены

**Тестовый сценарий 5: Тикет с датой создания до 1 месяца**

**Входные данные:**
```javascript
const ticket = {
  id: 12352,
  createdAt: '2024-11-15T10:00:00+02:00'  // 26 дней назад (до 1 месяца)
};
```

**Ожидаемый результат:**
- Элемент отображается
- Метка: "ДО 1 МЕСЯЦА"
- Дата: "15.11.2024"
- Цвета: красный акцент
  - Фон: `#f8d7da`
  - Текст: `#721c24`
  - Граница: `#dc3545`

**Проверка:**
- [ ] Метка корректная
- [ ] Дата корректная
- [ ] Цвета применены

**Тестовый сценарий 6: Тикет с датой создания более 2 месяцев**

**Входные данные:**
```javascript
const ticket = {
  id: 12353,
  createdAt: '2024-10-01T10:00:00+02:00'  // 71 день назад (более 2 месяцев)
};
```

**Ожидаемый результат:**
- Элемент отображается
- Метка: "БОЛЕЕ 2 МЕСЯЦЕВ"
- Дата: "01.10.2024"
- Цвета: серый акцент
  - Фон: `#e2e3e5`
  - Текст: `#383d41`
  - Граница: `#6c757d`

**Проверка:**
- [ ] Метка корректная
- [ ] Дата корректная
- [ ] Цвета применены

**Тестовый сценарий 7: Тикет с датой создания более полугода**

**Входные данные:**
```javascript
const ticket = {
  id: 12354,
  createdAt: '2024-06-01T10:00:00+02:00'  // 193 дня назад (более полугода)
};
```

**Ожидаемый результат:**
- Элемент отображается
- Метка: "БОЛЕЕ ПОЛУГОДА"
- Дата: "01.06.2024"
- Цвета: серый акцент
  - Фон: `#d6d8db`
  - Текст: `#212529`
  - Граница: `#6c757d`

**Проверка:**
- [ ] Метка корректная
- [ ] Дата корректная
- [ ] Цвета применены

**Тестовый сценарий 8: Тикет с датой создания более года**

**Входные данные:**
```javascript
const ticket = {
  id: 12355,
  createdAt: '2023-01-01T10:00:00+02:00'  // Более года
};
```

**Ожидаемый результат:**
- Элемент отображается
- Метка: "БОЛЕЕ ГОДА"
- Дата: "01.01.2023"
- Цвета: тёмно-серый акцент
  - Фон: `#c6c8ca`
  - Текст: `#000000`
  - Граница: `#343a40`

**Проверка:**
- [ ] Метка корректная
- [ ] Дата корректная
- [ ] Цвета применены

**Тестовый сценарий 9: Тикет без даты создания**

**Входные данные:**
```javascript
const ticket = {
  id: 12356,
  createdAt: null
};
```

**Действие:**
1. Найти тикет с ID 12356
2. Проверить отсутствие элемента даты создания

**Ожидаемый результат:**
- Элемент `.ticket-created-date` не отображается (условие `v-if="formattedCreatedDate"`)

**Проверка:**
- [ ] Элемент не отображается
- [ ] Нет ошибок в консоли

**Тестовый сценарий 10: Тикет с некорректной датой**

**Входные данные:**
```javascript
const ticket = {
  id: 12357,
  createdAt: 'invalid-date'
};
```

**Ожидаемый результат:**
- Элемент не отображается (парсинг вернёт `null`, форматирование вернёт пустую строку)

**Проверка:**
- [ ] Элемент не отображается
- [ ] Нет ошибок в консоли
- [ ] Нет предупреждений в консоли

#### 3.2. Проверка форматирования даты

**Тестовый сценарий: Формат даты DD.MM.YYYY**

**Действие:**
1. Проверить несколько тикетов с разными датами
2. Убедиться, что формат даты всегда DD.MM.YYYY

**Ожидаемый результат:**
- Формат: `DD.MM.YYYY` (день.месяц.год)
- Разделитель: точка (`.`)
- Ведущие нули добавляются (например, `01.01.2024`, а не `1.1.2024`)

**Примеры:**
- `2024-12-11T15:00:00+02:00` → `11.12.2024`
- `2024-01-05T00:00:00Z` → `05.01.2024`
- `2024-12-01T10:30:00` → `01.12.2024`

**Проверка:**
- [ ] Формат всегда DD.MM.YYYY
- [ ] Разделитель — точка
- [ ] Ведущие нули добавляются

#### 3.3. Проверка позиционирования

**Тестовый сценарий: Позиция элемента даты создания**

**Действие:**
1. Открыть DevTools браузера
2. Выбрать элемент `.ticket-created-date` через инспектор
3. Проверить computed-стили

**Ожидаемый результат:**
- `position: absolute`
- `bottom: 8px`
- `left: 8px`
- `z-index: 1`

**Проверка:**
- [ ] Позиция корректна
- [ ] Элемент не перекрывает описание тикета критично
- [ ] Элемент не перекрывает другие элементы критично

#### 3.4. Проверка структуры элемента даты

**Тестовый сценарий: Структура элемента даты**

**Действие:**
1. Открыть DevTools браузера
2. Выбрать элемент `.ticket-created-date`
3. Проверить структуру дочерних элементов

**Ожидаемый результат:**
- Контейнер `.ticket-created-date` содержит два span-элемента:
  1. `.ticket-date-label` — метка категории (например, "СЕГОДНЯ")
  2. `.ticket-date-value` — дата (например, "11.12.2024")

**Проверка:**
- [ ] Структура корректна (контейнер + 2 span)
- [ ] Метка отображается сверху
- [ ] Дата отображается снизу
- [ ] Отступ между меткой и датой (`gap: 2px`)

---

### 4. Проверка обработки граничных случаев

#### 4.1. Граничные случаи для отдела заказчика

**Тестовый сценарий 1: Пустая строка**

**Входные данные:**
```javascript
const ticket = {
  id: 12358,
  departmentHead: ''
};
```

**Ожидаемый результат:**
- Элемент не отображается (условие `v-if` проверяет на пустую строку)

**Проверка:**
- [ ] Элемент не отображается
- [ ] Нет ошибок

**Тестовый сценарий 2: Только пробелы**

**Входные данные:**
```javascript
const ticket = {
  id: 12359,
  departmentHead: '   '  // Только пробелы
};
```

**Ожидаемый результат:**
- Элемент не отображается (маппер должен обрезать пробелы, результат — пустая строка)

**Проверка:**
- [ ] Элемент не отображается
- [ ] Нет ошибок

**Тестовый сценарий 3: Очень длинный текст (если не обрезан в маппере)**

**Входные данные:**
```javascript
const ticket = {
  id: 12360,
  departmentHead: 'Очень длинное название отдела заказчика которое не должно быть обрезано в маппере'  // 80+ символов
};
```

**Ожидаемый результат:**
- Если маппер не обрезал, текст обрезается CSS с многоточием (`text-overflow: ellipsis`)
- Максимальная ширина: `80px`

**Проверка:**
- [ ] Текст обрезается CSS, если не обрезан в маппере
- [ ] Многоточие отображается
- [ ] Максимальная ширина соблюдается

#### 4.2. Граничные случаи для даты создания

**Тестовый сценарий 1: Пустая строка**

**Входные данные:**
```javascript
const ticket = {
  id: 12361,
  createdAt: ''
};
```

**Ожидаемый результат:**
- Элемент не отображается (`formattedCreatedDate` вернёт пустую строку)

**Проверка:**
- [ ] Элемент не отображается
- [ ] Нет ошибок

**Тестовый сценарий 2: Некорректный формат даты**

**Входные данные:**
```javascript
const ticket = {
  id: 12362,
  createdAt: 'not-a-date'
};
```

**Ожидаемый результат:**
- Элемент не отображается (парсинг вернёт `null`, форматирование вернёт пустую строку)

**Проверка:**
- [ ] Элемент не отображается
- [ ] Нет ошибок в консоли

**Тестовый сценарий 3: Дата в будущем**

**Входные данные:**
```javascript
const ticket = {
  id: 12363,
  createdAt: '2025-12-31T10:00:00+02:00'  // Дата в будущем
};
```

**Ожидаемый результат:**
- Элемент отображается
- Категория: "СЕГОДНЯ" или "НА ЭТОЙ НЕДЕЛЕ" (в зависимости от логики определения категории)
- Дата форматируется корректно

**Проверка:**
- [ ] Элемент отображается
- [ ] Категория определяется корректно (или обрабатывается как граничный случай)
- [ ] Дата форматируется корректно

**Тестовый сценарий 4: Дата без часового пояса**

**Входные данные:**
```javascript
const ticket = {
  id: 12364,
  createdAt: '2024-12-11T15:00:00'  // Без часового пояса
};
```

**Ожидаемый результат:**
- Элемент отображается
- Дата парсится корректно (используется локальный часовой пояс)
- Дата форматируется корректно

**Проверка:**
- [ ] Элемент отображается
- [ ] Дата парсится корректно
- [ ] Дата форматируется корректно

**Тестовый сценарий 5: Дата с разными часовыми поясами**

**Входные данные:**
```javascript
const ticket1 = {
  id: 12365,
  createdAt: '2024-12-11T15:00:00+02:00'  // UTC+2
};

const ticket2 = {
  id: 12366,
  createdAt: '2024-12-11T15:00:00Z'  // UTC
};

const ticket3 = {
  id: 12367,
  createdAt: '2024-12-11T15:00:00+05:00'  // UTC+5
};
```

**Ожидаемый результат:**
- Все элементы отображаются
- Даты парсятся корректно с учётом часового пояса
- Даты форматируются в локальное время (DD.MM.YYYY)

**Проверка:**
- [ ] Все элементы отображаются
- [ ] Даты парсятся корректно
- [ ] Даты форматируются корректно

#### 4.3. Граничные случаи для обоих элементов

**Тестовый сценарий: Тикет без обоих полей**

**Входные данные:**
```javascript
const ticket = {
  id: 12368,
  departmentHead: null,
  createdAt: null
};
```

**Ожидаемый результат:**
- Оба элемента не отображаются
- Карточка отображается корректно
- Нет ошибок

**Проверка:**
- [ ] Оба элемента не отображаются
- [ ] Карточка отображается корректно
- [ ] Нет ошибок

**Тестовый сценарий: Тикет с обоими полями**

**Входные данные:**
```javascript
const ticket = {
  id: 12369,
  departmentHead: 'Отдел',
  createdAt: '2024-12-11T15:00:00+02:00'
};
```

**Ожидаемый результат:**
- Оба элемента отображаются
- Элемент отдела заказчика в левом верхнем углу
- Элемент даты создания в левом нижнем углу
- Элементы не перекрывают друг друга

**Проверка:**
- [ ] Оба элемента отображаются
- [ ] Позиции корректны
- [ ] Элементы не перекрывают друг друга

---

### 5. Проверка совместимости

#### 5.1. Проверка в разных браузерах

**Тестовый сценарий: Chrome**

**Действие:**
1. Открыть дашборд сектора 1С в Chrome
2. Проверить отображение элементов
3. Проверить консоль на наличие ошибок

**Ожидаемый результат:**
- Элементы отображаются корректно
- Стили применяются корректно
- Нет ошибок в консоли

**Проверка:**
- [ ] Элементы отображаются
- [ ] Стили применены
- [ ] Нет ошибок

**Тестовый сценарий: Firefox**

**Действие:**
1. Открыть дашборд сектора 1С в Firefox
2. Проверить отображение элементов
3. Проверить консоль на наличие ошибок

**Ожидаемый результат:**
- Элементы отображаются корректно
- Стили применяются корректно
- Нет ошибок в консоли

**Проверка:**
- [ ] Элементы отображаются
- [ ] Стили применены
- [ ] Нет ошибок

**Тестовый сценарий: Safari**

**Действие:**
1. Открыть дашборд сектора 1С в Safari
2. Проверить отображение элементов
3. Проверить консоль на наличие ошибок

**Ожидаемый результат:**
- Элементы отображаются корректно
- Стили применяются корректно
- Нет ошибок в консоли

**Проверка:**
- [ ] Элементы отображаются
- [ ] Стили применены
- [ ] Нет ошибок

**Тестовый сценарий: Edge**

**Действие:**
1. Открыть дашборд сектора 1С в Edge
2. Проверить отображение элементов
3. Проверить консоль на наличие ошибок

**Ожидаемый результат:**
- Элементы отображаются корректно
- Стили применяются корректно
- Нет ошибок в консоли

**Проверка:**
- [ ] Элементы отображаются
- [ ] Стили применены
- [ ] Нет ошибок

#### 5.2. Проверка на мобильных устройствах

**Тестовый сценарий: Мобильный телефон (320px - 480px)**

**Действие:**
1. Открыть DevTools браузера
2. Включить режим эмуляции мобильного устройства (320px - 480px)
3. Проверить отображение элементов

**Ожидаемый результат:**
- Элементы отображаются корректно
- Текст читаем
- Элементы не перекрывают другие элементы критично
- Карточка не ломается

**Проверка:**
- [ ] Элементы отображаются
- [ ] Текст читаем
- [ ] Карточка не ломается

**Тестовый сценарий: Планшет (768px - 1024px)**

**Действие:**
1. Включить режим эмуляции планшета (768px - 1024px)
2. Проверить отображение элементов

**Ожидаемый результат:**
- Элементы отображаются корректно
- Текст читаем
- Карточка не ломается

**Проверка:**
- [ ] Элементы отображаются
- [ ] Текст читаем
- [ ] Карточка не ломается

#### 5.3. Проверка существующего функционала

**Тестовый сценарий: Клик по карточке**

**Действие:**
1. Кликнуть по карточке тикета
2. Проверить, что открывается iframe Bitrix24

**Ожидаемый результат:**
- Открывается новая вкладка с iframe Bitrix24
- URL корректный
- Новые элементы не мешают клику

**Проверка:**
- [ ] Открывается новая вкладка
- [ ] URL корректный
- [ ] Новые элементы не мешают клику

**Тестовый сценарий: Drag & Drop**

**Действие:**
1. Перетащить тикет из одной колонки в другую
2. Проверить, что перетаскивание работает

**Ожидаемый результат:**
- Перетаскивание работает корректно
- Новые элементы не мешают перетаскиванию
- Визуальный эффект перетаскивания работает

**Проверка:**
- [ ] Перетаскивание работает
- [ ] Новые элементы не мешают
- [ ] Визуальный эффект работает

**Тестовый сценарий: Hover эффект**

**Действие:**
1. Навести курсор на карточку тикета
2. Проверить hover эффект

**Ожидаемый результат:**
- Hover эффект работает (тень, поднятие карточки)
- Новые элементы не мешают hover эффекту

**Проверка:**
- [ ] Hover эффект работает
- [ ] Новые элементы не мешают

---

### 6. Проверка производительности

#### 6.1. Проверка времени рендеринга

**Тестовый сценарий: Рендеринг большого количества тикетов**

**Действие:**
1. Открыть DevTools браузера
2. Включить Performance profiling
3. Загрузить дашборд с большим количеством тикетов (100+)
4. Измерить время рендеринга

**Ожидаемый результат:**
- Время рендеринга не увеличилось значительно по сравнению с версией до изменений
- Нет задержек при отображении карточек

**Проверка:**
- [ ] Время рендеринга приемлемое (< 2 секунд для 100 тикетов)
- [ ] Нет задержек

#### 6.2. Проверка вычисления computed-свойств

**Тестовый сценарий: Эффективность computed-свойств**

**Действие:**
1. Открыть DevTools браузера
2. Включить Performance profiling
3. Проверить, что computed-свойства вычисляются только при необходимости

**Ожидаемый результат:**
- Computed-свойства вычисляются только при изменении зависимостей
- Нет лишних вычислений

**Проверка:**
- [ ] Computed-свойства вычисляются эффективно
- [ ] Нет лишних вычислений

#### 6.3. Проверка использования памяти

**Тестовый сценарий: Использование памяти**

**Действие:**
1. Открыть DevTools браузера
2. Включить Memory profiling
3. Загрузить дашборд с большим количеством тикетов
4. Измерить использование памяти

**Ожидаемый результат:**
- Использование памяти не увеличилось значительно
- Нет утечек памяти

**Проверка:**
- [ ] Использование памяти приемлемое
- [ ] Нет утечек памяти

---

### 7. Проверка консоли на ошибки

#### 7.1. Проверка JavaScript ошибок

**Действие:**
1. Открыть консоль браузера
2. Загрузить дашборд сектора 1С
3. Проверить наличие ошибок JavaScript

**Ожидаемый результат:**
- Нет ошибок JavaScript
- Нет предупреждений (warnings)

**Проверка:**
- [ ] Нет ошибок JavaScript
- [ ] Нет предупреждений

#### 7.2. Проверка ошибок загрузки ресурсов

**Действие:**
1. Открыть вкладку Network в DevTools
2. Загрузить дашборд сектора 1С
3. Проверить наличие ошибок загрузки (404, 500, и т.д.)

**Ожидаемый результат:**
- Нет ошибок загрузки ресурсов
- Все файлы загружаются корректно

**Проверка:**
- [ ] Нет ошибок загрузки
- [ ] Все файлы загружаются

---

## Чек-лист тестирования

### Обязательные проверки

- [ ] **Отображение отдела заказчика**
  - [ ] Элемент отображается в левом верхнем углу
  - [ ] Текст корректный (с обрезкой до 10 символов)
  - [ ] Позиция корректна (`top: 8px`, `left: 8px`)
  - [ ] Стили применены
  - [ ] Элемент не отображается, если поле пустое

- [ ] **Отображение даты создания**
  - [ ] Элемент отображается в левом нижнем углу
  - [ ] Дата форматируется в DD.MM.YYYY
  - [ ] Метка категории отображается
  - [ ] Визуальные акценты применяются для всех 8 категорий
  - [ ] Элемент не отображается, если дата отсутствует

- [ ] **Обработка граничных случаев**
  - [ ] Пустые значения обрабатываются корректно
  - [ ] Некорректные данные обрабатываются корректно
  - [ ] Очень длинные тексты обрезаются
  - [ ] Некорректные даты обрабатываются корректно

- [ ] **Совместимость**
  - [ ] Работает в Chrome
  - [ ] Работает в Firefox
  - [ ] Работает в Safari
  - [ ] Работает в Edge
  - [ ] Работает на мобильных устройствах
  - [ ] Работает на планшетах

- [ ] **Существующий функционал**
  - [ ] Клик по карточке работает
  - [ ] Drag & Drop работает
  - [ ] Hover эффект работает

- [ ] **Производительность**
  - [ ] Время рендеринга приемлемое
  - [ ] Computed-свойства вычисляются эффективно
  - [ ] Нет утечек памяти

- [ ] **Консоль**
  - [ ] Нет ошибок JavaScript
  - [ ] Нет предупреждений
  - [ ] Нет ошибок загрузки ресурсов

### Дополнительные проверки

- [ ] **Визуальные акценты для всех категорий**
  - [ ] СЕГОДНЯ — зелёный акцент
  - [ ] НА ЭТОЙ НЕДЕЛЕ — голубой акцент
  - [ ] НА ПРОШЛОЙ НЕДЕЛЕ — жёлтый акцент
  - [ ] БОЛЕЕ ДВУХ НЕДЕЛЬ — оранжевый акцент
  - [ ] ДО 1 МЕСЯЦА — красный акцент
  - [ ] БОЛЕЕ 2 МЕСЯЦЕВ — серый акцент
  - [ ] БОЛЕЕ ПОЛУГОДА — серый акцент
  - [ ] БОЛЕЕ ГОДА — тёмно-серый акцент

- [ ] **Форматирование даты**
  - [ ] Формат всегда DD.MM.YYYY
  - [ ] Разделитель — точка
  - [ ] Ведущие нули добавляются

- [ ] **Позиционирование**
  - [ ] Элемент отдела заказчика в левом верхнем углу
  - [ ] Элемент даты создания в левом нижнем углу
  - [ ] Элементы не перекрывают друг друга
  - [ ] Элементы не перекрывают другие элементы критично

---

## Примеры тестовых сценариев

### Пример 1: Полный тест тикета с обоими элементами

**Входные данные:**
```javascript
const ticket = {
  id: 12370,
  title: 'Тестовый тикет',
  departmentHead: 'Отдел продаж',
  createdAt: '2024-12-11T15:00:00+02:00',
  ufSubject: 'Тестовая тема',
  priorityLabel: 'Высокий',
  serviceLabel: '1С'
};
```

**Ожидаемый результат:**
- Элемент отдела заказчика отображается в левом верхнем углу: "Отдел прод" (10 символов)
- Элемент даты создания отображается в левом нижнем углу:
  - Метка: "СЕГОДНЯ"
  - Дата: "11.12.2024"
  - Цвета: зелёный акцент
- Оба элемента не перекрывают друг друга
- Карточка отображается корректно

### Пример 2: Тест тикета без обоих элементов

**Входные данные:**
```javascript
const ticket = {
  id: 12371,
  title: 'Тестовый тикет 2',
  departmentHead: null,
  createdAt: null,
  ufSubject: 'Тестовая тема 2',
  priorityLabel: 'Средний',
  serviceLabel: '1С'
};
```

**Ожидаемый результат:**
- Оба элемента не отображаются
- Карточка отображается корректно
- Нет ошибок в консоли

### Пример 3: Тест тикета с длинным отделом заказчика

**Входные данные:**
```javascript
const ticket = {
  id: 12372,
  title: 'Тестовый тикет 3',
  departmentHead: 'Отдел продаж и маркетинга с очень длинным названием',
  createdAt: '2024-12-09T10:00:00+02:00',
  ufSubject: 'Тестовая тема 3',
  priorityLabel: 'Низкий',
  serviceLabel: '1С'
};
```

**Ожидаемый результат:**
- Элемент отдела заказчика отображается: "Отдел прод" (10 символов, обрезано в маппере)
- Элемент даты создания отображается:
  - Метка: "НА ЭТОЙ НЕДЕЛЕ"
  - Дата: "09.12.2024"
  - Цвета: голубой акцент

---

## История правок

- **2025-12-11 21:00 (UTC+3, Брест):** Создана задача TASK-030-05
  - Добавлен детальный план тестирования
  - Добавлены тестовые сценарии для всех функций
  - Добавлены проверки совместимости и производительности
  - Добавлен чек-лист тестирования
  - Добавлены примеры тестовых сценариев

---

## Связанные документы

- `DOCS/TASKS/TASK-030-ticket-card-department-and-date.md` — родительская задача
- `DOCS/TASKS/TASK-030-01-add-department-head-to-mapper.md` — этап 1
- `DOCS/TASKS/TASK-030-02-create-date-utils-and-accent-config.md` — этап 2
- `DOCS/TASKS/TASK-030-03-add-department-head-to-ticket-card.md` — этап 3
- `DOCS/TASKS/TASK-030-04-add-created-date-with-accents-to-ticket-card.md` — этап 4
- `vue-app/src/components/dashboard/TicketCard.vue` — компонент для тестирования
- `DOCS/GUIDES/ui-ticket-card-guideline.md` — гайдлайн по карточке тикета

