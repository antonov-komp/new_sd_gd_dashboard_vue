# TASK-015-STEP-07: Улучшение интеграции логирования (TASK-014)

**Дата создания:** 2025-12-06 21:30 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Средний  
**Исполнитель:** Рефактор-менеджер  
**Родительская задача:** TASK-015  
**Связанная задача:** TASK-014

---

## 📋 Описание

Провести улучшение интеграции логирования, реализованного в TASK-014, с целью автоматизации контекста логирования, оптимизации форматирования сообщений и улучшения интеграции с компонентами.

**Цель:** Улучшить интеграцию `Logger` и `useLogger` для более удобного использования и автоматического определения контекста.

---

## 🎯 Контекст

**Текущее состояние (TASK-014):**

1. **Logger:**
   - Файл: `vue-app/src/services/dashboard-sector-1c/utils/logger.js` (127 строк)
   - Класс с методами: `log()`, `error()`, `warn()`, `info()`, `debug()`
   - Поддержка уровней логирования через `LoggerConfig.isEnabled()`
   - Форматирование сообщений: `[timestamp] [level] [context] message`
   - Текущее форматирование (строки 35-37):
     ```javascript
     const timestamp = new Date().toISOString();
     const contextStr = context ? `[${context}]` : '';
     const formattedMessage = `[${timestamp}] [${level}] ${contextStr} ${message}`;
     ```
   - Вывод в консоль через `console.error()`, `console.warn()`, `console.info()`, `console.log()`

2. **useLogger:**
   - Файл: `vue-app/src/composables/useLogger.js` (88 строк)
   - Композабл для использования в компонентах Vue
   - Принимает контекст вручную: `useLogger('ComponentName')`
   - Методы: `error()`, `warn()`, `info()`, `debug()`, `log()`
   - Текущая реализация (строки 27-86):
     ```javascript
     export function useLogger(context = '') {
       const error = (message, error = null) => {
         Logger.error(message, context, error);
       };
       // ... остальные методы ...
     }
     ```

3. **LoggerConfig:**
   - Файл: `vue-app/src/services/dashboard-sector-1c/utils/logger-config.js` (118 строк)
   - Управление уровнями логирования через `localStorage`
   - Уровни: `NONE`, `ERROR`, `WARN`, `INFO`, `DEBUG`
   - По умолчанию: `ERROR` для production, `DEBUG` для development

4. **Интеграция:**
   - Найдено 51 использование `Logger.*` и `useLogger` в 13 файлах
   - Все `console.*` заменены на `Logger.*` (согласно TASK-014)
   - Компоненты используют `useLogger('ComponentName')` вручную

**Текущее использование:**
- `EmployeeColumn.vue`: `const logger = useLogger('EmployeeColumn');`
- `DashboardStage.vue`: `const logger = useLogger('DashboardStage');`
- `ticket-grouper.js`: `Logger.warn('Tickets is not an array', 'TicketGrouper', tickets);`
- `ticket-details-service.js`: `Logger.error('Error getting ticket details', 'TicketDetailsService', error);`

**Выявленные проблемы:**
1. Контекст логирования можно автоматизировать лучше
   - Текущая реализация требует ручной передачи контекста: `useLogger('ComponentName')`
   - Можно использовать `getCurrentInstance()` для автоматического определения имени компонента
   - Это уменьшит количество ручной настройки
2. Форматирование сообщений можно оптимизировать
   - Текущее форматирование простое (строка с timestamp, level, context, message)
   - Можно добавить цвета для DevTools (через CSS стили в `console.*`)
   - Можно улучшить читаемость с группировкой
3. Можно добавить группировку логов
   - Текущая реализация выводит логи по одному
   - Можно группировать логи по контексту (сворачиваемые группы в DevTools)
   - Это улучшит читаемость при большом количестве логов
4. Можно улучшить интеграцию с DevTools
   - Текущая реализация использует стандартные методы `console.*`
   - Можно добавить CSS стили для цветов
   - Можно добавить группировку через `console.group()`

---

## 🏗️ Модули и компоненты

### Файлы для рефакторинга:

1. **`vue-app/src/services/dashboard-sector-1c/utils/logger.js`**
   - Улучшение форматирования сообщений
   - Добавление группировки логов
   - Улучшение интеграции с DevTools

2. **`vue-app/src/composables/useLogger.js`**
   - Улучшение автоматического определения контекста
   - Добавление дополнительных методов

---

## 📝 Ступенчатые подзадачи

### 1. Улучшение форматирования сообщений

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/logger.js`

**Улучшения:**
1. Добавить группировку логов по контексту
2. Улучшить форматирование с цветами (для DevTools)
3. Добавить поддержку структурированных данных

**Пример:**
```javascript
/**
 * Форматирование сообщения с улучшенным выводом
 * 
 * @param {string} level - Уровень логирования
 * @param {string} message - Сообщение
 * @param {string} context - Контекст
 * @param {*} data - Данные
 * @returns {string} Отформатированное сообщение
 */
function formatMessage(level, message, context, data) {
  const timestamp = new Date().toISOString();
  const levelColor = getLevelColor(level);
  
  // Форматирование для DevTools с цветами
  const formattedContext = `%c[${timestamp}] %c[${level}] %c[${context}]`;
  const formattedMessage = `%c${message}`;
  
  return {
    formatted: `${formattedContext} ${formattedMessage}`,
    styles: [
      'color: #6c757d; font-weight: normal', // timestamp
      `color: ${levelColor}; font-weight: bold`, // level
      'color: #007bff; font-weight: normal', // context
      'color: #212529; font-weight: normal' // message
    ],
    data
  };
}

/**
 * Получение цвета для уровня логирования
 * 
 * @param {string} level - Уровень логирования
 * @returns {string} Цвет в формате CSS
 */
function getLevelColor(level) {
  const colors = {
    ERROR: '#dc3545',
    WARN: '#ffc107',
    INFO: '#17a2b8',
    DEBUG: '#6c757d'
  };
  
  return colors[level] || '#6c757d';
}
```

**Критерии:**
- [ ] Улучшено форматирование сообщений
- [ ] Добавлена поддержка цветов для DevTools
- [ ] Добавлена группировка логов
- [ ] Улучшена читаемость логов

---

### 2. Улучшение автоматического определения контекста

**Файл:** `vue-app/src/composables/useLogger.js`

**Улучшения:**
1. Автоматическое определение имени компонента из `getCurrentInstance()`
2. Автоматическое определение имени функции из stack trace
3. Улучшенная интеграция с Vue компонентами

**Пример:**
```javascript
import { getCurrentInstance } from 'vue';
import { Logger } from '@/services/dashboard-sector-1c/utils/logger.js';

/**
 * Композабл для логирования в компонентах Vue
 * 
 * Автоматически определяет контекст из имени компонента или функции.
 * 
 * @param {string} context - Контекст логирования (опционально, определяется автоматически)
 * @returns {object} Объект с методами логирования
 */
export function useLogger(context = null) {
  // Автоматическое определение контекста из компонента
  const instance = getCurrentInstance();
  const componentName = instance?.type?.name || instance?.type?.__name || null;
  
  // Использование переданного контекста или автоматического
  const loggerContext = context || componentName || 'Unknown';
  
  /**
   * Логирование с автоматическим контекстом
   */
  const log = (level, message, data = null) => {
    Logger.log(level, message, loggerContext, data);
  };
  
  return {
    error: (message, data) => log('ERROR', message, data),
    warn: (message, data) => log('WARN', message, data),
    info: (message, data) => log('INFO', message, data),
    debug: (message, data) => log('DEBUG', message, data),
    context: loggerContext
  };
}
```

**Критерии:**
- [ ] Улучшено автоматическое определение контекста
- [ ] Добавлена поддержка определения из компонента
- [ ] Добавлена поддержка определения из функции
- [ ] Улучшена интеграция с Vue компонентами

---

### 3. Добавление группировки логов

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/logger.js`

**Улучшения:**
1. Группировка логов по контексту
2. Сворачиваемые группы в DevTools
3. Подсчёт количества логов в группе

**Пример:**
```javascript
/**
 * Группировка логов по контексту
 */
const logGroups = new Map();

/**
 * Логирование с группировкой
 * 
 * @param {string} level - Уровень логирования
 * @param {string} message - Сообщение
 * @param {string} context - Контекст
 * @param {*} data - Данные
 */
static log(level, message, context = '', data = null) {
  const config = LoggerConfig.getLevel();
  if (!LoggerConfig.isEnabled(level, config)) {
    return;
  }
  
  // Группировка по контексту
  if (context && !logGroups.has(context)) {
    logGroups.set(context, []);
  }
  
  const formatted = formatMessage(level, message, context, data);
  
  // Добавление в группу
  if (context) {
    logGroups.get(context).push({ level, message, data, timestamp: new Date() });
  }
  
  // Вывод в консоль с группировкой
  if (context && logGroups.get(context).length === 1) {
    console.group(`%c${context}`, 'color: #007bff; font-weight: bold');
  }
  
  // Вывод сообщения
  switch (level) {
    case 'ERROR':
      console.error(...formatted.styles ? [formatted.formatted, ...formatted.styles, formatted.data] : [formatted.formatted, formatted.data]);
      break;
    // ... остальные уровни
  }
  
  // Закрытие группы (если нужно)
  // console.groupEnd();
}
```

**Критерии:**
- [ ] Добавлена группировка логов
- [ ] Группы сворачиваемы в DevTools
- [ ] Добавлен подсчёт логов в группе
- [ ] Улучшена читаемость логов

---

## 🔧 Технические требования

### Принципы улучшения:

1. **Автоматизация**
   - Автоматическое определение контекста из компонента Vue
   - Минимум ручной настройки (контекст опционален)
   - Fallback на переданный контекст или 'Unknown'

2. **Читаемость**
   - Улучшенное форматирование с цветами для DevTools
   - Группировка логов по контексту (сворачиваемые группы)
   - Подсчёт количества логов в группе

3. **Производительность**
   - Минимальное влияние на производительность
   - Оптимизация форматирования (кеширование стилей)
   - Группировка только при необходимости

4. **Интеграция**
   - Хорошая интеграция с Vue (`getCurrentInstance()`)
   - Поддержка DevTools (цвета, группировка)
   - Обратная совместимость с текущим API

### ⚠️ Важно: Сохранение контекста функционала

**Критически важно сохранить следующую функциональность:**

1. **Уровни логирования:**
   - `ERROR`, `WARN`, `INFO`, `DEBUG` должны работать как раньше
   - Управление через `LoggerConfig.isEnabled()` должно сохраниться
   - Формат сообщений должен остаться читаемым

2. **API композабла:**
   - Методы `error()`, `warn()`, `info()`, `debug()`, `log()` должны работать идентично
   - Параметры методов не должны измениться
   - Обратная совместимость с текущим использованием

3. **Форматирование:**
   - Timestamp должен остаться в формате ISO
   - Уровень логирования должен отображаться
   - Контекст должен отображаться

**Что НЕЛЬЗЯ изменять:**
- ❌ API методов (`error()`, `warn()`, `info()`, `debug()`, `log()`)
- ❌ Параметры методов (message, context, data)
- ❌ Интеграцию с `LoggerConfig`
- ❌ Уровни логирования (`ERROR`, `WARN`, `INFO`, `DEBUG`)

---

## ✅ Критерии приёмки

- [ ] Улучшено форматирование сообщений
- [ ] Добавлена поддержка цветов для DevTools
- [ ] Улучшено автоматическое определение контекста
- [ ] Добавлена группировка логов
- [ ] Улучшена интеграция с Vue компонентами
- [ ] Функциональность не нарушена
- [ ] Производительность не ухудшилась
- [ ] Код соответствует стандартам проекта

---

## 🧪 Тестирование

### Функциональное тестирование:

1. **Проверить автоматическое определение контекста:**
   - Использовать `useLogger()` без параметров в компоненте Vue
   - Проверить, что контекст определяется автоматически из имени компонента
   - Использовать `useLogger('CustomContext')` и проверить, что используется переданный контекст
   - Проверить fallback на 'Unknown' если контекст не определён

2. **Проверить форматирование сообщений:**
   - Вызвать `logger.error('Test error', error)`
   - Проверить, что сообщение форматируется с цветами в DevTools
   - Проверить, что timestamp отображается корректно
   - Проверить, что уровень логирования отображается с правильным цветом

3. **Проверить группировку логов:**
   - Вызвать несколько логов с одним контекстом
   - Проверить, что логи группируются по контексту в DevTools
   - Проверить, что группы сворачиваемы
   - Проверить подсчёт количества логов в группе

4. **Проверить работу в DevTools:**
   - Открыть DevTools и проверить логи
   - Проверить, что цвета применяются корректно
   - Проверить, что группировка работает
   - Проверить, что форматирование читаемо

5. **Проверить обратную совместимость:**
   - Проверить, что существующий код работает без изменений
   - Проверить, что `useLogger('ComponentName')` работает как раньше
   - Проверить, что методы `error()`, `warn()`, `info()`, `debug()` работают идентично

### Производительность:

1. Проверить влияние на производительность
2. Проверить использование памяти

---

## 📚 Связанные документы

- **Родительская задача:** `DOCS/TASKS/TASK-015-refactor-tasks-008-009-011-012-013-014.md`
- **Связанная задача:** `DOCS/TASKS/TASK-014-logging-service-dashboard-sector-1c.md`

---

## 📝 История правок

- **2025-12-06 21:30 (UTC+3, Брест):** Создана подзадача STEP-07 для улучшения интеграции логирования

---

**Автор:** Рефактор-менеджер  
**Статус:** Новая

