# TASK-003-01: PHP Handler для приёма исходящих вебхуков Bitrix24

**Дата создания:** 2025-12-05 22:19 (UTC+3, Брест)  
**Дата завершения:** 2025-12-06 10:35 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** [TASK-003](./TASK-003-webhook-handler-system.md)

---

## 📋 Описание

Создать PHP handler для приёма исходящих вебхуков от Bitrix24. Handler должен принимать все типы событий (смарт-процессы и задачи), парсить payload и передавать данные для дальнейшей обработки.

**Расположение:** `api/webhook-handler.php` (рядом с `index.php`)

---

## 🎯 Контекст

Это первая подзадача из системы обработки вебхуков. Handler является точкой входа для всех событий от Bitrix24. Он должен корректно принимать и парсить данные, чтобы последующие подзадачи могли их обработать.

**Связи:**
- Зависит от: нет (базовая подзадача)
- Зависит от неё: TASK-003-02, TASK-003-03

---

## 📁 Модули и компоненты

- `api/webhook-handler.php` — основной handler для приёма вебхуков

---

## 🔗 Зависимости

### От других задач:
- Нет (базовая подзадача)

### От модулей:
- Нет (самостоятельный handler)

---

## 📝 Ступенчатые подзадачи

1. Создать файл `api/webhook-handler.php`
2. Настроить обработку HTTP POST запросов
3. Получить raw body запроса от Bitrix24
4. Парсить JSON payload
5. Определить тип события из payload
6. Валидировать наличие обязательных полей
7. Передать данные для дальнейшей обработки (в следующей подзадаче)
8. Вернуть HTTP 200 ответ Bitrix24

---

## 🔌 API-методы Bitrix24

### Структура исходящего вебхука:

**Документация:**
- https://context7.com/bitrix24/rest/webhook/
- https://apidocs.bitrix24.ru/rest_help/general/webhooks/index.php

**Формат payload от Bitrix24:**

```json
{
  "event": "ONCRMDYNAMICITEMADD",
  "data": {
    "FIELDS": {
      "ID": "123",
      "TITLE": "Название элемента",
      ...
    },
    "ENTITY_TYPE_ID": "128"
  },
  "ts": "1234567890",
  "auth": {
    "domain": "example.bitrix24.ru",
    "client_endpoint": "https://example.bitrix24.ru/rest/"
  }
}
```

**Типы событий:**

**Смарт-процессы:**
- `ONCRMDYNAMICITEMADD`
- `ONCRMDYNAMICITEMUPDATE`
- `ONCRMDYNAMICITEMDELETE`

**Задачи:**
- `ONTASKADD`
- `ONTASKUPDATE`
- `ONTASKDELETE`
- `ONTASKCOMMENTADD`
- `ONTASKCOMMENTUPDATE`
- `ONTASKCOMMENTDELETE`

---

## ⚙️ Технические требования

### PHP:
- **Версия:** 8.3+
- **Метод запроса:** POST
- **Content-Type:** application/json
- **Обработка ошибок:** try-catch блоки
- **Логирование:** на данном этапе только базовое (в следующей подзадаче)

### Структура handler:
- Получение raw body
- Парсинг JSON
- Валидация структуры payload
- Определение типа события
- Передача данных для обработки (заглушка на данном этапе)

---

## ✅ Критерии приёмки

- [x] Файл `api/webhook-handler.php` создан
- [x] Handler принимает POST запросы
- [x] Payload корректно парсится (поддержка JSON и URL-encoded форматов)
- [x] Тип события определяется из payload
- [x] Валидация обязательных полей работает
- [x] Handler возвращает HTTP 200 для успешной обработки
- [x] Обработка ошибок реализована (try-catch)
- [x] Код соответствует стандартам PSR-12

**Дополнительно реализовано:**
- [x] Обработка `application/x-www-form-urlencoded` формата (стандартный для Bitrix24)
- [x] Поддержка парсинга через `$_POST` (PHP автоматически парсит URL-encoded данные)
- [x] Улучшенное логирование ошибок с деталями (raw body, content-type, headers)

---

## 💻 Примеры кода

### Пример структуры handler:

```php
<?php
/**
 * Handler для приёма исходящих вебхуков от Bitrix24
 * 
 * Расположение: api/webhook-handler.php
 * 
 * Документация:
 * - https://context7.com/bitrix24/rest/webhook/
 * - https://apidocs.bitrix24.ru/rest_help/general/webhooks/index.php
 */

header('Content-Type: application/json');
header('Access-Control-Allow-Origin: *');
header('Access-Control-Allow-Methods: POST, OPTIONS');
header('Access-Control-Allow-Headers: Content-Type, X-Requested-With');

// Обработка preflight запросов
if ($_SERVER['REQUEST_METHOD'] === 'OPTIONS') {
    http_response_code(200);
    exit;
}

// Проверка метода запроса
if ($_SERVER['REQUEST_METHOD'] !== 'POST') {
    http_response_code(405);
    echo json_encode(['error' => 'Method not allowed']);
    exit;
}

try {
    // Получение raw body
    $rawBody = file_get_contents('php://input');
    
    if (empty($rawBody)) {
        throw new Exception('Empty request body');
    }
    
    // Парсинг JSON
    $payload = json_decode($rawBody, true);
    
    if (json_last_error() !== JSON_ERROR_NONE) {
        throw new Exception('Invalid JSON: ' . json_last_error_msg());
    }
    
    // Валидация обязательных полей
    if (!isset($payload['event'])) {
        throw new Exception('Missing required field: event');
    }
    
    if (!isset($payload['data'])) {
        throw new Exception('Missing required field: data');
    }
    
    // Определение типа события
    $eventType = $payload['event'];
    $eventData = $payload['data'];
    
    // Определение категории события
    $category = null;
    if (strpos($eventType, 'ONCRMDYNAMIC') === 0) {
        $category = 'smart-processes';
    } elseif (strpos($eventType, 'ONTASK') === 0) {
        $category = 'tasks';
    }
    
    // TODO: Передача данных для обработки (в следующей подзадаче)
    // processWebhookEvent($eventType, $eventData, $category, $payload);
    
    // Успешный ответ Bitrix24
    http_response_code(200);
    echo json_encode([
        'success' => true,
        'event' => $eventType,
        'category' => $category
    ]);
    
} catch (Exception $e) {
    // Обработка ошибок
    http_response_code(400);
    echo json_encode([
        'error' => 'Webhook processing failed',
        'error_description' => $e->getMessage()
    ]);
    
    // TODO: Логирование ошибок (в следующей подзадаче)
    // logError($e, $payload ?? null);
}
```

---

## 🧪 Тестирование

### Шаги для тестирования:

1. **Настройка вебхука в Bitrix24:**
   - Перейти в настройки исходящих вебхуков
   - Указать URL: `https://your-domain.com/api/webhook-handler.php`
   - Выбрать события для отправки

2. **Тестирование приёма событий:**
   - Создать/изменить/удалить элемент смарт-процесса
   - Создать/изменить/удалить задачу
   - Проверить получение события в handler

3. **Проверка валидации:**
   - Отправить запрос без поля `event` — должна быть ошибка
   - Отправить запрос с невалидным JSON — должна быть ошибка
   - Отправить корректный запрос — должен быть HTTP 200

4. **Проверка через curl:**
   ```bash
   curl -X POST https://your-domain.com/api/webhook-handler.php \
     -H "Content-Type: application/json" \
     -d '{
       "event": "ONTASKADD",
       "data": {
         "TASK": {
           "ID": "123",
           "TITLE": "Тестовая задача"
         }
       }
     }'
   ```

---

## 📚 Дополнительные ресурсы

- [Bitrix24 REST API - Вебхуки](https://context7.com/bitrix24/rest/webhook/)
- [Bitrix24 REST API - События](https://context7.com/bitrix24/rest/events/)
- [Официальная документация Bitrix24](https://apidocs.bitrix24.ru/rest_help/general/webhooks/index.php)

---

## 📝 История правок

- **2025-12-05 22:19 (UTC+3, Брест):** Создана подзадача TASK-003-01
- **2025-12-06 10:35 (UTC+3, Брест):** Задача завершена
  - Реализована обработка URL-encoded данных (Bitrix24 отправляет form-data)
  - Добавлена поддержка парсинга через `$_POST`
  - Улучшено логирование ошибок с деталями
  - Настроен nginx для обработки `/api/webhook-handler.php`
  - Протестировано: успешно принимает события от Bitrix24

---

## 🔗 Связанные задачи

- **Родительская:** [TASK-003: Система обработки исходящих вебхуков](./TASK-003-webhook-handler-system.md)
- **Следующая:** [TASK-003-02: Логирование событий в JSON](./TASK-003-02-webhook-logging.md)

