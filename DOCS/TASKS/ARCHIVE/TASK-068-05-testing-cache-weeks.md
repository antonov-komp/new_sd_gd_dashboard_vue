# TASK-068-05: Тестирование и проверка работы кеша для режима "4 недели"

**Дата создания:** 2025-12-23 15:01 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Backend Developer (PHP) / QA Engineer  
**Родительская задача:** [TASK-068: Добавление кеширования для режима "4 недели"](./TASK-068-cache-for-weeks-mode.md)  
**Подзадача:** Этап 5 из TASK-068

---

## 📋 Описание

Протестировать работу кеширования для режима "4 недели", проверить все сценарии использования кеша, производительность и корректность работы.

**Цель:** Убедиться, что кеширование для режима "4 недели" работает корректно и обеспечивает ожидаемое улучшение производительности.

---

## 🎯 Контекст

### Текущее состояние:

- Все этапы TASK-068 выполнены:
  - TASK-068-01: Расширение GraphAdmissionClosureCache
  - TASK-068-02: Обновление генерации ключа кеша
  - TASK-068-03: Модификация GraphAdmissionClosureService
  - TASK-068-04: Добавление логирования производительности

### Требуется:

- Протестировать все сценарии работы кеша
- Проверить производительность (время загрузки с кешем и без)
- Проверить корректность данных из кеша
- Проверить работу параметра `forceRefresh`
- Проверить очистку устаревших кешей

---

## 🏗️ Модули и компоненты

### Тестируемые компоненты:

- `api/cache/GraphAdmissionClosureCache.php` — работа с кешем
- `api/graph-admission-closure/service/GraphAdmissionClosureService.php` — интеграция кеша
- `api/graph-admission-closure/cache/CacheStore.php` — обёртка над кешем

### Тестовые данные:

- Запросы с `periodMode: 'weeks'`
- Разные периоды (разные `weekStartUtc` и `weekEndUtc`)
- Разные параметры (`includeTickets`, `includeCarryoverTickets`, и т.д.)

---

## 📦 Зависимости

- **От задач:** 
  - TASK-068-01: Расширение GraphAdmissionClosureCache
  - TASK-068-02: Обновление генерации ключа кеша
  - TASK-068-03: Модификация GraphAdmissionClosureService
  - TASK-068-04: Добавление логирования производительности
- **От модулей:** 
  - Модуль: График приёма и закрытий сектора 1С
  - Bitrix24 REST API (для загрузки данных)

---

## 🎯 Ступенчатые подзадачи

### Подзадача 5.1: Функциональное тестирование базовой работы кеша

**Цель:** Протестировать базовую функциональность кеша (сохранение и получение данных).

**Тестовые сценарии:**

1. **Тест сохранения данных в кеш:**
   - Отправить запрос с `periodMode: 'weeks'`
   - Проверить логи: `[Cache] Cache miss for key: weeks_...`
   - Проверить логи: `[Cache] Cache saved for key: weeks_...`
   - Проверить файл кеша: `api/cache/graph-admission-closure/weeks/weeks_{hash}.json`
   - Проверить структуру данных в файле кеша:
     ```bash
     cat api/cache/graph-admission-closure/weeks/weeks_*.json | jq '.metadata'
     # Должно быть:
     # {
     #   "created_at": 1703328000,
     #   "expires_at": 1703328120,  # created_at + 120
     #   "ttl": 120
     # }
     ```
   - Проверить, что `data` содержит полный ответ API

2. **Тест получения данных из кеша:**
   - Отправить запрос с `periodMode: 'weeks'`
   - Дождаться сохранения в кеш
   - Отправить тот же запрос повторно
   - Проверить, что данные возвращаются из кеша
   - Проверить, что данные идентичны исходным

3. **Тест структуры данных кеша:**
   - Проверить наличие полей: `data`, `metadata`
   - Проверить поля `metadata`: `created_at`, `expires_at`, `ttl`
   - Проверить, что `ttl` равен 120 секундам

**Критерии приёмки:**
- [ ] Данные сохраняются в кеш
- [ ] Данные получаются из кеша
- [ ] Структура данных корректна
- [ ] TTL установлен на 120 секунд

---

### Подзадача 5.2: Тестирование истечения TTL

**Цель:** Протестировать истечение срока действия кеша (TTL: 120 секунд).

**Тестовые сценарии:**

1. **Тест истечения TTL:**
   - Создать кеш с TTL 5 секунд (для быстрого тестирования)
   - Отправить запрос и дождаться сохранения в кеш
   - Подождать 6 секунд
   - Отправить тот же запрос повторно
   - Проверить, что кеш истёк и данные загружаются заново
   - Проверить, что файл кеша удалён

2. **Тест автоматического удаления истёкших файлов:**
   - Создать несколько истёкших файлов кеша
   - Вызвать `clearExpired()`
   - Проверить, что истёкшие файлы удалены
   - Проверить логи: `[Cache] Cleared N expired cache entries`

**Критерии приёмки:**
- [ ] TTL работает корректно (120 секунд)
- [ ] Истёкшие кеши автоматически удаляются
- [ ] Метод `clearExpired()` работает корректно

---

### Подзадача 5.3: Тестирование параметра forceRefresh

**Цель:** Протестировать работу параметра `forceRefresh` для принудительного обновления кеша.

**Тестовые сценарии:**

1. **Тест forceRefresh:**
   - Отправить запрос с `periodMode: 'weeks'`
   - Дождаться сохранения в кеш
   - Отправить тот же запрос с `forceRefresh: true`
   - Проверить, что кеш пропускается
   - Проверить, что данные загружаются заново
   - Проверить, что новые данные сохраняются в кеш
   - Проверить логи: `[Cache] Force refresh requested, skipping cache check`

2. **Тест без forceRefresh:**
   - Отправить запрос с `periodMode: 'weeks'` (без `forceRefresh`)
   - Дождаться сохранения в кеш
   - Отправить тот же запрос повторно (без `forceRefresh`)
   - Проверить, что данные возвращаются из кеша

**Критерии приёмки:**
- [ ] Параметр `forceRefresh` работает корректно
- [ ] Кеш пропускается при `forceRefresh: true`
- [ ] Кеш используется при `forceRefresh: false` (или отсутствии параметра)

---

### Подзадача 5.4: Тестирование генерации ключей кеша

**Цель:** Протестировать генерацию ключей кеша для разных сценариев.

**Тестовые сценарии:**

1. **Тест одинаковых периодов:**
   - Отправить запрос с одинаковыми `weekStartUtc` и `weekEndUtc`
   - Проверить, что ключи кеша одинаковые
   - Проверить, что данные из кеша возвращаются

2. **Тест разных периодов:**
   - Отправить запрос с разными `weekStartUtc` и `weekEndUtc`
   - Проверить, что ключи кеша разные
   - Проверить, что кеши независимы

3. **Тест разных параметров:**
   - Отправить запрос с `includeCarryoverTickets: false`
   - Отправить запрос с `includeCarryoverTickets: true`
   - Проверить, что ключи кеша разные
   - Проверить, что кеши независимы

4. **Тест префиксов ключей:**
   - Проверить, что ключи режима "weeks" начинаются с `weeks_`
   - Проверить, что ключи режима "months" начинаются с `months_`

**Критерии приёмки:**
- [ ] Одинаковые периоды генерируют одинаковые ключи
- [ ] Разные периоды генерируют разные ключи
- [ ] Разные параметры генерируют разные ключи
- [ ] Префиксы ключей корректны

---

### Подзадача 5.5: Тестирование производительности

**Цель:** Протестировать производительность режима "4 недели" с кешем и без кеша.

**Тестовые сценарии:**

1. **Тест времени загрузки без кеша:**
   - Отправить запрос с `periodMode: 'weeks'` и `forceRefresh: true`
   - Измерить время выполнения через `time` команду:
     ```bash
     time curl -X POST http://localhost/api/graph-1c-admission-closure.php \
       -H "Content-Type: application/json" \
       -d '{"periodMode": "weeks", "product": "1C", "forceRefresh": true}'
     ```
   - Ожидаемое время: 2-5 секунд (real time)
   - Проверить логи: `[WEEKS-PERFORMANCE] Total execution time: XX.XX seconds`
   - **Сравнение:** Время в логах должно быть близко к `real` времени из `time`

2. **Тест времени загрузки с кешем (cache hit):**
   - Отправить запрос с `periodMode: 'weeks'` (первый раз — cache miss)
   - Дождаться сохранения в кеш (проверить логи: `[Cache] Cache saved`)
   - Отправить тот же запрос повторно (cache hit)
   - Измерить время выполнения:
     ```bash
     time curl -X POST http://localhost/api/graph-1c-admission-closure.php \
       -H "Content-Type: application/json" \
       -d '{"periodMode": "weeks", "product": "1C"}'
     ```
   - Ожидаемое время: 0.01-0.05 секунд (real time)
   - Проверить логи: `[WEEKS-PERFORMANCE] Total execution time (from cache): X.XXX seconds`
   - **Важно:** Время должно быть значительно меньше, чем без кеша (в 10-100 раз)

3. **Сравнение производительности:**
   - Сравнить время загрузки с кешем и без кеша
   - Убедиться, что кеш значительно ускоряет ответ (в 10-100 раз)
   - **Таблица сравнения:**
     ```
     Режим "weeks":
     - Без кеша (forceRefresh: true): 2-5 секунд
     - С кешем (cache hit): 0.01-0.05 секунд
     - Ускорение: ~40-500 раз
     
     Режим "months" (для сравнения):
     - Без кеша: 5-15 секунд
     - С кешем: 0.01-0.05 секунд
     - Ускорение: ~100-1500 раз
     ```
   - **Проверка через логи:**
     ```bash
     # Извлечь все метрики производительности
     grep "WEEKS-PERFORMANCE" logs/app/graph-admission-closure-*.log | \
       awk '{print $NF}' | \
       sort -n
     # Должны быть значения: ~0.01-0.05 (cache hit) и ~2-5 (cache miss)
     ```

**Критерии приёмки:**
- [ ] Время загрузки без кеша: 2-5 секунд
- [ ] Время загрузки с кешем: 0.01-0.05 секунд
- [ ] Ускорение: минимум в 10 раз
- [ ] Логирование производительности работает

---

### Подзадача 5.6: Интеграционное тестирование

**Цель:** Протестировать работу кеша в реальных условиях с различными сценариями.

**Тестовые сценарии:**

1. **Тест переключения между режимами:**
   - Отправить запрос с `periodMode: 'weeks'`
   - Отправить запрос с `periodMode: 'months'`
   - Проверить, что кеши независимы
   - Проверить, что оба режима работают корректно

2. **Тест одновременных запросов:**
   - Отправить несколько запросов одновременно с `periodMode: 'weeks'`
   - Проверить, что кеш работает корректно
   - Проверить, что нет конфликтов при записи в кеш

3. **Тест очистки устаревших кешей:**
   - Создать несколько истёкших кешей
   - Отправить несколько запросов (чтобы сработала периодическая очистка)
   - Проверить, что устаревшие кеши очищаются
   - Проверить логи: `[Cache] Cleared N expired cache entries`

**Критерии приёмки:**
- [ ] Переключение между режимами работает корректно
- [ ] Одновременные запросы обрабатываются корректно
- [ ] Очистка устаревших кешей работает

---

## 🔧 Технические требования

### Инструменты для тестирования:

- **API клиент:** Postman, curl, или аналогичный
- **Проверка файлов кеша:** файловая система
- **Проверка логов:** `logs/app/graph-admission-closure-YYYY-MM-DD.log`
- **Проверка производительности:** `time` команда, логи `[WEEKS-PERFORMANCE]`

### Команды для проверки:

```bash
# Проверка структуры директорий кеша
ls -la api/cache/graph-admission-closure/
# Должны быть: months/ и weeks/

# Проверка файлов кеша
ls -la api/cache/graph-admission-closure/weeks/
# Должны быть файлы: weeks_*.json

# Просмотр содержимого файла кеша
cat api/cache/graph-admission-closure/weeks/weeks_*.json | jq '.'
# Проверить структуру: data, metadata (created_at, expires_at, ttl: 120)

# Просмотр логов в реальном времени
tail -f logs/app/graph-admission-closure-$(date +%Y-%m-%d).log

# Фильтрация логов по кешу
grep -i "cache" logs/app/graph-admission-closure-$(date +%Y-%m-%d).log

# Фильтрация логов по производительности
grep "WEEKS-PERFORMANCE" logs/app/graph-admission-closure-$(date +%Y-%m-%d).log
```

### Тестовые данные:

- **Периоды:** Разные недели (текущая + предыдущие)
- **Параметры:** Разные комбинации `includeTickets`, `includeCarryoverTickets`, и т.д.

---

## ✅ Критерии приёмки

### Общие критерии:
- [ ] Все функциональные тесты проходят успешно
- [ ] TTL работает корректно (120 секунд)
- [ ] Параметр `forceRefresh` работает корректно
- [ ] Генерация ключей кеша работает корректно
- [ ] Производительность соответствует ожиданиям:
  - Без кеша: 2-5 секунд
  - С кешем: 0.01-0.05 секунд
- [ ] Логирование производительности работает
- [ ] Интеграционное тестирование пройдено успешно
- [ ] Обработка ошибок работает корректно
- [ ] Документация обновлена (если нужно)

---

## 🧪 Тестовые сценарии

### Сценарий 1: Базовое тестирование кеша

```bash
# 1. Отправить запрос (cache miss)
curl -X POST http://localhost/api/graph-1c-admission-closure.php \
  -H "Content-Type: application/json" \
  -d '{
    "periodMode": "weeks",
    "product": "1C"
  }'

# Проверить логи:
# tail -f logs/app/graph-admission-closure-$(date +%Y-%m-%d).log
# Ожидаемые логи:
# [Cache] Cache miss for key: weeks_abc123...
# [WEEKS-PERFORMANCE] Total execution time: 3.45 seconds
# [Cache] Cache saved for key: weeks_abc123...

# 2. Отправить тот же запрос повторно (cache hit)
curl -X POST http://localhost/api/graph-1c-admission-closure.php \
  -H "Content-Type: application/json" \
  -d '{
    "periodMode": "weeks",
    "product": "1C"
  }'

# Проверить логи:
# Ожидаемые логи:
# [Cache] Cache hit for key: weeks_abc123...
# [WEEKS-PERFORMANCE] Total execution time (from cache): 0.023 seconds
```

**Ожидаемый результат:**
- Первый запрос: cache miss, время ~2-5 секунд
- Второй запрос: cache hit, время ~0.01-0.05 секунд
- **Проверка файла кеша:**
  ```bash
  ls -la api/cache/graph-admission-closure/weeks/
  # Должен быть файл: weeks_abc123....json
  cat api/cache/graph-admission-closure/weeks/weeks_abc123....json
  # Проверить структуру: data, metadata (created_at, expires_at, ttl: 120)
  ```

---

### Сценарий 2: Тестирование forceRefresh

```bash
# 1. Отправить запрос (cache miss)
curl -X POST http://localhost/api/graph-1c-admission-closure.php \
  -H "Content-Type: application/json" \
  -d '{
    "periodMode": "weeks",
    "product": "1C"
  }'

# Проверить логи:
# [Cache] Cache miss for key: weeks_abc123...
# [WEEKS-PERFORMANCE] Total execution time: 3.45 seconds
# [Cache] Cache saved for key: weeks_abc123...

# 2. Отправить запрос с forceRefresh (cache bypass)
curl -X POST http://localhost/api/graph-1c-admission-closure.php \
  -H "Content-Type: application/json" \
  -d '{
    "periodMode": "weeks",
    "product": "1C",
    "forceRefresh": true
  }'

# Проверить логи:
# [Cache] Force refresh requested, skipping cache check
# [WEEKS-PERFORMANCE] Total execution time: 3.52 seconds
# [Cache] Cache saved for key: weeks_abc123...
```

**Ожидаемый результат:**
- Первый запрос: cache miss, время ~2-5 секунд
- Второй запрос: cache bypass (пропуск кеша), время ~2-5 секунд
- **Важно:** Кеш обновляется даже при `forceRefresh: true`

---

### Сценарий 3: Тестирование разных периодов

```bash
# 1. Запрос для недели 1
curl -X POST http://localhost/api/graph-1c-admission-closure.php \
  -H "Content-Type: application/json" \
  -d '{
    "periodMode": "weeks",
    "weekStartUtc": "2025-12-16T00:00:00Z",
    "weekEndUtc": "2025-12-22T23:59:59Z",
    "product": "1C"
  }'

# Проверить ключ кеша в логах:
# [Cache] Cache miss for key: weeks_def456...
# Проверить файл кеша:
# ls -la api/cache/graph-admission-closure/weeks/weeks_def456....json

# 2. Запрос для недели 2
curl -X POST http://localhost/api/graph-1c-admission-closure.php \
  -H "Content-Type: application/json" \
  -d '{
    "periodMode": "weeks",
    "weekStartUtc": "2025-12-23T00:00:00Z",
    "weekEndUtc": "2025-12-29T23:59:59Z",
    "product": "1C"
  }'

# Проверить ключ кеша в логах:
# [Cache] Cache miss for key: weeks_ghi789...
# Проверить файл кеша:
# ls -la api/cache/graph-admission-closure/weeks/weeks_ghi789....json
```

**Ожидаемый результат:**
- Разные ключи кеша для разных периодов (`weeks_def456...` vs `weeks_ghi789...`)
- Кеши независимы (разные файлы)
- **Проверка:**
  ```bash
  # Должно быть 2 разных файла кеша
  ls -la api/cache/graph-admission-closure/weeks/
  # weeks_def456....json
  # weeks_ghi789....json
  ```

---

## 📝 Чек-лист тестирования

### Функциональное тестирование:
- [ ] Сохранение данных в кеш
- [ ] Получение данных из кеша
- [ ] Структура данных кеша корректна
- [ ] TTL работает (120 секунд)
- [ ] Автоматическое удаление истёкших кешей
- [ ] Параметр `forceRefresh` работает
- [ ] Генерация ключей кеша корректна

### Производительность:
- [ ] Время загрузки без кеша: 2-5 секунд
- [ ] Время загрузки с кешем: 0.01-0.05 секунд
- [ ] Ускорение: минимум в 10 раз

### Интеграционное тестирование:
- [ ] Переключение между режимами работает
- [ ] Одновременные запросы обрабатываются корректно
- [ ] Очистка устаревших кешей работает

### Логирование:
- [ ] Логи cache hit/miss записываются
- [ ] Логирование производительности работает
- [ ] Формат логов корректный

---

## ⚠️ Типичные ошибки и их решения

### Ошибка 1: "Failed to create cache directory"

**Симптомы:**
```
[Cache] Failed to create cache directory: /path/to/weeks
[Cache] Failed to save cache for key: weeks_...
```

**Причины:**
- Директория не существует и не может быть создана
- Нет прав на создание директории
- Родительская директория не существует

**Решение:**
```bash
# Создать директорию вручную
mkdir -p api/cache/graph-admission-closure/weeks

# Установить права
chmod 755 api/cache/graph-admission-closure/weeks

# Проверить владельца (должен быть веб-сервер)
ls -la api/cache/graph-admission-closure/
# Если нужно, изменить владельца:
chown www-data:www-data api/cache/graph-admission-closure/weeks
```

---

### Ошибка 2: "Cache hit, but data is null"

**Симптомы:**
```
[Cache] Cache hit for key: weeks_...
[WEEKS-PERFORMANCE] Total execution time (from cache): 0.023 seconds
# Но ответ пустой или некорректный
```

**Причины:**
- Файл кеша повреждён
- Структура данных в кеше некорректна
- Кеш истёк, но файл не удалён

**Решение:**
```bash
# Проверить содержимое файла кеша
cat api/cache/graph-admission-closure/weeks/weeks_*.json | jq '.'

# Удалить повреждённые файлы
rm api/cache/graph-admission-closure/weeks/weeks_*.json

# Повторить запрос (создаст новый кеш)
```

---

### Ошибка 3: "Different keys for same period"

**Симптомы:**
- Одинаковые запросы генерируют разные ключи
- Кеш не попадает при повторных запросах

**Причины:**
- Разный формат дат в ключе
- Разный порядок параметров в JSON
- Разные значения по умолчанию

**Решение:**
```php
// Проверить формат дат
$weekStart->format('Y-m-d\TH:i:s\Z'); // Правильно
$weekStart->format('Y-m-d H:i:s');    // Неправильно

// Проверить нормализацию параметров
$normalized = [
    'product' => $params['product'] ?? '1C', // Всегда одинаковое значение по умолчанию
    // ...
];
ksort($normalized); // Сортировка для консистентности
```

---

## ⚠️ Потенциальные проблемы при тестировании

### Проблема 1: Кеш не создаётся

**Симптомы:**
- Логи показывают `[Cache] Cache miss`, но нет `[Cache] Cache saved`
- Файлы кеша не появляются в директории `weeks/`

**Диагностика:**
```bash
# Проверить права доступа
ls -la api/cache/graph-admission-closure/weeks/
# Должны быть права на запись (drwxr-xr-x или drwxrwxrwx)

# Проверить логи ошибок
tail -f logs/app/graph-admission-closure-$(date +%Y-%m-%d).log | grep -i "cache\|error"

# Проверить, что директория существует
test -d api/cache/graph-admission-closure/weeks/ && echo "OK" || echo "NOT EXISTS"
```

**Решение:**
- Создать директорию вручную: `mkdir -p api/cache/graph-admission-closure/weeks`
- Установить права: `chmod 755 api/cache/graph-admission-closure/weeks`
- Проверить, что веб-сервер имеет права на запись

### Проблема 2: Разные ключи для одинаковых периодов

**Симптомы:**
- Одинаковые запросы генерируют разные ключи кеша
- Кеш не попадает при повторных запросах

**Диагностика:**
```php
// Проверить формат дат
$key1 = $cacheStore->generateKey([
    'periodMode' => 'weeks',
    'weekStartUtc' => '2025-12-16T00:00:00Z',
    'weekEndUtc' => '2025-12-22T23:59:59Z'
]);

$key2 = $cacheStore->generateKey([
    'periodMode' => 'weeks',
    'weekStartUtc' => '2025-12-16T00:00:00Z', // Тот же период
    'weekEndUtc' => '2025-12-22T23:59:59Z'
]);

// Должны быть одинаковыми
assert($key1 === $key2, "Keys should be identical for same period");
```

**Решение:**
- Убедиться, что формат дат одинаковый (`Y-m-d\TH:i:s\Z`)
- Проверить, что параметры нормализуются одинаково
- Использовать `ksort()` перед `json_encode()` для консистентности

### Проблема 3: Кеш истёк слишком быстро

**Симптомы:**
- Кеш создаётся, но сразу истекает
- Повторные запросы не попадают в кеш

**Диагностика:**
```bash
# Проверить TTL в файле кеша
cat api/cache/graph-admission-closure/weeks/weeks_*.json | jq '.metadata.ttl'
# Должно быть: 120

# Проверить expires_at
cat api/cache/graph-admission-closure/weeks/weeks_*.json | jq '.metadata.expires_at'
# Должно быть: created_at + 120 секунд
```

**Решение:**
- Убедиться, что TTL установлен на 120 секунд
- Проверить, что `time()` возвращает корректное время (синхронизация времени сервера)

---

## 🔗 Связанные документы

- [TASK-068: Добавление кеширования для режима "4 недели"](./TASK-068-cache-for-weeks-mode.md)
- [TASK-068-01: Расширение GraphAdmissionClosureCache для поддержки режима "weeks"](./TASK-068-01-extend-cache-for-weeks.md)
- [TASK-068-02: Обновление генерации ключа кеша для учёта weekStartUtc/weekEndUtc](./TASK-068-02-update-cache-key-generation.md)
- [TASK-068-03: Модификация GraphAdmissionClosureService для добавления кеширования](./TASK-068-03-add-cache-to-service.md)
- [TASK-068-04: Добавление логирования производительности для режима "4 недели"](./TASK-068-04-add-performance-logging.md)
- [TASK-059-05: Реализация файлового кеша для режима 3 месяца](./TASK-059-05-file-cache-implementation.md)

---

## 📊 История правок

- **2025-12-23 15:01 (UTC+3, Брест):** Создана подзадача для тестирования кеша

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

