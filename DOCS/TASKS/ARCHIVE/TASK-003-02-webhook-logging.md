# TASK-003-02: Логирование событий вебхуков в JSON файлы

**Дата создания:** 2025-12-05 22:19 (UTC+3, Брест)  
**Дата завершения:** 2025-12-06 10:35 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** [TASK-003](./TASK-003-webhook-handler-system.md)

---

## 📋 Описание

Реализовать систему логирования событий вебхуков в JSON файлы. Логи должны содержать полный payload от Bitrix24 и метаданные (timestamp, IP, тип события). Логи разделяются по категориям: Задачи и Смарт-процессы.

**Структура папок:**
- `logs/webhooks/tasks/` — логи событий задач
- `logs/webhooks/smart-processes/` — логи событий смарт-процессов
- `logs/webhooks/errors/` — логи ошибок обработки

**Формат файлов:** `YYYY-MM-DD-HH.json` (один файл на час)

---

## 🎯 Контекст

Эта подзадача расширяет функциональность handler из TASK-003-01. После приёма и парсинга вебхука необходимо сохранить все данные в структурированном виде для последующего анализа и просмотра через Vue.js интерфейс.

**Связи:**
- Зависит от: TASK-003-01 (handler должен быть создан)
- Зависит от неё: TASK-003-04 (Vue.js компонент для просмотра логов)

---

## 📁 Модули и компоненты

- `api/webhook-handler.php` — обновление handler для логирования
- `logs/webhooks/tasks/` — папка для логов событий задач
- `logs/webhooks/smart-processes/` — папка для логов событий смарт-процессов
- `logs/webhooks/errors/` — папка для логов ошибок

---

## 🔗 Зависимости

### От других задач:
- **TASK-003-01** — PHP handler должен быть создан и работать

### От модулей:
- Нет

---

## 📝 Ступенчатые подзадачи

1. Создать структуру папок для логов:
   - `logs/webhooks/tasks/`
   - `logs/webhooks/smart-processes/`
   - `logs/webhooks/errors/`
2. Реализовать функцию определения пути к файлу лога:
   - Формат: `YYYY-MM-DD-HH.json`
   - Выбор папки по категории события
3. Реализовать функцию формирования записи лога:
   - Полный payload от Bitrix24
   - Метаданные (timestamp обработки, IP отправителя, тип события)
   - Детали события (ID элемента, ID задачи, ID комментария и т.д.)
4. Реализовать функцию записи в JSON файл:
   - Чтение существующего файла (если есть)
   - Добавление новой записи в массив
   - Сохранение обновлённого массива в файл
5. Интегрировать логирование в handler:
   - Вызов функции логирования после успешного парсинга
   - Обработка ошибок записи в лог
6. Реализовать логирование ошибок:
   - Отдельная функция для ошибок
   - Сохранение в `logs/webhooks/errors/`
   - Формат с информацией об ошибке и payload (если был)

---

## ⚙️ Технические требования

### Структура JSON лога:

```json
{
  "timestamp": "2025-12-05T22:19:00+03:00",
  "ip": "192.168.1.1",
  "event": "ONTASKADD",
  "category": "tasks",
  "payload": {
    "event": "ONTASKADD",
    "data": {
      "TASK": {
        "ID": "123",
        "TITLE": "Название задачи",
        ...
      }
    },
    "ts": "1234567890",
    "auth": {
      "domain": "example.bitrix24.ru"
    }
  },
  "details": {
    "task_id": "123",
    "task_title": "Название задачи",
    "created_by": "456"
  }
}
```

### Детали для разных типов событий:

**Смарт-процессы:**
- `entity_type_id` — ID типа смарт-процесса
- `entity_id` — ID элемента
- `title` — название элемента
- `changed_fields` — изменённые поля (для UPDATE)

**Задачи:**
- `task_id` — ID задачи
- `task_title` — название задачи
- `created_by` — ID создателя
- `responsible_id` — ID ответственного
- `comment_id` — ID комментария (для событий комментариев)
- `comment_text` — текст комментария (для событий комментариев)

### Формат имени файла:
- `YYYY-MM-DD-HH.json` (например: `2025-12-05-22.json`)
- Один файл на час для удобства поиска и анализа

---

## ✅ Критерии приёмки

- [x] Структура папок для логов создана
- [x] Функция определения пути к файлу лога работает
- [x] Функция формирования записи лога работает
- [x] Полный payload сохраняется в лог
- [x] Метаданные (timestamp, IP, тип события) сохраняются
- [x] Детали события извлекаются и сохраняются
- [x] Запись в JSON файл работает корректно
- [x] Логи разделяются по категориям (Задачи, Смарт-процессы)
- [x] Ошибки логируются в отдельный файл
- [x] Обработка ошибок записи в лог реализована
- [x] Код соответствует стандартам PSR-12

**Дополнительно реализовано:**
- [x] Исправлены права доступа на папки логов (www-data:www-data)
- [x] Улучшено логирование ошибок с деталями (raw body preview, content-type, headers)
- [x] Протестировано: успешно логируется 22 события

---

## 💻 Примеры кода

### Пример функции логирования:

```php
<?php
/**
 * Логирование события вебхука в JSON файл
 * 
 * @param string $eventType Тип события (например, ONTASKADD)
 * @param array $eventData Данные события из payload
 * @param string $category Категория (tasks или smart-processes)
 * @param array $fullPayload Полный payload от Bitrix24
 * @param string $clientIp IP отправителя
 * @return bool Успешность записи
 */
function logWebhookEvent($eventType, $eventData, $category, $fullPayload, $clientIp) {
    try {
        // Определение пути к файлу лога
        $logDir = __DIR__ . '/../logs/webhooks/' . $category . '/';
        $logFile = $logDir . date('Y-m-d-H') . '.json';
        
        // Создание папки, если не существует
        if (!is_dir($logDir)) {
            mkdir($logDir, 0755, true);
        }
        
        // Формирование записи лога
        $logEntry = [
            'timestamp' => date('c'), // ISO 8601 формат
            'ip' => $clientIp,
            'event' => $eventType,
            'category' => $category,
            'payload' => $fullPayload,
            'details' => extractEventDetails($eventType, $eventData)
        ];
        
        // Чтение существующего файла
        $logs = [];
        if (file_exists($logFile)) {
            $existingContent = file_get_contents($logFile);
            $logs = json_decode($existingContent, true) ?? [];
        }
        
        // Добавление новой записи
        $logs[] = $logEntry;
        
        // Сохранение в файл
        file_put_contents(
            $logFile,
            json_encode($logs, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE),
            LOCK_EX
        );
        
        return true;
    } catch (Exception $e) {
        // Логирование ошибки
        logWebhookError($e, $fullPayload ?? null);
        return false;
    }
}

/**
 * Извлечение деталей события
 * 
 * @param string $eventType Тип события
 * @param array $eventData Данные события
 * @return array Детали события
 */
function extractEventDetails($eventType, $eventData) {
    $details = [];
    
    // Обработка событий задач
    if (strpos($eventType, 'ONTASK') === 0) {
        if (isset($eventData['TASK'])) {
            $task = $eventData['TASK'];
            $details['task_id'] = $task['ID'] ?? null;
            $details['task_title'] = $task['TITLE'] ?? null;
            $details['created_by'] = $task['CREATED_BY'] ?? null;
            $details['responsible_id'] = $task['RESPONSIBLE_ID'] ?? null;
        }
        
        // Для событий комментариев
        if (strpos($eventType, 'ONTASKCOMMENT') === 0) {
            if (isset($eventData['COMMENT'])) {
                $comment = $eventData['COMMENT'];
                $details['comment_id'] = $comment['ID'] ?? null;
                $details['comment_text'] = $comment['POST_MESSAGE'] ?? null;
                $details['task_id'] = $comment['TASK_ID'] ?? null;
            }
        }
    }
    
    // Обработка событий смарт-процессов
    if (strpos($eventType, 'ONCRMDYNAMIC') === 0) {
        if (isset($eventData['FIELDS'])) {
            $fields = $eventData['FIELDS'];
            $details['entity_id'] = $fields['ID'] ?? null;
            $details['title'] = $fields['TITLE'] ?? null;
        }
        
        if (isset($eventData['ENTITY_TYPE_ID'])) {
            $details['entity_type_id'] = $eventData['ENTITY_TYPE_ID'];
        }
        
        // Для UPDATE события - изменённые поля
        if ($eventType === 'ONCRMDYNAMICITEMUPDATE' && isset($eventData['PREVIOUS_FIELDS'])) {
            $details['changed_fields'] = array_keys($eventData['PREVIOUS_FIELDS']);
        }
    }
    
    return $details;
}

/**
 * Логирование ошибки обработки вебхука
 * 
 * @param Exception $error Объект ошибки
 * @param array|null $payload Payload вебхука (если был)
 */
function logWebhookError($error, $payload = null) {
    try {
        $errorDir = __DIR__ . '/../logs/webhooks/errors/';
        $errorFile = $errorDir . date('Y-m-d-H') . '.json';
        
        if (!is_dir($errorDir)) {
            mkdir($errorDir, 0755, true);
        }
        
        $errorEntry = [
            'timestamp' => date('c'),
            'error' => $error->getMessage(),
            'file' => $error->getFile(),
            'line' => $error->getLine(),
            'trace' => $error->getTraceAsString(),
            'payload' => $payload
        ];
        
        $errors = [];
        if (file_exists($errorFile)) {
            $existingContent = file_get_contents($errorFile);
            $errors = json_decode($existingContent, true) ?? [];
        }
        
        $errors[] = $errorEntry;
        
        file_put_contents(
            $errorFile,
            json_encode($errors, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE),
            LOCK_EX
        );
    } catch (Exception $e) {
        // Если не удалось записать ошибку в лог - выводим в error_log
        error_log('Failed to log webhook error: ' . $e->getMessage());
    }
}
```

### Интеграция в handler:

```php
// В api/webhook-handler.php после успешного парсинга:

// Получение IP отправителя
$clientIp = $_SERVER['REMOTE_ADDR'] ?? 'unknown';

// Логирование события
$logSuccess = logWebhookEvent(
    $eventType,
    $eventData,
    $category,
    $payload,
    $clientIp
);

if (!$logSuccess) {
    // Ошибка логирования не должна прерывать обработку
    // Но можно добавить уведомление
    error_log('Failed to log webhook event: ' . $eventType);
}
```

---

## 🧪 Тестирование

### Шаги для тестирования:

1. **Проверка создания папок:**
   - Отправить тестовый вебхук
   - Проверить создание папок `logs/webhooks/tasks/` и `logs/webhooks/smart-processes/`

2. **Проверка записи логов:**
   - Отправить событие задачи (ONTASKADD)
   - Проверить создание файла `YYYY-MM-DD-HH.json` в `logs/webhooks/tasks/`
   - Проверить содержимое файла (полный payload, метаданные, детали)

3. **Проверка деталей события:**
   - Отправить событие смарт-процесса (ONCRMDYNAMICITEMADD)
   - Проверить извлечение `entity_id`, `entity_type_id`, `title`
   - Отправить событие комментария (ONTASKCOMMENTADD)
   - Проверить извлечение `comment_id`, `task_id`, `comment_text`

4. **Проверка логирования ошибок:**
   - Симулировать ошибку (например, невалидный JSON)
   - Проверить создание файла в `logs/webhooks/errors/`
   - Проверить содержимое файла ошибки

5. **Проверка формата файлов:**
   - Проверить, что файлы создаются с форматом `YYYY-MM-DD-HH.json`
   - Проверить, что новые записи добавляются в существующий файл
   - Проверить корректность JSON структуры

---

## 📚 Дополнительные ресурсы

- [Bitrix24 REST API - Вебхуки](https://context7.com/bitrix24/rest/webhook/)
- [Bitrix24 REST API - События](https://context7.com/bitrix24/rest/events/)
- [PHP file_put_contents](https://www.php.net/manual/en/function.file-put-contents.php)
- [JSON в PHP](https://www.php.net/manual/en/book.json.php)

---

## 📝 История правок

- **2025-12-05 22:19 (UTC+3, Брест):** Создана подзадача TASK-003-02
- **2025-12-06 10:35 (UTC+3, Брест):** Задача завершена
  - Исправлены права доступа на папки логов
  - Улучшено логирование ошибок с деталями
  - Протестировано: успешно логируется 22 события (19 задач, 3 смарт-процесса)

---

## 🔗 Связанные задачи

- **Родительская:** [TASK-003: Система обработки исходящих вебхуков](./TASK-003-webhook-handler-system.md)
- **Предыдущая:** [TASK-003-01: PHP handler для приёма вебхуков](./TASK-003-01-webhook-handler-php.md)
- **Следующая:** [TASK-003-03: Валидация и обработка дублей](./TASK-003-03-webhook-validation.md)

