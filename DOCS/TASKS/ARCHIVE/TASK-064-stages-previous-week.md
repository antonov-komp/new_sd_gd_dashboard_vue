# TASK-064: Исправить карточку «Закрытия по стадиям» для предыдущей недели

**Дата создания:** 2025-12-23 13:03 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связанная задача:** TASK-061, TASK-062  

---

## 📋 Описание

В режиме «4 недели» модуля «График приёма и закрытий сектора 1С» блок «Закрытия по стадиям» для **предыдущей недели** показывает те же данные, что и для текущей недели. На скрине видно, что для предыдущей недели выводится одна цифра без разбивки по фактическим стадиям недели N-1.

---

## 🎯 Цель

Корректно отображать разбивку «Закрытия по стадиям» для предыдущей недели (N-1) в недельном режиме: данные должны браться из соответствующей недели, а не из текущей.

---

## 🔎 Наблюдение / как воспроизвести

1. Открыть график в режиме «4 недели».
2. Сверху отобразится блок «Текущая неделя», ниже — «Предыдущая неделя».
3. В карточке «Закрытия по стадиям» внутри блока предыдущей недели всегда одна цифра (фактически данные текущей недели), разбивки по стадиям недели N-1 нет.

---

## 🧭 Где править (frontend)

- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`
  - Блок summary-карточек предыдущей недели использует `data.stages`, которые формируются только для текущей недели.

---

## 📐 Требования к решению

1) Источник данных
- Для недельного режима нужен массив стадий **по неделям**, выровненный по `meta.weeks` / `data.weeksData`.
- Предлагаемый контракт (обсудить с backend, при отсутствии — договориться и внедрить заглушку):  
  - `data.stagesByWeek: Array<Array<{ stageId, stageName, count }>>`, индекс соответствует неделе в `meta.weeks` / `series` (последний — текущая, предпоследний — предыдущая).  
  - Fallback: если `stagesByWeek` нет, поведение остаётся как сейчас (используем `data.stages`), но UI должен явно показывать «Нет данных для недели N-1».

2) Отображение
- В блоке предыдущей недели брать стадии из `stagesByWeek[prevIndex]` (prevIndex = lastIndex - 1).  
- В блоке текущей недели — `stagesByWeek[lastIndex]` (при наличии) или старый `data.stages`.
- Если для недели нет данных по стадиям, показывать текст «Нет данных» (как сейчас), но привязанный к конкретной неделе.

3) Совместимость
- Не ломать существующий контракт (`data.stages` остаётся для текущей недели и для месячного режима).
- Компонент должен gracefully деградировать при отсутствии `stagesByWeek`.

4) Тесты / проверки
- Ручная проверка в UI:  
  - режим «4 недели», сравнить значения «Закрытия по стадиям» для текущей и предыдущей недели — должны различаться и соответствовать выборке недель.  
  - переключение типов графиков (Линейный/Столбчатый/Круговая) не должно сбивать данные карточек.  
  - при отсутствии данных за неделю N-1 карточка показывает «Нет данных» без краша.
- Логи консоли: отсутствие ошибок/варнингов после внедрения.

---

## 🚧 Зависимости / координация

- Backend (`api/graph-1c-admission-closure.php`) сейчас отдаёт `stages` только для текущей недели (агрегация по `weekStart`/`weekEnd`). Требуется договориться / синхронизировать контракт `stagesByWeek`. Если backend будет обновлён другим исполнителем — зафиксировать контракт в коде и этом TASK.

---

## ✅ Критерии приёмки

- [ ] В режиме «4 недели» карточка «Закрытия по стадиям» в блоке предыдущей недели выводит данные недели N-1 (а не текущей).  
- [ ] Для текущей недели используется актуальная разбивка (данные недели N).  
- [ ] При отсутствии данных недели N-1 отображается «Нет данных», UI не падает.  
- [ ] Контракт с backend задокументирован в коде (комментарий) и поддержан fallback на старое поле `data.stages`.  
- [ ] Консоль без ошибок/варнингов.

---

## 🧪 Тестирование

1. Открыть график, режим «4 недели», тип «Линейный»: проверить карточки «Закрытия по стадиям» в блоках текущей и предыдущей недели — значения должны различаться и соответствовать данным недели N и N-1.  
2. Переключить тип на «Столбчатый» и «Круговая» — убедиться, что карточки стадий не перерисовываются неправильными данными.  
3. Смоделировать отсутствие `stagesByWeek` (например, через мок) — карточка предыдущей недели показывает «Нет данных», без ошибок в консоли.

---

## 📎 Контекст для разработчика

- UI уже поддерживает вычисленные `currentWeekData` / `previousWeekData` / `prePreviousWeekData`. Ошибка в том, что блок предыдущей недели берёт `data.stages` (агрегация только для текущей недели).  
- Нужно привязать стадии к конкретному индексу недели так же, как это сделано для `series` / `weeksData`.



