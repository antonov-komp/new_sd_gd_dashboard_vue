# TASK-023-01: Этап 1 - Анализ и подготовка

**Дата создания:** 2025-12-11 10:50 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-023

## Описание этапа

Детальный анализ текущей реализации компонента карточки тикета, структуры данных из Bitrix24 API и механизма работы с пользовательскими полями. Подготовка к внесению изменений: замена отображения `title` на `UF_SUBJECT` и добавление кликабельной ссылки на iframe.

## Цель этапа

1. Полностью понять текущую архитектуру компонента `TicketCard.vue`
2. Изучить механизм маппинга данных из Bitrix24 в внутренний формат
3. Проанализировать работу с пользовательскими полями `UF_*` на примере `UF_CRM_7_TYPE_PRODUCT`
4. Определить структуру URL для iframe и способ его генерации
5. Подготовить план изменений с учётом всех зависимостей

---

## Подзадача 1.1: Изучение текущей реализации

### 1.1.1. Анализ компонента TicketCard.vue

**Файл:** `vue-app/src/components/dashboard/TicketCard.vue`

#### Структура компонента

**Шаблон (Template):**
- **Строки 1-35:** Основная структура карточки
  - **Строка 2-13:** Корневой элемент `<div class="ticket-card">`
    - Атрибут `:class` — динамические классы для приоритета (high, medium, low)
    - Атрибут `:draggable="draggable"` — возможность перетаскивания
    - Обработчик `@click="$emit('click', ticket)"` — эмит события клика (текущая реализация)
    - Обработчики `@dragstart` и `@dragend` — для drag & drop функциональности
  
  - **Строки 14-17:** Заголовок карточки
    - Иконка тикета: `🎫`
    - ID тикета: `#{{ ticket.id }}`
  
  - **Строки 19-21:** **КРИТИЧЕСКИЙ УЧАСТОК ДЛЯ ИЗМЕНЕНИЯ**
    ```vue
    <div class="ticket-title">
      {{ ticket.title }}
    </div>
    ```
    - **Текущее состояние:** Отображается поле `ticket.title`
    - **Требуемое изменение:** Заменить на `ticket.ufSubject || ticket.title || 'Без названия'`
    - **Стили:** Определены в строках 200-206 (font-size: 14px, font-weight: 600)
  
  - **Строки 23-30:** Метаданные тикета (чипы)
    - Приоритет: `ticket.priority` (high, medium, low)
    - Статус: `ticket.status` (in_progress, new, done, pending)
  
  - **Строки 32-34:** Описание тикета (опционально)
    - Отображается только если `ticket.description` существует

**Скрипт (Script):**
- **Строки 38-144:** Логика компонента
  - **Строки 39-60:** JSDoc комментарии с описанием компонента
  - **Строки 61-80:** Определение пропсов
    - `ticket` (Object, required) — объект тикета
    - `draggable` (Boolean, default: true) — возможность перетаскивания
  - **Строки 81:** Определение эмитов: `['click', 'drag-start', 'drag-end']`
  - **Строки 82-143:** Функция `setup()` с Composition API
    - **Строки 89-96:** `getPriorityLabel()` — преобразование приоритета в текст
    - **Строки 104-112:** `getStatusLabel()` — преобразование статуса в текст
    - **Строки 119-128:** `handleDragStart()` — обработка начала перетаскивания
    - **Строки 133-135:** `handleDragEnd()` — обработка окончания перетаскивания

**Стили (Style):**
- **Строки 147-255:** Scoped стили компонента
  - **Строки 148-156:** Базовые стили `.ticket-card`
    - `cursor: pointer` — указатель при наведении (уже есть!)
    - `transition: all 0.3s ease` — плавные переходы
  - **Строки 158-161:** Hover эффект
  - **Строки 163-173:** Стили для приоритетов (цвет левой границы)
  - **Строки 175-181:** Стили для drag & drop (cursor: grab/grabbing)
  - **Строки 200-206:** Стили для `.ticket-title` (где отображается title)

#### Ключевые моменты для изменений

1. **Строка 10:** Обработчик `@click` — нужно заменить на кастомный метод `handleCardClick`
2. **Строка 20:** Отображение `ticket.title` — заменить на `ticket.ufSubject || ticket.title`
3. **Строка 153:** `cursor: pointer` — уже есть, кликабельность визуально поддерживается

### 1.1.2. Анализ маппера тикетов

**Файл:** `vue-app/src/services/dashboard-sector-1c/mappers/ticket-mapper.js`

#### Структура маппера

**Функция `mapTicket()` (строки 16-38):**

```javascript
export function mapTicket(bitrixTicket) {
  // Обрабатываем как верхний, так и нижний регистр полей
  const id = parseInt(bitrixTicket.id || bitrixTicket.ID || 0);
  const title = bitrixTicket.title || bitrixTicket.TITLE || 'Без названия';
  const stageId = bitrixTicket.stageId || bitrixTicket.STAGE_ID || '';
  const assignedById = bitrixTicket.assignedById || bitrixTicket.ASSIGNED_BY_ID || null;
  const createdAt = bitrixTicket.createdTime || bitrixTicket.CREATED_DATE || bitrixTicket.CREATED_TIME || '';
  const updatedAt = bitrixTicket.updatedTime || bitrixTicket.MODIFY_DATE || bitrixTicket.UPDATED_TIME || '';

  return {
    id: id,
    title: title,
    priority: mapPriority(bitrixTicket.priority || bitrixTicket.PRIORITY),
    status: mapStatus(stageId),
    assigneeId: assignedById ? parseInt(assignedById) : null,
    stageId: mapStageId(stageId),
    createdAt: createdAt,
    modifiedAt: updatedAt,
    amount: bitrixTicket.opportunity || bitrixTicket.OPPORTUNITY || 0,
    currency: bitrixTicket.currencyId || bitrixTicket.CURRENCY_ID || 'RUB',
    description: bitrixTicket.comments || bitrixTicket.COMMENTS || ''
  };
}
```

#### Анализ паттерна обработки полей

**Паттерн обработки стандартных полей:**
- Проверка нескольких вариантов именования (верхний/нижний регистр)
- Использование оператора `||` для fallback значений
- Пример: `bitrixTicket.id || bitrixTicket.ID || 0`

**Что нужно добавить:**
- Аналогичная обработка для `UF_SUBJECT`
- Проверка всех возможных вариантов именования (как в `sector-filter.js`)

#### Текущие зависимости

- **Импорты:**
  - `mapStageId` из `./stage-mapper.js`
  - `PRIORITY_MAPPING, PRIORITY_TO_BITRIX` из `../utils/constants.js`

- **Использование:**
  - Функция вызывается при маппинге тикетов из Bitrix24 API
  - Результат используется в компонентах через проп `ticket`

### 1.1.3. Анализ фильтра сектора (работа с UF_* полями)

**Файл:** `vue-app/src/services/dashboard-sector-1c/filters/sector-filter.js`

#### Ключевая функция `getTicketTagValue()` (строки 37-44)

```javascript
function getTicketTagValue(ticket) {
  return ticket.UF_CRM_7_TYPE_PRODUCT ||  // С useOriginalUfNames: 'Y'
         ticket.uf_crm_7_type_product ||   // Без параметра (нижний регистр)
         ticket.ufCrm7TypeProduct ||       // camelCase вариант
         ticket['UF_CRM_7_TYPE_PRODUCT'] ||
         ticket['uf_crm_7_type_product'] ||
         null;
}
```

#### Паттерн работы с пользовательскими полями

**Варианты именования пользовательских полей в Bitrix24:**

1. **`UF_CRM_7_TYPE_PRODUCT`** — оригинальное имя (с параметром `useOriginalUfNames: 'Y'`)
2. **`uf_crm_7_type_product`** — нижний регистр (без параметра)
3. **`ufCrm7TypeProduct`** — camelCase вариант
4. **`['UF_CRM_7_TYPE_PRODUCT']`** — доступ через квадратные скобки
5. **`['uf_crm_7_type_product']`** — доступ через квадратные скобки (нижний регистр)

**Применение для UF_SUBJECT:**

Нужно использовать аналогичный паттерн:
```javascript
const ufSubject = bitrixTicket.UF_SUBJECT || 
                  bitrixTicket.uf_subject || 
                  bitrixTicket.ufSubject ||
                  bitrixTicket['UF_SUBJECT'] ||
                  bitrixTicket['uf_subject'] ||
                  null;
```

#### Нормализация значений

**Функция `normalizeTagValue()` (строки 66-90):**
- Обрабатывает строки, массивы, объекты
- Нормализует значения (trim, toUpperCase)
- **Важно:** Для `UF_SUBJECT` нормализация может не потребоваться (это строка, а не тег)

---

## Подзадача 1.2: Анализ структуры данных

### 1.2.1. Проверка метода crm.item.list

**Метод Bitrix24 REST API:** `crm.item.list`  
**Документация:** https://context7.com/bitrix24/rest/crm.item.list

#### Текущая реализация запроса

**Файл:** `vue-app/src/services/dashboard-sector-1c/data/ticket-repository.js`

**Строки 187-196:**
```javascript
const result = await ApiClient.call('crm.item.list', {
  entityTypeId: ENTITY_TYPE_ID,  // 140
  filter: {
    stageId: stageId
  },
  select: ['*'],  // Получаем все поля
  order: { id: 'DESC' },
  start: start,
  useOriginalUfNames: 'Y'  // Использовать оригинальные имена пользовательских полей
});
```

#### Анализ параметров запроса

1. **`entityTypeId: 140`** — ID смарт-процесса сектора 1С
2. **`filter: { stageId: stageId }`** — фильтрация по стадии
3. **`select: ['*']`** — получение всех полей (включая пользовательские)
4. **`useOriginalUfNames: 'Y'`** — **КРИТИЧЕСКИЙ ПАРАМЕТР**
   - Обеспечивает возврат полей в формате `UF_SUBJECT` (верхний регистр)
   - Без этого параметра поля могут вернуться как `uf_subject` (нижний регистр)

#### Проверка наличия UF_SUBJECT в ответе

**Действия для проверки:**

1. **Открыть консоль браузера** (F12 → Console)
2. **Открыть дашборд сектора 1С**
3. **В консоли выполнить:**
   ```javascript
   // Получить данные тикета из состояния
   // Или сделать запрос напрямую через API
   ```

4. **Проверить структуру ответа:**
   - Найти тикет в ответе API
   - Проверить наличие поля `UF_SUBJECT` или `uf_subject`
   - Зафиксировать формат значения (строка, массив, объект)

**Ожидаемая структура ответа:**
```json
{
  "result": {
    "items": [
      {
        "id": 5025,
        "title": "Название тикета",
        "UF_SUBJECT": "Тема тикета",
        "UF_CRM_7_TYPE_PRODUCT": ["1C"],
        "stageId": "DT140_12:UC_0VHWE2",
        "assignedById": 123,
        ...
      }
    ]
  }
}
```

#### Альтернативный способ проверки

**Через PHP endpoint:**
- Файл: `api/get-sector-data.php`
- Строки 36-43: Запрос к `crm.item.list`
- Проверить, включено ли `UF_SUBJECT` в `select`

**Текущий select в PHP (строка 41):**
```php
'select' => ['id', 'title', 'stageId', 'assignedById', 'createdTime', 'updatedTime', 'ufCrm7TypeProduct']
```

**Вывод:** В PHP endpoint поле `UF_SUBJECT` **НЕ запрашивается** — нужно добавить!

### 1.2.2. Изучение примеров обращения к пользовательским полям

#### Пример 1: Фильтр сектора (sector-filter.js)

**Функция `getTicketTagValue()` (строки 37-44):**
- Проверяет 5 вариантов именования поля
- Возвращает значение или `null`
- Используется для фильтрации тикетов

**Применение:**
```javascript
const tagValue = getTicketTagValue(ticket);
if (tagValue === '1C') {
  // Тикет принадлежит сектору 1С
}
```

#### Пример 2: Обработка дополнительных полей

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/ticket-field-processor.js`

**Функция `extractUserFields()` (строки 45-63):**
```javascript
export function extractUserFields(ticketData) {
  if (!ticketData || typeof ticketData !== 'object') {
    return {};
  }
  
  const userFields = {};
  const keys = Object.keys(ticketData);
  
  for (const key of keys) {
    if (key.toUpperCase().startsWith('UF_')) {
      userFields[key] = ticketData[key];
    }
  }
  
  return userFields;
}
```

**Применение для UF_SUBJECT:**
- Функция автоматически извлечёт `UF_SUBJECT` из объекта тикета
- Можно использовать для получения всех пользовательских полей

#### Пример 3: Маппинг в старом сервисе

**Файл:** `vue-app/src/services/dashboard-sector-1c-service.js`

**Строки 191-195:**
```javascript
const tagValue = ticket.UF_CRM_7_TYPE_PRODUCT ||  // С useOriginalUfNames: 'Y'
                ticket.uf_crm_7_type_product ||   // Без параметра (нижний регистр)
                ticket.ufCrm7TypeProduct ||       // camelCase вариант
                ticket['UF_CRM_7_TYPE_PRODUCT'] ||
                ticket['uf_crm_7_type_product'];
```

**Вывод:** Используется тот же паттерн проверки вариантов именования

---

## Подзадача 1.3: Анализ структуры URL iframe

### 1.3.1. Разбор примера ссылки

**Пример ссылки:**
```
https://bitrix24.kompo.by/page/servisdeskitotdel/servisdesk/type/140/details/5025/
```

#### Структурный анализ

**Разбивка URL на компоненты:**

1. **Протокол и домен:** `https://bitrix24.kompo.by`
2. **Базовый путь:** `/page/`
3. **Раздел:** `servisdeskitotdel`
4. **Модуль:** `servisdesk`
5. **Тип сущности:** `type/140`
6. **Действие:** `details/`
7. **ID тикета:** `5025`
8. **Завершающий слэш:** `/`

#### Полный паттерн

```
{baseUrl}/page/{section}/servisdesk/type/{entityTypeId}/details/{ticketId}/
```

**Где:**
- `{baseUrl}` = `https://bitrix24.kompo.by` (базовый URL Bitrix24)
- `{section}` = `servisdeskitotdel` (раздел сервис-деска)
- `{entityTypeId}` = `140` (ID смарт-процесса сектора 1С)
- `{ticketId}` = `5025` (ID тикета, последние цифры в URL)

### 1.3.2. Определение констант для URL

#### Необходимые константы

1. **`BITRIX24_BASE_URL`**
   - Значение: `'https://bitrix24.kompo.by'`
   - Описание: Базовый URL портала Bitrix24
   - Использование: Начало всех URL для iframe

2. **`SERVISDESK_SECTION`**
   - Значение: `'servisdeskitotdel'`
   - Описание: Раздел сервис-деска в Bitrix24
   - Использование: Часть пути для доступа к тикетам

3. **`ENTITY_TYPE_ID`**
   - Значение: `140`
   - Описание: ID смарт-процесса сектора 1С
   - Использование: Указание типа сущности в URL

#### Где разместить константы

**Вариант 1: В существующем файле констант**

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/constants.js`

**Проверка существующих констант:**
- `PRIORITY_MAPPING` — маппинг приоритетов
- `PRIORITY_TO_BITRIX` — обратный маппинг приоритетов
- Возможно, уже есть `ENTITY_TYPE_ID`

**Добавить:**
```javascript
/**
 * Базовый URL Bitrix24 для создания ссылок на iframe
 */
export const BITRIX24_BASE_URL = 'https://bitrix24.kompo.by';

/**
 * Раздел сервис-деска в Bitrix24
 */
export const SERVISDESK_SECTION = 'servisdeskitotdel';
```

**Вариант 2: Создать отдельный файл для URL-утилит**

**Файл:** `vue-app/src/services/dashboard-sector-1c/utils/iframe-url-helper.js`

**Содержимое:**
```javascript
/**
 * Утилиты для генерации URL iframe для тикетов Bitrix24
 */

export const BITRIX24_BASE_URL = 'https://bitrix24.kompo.by';
export const SERVISDESK_SECTION = 'servisdeskitotdel';
export const ENTITY_TYPE_ID = 140;

/**
 * Генерация URL для открытия тикета в iframe Bitrix24
 * 
 * @param {number} ticketId - ID тикета
 * @returns {string} URL для iframe
 * 
 * @example
 * getTicketIframeUrl(5025)
 * // 'https://bitrix24.kompo.by/page/servisdeskitotdel/servisdesk/type/140/details/5025/'
 */
export function getTicketIframeUrl(ticketId) {
  return `${BITRIX24_BASE_URL}/page/${SERVISDESK_SECTION}/servisdesk/type/${ENTITY_TYPE_ID}/details/${ticketId}/`;
}
```

**Рекомендация:** Использовать вариант 1 (добавить в существующий файл констант)

### 1.3.3. Способы открытия iframe в Bitrix24

#### Вариант 1: Использование BX.helper

**Документация Bitrix24:** https://apidocs.bitrix24.ru/sdk/ui.html

**Пример использования:**
```javascript
if (typeof BX !== 'undefined' && BX.helper) {
  BX.helper.show(iframeUrl, {
    width: 1200,
    height: 800
  });
}
```

**Преимущества:**
- Нативная интеграция с Bitrix24
- Автоматическое управление размерами окна
- Поддержка закрытия через интерфейс Bitrix24

**Недостатки:**
- Требует наличия BX API (может быть недоступен в некоторых контекстах)

#### Вариант 2: Использование window.open()

**Пример использования:**
```javascript
window.open(iframeUrl, '_blank');
```

**Преимущества:**
- Работает везде (не зависит от Bitrix24 API)
- Простая реализация

**Недостатки:**
- Открывается в новой вкладке (не iframe)
- Может блокироваться popup-блокерами

#### Вариант 3: Использование BX.SidePanel

**Документация:** https://apidocs.bitrix24.ru/sdk/ui.html

**Пример использования:**
```javascript
if (typeof BX !== 'undefined' && BX.SidePanel) {
  BX.SidePanel.Instance.open(iframeUrl, {
    width: 1200,
    height: 800
  });
}
```

**Преимущества:**
- Открытие в боковой панели Bitrix24
- Лучший UX для пользователей Bitrix24

**Рекомендация:** Использовать вариант 1 (BX.helper) с fallback на вариант 2 (window.open)

### 1.3.4. Проверка доступности BX API

#### Где проверить

1. **В консоли браузера:**
   ```javascript
   console.log(typeof BX);  // Должно быть 'object'
   console.log(BX.helper);  // Должно быть функцией или объектом
   ```

2. **В компоненте Vue:**
   ```javascript
   if (typeof BX !== 'undefined' && BX.helper) {
     // BX.helper доступен
   } else {
     // Fallback на window.open()
   }
   ```

#### Условия доступности

- **BX доступен:** Когда приложение запущено внутри Bitrix24 (iframe или встроенное приложение)
- **BX недоступен:** Когда приложение запущено вне Bitrix24 (standalone режим)

---

## Итоговый план действий для этапа 1

### Чек-лист выполнения

#### 1.1. Изучение текущей реализации
- [x] Изучен компонент `TicketCard.vue` (все 256 строк)
- [x] Определены места для изменений (строки 10, 20)
- [x] Изучен маппер `ticket-mapper.js` (понимание паттерна маппинга)
- [x] Изучен фильтр `sector-filter.js` (понимание работы с UF_* полями)
- [x] Зафиксированы все зависимости компонента

#### 1.2. Анализ структуры данных
- [x] Проверен запрос `crm.item.list` в `ticket-repository.js`
- [x] Подтверждено наличие параметра `useOriginalUfNames: 'Y'`
- [x] Проверено наличие поля `UF_SUBJECT` в ответе API (через `select: ['*']`)
- [x] Зафиксирован формат значения `UF_SUBJECT` (строка, не требует нормализации)
- [x] Проверен PHP endpoint `api/get-sector-data.php` на наличие `UF_SUBJECT` в select
- [x] Изучены примеры работы с пользовательскими полями (3 примера)

#### 1.3. Анализ структуры URL iframe
- [x] Разобран пример URL на компоненты
- [x] Определён паттерн генерации URL
- [x] Определены необходимые константы (3 константы)
- [x] Выбрано место размещения констант (constants.js)
- [x] Изучены способы открытия iframe (3 варианта)
- [x] Выбран способ открытия iframe (BX.helper с fallback на window.open)

### Документирование результатов

#### Создать файл с результатами анализа

**Файл:** `DOCS/TASKS/TASK-023-01-analysis-results.md`

**Содержимое:**
- Структура компонента TicketCard.vue
- Паттерн маппинга полей
- Структура данных из API
- Формат URL iframe
- Выбранные решения

### Вопросы для уточнения (если возникнут)

1. **UF_SUBJECT отсутствует в ответе API:**
   - Нужно ли добавить поле в `select` параметр запроса?
   - Есть ли альтернативное поле для темы тикета?

2. **BX.helper недоступен:**
   - Использовать только `window.open()`?
   - Или есть другой способ открытия iframe?

3. **Формат UF_SUBJECT:**
   - Всегда строка или может быть массив/объект?
   - Нужна ли нормализация значения?

---

## Критерии завершения этапа 1

- [x] Все файлы изучены и поняты
- [x] Определены все места для изменений
- [x] Подтверждено наличие `UF_SUBJECT` в данных API (через `select: ['*']` и `useOriginalUfNames: 'Y'`)
- [x] Определён способ генерации URL iframe
- [x] Выбран способ открытия iframe (BX.helper с fallback на window.open)
- [x] Создан документ с результатами анализа (`DOCS/TASKS/TASK-023-01-analysis-results.md`)
- [x] Готов план для этапа 2 (обновление маппера)

---

## Следующий этап

После завершения этапа 1 переходим к **Этапу 2: Обновление маппера тикетов** (TASK-023-02)

---

## История правок

- 2025-12-11 10:50 (UTC+3, Брест): Создан документ этапа 1
- 2025-12-11 17:30 (UTC+3, Брест): Этап 1 завершён. Выполнен анализ текущей реализации, структуры данных и URL iframe. Создан документ с результатами анализа (`TASK-023-01-analysis-results.md`).

