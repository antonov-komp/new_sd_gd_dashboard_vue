# TASK-056-03: Оптимизация легенды графика

**Дата создания:** 2025-12-17 16:42 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-056: Улучшение дизайна линейных графиков](./TASK-056-line-charts-design-improvements.md)  
**Этап:** 3/7

---

## 📋 Описание

Оптимизировать легенду линейных графиков модуля «График приёма и закрытий сектора 1С»: переместить вниз графика, уменьшить размеры, добавить hover-эффекты и реализовать toggle серий при клике.

---

## 🎯 Контекст

**Текущее состояние:**
- Легенда расположена сверху графика
- Размер шрифта: `15px`
- Padding: `16px`
- BoxWidth: `20px`
- Нет hover-эффектов
- Нет возможности скрыть/показать серии

**Проблема:**
- Легенда занимает много места сверху
- Нет интерактивности
- Сложно быстро скрыть ненужные серии

**Требуемое состояние:**
- Легенда внизу графика
- Уменьшенные размеры (шрифт `13px`, padding `12px`, boxWidth `18px`)
- Hover-эффект на элементы легенды (подсветка соответствующей линии)
- Toggle серий при клике на элемент легенды
- Группировка основных и вспомогательных серий

---

## 🔧 Технические требования

### 1. Расположение легенды

**Позиция:**
- `position: 'bottom'` (вместо `'top'`)
- Горизонтальное расположение элементов
- Максимум 2 строки легенды (если не помещается)

### 2. Стилизация легенды

**Размеры:**
- Шрифт: `13px` (вместо `15px`)
- Padding: `12px` (вместо `16px`)
- BoxWidth: `18px` (вместо `20px`)
- BoxHeight: `12px`

**Стиль:**
- `usePointStyle: true` — использовать круглые точки вместо прямоугольников
- `pointStyle: 'circle'` — стиль точки

### 3. Hover-эффекты

**Требования:**
- При наведении на элемент легенды — подсветка соответствующей линии на графике
- Визуальная обратная связь (изменение цвета или размера элемента легенды)

**Реализация:**
- Использовать события Chart.js для hover
- Или кастомный обработчик через CSS

### 4. Toggle серий

**Функциональность:**
- Клик по элементу легенды скрывает/показывает серию
- Визуальная индикация скрытых серий (зачёркнутый текст или уменьшенная прозрачность)

**Реализация:**
- Использовать `onClick` callback в конфигурации легенды
- Обновлять график через `chart.update()`

### 5. Группировка серий

**Основные серии:**
- Новые
- Закрытые (все)
- Переходящие (все)
- Обычный стиль текста

**Вспомогательные серии:**
- Закрытые (созданы этой неделей)
- Закрытые (созданы другой неделей)
- Переходящие (созданы этой неделей)
- Переходящие (созданы другой неделей)
- Более светлый цвет текста (`#6b7280` вместо `#111827`)

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Переместить легенду вниз

1. **Открыть файл:**
   - `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`

2. **Обновить `chartOptions.computed`:**
   ```javascript
   plugins: {
     legend: {
       position: 'bottom', // Вместо 'top'
       // ... остальные настройки
     }
   }
   ```

### Шаг 2: Уменьшить размеры

1. **Обновить настройки labels:**
   ```javascript
   legend: {
     position: 'bottom',
     labels: {
       font: {
         size: 13, // Вместо 15
         weight: '500'
       },
       padding: 12, // Вместо 16
       boxWidth: 18, // Вместо 20
       boxHeight: 12,
       usePointStyle: true,
       pointStyle: 'circle'
     }
   }
   ```

### Шаг 3: Добавить hover-эффект

1. **Настроить обработчик hover:**
   - Использовать события Chart.js или кастомный CSS
   - При hover подсвечивать соответствующую линию

2. **Пример реализации:**
   ```javascript
   // В chartOptions можно добавить обработчик через плагин
   plugins: {
     legend: {
       // ... настройки
       onHover: (e, legendItem) => {
         // Подсветка линии при hover на элемент легенды
         e.native.target.style.cursor = 'pointer';
       },
       onLeave: (e, legendItem) => {
         // Сброс подсветки
       }
     }
   }
   ```

### Шаг 4: Реализовать toggle серий

1. **Добавить обработчик onClick с обработкой ошибок:**
   ```javascript
   legend: {
     // ... настройки
     onClick: (e, legendItem) => {
       // Предотвращаем стандартное поведение
       e.native?.preventDefault?.();
       
       const index = legendItem.datasetIndex;
       if (index === undefined || index === null) {
         console.warn('[Legend] Invalid datasetIndex:', index);
         return;
       }
       
       const chart = e.chart;
       if (!chart) {
         console.warn('[Legend] Chart not found');
         return;
       }
       
       const meta = chart.getDatasetMeta(index);
       if (!meta) {
         console.warn('[Legend] Dataset meta not found for index:', index);
         return;
       }
       
       // Toggle видимости серии
       meta.hidden = !meta.hidden;
       
       // Обновить график с анимацией
       chart.update('active'); // 'active' для плавной анимации
     }
   }
   ```

2. **Добавить визуальную индикацию скрытых серий в generateLabels:**
   ```javascript
   generateLabels: (chart) => {
     const original = Chart.defaults.plugins.legend.labels.generateLabels(chart);
     return original.map((label, index) => {
       const dataset = chart.data.datasets[index];
       const meta = chart.getDatasetMeta(index);
       
       // Определить, является ли серия вспомогательной
       const isAuxiliary = dataset.borderDash && dataset.borderDash.length > 0;
       
       // Настройка цвета текста
       if (meta.hidden) {
         // Скрытая серия: серый цвет и зачёркнутый текст
         label.fontColor = '#9ca3af';
         label.textDecoration = 'line-through';
         label.opacity = 0.5;
       } else if (isAuxiliary) {
         // Вспомогательная серия: светло-серый
         label.fontColor = '#6b7280';
       } else {
         // Основная серия: тёмно-серый
         label.fontColor = '#111827';
       }
       
       return label;
     });
   }
   ```

### Шаг 5: Группировка серий

1. **Настроить разные стили для основных и вспомогательных серий:**
   ```javascript
   legend: {
     labels: {
       // ... базовые настройки
       generateLabels: (chart) => {
         const original = Chart.defaults.plugins.legend.labels.generateLabels(chart);
         return original.map((label, index) => {
           // Определить, является ли серия вспомогательной
           const isAuxiliary = /* логика определения */;
           
           if (isAuxiliary) {
             label.fontColor = '#6b7280'; // Светло-серый для вспомогательных
           }
           
           return label;
         });
       }
     }
   }
   ```

---

## 🔍 Детали реализации

### Файлы для изменения

1. **`vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`**
   - Обновить `chartOptions.computed`
   - Добавить обработчики для легенды

### Применение к обоим графикам

**Левый график (Новые и Закрытые):**
- Легенда должна показывать все 4 серии
- Основные: Новые, Закрытые (все)
- Вспомогательные: Закрытые (созданы этой неделей), Закрытые (созданы другой неделей)

**Правый график (Переходящие):**
- Легенда должна показывать все 3 серии
- Основные: Переходящие (все)
- Вспомогательные: Переходящие (созданы этой неделей), Переходящие (созданы другой неделей)

### Определение вспомогательных серий

**Критерии (любой из них):**
- Серии с пунктирными линиями (`borderDash && borderDash.length > 0`)
- Серии с уменьшенной толщиной (`borderWidth === 2`)
- Серии без градиентов (`fill === false`)

**Логика определения:**
```javascript
function isAuxiliarySeries(dataset) {
  // Проверяем наличие пунктира
  if (dataset.borderDash && Array.isArray(dataset.borderDash) && dataset.borderDash.length > 0) {
    return true;
  }
  
  // Проверяем толщину линии
  if (dataset.borderWidth === 2) {
    return true;
  }
  
  // Проверяем отсутствие заливки
  if (dataset.fill === false) {
    return true;
  }
  
  return false;
}
```

**Применение в generateLabels:**
```javascript
generateLabels: (chart) => {
  const original = Chart.defaults.plugins.legend.labels.generateLabels(chart);
  return original.map((label, index) => {
    const dataset = chart.data.datasets[index];
    const isAuxiliary = isAuxiliarySeries(dataset);
    
    // ... остальная логика
  });
}
```

---

## 💻 Примеры кода

### Обновлённая конфигурация легенды

```javascript
const chartOptions = computed(() => ({
  // ... другие настройки
  plugins: {
    legend: {
      position: 'bottom',
      labels: {
        font: {
          size: 13,
          weight: '500'
        },
        padding: 12,
        boxWidth: 18,
        boxHeight: 12,
        usePointStyle: true,
        pointStyle: 'circle',
        generateLabels: (chart) => {
          const original = Chart.defaults.plugins.legend.labels.generateLabels(chart);
          return original.map((label, index) => {
            const dataset = chart.data.datasets[index];
            // Определить, является ли серия вспомогательной
            const isAuxiliary = dataset.borderDash && dataset.borderDash.length > 0;
            
            if (isAuxiliary) {
              label.fontColor = '#6b7280'; // Светло-серый для вспомогательных
            } else {
              label.fontColor = '#111827'; // Тёмно-серый для основных
            }
            
            return label;
          });
        }
      },
      onClick: (e, legendItem) => {
        const index = legendItem.datasetIndex;
        const chart = e.chart;
        const meta = chart.getDatasetMeta(index);
        
        // Toggle видимости серии
        meta.hidden = !meta.hidden;
        
        // Обновить график
        chart.update();
      },
      onHover: (e, legendItem) => {
        // Изменить курсор при hover
        e.native.target.style.cursor = 'pointer';
      },
      onLeave: (e, legendItem) => {
        // Сброс курсора
        e.native.target.style.cursor = 'default';
      }
    }
  }
}));
```

### Стили для скрытых серий

```javascript
// В generateLabels можно добавить стиль для скрытых серий
generateLabels: (chart) => {
  const original = Chart.defaults.plugins.legend.labels.generateLabels(chart);
  return original.map((label, index) => {
    const meta = chart.getDatasetMeta(index);
    
    // Если серия скрыта, добавить стиль
    if (meta.hidden) {
      label.textDecoration = 'line-through';
      label.fontColor = '#9ca3af'; // Серый для скрытых
    }
    
    return label;
  });
}
```

---

## ✅ Критерии приёмки

- [ ] Легенда расположена внизу графика
- [ ] Размер шрифта уменьшен до `13px`
- [ ] Padding уменьшен до `12px`
- [ ] BoxWidth уменьшен до `18px`
- [ ] Используется стиль точек (`usePointStyle: true`)
- [ ] Hover-эффект работает на элементах легенды (курсор pointer)
- [ ] Клик по элементу легенды скрывает/показывает серию
- [ ] Скрытые серии имеют визуальную индикацию (зачёркнутый текст или уменьшенная прозрачность)
- [ ] Вспомогательные серии имеют более светлый цвет текста (`#6b7280`)
- [ ] Основные серии имеют обычный цвет текста (`#111827`)
- [ ] Легенда работает корректно для обоих графиков
- [ ] Легенда адаптивна (максимум 2 строки на мобильных)

---

## 🔗 Зависимости

**Зависит от:**
- [TASK-056-01: Улучшение визуализации линий](./TASK-056-01-lines-visualization.md) — легенда должна соответствовать улучшенным линиям
- [TASK-056-02: Улучшение tooltips](./TASK-056-02-tooltips-improvement.md) — может использовать аналогичное форматирование

**Зависит от этого этапа:**
- [TASK-056-06: Добавление анимаций](./TASK-056-06-animations.md) — может использовать hover-эффекты легенды

---

## 📝 История правок

- **2025-12-17 16:42 (UTC+3, Брест):** Создан этап TASK-056-03
  - Определены требования для оптимизации легенды
  - Зафиксированы технические детали и примеры кода
  - Добавлены критерии приёмки

---

## 💡 Примечания

- **Интерактивность:** Toggle серий должен работать плавно, без лагов
- **Визуальная обратная связь:** Важно показать пользователю, что серия скрыта
- **Адаптивность:** На мобильных устройствах легенда может занимать 2 строки
- **Совместимость:** Проверить работу на разных версиях Chart.js
- **Тестирование:** Протестировать toggle серий, hover-эффекты, группировку

## ⚠️ Возможные проблемы и решения

### Проблема 1: Toggle не работает
**Причина:** `onClick` не вызывается или `chart.update()` не срабатывает  
**Решение:** 
- Проверить, что `legendItem.datasetIndex` не `undefined`
- Использовать `chart.update('active')` вместо `chart.update()`
- Добавить обработку ошибок

### Проблема 2: Визуальная индикация скрытых серий не обновляется
**Причина:** `generateLabels` вызывается только при создании легенды  
**Решение:** Chart.js автоматически перерисовывает легенду при `chart.update()`, но можно принудительно обновить через `chart.update('none')`

### Проблема 3: Hover-эффект не работает
**Причина:** `onHover` может не работать в некоторых версиях Chart.js  
**Решение:** Использовать CSS hover через глобальные стили:
```css
/* Глобальные стили для легенды Chart.js */
.chartjs-legend-item {
  cursor: pointer;
  transition: opacity 0.2s ease;
}

.chartjs-legend-item:hover {
  opacity: 0.7;
}
```

### Проблема 4: Легенда не помещается в 2 строки
**Причина:** Слишком много серий или длинные названия  
**Решение:** 
- Уменьшить `padding` до `8px` на мобильных
- Уменьшить размер шрифта до `11px` на мобильных
- Использовать `maxWidth` для элементов легенды

### Проблема 5: Группировка не работает правильно
**Причина:** Логика определения вспомогательных серий неверна  
**Решение:** Проверить все критерии (пунктир, толщина, заливка) и убедиться, что они применяются ко всем вспомогательным сериям

## 🔍 Детальная проверка реализации

**После реализации проверить:**
1. Легенда расположена внизу обоих графиков
2. Размеры уменьшены (шрифт 13px, padding 12px, boxWidth 18px)
3. Используется стиль точек (`usePointStyle: true`)
4. Hover меняет курсор на `pointer`
5. Клик скрывает/показывает серию
6. Скрытые серии имеют зачёркнутый текст и серый цвет
7. Вспомогательные серии имеют светло-серый цвет (`#6b7280`)
8. Основные серии имеют тёмно-серый цвет (`#111827`)
9. Легенда адаптивна (максимум 2 строки на мобильных)
10. Toggle работает плавно без лагов

