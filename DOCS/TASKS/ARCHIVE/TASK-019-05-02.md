# TASK-019-05-02: Интеграция CreateSnapshotButton с сервисами, добавление индикатора прогресса и обработка ошибок

**Дата создания:** 2025-12-08 11:00 (UTC+3, Брест)  
**Статус:** Выполнена  
**Дата завершения:** 2025-12-08 13:54 (UTC+3, Брест)  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 5, часть 2)

---

## 📋 Цель задачи

Интегрировать компонент `CreateSnapshotButton` с сервисами создания слепков (`SectorDataAdapter`, `SnapshotService`), добавить индикатор прогресса загрузки данных и обработку ошибок с детальными уведомлениями.

---

## 🎯 Контекст

После создания базового компонента `CreateSnapshotButton` (TASK-019-05-01) необходимо:
- Интегрировать с `SectorDataAdapter` для получения данных сектора
- Интегрировать с `SnapshotService` для создания слепка
- Добавить индикатор прогресса загрузки данных
- Реализовать обработку ошибок с понятными сообщениями
- Добавить уведомления об успехе/ошибке через `useNotifications`

**Требования:**
- Отображение прогресса загрузки данных сектора
- Обработка всех типов ошибок (сеть, валидация, сервер)
- Детальные уведомления для пользователя
- Блокировка повторных запросов во время создания

**Зависимости:**
- TASK-019-05-01 должна быть завершена — базовый компонент создан
- TASK-019-03-01 должна быть завершена — `SectorDataAdapter` создан
- TASK-019-03-02 должна быть завершена — нормализация данных реализована
- TASK-019-02-01 должна быть завершена — `SnapshotService` создан

---

## 🏗️ Архитектура решения

### Интеграция с сервисами

**Поток создания слепка:**
1. Пользователь нажимает "Создать" в модальном окне
2. Загрузка данных сектора через `SectorDataAdapter.getSectorDataForSnapshot()`
   - Отображение прогресса загрузки
   - Обработка ошибок загрузки
3. Нормализация данных (автоматически в `SectorDataAdapter`)
4. Создание слепка через `SnapshotService.createSnapshot()`
   - Передача нормализованных данных
   - Указание типа `'manual'`
   - Передача метаданных (создатель)
5. Уведомление об успехе/ошибке
6. Эмит события `snapshot-created` с созданным слепком

### Индикатор прогресса

**Типы прогресса:**
- Загрузка данных сектора (0-80%)
- Создание слепка (80-100%)

**Отображение:**
- Прогресс-бар в модальном окне
- Текстовое описание текущего этапа
- Процент выполнения

---

## 📝 Что нужно сделать

### 1. Интегрировать SectorDataAdapter

**Действия:**
- Импортировать `SectorDataAdapter` в компонент
- Вызвать `SectorDataAdapter.getSectorDataForSnapshot()` при создании слепка
- Передать колбэк прогресса для отображения прогресса загрузки
- Обработать ошибки загрузки данных

**Пример:**
```javascript
import { SectorDataAdapter } from '@/services/graph-state/SectorDataAdapter';

const loadSectorData = async () => {
  try {
    const sectorData = await SectorDataAdapter.getSectorDataForSnapshot({
      useCache: true,
      onProgress: (progressInfo) => {
        // Обновление прогресса
        loadingProgress.value = progressInfo.progress || 0;
        loadingStep.value = progressInfo.step || 'loading';
        loadingDescription.value = progressInfo.description || 'Загрузка данных...';
      }
    });
    
    return sectorData;
  } catch (error) {
    throw new Error(`Ошибка загрузки данных сектора: ${error.message}`);
  }
};
```

---

### 2. Интегрировать SnapshotService

**Действия:**
- Импортировать `SnapshotService` в компонент
- Вызвать `SnapshotService.createSnapshot()` после загрузки данных
- Передать нормализованные данные, тип `'manual'` и метаданные
- Обработать ошибки создания слепка

**Пример:**
```javascript
import { SnapshotService } from '@/services/graph-state/SnapshotService';

const createSnapshot = async (sectorData) => {
  try {
    // Получение информации о текущем пользователе
    const user = currentUser.value;
    
    const snapshot = await SnapshotService.createSnapshot(
      sectorData,
      'manual',
      {
        createdBy: {
          id: user.ID,
          name: `${user.NAME} ${user.LAST_NAME}`.trim() || user.EMAIL
        },
        sectorId: '1C'
      }
    );
    
    return snapshot;
  } catch (error) {
    throw new Error(`Ошибка создания слепка: ${error.message}`);
  }
};
```

---

### 3. Добавить индикатор прогресса

**Компонент прогресса:**
- Прогресс-бар с процентами
- Текстовое описание текущего этапа
- Анимация загрузки

**Состояние прогресса:**
```javascript
const loadingProgress = ref(0); // 0-100
const loadingStep = ref('idle'); // idle, loading_data, creating_snapshot, success, error
const loadingDescription = ref('');
```

**Отображение в модальном окне:**
- Прогресс-бар в теле модального окна
- Описание этапа под прогресс-баром
- Блокировка кнопок во время загрузки

---

### 4. Реализовать обработку ошибок

**Типы ошибок:**
- Ошибка загрузки данных сектора
- Ошибка нормализации данных
- Ошибка создания слепка (сеть, сервер, валидация)
- Ошибка доступа (не администратор)

**Обработка:**
- Try/catch для всех асинхронных операций
- Детальные сообщения об ошибках для пользователя
- Логирование ошибок в консоль
- Уведомления через `useNotifications`

**Примеры сообщений:**
- "Ошибка загрузки данных сектора: Превышено время ожидания"
- "Ошибка создания слепка: Не удалось сохранить файл"
- "Ошибка: Только администраторы могут создавать слепки"

---

### 5. Добавить уведомления

**Типы уведомлений:**
- Успех: "Слепок успешно создан"
- Ошибка: детальное сообщение об ошибке
- Предупреждение: "Процесс создания может занять некоторое время"

**Интеграция:**
- Использовать `useNotifications` композабл
- Показывать уведомления после завершения операций
- Не показывать уведомления во время процесса (только в конце)

---

## 💡 Примеры кода

### Полная реализация handleCreate

```javascript
import { SectorDataAdapter } from '@/services/graph-state/SectorDataAdapter';
import { SnapshotService } from '@/services/graph-state/SnapshotService';
import { useNotifications } from '@/composables/useNotifications.js';

// В setup()
const notifications = useNotifications();
const loadingProgress = ref(0);
const loadingStep = ref('idle');
const loadingDescription = ref('');

/**
 * Обработка создания слепка
 */
const handleCreate = async () => {
  if (isLoading.value) {
    return;
  }

  // Проверка прав администратора
  if (!isUserAdmin.value) {
    notifications.warning('Только администраторы могут создавать слепки');
    return;
  }

  isLoading.value = true;
  loadingStep.value = 'loading_data';
  loadingProgress.value = 0;
  loadingDescription.value = 'Инициализация загрузки данных...';

  try {
    // Шаг 1: Загрузка данных сектора
    loadingDescription.value = 'Загрузка данных сектора из Bitrix24...';
    
    const sectorData = await SectorDataAdapter.getSectorDataForSnapshot({
      useCache: true,
      onProgress: (progressInfo) => {
        // Нормализация прогресса: 0-80% для загрузки данных
        const normalizedProgress = Math.min(80, (progressInfo.progress || 0) * 0.8);
        loadingProgress.value = normalizedProgress;
        loadingStep.value = 'loading_data';
        loadingDescription.value = progressInfo.description || 'Загрузка данных сектора...';
      }
    });

    // Шаг 2: Создание слепка
    loadingStep.value = 'creating_snapshot';
    loadingProgress.value = 85;
    loadingDescription.value = 'Создание слепка...';

    const user = currentUser.value;
    const snapshot = await SnapshotService.createSnapshot(
      sectorData,
      'manual',
      {
        createdBy: {
          id: user.ID,
          name: `${user.NAME || ''} ${user.LAST_NAME || ''}`.trim() || user.EMAIL || `User ${user.ID}`
        },
        sectorId: '1C'
      }
    );

    // Успех
    loadingStep.value = 'success';
    loadingProgress.value = 100;
    loadingDescription.value = 'Слепок успешно создан!';

    // Уведомление об успехе
    notifications.success('Слепок состояния сектора успешно создан');

    // Эмит события
    emit('snapshot-created', snapshot);

    // Закрытие модального окна через небольшую задержку
    setTimeout(() => {
      closeModal();
      // Сброс состояния
      loadingStep.value = 'idle';
      loadingProgress.value = 0;
      loadingDescription.value = '';
    }, 1500);

  } catch (error) {
    // Обработка ошибок
    loadingStep.value = 'error';
    loadingProgress.value = 0;
    loadingDescription.value = 'Ошибка создания слепка';

    console.error('Error creating snapshot:', error);

    // Детальное сообщение об ошибке
    let errorMessage = 'Ошибка создания слепка';
    
    if (error.message) {
      if (error.message.includes('загрузки данных')) {
        errorMessage = 'Не удалось загрузить данные сектора. Проверьте подключение к Bitrix24.';
      } else if (error.message.includes('создания слепка')) {
        errorMessage = 'Не удалось создать слепок. Обратитесь в поддержку.';
      } else {
        errorMessage = error.message;
      }
    }

    notifications.error(errorMessage);

    // Не закрываем модальное окно при ошибке, чтобы пользователь мог повторить попытку
  } finally {
    isLoading.value = false;
  }
};
```

---

### Обновлённый template с индикатором прогресса

```vue
<template>
  <div v-if="isUserAdmin" class="create-snapshot-button-container">
    <!-- Кнопка создания слепка -->
    <button
      class="create-snapshot-btn"
      @click="openModal"
      :disabled="isLoading"
      title="Создать слепок состояния сектора"
    >
      <span class="btn-icon">📸</span>
      <span class="btn-text">Создать слепок</span>
    </button>

    <!-- Модальное окно подтверждения -->
    <Teleport to="body">
      <Transition name="modal">
        <div
          v-if="isModalOpen"
          class="modal-overlay"
          @click.self="!isLoading && closeModal()"
        >
          <div class="modal-content">
            <div class="modal-header">
              <h3 class="modal-title">Создать слепок состояния сектора</h3>
              <button
                class="modal-close"
                @click="closeModal"
                :disabled="isLoading"
                aria-label="Закрыть"
              >
                ✕
              </button>
            </div>

            <div class="modal-body">
              <!-- Индикатор прогресса -->
              <div v-if="loadingStep !== 'idle'" class="progress-container">
                <div class="progress-bar-wrapper">
                  <div
                    class="progress-bar"
                    :style="{ width: `${loadingProgress}%` }"
                  ></div>
                </div>
                <div class="progress-text">
                  <span class="progress-percent">{{ Math.round(loadingProgress) }}%</span>
                  <span class="progress-description">{{ loadingDescription }}</span>
                </div>
              </div>

              <!-- Описание (показывается только когда нет загрузки) -->
              <div v-else>
                <p class="modal-description">
                  Будет создан слепок текущего состояния сектора 1С.
                  Слепок будет сохранён с типом "manual" (ручной).
                </p>
                <p class="modal-warning">
                  ⚠️ Процесс создания слепка может занять некоторое время.
                </p>
              </div>
            </div>

            <div class="modal-footer">
              <button
                class="btn btn-secondary"
                @click="closeModal"
                :disabled="isLoading"
              >
                Отмена
              </button>
              <button
                class="btn btn-primary"
                @click="handleCreate"
                :disabled="isLoading"
              >
                <span v-if="isLoading">
                  <span v-if="loadingStep === 'loading_data'">Загрузка данных...</span>
                  <span v-else-if="loadingStep === 'creating_snapshot'">Создание...</span>
                  <span v-else>Обработка...</span>
                </span>
                <span v-else>Создать</span>
              </button>
            </div>
          </div>
        </div>
      </Transition>
    </Teleport>
  </div>
</template>
```

---

### Стили для индикатора прогресса

```css
/* Индикатор прогресса */
.progress-container {
  margin: 20px 0;
}

.progress-bar-wrapper {
  width: 100%;
  height: 8px;
  background-color: #e0e0e0;
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 12px;
}

.progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #007bff, #0056b3);
  border-radius: 4px;
  transition: width 0.3s ease;
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.8;
  }
}

.progress-text {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 14px;
}

.progress-percent {
  font-weight: 600;
  color: #007bff;
}

.progress-description {
  color: #666;
  flex: 1;
  text-align: right;
  margin-left: 12px;
}
```

---

## ✅ Критерии приёмки

- [ ] Компонент интегрирован с `SectorDataAdapter.getSectorDataForSnapshot()`
- [ ] Компонент интегрирован с `SnapshotService.createSnapshot()`
- [ ] Индикатор прогресса отображается во время загрузки данных
- [ ] Индикатор прогресса отображается во время создания слепка
- [ ] Прогресс обновляется в реальном времени через колбэк `onProgress`
- [ ] Обработка ошибок реализована для всех типов ошибок
- [ ] Детальные сообщения об ошибках показываются пользователю
- [ ] Уведомления об успехе/ошибке работают через `useNotifications`
- [ ] Событие `snapshot-created` эмитится при успешном создании
- [ ] Модальное окно закрывается после успешного создания (с задержкой)
- [ ] Модальное окно не закрывается при ошибке (для повторной попытки)
- [ ] Кнопки блокируются во время создания слепка
- [ ] Состояние прогресса сбрасывается после закрытия модального окна
- [ ] Код соответствует стандартам проекта (ESLint, форматирование)
- [ ] Комментарии добавлены к основным блокам кода
- [ ] Компонент протестирован (успешное создание, ошибки, отмена)

---

## 📝 Действия

1. **Интегрировать SectorDataAdapter:**
   - Импортировать `SectorDataAdapter` в компонент
   - Вызвать `getSectorDataForSnapshot()` в `handleCreate()`
   - Передать колбэк прогресса для обновления индикатора
   - Обработать ошибки загрузки данных

2. **Интегрировать SnapshotService:**
   - Импортировать `SnapshotService` в компонент
   - Вызвать `createSnapshot()` после загрузки данных
   - Передать нормализованные данные, тип `'manual'` и метаданные
   - Обработать ошибки создания слепка

3. **Добавить индикатор прогресса:**
   - Добавить состояние прогресса (loadingProgress, loadingStep, loadingDescription)
   - Добавить компонент прогресс-бара в template
   - Обновлять прогресс через колбэк `onProgress`
   - Добавить стили для прогресс-бара

4. **Реализовать обработку ошибок:**
   - Добавить try/catch для всех асинхронных операций
   - Создать детальные сообщения об ошибках
   - Показывать уведомления об ошибках через `useNotifications`
   - Логировать ошибки в консоль

5. **Добавить уведомления:**
   - Показывать уведомление об успехе после создания слепка
   - Показывать уведомление об ошибке при ошибках
   - Использовать соответствующие типы уведомлений (success, error)

6. **Протестировать:**
   - Проверить успешное создание слепка
   - Проверить обработку ошибок (сеть, сервер, валидация)
   - Проверить отображение прогресса
   - Проверить уведомления
   - Проверить закрытие модального окна

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущие задачи:**
  - `DOCS/TASKS/TASK-019-05-01.md` (базовый компонент)
  - `DOCS/TASKS/TASK-019-03-01.md` (SectorDataAdapter)
  - `DOCS/TASKS/TASK-019-03-02.md` (нормализация данных)
  - `DOCS/TASKS/TASK-019-02-01.md` (SnapshotService)
- **Следующая задача:** `DOCS/TASKS/TASK-019-06-01.md` (компонент графика состояния)
- **Существующие файлы:**
  - `vue-app/src/services/graph-state/SectorDataAdapter.js`
  - `vue-app/src/services/graph-state/SnapshotService.js`
  - `vue-app/src/composables/useNotifications.js`
- **Обновляемые файлы:**
  - `vue-app/src/components/graph-state/CreateSnapshotButton.vue`

---

## ⏱️ Оценка времени

**2-3 часа**

**Разбивка:**
- Интеграция с SectorDataAdapter: 30-45 минут
- Интеграция с SnapshotService: 30-45 минут
- Добавление индикатора прогресса: 30-45 минут
- Реализация обработки ошибок: 30-45 минут
- Тестирование и отладка: 30-45 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-06-01: Разработка компонента графика состояния (GraphStateChart.vue)**.

---

## 💡 Дополнительные замечания

### Обработка ошибок

Рекомендуется:
- Различать типы ошибок (сеть, сервер, валидация)
- Показывать понятные сообщения пользователю
- Логировать детальную информацию в консоль для отладки
- Не закрывать модальное окно при ошибке (для повторной попытки)

### Производительность

Оптимизации:
- Использовать кеш при загрузке данных сектора (по умолчанию)
- Показывать прогресс для долгих операций
- Блокировать повторные запросы во время создания

### UX

Улучшения:
- Показывать понятные описания этапов загрузки
- Анимировать прогресс-бар
- Давать возможность отменить операцию (опционально)

---

## История правок

- **2025-12-08 11:00 (UTC+3, Брест):** Создана задача
- **2025-12-08 11:27 (UTC+3, Брест):** Задача выполнена
- **2025-12-08 13:54 (UTC+3, Брест):** Задача закрыта как успешная (все задачи TASK-019 завершены)

---

## ✅ Выполненные действия

**Дата:** 2025-12-08 11:27 (UTC+3, Брест)

**Выполнено:**
- ✅ Интегрирован `SectorDataAdapter` в компонент `CreateSnapshotButton`
- ✅ Реализован вызов `SectorDataAdapter.getSectorDataForSnapshot()` с колбэком прогресса
- ✅ Интегрирован `SnapshotService` в компонент
- ✅ Реализован вызов `SnapshotService.createSnapshot()` с типом `'manual'` и метаданными
- ✅ Добавлено состояние прогресса (`loadingProgress`, `loadingStep`, `loadingDescription`)
- ✅ Добавлен индикатор прогресса в модальное окно:
  - Прогресс-бар с анимацией pulse
  - Отображение процентов выполнения
  - Текстовое описание текущего этапа
- ✅ Реализовано обновление прогресса в реальном времени через колбэк `onProgress`:
  - 0-80% для загрузки данных сектора
  - 80-100% для создания слепка
- ✅ Реализована обработка ошибок для всех типов:
  - Ошибки загрузки данных сектора
  - Ошибки создания слепка (сеть, сервер, валидация)
  - Ошибки доступа (не администратор)
- ✅ Добавлены детальные сообщения об ошибках для пользователя
- ✅ Реализованы уведомления через `useNotifications`:
  - Успех: "Слепок состояния сектора успешно создан"
  - Ошибка: детальное сообщение в зависимости от типа ошибки
- ✅ Реализован эмит события `snapshot-created` при успешном создании
- ✅ Модальное окно закрывается после успешного создания (с задержкой 1.5 секунды)
- ✅ Модальное окно не закрывается при ошибке (для повторной попытки)
- ✅ Кнопки блокируются во время создания слепка
- ✅ Состояние прогресса сбрасывается при закрытии модального окна
- ✅ Добавлены стили для прогресс-бара с анимацией
- ✅ Обновлён текст кнопки в зависимости от этапа загрузки
- ✅ Добавлены комментарии к основным блокам кода
- ✅ Код проверен линтером — ошибок не найдено

**Результат:** Компонент CreateSnapshotButton полностью интегрирован с сервисами, добавлен индикатор прогресса и обработка ошибок. Все критерии приёмки выполнены.

---

**Автор:** Технический писатель  
**Версия документа:** 1.1

