# TASK-059-05: Реализация файлового кеша для режима 3 месяца

**Дата создания:** 2025-12-18 10:30 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Backend Developer (PHP)  
**Родительская задача:** [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)  
**Подзадача:** Подзадача 5 из TASK-059

---

## 📋 Описание

Реализовать файловый кеш для результатов API endpoint `graph-1c-admission-closure.php` в режиме "3 месяца". Кеш должен сократить время загрузки с 15-30 секунд до 0.5-2 секунд для повторных запросов.

**Цель:** Добавить файловый кеш с TTL 5 минут для результатов запросов в режиме "3 месяца".

---

## 🎯 Контекст

### Текущая проблема:

- Данные загружаются заново при каждом запросе
- Время загрузки: 15-30 секунд
- Нет механизма кеширования результатов

### Решение:

- Файловый кеш с TTL 5 минут
- Автоматическая очистка устаревших кешей
- Инвалидация кеша при изменении параметров запроса

---

## 🏗️ Модули и компоненты

### Новые файлы:
- `api/cache/GraphAdmissionClosureCache.php` — класс для работы с файловым кешем
- `api/cache/.gitkeep` — файл для создания директории (если нужно)

### Изменяемые файлы:
- `api/graph-1c-admission-closure.php` — интеграция кеша в API endpoint

### Структура директорий:
```
api/
├── cache/
│   ├── graph-admission-closure/
│   │   ├── months/
│   │   │   ├── {cache-key-1}.json
│   │   │   ├── {cache-key-2}.json
│   │   │   └── ...
│   │   └── .gitkeep
│   └── .gitkeep
└── graph-1c-admission-closure.php
```

---

## 📦 Зависимости

- **От задач:** 
  - TASK-059: Оптимизация загрузки режима 3 месяца
- **От модулей:** 
  - Модуль 3: График приёма и закрытий сектора 1С
- **От библиотек:** 
  - PHP 8.4+ (встроенные функции для работы с файлами)

---

## 🎯 Ступенчатые подзадачи

### Подзадача 5.1: Создание класса для работы с файловым кешем

**Цель:** Создать класс `GraphAdmissionClosureCache` для управления файловым кешем.

**Шаги:**

1. **Создать директорию для кеша**
   - Создать `api/cache/graph-admission-closure/months/`
   - Убедиться, что директория доступна для записи (права 755)
   - Добавить `.gitkeep` для сохранения структуры в Git

2. **Создать класс `GraphAdmissionClosureCache`**
   - Файл: `api/cache/GraphAdmissionClosureCache.php`
   - Методы:
     - `get(string $key): ?array` — получение данных из кеша
     - `set(string $key, array $data, int $ttl = 300): bool` — сохранение данных в кеш
     - `delete(string $key): bool` — удаление записи из кеша
     - `clear(): bool` — очистка всего кеша
     - `clearExpired(): int` — очистка устаревших записей
     - `getCachePath(string $key): string` — получение пути к файлу кеша
     - `generateKey(array $params): string` — генерация ключа кеша
     - `isExpired(string $key): bool` — проверка истечения срока действия

3. **Реализовать структуру данных кеша**
   ```php
   [
       'data' => [...],           // Данные ответа API
       'metadata' => [
           'created_at' => 1234567890,  // Unix timestamp
           'expires_at' => 1234567890,  // Unix timestamp
           'ttl' => 300,                // TTL в секундах
           'params' => [...]            // Параметры запроса (для отладки)
       ]
   ]
   ```

4. **Добавить обработку ошибок**
   - Проверка прав доступа к директории
   - Обработка ошибок записи/чтения файлов
   - Логирование ошибок

**Критерии приёмки:**
- [ ] Класс `GraphAdmissionClosureCache` создан
- [ ] Все методы реализованы и протестированы
- [ ] Обработка ошибок добавлена
- [ ] Логирование ошибок реализовано

**Пример кода:**

```php
<?php
/**
 * Файловый кеш для API endpoint graph-1c-admission-closure.php
 * 
 * Расположение: api/cache/GraphAdmissionClosureCache.php
 * 
 * Управляет кешированием результатов запросов в режиме "3 месяца"
 */

class GraphAdmissionClosureCache
{
    /**
     * Базовый путь к директории кеша
     */
    private const CACHE_BASE_DIR = __DIR__ . '/cache/graph-admission-closure';
    
    /**
     * Поддиректория для режима "months"
     */
    private const CACHE_MONTHS_DIR = self::CACHE_BASE_DIR . '/months';
    
    /**
     * TTL по умолчанию (5 минут)
     */
    private const DEFAULT_TTL = 300;
    
    /**
     * Получить данные из кеша
     * 
     * @param string $key Ключ кеша
     * @return array|null Данные или null, если кеш не найден/истёк
     */
    public static function get(string $key): ?array
    {
        $cachePath = self::getCachePath($key);
        
        // Проверка существования файла
        if (!file_exists($cachePath)) {
            return null;
        }
        
        // Чтение файла
        $content = @file_get_contents($cachePath);
        if ($content === false) {
            error_log("[Cache] Failed to read cache file: {$cachePath}");
            return null;
        }
        
        // Декодирование JSON
        $data = @json_decode($content, true);
        if ($data === null || !is_array($data)) {
            error_log("[Cache] Failed to decode cache file: {$cachePath}");
            return null;
        }
        
        // Проверка структуры данных
        if (!isset($data['metadata']['expires_at'])) {
            error_log("[Cache] Invalid cache structure: {$cachePath}");
            return null;
        }
        
        // Проверка истечения срока действия
        if (time() > $data['metadata']['expires_at']) {
            // Кеш истёк, удаляем файл
            @unlink($cachePath);
            return null;
        }
        
        return $data['data'] ?? null;
    }
    
    /**
     * Сохранить данные в кеш
     * 
     * @param string $key Ключ кеша
     * @param array $data Данные для кеширования
     * @param int $ttl TTL в секундах (по умолчанию 5 минут)
     * @return bool true если успешно
     */
    public static function set(string $key, array $data, int $ttl = self::DEFAULT_TTL): bool
    {
        // Создание директории, если не существует
        if (!self::ensureCacheDirectory()) {
            return false;
        }
        
        $cachePath = self::getCachePath($key);
        $now = time();
        
        // Формирование структуры данных
        $cacheData = [
            'data' => $data,
            'metadata' => [
                'created_at' => $now,
                'expires_at' => $now + $ttl,
                'ttl' => $ttl
            ]
        ];
        
        // Кодирование в JSON
        $json = json_encode($cacheData, JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT);
        if ($json === false) {
            error_log("[Cache] Failed to encode cache data for key: {$key}");
            return false;
        }
        
        // Запись в файл (атомарная операция через временный файл)
        $tempPath = $cachePath . '.tmp';
        if (@file_put_contents($tempPath, $json, LOCK_EX) === false) {
            error_log("[Cache] Failed to write cache file: {$tempPath}");
            return false;
        }
        
        // Атомарное переименование
        if (!@rename($tempPath, $cachePath)) {
            error_log("[Cache] Failed to rename cache file: {$tempPath} -> {$cachePath}");
            @unlink($tempPath);
            return false;
        }
        
        return true;
    }
    
    /**
     * Удалить запись из кеша
     * 
     * @param string $key Ключ кеша
     * @return bool true если успешно
     */
    public static function delete(string $key): bool
    {
        $cachePath = self::getCachePath($key);
        
        if (file_exists($cachePath)) {
            return @unlink($cachePath);
        }
        
        return true; // Файл уже не существует
    }
    
    /**
     * Очистить весь кеш
     * 
     * @return bool true если успешно
     */
    public static function clear(): bool
    {
        $cacheDir = self::CACHE_MONTHS_DIR;
        
        if (!is_dir($cacheDir)) {
            return true; // Директория не существует
        }
        
        $files = glob($cacheDir . '/*.json');
        $success = true;
        
        foreach ($files as $file) {
            if (!@unlink($file)) {
                error_log("[Cache] Failed to delete cache file: {$file}");
                $success = false;
            }
        }
        
        return $success;
    }
    
    /**
     * Очистить устаревшие записи
     * 
     * @return int Количество удалённых файлов
     */
    public static function clearExpired(): int
    {
        $cacheDir = self::CACHE_MONTHS_DIR;
        
        if (!is_dir($cacheDir)) {
            return 0;
        }
        
        $files = glob($cacheDir . '/*.json');
        $deleted = 0;
        $now = time();
        
        foreach ($files as $file) {
            $content = @file_get_contents($file);
            if ($content === false) {
                continue;
            }
            
            $data = @json_decode($content, true);
            if ($data === null || !is_array($data)) {
                continue;
            }
            
            $expiresAt = $data['metadata']['expires_at'] ?? 0;
            if ($now > $expiresAt) {
                if (@unlink($file)) {
                    $deleted++;
                }
            }
        }
        
        return $deleted;
    }
    
    /**
     * Получить путь к файлу кеша
     * 
     * @param string $key Ключ кеша
     * @return string Полный путь к файлу
     */
    private static function getCachePath(string $key): string
    {
        // Безопасное имя файла (только буквы, цифры, дефисы, подчёркивания)
        $safeKey = preg_replace('/[^a-zA-Z0-9_-]/', '_', $key);
        
        return self::CACHE_MONTHS_DIR . '/' . $safeKey . '.json';
    }
    
    /**
     * Генерация ключа кеша на основе параметров запроса
     * 
     * @param array $params Параметры запроса
     * @return string Ключ кеша
     */
    public static function generateKey(array $params): string
    {
        // Нормализация параметров для генерации ключа
        $normalized = [
            'product' => $params['product'] ?? '1C',
            'periodMode' => $params['periodMode'] ?? 'months',
            'includeTickets' => $params['includeTickets'] ?? false,
            'includeNewTicketsByStages' => $params['includeNewTicketsByStages'] ?? false,
            'includeCarryoverTickets' => $params['includeCarryoverTickets'] ?? true,
            'includeCarryoverTicketsByDuration' => $params['includeCarryoverTicketsByDuration'] ?? false
        ];
        
        // Для режима "months" период определяется автоматически (последние 3 месяца)
        // Поэтому не включаем weekStartUtc/weekEndUtc в ключ
        
        // Генерация MD5 хеша от нормализованных параметров
        $keyString = json_encode($normalized, JSON_UNESCAPED_UNICODE);
        $hash = md5($keyString);
        
        // Добавляем префикс для читаемости
        return 'months_' . $hash;
    }
    
    /**
     * Проверка истечения срока действия кеша
     * 
     * @param string $key Ключ кеша
     * @return bool true если кеш истёк или не существует
     */
    public static function isExpired(string $key): bool
    {
        $cachePath = self::getCachePath($key);
        
        if (!file_exists($cachePath)) {
            return true;
        }
        
        $content = @file_get_contents($cachePath);
        if ($content === false) {
            return true;
        }
        
        $data = @json_decode($content, true);
        if ($data === null || !is_array($data)) {
            return true;
        }
        
        $expiresAt = $data['metadata']['expires_at'] ?? 0;
        return time() > $expiresAt;
    }
    
    /**
     * Убедиться, что директория кеша существует
     * 
     * @return bool true если директория существует или создана
     */
    private static function ensureCacheDirectory(): bool
    {
        $cacheDir = self::CACHE_MONTHS_DIR;
        
        if (is_dir($cacheDir)) {
            return is_writable($cacheDir);
        }
        
        // Создание директории рекурсивно
        if (!@mkdir($cacheDir, 0755, true)) {
            error_log("[Cache] Failed to create cache directory: {$cacheDir}");
            return false;
        }
        
        return true;
    }
}
```

---

### Подзадача 5.2: Интеграция кеша в API endpoint

**Цель:** Интегрировать файловый кеш в `api/graph-1c-admission-closure.php`.

**Шаги:**

1. **Подключить класс кеша**
   - Добавить `require_once` в начало файла
   - Проверить, что класс доступен

2. **Добавить проверку кеша перед выполнением запросов**
   - Генерация ключа кеша на основе параметров запроса
   - Проверка наличия данных в кеше
   - Возврат данных из кеша, если они актуальны

3. **Добавить сохранение результатов в кеш**
   - Сохранение результатов после выполнения запросов
   - Сохранение только для успешных запросов
   - Установка TTL 5 минут

4. **Добавить параметр для принудительного обновления кеша**
   - Параметр `forceRefresh` в запросе
   - Пропуск проверки кеша при `forceRefresh === true`

5. **Добавить очистку устаревших кешей**
   - Периодическая очистка (например, при каждом 10-м запросе)
   - Или через cron job (опционально)

**Критерии приёмки:**
- [ ] Кеш интегрирован в API endpoint
- [ ] Проверка кеша работает корректно
- [ ] Сохранение в кеш работает корректно
- [ ] Параметр `forceRefresh` работает
- [ ] Очистка устаревших кешей работает

**Пример кода:**

```php
<?php
// В начале файла api/graph-1c-admission-closure.php

require_once __DIR__ . '/../crest.php';
require_once __DIR__ . '/cache/GraphAdmissionClosureCache.php';

// ... существующий код ...

try {
    $body = parseJsonBody();
    
    $product = isset($body['product']) ? (string)$body['product'] : '1C';
    $periodMode = isset($body['periodMode']) ? (string)$body['periodMode'] : 'weeks';
    $forceRefresh = isset($body['forceRefresh']) ? (bool)$body['forceRefresh'] : false;
    // ... другие параметры ...
    
    // TASK-059-05: Проверка кеша для режима "months"
    if ($periodMode === 'months' && !$forceRefresh) {
        $cacheKey = GraphAdmissionClosureCache::generateKey([
            'product' => $product,
            'periodMode' => $periodMode,
            'includeTickets' => $includeTickets,
            'includeNewTicketsByStages' => $includeNewTicketsByStages,
            'includeCarryoverTickets' => $includeCarryoverTickets,
            'includeCarryoverTicketsByDuration' => $includeCarryoverTicketsByDuration
        ]);
        
        $cachedData = GraphAdmissionClosureCache::get($cacheKey);
        if ($cachedData !== null) {
            error_log("[Cache] Cache hit for key: {$cacheKey}");
            jsonResponse($cachedData);
        }
        
        error_log("[Cache] Cache miss for key: {$cacheKey}");
    }
    
    // ... выполнение запросов к Bitrix24 API ...
    
    // Формирование ответа
    $response = [
        'success' => true,
        'meta' => [...],
        'data' => [...]
    ];
    
    // TASK-059-05: Сохранение в кеш для режима "months"
    if ($periodMode === 'months') {
        $cacheKey = GraphAdmissionClosureCache::generateKey([
            'product' => $product,
            'periodMode' => $periodMode,
            'includeTickets' => $includeTickets,
            'includeNewTicketsByStages' => $includeNewTicketsByStages,
            'includeCarryoverTickets' => $includeCarryoverTickets,
            'includeCarryoverTicketsByDuration' => $includeCarryoverTicketsByDuration
        ]);
        
        if (GraphAdmissionClosureCache::set($cacheKey, $response, 300)) {
            error_log("[Cache] Cache saved for key: {$cacheKey}");
        } else {
            error_log("[Cache] Failed to save cache for key: {$cacheKey}");
        }
        
        // Периодическая очистка устаревших кешей (каждый 10-й запрос)
        if (rand(1, 10) === 1) {
            $deleted = GraphAdmissionClosureCache::clearExpired();
            if ($deleted > 0) {
                error_log("[Cache] Cleared {$deleted} expired cache entries");
            }
        }
    }
    
    jsonResponse($response);
} catch (Exception $e) {
    // ... обработка ошибок ...
}
```

---

### Подзадача 5.3: Создание директории кеша и настройка прав доступа

**Цель:** Создать директорию для кеша и настроить права доступа.

**Шаги:**

1. **Создать директорию кеша**
   - Создать `api/cache/graph-admission-closure/months/`
   - Установить права доступа 755
   - Добавить `.gitkeep` для сохранения структуры в Git

2. **Проверить права доступа**
   - Убедиться, что веб-сервер может создавать/удалять файлы
   - Проверить, что директория доступна для записи

3. **Добавить директорию в .gitignore (если нужно)**
   - Игнорировать файлы кеша `*.json`
   - Сохранить структуру директорий через `.gitkeep`

**Критерии приёмки:**
- [ ] Директория кеша создана
- [ ] Права доступа настроены корректно
- [ ] Веб-сервер может создавать/удалять файлы
- [ ] `.gitkeep` добавлен для сохранения структуры

**Команды:**

```bash
# Создание директории
mkdir -p api/cache/graph-admission-closure/months

# Установка прав доступа
chmod 755 api/cache/graph-admission-closure/months

# Создание .gitkeep
touch api/cache/graph-admission-closure/months/.gitkeep
touch api/cache/graph-admission-closure/.gitkeep
touch api/cache/.gitkeep
```

---

### Подзадача 5.4: Тестирование файлового кеша

**Цель:** Протестировать работу файлового кеша.

**Шаги:**

1. **Тестирование базовой функциональности**
   - Проверить сохранение данных в кеш
   - Проверить получение данных из кеша
   - Проверить удаление данных из кеша

2. **Тестирование TTL**
   - Проверить истечение срока действия кеша
   - Проверить автоматическое удаление устаревших записей

3. **Тестирование интеграции с API**
   - Проверить работу кеша в режиме "months"
   - Проверить параметр `forceRefresh`
   - Проверить время загрузки с кешем и без кеша

4. **Тестирование обработки ошибок**
   - Проверить обработку ошибок записи/чтения
   - Проверить обработку отсутствия прав доступа
   - Проверить обработку повреждённых файлов кеша

**Критерии приёмки:**
- [ ] Все тесты проходят успешно
- [ ] Время загрузки с кешем: 0.5-2 секунды
- [ ] Время загрузки без кеша: 15-30 секунд (не изменилось)
- [ ] Обработка ошибок работает корректно

**Тестовые сценарии:**

```php
<?php
// Тест 1: Сохранение и получение данных
$key = 'test_key';
$data = ['test' => 'data'];
GraphAdmissionClosureCache::set($key, $data, 60);
$cached = GraphAdmissionClosureCache::get($key);
assert($cached === $data, 'Cache get/set failed');

// Тест 2: Истечение срока действия
$key = 'test_key_expired';
$data = ['test' => 'data'];
GraphAdmissionClosureCache::set($key, $data, 1); // TTL 1 секунда
sleep(2);
$cached = GraphAdmissionClosureCache::get($key);
assert($cached === null, 'Expired cache should return null');

// Тест 3: Очистка устаревших записей
$deleted = GraphAdmissionClosureCache::clearExpired();
assert($deleted >= 0, 'clearExpired should return count');

// Тест 4: Генерация ключа
$key1 = GraphAdmissionClosureCache::generateKey(['product' => '1C', 'periodMode' => 'months']);
$key2 = GraphAdmissionClosureCache::generateKey(['product' => '1C', 'periodMode' => 'months']);
assert($key1 === $key2, 'Same params should generate same key');
```

---

### Подзадача 5.5: Документирование файлового кеша

**Цель:** Документировать реализацию файлового кеша.

**Шаги:**

1. **Документировать класс `GraphAdmissionClosureCache`**
   - Описать все методы
   - Добавить примеры использования
   - Описать структуру данных кеша

2. **Документировать интеграцию в API**
   - Описать параметры запроса
   - Описать поведение кеша
   - Описать очистку устаревших кешей

3. **Добавить комментарии в код**
   - PHPDoc для всех методов
   - Комментарии для сложных участков кода

**Критерии приёмки:**
- [ ] Класс полностью задокументирован
- [ ] Интеграция в API задокументирована
- [ ] Примеры использования добавлены
- [ ] Комментарии в коде добавлены

---

## 🔧 Технические требования

### Backend (PHP):
- PHP 8.4+
- Права доступа к директории кеша (755)
- Права на запись файлов для веб-сервера

### Структура данных кеша:
```php
[
    'data' => [
        'success' => true,
        'meta' => [...],
        'data' => [...]
    ],
    'metadata' => [
        'created_at' => 1234567890,
        'expires_at' => 1234567890,
        'ttl' => 300
    ]
]
```

### TTL:
- **По умолчанию:** 5 минут (300 секунд)
- **Настраиваемый:** через параметр метода `set()`

---

## ✅ Критерии приёмки

### Общие критерии:
- [ ] Файловый кеш реализован и работает
- [ ] Время загрузки с кешем: 0.5-2 секунды
- [ ] Время загрузки без кеша: 15-30 секунд (не изменилось)
- [ ] TTL работает корректно (5 минут)
- [ ] Очистка устаревших кешей работает
- [ ] Параметр `forceRefresh` работает
- [ ] Обработка ошибок реализована
- [ ] Логирование добавлено для всех операций
- [ ] Код соответствует стандартам PSR-12
- [ ] Документация добавлена

### Критерии для каждой подзадачи:
- См. раздел "Ступенчатые подзадачи"

---

## 🧪 Тестирование

### Функциональное тестирование:
1. Проверить сохранение данных в кеш
2. Проверить получение данных из кеша
3. Проверить истечение срока действия кеша
4. Проверить очистку устаревших кешей
5. Проверить параметр `forceRefresh`

### Производительность:
1. Измерить время загрузки с кешем (первый запрос)
2. Измерить время загрузки с кешем (повторный запрос)
3. Измерить время загрузки без кеша
4. Сравнить результаты

### Нагрузочное тестирование:
1. Проверить работу кеша при множественных запросах
2. Проверить очистку устаревших кешей при большой нагрузке

---

## 📝 Примеры использования

### Использование кеша в API:

```php
// Проверка кеша
$cacheKey = GraphAdmissionClosureCache::generateKey($params);
$cachedData = GraphAdmissionClosureCache::get($cacheKey);
if ($cachedData !== null) {
    return $cachedData; // Возврат из кеша
}

// Выполнение запросов к Bitrix24 API
$response = performBitrix24Queries();

// Сохранение в кеш
GraphAdmissionClosureCache::set($cacheKey, $response, 300);

return $response;
```

### Очистка кеша:

```php
// Очистка конкретной записи
GraphAdmissionClosureCache::delete($cacheKey);

// Очистка всех устаревших записей
$deleted = GraphAdmissionClosureCache::clearExpired();

// Очистка всего кеша
GraphAdmissionClosureCache::clear();
```

---

## 🔗 Связанные документы

- [TASK-059: Оптимизация загрузки режима 3 месяца](./TASK-059-optimization-3-months-mode-loading.md)
- [Анализ оптимизации](../../REFACTOR/PERFORMANCE/optimization-3-months-mode-loading.md)
- [Модуль 3: График приёма и закрытий сектора 1С](../../MODULES/module-03-graph-admission-closure/README.md)

---

## 📊 История правок

- **2025-12-18 10:30 (UTC+3, Брест):** Создана подзадача для реализации файлового кеша

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

