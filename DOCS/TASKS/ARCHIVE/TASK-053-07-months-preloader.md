# TASK-053-07: Прелоадер с глубокой детализацией для 3-месячного режима

**Дата создания:** 2025-12-17 14:00 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-053: Изменение формата выбора периода и внедрение 3-месячного режима](./TASK-053-period-selection-3-months-mode.md)  
**Этап:** 2.5 (Часть 2: Внедрение 3-месячного режима)

---

## 📋 Описание

Создать компонент прелоадера с глубокой детализацией процесса загрузки для 3-месячного режима. Прелоадер должен показывать этапы загрузки данных по месяцам, прогресс-бар с процентами и информативные сообщения для пользователя.

---

## 🎯 Контекст

### Текущее состояние

**Текущий прелоадер:**
- Простой спиннер с сообщением "Загрузка графика"
- Нет детализации процесса загрузки
- Нет прогресс-бара

**Проблема:**
- Загрузка данных за 3 месяца может быть долгой
- Пользователь не видит прогресс загрузки
- Нет информации о том, что происходит

### Требуемое состояние

**Новый прелоадер:**
- Глубокая детализация процесса загрузки
- Прогресс-бар с процентами
- Этапы загрузки (по месяцам)
- Информативные сообщения

---

## 🔧 Технические требования

### Компонент: MonthsModePreloader.vue

**Расположение:** `vue-app/src/components/graph-admission-closure/MonthsModePreloader.vue`

**Props:**
```typescript
{
  progress: number,  // Прогресс загрузки (0-100)
  currentStage: string  // Текущий этап загрузки
}
```

**Функциональность:**
- Отображение спиннера (анимация загрузки)
- Прогресс-бар с процентами
- Текущий этап (текст)
- Список этапов с отметками (✓ для завершённых)

**Этапы загрузки:**
1. "Подготовка данных..." (0-10%)
2. "Загрузка данных за октябрь..." (10-40%)
3. "Загрузка данных за ноябрь..." (40-70%)
4. "Загрузка данных за декабрь..." (70-90%)
5. "Агрегация данных..." (90-95%)
6. "Формирование графиков..." (95-100%)

**Визуальный дизайн:**
- Соответствие гайдлайнам Bitrix24 UI
- Анимация спиннера
- Плавное обновление прогресс-бара
- Адаптивность (работа на мобильных устройствах)

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Создание компонента MonthsModePreloader.vue

1. **Создать файл компонента:**
   ```bash
   touch vue-app/src/components/graph-admission-closure/MonthsModePreloader.vue
   ```

2. **Реализовать структуру компонента:**
   - Template с прелоадером
   - Script setup с props
   - Стили в соответствии с гайдлайнами Bitrix24

3. **Реализовать спиннер:**
   - Анимация загрузки
   - Визуально приятный дизайн

4. **Реализовать прогресс-бар:**
   - Отображение процентов
   - Плавное обновление

5. **Реализовать список этапов:**
   - Отметки для завершённых этапов
   - Выделение текущего этапа

### Шаг 2: Интеграция в GraphAdmissionClosureMonthsDashboard.vue

1. **Импортировать компонент:**
   ```javascript
   import MonthsModePreloader from './MonthsModePreloader.vue';
   ```

2. **Добавить состояние для прогресса:**
   ```javascript
   const loadingProgress = ref(0);
   const currentLoadingStage = ref('');
   ```

3. **Обновлять прогресс при загрузке:**
   - Симулировать прогресс или получать от бэкенда
   - Обновлять этапы по мере загрузки

---

## 🔍 Детали реализации

### Симуляция прогресса

**Подход 1: Симуляция на фронтенде**
- Обновлять прогресс по таймеру
- Симулировать этапы загрузки
- Плюсы: работает всегда, не зависит от бэкенда
- Минусы: может не соответствовать реальному прогрессу

**Подход 2: Получение от бэкенда (если реализовано)**
- Бэкенд отправляет события прогресса через WebSocket или Server-Sent Events
- Обновлять прогресс в реальном времени
- Плюсы: точный прогресс
- Минусы: требует реализации на бэкенде

**Рекомендация:**
- Начать с симуляции на фронтенде
- В будущем можно добавить получение от бэкенда

### Этапы загрузки

**Определение текущего этапа:**
- Сравнивать `progress` с порогами этапов
- Отмечать завершённые этапы (✓)
- Выделять текущий этап (→)

**Обновление этапов:**
- Плавное обновление прогресс-бара (transition)
- Анимация изменения этапов
- Визуальная обратная связь

### Обработка ошибок

**Ошибки загрузки:**
- Показывать сообщение об ошибке вместо прелоадера
- Не скрывать прелоадер резко при ошибке
- Давать возможность повторить загрузку

**Таймауты:**
- Устанавливать таймаут для загрузки (например, 60 секунд)
- Показывать предупреждение при долгой загрузке
- Предлагать отменить загрузку

### Производительность

**Оптимизация анимаций:**
- Использовать CSS transitions вместо JavaScript
- Использовать `will-change` для оптимизации
- Избегать лишних перерисовок

**Память:**
- Очищать таймеры при размонтировании
- Не накапливать события прогресса

## 💻 Примеры кода

### MonthsModePreloader.vue

```vue
<template>
  <div class="months-preloader">
    <div class="preloader-content">
      <div class="preloader-spinner">
        <div class="spinner-ring"></div>
        <div class="spinner-ring"></div>
        <div class="spinner-ring"></div>
      </div>
      
      <h3 class="preloader-title">Загрузка данных за 3 месяца</h3>
      
      <div class="progress-container">
        <div class="progress-bar">
          <div
            class="progress-fill"
            :style="{ width: `${progress}%` }"
          ></div>
        </div>
        <div class="progress-text">{{ progress }}%</div>
      </div>
      
      <p class="preloader-message">{{ currentStage }}</p>
      
      <div class="stages-list">
        <div
          v-for="(stage, index) in stages"
          :key="index"
          class="stage-item"
          :class="{
            'stage-completed': isStageCompleted(index),
            'stage-current': isStageCurrent(index)
          }"
        >
          <span class="stage-icon">
            {{ isStageCompleted(index) ? '✓' : isStageCurrent(index) ? '→' : '○' }}
          </span>
          <span class="stage-text">{{ stage.label }}</span>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
import { computed, onUnmounted } from 'vue';

const props = defineProps({
  progress: {
    type: Number,
    required: true,
    validator: (value) => value >= 0 && value <= 100
  },
  currentStage: {
    type: String,
    default: ''
  }
});

const stages = [
  { label: 'Подготовка данных...', threshold: 10 },
  { label: 'Загрузка данных за октябрь...', threshold: 40 },
  { label: 'Загрузка данных за ноябрь...', threshold: 70 },
  { label: 'Загрузка данных за декабрь...', threshold: 90 },
  { label: 'Агрегация данных...', threshold: 95 },
  { label: 'Формирование графиков...', threshold: 100 }
];

function isStageCompleted(index) {
  return props.progress >= stages[index].threshold;
}

function isStageCurrent(index) {
  const currentThreshold = stages[index].threshold;
  const prevThreshold = index > 0 ? stages[index - 1].threshold : 0;
  return props.progress >= prevThreshold && props.progress < currentThreshold;
}

// Очистка при размонтировании (если используются таймеры)
onUnmounted(() => {
  // Очистка таймеров, если есть
});
</script>

<style scoped>
.months-preloader {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  min-height: 70vh;
  padding: 60px 20px;
  background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
  border-radius: var(--radius-lg, 12px);
}

.preloader-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  max-width: 500px;
  width: 100%;
}

.preloader-spinner {
  position: relative;
  width: 80px;
  height: 80px;
  margin-bottom: 32px;
}

.spinner-ring {
  position: absolute;
  width: 100%;
  height: 100%;
  border: 4px solid transparent;
  border-top-color: var(--b24-primary, #007bff);
  border-radius: 50%;
  animation: spin 1.2s cubic-bezier(0.5, 0, 0.5, 1) infinite;
}

.spinner-ring:nth-child(1) {
  animation-delay: 0s;
  border-top-color: var(--b24-primary, #007bff);
  opacity: 1;
}

.spinner-ring:nth-child(2) {
  animation-delay: -0.4s;
  border-top-color: var(--b24-success, #28a745);
  opacity: 0.7;
  width: 70%;
  height: 70%;
  top: 15%;
  left: 15%;
}

.spinner-ring:nth-child(3) {
  animation-delay: -0.8s;
  border-top-color: #ff9800;
  opacity: 0.5;
  width: 50%;
  height: 50%;
  top: 25%;
  left: 25%;
}

@keyframes spin {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

.preloader-title {
  margin: 0 0 24px 0;
  font-size: 20px;
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
}

.progress-container {
  width: 100%;
  margin-bottom: 24px;
}

.progress-bar {
  width: 100%;
  height: 8px;
  background-color: var(--b24-bg-light, #f5f7fb);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 8px;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--b24-primary, #007bff) 0%, var(--b24-success, #28a745) 100%);
  border-radius: 4px;
  transition: width 0.3s ease;
}

.progress-text {
  font-size: var(--font-size-sm, 14px);
  font-weight: 600;
  color: var(--b24-text-primary, #111827);
  text-align: center;
}

.preloader-message {
  margin: 0 0 24px 0;
  font-size: var(--font-size-base, 14px);
  color: var(--b24-text-secondary, #6b7280);
  font-weight: 500;
}

.stages-list {
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs, 4px);
  text-align: left;
}

.stage-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm, 8px);
  font-size: var(--font-size-sm, 14px);
  padding: var(--spacing-xs, 4px) 0;
  color: var(--b24-text-secondary, #6b7280);
  transition: color 0.2s ease;
}

.stage-item.stage-completed {
  color: var(--b24-success, #28a745);
}

.stage-item.stage-current {
  color: var(--b24-primary, #007bff);
  font-weight: 600;
}

.stage-icon {
  font-size: 16px;
  font-weight: 600;
  width: 20px;
  text-align: center;
}

.stage-text {
  flex: 1;
}

/* Адаптивность */
@media (max-width: 768px) {
  .months-preloader {
    min-height: 400px;
    padding: 40px 20px;
  }
  
  .preloader-spinner {
    width: 60px;
    height: 60px;
    margin-bottom: 24px;
  }
  
  .preloader-title {
    font-size: 18px;
  }
}
</style>
```

---

## ✅ Критерии приёмки

- [ ] Компонент `MonthsModePreloader.vue` создан и работает корректно
- [ ] Прелоадер показывает глубокую детализацию загрузки
- [ ] Отображаются все этапы загрузки
- [ ] Прогресс-бар обновляется в реальном времени
- [ ] Сообщения информативны и понятны пользователю
- [ ] Спиннер анимируется плавно
- [ ] Этапы отмечаются корректно (✓ для завершённых, → для текущего)
- [ ] Компонент адаптивен (работает на мобильных устройствах)
- [ ] Компонент соответствует гайдлайнам Bitrix24 UI

---

## 🔗 Зависимости

**Зависит от:**
- Нет (это независимый компонент)

**Зависит от этого этапа:**
- [TASK-053-06: Создание отдельного шаблона](./TASK-053-06-months-dashboard-template.md) — использует компонент в дашборде

---

## 📝 История правок

- **2025-12-17 14:00 (UTC+3, Брест):** Создана подзадача TASK-053-07
  - Определены требования для прелоадера с глубокой детализацией
  - Зафиксированы этапы загрузки и примеры кода
  - Добавлены критерии приёмки

- **2025-12-17 14:50 (UTC+3, Брест):** Задача завершена
  - ✅ Создан компонент `MonthsModePreloader.vue`:
    - Отображение спиннера с анимацией (3 кольца)
    - Прогресс-бар с процентами (0-100%)
    - Текущий этап загрузки (текстовое сообщение)
    - Список этапов с отметками (✓ для завершённых, → для текущего, ○ для ожидающих)
    - Плавные переходы и анимации
  - ✅ Реализованы этапы загрузки:
    1. Подготовка данных... (0-10%)
    2. Загрузка данных за октябрь... (10-40%)
    3. Загрузка данных за ноябрь... (40-70%)
    4. Загрузка данных за декабрь... (70-90%)
    5. Агрегация данных... (90-95%)
    6. Формирование графиков... (95-100%)
  - ✅ Интегрирован в `GraphAdmissionClosureMonthsDashboard.vue`:
    - Заменён простой прелоадер на `MonthsModePreloader`
    - Добавлены состояния `loadingProgress` и `currentLoadingStage`
    - Реализована функция `updateProgress()` для плавного обновления прогресса
  - ✅ Реализована симуляция прогресса:
    - Плавное обновление прогресс-бара по шагам
    - Обновление этапов по мере загрузки
    - Синхронизация с реальным запросом к API
    - Обработка ошибок (прогресс до 100% для скрытия прелоадера)
  - ✅ Стилизация соответствует гайдлайнам Bitrix24:
    - Использованы CSS-переменные для цветов
    - Адаптивный дизайн для мобильных устройств
    - Плавные переходы и анимации
    - Градиентный прогресс-бар

---

## 💡 Примечания

- **Прогресс:** Можно симулировать прогресс на фронтенде или получать от бэкенда (если реализовано)
- **Этапы:** Этапы можно настроить в зависимости от реальной логики загрузки
- **UX:** Прелоадер должен быть информативным, но не перегруженным
- **Тестирование:** Протестировать обновление прогресса, отображение этапов, анимации

