# TASK-072: Реализация модуля "Управление пользователями" в разделе Администрирования

**Дата создания:** 2025-12-24 09:30 (UTC+3, Брест)  
**Статус:** Черновик (Draft)  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** Модуль "Администрирование" (TASK-004-02)

---

## 📋 Описание

Реализовать модуль **"Управление пользователями"** в разделе Администрирования приложения. Модуль должен предоставлять администраторам возможность управлять:

1. **Доступом к приложению** — какие отделы могут видеть стартовую страницу приложения
2. **Видимостью модулей** — какие 4 основных модуля (отчёты) отображать на стартовой странице и кому их показывать

**Цель:** Создать интерфейс для управления конфигурацией `access-config.js` (доступ к приложению) и `reports-config.js` (видимость модулей на стартовой странице).

**Важно:** Модуль "Логи вебхуков" является внутренним администраторским модулем и не управляется через этот интерфейс.

---

## 🎯 Контекст

### Текущая ситуация:

- ✅ Конфигурация администраторских интерфейсов создана (`admin-config.js`)
- ✅ Маршрут `/admin/users` определён в конфигурации
- ✅ Кнопка администрирования отображается для администраторов
- ✅ Конфигурация доступа (`access-config.js`) — управление `allowedDepartmentIds`
- ✅ Конфигурация отчётов (`reports-config.js`) — управление модулями на стартовой странице
- ❌ Страница управления пользователями не реализована
- ❌ Нет интерфейса для управления доступом к приложению
- ❌ Нет интерфейса для управления видимостью модулей

### Требования:

**Управление доступом к приложению:**
- Просмотр текущего списка разрешённых отделов (`allowedDepartmentIds`)
- Добавление/удаление отделов из списка доступа
- Просмотр пользователей с доступом и без доступа
- Статистика доступа по отделам

**Управление видимостью модулей:**
- Просмотр текущей конфигурации модулей (`reports-config.js`)
- Включение/выключение модулей на стартовой странице
- Настройка видимости модулей для разных отделов (опционально)
- Предпросмотр изменений

### Референсная реализация:

- **Модуль:** "Логи вебхуков" (`WebhookLogsPage.vue`) — структура администраторской страницы
- **Конфигурация:** `access-config.js` и `reports-config.js` — примеры конфигурационных файлов
- **Стиль:** Соответствие гайдлайнам Bitrix24

---

## 🏗️ Модули и компоненты

### Новые файлы (Vue.js):

- `vue-app/src/pages/UsersManagementPage.vue` — основная страница управления доступом и модулями
- `vue-app/src/components/users/AccessManager.vue` — управление доступом к приложению (отделы)
- `vue-app/src/components/users/ModulesManager.vue` — управление видимостью модулей на стартовой странице
- `vue-app/src/components/users/DepartmentsList.vue` — список отделов с доступом
- `vue-app/src/components/users/ModulesList.vue` — список модулей с настройками видимости
- `vue-app/src/components/users/AccessStats.vue` — статистика доступа
- `vue-app/src/services/access-management-service.js` — сервис для работы с конфигурацией доступа
- `vue-app/src/services/modules-management-service.js` — сервис для работы с конфигурацией модулей

### Изменяемые файлы:

- `vue-app/src/router/index.js` — добавление маршрута `/admin/users` с проверкой прав администратора
- `vue-app/src/config/access-config.js` — (опционально) функции для обновления конфигурации через API
- `vue-app/src/config/reports-config.js` — (опционально) функции для обновления конфигурации через API

### Структура директорий:

```
vue-app/src/
├── pages/
│   └── UsersManagementPage.vue          # Основная страница
├── components/
│   └── users/
│       ├── AccessManager.vue            # Управление доступом к приложению
│       ├── ModulesManager.vue           # Управление видимостью модулей
│       ├── DepartmentsList.vue          # Список отделов с доступом
│       ├── ModulesList.vue              # Список модулей с настройками
│       └── AccessStats.vue              # Статистика доступа
└── services/
    ├── access-management-service.js     # Сервис управления доступом
    └── modules-management-service.js    # Сервис управления модулями
```

---

## 📦 Зависимости

### От задач:

- TASK-004-02: Конфигурация администраторских интерфейсов
- TASK-003: Реализация модуля "Логи вебхуков" (референс структуры)

### От модулей:

- Модуль "Администрирование" (конфигурация и маршрутизация)
- AccessControlService (проверка прав администратора)
- Конфигурация `access-config.js` (управление доступом к приложению)
- Конфигурация `reports-config.js` (управление модулями на стартовой странице)
- Bitrix24BxApi / Bitrix24ApiService (работа с Bitrix24 REST API для получения информации об отделах)

### От библиотек:

- Vue.js 3.x (Composition API)
- Bitrix24 REST API методы (опционально, для получения информации):
  - `user.get` — получение списка пользователей (для статистики)
  - `user.current` — получение текущего пользователя
  - `department.get` — получение информации об отделе (если доступно)

---

## 🎯 Ступенчатые подзадачи

### Этап 1: Создание структуры и базовой страницы

**Цель:** Создать основную структуру модуля и страницу управления доступом и модулями.

**Шаги:**

1. **Создать директорию для компонентов**
   ```
   vue-app/src/components/users/
   ```

2. **Создать основную страницу `UsersManagementPage.vue`**
   - Структура страницы (заголовок, кнопка "Назад")
   - Проверка прав администратора
   - Базовый layout с двумя основными секциями:
     - Секция 1: Управление доступом к приложению
     - Секция 2: Управление видимостью модулей

3. **Добавить маршрут в `router/index.js`**
   ```javascript
   {
     path: '/admin/users',
     name: 'admin-users',
     component: () => import('@/pages/UsersManagementPage.vue'),
     meta: {
       requiresAuth: true,
       title: 'Управление пользователями',
       adminOnly: true
     }
   }
   ```

4. **Создать базовые компоненты-заглушки**
   - `AccessManager.vue` — управление доступом к приложению
   - `ModulesManager.vue` — управление видимостью модулей
   - `AccessStats.vue` — статистика доступа

**Критерии приёмки:**
- [ ] Страница доступна по маршруту `/admin/users`
- [ ] Проверка прав администратора работает
- [ ] Кнопка "Назад" ведёт на главную страницу
- [ ] Базовая структура страницы с двумя секциями отображается

---

### Этап 2: Реализация сервисов для работы с конфигурацией

**Цель:** Создать сервисы для работы с конфигурацией доступа и модулей.

**Шаги:**

1. **Создать `access-management-service.js`**
   ```javascript
   /**
    * Сервис для управления доступом к приложению
    * 
    * Работает с конфигурацией access-config.js
    */
   import { accessConfig, getAllowedDepartmentIds } from '@/config/access-config.js';
   
   export class AccessManagementService {
     /**
      * Получение текущей конфигурации доступа
      * 
      * @returns {object} Конфигурация доступа
      */
     static getAccessConfig() {
       return {
         allowedDepartmentIds: getAllowedDepartmentIds(),
         adminDepartmentIds: accessConfig.adminDepartmentIds
       };
     }
     
     /**
      * Обновление списка разрешённых отделов
      * 
      * ВАЖНО: Требуется backend API endpoint для обновления файла access-config.js
      * 
      * @param {number[]} departmentIds - Массив ID отделов
      * @returns {Promise<boolean>} Успешность обновления
      */
     static async updateAllowedDepartments(departmentIds) {
       // Вызов backend API для обновления конфигурации
       // POST /api/admin/update-access-config
       // { allowedDepartmentIds: departmentIds }
     }
     
     /**
      * Добавление отдела в список разрешённых
      * 
      * @param {number} departmentId - ID отдела
      * @returns {Promise<boolean>} Успешность добавления
      */
     static async addAllowedDepartment(departmentId) {
       const current = getAllowedDepartmentIds();
       if (current.includes(departmentId)) {
         return false; // Уже есть в списке
       }
       return await this.updateAllowedDepartments([...current, departmentId]);
     }
     
     /**
      * Удаление отдела из списка разрешённых
      * 
      * @param {number} departmentId - ID отдела
      * @returns {Promise<boolean>} Успешность удаления
      */
     static async removeAllowedDepartment(departmentId) {
       const current = getAllowedDepartmentIds();
       const updated = current.filter(id => id !== departmentId);
       return await this.updateAllowedDepartments(updated);
     }
   }
   ```

2. **Создать `modules-management-service.js`**
   ```javascript
   /**
    * Сервис для управления видимостью модулей на стартовой странице
    * 
    * Работает с конфигурацией reports-config.js
    */
   import { getReports } from '@/config/reports-config.js';
   
   export class ModulesManagementService {
     /**
      * Получение текущей конфигурации модулей
      * 
      * @returns {Array} Массив модулей с настройками
      */
     static getModulesConfig() {
       return getReports();
     }
     
     /**
      * Обновление конфигурации модулей
      * 
      * ВАЖНО: Требуется backend API endpoint для обновления файла reports-config.js
      * 
      * @param {Array} modules - Массив модулей с настройками
      * @returns {Promise<boolean>} Успешность обновления
      */
     static async updateModulesConfig(modules) {
       // Вызов backend API для обновления конфигурации
       // POST /api/admin/update-reports-config
       // { modules: modules }
     }
     
     /**
      * Включение/выключение модуля
      * 
      * @param {number} moduleId - ID модуля
      * @param {boolean} enabled - Включен ли модуль
      * @returns {Promise<boolean>} Успешность обновления
      */
     static async toggleModule(moduleId, enabled) {
       const modules = this.getModulesConfig();
       const updated = modules.map(module => 
         module.id === moduleId ? { ...module, enabled } : module
       );
       return await this.updateModulesConfig(updated);
     }
   }
   ```

**Критерии приёмки:**
- [ ] Сервис `AccessManagementService` создан и работает
- [ ] Сервис `ModulesManagementService` создан и работает
- [ ] Методы чтения конфигурации работают
- [ ] Методы обновления конфигурации готовы (требуют backend API)
- [ ] Обработка ошибок реализована

---

### Этап 3: Реализация компонента управления доступом

**Цель:** Создать компонент для управления списком разрешённых отделов.

**Шаги:**

1. **Создать `AccessManager.vue`**
   - Отображение текущего списка разрешённых отделов
   - Добавление нового отдела (ввод ID отдела)
   - Удаление отдела из списка
   - Валидация изменений
   - Сохранение изменений через API

2. **Создать `DepartmentsList.vue`**
   - Список отделов с доступом
   - Отображение ID отдела
   - Кнопка удаления для каждого отдела
   - Индикация изменений (несохранённые изменения)

3. **Интеграция с сервисом**
   - Загрузка данных через `AccessManagementService`
   - Обработка состояний загрузки
   - Обработка ошибок

**Пример структуры компонента:**

```vue
<template>
  <div class="access-manager">
    <h2>Управление доступом к приложению</h2>
    
    <div class="current-departments">
      <h3>Разрешённые отделы</h3>
      <DepartmentsList
        :departments="allowedDepartments"
        @remove="handleRemoveDepartment"
      />
    </div>
    
    <div class="add-department">
      <h3>Добавить отдел</h3>
      <input
        v-model.number="newDepartmentId"
        type="number"
        placeholder="ID отдела"
        @keyup.enter="handleAddDepartment"
      />
      <button @click="handleAddDepartment">Добавить</button>
    </div>
    
    <div class="actions">
      <button @click="handleSave" :disabled="!hasChanges">
        Сохранить изменения
      </button>
      <button @click="handleCancel" :disabled="!hasChanges">
        Отменить
      </button>
    </div>
  </div>
</template>

<script>
import { ref, computed } from 'vue';
import { AccessManagementService } from '@/services/access-management-service.js';
import DepartmentsList from './DepartmentsList.vue';

export default {
  name: 'AccessManager',
  components: {
    DepartmentsList
  },
  setup() {
    const allowedDepartments = ref([]);
    const newDepartmentId = ref(null);
    const hasChanges = ref(false);
    const loading = ref(false);
    const error = ref(null);
    
    const loadConfig = () => {
      const config = AccessManagementService.getAccessConfig();
      allowedDepartments.value = [...config.allowedDepartmentIds];
      hasChanges.value = false;
    };
    
    const handleAddDepartment = () => {
      if (!newDepartmentId.value) return;
      
      if (allowedDepartments.value.includes(newDepartmentId.value)) {
        error.value = 'Отдел уже в списке';
        return;
      }
      
      allowedDepartments.value.push(newDepartmentId.value);
      newDepartmentId.value = null;
      hasChanges.value = true;
      error.value = null;
    };
    
    const handleRemoveDepartment = (departmentId) => {
      allowedDepartments.value = allowedDepartments.value.filter(
        id => id !== departmentId
      );
      hasChanges.value = true;
    };
    
    const handleSave = async () => {
      loading.value = true;
      error.value = null;
      
      try {
        const success = await AccessManagementService.updateAllowedDepartments(
          allowedDepartments.value
        );
        
        if (success) {
          hasChanges.value = false;
          // Уведомление об успехе
        } else {
          error.value = 'Ошибка сохранения';
        }
      } catch (err) {
        error.value = err.message || 'Ошибка сохранения';
      } finally {
        loading.value = false;
      }
    };
    
    const handleCancel = () => {
      loadConfig();
    };
    
    loadConfig();
    
    return {
      allowedDepartments,
      newDepartmentId,
      hasChanges,
      loading,
      error,
      handleAddDepartment,
      handleRemoveDepartment,
      handleSave,
      handleCancel
    };
  }
};
</script>
```

**Критерии приёмки:**
- [ ] Компонент `AccessManager.vue` отображает список отделов
- [ ] Компонент `DepartmentsList.vue` отображает отделы
- [ ] Добавление/удаление отделов работает
- [ ] Валидация изменений реализована
- [ ] Сохранение изменений работает (через API)

---

### Этап 4: Реализация компонента управления модулями

**Цель:** Создать компонент для управления видимостью модулей на стартовой странице.

**Шаги:**

1. **Создать `ModulesManager.vue`**
   - Отображение текущего списка модулей
   - Включение/выключение модулей
   - Изменение порядка модулей (опционально)
   - Предпросмотр изменений
   - Сохранение изменений через API

2. **Создать `ModulesList.vue`**
   - Список модулей с настройками
   - Переключатель включения/выключения для каждого модуля
   - Отображение иконки, названия, описания модуля
   - Индикация изменений

3. **Интеграция с сервисом**
   - Загрузка данных через `ModulesManagementService`
   - Обработка состояний загрузки
   - Обработка ошибок

**Пример структуры компонента:**

```vue
<template>
  <div class="modules-manager">
    <h2>Управление видимостью модулей</h2>
    
    <div class="modules-list">
      <ModulesList
        :modules="modules"
        @toggle="handleToggleModule"
      />
    </div>
    
    <div class="actions">
      <button @click="handleSave" :disabled="!hasChanges">
        Сохранить изменения
      </button>
      <button @click="handleCancel" :disabled="!hasChanges">
        Отменить
      </button>
    </div>
  </div>
</template>

<script>
import { ref, computed } from 'vue';
import { ModulesManagementService } from '@/services/modules-management-service.js';
import ModulesList from './ModulesList.vue';

export default {
  name: 'ModulesManager',
  components: {
    ModulesList
  },
  setup() {
    const modules = ref([]);
    const hasChanges = ref(false);
    const loading = ref(false);
    const error = ref(null);
    
    const loadConfig = () => {
      modules.value = ModulesManagementService.getModulesConfig().map(m => ({
        ...m,
        enabled: m.enabled !== false // По умолчанию включен
      }));
      hasChanges.value = false;
    };
    
    const handleToggleModule = (moduleId, enabled) => {
      const module = modules.value.find(m => m.id === moduleId);
      if (module) {
        module.enabled = enabled;
        hasChanges.value = true;
      }
    };
    
    const handleSave = async () => {
      loading.value = true;
      error.value = null;
      
      try {
        const success = await ModulesManagementService.updateModulesConfig(
          modules.value
        );
        
        if (success) {
          hasChanges.value = false;
          // Уведомление об успехе
        } else {
          error.value = 'Ошибка сохранения';
        }
      } catch (err) {
        error.value = err.message || 'Ошибка сохранения';
      } finally {
        loading.value = false;
      }
    };
    
    const handleCancel = () => {
      loadConfig();
    };
    
    loadConfig();
    
    return {
      modules,
      hasChanges,
      loading,
      error,
      handleToggleModule,
      handleSave,
      handleCancel
    };
  }
};
</script>
```

**Критерии приёмки:**
- [ ] Компонент `ModulesManager.vue` отображает список модулей
- [ ] Компонент `ModulesList.vue` отображает модули с переключателями
- [ ] Включение/выключение модулей работает
- [ ] Валидация изменений реализована
- [ ] Сохранение изменений работает (через API)

---

### Этап 5: Реализация статистики доступа

**Цель:** Добавить компонент статистики доступа к приложению.

**Шаги:**

1. **Создать `AccessStats.vue`**
   - Общая статистика (количество отделов с доступом)
   - Список модулей и их статус (включен/выключен)
   - Визуализация (карточки статистики)

2. **Интеграция с сервисами**
   - Использование `AccessManagementService.getAccessConfig()`
   - Использование `ModulesManagementService.getModulesConfig()`

**Критерии приёмки:**
- [ ] Компонент `AccessStats.vue` отображает статистику
- [ ] Данные загружаются через сервисы
- [ ] Визуализация статистики работает

---

### Этап 6: Интеграция и финализация

**Цель:** Интегрировать все компоненты на странице и завершить реализацию.

**Шаги:**

1. **Интеграция компонентов в `UsersManagementPage.vue`**
   - Размещение `AccessManager` и `ModulesManager` на странице
   - Размещение `AccessStats` на странице
   - Управление состоянием
   - Обработка событий

2. **Стилизация**
   - Соответствие гайдлайнам Bitrix24
   - Адаптивность (мобильные устройства)
   - Анимации и переходы

3. **Обработка ошибок**
   - Глобальная обработка ошибок
   - Уведомления пользователю
   - Логирование ошибок

4. **Тестирование**
   - Проверка всех функций
   - Проверка на разных устройствах
   - Проверка производительности

**Критерии приёмки:**
- [ ] Все компоненты интегрированы на странице
- [ ] Стилизация соответствует гайдлайнам Bitrix24
- [ ] Обработка ошибок реализована
- [ ] Тестирование пройдено успешно

---

## 🔗 API-методы (Backend)

### Требуемые API endpoints:

1. **`POST /api/admin/update-access-config`** — обновление конфигурации доступа
   - Параметры: `{ allowedDepartmentIds: number[] }`
   - Ответ: `{ success: boolean, message?: string }`
   - Действие: Обновление файла `vue-app/src/config/access-config.js`

2. **`POST /api/admin/update-reports-config`** — обновление конфигурации модулей
   - Параметры: `{ modules: Array }`
   - Ответ: `{ success: boolean, message?: string }`
   - Действие: Обновление файла `vue-app/src/config/reports-config.js`

**Примечание:** Эти endpoints должны быть реализованы на backend (PHP/Laravel) для безопасного обновления конфигурационных файлов.

---

## 🔗 API-методы Bitrix24 (опционально)

### Используемые методы (для получения дополнительной информации):

1. **`user.get`** — получение списка пользователей (для статистики)
   - Документация: https://context7.com/bitrix24/rest/user.get
   - Используется опционально для отображения количества пользователей с доступом

2. **`user.current`** — получение текущего пользователя
   - Документация: https://context7.com/bitrix24/rest/user.current
   - Используется для проверки прав администратора
   
   export class UsersManagementService {
     /**
      * Получение списка всех пользователей
      * 
      * Метод: user.get
      * Документация: https://context7.com/bitrix24/rest/user.get
      * 
      * @param {object} options - Опции запроса
      * @param {string} options.search - Поисковый запрос
      * @param {number[]} options.departmentIds - Фильтр по отделам
      * @param {boolean} options.onlyWithAccess - Только пользователи с доступом
      * @returns {Promise<Array>} Массив пользователей
      */
     static async getUsers(options = {}) {
       const { search = '', departmentIds = [], onlyWithAccess = false } = options;
       
       // Подготовка фильтра для Bitrix24 API
       const filter = {
         'ACTIVE': 'Y' // Только активные пользователи
       };
       
       // Фильтр по отделам
       if (departmentIds.length > 0) {
         filter['UF_DEPARTMENT'] = departmentIds;
       }
       
       // Поиск по имени/фамилии/email
       if (search && search.length >= 2) {
         filter['%NAME'] = search;
         filter['%LAST_NAME'] = search;
         filter['%EMAIL'] = search;
       }
       
       // Выбор полей для получения
       const select = [
         'ID',
         'NAME',
         'LAST_NAME',
         'SECOND_NAME',
         'EMAIL',
         'WORK_POSITION',
         'UF_DEPARTMENT',
         'ACTIVE'
       ];
       
       try {
         // Используем Bitrix24BxApi или Bitrix24ApiService
         const result = await Bitrix24ApiService.call('user.get', {
           filter: filter,
           select: select,
           order: { 'LAST_NAME': 'ASC', 'NAME': 'ASC' },
           start: 0
         });
         
         let users = result.result || [];
         
         // Обогащение данных о доступе
         users = users.map(user => ({
           ...user,
           hasAccess: this.checkUserAccess(user),
           allowedDepartments: this.getAllowedDepartmentsForUser(user)
         }));
         
         // Фильтр по доступу (если требуется)
         if (onlyWithAccess) {
           users = users.filter(user => user.hasAccess);
         }
         
         return users;
       } catch (error) {
         console.error('[UsersManagementService] Error getting users:', error);
         throw error;
       }
     }
     
     /**
      * Проверка доступа пользователя к приложению
      * 
      * @param {object} user - Объект пользователя из Bitrix24
      * @returns {boolean} true, если пользователь имеет доступ
      */
     static checkUserAccess(user) {
       if (!user || !user.UF_DEPARTMENT) {
         return false;
       }
       
       const departmentIds = Array.isArray(user.UF_DEPARTMENT) 
         ? user.UF_DEPARTMENT 
         : [];
       
       if (departmentIds.length === 0) {
         return false;
       }
       
       // Проверяем, есть ли хотя бы один отдел пользователя в списке разрешённых
       return departmentIds.some(deptId => 
         isDepartmentAllowed(deptId)
       );
     }
     
     /**
      * Получение списка разрешённых отделов для пользователя
      * 
      * @param {object} user - Объект пользователя из Bitrix24
      * @returns {number[]} Массив ID разрешённых отделов пользователя
      */
     static getAllowedDepartmentsForUser(user) {
       if (!user || !user.UF_DEPARTMENT) {
         return [];
       }
       
       const departmentIds = Array.isArray(user.UF_DEPARTMENT) 
         ? user.UF_DEPARTMENT 
         : [];
       
       return departmentIds.filter(deptId => 
         isDepartmentAllowed(deptId)
       );
     }
     
     /**
      * Получение статистики доступа
      * 
      * @returns {Promise<object>} Статистика доступа
      */
     static async getAccessStats() {
       try {
         const users = await this.getUsers();
         
         const stats = {
           total: users.length,
           withAccess: users.filter(u => u.hasAccess).length,
           withoutAccess: users.filter(u => !u.hasAccess).length,
           byDepartment: {}
         };
         
         // Группировка по отделам
         users.forEach(user => {
           if (user.UF_DEPARTMENT && Array.isArray(user.UF_DEPARTMENT)) {
             user.UF_DEPARTMENT.forEach(deptId => {
               if (!stats.byDepartment[deptId]) {
                 stats.byDepartment[deptId] = {
                   total: 0,
                   withAccess: 0,
                   withoutAccess: 0
                 };
               }
               stats.byDepartment[deptId].total++;
               if (user.hasAccess) {
                 stats.byDepartment[deptId].withAccess++;
               } else {
                 stats.byDepartment[deptId].withoutAccess++;
               }
             });
           }
         });
         
         return stats;
       } catch (error) {
         console.error('[UsersManagementService] Error getting stats:', error);
         throw error;
       }
     }
   }
   ```

**Критерии приёмки:**
- [ ] Сервис `UsersManagementService` создан и работает
- [ ] Метод `getUsers()` возвращает список пользователей
- [ ] Метод `checkUserAccess()` корректно определяет доступ
- [ ] Метод `getAccessStats()` возвращает статистику
- [ ] Обработка ошибок реализована

---


---

### Этап 7: Интеграция и финализация

**Цель:** Интегрировать все компоненты на странице и завершить реализацию.

**Шаги:**

1. **Интеграция компонентов в `UsersManagementPage.vue`**
   - Размещение всех компонентов на странице
   - Управление состоянием
   - Обработка событий

2. **Стилизация**
   - Соответствие гайдлайнам Bitrix24
   - Адаптивность (мобильные устройства)
   - Анимации и переходы

3. **Обработка ошибок**
   - Глобальная обработка ошибок
   - Уведомления пользователю
   - Логирование ошибок

4. **Тестирование**
   - Проверка всех функций
   - Проверка на разных устройствах
   - Проверка производительности

**Критерии приёмки:**
- [ ] Все компоненты интегрированы на странице
- [ ] Стилизация соответствует гайдлайнам Bitrix24
- [ ] Обработка ошибок реализована
- [ ] Тестирование пройдено успешно

---

## 🔗 API-методы Bitrix24

### Используемые методы:

1. **`user.get`** — получение списка пользователей
   - Документация: https://context7.com/bitrix24/rest/user.get
   - Параметры:
     - `filter` — фильтр пользователей (ACTIVE, UF_DEPARTMENT, %NAME, %LAST_NAME, %EMAIL)
     - `select` — поля для выборки (ID, NAME, LAST_NAME, EMAIL, UF_DEPARTMENT, WORK_POSITION, ACTIVE)
     - `order` — сортировка (LAST_NAME, NAME)
     - `start` — смещение для пагинации

2. **`user.current`** — получение текущего пользователя
   - Документация: https://context7.com/bitrix24/rest/user.current
   - Используется для проверки прав администратора

3. **`department.get`** (опционально) — получение информации об отделе
   - Документация: https://context7.com/bitrix24/rest/department.get
   - Примечание: Может быть недоступен в облачной версии Bitrix24

---

## 🎨 Технические требования

### Vue.js:

- **Версия:** 3.x (Composition API)
- **Стиль:** Composition API с `<script setup>` или `setup()`
- **Реактивность:** `ref()`, `reactive()`, `computed()`, `watch()`

### Компоненты:

- **Модульность:** Каждый компонент в отдельном файле
- **Переиспользуемость:** Компоненты должны быть переиспользуемыми
- **Props/Emits:** Чёткое определение интерфейса компонентов

### Стилизация:

- **Подход:** Scoped styles в компонентах
- **Гайдлайны:** Соответствие стилям Bitrix24
- **Адаптивность:** Поддержка мобильных устройств

### Производительность:

- **Ленивая загрузка:** Использование `defineAsyncComponent` для тяжёлых компонентов
- **Кеширование:** Кеширование данных о пользователях (sessionStorage)
- **Оптимизация:** Минимизация запросов к API

---

## ✅ Критерии приёмки

### Функциональность:

- [ ] Страница доступна по маршруту `/admin/users`
- [ ] Проверка прав администратора работает
- [ ] Управление доступом к приложению работает:
  - [ ] Отображение текущего списка разрешённых отделов
  - [ ] Добавление отдела в список
  - [ ] Удаление отдела из списка
  - [ ] Сохранение изменений через API
- [ ] Управление видимостью модулей работает:
  - [ ] Отображение списка модулей
  - [ ] Включение/выключение модулей
  - [ ] Сохранение изменений через API
- [ ] Статистика доступа отображается
- [ ] Кнопка "Назад" ведёт на главную страницу

### Безопасность:

- [ ] Доступ только для администраторов
- [ ] Проверка прав на уровне маршрутизации
- [ ] Проверка прав на уровне страницы
- [ ] Валидация изменений конфигурации

### UX:

- [ ] Индикация загрузки данных
- [ ] Обработка ошибок с понятными сообщениями
- [ ] Адаптивность для мобильных устройств
- [ ] Соответствие гайдлайнам Bitrix24

### Производительность:

- [ ] Загрузка данных не превышает 3 секунд
- [ ] Кеширование данных работает
- [ ] Оптимизация запросов к API

---

## 📝 Примечания

### Важные замечания:

1. **Обновление конфигурации:**
   - Прямое обновление `access-config.js` и `reports-config.js` из Vue.js невозможно
   - Требуется backend API endpoint для обновления конфигурации
   - Backend должен безопасно обновлять файлы конфигурации
   - Рекомендуется валидация изменений на backend

2. **Модуль "Логи вебхуков":**
   - Модуль "Логи вебхуков" является внутренним администраторским модулем
   - Он не управляется через этот интерфейс
   - Доступ к нему контролируется через `adminDepartmentIds` в `access-config.js`

3. **Управление модулями:**
   - Управляются только 4 основных модуля на стартовой странице
   - Конфигурация хранится в `reports-config.js`
   - Изменения применяются сразу после сохранения (требуется перезагрузка страницы или обновление через API)

4. **Безопасность:**
   - Все изменения конфигурации должны проверяться на backend
   - Валидация ID отделов (должны быть числами)
   - Валидация структуры модулей
   - Логирование всех изменений конфигурации

5. **Обратная совместимость:**
   - При обновлении конфигурации необходимо сохранять существующие настройки
   - Не удалять отделы/модули, которые не были явно изменены

---

## 🔄 История правок

- 2025-12-24 09:30 (UTC+3, Брест): Создан черновик задачи TASK-072

---

## 📚 Связанные документы

- `DOCS/ANALYSIS/administration-module-comprehensive-analysis.md` — анализ модуля администрирования
- `DOCS/TASKS/TASK-004-02-admin-access-config.md` — конфигурация администраторских интерфейсов
- `vue-app/src/pages/WebhookLogsPage.vue` — референсная реализация администраторской страницы

