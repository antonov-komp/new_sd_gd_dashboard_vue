# TASK-004-01: Вывод кнопок отчётов в grid layout на IndexPage

**Дата создания:** 2025-12-06 14:00 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Дата завершения:** 2025-12-06 (UTC+3, Брест)  
**Родительская задача:** [TASK-004](./TASK-004-index-page-reports-admin.md) (если создана)

---

## 📋 Описание

Реализовать вывод кнопок отчётов на стартовой странице IndexPage.vue в виде адаптивного grid layout. Кнопки должны размещаться слева направо и сверху вниз, с иконками, названиями и описаниями.

**Текущее состояние:**
- IndexPage.vue имеет только приветствие и пустую секцию `.analytics-content`
- Нет кнопок для навигации к отчётам

**Целевое состояние:**
- Секция `.reports-section` с grid layout для кнопок отчётов
- Каждая кнопка содержит иконку, название и описание
- Адаптивный дизайн (десктоп, планшет, мобильный)
- Навигация к страницам отчётов через Vue Router

---

## 🎯 Контекст

Эта задача является частью расширения функциональности стартовой страницы приложения. После проверки доступа пользователь должен видеть доступные ему отчёты в удобном grid layout.

**Связи с другими задачами:**
- Зависит от: TASK-001 (IndexPage.vue должен быть создан)
- Зависит от неё: TASK-004-02 (расширение конфига доступа для админов)
- Использует: Vue Router для навигации

**Ссылка на дизайн:**
- ASCII-схема: `/UIX/ascii-schemas/index-page-all-states.txt` (Состояние 6, секция `.reports-section`)

---

## 📁 Модули и компоненты

### Файлы для создания/изменения:

- `vue-app/src/components/IndexPage.vue` — добавление секции с кнопками отчётов
- `vue-app/src/config/reports-config.js` — конфигурация отчётов (новый файл)
- `vue-app/src/styles/reports-grid.css` — стили для grid layout (опционально, можно в компоненте)

### Структура компонента:

```
IndexPage.vue
├── .greeting-section (уже есть)
├── .reports-section (новое)
│   └── .reports-grid (новое)
│       └── .report-button (новое, для каждой кнопки)
└── .admin-section (будет в TASK-004-02)
```

---

## 🔗 Зависимости

### От других задач:
- **TASK-001** — IndexPage.vue должен быть создан и работать
- **Vue Router** — должен быть настроен в приложении

### От модулей:
- `vue-router` — для навигации между страницами
- `@/config/access-config.js` — для проверки доступа (уже используется)

---

## 📝 Ступенчатые подзадачи

### 1. Создать конфигурацию отчётов

**Файл:** `vue-app/src/config/reports-config.js`

```javascript
/**
 * Конфигурация отчётов для стартовой страницы
 * 
 * Определяет список доступных отчётов с их метаданными
 */

export const reportsConfig = {
  /**
   * Массив отчётов, доступных всем пользователям с доступом
   * 
   * @type {Array<Object>}
   */
  reports: [
    {
      id: 1,
      title: 'Отчёт по тикетам',
      icon: '📊',
      route: '/reports/tickets',
      description: 'Статистика по тикетам и обращениям',
      // Дополнительные параметры (опционально)
      // category: 'tickets',
      // requiredAccess: 'basic'
    },
    {
      id: 2,
      title: 'Отчёт по производительности',
      icon: '📈',
      route: '/reports/performance',
      description: 'Анализ производительности работы отдела'
    },
    {
      id: 3,
      title: 'Отчёт по срокам',
      icon: '📉',
      route: '/reports/deadlines',
      description: 'Анализ соблюдения сроков выполнения задач'
    },
    {
      id: 4,
      title: 'Сводный отчёт',
      icon: '📋',
      route: '/reports/summary',
      description: 'Общая сводка по всем показателям'
    }
    // Добавьте другие отчёты по мере необходимости
  ]
};

/**
 * Получение списка всех отчётов
 * 
 * @returns {Array<Object>} Массив отчётов
 */
export function getReports() {
  return [...reportsConfig.reports];
}

/**
 * Получение отчёта по ID
 * 
 * @param {number} reportId - ID отчёта
 * @returns {Object|null} Объект отчёта или null
 */
export function getReportById(reportId) {
  return reportsConfig.reports.find(report => report.id === reportId) || null;
}
```

**Требования:**
- Экспортировать конфигурацию отчётов
- Каждый отчёт должен иметь: id, title, icon, route, description
- Функции для получения отчётов

---

### 2. Обновить IndexPage.vue: добавить state для отчётов

**Файл:** `vue-app/src/components/IndexPage.vue`

В секции `setup()` добавить:

```javascript
import { getReports } from '@/config/reports-config.js';

// В setup():
const reportsButtons = ref(getReports());
```

**Требования:**
- Импортировать функцию `getReports()` из конфига
- Создать ref для хранения кнопок отчётов
- Инициализировать массив отчётов при монтировании компонента

---

### 3. Добавить метод навигации к отчёту

**Файл:** `vue-app/src/components/IndexPage.vue`

В секции `setup()` добавить:

```javascript
import { useRouter } from 'vue-router';

// В setup():
const router = useRouter();

const navigateToReport = (route) => {
  if (!route) {
    console.error('Route is not defined for report');
    return;
  }
  
  router.push(route);
};
```

**Требования:**
- Использовать `useRouter()` из vue-router
- Метод должен принимать route и выполнять навигацию
- Обработка ошибок (если route не определён)

---

### 4. Добавить HTML-разметку для секции отчётов

**Файл:** `vue-app/src/components/IndexPage.vue`

В секции `<template>`, после `.greeting-section` и перед `.admin-section`:

```vue
<!-- Секция с кнопками отчётов -->
<div class="reports-section">
  <div class="reports-grid">
    <div
      v-for="report in reportsButtons"
      :key="report.id"
      class="report-button"
      @click="navigateToReport(report.route)"
    >
      <div class="report-icon">{{ report.icon }}</div>
      <div class="report-title">{{ report.title }}</div>
      <div v-if="report.description" class="report-description">
        {{ report.description }}
      </div>
    </div>
  </div>
</div>
```

**Требования:**
- Секция `.reports-section` с margin-top: 30px
- Grid layout `.reports-grid` с адаптивными колонками
- Каждая кнопка `.report-button` с обработчиком клика
- Отображение иконки, названия и описания

---

### 5. Добавить стили для grid layout

**Файл:** `vue-app/src/components/IndexPage.vue`

В секции `<style scoped>`:

```css
.reports-section {
  margin-top: 30px;
}

.reports-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 20px;
}

.report-button {
  padding: 20px;
  border-radius: 8px;
  background: white;
  border: 1px solid #ddd;
  cursor: pointer;
  transition: all 0.3s ease;
  text-align: center;
}

.report-button:hover {
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  transform: translateY(-2px);
  border-color: #2196F3;
}

.report-icon {
  font-size: 32px;
  margin-bottom: 10px;
}

.report-title {
  font-size: 16px;
  font-weight: 600;
  color: #333;
  margin-bottom: 8px;
}

.report-description {
  font-size: 14px;
  color: #666;
  line-height: 1.4;
}

/* Адаптивность для планшетов */
@media (max-width: 1024px) {
  .reports-grid {
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 15px;
  }
  
  .report-button {
    padding: 15px;
  }
  
  .report-icon {
    font-size: 28px;
  }
}

/* Адаптивность для мобильных */
@media (max-width: 768px) {
  .reports-grid {
    grid-template-columns: 1fr;
    gap: 15px;
  }
  
  .report-button {
    padding: 15px;
  }
  
  .report-icon {
    font-size: 24px;
  }
  
  .report-title {
    font-size: 14px;
  }
  
  .report-description {
    font-size: 12px;
  }
}
```

**Требования:**
- Grid layout с адаптивными колонками
- Hover-эффекты для кнопок
- Адаптивность для разных размеров экранов
- Соответствие стилям Bitrix24 UI Kit

---

### 6. Обновить return в setup()

**Файл:** `vue-app/src/components/IndexPage.vue`

Добавить в return:

```javascript
return {
  // ... существующие свойства
  reportsButtons,
  navigateToReport
};
```

**Требования:**
- Экспортировать `reportsButtons` и `navigateToReport` для использования в template

---

## ⚙️ Технические требования

### Vue.js версия:
- Vue.js 3.x (Composition API)

### Зависимости:
- `vue-router` — для навигации
- `@/config/reports-config.js` — конфигурация отчётов

### Стилизация:
- Использовать scoped styles в компоненте
- Соответствие гайдлайнам Bitrix24 UI Kit
- Адаптивный дизайн (десктоп, планшет, мобильный)

### Grid layout:
- Десктоп (> 1024px): 3-4 колонки (minmax(250px, 1fr))
- Планшет (768px - 1024px): 2-3 колонки (minmax(200px, 1fr))
- Мобильный (< 768px): 1 колонка (100%)

---

## ✅ Критерии приёмки

- [ ] Создан файл `vue-app/src/config/reports-config.js` с конфигурацией отчётов
- [ ] В IndexPage.vue добавлена секция `.reports-section` с grid layout
- [ ] Кнопки отчётов отображаются в grid layout (слева направо, сверху вниз)
- [ ] Каждая кнопка содержит иконку, название и описание
- [ ] При клике на кнопку выполняется навигация к соответствующему отчёту
- [ ] Grid layout адаптивный (меняется количество колонок в зависимости от размера экрана)
- [ ] Hover-эффекты работают корректно
- [ ] Стили соответствуют гайдлайнам Bitrix24 UI Kit
- [ ] Код соответствует стандартам ESLint
- [ ] Компонент работает без ошибок в консоли

---

## 🧪 Тестирование

### Функциональное тестирование:

1. **Отображение кнопок:**
   - Открыть IndexPage
   - Проверить, что кнопки отчётов отображаются после приветствия
   - Проверить, что каждая кнопка содержит иконку, название и описание

2. **Навигация:**
   - Кликнуть на кнопку отчёта
   - Проверить, что происходит переход на соответствующую страницу
   - Проверить, что URL изменился корректно

3. **Адаптивность:**
   - Проверить отображение на десктопе (> 1024px): 3-4 колонки
   - Проверить отображение на планшете (768px - 1024px): 2-3 колонки
   - Проверить отображение на мобильном (< 768px): 1 колонка

4. **Hover-эффекты:**
   - Навести курсор на кнопку отчёта
   - Проверить, что появляется тень и кнопка поднимается
   - Проверить, что граница меняет цвет

### Тестирование в разных браузерах:
- Chrome (последняя версия)
- Firefox (последняя версия)
- Safari (если доступен)
- Edge (последняя версия)

---

## 📚 Дополнительные материалы

### Ссылки на документацию:
- **Vue Router:** https://router.vuejs.org/
- **CSS Grid:** https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Grid_Layout
- **Bitrix24 UI Kit:** https://apidocs.bitrix24.ru/sdk/ui.html
- **ASCII-схема дизайна:** `/UIX/ascii-schemas/index-page-all-states.txt`

### Примеры кода:

**Конфигурация отчётов:**
```javascript
// vue-app/src/config/reports-config.js
export const reportsConfig = {
  reports: [
    {
      id: 1,
      title: 'Отчёт по тикетам',
      icon: '📊',
      route: '/reports/tickets',
      description: 'Статистика по тикетам и обращениям'
    }
  ]
};
```

**Использование в компоненте:**
```vue
<template>
  <div class="reports-section">
    <div class="reports-grid">
      <div
        v-for="report in reportsButtons"
        :key="report.id"
        class="report-button"
        @click="navigateToReport(report.route)"
      >
        <div class="report-icon">{{ report.icon }}</div>
        <div class="report-title">{{ report.title }}</div>
        <div class="report-description">{{ report.description }}</div>
      </div>
    </div>
  </div>
</template>
```

---

## 🔄 История правок

- **2025-12-06 14:00 (UTC+3, Брест):** Создана задача
- **2025-12-06 (UTC+3, Брест):** Задача выполнена
  - Создан файл `vue-app/src/config/reports-config.js` с конфигурацией отчётов
  - Добавлена секция `.reports-section` с grid layout в IndexPage.vue
  - Реализована навигация к отчётам через Vue Router
  - Добавлены адаптивные стили для grid layout (десктоп, планшет, мобильный)
  - Все критерии приёмки выполнены
- **2025-12-06 (UTC+3, Брест):** Добавлена возможность управления видимостью отчётов
  - Добавлено поле `active` в конфигурацию каждого отчёта
  - Функция `getReports()` теперь возвращает только активные отчёты (active: true)
  - Добавлена функция `getAllReports()` для получения всех отчётов (включая неактивные)
  - Неактивные отчёты автоматически скрываются из grid layout

---

## 💡 Примечания

- Количество отчётов может быть любым (grid layout автоматически адаптируется)
- Иконки можно использовать эмодзи или иконки из библиотеки (например, Font Awesome)
- Маршруты отчётов (`route`) должны быть настроены в Vue Router
- В будущем можно добавить фильтрацию отчётов по категориям или правам доступа

