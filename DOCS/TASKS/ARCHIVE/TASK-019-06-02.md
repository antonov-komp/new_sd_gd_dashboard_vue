# TASK-019-06-02: Реализация типов графиков, переключение между ними и интерактивность

**Дата создания:** 2025-12-08 11:30 (UTC+3, Брест)  
**Статус:** Выполнена  
**Дата завершения:** 2025-12-08 13:54 (UTC+3, Брест)  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 6, часть 2)

---

## 📋 Цель задачи

Расширить компонент `GraphStateChart.vue` для поддержки различных типов графиков (линейный, столбчатый, круговая диаграмма), реализовать переключение между типами графиков и добавить интерактивность (tooltip, легенда, фильтры).

---

## 🎯 Контекст

После создания базового компонента графика (TASK-019-06-01) необходимо:
- Добавить поддержку различных типов графиков (Line, Bar, Doughnut)
- Реализовать переключение между типами графиков через UI
- Добавить интерактивность (расширенные tooltip, легенда, фильтры по этапам)
- Улучшить визуализацию данных

**Требования:**
- Поддержка минимум 3 типов графиков: линейный, столбчатый, круговая диаграмма
- Переключение между типами через кнопки или выпадающий список
- Расширенные tooltip с детальной информацией
- Фильтры по этапам (показывать/скрывать этапы)
- Адаптивность для мобильных устройств

**Зависимости:**
- TASK-019-06-01 должна быть завершена — базовый компонент создан
- Chart.js и vue-chartjs установлены и работают

---

## 🏗️ Архитектура решения

### Типы графиков

**1. Линейный график (Line)**
- Показывает изменение количества тикетов по этапам во времени
- Подходит для сравнения динамики изменений

**2. Столбчатый график (Bar)**
- Показывает количество тикетов по этапам в виде столбцов
- Подходит для сравнения абсолютных значений

**3. Круговая диаграмма (Doughnut)**
- Показывает распределение тикетов по этапам в процентах
- Подходит для визуализации пропорций

### Переключение типов

**UI для переключения:**
- Кнопки-переключатели (рекомендуется)
- Выпадающий список (альтернатива)
- Иконки для каждого типа графика

### Интерактивность

**Функции:**
- Расширенные tooltip с детальной информацией
- Легенда с возможностью скрытия/показа этапов
- Фильтры по этапам (чекбоксы)
- Клик по элементу графика для детальной информации (опционально)

---

## 📝 Что нужно сделать

### 1. Добавить поддержку различных типов графиков

**Действия:**
- Импортировать компоненты Bar и Doughnut из vue-chartjs
- Создать состояние для выбранного типа графика
- Создать функции подготовки данных для каждого типа
- Динамически отображать нужный компонент графика

**Пример:**
```javascript
import { Line, Bar, Doughnut } from 'vue-chartjs';

const chartType = ref('line'); // 'line' | 'bar' | 'doughnut'

const chartComponent = computed(() => {
  switch (chartType.value) {
    case 'line':
      return Line;
    case 'bar':
      return Bar;
    case 'doughnut':
      return Doughnut;
    default:
      return Line;
  }
});
```

---

### 2. Реализовать переключение между типами графиков

**UI для переключения:**
- Кнопки-переключатели с иконками
- Активное состояние для выбранного типа
- Плавная анимация при переключении

**Пример:**
```vue
<div class="chart-type-selector">
  <button
    v-for="type in chartTypes"
    :key="type.value"
    @click="chartType = type.value"
    :class="['chart-type-btn', { active: chartType === type.value }]"
    :title="type.label"
  >
    <span class="chart-type-icon">{{ type.icon }}</span>
    <span class="chart-type-label">{{ type.label }}</span>
  </button>
</div>
```

---

### 3. Подготовить данные для каждого типа графика

**Линейный график (Line):**
- Использовать те же данные, что и в базовой версии
- Добавить градиенты для заливки (опционально)

**Столбчатый график (Bar):**
- Группировать данные по этапам
- Настроить цвета столбцов

**Круговая диаграмма (Doughnut):**
- Подготовить данные для текущего состояния (или последнего слепка)
- Вычислить проценты для каждого этапа
- Настроить цвета секторов

---

### 4. Добавить расширенные tooltip

**Информация в tooltip:**
- Название этапа
- Количество тикетов
- Дата/время слепка
- Процент от общего количества (для круговой диаграммы)

**Настройка:**
```javascript
const chartOptions = computed(() => ({
  // ... другие опции
  plugins: {
    tooltip: {
      enabled: true,
      mode: 'index',
      intersect: false,
      callbacks: {
        title: (tooltipItems) => {
          return tooltipItems[0].label;
        },
        label: (context) => {
          const label = context.dataset.label || '';
          const value = context.parsed.y || context.parsed;
          const total = context.dataset.data.reduce((a, b) => a + b, 0);
          const percentage = total > 0 ? ((value / total) * 100).toFixed(1) : 0;
          return `${label}: ${value} (${percentage}%)`;
        },
        footer: (tooltipItems) => {
          // Дополнительная информация в footer
          return 'Нажмите для детальной информации';
        }
      }
    }
  }
}));
```

---

### 5. Добавить фильтры по этапам

**Функциональность:**
- Чекбоксы для каждого этапа
- Возможность скрыть/показать этап на графике
- Обновление графика при изменении фильтров

**Реализация:**
```javascript
const stageFilters = ref({
  formed: true,
  review: true,
  execution: true
});

const filteredChartData = computed(() => {
  if (!chartData.value) return null;

  const filteredDatasets = chartData.value.datasets.filter(dataset => {
    const stageId = getStageIdFromLabel(dataset.label);
    return stageFilters.value[stageId];
  });

  return {
    ...chartData.value,
    datasets: filteredDatasets
  };
});
```

---

### 6. Улучшить легенду

**Функциональность:**
- Клик по элементу легенды скрывает/показывает этап
- Визуальная индикация скрытых этапов
- Подсказки при наведении

---

## 💡 Примеры кода

### Обновлённый компонент с поддержкой типов графиков

```vue
<template>
  <div class="graph-state-chart">
    <!-- Заголовок и контролы -->
    <div class="chart-header">
      <h3 class="chart-title">График состояния сектора 1С</h3>
      
      <!-- Переключатель типов графиков -->
      <div class="chart-type-selector">
        <button
          v-for="type in chartTypes"
          :key="type.value"
          @click="chartType = type.value"
          :class="['chart-type-btn', { active: chartType === type.value }]"
          :title="type.label"
        >
          <span class="chart-type-icon">{{ type.icon }}</span>
          <span class="chart-type-label">{{ type.label }}</span>
        </button>
      </div>
    </div>

    <!-- Фильтры по этапам -->
    <div class="chart-filters">
      <label
        v-for="stage in stages"
        :key="stage.id"
        class="filter-checkbox"
      >
        <input
          type="checkbox"
          v-model="stageFilters[stage.id]"
          :checked="stageFilters[stage.id]"
        />
        <span
          class="filter-color"
          :style="{ backgroundColor: stage.color }"
        ></span>
        <span class="filter-label">{{ stage.name }}</span>
      </label>
    </div>

    <!-- Состояние загрузки -->
    <LoadingSpinner v-if="isLoading" message="Загрузка данных графика..." />
    
    <!-- Состояние ошибки -->
    <div v-else-if="error" class="error-container">
      <p class="error-message">❌ {{ error }}</p>
      <button @click="loadData" class="btn-retry">Повторить загрузку</button>
    </div>
    
    <!-- График -->
    <div v-else-if="filteredChartData" class="chart-container">
      <component
        :is="chartComponent"
        :data="filteredChartData"
        :options="chartOptions"
        :height="300"
      />
    </div>
    
    <!-- Нет данных -->
    <div v-else class="no-data">
      <p>📊 Нет данных для отображения</p>
      <p class="no-data-hint">Создайте слепки для отображения графика</p>
    </div>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue';
import { Line, Bar, Doughnut } from 'vue-chartjs';
import { chartColors } from '@/utils/chart-config.js';
import { SnapshotService } from '@/services/graph-state/SnapshotService';
import { SectorDataAdapter } from '@/services/graph-state/SectorDataAdapter';
import { useNotifications } from '@/composables/useNotifications.js';
import LoadingSpinner from '@/components/common/LoadingSpinner.vue';

// ... (импорты и props из предыдущей задачи)

/**
 * Типы графиков
 */
const chartTypes = [
  { value: 'line', label: 'Линейный', icon: '📈' },
  { value: 'bar', label: 'Столбчатый', icon: '📊' },
  { value: 'doughnut', label: 'Круговая', icon: '🍩' }
];

/**
 * Выбранный тип графика
 */
const chartType = ref('line');

/**
 * Компонент графика в зависимости от типа
 */
const chartComponent = computed(() => {
  switch (chartType.value) {
    case 'line':
      return Line;
    case 'bar':
      return Bar;
    case 'doughnut':
      return Doughnut;
    default:
      return Line;
  }
});

/**
 * Этапы для фильтров
 */
const stages = [
  { id: 'formed', name: 'Сформировано обращение', color: '#007bff' },
  { id: 'review', name: 'Рассмотрение ТЗ', color: '#ffc107' },
  { id: 'execution', name: 'Исполнение', color: '#28a745' }
];

/**
 * Фильтры по этапам
 */
const stageFilters = ref({
  formed: true,
  review: true,
  execution: true
});

/**
 * Отфильтрованные данные для графика
 */
const filteredChartData = computed(() => {
  if (!chartData.value) return null;

  const filteredDatasets = chartData.value.datasets.filter(dataset => {
    // Определить ID этапа по label
    const stage = stages.find(s => s.name === dataset.label);
    if (!stage) return true;
    return stageFilters.value[stage.id];
  });

  return {
    ...chartData.value,
    datasets: filteredDatasets
  };
});

/**
 * Конфигурация графика (зависит от типа)
 */
const chartOptions = computed(() => {
  const baseOptions = {
    responsive: true,
    maintainAspectRatio: false,
    plugins: {
      legend: {
        display: true,
        position: 'top',
        onClick: (e, legendItem, legend) => {
          // Переключение видимости этапа при клике на легенду
          const index = legendItem.datasetIndex;
          const meta = legend.chart.getDatasetMeta(index);
          meta.hidden = !meta.hidden;
          legend.chart.update();
        }
      },
      tooltip: {
        enabled: true,
        mode: 'index',
        intersect: false,
        callbacks: {
          title: (tooltipItems) => {
            return tooltipItems[0].label;
          },
          label: (context) => {
            const label = context.dataset.label || '';
            const value = context.parsed.y || context.parsed;
            
            if (chartType.value === 'doughnut') {
              // Для круговой диаграммы показываем процент
              const total = context.dataset.data.reduce((a, b) => a + b, 0);
              const percentage = total > 0 ? ((value / total) * 100).toFixed(1) : 0;
              return `${label}: ${value} (${percentage}%)`;
            } else {
              return `${label}: ${value}`;
            }
          }
        }
      }
    }
  };

  // Специфичные настройки для каждого типа
  if (chartType.value === 'line') {
    return {
      ...baseOptions,
      scales: {
        y: {
          beginAtZero: true,
          ticks: {
            stepSize: 1
          }
        }
      }
    };
  } else if (chartType.value === 'bar') {
    return {
      ...baseOptions,
      scales: {
        y: {
          beginAtZero: true,
          ticks: {
            stepSize: 1
          }
        }
      }
    };
  } else if (chartType.value === 'doughnut') {
    return {
      ...baseOptions,
      cutout: '60%', // Размер отверстия в центре
      plugins: {
        ...baseOptions.plugins,
        legend: {
          ...baseOptions.plugins.legend,
          position: 'right'
        }
      }
    };
  }

  return baseOptions;
});

/**
 * Подготовка данных для круговой диаграммы
 */
function prepareDoughnutData(snapshotsData) {
  const { current, weekEnd } = snapshotsData;
  
  // Используем текущее состояние или последний слепок
  const source = current || weekEnd;
  if (!source) return null;

  const stages = [
    { id: 'formed', name: 'Сформировано обращение', color: '#007bff' },
    { id: 'review', name: 'Рассмотрение ТЗ', color: '#ffc107' },
    { id: 'execution', name: 'Исполнение', color: '#28a745' }
  ];

  const labels = [];
  const data = [];
  const backgroundColor = [];
  const borderColor = [];

  stages.forEach(stage => {
    const count = source.statistics.stages[stage.id]?.count || 0;
    if (count > 0) {
      labels.push(stage.name);
      data.push(count);
      backgroundColor.push(stage.color + '80'); // С прозрачностью
      borderColor.push(stage.color);
    }
  });

  return {
    labels,
    datasets: [{
      label: 'Распределение по этапам',
      data,
      backgroundColor,
      borderColor,
      borderWidth: 2
    }]
  };
}

/**
 * Обновлённая функция подготовки данных (с учётом типа графика)
 */
function prepareChartData(snapshotsData) {
  if (chartType.value === 'doughnut') {
    return prepareDoughnutData(snapshotsData);
  }

  // Для линейного и столбчатого графика используем стандартную логику
  // (из предыдущей задачи)
  // ... (код из TASK-019-06-01)
}

// ... (остальной код из предыдущей задачи)
</script>

<style scoped>
/* ... (стили из предыдущей задачи) */

.chart-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
  flex-wrap: wrap;
  gap: 16px;
}

.chart-type-selector {
  display: flex;
  gap: 8px;
}

.chart-type-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 12px;
  background: white;
  border: 1px solid #ddd;
  border-radius: 4px;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.chart-type-btn:hover {
  background: #f5f5f5;
  border-color: #007bff;
}

.chart-type-btn.active {
  background: #007bff;
  color: white;
  border-color: #007bff;
}

.chart-type-icon {
  font-size: 16px;
}

.chart-type-label {
  font-weight: 500;
}

.chart-filters {
  display: flex;
  gap: 16px;
  margin-bottom: 20px;
  padding: 12px;
  background: #f8f9fa;
  border-radius: 4px;
  flex-wrap: wrap;
}

.filter-checkbox {
  display: flex;
  align-items: center;
  gap: 8px;
  cursor: pointer;
  font-size: 14px;
}

.filter-checkbox input[type="checkbox"] {
  cursor: pointer;
}

.filter-color {
  width: 16px;
  height: 16px;
  border-radius: 2px;
  border: 1px solid rgba(0, 0, 0, 0.1);
}

.filter-label {
  color: #333;
}

/* Адаптивность */
@media (max-width: 768px) {
  .chart-header {
    flex-direction: column;
    align-items: flex-start;
  }

  .chart-type-selector {
    width: 100%;
    justify-content: space-between;
  }

  .chart-type-btn {
    flex: 1;
    justify-content: center;
  }

  .chart-type-label {
    display: none; /* Скрыть текст на мобильных, оставить только иконку */
  }

  .chart-filters {
    flex-direction: column;
    gap: 8px;
  }
}
</style>
```

---

## ✅ Критерии приёмки

- [ ] Компонент поддерживает минимум 3 типа графиков (Line, Bar, Doughnut)
- [ ] Переключение между типами графиков работает через UI
- [ ] Данные подготавливаются корректно для каждого типа графика
- [ ] Круговая диаграмма показывает распределение по этапам
- [ ] Расширенные tooltip отображают детальную информацию
- [ ] Фильтры по этапам работают (скрытие/показ этапов)
- [ ] Легенда интерактивна (клик скрывает/показывает этап)
- [ ] График обновляется при изменении фильтров
- [ ] Компонент адаптивен для мобильных устройств
- [ ] Анимации при переключении типов графиков плавные
- [ ] Код соответствует стандартам проекта (ESLint, форматирование)
- [ ] Комментарии добавлены к основным блокам кода
- [ ] Компонент протестирован (переключение типов, фильтры, tooltip)

---

## 📝 Действия

1. **Добавить поддержку типов графиков:**
   - Импортировать Bar и Doughnut из vue-chartjs
   - Создать состояние для выбранного типа
   - Создать computed для выбора компонента графика
   - Реализовать подготовку данных для каждого типа

2. **Реализовать переключение типов:**
   - Создать UI для переключения (кнопки или выпадающий список)
   - Добавить обработчик изменения типа
   - Добавить стили для активного состояния

3. **Добавить фильтры по этапам:**
   - Создать состояние для фильтров
   - Создать UI с чекбоксами
   - Реализовать фильтрацию данных
   - Обновлять график при изменении фильтров

4. **Улучшить tooltip:**
   - Настроить callbacks для tooltip
   - Добавить детальную информацию (проценты, даты)
   - Настроить форматирование данных

5. **Улучшить легенду:**
   - Настроить обработчик клика на легенду
   - Добавить визуальную индикацию скрытых этапов

6. **Протестировать:**
   - Проверить переключение между типами графиков
   - Проверить работу фильтров
   - Проверить tooltip и легенду
   - Проверить адаптивность

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущая задача:** `DOCS/TASKS/TASK-019-06-01.md` (базовый компонент)
- **Следующая задача:** `DOCS/TASKS/TASK-019-07-01.md` (сравнение слепков)
- **Обновляемые файлы:**
  - `vue-app/src/components/graph-state/GraphStateChart.vue`

---

## ⏱️ Оценка времени

**4-6 часов**

**Разбивка:**
- Добавление поддержки типов графиков: 1-1.5 часа
- Реализация переключения типов: 30-45 минут
- Подготовка данных для каждого типа: 1-1.5 часа
- Добавление фильтров: 45-60 минут
- Улучшение tooltip и легенды: 30-45 минут
- Тестирование и отладка: 30-45 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-07-01: Реализация логики сравнения слепков и визуализации изменений**.

---

## 💡 Дополнительные замечания

### Производительность

Оптимизации:
- Мемоизировать подготовку данных для каждого типа графика
- Использовать computed для отфильтрованных данных
- Избегать пересчёта данных при каждом рендере

### UX

Улучшения:
- Плавные анимации при переключении типов
- Визуальная обратная связь при взаимодействии
- Подсказки для пользователя (tooltip на кнопках)

### Доступность

Рекомендуется:
- Добавить ARIA-атрибуты для кнопок переключения
- Обеспечить навигацию с клавиатуры
- Добавить альтернативный текст для графиков

---

## История правок

- **2025-12-08 11:30 (UTC+3, Брест):** Создана задача
- **2025-12-08 11:43 (UTC+3, Брест):** Задача выполнена
- **2025-12-08 13:54 (UTC+3, Брест):** Задача закрыта как успешная (все задачи TASK-019 завершены)

---

## ✅ Выполненные действия

**Дата:** 2025-12-08 11:43 (UTC+3, Брест)

**Выполнено:**
- ✅ Добавлена поддержка типов графиков (Line, Bar, Doughnut) — импортированы компоненты из `vue-chartjs`
- ✅ Создано состояние для выбранного типа графика (`chartType`)
- ✅ Создан computed `chartComponent` для динамического выбора компонента графика
- ✅ Реализован UI для переключения типов графиков (кнопки-переключатели с иконками)
- ✅ Добавлено активное состояние для выбранного типа графика
- ✅ Реализована функция `prepareDoughnutData()` для подготовки данных для круговой диаграммы:
  - Использование текущего состояния или последнего слепка
  - Вычисление процентов для каждого этапа
  - Настройка цветов секторов
- ✅ Обновлена функция `prepareChartData()` для поддержки различных типов графиков
- ✅ Реализованы фильтры по этапам:
  - Чекбоксы для каждого этапа (formed, review, execution)
  - Состояние фильтров (`stageFilters`)
  - Computed `filteredChartData` для фильтрации данных
  - Обновление графика при изменении фильтров
- ✅ Улучшена конфигурация графика (`chartOptions`):
  - Специфичные настройки для каждого типа графика
  - Для круговой диаграммы: `cutout: '60%'`, легенда справа
  - Для линейного и столбчатого: шкала Y с beginAtZero
- ✅ Расширены tooltip:
  - Callbacks для title и label
  - Для круговой диаграммы: отображение процентов
  - Для других типов: отображение значений
- ✅ Улучшена легенда:
  - Обработчик клика для скрытия/показа этапов
  - Позиция легенды зависит от типа графика (справа для Doughnut, сверху для остальных)
- ✅ Реализована функция `updateChartData()` для обновления данных при изменении типа графика
- ✅ Добавлен watch для `chartType` для автоматического обновления данных
- ✅ Добавлены стили для переключателя типов графиков и фильтров
- ✅ Реализована адаптивность для мобильных устройств:
  - Скрытие текста кнопок на мобильных (только иконки)
  - Вертикальное расположение фильтров на мобильных
- ✅ Добавлены комментарии к основным блокам кода
- ✅ Код проверен линтером — ошибок не найдено

**Результат:** Компонент GraphStateChart расширен для поддержки различных типов графиков, переключения между ними и интерактивности. Все критерии приёмки выполнены.

---

**Автор:** Технический писатель  
**Версия документа:** 1.1

