# TASK-068-03: Модификация GraphAdmissionClosureService для добавления кеширования

**Дата создания:** 2025-12-23 15:01 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Backend Developer (PHP)  
**Родительская задача:** [TASK-068: Добавление кеширования для режима "4 недели"](./TASK-068-cache-for-weeks-mode.md)  
**Подзадача:** Этап 3 из TASK-068

---

## 📋 Описание

Добавить проверку и сохранение кеша в метод `handle()` класса `GraphAdmissionClosureService` для режима "weeks", аналогично тому, как это реализовано для режима "months" в методе `handleMonthsMode()`.

**Цель:** Интегрировать кеширование в обработку запросов режима "4 недели" для сокращения времени ответа.

---

## 🎯 Контекст

### Текущее состояние:

- **Текущая реализация:** Строки 29-222 в `GraphAdmissionClosureService.php`
- Метод `handle()` в `GraphAdmissionClosureService` обрабатывает режим "weeks" без кеширования
- Метод `handleMonthsMode()` уже использует кеширование (TTL: 300 секунд, строки 232-493)
- Режим "weeks" всегда выполняет полную загрузку данных из Bitrix24 (строки 52-221)
- **Текущий поток для режима "weeks":**
  1. Определение границ недель (строка 53)
  2. Загрузка тикетов из Bitrix24 (строки 60-80)
  3. Агрегация метрик (строки 84-92)
  4. Формирование ответа (строки 111-219)
  5. **НЕТ проверки кеша**
  6. **НЕТ сохранения в кеш**

### Требуется:

- Добавить проверку кеша перед загрузкой данных (если не `forceRefresh`)
- Добавить сохранение результатов в кеш после обработки (TTL: 120 секунд)
- Добавить поддержку параметра `forceRefresh` для режима "weeks"
- Добавить периодическую очистку устаревших кешей

---

## 🏗️ Модули и компоненты

### Изменяемые файлы:

- `api/graph-admission-closure/service/GraphAdmissionClosureService.php` — добавление кеширования в метод `handle()`

---

## 📦 Зависимости

- **От задач:** 
  - TASK-068-01: Расширение GraphAdmissionClosureCache для поддержки режима "weeks"
  - TASK-068-02: Обновление генерации ключа кеша для учёта weekStartUtc/weekEndUtc
- **От модулей:** 
  - Класс: `GraphAdmissionClosureService`
  - Класс: `CacheStore`
  - Класс: `DatePeriodHelper`

---

## 🎯 Ступенчатые подзадачи

### Подзадача 3.1: Добавление проверки кеша перед загрузкой данных

**Цель:** Добавить проверку кеша в начале метода `handle()` для режима "weeks".

**Шаги:**

1. **Добавить проверку кеша после определения режима**
   - **Местоположение:** После строки 54 (после `$weeks = $this->dateHelper->getFourWeeksBounds(...)`)
   - Проверять кеш только для режима "weeks"
   - Пропускать проверку, если `forceRefresh === true`
   - Генерировать ключ кеша с учётом всех параметров, включая `weekStartUtc` и `weekEndUtc`
   - **Важно:** Измерять время выполнения с самого начала (для логирования производительности)
   ```php
   // Недельный режим
   $weeksModeStartTime = microtime(true); // TASK-068-04: Начало измерения времени
   
   [$weekStart, $weekEnd] = $this->dateHelper->getWeekBounds($weekStartParam, $weekEndParam);
   $weeks = $this->dateHelper->getFourWeeksBounds($weekStart, $weekEnd);

   // TASK-068-03: Проверка кеша для режима "weeks" (если не forceRefresh)
   if (!$forceRefresh) {
       $cacheKey = $this->cacheStore->generateKey([
           'product' => $product,
           'periodMode' => 'weeks',
           'weekStartUtc' => $weekStart->format('Y-m-d\TH:i:s\Z'),
           'weekEndUtc' => $weekEnd->format('Y-m-d\TH:i:s\Z'),
           'includeTickets' => $includeTickets,
           'includeNewTicketsByStages' => $includeNewTicketsByStages,
           'includeCarryoverTickets' => $includeCarryoverTickets,
           'includeCarryoverTicketsByDuration' => $includeCarryoverTicketsByDuration
       ]);
       
       $cachedData = $this->cacheStore->get($cacheKey);
       if ($cachedData !== null) {
           $cacheResponseTime = microtime(true) - $weeksModeStartTime;
           error_log("[Cache] Cache hit for key: {$cacheKey}");
           error_log("[WEEKS-PERFORMANCE] Total execution time (from cache): " . round($cacheResponseTime, 3) . " seconds");
           return $cachedData;
       }
       
       error_log("[Cache] Cache miss for key: {$cacheKey}");
   } else {
       error_log("[Cache] Force refresh requested, skipping cache check");
   }
   ```
   - **Примечания:**
     - Формат дат: `Y-m-d\TH:i:s\Z` (ISO-8601 с UTC, как в `DatePeriodHelper::getFourWeeksBounds()`)
     - Использовать `$this->cacheStore->generateKey()` (обёртка над `GraphAdmissionClosureCache::generateKey()`)
     - Логирование производительности добавлено для консистентности с режимом "months"

2. **Добавить измерение времени выполнения для кеша**
   - Измерять время выполнения при попадании в кеш
   - Логировать время выполнения

**Критерии приёмки:**
- [ ] Проверка кеша добавлена для режима "weeks"
- [ ] Параметр `forceRefresh` учитывается
- [ ] Генерация ключа кеша корректна
- [ ] Логирование добавлено

---

### Подзадача 3.2: Добавление сохранения результатов в кеш

**Цель:** Добавить сохранение результатов в кеш после обработки запроса для режима "weeks".

**Шаги:**

1. **Добавить сохранение в кеш после формирования ответа**
   - **Местоположение:** После строки 219 (после формирования `$response`)
   - Сохранять только для режима "weeks"
   - Использовать TTL: 120 секунд (2 минуты)
   - Сохранять только успешные ответы (когда `$response['success'] === true`)
   ```php
   // После формирования ответа в методе handle()
   // TASK-068-03: Сохранение в кеш для режима "weeks"
   if ($periodMode === 'weeks' && isset($response['success']) && $response['success'] === true) {
       $cacheKey = $this->cacheStore->generateKey([
           'product' => $product,
           'periodMode' => 'weeks',
           'weekStartUtc' => $weekStart->format('Y-m-d\TH:i:s\Z'),
           'weekEndUtc' => $weekEnd->format('Y-m-d\TH:i:s\Z'),
           'includeTickets' => $includeTickets,
           'includeNewTicketsByStages' => $includeNewTicketsByStages,
           'includeCarryoverTickets' => $includeCarryoverTickets,
           'includeCarryoverTicketsByDuration' => $includeCarryoverTicketsByDuration
       ]);
       
       if ($this->cacheStore->set($cacheKey, $response, 120)) {
           error_log("[Cache] Cache saved for key: {$cacheKey}");
       } else {
           error_log("[Cache] Failed to save cache for key: {$cacheKey}");
       }
       
       // Периодическая очистка устаревших кешей (каждый 10-й запрос)
       if (rand(1, 10) === 1) {
           $deleted = $this->cacheStore->clearExpired();
           if ($deleted > 0) {
               error_log("[Cache] Cleared {$deleted} expired cache entries");
           }
       }
       
       // TASK-068-04: Логирование времени выполнения
       $weeksModeTotalTime = microtime(true) - $weeksModeStartTime;
       error_log("[WEEKS-PERFORMANCE] Total execution time: " . round($weeksModeTotalTime, 2) . " seconds");
   }
   ```
   - **Важные детали:**
     - Проверять `$response['success']` перед сохранением (не сохранять ошибки)
     - TTL: 120 секунд (2 минуты) — короче, чем для режима "months" (300 секунд), так как данные меняются чаще
     - Периодическая очистка: каждый 10-й запрос (аналогично режиму "months", строка 481)
     - Логирование производительности добавлено для консистентности

2. **Добавить обработку ошибок**
   - Не прерывать выполнение при ошибке сохранения кеша
   - Логировать ошибки

**Критерии приёмки:**
- [ ] Сохранение в кеш добавлено для режима "weeks"
- [ ] TTL установлен на 120 секунд
- [ ] Обработка ошибок реализована
- [ ] Логирование добавлено

---

### Подзадача 3.3: Добавление периодической очистки устаревших кешей

**Цель:** Добавить периодическую очистку устаревших кешей для режима "weeks".

**Шаги:**

1. **Добавить очистку устаревших кешей**
   - Выполнять очистку периодически (каждый 10-й запрос)
   - Очищать кеши обоих режимов (weeks и months)
   ```php
   // После сохранения в кеш
   if ($periodMode === 'weeks') {
       // ... сохранение в кеш ...
       
       // Периодическая очистка устаревших кешей (каждый 10-й запрос)
       if (rand(1, 10) === 1) {
           $deleted = $this->cacheStore->clearExpired();
           if ($deleted > 0) {
               error_log("[Cache] Cleared {$deleted} expired cache entries");
           }
       }
   }
   ```

2. **Убедиться, что очистка работает для обоих режимов**
   - Метод `clearExpired()` должен очищать кеши обоих режимов

**Критерии приёмки:**
- [ ] Периодическая очистка добавлена
- [ ] Очистка работает для обоих режимов
- [ ] Логирование добавлено

---

### Подзадача 3.4: Рефакторинг для избежания дублирования кода

**Цель:** Вынести общую логику работы с кешем в отдельные методы для избежания дублирования.

**Шаги:**

1. **Создать приватные методы для работы с кешем**
   - **Местоположение:** В конце класса `GraphAdmissionClosureService` (после метода `handleMonthsMode()`)
   - `getCacheKeyForWeeks()` — генерация ключа кеша для режима "weeks"
   - **Цель:** Избежать дублирования кода генерации ключа
   ```php
   /**
    * Генерация ключа кеша для режима "weeks"
    * 
    * @param array $payload Параметры запроса (payload)
    * @param DateTimeImmutable $weekStart Начало недели
    * @param DateTimeImmutable $weekEnd Конец недели
    * @return string Ключ кеша
    */
   private function getCacheKeyForWeeks(array $payload, DateTimeImmutable $weekStart, DateTimeImmutable $weekEnd): string
   {
       return $this->cacheStore->generateKey([
           'product' => $payload['product'] ?? '1C',
           'periodMode' => 'weeks',
           'weekStartUtc' => $weekStart->format('Y-m-d\TH:i:s\Z'),
           'weekEndUtc' => $weekEnd->format('Y-m-d\TH:i:s\Z'),
           'includeTickets' => $payload['includeTickets'] ?? false,
           'includeNewTicketsByStages' => $payload['includeNewTicketsByStages'] ?? false,
           'includeCarryoverTickets' => $payload['includeCarryoverTickets'] ?? false,
           'includeCarryoverTicketsByDuration' => $payload['includeCarryoverTicketsByDuration'] ?? false
       ]);
   }
   ```
   - **Использование:**
     ```php
     // Вместо дублирования кода
     $cacheKey = $this->getCacheKeyForWeeks($payload, $weekStart, $weekEnd);
     ```
   - **Примечание:** Использовать `$payload` вместо отдельных переменных для консистентности с методом `handleMonthsMode()`

2. **Использовать методы в коде**
   - Заменить дублирующийся код вызовами методов

**Критерии приёмки:**
- [ ] Вспомогательные методы созданы
- [ ] Дублирование кода устранено
- [ ] Код стал более читаемым

---

## 🔧 Технические требования

### Изменения в коде:

1. **Добавить проверку кеша в начале метода `handle()`**
   - Только для режима "weeks"
   - Учитывать параметр `forceRefresh`

2. **Добавить сохранение в кеш после формирования ответа**
   - TTL: 120 секунд
   - Только для успешных ответов

3. **Добавить периодическую очистку устаревших кешей**
   - Каждый 10-й запрос
   - Очистка обоих режимов

4. **Добавить логирование**
   - Cache hit/miss
   - Сохранение кеша
   - Очистка устаревших кешей

---

## ✅ Критерии приёмки

### Общие критерии:
- [ ] Проверка кеша добавлена для режима "weeks" (после строки 54)
- [ ] Сохранение в кеш добавлено для режима "weeks" (после формирования ответа)
- [ ] TTL установлен на 120 секунд (второй параметр `set()`)
- [ ] Параметр `forceRefresh` работает (пропуск проверки кеша)
- [ ] Периодическая очистка устаревших кешей добавлена (каждый 10-й запрос)
- [ ] Логирование добавлено для всех операций (cache hit/miss/saved)
- [ ] Обработка ошибок реализована (не прерывать выполнение при ошибке сохранения)
- [ ] Дублирование кода устранено (метод `getCacheKeyForWeeks()`)
- [ ] Проверка успешности ответа перед сохранением (`$response['success'] === true`)
- [ ] Код соответствует стандартам PSR-12
- [ ] Комментарии добавлены с ссылками на TASK-068-03

### Детальная проверка:

**Проверка места вставки кода:**
- Проверка кеша: После строки 54 (`$weeks = $this->dateHelper->getFourWeeksBounds(...)`)
- Сохранение в кеш: После строки 219 (после формирования `$response`)

**Проверка логики:**
```php
// Проверка кеша должна быть ДО загрузки данных
if (!$forceRefresh) {
    // Проверка кеша
    // Если найден - return
}
// Загрузка данных (только если кеш не найден)

// Сохранение в кеш должно быть ПОСЛЕ формирования ответа
if ($response['success'] === true) {
    // Сохранение в кеш
}
```

**Проверка TTL:**
```php
$this->cacheStore->set($cacheKey, $response, 120); // TTL: 120 секунд
// Должно быть 120, а не 300 (как для режима "months")
```

---

## 🧪 Тестирование

### Функциональное тестирование:

1. **Тест проверки кеша:**
   - Отправить запрос с `periodMode: 'weeks'`
   - Проверить, что выполняется проверка кеша
   - Проверить логи: `[Cache] Cache miss for key: ...`

2. **Тест сохранения в кеш:**
   - Отправить запрос с `periodMode: 'weeks'`
   - Проверить, что данные сохраняются в кеш
   - Проверить логи: `[Cache] Cache saved for key: ...`

3. **Тест cache hit:**
   - Отправить запрос с `periodMode: 'weeks'`
   - Дождаться сохранения в кеш
   - Отправить тот же запрос повторно
   - Проверить, что данные возвращаются из кеша
   - Проверить логи: `[Cache] Cache hit for key: ...`

4. **Тест forceRefresh:**
   - Отправить запрос с `periodMode: 'weeks'` и `forceRefresh: true`
   - Проверить, что кеш пропускается
   - Проверить логи: `[Cache] Force refresh requested, skipping cache check`

5. **Тест очистки устаревших кешей:**
   - Создать истёкший кеш
   - Отправить несколько запросов
   - Проверить, что устаревшие кеши очищаются
   - Проверить логи: `[Cache] Cleared N expired cache entries`

---

## 📝 Примеры кода

### Обновлённый метод handle() (фрагмент):

```php
public function handle(array $payload): array
{
    $product = $payload['product'] ?? '1C';
    $weekStartParam = $payload['weekStartUtc'] ?? null;
    $weekEndParam = $payload['weekEndUtc'] ?? null;
    $periodMode = $payload['periodMode'] ?? 'weeks';
    $includeTickets = $payload['includeTickets'] ?? false;
    $includeNewTicketsByStages = $payload['includeNewTicketsByStages'] ?? false;
    $includeCarryoverTickets = $payload['includeCarryoverTickets'] ?? false;
    $includeCarryoverTicketsByDuration = $payload['includeCarryoverTicketsByDuration'] ?? false;
    $forceRefresh = $payload['forceRefresh'] ?? false;
    $debug = $payload['debug'] ?? false;

    // Валидация periodMode
    if (!in_array($periodMode, ['weeks', 'months'], true)) {
        throw new InvalidArgumentException('Invalid periodMode. Must be "weeks" or "months".');
    }

    // Обработка режима months
    if ($periodMode === 'months') {
        return $this->handleMonthsMode($payload);
    }

    // Недельный режим
    $weeksModeStartTime = microtime(true); // TASK-068-04: Начало измерения времени (перед проверкой кеша)
    
    [$weekStart, $weekEnd] = $this->dateHelper->getWeekBounds($weekStartParam, $weekEndParam);
    $weeks = $this->dateHelper->getFourWeeksBounds($weekStart, $weekEnd);

    // TASK-068-03: Проверка кеша для режима "weeks" (если не forceRefresh)
    if (!$forceRefresh) {
        $cacheKey = $this->getCacheKeyForWeeks($payload, $weekStart, $weekEnd);
        
        $cachedData = $this->cacheStore->get($cacheKey);
        if ($cachedData !== null) {
            $cacheResponseTime = microtime(true) - $weeksModeStartTime;
            error_log("[Cache] Cache hit for key: {$cacheKey}");
            error_log("[WEEKS-PERFORMANCE] Total execution time (from cache): " . round($cacheResponseTime, 3) . " seconds");
            return $cachedData;
        }
        
        error_log("[Cache] Cache miss for key: {$cacheKey}");
    } else {
        error_log("[Cache] Force refresh requested, skipping cache check");
    }

    // Загрузка тикетов через BitrixClient (строки 56-80 в текущей реализации)
    $periodStart = $weeks[0]['weekStart'];
    $periodEnd = $weeks[count($weeks) - 1]['weekEnd'];

    $createdTickets = $this->bitrixClient->fetchCreatedTickets($periodStart, $periodEnd, $debug);
    $closedTickets = $this->bitrixClient->fetchClosedTickets(
        $periodStart,
        $periodEnd,
        $this->config->getClosingStages(),
        $debug
    );

    $allTickets = $this->bitrixClient->mergeTickets($createdTickets, $closedTickets);

    if ($includeCarryoverTickets) {
        $carryoverTickets = $this->bitrixClient->fetchCarryoverTickets(
            $this->config->getTargetStages(),
            $periodEnd,
            $debug
        );
        $allTickets = $this->bitrixClient->mergeTickets($allTickets, $carryoverTickets);
    }

    // Фильтрация по product=1C
    $tickets = $this->bitrixClient->filterByProduct($allTickets, $product);

    // Агрегация через Aggregator (строки 82-92)
    $allStages = $this->config->getStagesWithMeta();
    $aggregated = $this->aggregator->aggregateWeeks(
        $weeks,
        $tickets,
        $this->config->getTargetStages(),
        $this->config->getClosingStages(),
        $this->config->getKeeperId(),
        $debug,
        $allStages
    );

    // Формирование ответа (строки 94-219)

    $response = [
        'success' => true,
        'meta' => [...],
        'data' => [...]
    ];

    // TASK-068-03: Сохранение в кеш для режима "weeks"
    // Сохраняем только успешные ответы
    if (isset($response['success']) && $response['success'] === true) {
        $cacheKey = $this->getCacheKeyForWeeks($payload, $weekStart, $weekEnd);
        
        if ($this->cacheStore->set($cacheKey, $response, 120)) {
            error_log("[Cache] Cache saved for key: {$cacheKey}");
        } else {
            error_log("[Cache] Failed to save cache for key: {$cacheKey}");
        }

        // Периодическая очистка устаревших кешей (каждый 10-й запрос)
        if (rand(1, 10) === 1) {
            $deleted = $this->cacheStore->clearExpired();
            if ($deleted > 0) {
                error_log("[Cache] Cleared {$deleted} expired cache entries");
            }
        }
    }

    // TASK-068-04: Логирование общего времени выполнения
    $weeksModeTotalTime = microtime(true) - $weeksModeStartTime;
    error_log("[WEEKS-PERFORMANCE] Total execution time: " . round($weeksModeTotalTime, 2) . " seconds");

    return $response;
}
```

---

## ⚠️ Потенциальные проблемы и решения

### Проблема 1: Дублирование генерации ключа кеша

**Проблема:** Ключ кеша генерируется дважды (при проверке и при сохранении).

**Решение:**
- Создать приватный метод `getCacheKeyForWeeks()` для избежания дублирования
- Сохранить ключ в переменную при первой генерации (если не используется метод)

**Пример:**
```php
// Вариант 1: Сохранение в переменную
$cacheKey = $this->getCacheKeyForWeeks($payload, $weekStart, $weekEnd);

// Вариант 2: Использование метода дважды (менее эффективно, но проще)
if (!$forceRefresh) {
    $cacheKey = $this->getCacheKeyForWeeks($payload, $weekStart, $weekEnd);
    // ...
}

// Позже
$cacheKey = $this->getCacheKeyForWeeks($payload, $weekStart, $weekEnd); // Дублирование
```

### Проблема 2: Сохранение ошибок в кеш

**Проблема:** Если ответ содержит ошибку (`success: false`), он не должен сохраняться в кеш.

**Решение:**
- Проверять `$response['success'] === true` перед сохранением
- Логировать случаи, когда ответ не сохраняется

**Пример:**
```php
if (isset($response['success']) && $response['success'] === true) {
    // Сохранять только успешные ответы
    $this->cacheStore->set($cacheKey, $response, 120);
} else {
    error_log("[Cache] Skipping cache save for failed response");
}
```

### Проблема 3: Измерение времени при cache hit

**Проблема:** Время должно измеряться с самого начала, даже если данные возвращаются из кеша.

**Решение:**
- Начинать измерение времени ДО проверки кеша
- Включать время проверки кеша в общее время выполнения

**Пример:**
```php
$weeksModeStartTime = microtime(true); // ДО проверки кеша

// Проверка кеша
$cachedData = $this->cacheStore->get($cacheKey);
if ($cachedData !== null) {
    $cacheResponseTime = microtime(true) - $weeksModeStartTime; // Включает время проверки
    // ...
}
```

---

## 🔗 Связанные документы

- [TASK-068: Добавление кеширования для режима "4 недели"](./TASK-068-cache-for-weeks-mode.md)
- [TASK-068-01: Расширение GraphAdmissionClosureCache для поддержки режима "weeks"](./TASK-068-01-extend-cache-for-weeks.md)
- [TASK-068-02: Обновление генерации ключа кеша для учёта weekStartUtc/weekEndUtc](./TASK-068-02-update-cache-key-generation.md)
- [GraphAdmissionClosureService::handleMonthsMode()](../../../api/graph-admission-closure/service/GraphAdmissionClosureService.php) (строки 232-493)

---

## 📊 История правок

- **2025-12-23 15:01 (UTC+3, Брест):** Создана подзадача для модификации GraphAdmissionClosureService

---

**Автор:** Технический писатель  
**Версия документа:** 1.0

