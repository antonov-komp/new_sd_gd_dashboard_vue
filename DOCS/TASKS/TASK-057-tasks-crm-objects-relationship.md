# TASK-057: Поиск связи Задач с Объектами CRM в Bitrix24

**Дата создания:** 2025-12-17 14:30 (UTC+3, Брест)  
**Дата завершения:** 2025-12-17 14:50 (UTC+3, Брест)  
**Статус:** ✅ Завершён  
**Приоритет:** Средний  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Связанные задачи:** 
- TASK-050 (Трудозатраты на Тикеты сектора 1С) — используется матчинг задач с тикетами
- TASK-042 (Popup level 2 tickets list) — попап с задачами

---

## Цель

Найти в документации Bitrix24 точное описание связи Задач (Tasks) с Объектами CRM и проверить эту связь на реальных данных. Подготовить тестовый запрос для проверки связи задач с тикетами (CRM объекты типа 140).

---

## Контекст

- В модуле «Трудозатраты на Тикеты сектора 1С» (TASK-050) используется матчинг задач с тикетами
- В коде уже используется поле `ufCrmTask` в формате `["T8c_3093"]`, где:
  - `8c` = 140 (тип сущности сервис деск) в hex
  - `3093` = ID тикета
- Первый попап теперь открывает список задач как карточки
- Необходимо подтвердить правильность использования поля связи и найти официальную документацию

---

## Задачи этапа

### 1. Изучение документации Bitrix24

**Источники документации:**
- https://context7.com/bitrix24/rest/ (общая документация REST API)
- https://apidocs.bitrix24.ru/ (официальная документация API)
- Документация по методу `tasks.task.get`
- Документация по полю `UF_CRM_TASK` или `ufCrmTask`

**Что нужно найти:**
- Официальное описание поля связи задачи с CRM объектом
- Формат значения поля (например, `["T8c_3093"]` или другой)
- Типы префиксов для разных типов CRM объектов:
  - `T8c_` — для сервис деска (тип 140)
  - `D_` — для сделок (Deal)
  - `L_` — для лидов (Lead)
  - `C_` — для контактов (Contact)
  - `CO_` — для компаний (Company)
- Метод получения задачи с полем связи: `tasks.task.get` с параметром `select: ['*', 'UF_*']`

### 2. Анализ существующего кода

**Файлы для анализа:**
- `api/tickets-time-tracking-sector-1c.php` — функция `matchTasksWithTickets()` (строки 418-474)
- Использование поля `ufCrmTask` в формате `["T8c_3093"]`

**Текущая реализация:**
```php
// Поле ufCrmTask содержит массив строк формата ["T8c_3093"]
// где 8c = 140 (тип сущности) в hex, 3093 = ID тикета
if (isset($task['ufCrmTask']) && is_array($task['ufCrmTask']) && !empty($task['ufCrmTask'])) {
    $ufCrmTaskValue = $task['ufCrmTask'][0] ?? null;
    if ($ufCrmTaskValue && preg_match('/T8c_(\d+)/', $ufCrmTaskValue, $matches)) {
        $ticketId = (int)$matches[1];
    }
}
```

**Вопросы для уточнения:**
- Правильно ли используется поле `ufCrmTask`?
- Есть ли альтернативные поля связи?
- Какой точный формат значения поля?

### 3. Подготовка тестового скрипта

**Файл:** `api/test-tasks-crm-relationship.php` (временный файл для тестирования)

**Функционал:**
- Получение задач по списку ID (от пользователя)
- Извлечение поля связи с CRM объектом
- Парсинг значения поля для получения ID тикета
- Проверка связи с тикетом через `crm.item.get`
- Вывод результатов в читаемом формате

**Структура тестового скрипта:**
```php
<?php
/**
 * Тестовый скрипт для проверки связи задач с CRM объектами
 * 
 * Использование:
 * POST /api/test-tasks-crm-relationship.php
 * Body: { "taskIds": [123, 456, 789] }
 */

require_once __DIR__ . '/../crest.php';

header('Content-Type: application/json; charset=utf-8');

function jsonResponse($data) {
    echo json_encode($data, JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT);
    exit;
}

// Получение списка ID задач из запроса
$input = file_get_contents('php://input');
$body = json_decode($input, true);
$taskIds = $body['taskIds'] ?? [];

if (empty($taskIds)) {
    jsonResponse([
        'error' => 'No task IDs provided',
        'message' => 'Please provide taskIds array in request body'
    ]);
}

// Получение задач батч-запросом
$batchData = [];
foreach ($taskIds as $taskId) {
    $batchData["task_{$taskId}"] = [
        'method' => 'tasks.task.get',
        'params' => [
            'taskId' => (int)$taskId,
            'select' => ['*', 'UF_*'] // Получаем все поля, включая пользовательские
        ]
    ];
}

$result = CRest::callBatch($batchData);

if (isset($result['error'])) {
    jsonResponse([
        'error' => $result['error'],
        'error_description' => $result['error_description'] ?? 'Unknown error'
    ]);
}

// Анализ результатов
$analysis = [];
foreach ($taskIds as $taskId) {
    $key = "task_{$taskId}";
    $taskData = $result['result']['result'][$key] ?? null;
    
    if (!$taskData || isset($taskData['error'])) {
        $analysis[$taskId] = [
            'status' => 'error',
            'error' => $taskData['error'] ?? 'Task not found',
            'taskData' => null
        ];
        continue;
    }
    
    // Поиск поля связи с CRM
    $crmFields = [];
    $crmObjectId = null;
    $crmObjectType = null;
    
    // Проверка различных вариантов полей
    $possibleFields = [
        'ufCrmTask',
        'UF_CRM_TASK',
        'ufCrmTicketId',
        'UF_CRM_TICKET_ID',
        'UF_CRM_140_ID',
        'ufCrm140Id'
    ];
    
    foreach ($possibleFields as $field) {
        if (isset($taskData[$field])) {
            $crmFields[$field] = $taskData[$field];
            
            // Парсинг значения для получения ID тикета
            $value = $taskData[$field];
            
            // Формат ["T8c_3093"] где 8c = 140 в hex, 3093 = ID тикета
            if (is_array($value) && !empty($value)) {
                $stringValue = $value[0] ?? null;
                if ($stringValue && preg_match('/T8c_(\d+)/', $stringValue, $matches)) {
                    $crmObjectId = (int)$matches[1];
                    $crmObjectType = 140; // Сервис деск
                }
                // Другие форматы (D_, L_, C_, CO_)
                elseif ($stringValue && preg_match('/T([0-9a-f]+)_(\d+)/i', $stringValue, $matches)) {
                    $crmObjectType = hexdec($matches[1]);
                    $crmObjectId = (int)$matches[2];
                }
            }
            // Прямое значение ID
            elseif (is_numeric($value)) {
                $crmObjectId = (int)$value;
                $crmObjectType = 140; // Предполагаем сервис деск
            }
        }
    }
    
    // Проверка связи с тикетом (если найден ID)
    $ticketData = null;
    if ($crmObjectId && $crmObjectType === 140) {
        $ticketResult = CRest::call('crm.item.get', [
            'entityTypeId' => 140,
            'id' => $crmObjectId,
            'select' => ['id', 'title', 'createdTime', 'UF_CRM_7_TYPE_PRODUCT']
        ]);
        
        if (!isset($ticketResult['error'])) {
            $ticketData = $ticketResult['result']['item'] ?? null;
        }
    }
    
    $analysis[$taskId] = [
        'status' => 'success',
        'taskId' => $taskId,
        'taskTitle' => $taskData['title'] ?? null,
        'crmFields' => $crmFields,
        'crmObjectId' => $crmObjectId,
        'crmObjectType' => $crmObjectType,
        'ticketData' => $ticketData,
        'isLinked' => $ticketData !== null,
        'rawTaskData' => $taskData // Для анализа всех полей
    ];
}

jsonResponse([
    'summary' => [
        'totalTasks' => count($taskIds),
        'successful' => count(array_filter($analysis, fn($a) => $a['status'] === 'success')),
        'errors' => count(array_filter($analysis, fn($a) => $a['status'] === 'error')),
        'linked' => count(array_filter($analysis, fn($a) => $a['isLinked'] ?? false))
    ],
    'analysis' => $analysis
]);
```

### 4. Тестирование с реальными данными

**Ожидается от пользователя:**
- Список ID задач, которые точно связаны с CRM (тикетами)
- Эти задачи уже используются в системе и точно имеют связь с тикетами

**Процесс тестирования:**
1. Получить список ID задач от пользователя
2. Выполнить тестовый запрос через скрипт `test-tasks-crm-relationship.php`
3. Проанализировать результаты:
   - Какое поле используется для связи
   - Формат значения поля
   - Корректность парсинга ID тикета
   - Успешность получения данных тикета
4. Зафиксировать результаты в документации

---

## Ожидаемые результаты

### 1. Документация по связи задач с CRM

**Создать документ:** `DOCS/ANALYSIS/tasks-crm-relationship.md`

**Содержание:**
- Официальное описание поля связи из документации Bitrix24
- Формат значения поля (примеры)
- Типы префиксов для разных CRM объектов
- Метод получения задачи с полем связи

### 2. Результаты тестирования

**Создать документ:** `DOCS/TESTING/tasks-crm-relationship-test-results.md`

**Содержание:**
- Список протестированных задач
- Найденные поля связи
- Формат значений полей
- Результаты проверки связи с тикетами
- Выводы и рекомендации

### 3. Обновление кода (если необходимо)

**Если найдены несоответствия:**
- Обновить функцию `matchTasksWithTickets()` в `api/tickets-time-tracking-sector-1c.php`
- Исправить парсинг поля связи
- Добавить обработку альтернативных форматов

---

## Критерии приёмки

- [x] Изучена официальная документация Bitrix24 по связи задач с CRM объектами
- [x] Найдено точное описание поля связи (название, формат значения)
  - Поле: `ufCrmTask`
  - Формат: `["T8c_XXXX"]`, где `8c` = 140 (hex), `XXXX` = ID тикета
- [x] Создан тестовый скрипт `api/test-tasks-crm-relationship.php`
- [x] Получен список ID задач от пользователя для тестирования (73885, 73881, 74110)
- [x] Выполнен тестовый запрос с реальными данными
- [x] Проанализированы результаты тестирования
- [x] Создана документация с результатами (`DOCS/ANALYSIS/tasks-crm-relationship.md`)
- [x] Создан отчёт о тестировании (`DOCS/TESTING/tasks-crm-relationship-test-results.md`)
- [x] Обновлён код (если найдены несоответствия)
  - Текущая реализация в `api/tickets-time-tracking-sector-1c.php` корректна
  - Добавлена обработка структуры ответа батч-запроса в тестовом скрипте

---

## Вопросы для уточнения

### 🔴 Критические вопросы

1. **Список ID задач для тестирования:**
   - Какой список ID задач предоставить для тестирования?
   - Сколько задач нужно протестировать (минимум/максимум)?

2. **Формат запроса:**
   - Какой формат запроса использовать (POST с JSON body)?
   - Нужна ли авторизация для тестового скрипта?

### 🟡 Важные вопросы

3. **Документация:**
   - Есть ли доступ к официальной документации Bitrix24?
   - Нужно ли искать документацию на русском или английском языке?

4. **Обработка ошибок:**
   - Как обрабатывать случаи, когда задача не найдена?
   - Как обрабатывать случаи, когда тикет не найден?

---

## Ссылки на документацию

### Официальная документация Bitrix24

- **Метод получения задачи:**
  - https://context7.com/bitrix24/rest/tasks.task.get
  - https://apidocs.bitrix24.ru/rest/tasks.task.get

- **Пользовательские поля (UF_*):**
  - https://context7.com/bitrix24/rest/tasks.task.get (раздел про select)
  - https://dev.1c-bitrix.ru/api_help/tasks/tasks/tasks/tasks_task_get.php

- **Связь задач с CRM:**
  - Требуется найти в документации описание поля `UF_CRM_TASK` или `ufCrmTask`

### Существующий код

- `api/tickets-time-tracking-sector-1c.php` — функция `matchTasksWithTickets()` (строки 418-474)
- Использование поля `ufCrmTask` в формате `["T8c_3093"]`

---

## Результаты тестирования

### Тестовые задачи
- **73885** → Тикет #4872 ✅
- **73881** → (связана с тикетом) ✅
- **74110** → (связана с тикетом) ✅

### Статистика
- **Всего задач:** 3
- **Успешно обработано:** 3
- **Связано с тикетами:** 3 (100%)
- **Поле `ufCrmTask` найдено:** 3 (100%)

### Выводы

1. **Поле связи подтверждено:**
   - Поле: `ufCrmTask`
   - Формат: `["T8c_XXXX"]`, где `8c` = 140 (hex), `XXXX` = ID тикета
   - Пример: `["T8c_4872"]` → Тикет #4872

2. **Текущая реализация корректна:**
   - Используется правильное поле `ufCrmTask`
   - Парсинг выполняется корректно через регулярное выражение `/T8c_(\d+)/`
   - Структура батч-запроса обрабатывается правильно

3. **Документация создана:**
   - `DOCS/ANALYSIS/tasks-crm-relationship.md` — анализ связи задач с CRM
   - `DOCS/TESTING/tasks-crm-relationship-test-results.md` — результаты тестирования

---

## История правок

- **2025-12-17 14:30 (UTC+3, Брест):** Создан черновик задачи TASK-057
  - Добавлена цель и контекст
  - Описаны задачи этапа
  - Создан тестовый скрипт для проверки связи
  - Добавлены критерии приёмки и вопросы для уточнения

- **2025-12-17 14:50 (UTC+3, Брест):** Задача завершена
  - Выполнен тестовый запрос с реальными данными (задачи: 73885, 73881, 74110)
  - Подтверждена связь задач с тикетами через поле `ufCrmTask`
  - Создана документация с результатами анализа и тестирования
  - Подтверждена корректность текущей реализации в коде

---

## Примечания

- **Статус:** ✅ Завершён — связь задач с CRM объектами подтверждена
- **Результаты:** Все тестовые задачи успешно связаны с тикетами
- **Документация:** Создана полная документация по связи задач с CRM объектами

