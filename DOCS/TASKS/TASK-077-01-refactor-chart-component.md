# TASK-077-01: Рефакторинг GraphAdmissionClosureChart.vue

**Дата создания:** 2025-12-24 17:15 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-077 (Рефакторинг крупных компонентов)

---

## 📌 Краткое резюме

**Что реализуется:**
- Рефакторинг основного компонента `GraphAdmissionClosureChart.vue` (2650 строк)
- Разбиение на 5 меньших компонентов и 4 composables
- Создание переиспользуемой архитектуры для графиков

**Текущая структура (2650 строк):**
- Шаблон: заголовок + сводная секция + графики + контролы
- Скрипт: 30+ computed-свойств + логика графиков + обработчики событий
- Стили: комплексная стилизация всех элементов

**Целевая структура:**
- 5 компонентов по 200-600 строк каждый
- 4 composables для переиспользования логики
- Четкое разделение ответственности

**Время выполнения:** ~12-16 часов  
**Сложность:** Средняя-Высокая

---

## 📋 Описание

Компонент `GraphAdmissionClosureChart.vue` является основным компонентом системы графиков. Он содержит слишком много логики в одном файле, что затрудняет поддержку и развитие.

### Текущие проблемы:

1. **Монолитность:** 2650 строк в одном файле
2. **Смешанная ответственность:** UI, бизнес-логика, API, стили в одном месте
3. **Дублирование:** Логика обработки данных повторяется
4. **Сложность тестирования:** Трудно тестировать отдельные части

### Цели рефакторинга:

1. **Разбиение на компоненты:** Создать 5 меньших, фокусированных компонентов
2. **Извлечение composables:** Вынести логику в переиспользуемые composables
3. **Улучшение структуры:** Четкое разделение ответственности
4. **Повышение поддерживаемости:** Легче изменять и тестировать отдельные части

---

## 🎯 Контекст

### Текущая структура GraphAdmissionClosureChart.vue:

```
GraphAdmissionClosureChart.vue (2650 строк)
├── Шаблон (317 строк)
│   ├── Заголовок с метаданными недели
│   ├── Контролы типов графиков
│   ├── Сводная секция (текущая + предыдущая неделя)
│   └── Контейнер графиков (line/bar/doughnut)
├── Скрипт (2300+ строк)
│   ├── Props & Emits
│   ├── 30+ computed-свойств для данных графиков
│   ├── Логика обработки метрик
│   ├── Расчет процентов и трендов
│   ├── Обработчики событий
│   └── Вспомогательные функции
└── Стили (30+ строк)
    └── Стилизация всех элементов
```

### Архитектура после рефакторинга:

```
chart/
├── ChartHeader.vue (200 строк) - заголовок и контролы
├── ChartSummary.vue (600 строк) - сводные метрики недели
├── ChartContainer.vue (300 строк) - контейнер графиков
├── ChartFooter.vue (150 строк) - дополнительная информация
└── composables/
    ├── useChartData.js (400 строк) - данные графиков
    ├── useChartControls.js (200 строк) - управление типами
    ├── useMetricsCalculation.js (300 строк) - расчет метрик
    └── useWeekComparison.js (250 строк) - сравнение недель
```

---

## 🏗️ Модули и компоненты

### Новые компоненты:

#### 1. ChartHeader.vue (200 строк)
**Назначение:** Заголовок компонента с метаданными недели и контролы типов графиков.

**Функциональность:**
- Отображение заголовка "График приёма и закрытий сектора 1С"
- Метаданные текущей недели (номер, даты в UTC)
- Переключатель типов графиков (Line, Bar, Doughnut) с иконками
- Адаптивная верстка (вертикальная ориентация на мобильных)
- Обработка клавиатурной навигации (Tab, Enter, Space)

**Технические детали:**
- Использует CSS Grid для адаптивной раскладки
- Поддержка темной/светлой темы через CSS переменные
- Анимации при переключении типов графиков
- Доступность (ARIA labels, keyboard navigation)

**Интерфейс:**
```javascript
// Props
{
  meta: {
    weekNumber: Number,
    weekStartUtc: String,
    weekEndUtc: String
  },
  chartType: String // 'line' | 'bar' | 'doughnut'
}

// Events
{
  'update:chartType': String // новое значение типа графика
}
```

#### 2. ChartSummary.vue (600 строк)
**Назначение:** Сводная информация по текущей и предыдущей неделе с интерактивными элементами.

**Функциональность:**
- **Карточки текущей недели:**
  - Новые тикеты (с индикатором изменения)
  - Закрытые тикеты (с разбивкой по типам)
  - Переходящие тикеты (с разбивкой по неделям)
  - Стадии закрытия (список с количеством)
- **Карточки предыдущей недели:** Для сравнения с текущей
- **Процентные индикаторы:** Визуализация трендов (рост/падение)
- **Кликабельные карточки:** Открытие модальных окон для детализации
- **Адаптивная сетка:** Автоматическая перестройка на мобильных

**Технические детали:**
- Использует CSS Flexbox/Grid для раскладки карточек
- Анимации hover эффектов и кликов
- Lazy loading для тяжелых вычислений
- Memoization для расчетов процентов

**Интерфейс:**
```javascript
// Props
{
  currentWeekData: Object,     // данные текущей недели
  previousWeekData: Object,    // данные предыдущей недели
  currentWeekPercentages: Object, // проценты для текущей недели
  previousWeekPercentages: Object, // проценты для предыдущей недели
  formatNumber: Function,      // функция форматирования чисел
  formatPercentage: Function   // функция форматирования процентов
}

// Events
{
  'summary-click': String,     // тип карточки ('new', 'closed', 'carryover')
  'previous-week-summary-click': String // тип карточки для предыдущей недели
}
```

**Визуальные компоненты:**
- PercentageIndicator - компонент для отображения процентов
- MetricCard - базовый компонент карточки метрики
- StageBadge - компонент для отображения стадий

#### 3. ChartContainer.vue (300 строк)
**Назначение:** Контейнер для отображения графиков разных типов с адаптивным поведением.

**Функциональность:**
- **Типы графиков:**
  - Line: Два графика рядом (новые+закрытые, переходящие)
  - Bar: Единый столбчатый график
  - Doughnut: Круговая диаграмма с легендой
- **Адаптивность:** Вертикальная раскладка на мобильных устройствах
- **Оптимизация производительности:** Lazy loading графиков
- **Обработка ошибок:** Fallback для случаев, когда данные недоступны

**Технические детали:**
- Динамический импорт компонентов Chart.js для оптимизации бандла
- CSS Grid для раскладки с автоматической перестройкой
- Intersection Observer для отложенной загрузки
- Error boundaries для обработки ошибок рендеринга графиков

**Интерфейс:**
```javascript
// Props
{
  chartType: String,           // 'line' | 'bar' | 'doughnut'
  chartComponent: Object,      // компонент Chart.js
  chartData: Object,           // данные для графика
  chartOptions: Object,        // настройки графика
  newClosedChartData: Object,  // данные для линейного графика новых+закрытых
  carryoverChartData: Object   // данные для линейного графика переходящих
}

// Events
{
  // Пока без events, но можно добавить для взаимодействия с графиком
}
```

**Особенности:**
- Поддержка touch-событий для мобильных устройств
- Экспорт графиков в изображения (будущая функция)
- Анимации при переключении типов графиков

#### 4. ChartFooter.vue (150 строк)
**Назначение:** Дополнительная информация, служебные данные и элементы управления.

**Функциональность:**
- **Информация о периодах:** Даты начала и окончания анализируемого периода
- **Служебная информация:** Время последнего обновления данных, источник данных
- **Экспорт данных:** Кнопки для экспорта графика или данных в CSV/PDF
- **Настройки отображения:** Дополнительные опции визуализации
- **Помощь:** Ссылки на документацию или подсказки

**Технические детали:**
- Использует localStorage для сохранения настроек пользователя
- Lazy loading для тяжелых операций экспорта
- Интеграция с сервисами экспорта данных

**Интерфейс:**
```javascript
// Props
{
  meta: Object,              // метаданные периода
  exportEnabled: Boolean,    // включен ли экспорт
  showHelp: Boolean         // показывать ли подсказки
}

// Events
{
  'export-data': Object,     // запрос на экспорт данных
  'export-chart': Object,    // запрос на экспорт графика
  'settings-change': Object  // изменение настроек
}
```

**Примечание:** В первой итерации рефакторинга ChartFooter может быть минимальным компонентом с базовой информацией. Функциональность экспорта можно добавить позже.

### Новые composables:

#### 1. useChartData.js (400 строк)
**Назначение:** Логика подготовки данных для всех типов графиков с оптимизацией производительности.

**Функциональность:**
- **newClosedChartData:** Данные для графика новых и закрытых тикетов
  - Метки по неделям
  - Два датасета (новые и закрытые)
  - Цветовая схема и стили линий
  - DataLabels для отображения значений
- **carryoverChartData:** Данные для графика переходящих тикетов
  - Один датасет с градиентной заливкой
  - Настройка dataLabels
- **lineBarData:** Универсальные данные для линейных и столбчатых графиков
- **doughnutData:** Данные для круговой диаграммы с распределением по типам
- **chartOptions:** Настройки Chart.js с адаптивностью и оптимизациями

**Технические детали:**
- **Memoization:** Использует computed для автоматического кеширования
- **Оптимизация:** Создание градиентов только при необходимости
- **Валидация:** Проверка входных данных на корректность
- **Обработка ошибок:** Graceful degradation при отсутствии данных

**API:**
```javascript
// Использование
const {
  newClosedChartData,
  carryoverChartData,
  lineBarData,
  doughnutData,
  chartOptions,
  getWeekLabels,
  createGradient
} = useChartData(props);

// Параметры
props: {
  data: Object,    // данные из GraphAdmissionClosureChart
  meta: Object,    // метаданные периода
  chartType: String // тип графика
}
```

**Оптимизации:**
- Debounced пересчет при частых изменениях данных
- Shared references для уменьшения GC pressure
- Tree-shaking совместимый экспорт

#### 2. useChartControls.js (200 строк)
**Назначение:** Управление типами графиков, их переключением и настройками отображения.

**Функциональность:**
- **chartType:** Reactive ref для текущего типа графика ('line', 'bar', 'doughnut')
- **chartTypes:** Статический массив доступных типов с метаданными
- **chartComponent:** Динамический выбор компонента Chart.js на основе типа
- **setChartType:** Метод для изменения типа с валидацией
- **isValidChartType:** Валидация типа графика

**Технические детали:**
- **Reactive state:** Использует Vue 3 reactivity для автоматических обновлений
- **Type safety:** Строгая типизация доступных значений
- **Performance:** Минимизация перерисовок при переключении
- **Persistence:** Сохранение выбранного типа в localStorage

**API:**
```javascript
// Использование
const {
  chartType,
  chartTypes,
  chartComponent,
  setChartType,
  isValidChartType
} = useChartControls(initialType);

// Методы
setChartType('bar'); // Изменить тип графика
isValidChartType('invalid'); // false

// Данные
chartTypes = [
  { value: 'line', label: 'Линейный', icon: '📈' },
  { value: 'bar', label: 'Столбчатый', icon: '📊' },
  { value: 'doughnut', label: 'Круговая', icon: '🍩' }
];
```

**Интеграция:**
- Автоматически синхронизируется с компонентами ChartContainer
- Поддерживает URL параметры для прямых ссылок на типы графиков

#### 3. useMetricsCalculation.js (300 строк)
**Назначение:** Математические расчеты и форматирование числовых данных для отображения.

**Функциональность:**
- **calculatePercentage(current, previous):** Расчет процентного изменения
  - Обработка деления на ноль
  - Проверка на NaN и бесконечности
  - Возврат null для некорректных данных
- **formatPercentage(value):** Форматирование процентов для UI
  - Добавление знака (+/-)
  - Округление до 1 знака после запятой
  - Обработка null значений
- **formatNumber(value):** Форматирование больших чисел
  - Разделители тысяч
  - Сокращения для миллионов (M)
  - Локализация для русского языка
- **getUnitLabel(count):** Получение единиц измерения

**Технические детали:**
- **Pure functions:** Все функции чистые, без побочных эффектов
- **Type safety:** Строгая проверка типов входных параметров
- **Performance:** Оптимизированные алгоритмы для больших чисел
- **Internationalization:** Поддержка локализации чисел

**API:**
```javascript
// Использование
const {
  calculatePercentage,
  formatPercentage,
  formatNumber,
  getUnitLabel
} = useMetricsCalculation();

// Примеры
calculatePercentage(150, 100); // 50 (рост на 50%)
formatPercentage(50); // "+50.0%"
formatNumber(1500000); // "1.5M"
formatNumber(1500); // "1 500"
```

**Edge cases:**
- `calculatePercentage(0, 0)` → 0 (без изменений)
- `calculatePercentage(100, 0)` → null (деление на ноль)
- `calculatePercentage(null, 100)` → null (некорректные данные)

#### 4. useWeekComparison.js (250 строк)
**Назначение:** Логика извлечения и сравнения данных по неделям с расчетом трендов.

**Функциональность:**
- **currentWeekData:** Данные текущей (последней) недели из массива series
- **previousWeekData:** Данные предыдущей недели для сравнения
- **prePreviousWeekData:** Данные предпредыдущей недели для глубокого анализа
- **currentWeekPercentages:** Проценты изменения текущей недели относительно предыдущей
- **previousWeekPercentages:** Проценты изменения предыдущей недели относительно предпредыдущей
- **formatWeekDates(start, end):** Форматирование периода недели для отображения

**Технические детали:**
- **Data extraction:** Умное извлечение данных из сложной структуры series
- **Caching:** Computed properties для автоматического кеширования результатов
- **Error handling:** Graceful degradation при отсутствии данных
- **Performance:** Минимизация вычислений при частых обновлениях

**API:**
```javascript
// Использование
const {
  currentWeekData,
  previousWeekData,
  prePreviousWeekData,
  currentWeekPercentages,
  previousWeekPercentages,
  formatWeekDates
} = useWeekComparison(props);

// Структура данных
currentWeekData = {
  newTickets: 150,
  closedTickets: 120,
  carryoverTickets: 30,
  // ... другие метрики
}

currentWeekPercentages = {
  newTickets: 25.5,      // +25.5% к предыдущей неделе
  closedTickets: -5.2,   // -5.2% к предыдущей неделе
  carryoverTickets: 10.0  // +10.0% к предыдущей неделе
}
```

**Алгоритм извлечения данных:**
1. Найти индекс текущей недели в массиве series
2. Извлечь данные по этому индексу
3. Рассчитать проценты изменения относительно предыдущих периодов
4. Отформатировать для отображения в UI

---

## 📦 Зависимости

### От задач:
- TASK-077: Рефакторинг крупных компонентов (родительская задача)

### От модулей:
- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue` - компонент для рефакторинга
- `vue-app/src/composables/` - директория для новых composables
- Chart.js, Vue ChartJS - для графиков

### От библиотек:
- Vue.js 3.x (Composition API)
- Chart.js & vue-chartjs
- SCSS для стилей

---

## 🎯 Детальный план реализации

### Обзор этапов

Рефакторинг разделен на **8 основных этапов**:

1. **Подготовка:** Создание структуры файлов и базовых composables
2. **ChartHeader:** Извлечение заголовка и контроллов
3. **useChartData:** Создание composable для данных графиков
4. **useMetricsCalculation:** Создание composable для расчетов
5. **useWeekComparison:** Создание composable для сравнения недель
6. **ChartSummary:** Извлечение сводной секции
7. **ChartContainer:** Извлечение контейнера графиков
8. **Финализация:** Обновление основного компонента и тестирование

---

## 📝 Подэтап 1.1: Создание структуры файлов

**Цель:** Подготовить структуру директорий и файлов для рефакторинга.

**Время выполнения:** ~30 минут

### Детальные шаги:

1. **Создать директорию для новых компонентов:**
   ```bash
   mkdir -p vue-app/src/components/graph-admission-closure/chart
   ```

2. **Создать директорию для composables:**
   ```bash
   mkdir -p vue-app/src/composables/graph-admission-closure
   ```

3. **Создать базовые файлы компонентов:**
   ```vue
   <!-- ChartHeader.vue -->
   <template>
     <header class="ac-chart__header">
       <!-- TODO: Реализовать заголовок и контролы -->
       <div>Chart Header - TODO</div>
     </header>
   </template>

   <script setup>
// TODO: Реализовать логику
   </script>

   <style scoped>
   /* TODO: Добавить стили */
   </style>
   ```

4. **Создать базовые composables:**
   ```javascript
   // useChartControls.js
   export function useChartControls() {
     // TODO: Реализовать логику управления типами графиков
     return {
       chartType: 'line',
       chartTypes: [],
       chartComponent: null,
       setChartType: () => {},
       isValidChartType: () => false
     };
   }
   ```

5. **Проверить компиляцию:**
   ```bash
   npm run build  # или yarn build
   # Убедиться, что нет ошибок компиляции
   ```

**Критерии приёмки:**
- [ ] Созданы все директории
- [ ] Созданы все файлы с базовой структурой
- [ ] Файлы компилируются без ошибок

---

## 📝 Подэтап 1.2: Создание базовых composables

**Цель:** Создать каркас composables с базовой функциональностью.

**Время выполнения:** ~1 час

### Детальные шаги:

1. **useChartControls.js:** Базовая логика переключения типов графиков
2. **useMetricsCalculation.js:** Базовые функции форматирования
3. **useWeekComparison.js:** Базовые computed для данных недель
4. **useChartData.js:** Базовые структуры данных графиков

**Критерии приёмки:**
- [ ] Все composables созданы
- [ ] Базовые функции экспортированы
- [ ] Нет ошибок импорта

---

## 📝 Подэтап 2.1: Извлечение ChartHeader

**Цель:** Выделить заголовок и контролы типов графиков в отдельный компонент.

**Время выполнения:** ~1.5 часа

### Детальные шаги:

1. **Извлечь HTML из оригинального компонента:**
   ```bash
   # Найти и скопировать секцию header из GraphAdmissionClosureChart.vue
   # Строки примерно 3-25
   ```

2. **Создать ChartHeader.vue с извлеченным кодом:**
   ```vue
   <template>
     <header class="ac-chart__header">
       <div>
         <h2 class="ac-chart__title">График приёма и закрытий сектора 1С</h2>
         <p class="ac-chart__subtitle">
           Неделя {{ (meta?.currentWeek?.weekNumber ?? meta?.weekNumber) ?? '—' }} ·
           {{ (meta?.currentWeek?.weekStartUtc ?? meta?.weekStartUtc) || '—' }} — {{ (meta?.currentWeek?.weekEndUtc ?? meta?.weekEndUtc) || '—' }} (UTC)
         </p>
       </div>

       <div class="ac-chart__controls">
         <div class="chart-type-selector">
           <button
             v-for="type in chartTypes"
             :key="type.value"
             :class="['chart-type-btn', { active: chartType === type.value }]"
             @click="handleTypeChange(type.value)"
           >
             <span class="chart-type-icon">{{ type.icon }}</span>
             <span class="chart-type-label">{{ type.label }}</span>
           </button>
         </div>
       </div>
     </header>
   </template>
   ```

3. **Добавить props и emits:**
   ```javascript
   import { useChartControls } from '@/composables/graph-admission-closure/useChartControls.js';

   const props = defineProps({
     meta: {
       type: Object,
       required: true
     },
     chartType: {
       type: String,
       required: true,
       validator: (value) => ['line', 'bar', 'doughnut'].includes(value)
     }
   });

   const emit = defineEmits(['update:chartType']);

   // Используем composable для типов графиков
   const { chartTypes } = useChartControls();

   const handleTypeChange = (newType) => {
     emit('update:chartType', newType);
   };
   ```

4. **Копировать стили:**
   ```css
   /* Извлечь стили из оригинального компонента */
   .ac-chart__header { /* ... */ }
   .ac-chart__title { /* ... */ }
   .ac-chart__subtitle { /* ... */ }
   .ac-chart__controls { /* ... */ }
   .chart-type-selector { /* ... */ }
   .chart-type-btn { /* ... */ }
   .chart-type-icon { /* ... */ }
   .chart-type-label { /* ... */ }
   ```

5. **Протестировать компонент:**
   ```vue
   <!-- Временный тестовый компонент -->
   <template>
     <div>
       <ChartHeader
         :meta="testMeta"
         :chart-type="testChartType"
         @update:chart-type="testChartType = $event"
       />
       <p>Текущий тип: {{ testChartType }}</p>
     </div>
   </template>

   <script setup>
   import ChartHeader from './chart/ChartHeader.vue';
   import { ref } from 'vue';

   const testMeta = ref({
     weekNumber: 51,
     weekStartUtc: '2025-12-15',
     weekEndUtc: '2025-12-21'
   });
   const testChartType = ref('line');
   </script>
   ```

**Критерии приёмки:**
- [ ] ChartHeader.vue создан и работает
- [ ] Отображает заголовок и метаданные
- [ ] Переключатель типов графиков функционален
- [ ] Адаптивная верстка работает

---

## 📝 Подэтап 3.1: Создание useChartData composable

**Цель:** Извлечь всю логику подготовки данных графиков.

**Время выполнения:** ~2 часа

### Детальные шаги:

1. **Извлечь chartTypes массив:**
   ```javascript
   const chartTypes = [
     { value: 'line', label: 'Линейный', icon: '📈' },
     { value: 'bar', label: 'Столбчатый', icon: '📊' },
     { value: 'doughnut', label: 'Круговая', icon: '🍩' }
   ];
   ```

2. **Извлечь baseDataLabelConfig:**
   ```javascript
   const baseDataLabelConfig = { /* ... */ };
   ```

3. **Извлечь computed-свойства данных графиков:**
   - `newClosedChartData`
   - `carryoverChartData`
   - `lineBarData`
   - `doughnutData`

4. **Извлечь chartOptions:**
   ```javascript
   const chartOptions = computed(() => ({ /* ... */ }));
   ```

5. **Извлечь вспомогательные функции:**
   - `getWeekLabels()`
   - `createGradient()`

**Критерии приёмки:**
- [ ] useChartData.js содержит все данные графиков
- [ ] Функции возвращают корректные структуры данных
- [ ] Chart.js интеграция работает

---

## 📝 Подэтап 4.1: Создание useMetricsCalculation composable

**Цель:** Извлечь логику расчетов и форматирования.

**Время выполнения:** ~1.5 часа

### Детальные шаги:

1. **Извлечь функции форматирования:**
   ```javascript
   function formatNumber(value) { /* ... */ }
   function formatPercentage(value) { /* ... */ }
   function getUnitLabel(count) { /* ... */ }
   ```

2. **Извлечь функции расчетов:**
   ```javascript
   function calculatePercentage(current, previous) { /* ... */ }
   ```

3. **Экспортировать все функции**

**Критерии приёмки:**
- [ ] Все функции форматирования работают корректно
- [ ] Обработка edge cases (null, undefined, NaN)
- [ ] Возврат ожидаемых значений

---

## 📝 Подэтап 5.1: Создание useWeekComparison composable

**Цель:** Извлечь логику работы с данными недель.

**Время выполнения:** ~1.5 часа

### Детальные шаги:

1. **Извлечь computed для метаданных недель:**
   ```javascript
   const previousWeekMeta = computed(() => { /* ... */ });
   const currentWeekMeta = computed(() => { /* ... */ });
   ```

2. **Извлечь computed для данных недель:**
   ```javascript
   const currentWeekData = computed(() => { /* ... */ });
   const previousWeekData = computed(() => { /* ... */ });
   const prePreviousWeekData = computed(() => { /* ... */ });
   ```

3. **Извлечь computed для процентов:**
   ```javascript
   const currentWeekPercentages = computed(() => { /* ... */ });
   const previousWeekPercentages = computed(() => { /* ... */ });
   ```

4. **Извлечь функции:**
   ```javascript
   function formatWeekDates(startUtc, endUtc) { /* ... */ }
   ```

**Критерии приёмки:**
- [ ] Все computed работают корректно
- [ ] Форматирование дат работает
- [ ] Расчет процентов корректен

---

## 📝 Подэтап 6.1: Извлечение ChartSummary

**Цель:** Выделить сводную секцию с метриками в отдельный компонент.

**Время выполнения:** ~2 часа

### Детальные шаги:

1. **Копировать template сводной секции:**
   ```vue
   <section class="ac-chart__summary">
     <!-- Блок текущей недели -->
     <!-- Блок предыдущей недели -->
   </section>
   ```

2. **Добавить props для всех необходимых данных:**
   ```javascript
   const props = defineProps({
     currentWeekData: Object,
     previousWeekData: Object,
     currentWeekMeta: Object,
     previousWeekMeta: Object,
     currentWeekPercentages: Object,
     previousWeekPercentages: Object,
     // ... остальные props
   });
   ```

3. **Добавить emits для кликов:**
   ```javascript
   const emit = defineEmits(['summary-click', 'previous-week-summary-click']);
   ```

4. **Использовать composables для внутренней логики**

5. **Копировать стили сводной секции**

**Критерии приёмки:**
- [ ] ChartSummary.vue создан и работает
- [ ] Отображает все метрики текущей недели
- [ ] Отображает сравнение с предыдущей неделей
- [ ] Кликабельные карточки работают

---

## 📝 Подэтап 7.1: Извлечение ChartContainer

**Цель:** Выделить контейнер графиков в отдельный компонент.

**Время выполнения:** ~1.5 часа

### Детальные шаги:

1. **Копировать template контейнера графиков:**
   ```vue
   <div class="ac-chart__body">
     <!-- Линейные графики (два рядом) -->
     <!-- Другие типы графиков (один) -->
   </div>
   ```

2. **Добавить props:**
   ```javascript
   const props = defineProps({
     chartType: String,
     chartComponent: Object,
     chartData: Object,
     chartOptions: Object,
     newClosedChartData: Object,
     carryoverChartData: Object
   });
   ```

3. **Копировать стили контейнера графиков**

**Критерии приёмки:**
- [ ] ChartContainer.vue создан и работает
- [ ] Все типы графиков отображаются корректно
- [ ] Адаптивная верстка работает

---

## 📝 Подэтап 8.1: Обновление основного компонента

**Цель:** Обновить GraphAdmissionClosureChart.vue для использования новых компонентов.

**Время выполнения:** ~1 час

### Детальные шаги:

1. **Обновить template:**
   ```vue
   <template>
     <div class="ac-chart">
       <ChartHeader
         :meta="meta"
         :chart-type="chartType"
         @update:chart-type="chartType = $event"
       />
       <ChartSummary
         :current-week-data="currentWeekData"
         :previous-week-data="previousWeekData"
         @summary-click="handleSummaryClick"
         @previous-week-summary-click="handlePreviousWeekSummaryClick"
       />
       <ChartContainer
         :chart-type="chartType"
         :chart-component="chartComponent"
         :chart-data="chartData"
         :chart-options="chartOptions"
         :new-closed-chart-data="newClosedChartData"
         :carryover-chart-data="carryoverChartData"
       />
     </div>
   </template>
   ```

2. **Обновить script для использования composables:**
   ```javascript
   import { useChartData } from '@/composables/graph-admission-closure/useChartData.js';
   import { useChartControls } from '@/composables/graph-admission-closure/useChartControls.js';
   import { useMetricsCalculation } from '@/composables/graph-admission-closure/useMetricsCalculation.js';
   import { useWeekComparison } from '@/composables/graph-admission-closure/useWeekComparison.js';

   // Использование composables
   const { chartType, chartComponent, chartData } = useChartControls();
   const { newClosedChartData, carryoverChartData, chartOptions } = useChartData(props);
   const { formatPercentage, formatNumber } = useMetricsCalculation();
   const { currentWeekData, previousWeekData, currentWeekPercentages } = useWeekComparison(props);
   ```

3. **Оставить только обработчики событий в основном компоненте**

**Критерии приёмки:**
- [ ] Основной компонент использует новые подкомпоненты
- [ ] Все composables интегрированы
- [ ] Функциональность не нарушена

---

## ✅ Критерии приёмки

### Функциональность:

#### Компоненты:
- [ ] **ChartHeader.vue** создан и отображает заголовок и контролы типов графиков
- [ ] **ChartSummary.vue** создан и отображает метрики текущей и предыдущей недели
- [ ] **ChartContainer.vue** создан и корректно переключает типы графиков
- [ ] **ChartFooter.vue** создан (минимальная версия для первой итерации)

#### Composables:
- [ ] **useChartData.js** экспортирует все необходимые функции данных графиков
- [ ] **useChartControls.js** управляет типами графиков и валидацией
- [ ] **useMetricsCalculation.js** корректно рассчитывает проценты и форматирует числа
- [ ] **useWeekComparison.js** правильно извлекает данные недель и рассчитывает изменения

#### Интеграция:
- [ ] Основной компонент **GraphAdmissionClosureChart.vue** использует новые подкомпоненты
- [ ] **Props/Events интерфейс** корректно работает между компонентами
- [ ] **Рефакторинг не нарушает** существующую функциональность (регрессионное тестирование)

#### Пользовательский интерфейс:
- [ ] Переключение типов графиков работает без задержек
- [ ] Метрики и проценты отображаются корректно
- [ ] Кликабельные карточки открывают соответствующие модальные окна
- [ ] Адаптивная верстка работает на мобильных устройствах
- [ ] Анимации и переходы плавные

#### Производительность:
- [ ] Время загрузки компонентов не увеличилось значительно
- [ ] Перерисовки происходят только при изменении данных
- [ ] Память не утекает при переключении компонентов

### Структура кода:

- [ ] Каждый компонент имеет четкую ответственность
- [ ] Composables переиспользуемы и тестируемы
- [ ] Код разбит на логические модули
- [ ] Нет дублирования кода
- [ ] Компоненты имеют адекватный размер (200-600 строк)

### Качество:

- [ ] Код соответствует стандартам Vue 3 Composition API
- [ ] Обработка ошибок присутствует
- [ ] TypeScript типы (если применимо)
- [ ] Документация для composables
- [ ] Адаптивная верстка работает

---

## 🧪 Тестирование и валидация

### Стратегия тестирования:

#### 1. Unit-тестирование composables:
```javascript
// tests/composables/useMetricsCalculation.test.js
import { describe, it, expect } from 'vitest';
import { useMetricsCalculation } from '@/composables/graph-admission-closure/useMetricsCalculation.js';

describe('useMetricsCalculation', () => {
  it('calculatePercentage должен корректно рассчитывать проценты', () => {
    const { calculatePercentage } = useMetricsCalculation();

    expect(calculatePercentage(150, 100)).toBe(50);
    expect(calculatePercentage(50, 100)).toBe(-50);
    expect(calculatePercentage(100, 100)).toBe(0);
    expect(calculatePercentage(100, 0)).toBe(null);
  });

  it('formatNumber должен форматировать большие числа', () => {
    const { formatNumber } = useMetricsCalculation();

    expect(formatNumber(1500)).toBe('1 500');
    expect(formatNumber(1500000)).toBe('1.5M');
  });
});
```

#### 2. Component-тестирование:
```vue
<!-- tests/components/ChartHeader.test.vue -->
<template>
  <div>
    <ChartHeader
      :meta="meta"
      :chart-type="chartType"
      @update:chartType="handleTypeChange"
    />
  </div>
</template>

<script setup>
import { mount } from '@vue/test-utils';
import ChartHeader from '@/components/graph-admission-closure/chart/ChartHeader.vue';

// Тесты для ChartHeader
describe('ChartHeader', () => {
  it('должен отображать заголовок и метаданные', () => {
    const wrapper = mount(ChartHeader, {
      props: {
        meta: { weekNumber: 51 },
        chartType: 'line'
      }
    });

    expect(wrapper.text()).toContain('График приёма и закрытий');
    expect(wrapper.text()).toContain('Неделя 51');
  });

  it('должен эмитить событие при переключении типа графика', async () => {
    const wrapper = mount(ChartHeader, {
      props: { meta: {}, chartType: 'line' }
    });

    await wrapper.find('.chart-type-btn').trigger('click');
    expect(wrapper.emitted('update:chartType')).toBeTruthy();
  });
});
</script>
```

#### 3. Интеграционное тестирование:
```javascript
// tests/integration/GraphAdmissionClosureChart.integration.test.js
describe('GraphAdmissionClosureChart Integration', () => {
  it('должен работать с реальными данными без ошибок', async () => {
    // Тест полной интеграции компонентов
  });

  it('должен корректно переключать типы графиков', async () => {
    // Тест взаимодействия между ChartHeader и ChartContainer
  });
});
```

### Миграция и обратная совместимость:

#### 1. План миграции:
1. **Фаза 1:** Создание новых компонентов параллельно со старыми
2. **Фаза 2:** Интеграция новых компонентов в основной компонент
3. **Фаза 3:** Удаление старого кода после успешного тестирования
4. **Фаза 4:** Оптимизация и финальные улучшения

#### 2. Обратная совместимость:
- **API компонента:** Остается неизменным для внешних пользователей
- **Props/Events:** Сохраняются все существующие интерфейсы
- **Внешний вид:** UI остается визуально идентичным
- **Функциональность:** Все существующие возможности сохраняются

#### 3. Резервные копии:
```bash
# Создание резервной копии перед рефакторингом
cp GraphAdmissionClosureChart.vue GraphAdmissionClosureChart.vue.backup

# Возможность быстрого отката
git stash  # или git reset --hard HEAD~1
```

## 📝 Примечания

### Важные замечания:

1. **Последовательность:** Этапы выполняются строго последовательно, каждый этап тестируется отдельно
2. **Тестирование:** Каждый этап проходит unit и integration тестирование перед переходом к следующему
3. **Резервные копии:** Сохранять версии компонента на каждом этапе, иметь план отката
4. **Совместимость:** API компонента не меняется для внешних пользователей, UI остается неизменным
5. **Документация:** Обновлять JSDoc комментарии и документацию по мере рефакторинга

### Риски и их минимизация:

1. **Регрессии:**
   - **МитIGATION:** Обширное тестирование, постепенное внедрение
   - **План B:** Быстрый откат к резервной копии

2. **Производительность:**
   - **МитIGATION:** Профилирование производительности на каждом этапе
   - **План B:** Оптимизация или отказ от проблемных изменений

3. **Сложность отладки:**
   - **МитIGATION:** Хорошая структура composables, логирование
   - **План B:** Улучшение инструментов отладки

### Метрики успеха:

#### Количественные метрики:
- **Размер кода:** Снижение основного компонента с 2650 до ~200 строк (-92%)
- **Количество файлов:** Увеличение с 1 до 5 компонентов + 4 composables
- **Тестовое покрытие:** Минимум 80% для новых composables
- **Производительность:** Не более 5% ухудшения скорости загрузки

#### Качественные метрики:
- **Поддерживаемость:** Код легко изменять и расширять
- **Переиспользуемость:** Composables используются в других компонентах
- **Читаемость:** Код понятен без дополнительных комментариев
- **Надежность:** Минимальное количество багов после релиза

---

## 📋 Контрольный список выполнения

### Предварительная подготовка:
- [ ] Создать ветку для рефакторинга: `git checkout -b feature/refactor-chart-component`
- [ ] Установить инструменты тестирования: Vitest, Vue Test Utils
- [ ] Настроить линтеры и форматеры для новых файлов
- [ ] Создать резервную копию оригинального компонента

### Этап 1.1: Создание структуры файлов
- [ ] Создать директорию `vue-app/src/components/graph-admission-closure/chart/`
- [ ] Создать директорию `vue-app/src/composables/graph-admission-closure/`
- [ ] Создать базовые файлы всех компонентов
- [ ] Создать базовые composables с mock-функциями
- [ ] Проверить компиляцию проекта

### Этап 1.2: Создание базовых composables
- [ ] Реализовать `useChartControls.js` с базовой логикой
- [ ] Реализовать `useMetricsCalculation.js` с функциями форматирования
- [ ] Реализовать `useWeekComparison.js` с базовыми computed
- [ ] Реализовать `useChartData.js` с mock-данными
- [ ] Написать unit-тесты для composables

### Этап 2.1: Извлечение ChartHeader
- [ ] Извлечь HTML шаблон из оригинального компонента
- [ ] Реализовать props и emits интерфейс
- [ ] Интегрировать useChartControls composable
- [ ] Копировать и адаптировать стили
- [ ] Протестировать компонент отдельно

### Этап 3.1: Создание useChartData composable
- [ ] Извлечь все computed-свойства данных графиков
- [ ] Реализовать функции createGradient и getWeekLabels
- [ ] Добавить валидацию входных данных
- [ ] Оптимизировать производительность
- [ ] Написать unit-тесты

### Этап 4.1: Создание useMetricsCalculation composable
- [ ] Реализовать calculatePercentage с обработкой edge cases
- [ ] Реализовать formatPercentage и formatNumber
- [ ] Добавить getUnitLabel функцию
- [ ] Протестировать все комбинации входных данных
- [ ] Написать comprehensive unit-тесты

### Этап 5.1: Создание useWeekComparison composable
- [ ] Реализовать извлечение данных недель из props.data.series
- [ ] Добавить расчет процентов для каждой недели
- [ ] Реализовать formatWeekDates функцию
- [ ] Оптимизировать с помощью computed properties
- [ ] Протестировать с различными структурами данных

### Этап 6.1: Извлечение ChartSummary
- [ ] Извлечь HTML шаблон сводной секции
- [ ] Реализовать сложный props интерфейс
- [ ] Интегрировать useMetricsCalculation и useWeekComparison
- [ ] Добавить emits для кликов по карточкам
- [ ] Адаптировать стили для нового компонента

### Этап 7.1: Извлечение ChartContainer
- [ ] Извлечь HTML шаблон контейнера графиков
- [ ] Реализовать логику переключения типов графиков
- [ ] Интегрировать useChartData composable
- [ ] Добавить адаптивную верстку
- [ ] Протестировать все типы графиков

### Этап 8.1: Обновление основного компонента
- [ ] Обновить template для использования новых компонентов
- [ ] Рефакторить script для использования composables
- [ ] Удалить дублированный код
- [ ] Провести финальное интеграционное тестирование
- [ ] Оптимизировать производительность

### Пост-рефакторинг задачи:
- [ ] Удалить старый код из GraphAdmissionClosureChart.vue
- [ ] Обновить документацию компонентов
- [ ] Провести финальное регрессионное тестирование
- [ ] Замер производительности до/после рефакторинга
- [ ] Создать PR с описанием изменений

---

## 🔄 История правок

- 2025-12-24 17:15 (UTC+3, Брест): Создана подзадача TASK-077-01 для рефакторинга GraphAdmissionClosureChart.vue
- 2025-12-24 17:15 (UTC+3, Брест): Определены 8 этапов рефакторинга с детальными шагами реализации
- 2025-12-24 17:45 (UTC+3, Брест): Добавлены детальные описания компонентов и composables
- 2025-12-24 17:45 (UTC+3, Брест): Добавлены примеры кода, API интерфейсы, технические детали
- 2025-12-24 17:45 (UTC+3, Брест): Расширены критерии приёмки и добавлен раздел тестирования
- 2025-12-24 17:45 (UTC+3, Брест): Добавлен контрольный список выполнения и план миграции

---

## 📚 Связанные документы

- `DOCS/TASKS/TASK-077-refactor-large-components.md` — родительская задача
- `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue` — компонент для рефакторинга
- `vue-app/src/composables/` — директория для composables
