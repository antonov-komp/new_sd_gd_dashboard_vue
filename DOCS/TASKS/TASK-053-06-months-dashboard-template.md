# TASK-053-06: Создание отдельного шаблона для 3-месячного режима

**Дата создания:** 2025-12-17 13:55 (UTC+3, Брест)  
**Статус:** ✅ Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js Developer)  
**Родительская задача:** [TASK-053: Изменение формата выбора периода и внедрение 3-месячного режима](./TASK-053-period-selection-3-months-mode.md)  
**Этап:** 2.4 (Часть 2: Внедрение 3-месячного режима)

---

## 📋 Описание

Создать отдельный компонент дашборда для 3-месячного режима `GraphAdmissionClosureMonthsDashboard.vue`, который будет работать независимо от недельного режима. Компонент должен интегрировать все созданные компоненты (summary-карточки, графики) и обеспечивать условный рендеринг в главном компоненте.

---

## 🎯 Контекст

### Текущее состояние

**Текущий дашборд:**
- `GraphAdmissionClosureDashboard.vue` работает только с недельным режимом
- Все компоненты заточены под недельную агрегацию

**Проблема:**
- Нет отдельного компонента для 3-месячного режима
- Логика смешана в одном компоненте

### Требуемое состояние

**Новый дашборд:**
- `GraphAdmissionClosureMonthsDashboard.vue` — отдельный компонент для 3-месячного режима
- Условный рендеринг в главном компоненте
- Независимая работа от недельного режима

---

## 🔧 Технические требования

### Компонент: GraphAdmissionClosureMonthsDashboard.vue

**Расположение:** `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureMonthsDashboard.vue`

**Структура компонента:**
- Аналогична `GraphAdmissionClosureDashboard.vue`, но:
  - Работа с месячными данными
  - Другая структура summary-карточек
  - Другие графики (только линейный, расположение сверху-снизу)
  - Другая табличная часть

**Функциональность:**
- Загрузка данных через API с `periodMode: 'months'`
- Отображение summary-карточек (`SummaryCardsMonths`)
- Отображение линейных графиков (`LineChartMonths`)
- Прелоадер с глубокой детализацией (`MonthsModePreloader`)
- Обработка ошибок

### Интеграция в GraphAdmissionClosureDashboard.vue

**Условный рендеринг:**
```vue
<GraphAdmissionClosureMonthsDashboard
  v-if="periodMode === 'months'"
/>
<GraphAdmissionClosureDashboardWeeks
  v-else
/>
```

---

## 📝 Ступенчатые подзадачи

### Шаг 1: Создание компонента GraphAdmissionClosureMonthsDashboard.vue

1. **Создать файл компонента:**
   ```bash
   touch vue-app/src/components/graph-admission-closure/GraphAdmissionClosureMonthsDashboard.vue
   ```

2. **Скопировать структуру из GraphAdmissionClosureDashboard.vue:**
   - Header с breadcrumbs
   - Логика загрузки данных
   - Обработка ошибок

3. **Адаптировать для месячного режима:**
   - Изменить запрос к API (добавить `periodMode: 'months'`)
   - Заменить summary-карточки на `SummaryCardsMonths`
   - Заменить графики на `LineChartMonths`
   - Добавить прелоадер `MonthsModePreloader`

### Шаг 2: Интеграция условного рендеринга

1. **Обновить GraphAdmissionClosureDashboard.vue:**
   - Добавить условный рендеринг компонентов
   - Разделить логику для недельного и месячного режимов

2. **Создать обёртку (опционально):**
   - Если нужно — создать компонент-обёртку для условного рендеринга

---

## 🔍 Детали реализации

### Структура компонента

**Основные секции:**
1. Header с breadcrumbs и заголовком
2. Summary-карточки (SummaryCardsMonths)
3. Линейные графики (LineChartMonths)
4. Прелоадер (MonthsModePreloader)

**Управление состоянием:**
- `isLoading` — состояние загрузки
- `loadingProgress` — прогресс загрузки (0-100)
- `currentLoadingStage` — текущий этап загрузки
- `error` — ошибка загрузки
- `chartData` — данные для отображения

### Логика загрузки данных

**Последовательность загрузки:**
1. Показать прелоадер
2. Начать симуляцию прогресса
3. Выполнить запрос к API
4. Обновить прогресс по мере загрузки
5. Скрыть прелоадер после завершения

**Обработка ошибок:**
- Показывать понятные сообщения об ошибках
- Предлагать повторить загрузку
- Логировать ошибки для отладки

### Симуляция прогресса

**Этапы загрузки:**
- Подготовка данных (0-10%)
- Загрузка данных за октябрь (10-40%)
- Загрузка данных за ноябрь (40-70%)
- Загрузка данных за декабрь (70-90%)
- Агрегация данных (90-95%)
- Формирование графиков (95-100%)

**Реализация:**
- Использовать `setTimeout` для симуляции
- Обновлять прогресс плавно
- Синхронизировать с реальной загрузкой данных

### Интеграция с родительским компонентом

**Условный рендеринг:**
- Родительский компонент решает, какой дашборд показывать
- Переключение происходит без перезагрузки страницы
- Сохранять состояние при переключении (опционально)

## 💻 Примеры кода

### GraphAdmissionClosureMonthsDashboard.vue

```vue
<template>
  <div class="ac-dashboard-months">
    <Transition name="fade" mode="out-in">
      <MonthsModePreloader
        v-if="isLoading"
        :progress="loadingProgress"
        :currentStage="currentLoadingStage"
      />
      
      <div v-else>
        <div class="dashboard-header">
          <!-- ... breadcrumbs ... -->
        </div>

        <div class="dashboard-layout">
          <SummaryCardsMonths :data="chartData" />
          
          <LineChartMonths :data="chartData" />
        </div>
      </div>
    </Transition>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue';
import SummaryCardsMonths from './SummaryCardsMonths.vue';
import LineChartMonths from './LineChartMonths.vue';
import MonthsModePreloader from './MonthsModePreloader.vue';
import { fetchAdmissionClosureStats } from '@/services/graph-admission-closure/admissionClosureService.js';

const isLoading = ref(true);
const loadingProgress = ref(0);
const currentLoadingStage = ref('');
const error = ref(null);
const chartData = ref(null);

async function loadData() {
  isLoading.value = true;
  error.value = null;
  loadingProgress.value = 0;
  
  try {
    currentLoadingStage.value = 'Подготовка данных...';
    loadingProgress.value = 10;
    
    // Симуляция прогресса загрузки
    const progressSteps = [
      { stage: 'Загрузка данных за октябрь...', progress: 30 },
      { stage: 'Загрузка данных за ноябрь...', progress: 60 },
      { stage: 'Загрузка данных за декабрь...', progress: 80 },
      { stage: 'Агрегация данных...', progress: 90 },
      { stage: 'Формирование графиков...', progress: 95 }
    ];
    
    // Симуляция прогресса загрузки
    for (const step of progressSteps) {
      await new Promise(resolve => setTimeout(resolve, 500));
      
      // Плавное обновление прогресса
      const startProgress = loadingProgress.value;
      const endProgress = step.progress;
      const duration = 500; // 500ms для плавного обновления
      const steps = 10;
      const stepProgress = (endProgress - startProgress) / steps;
      
      for (let i = 0; i < steps; i++) {
        await new Promise(resolve => setTimeout(resolve, duration / steps));
        loadingProgress.value = Math.min(
          startProgress + stepProgress * (i + 1),
          endProgress
        );
      }
      
      currentLoadingStage.value = step.stage;
      loadingProgress.value = step.progress;
    }
    
    const result = await fetchAdmissionClosureStats({
      product: '1C',
      periodMode: 'months'
    });
    
    chartData.value = result.data;
    loadingProgress.value = 100;
    
    await new Promise(resolve => setTimeout(resolve, 300));
  } catch (err) {
    error.value = err instanceof Error ? err : new Error('Неизвестная ошибка загрузки');
    console.error('[GraphAdmissionClosureMonthsDashboard] loadData error:', err);
  } finally {
    isLoading.value = false;
  }
}

onMounted(() => {
  loadData();
});
</script>
```

### Интеграция в GraphAdmissionClosureDashboard.vue

```vue
<template>
  <div class="ac-dashboard">
    <PeriodModeSelector
      v-model="periodMode"
      @change="handlePeriodModeChange"
    />
    
    <PeriodModeInfoModal
      :is-visible="showPeriodModeInfo"
      @close="showPeriodModeInfo = false"
    />
    
    <!-- Условный рендеринг -->
    <GraphAdmissionClosureMonthsDashboard
      v-if="periodMode === 'months'"
    />
    <GraphAdmissionClosureDashboardWeeks
      v-else
    />
  </div>
</template>

<script setup>
import { ref } from 'vue';
import PeriodModeSelector from './PeriodModeSelector.vue';
import PeriodModeInfoModal from './PeriodModeInfoModal.vue';
import GraphAdmissionClosureMonthsDashboard from './GraphAdmissionClosureMonthsDashboard.vue';
import GraphAdmissionClosureDashboardWeeks from './GraphAdmissionClosureDashboard.vue'; // Переименовать текущий

const periodMode = ref<'weeks' | 'months'>('weeks');
const showPeriodModeInfo = ref(false);

function handlePeriodModeChange(mode) {
  periodMode.value = mode;
}
</script>
```

---

## ✅ Критерии приёмки

- [ ] Компонент `GraphAdmissionClosureMonthsDashboard.vue` создан и работает корректно
- [ ] Компонент работает независимо от недельного режима
- [ ] Условный рендеринг работает корректно
- [ ] Переключение между режимами происходит без ошибок
- [ ] Все компоненты интегрированы (summary-карточки, графики, прелоадер)
- [ ] Загрузка данных работает корректно
- [ ] Обработка ошибок реализована
- [ ] Прелоадер показывает прогресс загрузки

---

## 🔗 Зависимости

**Зависит от:**
- [TASK-053-03: Backend API для 3-месячного режима](./TASK-053-03-backend-api-3-months.md) — использует API
- [TASK-053-04: Изменения в блоках данных](./TASK-053-04-summary-cards-3-months.md) — использует компонент
- [TASK-053-05: Линейные графики](./TASK-053-05-line-charts-3-months.md) — использует компонент
- [TASK-053-07: Прелоадер](./TASK-053-07-months-preloader.md) — использует компонент

**Зависит от этого этапа:**
- Нет (это финальный этап интеграции)

---

## 📝 История правок

- **2025-12-17 13:55 (UTC+3, Брест):** Создана подзадача TASK-053-06
  - Определены требования для создания отдельного шаблона
  - Зафиксированы структура компонента и примеры кода
  - Добавлены критерии приёмки

- **2025-12-17 14:45 (UTC+3, Брест):** Задача завершена
  - ✅ Создан компонент `GraphAdmissionClosureMonthsDashboard.vue`:
    - Отдельный компонент для 3-месячного режима
    - Интегрированы `SummaryCardsMonths` и `LineChartMonths`
    - Реализована логика загрузки данных через API с `periodMode: 'months'`
    - Добавлен прелоадер с сообщением "Получение данных за 3 месяца..."
    - Реализована обработка ошибок через `StatusMessage`
    - Добавлена навигация "Назад" с breadcrumbs
  - ✅ Обновлён `GraphAdmissionClosureDashboard.vue`:
    - Добавлен условный рендеринг для месячного и недельного режимов
    - `PeriodModeSelector` вынесен на уровень обёртки
    - Логика загрузки данных работает только для недельного режима
    - Месячный режим обрабатывается в отдельном компоненте
  - ✅ Компоненты работают независимо:
    - Недельный режим: `GraphAdmissionClosureDashboard` (текущая логика)
    - Месячный режим: `GraphAdmissionClosureMonthsDashboard` (новый компонент)
  - ✅ Переключение между режимами происходит без перезагрузки страницы
  - ✅ Состояние `periodMode` сохраняется в `localStorage` через `PeriodModeSelector`

---

## 💡 Примечания

- **Независимость:** Компонент должен работать независимо от недельного режима
- **Переиспользование:** Можно переиспользовать логику из текущего дашборда
- **Тестирование:** Протестировать переключение между режимами, загрузку данных, обработку ошибок

