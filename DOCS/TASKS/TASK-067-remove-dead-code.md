# TASK-067: Удаление мёртвого кода из `api/graph-1c-admission-closure.php`

**Дата создания:** 2025-12-23 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Средний  
**Исполнитель:** Рефактор-менеджер  
**Связанная задача:** TASK-065

---

## 📋 Описание

После рефакторинга недельного режима в новый модуль (TASK-065), в legacy файле `api/graph-1c-admission-closure.php` остался мёртвый код (dead code) — код недельного режима, который больше не выполняется, так как недельный режим перенаправляется на новый модуль в строках 664-667.

---

## 🎯 Цель

Удалить неиспользуемый код недельного режима из legacy файла, оставив только:
1. Общие функции (используются месячным режимом)
2. Код месячного режима (`periodMode=months`) — режим "3 месяца" (TASK-053-03, TASK-058-01)
3. Прокси для недельного режима (redirect на новый модуль)

---

## 🔎 Анализ текущего состояния

### Что остаётся (нужно сохранить):

**Строки 1-639: Общие функции**
- `setupAppLogging()` — используется месячным режимом
- `jsonResponse()`, `parseJsonBody()` — утилиты ответа
- `getWeekBounds()`, `getFourWeeksBounds()` — используются в месячном режиме
- `calculateLastFourMonths()`, `calculateWeeksInMonth()` — месячные периоды
- `isInRange()` — проверка дат
- `calculateWeekMetrics()` — используется в месячном режиме (агрегация по неделям внутри месяца)
- `calculateDurationCategory()` — категории сроков
- `isCarryoverTicket()` — используется в месячном режиме

**Строки 640-1693: Месячный режим (`periodMode=months`)**
- Режим "3 месяца" (TASK-053-03): показывает данные за последние 3 месяца
- Режим "4 месяца" (TASK-058-01): загружает 4 месяца (3 для отображения + 1 для расчёта процентов)
- Валидация параметров
- Проверка кеша
- Загрузка данных из Bitrix24 (3 запроса)
- Агрегация по месяцам и неделям
- Формирование ответа

**Строки 663-667: Прокси для недельного режима**
```php
if ($periodMode === 'weeks') {
    require_once __DIR__ . '/graph-admission-closure/bootstrap.php';
    exit;
}
```

### Что можно удалить (мёртвый код):

**Строки 1695-2787: Код недельного режима**
- Код после строки 1693 не выполняется, так как недельный режим перенаправляется на новый модуль
- Включает:
  - Запросы к Bitrix24 для недельного режима
  - Агрегацию для недельного режима
  - Формирование ответа для недельного режима

---

## 📝 План работ

### Шаг 1: Проверка зависимостей
- [ ] Убедиться, что функции из строк 1695-2787 не используются месячным режимом
- [ ] Проверить, что все функции недельного режима перенесены в новый модуль

### Шаг 2: Резервная копия
- [ ] Создать резервную копию файла перед удалением
- [ ] Закоммитить текущее состояние в git

### Шаг 3: Удаление мёртвого кода
- [ ] Удалить код после строки 1693 (после закрытия блока месячного режима)
- [ ] Оставить только:
  - Общие функции (1-639)
  - Месячный режим (640-1693)
  - Прокси для недельного режима (663-667)
  - Обработку ошибок (try-catch)

### Шаг 4: Проверка синтаксиса
- [ ] Проверить синтаксис PHP: `php -l api/graph-1c-admission-closure.php`
- [ ] Убедиться, что файл корректно закрывается

### Шаг 5: Тестирование
- [ ] Протестировать месячный режим (должен работать как раньше)
- [ ] Протестировать недельный режим (должен перенаправляться на новый модуль)
- [ ] Проверить обработку ошибок

---

## ✅ Критерии приёмки

- [ ] Мёртвый код удалён (строки 1695-2787)
- [ ] Месячный режим работает корректно
- [ ] Недельный режим перенаправляется на новый модуль
- [ ] Синтаксис PHP корректен
- [ ] Файл стал короче (~1693 строки вместо 2796)
- [ ] Код стал проще для понимания и поддержки

---

## ⚠️ Риски и предосторожности

1. **Риск:** Удаление кода, который используется месячным режимом
   - **Митигация:** Тщательно проверить все зависимости перед удалением

2. **Риск:** Нарушение работы месячного режима
   - **Митигация:** Полное тестирование месячного режима после удаления

3. **Риск:** Потеря важного кода
   - **Митигация:** Резервная копия и git commit перед удалением

---

## 📊 Ожидаемый результат

### До:
- Файл: ~2796 строк
- Смешанная логика: недельный + месячный режимы
- Мёртвый код: ~1092 строки (недельный режим после перенаправления)

### После:
- Файл: ~1693 строки
- Чистая логика: только месячный режим (3 месяца) + прокси для недельного режима
- Мёртвый код: 0 строк

### Преимущества:
- ✅ Упрощение поддержки
- ✅ Уменьшение размера файла на ~40%
- ✅ Улучшение читаемости
- ✅ Снижение риска ошибок

---

## 🔗 Связанные задачи

- **TASK-065:** Рефакторинг на слои (недельный режим перенесён)
- **TASK-065 (продолжение):** Перенос месячного режима в новый модуль (после этого можно будет удалить весь legacy файл)

---

## 📝 Примечания

- После переноса месячного режима в новый модуль (TASK-065 продолжение), можно будет удалить весь legacy файл `api/graph-1c-admission-closure.php` или оставить его как тонкий прокси для обратной совместимости.
- На данном этапе удаляем только мёртвый код недельного режима, оставляя месячный режим (3 месяца) в legacy до его переноса.
- Месячный режим (`periodMode=months`) реализует функционал из TASK-053-03 (3 месяца) и TASK-058-01 (4 месяца для расчёта процентов).

