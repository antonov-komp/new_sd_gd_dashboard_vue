# TASK-034-04: Реализация шаблона уровня 4

**Дата создания:** 2025-12-12 11:51 (UTC+3, Брест)  
**Статус:** Завершена  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-034 (Этап 4)

---

## 📋 Описание

Создать шаблон для отображения списка тикетов на уровне 4 попапа. Шаблон должен включать заголовок с контекстом перехода, контейнер для списка тикетов и интеграцию компонента `TicketCard.vue` для отображения каждого тикета в той же визуальной структуре, что и в канбан-режиме дашборда 1С.

---

## 🎯 Цель

1. Добавить секцию уровня 4 в шаблон компонента
2. Реализовать структуру заголовка с контекстом перехода
3. Создать контейнер для списка тикетов с вертикальной прокруткой
4. Интегрировать компонент `TicketCard.vue` для отображения тикетов
5. Применить стили, соответствующие дизайну попапа и канбан-режима

---

## 🔍 Контекст задачи

### Компонент EmployeeDetailsModal.vue

**Расположение:** `vue-app/src/components/graph-state/EmployeeDetailsModal.vue`

**Текущая структура шаблона:**
- Уровень 1: Список сотрудников стадии (с переключателями видов)
- Уровень 2: Временные градации тикетов сотрудника
- Уровень 3: Детализация по заказчикам (отделам)

**Новый уровень:**
- Уровень 4: Список тикетов в виде таблицы с карточками

### Компонент TicketCard.vue

**Расположение:** `vue-app/src/components/dashboard/TicketCard.vue`

**Использование в других компонентах:**
- `EmployeeColumn.vue` — колонка сотрудника (тикеты с drag & drop)
- `ZeroPoint.vue` — нулевая точка (входящие тикеты)

**Пример использования:**
```vue
<TicketCard
  v-for="ticket in tickets"
  :key="ticket.id"
  :ticket="ticket"
  :draggable="false"
  @click="handleTicketClick"
/>
```

### Зависимости от предыдущих этапов

**Этап 3 (TASK-034-03):**
- Состояние `level4Data` добавлено
- Функция `goToLevel4()` реализована
- Данные уровня 4 подготовлены

---

## 📝 Задачи

### Задача 4.1: Добавление секции уровня 4 в шаблон

**Цель:** Добавить блок уровня 4 в шаблон компонента с использованием Transition для плавной анимации.

#### 4.1.1. Изучение структуры Transition

**Текущая структура:**
```vue
<Transition name="level" mode="out-in">
  <div v-if="popupLevel === 1" key="level-1" class="level-1">
    <!-- Уровень 1 -->
  </div>
  
  <div v-else-if="popupLevel === 2 && level2Data" key="level-2" class="level-2">
    <!-- Уровень 2 -->
  </div>
  
  <div v-else-if="popupLevel === 3 && level3Data" key="level-3" class="level-3">
    <!-- Уровень 3 -->
  </div>
</Transition>
```

**Действия:**
1. Найти блок `<Transition>` в шаблоне
2. Определить место для добавления уровня 4 (после уровня 3)
3. Добавить блок `v-else-if="popupLevel === 4 && level4Data"`

#### 4.1.2. Добавление блока уровня 4

**Структура блока:**
```vue
<!-- Уровень 4: Список тикетов -->
<div v-else-if="popupLevel === 4 && level4Data" key="level-4" class="level-4">
  <!-- Заголовок уровня 4 -->
  <div class="modal-header">
    <button class="btn-back" @click="goBack" aria-label="Назад">
      ←
    </button>
    <div class="header-info">
      <h3>{{ getLevel4Title() }}</h3>
      <div v-if="level4Data.context" class="header-badges">
        <span v-if="level4Data.context.dateCategoryLabel" class="date-badge">
          {{ level4Data.context.dateCategoryLabel }}
        </span>
        <span v-if="level4Data.context.departmentName" class="department-badge">
          {{ level4Data.context.departmentName }}
        </span>
        <span class="stage-badge">{{ level4Data.context.stageName }}</span>
      </div>
    </div>
    <span class="modal-total">Всего: {{ level4Data.totalCount }} тикетов</span>
    <button class="modal-close" @click="close" aria-label="Закрыть">×</button>
  </div>
  
  <!-- Тело уровня 4 -->
  <div class="modal-body">
    <!-- Контейнер для списка тикетов -->
    <!-- Будет реализован в задаче 4.2 -->
  </div>
</div>
```

**Действия:**
1. Добавить блок уровня 4 после блока уровня 3
2. Использовать `key="level-4"` для Transition
3. Добавить проверку `level4Data` для безопасности
4. Добавить класс `level-4` для стилизации

**Ожидаемый результат:**
- Блок уровня 4 добавлен в шаблон
- Transition работает корректно
- Структура соответствует другим уровням

#### 4.1.3. Функция формирования заголовка уровня 4

**Функция:**
```javascript
/**
 * Получить заголовок для уровня 4
 * 
 * Формирует заголовок на основе контекста перехода
 * 
 * @returns {String} Заголовок уровня 4
 */
function getLevel4Title() {
  if (!level4Data.value?.context) {
    return 'Список тикетов';
  }

  const ctx = level4Data.value.context;
  const parts = [];

  // Добавить имя сотрудника (если есть)
  if (ctx.employeeName) {
    parts.push(ctx.employeeName);
  }

  // Добавить категорию давности (если есть)
  if (ctx.dateCategoryLabel) {
    parts.push(ctx.dateCategoryLabel);
  }

  // Добавить заказчика (если есть)
  if (ctx.departmentName) {
    parts.push(ctx.departmentName);
  }

  // Добавить название стадии
  if (ctx.stageName) {
    parts.push(ctx.stageName);
  }

  return parts.length > 0 ? parts.join(' — ') : 'Список тикетов';
}
```

**Действия:**
1. Создать функцию `getLevel4Title()` в секции `<script setup>`
2. Использовать функцию в шаблоне для заголовка
3. Обработать все варианты контекста

**Варианты заголовков:**

1. **Из уровня 2:**
   - "Иван Иванов — СЕГОДНЯ — Сформировано обращение"

2. **Из уровня 3:**
   - "Иван Иванов — СЕГОДНЯ — Отдел продаж — Сформировано обращение"

3. **Из уровня 1 (По времени):**
   - "СЕГОДНЯ — Сформировано обращение"

4. **Из уровня 1 (По заказчикам):**
   - "Отдел продаж — Сформировано обращение"

**Ожидаемый результат:**
- Функция `getLevel4Title()` реализована
- Заголовок формируется корректно для всех вариантов контекста

**Критерии приёмки задачи 4.1:**
- [ ] Блок уровня 4 добавлен в шаблон
- [ ] Transition работает корректно
- [ ] Функция `getLevel4Title()` реализована
- [ ] Заголовок отображается корректно
- [ ] Структура соответствует другим уровням

---

### Задача 4.2: Контейнер для списка тикетов

**Цель:** Создать контейнер с вертикальной прокруткой для отображения списка тикетов.

#### 4.2.1. Изучение структуры контейнеров в других компонентах

**Пример из EmployeeColumn.vue:**
```vue
<div class="tickets-list">
  <transition-group name="ticket" tag="div">
    <TicketCard
      v-for="ticket in ticketsToDisplay"
      :key="ticket.id"
      :ticket="ticket"
      :draggable="true"
      @click="$emit('ticket-clicked', ticket)"
    />
  </transition-group>
</div>
```

**Пример из ZeroPoint.vue:**
```vue
<div class="zero-point-tickets">
  <TicketCard
    v-for="ticket in tickets"
    :key="ticket.id"
    :ticket="ticket"
    :draggable="true"
    @click="$emit('ticket-clicked', ticket)"
  />
</div>
```

**Действия:**
1. Изучить структуру контейнеров в `EmployeeColumn.vue` и `ZeroPoint.vue`
2. Определить, какие стили используются
3. Адаптировать для попапа

#### 4.2.2. Создание контейнера для списка тикетов

**Структура контейнера:**
```vue
<div class="modal-body">
  <!-- Индикатор загрузки -->
  <div v-if="level4Data.isLoading" class="loading-state">
    <div class="loading-spinner"></div>
    <p>Загрузка тикетов...</p>
  </div>
  
  <!-- Пустое состояние -->
  <div v-else-if="!level4Data.tickets || level4Data.tickets.length === 0" class="empty-state">
    <p>Нет тикетов для отображения</p>
  </div>
  
  <!-- Список тикетов -->
  <div v-else class="tickets-list-container">
    <div class="tickets-list">
      <TicketCard
        v-for="ticket in level4Data.tickets"
        :key="ticket.id"
        :ticket="ticket"
        :draggable="false"
        @click="handleTicketClick(ticket)"
      />
    </div>
  </div>
</div>
```

**Действия:**
1. Добавить контейнер `tickets-list-container` в `modal-body`
2. Добавить обработку состояния загрузки
3. Добавить обработку пустого состояния
4. Добавить список тикетов с `v-for`

**Ожидаемый результат:**
- Контейнер для списка тикетов создан
- Обработка состояний (загрузка, пустое состояние) реализована

#### 4.2.3. Стилизация контейнера

**Базовые стили:**
```css
.tickets-list-container {
  width: 100%;
  max-height: calc(80vh - 200px); /* Высота модального окна минус заголовок и отступы */
  overflow-y: auto;
  overflow-x: hidden;
  padding: 0;
}

.tickets-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 20px;
}

.loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60px 20px;
  color: var(--b24-text-muted, #9ca3af);
}

.loading-spinner {
  width: 40px;
  height: 40px;
  border: 4px solid var(--b24-border-light, #e5e7eb);
  border-top-color: var(--b24-primary, #007bff);
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 16px;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.empty-state {
  text-align: center;
  padding: 60px 20px;
  color: var(--b24-text-muted, #9ca3af);
  font-size: 14px;
  font-style: italic;
}
```

**Действия:**
1. Добавить стили для `tickets-list-container`
2. Добавить стили для `tickets-list`
3. Добавить стили для состояний загрузки и пустого состояния
4. Добавить анимацию спиннера загрузки

**Ожидаемый результат:**
- Стили применены корректно
- Вертикальная прокрутка работает
- Состояния загрузки и пустого состояния стилизованы

**Критерии приёмки задачи 4.2:**
- [ ] Контейнер для списка тикетов создан
- [ ] Вертикальная прокрутка работает
- [ ] Обработка состояния загрузки реализована
- [ ] Обработка пустого состояния реализована
- [ ] Стили применены корректно

---

### Задача 4.3: Интеграция компонента TicketCard

**Цель:** Интегрировать компонент `TicketCard.vue` для отображения тикетов в списке.

#### 4.3.1. Импорт компонента TicketCard

**Действия:**
1. Найти секцию импортов в `<script setup>`
2. Добавить импорт компонента:
   ```javascript
   import TicketCard from '@/components/dashboard/TicketCard.vue';
   ```

3. Проверить, что компонент не импортирован ранее

**Ожидаемый результат:**
- Компонент `TicketCard` импортирован
- Импорт выполнен корректно

#### 4.3.2. Использование TicketCard в шаблоне

**Структура использования:**
```vue
<div class="tickets-list">
  <TicketCard
    v-for="ticket in level4Data.tickets"
    :key="ticket.id"
    :ticket="ticket"
    :draggable="false"
    @click="handleTicketClick(ticket)"
  />
</div>
```

**Действия:**
1. Добавить компонент `TicketCard` в шаблон
2. Использовать `v-for` для отображения всех тикетов
3. Передать `ticket` как prop
4. Установить `draggable="false"` (drag & drop не нужен в попапе)
5. Добавить обработчик `@click` для открытия тикета

**Особенности:**
- `key="ticket.id"` — уникальный ключ для каждого тикета
- `:ticket="ticket"` — передача объекта тикета
- `:draggable="false"` — отключение drag & drop
- `@click="handleTicketClick(ticket)"` — обработчик клика (будет реализован в этапе 5)

**Ожидаемый результат:**
- Компонент `TicketCard` используется в шаблоне
- Все тикеты отображаются в списке
- Props передаются корректно

#### 4.3.3. Проверка соответствия данных

**Действия:**
1. Убедиться, что данные тикетов подготовлены функцией `prepareTicketsForDisplay()` (этап 2)
2. Проверить, что все поля, требуемые `TicketCard.vue`, присутствуют:
   - `id`, `title`, `ufSubject`
   - `priorityId`, `priorityLabel`, `priorityColors`
   - `service`, `serviceLabel`, `serviceColors`
   - `actionStr`, `departmentHead`, `description`, `createdAt`

3. Добавить проверку данных (если нужно):
   ```vue
   <TicketCard
     v-for="ticket in level4Data.tickets"
     :key="ticket.id"
     :ticket="ticket"
     :draggable="false"
     @click="handleTicketClick(ticket)"
   />
   ```

**Ожидаемый результат:**
- Данные тикетов соответствуют требованиям `TicketCard.vue`
- Все карточки отображаются корректно

#### 4.3.4. Оптимизация рендеринга (опционально)

**Использование v-memo (если нужно):**
```vue
<div
  v-memo="[level4Data.tickets.length, level4Data.tickets.map(t => t.id).join(',')]"
  class="tickets-list"
>
  <TicketCard
    v-for="ticket in level4Data.tickets"
    :key="ticket.id"
    :ticket="ticket"
    :draggable="false"
    @click="handleTicketClick(ticket)"
  />
</div>
```

**Действия:**
1. Рассмотреть использование `v-memo` для оптимизации рендеринга
2. Применить только если список тикетов большой (>50 тикетов)

**Ожидаемый результат:**
- Рендеринг оптимизирован (если нужно)

**Критерии приёмки задачи 4.3:**
- [ ] Компонент `TicketCard` импортирован
- [ ] Компонент используется в шаблоне
- [ ] Все тикеты отображаются в списке
- [ ] Props передаются корректно
- [ ] Данные соответствуют требованиям `TicketCard.vue`
- [ ] Оптимизация рендеринга применена (если нужно)

---

### Задача 4.4: Стилизация уровня 4

**Цель:** Применить стили для уровня 4, соответствующие дизайну попапа и канбан-режима.

#### 4.4.1. Изучение стилей других уровней

**Действия:**
1. Изучить стили уровней 1, 2, 3 в компоненте
2. Определить общие стили для модального окна:
   - `.modal-header` — заголовок
   - `.modal-body` — тело
   - `.btn-back` — кнопка "Назад"
   - `.modal-close` — кнопка закрытия
   - `.modal-total` — общее количество

3. Определить специфичные стили для каждого уровня

**Ожидаемый результат:**
- Понимание структуры стилей
- Определены общие и специфичные стили

#### 4.4.2. Стили для заголовка уровня 4

**Стили:**
```css
/* Уровень 4: Список тикетов */
.level-4 {
  /* Наследуем базовые стили модального окна */
}

.level-4 .header-info {
  display: flex;
  flex-direction: column;
  gap: 8px;
  flex: 1;
}

.level-4 .header-info h3 {
  margin: 0;
  font-size: 18px;
  font-weight: 600;
  color: var(--b24-text-primary, #1f2937);
}

.level-4 .header-badges {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.level-4 .date-badge {
  display: inline-block;
  padding: 4px 12px;
  background-color: var(--b24-info, #17a2b8);
  color: white;
  border-radius: var(--radius-xl, 9999px);
  font-size: 12px;
  font-weight: 600;
}

.level-4 .department-badge {
  display: inline-block;
  padding: 4px 12px;
  background-color: var(--b24-secondary, #6c757d);
  color: white;
  border-radius: var(--radius-xl, 9999px);
  font-size: 12px;
  font-weight: 600;
}

.level-4 .stage-badge {
  display: inline-block;
  padding: 4px 12px;
  background-color: var(--b24-primary, #007bff);
  color: white;
  border-radius: var(--radius-xl, 9999px);
  font-size: 12px;
  font-weight: 600;
}
```

**Действия:**
1. Добавить стили для заголовка уровня 4
2. Использовать те же стили, что и для уровня 3 (для консистентности)
3. Добавить стили для бейджей (дата, заказчик, стадия)

**Ожидаемый результат:**
- Стили для заголовка применены
- Бейджи отображаются корректно

#### 4.4.3. Стили для контейнера списка тикетов

**Стили:**
```css
.level-4 .tickets-list-container {
  width: 100%;
  max-height: calc(80vh - 200px);
  overflow-y: auto;
  overflow-x: hidden;
  padding: 0;
  /* Плавная прокрутка */
  scroll-behavior: smooth;
  /* Стилизация скроллбара (для WebKit) */
  scrollbar-width: thin;
  scrollbar-color: var(--b24-border-medium, #d1d5db) var(--b24-bg-light, #f3f4f6);
}

.level-4 .tickets-list-container::-webkit-scrollbar {
  width: 8px;
}

.level-4 .tickets-list-container::-webkit-scrollbar-track {
  background: var(--b24-bg-light, #f3f4f6);
  border-radius: 4px;
}

.level-4 .tickets-list-container::-webkit-scrollbar-thumb {
  background: var(--b24-border-medium, #d1d5db);
  border-radius: 4px;
}

.level-4 .tickets-list-container::-webkit-scrollbar-thumb:hover {
  background: var(--b24-text-muted, #9ca3af);
}

.level-4 .tickets-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 20px;
}

/* Стили для карточек тикетов (наследуются из TicketCard.vue) */
.level-4 .tickets-list .ticket-card {
  /* Дополнительные стили, если нужно */
  margin-bottom: 0; /* Убираем margin-bottom, так как gap уже есть */
}
```

**Действия:**
1. Добавить стили для `tickets-list-container`
2. Настроить вертикальную прокрутку
3. Стилизовать скроллбар (для лучшего UX)
4. Добавить стили для `tickets-list`
5. Настроить отступы между карточками

**Ожидаемый результат:**
- Контейнер стилизован корректно
- Прокрутка работает плавно
- Скроллбар стилизован

#### 4.4.4. Стили для состояний загрузки и пустого состояния

**Стили:**
```css
.level-4 .loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60px 20px;
  color: var(--b24-text-muted, #9ca3af);
  font-size: 14px;
}

.level-4 .loading-spinner {
  width: 40px;
  height: 40px;
  border: 4px solid var(--b24-border-light, #e5e7eb);
  border-top-color: var(--b24-primary, #007bff);
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 16px;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.level-4 .empty-state {
  text-align: center;
  padding: 60px 20px;
  color: var(--b24-text-muted, #9ca3af);
  font-size: 14px;
  font-style: italic;
}
```

**Действия:**
1. Добавить стили для состояния загрузки
2. Добавить анимацию спиннера
3. Добавить стили для пустого состояния

**Ожидаемый результат:**
- Состояния стилизованы корректно
- Анимация спиннера работает

#### 4.4.5. Адаптивность для разных размеров экрана

**Медиа-запросы:**
```css
/* Адаптивность для мобильных устройств */
@media (max-width: 768px) {
  .level-4 .tickets-list-container {
    max-height: calc(90vh - 180px);
  }

  .level-4 .tickets-list {
    padding: 12px;
    gap: 8px;
  }

  .level-4 .header-info {
    width: 100%;
  }

  .level-4 .header-badges {
    width: 100%;
    flex-wrap: wrap;
  }

  .level-4 .tickets-list .ticket-card {
    /* Адаптация карточек для мобильных (если нужно) */
  }
}
```

**Действия:**
1. Добавить медиа-запросы для мобильных устройств
2. Адаптировать отступы и размеры
3. Оптимизировать прокрутку для touch-устройств

**Ожидаемый результат:**
- Адаптивность работает на всех устройствах
- Прокрутка оптимизирована для touch-устройств

**Критерии приёмки задачи 4.4:**
- [ ] Стили для заголовка применены
- [ ] Стили для контейнера списка применены
- [ ] Стили для состояний загрузки и пустого состояния применены
- [ ] Адаптивность работает на всех устройствах
- [ ] Скроллбар стилизован
- [ ] Стили соответствуют дизайну попапа и канбан-режима

---

## 📚 Зависимости

### Существующие файлы

- `vue-app/src/components/graph-state/EmployeeDetailsModal.vue` — компонент попапа (основной файл для изменений)
- `vue-app/src/components/dashboard/TicketCard.vue` — компонент карточки тикета

### Зависимости от предыдущих этапов

**Этап 3 (TASK-034-03):**
- Состояние `level4Data` добавлено
- Функция `goToLevel4()` реализована
- Функция `getLevel4Title()` может быть использована (или реализована в этом этапе)

**Этап 2 (TASK-034-02):**
- Функция `prepareTicketsForDisplay()` подготовила тикеты для отображения

### Импорты

```javascript
import TicketCard from '@/components/dashboard/TicketCard.vue';
```

---

## ✅ Критерии приёмки этапа 4

- [x] Блок уровня 4 добавлен в шаблон
- [x] Заголовок отображается корректно с контекстом перехода
- [x] Контейнер для списка тикетов создан
- [x] Вертикальная прокрутка работает
- [x] Компонент `TicketCard` интегрирован
- [x] Все тикеты отображаются в списке
- [x] Обработка состояния загрузки реализована
- [x] Обработка пустого состояния реализована
- [x] Стили применены корректно
- [x] Адаптивность работает на всех устройствах
- [x] Визуальная структура идентична канбан-режиму

**Статус:** ✅ Завершена (2025-12-12, UTC+3, Брест)

---

## 🧪 Тестирование

### Тест 1: Отображение уровня 4

**Сценарий:**
1. Перейти на уровень 4 из уровня 2
2. Проверить, что уровень 4 отображается
3. Проверить, что заголовок формируется корректно

**Ожидаемый результат:**
- Уровень 4 отображается
- Заголовок содержит имя сотрудника, категорию давности и стадию
- Кнопка "Назад" работает

### Тест 2: Отображение списка тикетов

**Сценарий:**
1. Перейти на уровень 4
2. Проверить, что тикеты отображаются в списке
3. Проверить, что карточки тикетов идентичны канбан-режиму

**Ожидаемый результат:**
- Все тикеты отображаются
- Карточки имеют ту же визуальную структуру, что и в канбан-режиме
- Отступы между карточками корректны

### Тест 3: Вертикальная прокрутка

**Сценарий:**
1. Перейти на уровень 4 с большим количеством тикетов (>20)
2. Проверить, что прокрутка работает
3. Проверить, что скроллбар отображается

**Ожидаемый результат:**
- Прокрутка работает плавно
- Скроллбар стилизован и виден
- Контейнер не выходит за границы модального окна

### Тест 4: Состояние загрузки

**Сценарий:**
1. Перейти на уровень 4 (данные загружаются)
2. Проверить, что индикатор загрузки отображается
3. Дождаться загрузки данных
4. Проверить, что список тикетов отображается

**Ожидаемый результат:**
- Индикатор загрузки отображается
- После загрузки список тикетов отображается
- Переход плавный

### Тест 5: Пустое состояние

**Сценарий:**
1. Перейти на уровень 4 с пустым списком тикетов
2. Проверить, что пустое состояние отображается

**Ожидаемый результат:**
- Пустое состояние отображается корректно
- Сообщение информативное

### Тест 6: Адаптивность

**Сценарий:**
1. Открыть попап на мобильном устройстве
2. Перейти на уровень 4
3. Проверить отображение на разных размерах экрана

**Ожидаемый результат:**
- Адаптивность работает на всех устройствах
- Прокрутка работает на touch-устройствах
- Карточки тикетов читаемы на всех экранах

---

## 📝 История правок

- **2025-12-12 11:51 (UTC+3, Брест):** Создана задача TASK-034-04
  - Определены 4 подзадачи с детальными шагами
  - Добавлены примеры кода и структуры шаблона
  - Описаны критерии приёмки и тесты

- **2025-12-12 (UTC+3, Брест):** Выполнена задача TASK-034-04
  - Добавлен блок уровня 4 в шаблон компонента после уровня 3
  - Реализован заголовок уровня 4 с использованием computed-свойства `popupTitle`:
    - Отображение имени сотрудника (если есть)
    - Отображение категории давности (если есть)
    - Отображение заказчика (если есть)
    - Отображение названия стадии
    - Бейджи для визуального отображения контекста
  - Создан контейнер `tickets-list-container` с вертикальной прокруткой
  - Реализована обработка состояния загрузки (индикатор со спиннером)
  - Реализована обработка пустого состояния (сообщение "Нет тикетов для отображения")
  - Интегрирован компонент `TicketCard.vue`:
    - Импортирован компонент
    - Использован в шаблоне с `v-for` для отображения всех тикетов
    - Установлен `draggable="false"` (drag & drop не нужен в попапе)
    - Добавлен обработчик `@click` (заглушка, будет реализовано в этапе 5)
  - Добавлены стили для уровня 4:
    - Стили для заголовка и бейджей (date-badge, department-badge, stage-badge)
    - Стили для контейнера списка тикетов с прокруткой
    - Стилизация скроллбара (WebKit и стандартный)
    - Стили для состояний загрузки и пустого состояния
    - Анимация спиннера загрузки
    - Адаптивные стили для мобильных устройств
  - Все стили соответствуют дизайну попапа и канбан-режима

---

**Автор:** Технический писатель  
**Исполнитель:** Bitrix24 Программист (Vue.js)



