# TASK-088-01: Улучшение UI анализа активности пользователей

**Дата создания:** 2026-01-12 13:00 (UTC+3, Брест)
**Дата завершения:** 2026-01-12 18:00 (UTC+3, Брест)
**Версия:** 1.0
**Статус:** ✅ Завершено
**Приоритет:** Средний
**Исполнитель:** UIX дизайнер → Vue.js разработчик
**Тип задачи:** UI/UX улучшения  

---

## 🎯 Цель задачи

Улучшить пользовательский интерфейс анализа активности пользователей в существующем модуле, предоставив более детализированный и удобный анализ логов и действий пользователей с расширенными возможностями фильтрации, группировки и визуализации данных.

**Важно:** Не расширять функционал модуля управления пользователями, а улучшить анализ существующей активности.

---

## 📋 Оглавление

1. [Контекст задачи](#контекст-задачи)
2. [Текущий анализ интерфейса](#текущий-анализ-интерфейса)
3. [Требования к улучшениям](#требования-к-улучшениям)
4. [Дизайн-концепция](#дизайн-концепция)
5. [Техническая реализация](#техническая-реализация)
6. [Критерии приёмки](#критерии-приёмки)
7. [Тестирование](#тестирование)

---

## 🎯 Контекст задачи

### Исходная задача
На основе анализа [TASK-088](./TASK-088-user-management-module-analysis.md) выявлено, что модуль активности пользователей имеет базовую функциональность, но интерфейс анализа требует значительных улучшений для эффективной работы администраторов.

### Текущие возможности
- Просмотр списка активности в виде простого списка
- Базовая статистика (общее количество записей, уникальные пользователи)
- Простые фильтры по пользователю, типу активности, датам
- Возможность скрытия пользователей

### Проблемы текущего интерфейса
- Отсутствие глубокой аналитики по действиям пользователей
- Недостаточная визуализация данных
- Ограниченные возможности группировки и фильтрации
- Неудобный анализ поведения конкретных пользователей
- Отсутствие трендов и паттернов использования

---

## 📊 Текущий анализ интерфейса

### Существующие компоненты

#### 1. UserActivityList.vue
**Текущий вид:** Простой список записей активности

**Проблемы:**
- Линейное отображение без группировки
- Отсутствие визуальной иерархии
- Неудобно анализировать большое количество записей
- Отсутствие быстрого доступа к деталям

#### 2. UserActivityStats.vue
**Текущий вид:** Базовые метрики в виде карточек

**Проблемы:**
- Ограниченное количество показателей
- Отсутствие графиков и трендов
- Нет возможности drill-down анализа
- Статические данные без интерактивности

#### 3. UserActivityFilters.vue
**Текущий вид:** Простые выпадающие списки и поля дат

**Проблемы:**
- Недостаточно фильтров для глубокого анализа
- Отсутствие предустановленных фильтров
- Нет возможности сохранения фильтров
- Ограниченные возможности комбинации фильтров

#### 4. HiddenUsersManager.vue
**Текущий вид:** Базовое управление видимостью

**Проблемы:**
- Не интегрировано с основным анализом
- Отсутствие аналитики по скрытым пользователям
- Неудобный интерфейс выбора

---

## 🎨 Требования к улучшениям

### 1. Суммарный анализ всех пользователей

#### Дашборд активности
- **Временные тренды:** Графики активности по часам/дням/неделям/месяцам
- **Типы действий:** Круговая диаграмма распределения по типам активности (app_entry, page_visit)
- **Топ пользователей:** Рейтинг 10 самых активных пользователей с количеством действий
- **География:** Если доступен IP анализ (опционально)
- **Устройства:** Анализ по user agent (браузеры, ОС, мобильные/десктоп)
- **Сравнение периодов:** Возможность сравнения текущего периода с предыдущим

#### Расширенная статистика
- **Коэффициент удержания:** Процент пользователей, вернувшихся через 1/7/30 дней
- **Сессии:** Средняя длительность сессии, количество сессий на пользователя
- **Пути пользователей:** Популярные последовательности страниц (flow analysis)
- **Время использования:** Пиковые часы активности, распределение по дням недели
- **Эффективность:** Среднее время на страницу, bounce rate

#### Метрики для отображения
```javascript
// Структура данных для дашборда
const dashboardMetrics = {
  total_entries: 1234,
  unique_users: 89,
  total_sessions: 456,
  avg_session_duration: '12m 34s',
  peak_hour: '14:00-15:00',
  retention_7d: 78.5, // %
  top_pages: ['/dashboard', '/reports', '/users'],
  device_breakdown: {
    desktop: 65,
    mobile: 30,
    tablet: 5
  }
};
```

### 2. Анализ конкретного пользователя

#### Профиль пользователя
- **Общая информация:** ID, ФИО, email, список отделов, дата первого/последнего входа
- **Сводная статистика:** Общее время использования, количество сессий, среднее время сессии
- **График активности:** Линейный график посещений по дням с возможностью зума
- **Предпочтения:** Любимые страницы, пиковое время активности, предпочитаемые устройства

#### Детальный анализ действий
- **Хронология действий:** Временная линия всех действий с группировкой
- **Группировка по дням:** Ежедневная активность с возможностью разворачивания
- **Анализ сессий:** Автоматическая группировка действий в сессии (интервал > 30 мин)
- **Сравнение периодов:** Динамика активности (текущая неделя vs предыдущая)

#### Пример структуры данных профиля пользователя
```javascript
const userProfile = {
  id: 123,
  name: 'Иван Иванов',
  email: 'ivan@example.com',
  departments: ['Битрикс24 отдел', 'Сектор 1С'],
  first_visit: '2026-01-01T09:00:00Z',
  last_visit: '2026-01-12T14:30:00Z',
  stats: {
    total_actions: 156,
    total_sessions: 23,
    avg_session_duration: '12m 30s',
    favorite_page: '/dashboard',
    peak_hour: 14,
    device_preference: 'desktop'
  }
};
```

### 3. Улучшенные фильтры и группировки

#### Продвинутые фильтры
- **Временные диапазоны:** Сегодня, вчера, 3 дня, неделя, месяц, квартал, год, custom
- **Типы активности:** Множественный выбор с чекбоксами (app_entry, page_visit)
- **Отделы пользователей:** Множественный выбор отделов из списка
- **Частота использования:** Новые (первый вход за период), активные (≥5 действий), неактивные (<5 действий)
- **Устройства:** Desktop, Mobile, Tablet
- **Браузеры:** Chrome, Firefox, Safari, Edge, другие

#### Группировка данных
- **По времени:** Часы, дни, недели, месяцы с возможностью drill-down
- **По пользователям:** Индивидуально или по отделам с агрегацией
- **По страницам:** Группировка по маршрутам с подсчётом посещений
- **По типам действий:** Категории активности с иерархией
- **По сессиям:** Группировка действий пользователя в логические сессии

#### Сохранённые фильтры
- **Предустановки:** "Сегодня", "Активные пользователи", "Проблемные сессии", "Новый трафик"
- **Сохранение:** Возможность сохранения персональных фильтров в localStorage
- **Шаринг:** Экспорт/импорт фильтров в JSON формате
- **Управление:** Список сохранённых фильтров с возможностью редактирования/удаления

#### Пример структуры фильтров
```javascript
const advancedFilters = {
  timeRange: {
    type: 'preset', // 'preset' | 'custom'
    preset: 'week', // 'today' | 'yesterday' | 'week' | 'month' | 'quarter' | 'year'
    custom: {
      from: '2026-01-01',
      to: '2026-01-12'
    }
  },
  activityTypes: ['app_entry', 'page_visit'], // массив выбранных типов
  departments: [369, 366], // массив ID отделов
  userActivity: 'active', // 'all' | 'new' | 'active' | 'inactive'
  devices: ['desktop', 'mobile'], // массив устройств
  browsers: ['chrome', 'firefox'], // массив браузеров
  groupBy: 'day', // 'hour' | 'day' | 'week' | 'month' | 'user' | 'page' | 'session'
  sortBy: 'timestamp', // 'timestamp' | 'user' | 'type' | 'page'
  sortOrder: 'desc' // 'asc' | 'desc'
};
```

---

## 🎨 Дизайн-концепция

### Общий подход

#### 1. Dashboard-first подход
- Главная страница с обзором ключевых метрик
- Возможность drill-down в детали
- Интерактивные графики и диаграммы

#### 2. Контекстная навигация
- Быстрое переключение между общим анализом и анализом пользователя
- Сохранение контекста при навигации
- Breadcrumbs для понимания текущего уровня анализа

#### 3. Адаптивный дизайн
- Полноценная работа на всех устройствах
- Оптимизация под мобильные экраны
- Touch-friendly интерфейс

### Макеты интерфейса

#### Главный дашборд активности
```
┌─────────────────────────────────────────────────────────────┐
│  📊 Дашборд активности пользователей                       │
├─────────────────────────────────────────────────────────────┤
│  [Карточки метрик] [График трендов]                        │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐ │
│  │ Всего       │ Уникальных  │ Открытий    │ Переходов   │ │
│  │ записей     │ пользователей│ приложений  │ страниц     │ │
│  │ 1,234       │ 89          │ 456         │ 778         │ │
│  └─────────────┴─────────────┴─────────────┴─────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  [Фильтры и группировки]                                   │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Период: [Сегодня ▼] Тип: [Все ▼] Отдел: [Все ▼]       │ │
│  └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  [Графики и диаграммы]                                      │
│  ┌─────────────────┬─────────────────┬─────────────────┐   │
│  │ Активность по   │ Популярные      │ Топ             │   │
│  │ времени         │ страницы        │ пользователи    │   │
│  │ [График]        │ [Диаграмма]     │ [Список]        │   │
│  └─────────────────┴─────────────────┴─────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  [Детальная таблица активности]                             │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Пользователь │ Действие │ Страница │ Время           │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │ Иван Иванов  │ app_entry│ -        │ 12:30:15        │ │
│  │ Петр Петров  │ page_visit│ /dashboard│ 12:31:22        │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Профиль пользователя
```
┌─────────────────────────────────────────────────────────────┐
│  👤 Анализ пользователя: Иван Иванов (ID: 123)             │
├─────────────────────────────────────────────────────────────┤
│  [Информация о пользователе]                                │
│  ┌─────────────────┬─────────────────────────────────────┐   │
│  │ Основная информация │ Отделы: Битрикс24 отдел        │   │
│  │ Email: ivan@... │ Последний вход: 2026-01-12 12:30   │   │
│  └─────────────────┴─────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  [Статистика пользователя]                                  │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐ │
│  │ Всего       │ Сессий      │ Ср. время   │ Любимая     │ │
│  │ действий    │             │ сессии      │ страница    │ │
│  │ 156         │ 23          │ 12 мин      │ /dashboard  │ │
│  └─────────────┴─────────────┴─────────────┴─────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  [График активности]                                        │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ [Интерактивный график активности по дням/часам]         │ │
│  └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  [Детальная активность]                                     │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Группировка: [По дням ▼] [По сессиям ▼]                 │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │ 📅 2026-01-12 (5 действий)                              │ │
│  │   ├── 09:15: app_entry                                  │ │
│  │   ├── 09:20: page_visit → /dashboard                    │ │
│  │   ├── 09:25: page_visit → /reports                      │ │
│  │   └── ...                                               │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### Цветовая схема и компоненты

#### Цвета для анализа
- **Основной:** Синий (#2196F3) - для основных метрик и графиков
- **Успех:** Зеленый (#4CAF50) - для положительных трендов и роста
- **Предупреждение:** Оранжевый (#FF9800) - для среднего уровня и предупреждений
- **Ошибка:** Красный (#F44336) - для проблемных зон и снижения
- **Нейтральный:** Серый (#9E9E9E) - для второстепенной информации
- **Акцент:** Фиолетовый (#9C27B0) - для выделения важных элементов
- **Фон графиков:** Светло-серый (#F5F5F5) - для областей графиков

#### Палитра для диаграмм
```javascript
const chartColors = {
  primary: '#2196F3',
  success: '#4CAF50',
  warning: '#FF9800',
  danger: '#F44336',
  info: '#00BCD4',
  secondary: '#9E9E9E',
  accent: '#9C27B0'
};

// Градиенты для линейных графиков
const gradients = {
  primary: ['rgba(33, 150, 243, 0.1)', 'rgba(33, 150, 243, 0.05)'],
  success: ['rgba(76, 175, 80, 0.1)', 'rgba(76, 175, 80, 0.05)'],
  warning: ['rgba(255, 152, 0, 0.1)', 'rgba(255, 152, 0, 0.05)']
};
```

#### Компоненты интерфейса

##### 1. Карточки метрик (MetricCard.vue)
```vue
<template>
  <div class="metric-card" :class="trendClass" @click="handleClick">
    <div class="metric-header">
      <div class="metric-icon">
        <component :is="iconComponent" />
      </div>
      <div class="metric-trend" v-if="showTrend">
        <span class="trend-icon">{{ trendIcon }}</span>
        <span class="trend-value">{{ formattedChange }}</span>
      </div>
    </div>

    <div class="metric-content">
      <div class="metric-value">{{ formattedValue }}</div>
      <div class="metric-title">{{ title }}</div>
      <div class="metric-subtitle" v-if="subtitle">{{ subtitle }}</div>
    </div>

    <div class="metric-actions" v-if="drillDown">
      <button class="drill-down-btn" @click.stop="handleDrillDown">
        Подробнее
      </button>
    </div>
  </div>
</template>
```

##### 2. Интерактивные графики
- **Библиотека:** Chart.js с vue-chartjs
- **Типы графиков:** Line, Bar, Doughnut, Scatter
- **Интерактивность:** Zoom, pan, tooltip, legend click
- **Responsive:** Адаптация под размер контейнера

##### 3. Фильтры с автодополнением
- **Библиотека:** Vue Multiselect или custom implementation
- **Особенности:** Multiple selection, tagging, search
- **Валидация:** Предотвращение конфликтующих фильтров

##### 4. Группируемые списки
- **Структура:** Дерево с уровнями группировки
- **Интерактивность:** Expand/collapse, lazy loading
- **Производительность:** Virtual scrolling для больших списков

##### 5. Временная линия
- **Библиотека:** Custom component с SVG или Canvas
- **Особенности:** Zoom по времени, фильтры по типам
- **Интеграция:** С группировкой по сессиям

---

## 🔧 Техническая реализация

### Архитектура компонентов

#### Структура компонентов анализа активности
```
vue-app/src/components/users/
├── analysis/                          # Новая папка для компонентов анализа
│   ├── ActivityDashboard.vue         # Главный дашборд
│   ├── UserProfileAnalysis.vue       # Анализ профиля пользователя
│   ├── AdvancedFilters.vue           # Продвинутые фильтры
│   ├── ActivityVisualization.vue     # Компоненты визуализации
│   │   ├── TimeChart.vue            # График по времени
│   │   ├── DistributionChart.vue    # Круговая диаграмма
│   │   ├── UserRanking.vue          # Рейтинг пользователей
│   │   └── SessionTimeline.vue      # Временная линия сессий
│   └── ActivityMetrics.vue          # Метрики и KPI
├── UserActivityList.vue              # Улучшенный список активности
├── UserActivityStats.vue             # Улучшенная статистика
└── UserActivityFilters.vue           # Улучшенные фильтры
```

### Vue.js компоненты для создания

#### 1. ActivityDashboard.vue
**Назначение:** Главный дашборд с обзором активности всех пользователей

**Props:**
```javascript
props: {
  filters: {
    type: Object,
    default: () => ({})
  },
  refreshInterval: {
    type: Number,
    default: 300000 // 5 минут
  }
}
```

**Функциональность:**
- Агрегация данных из UserActivityService и ActivityAnalyticsService
- Отображение ключевых метрик в карточках с трендовыми индикаторами
- Интерактивные графики трендов с drill-down возможностями
- Автоматическое обновление данных по таймеру
- Экспорт данных в CSV/PDF

**Пример структуры компонента:**
```vue
<template>
  <div class="activity-dashboard">
    <!-- Метрики -->
    <div class="metrics-grid">
      <MetricCard
        v-for="metric in metrics"
        :key="metric.id"
        :metric="metric"
        @drill-down="handleDrillDown"
      />
    </div>

    <!-- Графики -->
    <div class="charts-grid">
      <TimeChart
        :data="timeData"
        :filters="filters"
        @time-range-change="updateTimeRange"
      />
      <DistributionChart
        :data="distributionData"
        :type="'activity_types'"
      />
      <UserRanking
        :users="topUsers"
        :limit="10"
        @user-select="viewUserProfile"
      />
    </div>

    <!-- Детальная таблица -->
    <ActivityDataTable
      :data="filteredActivity"
      :loading="loading"
      @sort-change="handleSort"
      @export="handleExport"
    />
  </div>
</template>
```

#### 2. UserProfileAnalysis.vue
**Назначение:** Детальный анализ активности конкретного пользователя

**Props:**
```javascript
props: {
  userId: {
    type: Number,
    required: true
  },
  filters: {
    type: Object,
    default: () => ({})
  }
}
```

**Функциональность:**
- Загрузка профиля пользователя через Bitrix24BxApi
- Расчёт персональной статистики активности
- Отображение временной линии с группировкой по сессиям
- Сравнение периодов активности
- Экспорт отчёта по пользователю

**Ключевые методы:**
```javascript
const loadUserProfile = async () => {
  try {
    // Получить данные пользователя
    const user = await Bitrix24BxApi.getCurrentUser(userId);

    // Получить активность пользователя
    const activity = await UserActivityService.getUserActivity(userId, filters.value);

    // Рассчитать статистику
    const stats = ActivityAnalyticsService.calculateUserStats(activity);

    // Сгруппировать по сессиям
    const sessions = ActivityAnalyticsService.groupBySessions(activity);

    return { user, activity, stats, sessions };
  } catch (error) {
    console.error('[UserProfileAnalysis] Error loading user profile:', error);
    throw error;
  }
};
```

#### 3. AdvancedFilters.vue
**Назначение:** Продвинутые фильтры с предустановками и сохранением

**Функциональность:**
- Множественный выбор фильтров с чекбоксами
- Предустановки фильтров ("Сегодня", "Активные пользователи", etc.)
- Сохранение персональных фильтров в localStorage
- Импорт/экспорт фильтров
- История применённых фильтров

**Структура состояния:**
```javascript
const filterState = ref({
  // Временные фильтры
  timeRange: {
    type: 'preset',
    preset: 'week',
    custom: { from: null, to: null }
  },

  // Фильтры активности
  activityTypes: [],
  departments: [],
  userActivity: 'all',

  // Фильтры устройств
  devices: [],
  browsers: [],

  // Группировка и сортировка
  groupBy: 'day',
  sortBy: 'timestamp',
  sortOrder: 'desc'
});
```

**Методы управления фильтрами:**
```javascript
const saveFilterPreset = (name, filters) => {
  const presets = getSavedPresets();
  presets[name] = { ...filters, createdAt: new Date().toISOString() };
  localStorage.setItem('activity_filter_presets', JSON.stringify(presets));
};

const loadFilterPreset = (name) => {
  const presets = getSavedPresets();
  if (presets[name]) {
    filterState.value = { ...presets[name] };
    emit('filters-changed', filterState.value);
  }
};

const exportFilters = () => {
  const dataStr = JSON.stringify(filterState.value, null, 2);
  const dataBlob = new Blob([dataStr], { type: 'application/json' });
  const url = URL.createObjectURL(dataBlob);
  const link = document.createElement('a');
  link.href = url;
  link.download = 'activity-filters.json';
  link.click();
};
```

#### 4. ActivityVisualization.vue
**Назначение:** Базовый компонент для всех типов визуализации

**Подкомпоненты:**
- `TimeChart.vue` - линейные графики активности по времени
- `DistributionChart.vue` - круговые/столбчатые диаграммы распределения
- `UserRanking.vue` - рейтинги пользователей
- `SessionTimeline.vue` - временные линии сессий

**Props для всех визуализаций:**
```javascript
props: {
  data: {
    type: [Array, Object],
    required: true
  },
  type: {
    type: String,
    required: true
  },
  height: {
    type: Number,
    default: 300
  },
  interactive: {
    type: Boolean,
    default: true
  }
}
```

### Улучшение существующих компонентов

#### UserActivityStats.vue → ActivityMetrics.vue
**Изменения:**
- Преобразование статических карточек в интерактивные метрики
- Добавление трендовых индикаторов (▲▼) с процентными изменениями
- Drill-down возможности при клике на метрику
- Сравнение с предыдущим периодом

**Новая структура метрики:**
```javascript
const metricDefinition = {
  id: 'total_entries',
  title: 'Всего записей',
  value: 1234,
  previousValue: 1156,
  change: 78, // абсолютное изменение
  changePercent: 6.7, // процентное изменение
  trend: 'up', // 'up' | 'down' | 'neutral'
  format: 'number', // 'number' | 'percentage' | 'duration'
  drillDownRoute: '/activity/details'
};
```

#### UserActivityFilters.vue → Enhanced Filters
**Изменения:**
- Расширение набора фильтров (устройства, браузеры, отделы)
- Добавление предустановок быстрого доступа
- Возможность сохранения и загрузки фильтров
- Валидация комбинаций фильтров

#### UserActivityList.vue → Grouped Activity List
**Изменения:**
- Группировка записей по времени/пользователям/сессиям
- Древовидное отображение с возможностью сворачивания
- Быстрый поиск по пользователям и страницам
- Пагинация с виртуализацией для больших объёмов данных

### Новые сервисы и утилиты

#### ActivityAnalyticsService
```javascript
export class ActivityAnalyticsService {

  /**
   * Анализ паттернов использования
   */
  static analyzeUsagePatterns(activity) {
    return {
      peakHours: this.calculatePeakHours(activity),
      sessionDuration: this.calculateAverageSession(activity),
      userRetention: this.calculateRetention(activity),
      popularPaths: this.findPopularPaths(activity),
      deviceBreakdown: this.analyzeDevices(activity),
      browserStats: this.analyzeBrowsers(activity)
    };
  }

  /**
   * Группировка активности
   */
  static groupActivity(activity, groupBy) {
    const groups = {};

    activity.forEach(entry => {
      let key;
      switch (groupBy) {
        case 'hour':
          key = new Date(entry.timestamp).getHours();
          break;
        case 'day':
          key = new Date(entry.timestamp).toISOString().split('T')[0];
          break;
        case 'week':
          const date = new Date(entry.timestamp);
          const weekStart = new Date(date.setDate(date.getDate() - date.getDay()));
          key = weekStart.toISOString().split('T')[0];
          break;
        case 'user':
          key = entry.user_id;
          break;
        case 'page':
          key = entry.route_path || entry.route_name || 'unknown';
          break;
        case 'session':
          key = this.calculateSessionId(entry);
          break;
        default:
          key = 'all';
      }

      if (!groups[key]) {
        groups[key] = {
          key,
          entries: [],
          count: 0,
          metadata: this.getGroupMetadata(key, groupBy)
        };
      }

      groups[key].entries.push(entry);
      groups[key].count++;
    });

    return Object.values(groups).sort((a, b) => {
      if (groupBy === 'day' || groupBy === 'week') {
        return new Date(b.key) - new Date(a.key);
      }
      return b.count - a.count;
    });
  }

  /**
   * Расчёт метрик для дашборда
   */
  static calculateDashboardMetrics(activity, previousActivity = []) {
    const current = this.calculateMetrics(activity);
    const previous = previousActivity.length > 0 ? this.calculateMetrics(previousActivity) : {};

    // Расчёт изменений
    const metrics = {};
    Object.keys(current).forEach(key => {
      const currentValue = current[key];
      const previousValue = previous[key] || 0;

      metrics[key] = {
        value: currentValue,
        previousValue,
        change: currentValue - previousValue,
        changePercent: previousValue > 0 ? ((currentValue - previousValue) / previousValue) * 100 : 0,
        trend: currentValue > previousValue ? 'up' : currentValue < previousValue ? 'down' : 'neutral'
      };
    });

    return metrics;
  }

  /**
   * Анализ сессий пользователя
   */
  static analyzeUserSessions(userActivity) {
    const sortedActivity = [...userActivity].sort((a, b) =>
      new Date(a.timestamp) - new Date(b.timestamp)
    );

    const sessions = [];
    let currentSession = null;
    const SESSION_TIMEOUT = 30 * 60 * 1000; // 30 минут

    sortedActivity.forEach(entry => {
      const entryTime = new Date(entry.timestamp).getTime();

      if (!currentSession || (entryTime - currentSession.endTime) > SESSION_TIMEOUT) {
        // Новая сессия
        currentSession = {
          id: `session_${sessions.length + 1}`,
          startTime: entryTime,
          endTime: entryTime,
          entries: [],
          duration: 0,
          pages: new Set(),
          device: entry.user_agent
        };
        sessions.push(currentSession);
      }

      currentSession.entries.push(entry);
      currentSession.endTime = entryTime;
      currentSession.pages.add(entry.route_path);
    });

    // Расчёт длительности сессий
    sessions.forEach(session => {
      session.duration = session.endTime - session.startTime;
      session.pageCount = session.pages.size;
      session.entriesCount = session.entries.length;
    });

    return sessions;
  }
}
```

#### VisualizationHelpers
```javascript
export class VisualizationHelpers {

  /**
   * Подготовка данных для линейного графика по времени
   */
  static prepareTimeChartData(activity, groupBy = 'day') {
    const grouped = ActivityAnalyticsService.groupActivity(activity, groupBy);

    return {
      labels: grouped.map(group => this.formatGroupLabel(group.key, groupBy)),
      datasets: [{
        label: 'Активность',
        data: grouped.map(group => group.count),
        borderColor: '#2196F3',
        backgroundColor: 'rgba(33, 150, 243, 0.1)',
        fill: true,
        tension: 0.4
      }]
    };
  }

  /**
   * Подготовка данных для круговой диаграммы распределения
   */
  static prepareDistributionChartData(activity, type) {
    const distribution = {};

    activity.forEach(entry => {
      let key;
      switch (type) {
        case 'activity_types':
          key = entry.type;
          break;
        case 'devices':
          key = this.detectDevice(entry.user_agent);
          break;
        case 'browsers':
          key = this.detectBrowser(entry.user_agent);
          break;
        case 'departments':
          key = entry.user_department || 'Не указан';
          break;
        default:
          key = 'Другое';
      }

      distribution[key] = (distribution[key] || 0) + 1;
    });

    return {
      labels: Object.keys(distribution),
      datasets: [{
        data: Object.values(distribution),
        backgroundColor: this.generateColors(Object.keys(distribution).length),
        borderWidth: 1
      }]
    };
  }

  /**
   * Форматирование данных для отображения
   */
  static formatActivityData(activity) {
    return activity.map(entry => ({
      ...entry,
      formattedTimestamp: this.formatTimestamp(entry.timestamp),
      formattedDuration: entry.duration ? this.formatDuration(entry.duration) : null,
      userDisplayName: entry.user_name || `User #${entry.user_id}`,
      pageDisplayName: this.formatPageName(entry),
      deviceInfo: this.parseUserAgent(entry.user_agent)
    }));
  }

  /**
   * Определение устройства по User Agent
   */
  static detectDevice(userAgent) {
    if (!userAgent) return 'Неизвестно';

    const ua = userAgent.toLowerCase();
    if (ua.includes('mobile') || ua.includes('android') || ua.includes('iphone')) {
      return 'Mobile';
    }
    if (ua.includes('tablet') || ua.includes('ipad')) {
      return 'Tablet';
    }
    return 'Desktop';
  }

  /**
   * Определение браузера по User Agent
   */
  static detectBrowser(userAgent) {
    if (!userAgent) return 'Неизвестно';

    const ua = userAgent.toLowerCase();
    if (ua.includes('chrome') && !ua.includes('edg')) return 'Chrome';
    if (ua.includes('firefox')) return 'Firefox';
    if (ua.includes('safari') && !ua.includes('chrome')) return 'Safari';
    if (ua.includes('edg')) return 'Edge';
    if (ua.includes('opera')) return 'Opera';
    return 'Другое';
  }

  /**
   * Генерация цветов для диаграмм
   */
  static generateColors(count) {
    const colors = [
      '#2196F3', '#4CAF50', '#FF9800', '#F44336', '#9C27B0',
      '#00BCD4', '#8BC34A', '#FFC107', '#795548', '#607D8B'
    ];

    if (count <= colors.length) {
      return colors.slice(0, count);
    }

    // Генерация дополнительных цветов
    const additional = [];
    for (let i = colors.length; i < count; i++) {
      additional.push(this.generateRandomColor());
    }

    return [...colors, ...additional];
  }
}
```

### Зависимости и интеграция

#### Необходимые npm пакеты
```json
{
  "dependencies": {
    "chart.js": "^4.4.0",
    "vue-chartjs": "^5.3.0",
    "date-fns": "^2.30.0",
    "lodash": "^4.17.21",
    "vue-multiselect": "^3.0.0"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.195"
  }
}
```

#### Интеграция с существующими компонентами

##### 1. UserActivityList.vue → GroupedActivityList.vue
**Изменения в интеграции:**
- Использование ActivityAnalyticsService для группировки
- Интеграция с AdvancedFilters.vue для фильтрации
- Добавление drill-down навигации к профилям пользователей

**Пример интеграции:**
```javascript
// В GroupedActivityList.vue
import { ActivityAnalyticsService } from '@/services/activity-analytics-service.js';
import { VisualizationHelpers } from '@/utils/visualization-helpers.js';

const loadGroupedActivity = async () => {
  const rawActivity = await UserActivityService.getActivity(filters.value);
  const filteredActivity = filterHiddenUsers(rawActivity);
  const groupedActivity = ActivityAnalyticsService.groupActivity(
    filteredActivity,
    filters.value.groupBy
  );

  // Подготовка данных для отображения
  groupedActivity.value = groupedActivity.map(group => ({
    ...group,
    formattedData: VisualizationHelpers.formatActivityData(group.entries)
  }));
};
```

##### 2. UserActivityStats.vue → ActivityMetrics.vue
**Интеграция с новыми сервисами:**
- Использование ActivityAnalyticsService для расчёта метрик
- Интеграция с ActivityDashboard для drill-down
- Добавление сравнения периодов

##### 3. HiddenUsersManager.vue
**Улучшения:**
- Интеграция с AdvancedFilters для применения фильтров
- Добавление статистики по скрытым пользователям
- Уведомления об изменениях через Event Bus

#### API endpoints и сервисы

##### Новые API endpoints (опционально)
```
GET /api/activity/metrics?period=week&compare=true
- Получение агрегированных метрик с сравнением периодов

GET /api/activity/user-profile/{userId}?period=month
- Детальный профиль пользователя с расчётами

POST /api/activity/export
- Экспорт данных активности в различных форматах
```

##### Расширение существующих сервисов

**UserActivityService enhancements:**
```javascript
export class UserActivityService {
  // Существующие методы...

  /**
   * Получение метрик для дашборда
   */
  static async getDashboardMetrics(filters = {}) {
    const activity = await this.getActivity(filters);

    // Получить данные за предыдущий период для сравнения
    const previousFilters = this.getPreviousPeriodFilters(filters);
    const previousActivity = await this.getActivity(previousFilters);

    return ActivityAnalyticsService.calculateDashboardMetrics(activity, previousActivity);
  }

  /**
   * Получение профиля пользователя с аналитикой
   */
  static async getUserProfileAnalytics(userId, filters = {}) {
    const [userActivity, userInfo] = await Promise.all([
      this.getUserActivity(userId, filters),
      Bitrix24BxApi.getCurrentUser(userId)
    ]);

    return {
      user: userInfo,
      activity: userActivity,
      analytics: ActivityAnalyticsService.calculateUserStats(userActivity),
      sessions: ActivityAnalyticsService.analyzeUserSessions(userActivity)
    };
  }
}
```

#### Обработка ошибок и валидация

##### Глобальная обработка ошибок
```javascript
// В main.js или отдельном модуле
import { createApp } from 'vue';
import { ActivityErrorHandler } from '@/utils/activity-error-handler.js';

const app = createApp(App);

// Глобальная обработка ошибок анализа активности
app.config.errorHandler = (error, instance, info) => {
  ActivityErrorHandler.handle(error, {
    component: instance?.$?.type?.name,
    info,
    context: 'activity_analysis'
  });
};
```

##### Валидация данных
```javascript
// Валидация фильтров
export const validateActivityFilters = (filters) => {
  const errors = [];

  // Валидация временного диапазона
  if (filters.timeRange?.custom) {
    const { from, to } = filters.timeRange.custom;
    if (from && to && new Date(from) > new Date(to)) {
      errors.push('Дата начала не может быть позже даты окончания');
    }
  }

  // Валидация лимитов
  if (filters.limit && filters.limit > 10000) {
    errors.push('Максимальный лимит записей: 10,000');
  }

  return errors;
};

// Валидация данных активности
export const validateActivityEntry = (entry) => {
  const required = ['timestamp', 'type', 'user_id'];

  for (const field of required) {
    if (!entry[field]) {
      throw new Error(`Отсутствует обязательное поле: ${field}`);
    }
  }

  // Валидация типа активности
  if (!['app_entry', 'page_visit'].includes(entry.type)) {
    throw new Error(`Неверный тип активности: ${entry.type}`);
  }

  return true;
};
```

#### Производительность и оптимизация

##### Lazy loading и виртуализация
```javascript
// Для больших списков активности
import { VirtualList } from '@/components/common/virtual-list.vue';

const handleLargeDataset = (activity) => {
  if (activity.length > 1000) {
    // Использовать виртуальную прокрутку
    return {
      component: VirtualList,
      props: {
        items: activity,
        itemHeight: 50,
        containerHeight: 500
      }
    };
  }

  return activity;
};
```

##### Кеширование данных
```javascript
// Кеш для метрик дашборда
const dashboardCache = new Map();

export const getCachedDashboardMetrics = async (filters) => {
  const cacheKey = JSON.stringify(filters);

  if (dashboardCache.has(cacheKey)) {
    const cached = dashboardCache.get(cacheKey);
    if (Date.now() - cached.timestamp < 5 * 60 * 1000) { // 5 минут
      return cached.data;
    }
  }

  const data = await UserActivityService.getDashboardMetrics(filters);
  dashboardCache.set(cacheKey, {
    data,
    timestamp: Date.now()
  });

  return data;
};
```

---

## ✅ Критерии приёмки

### Функциональные критерии

#### Дашборд активности (общий анализ)
- [ ] Отображение ключевых метрик в карточках
- [ ] Интерактивные графики активности по времени
- [ ] Диаграмма распределения по типам действий
- [ ] Топ пользователей по активности
- [ ] Возможность drill-down в детали

#### Анализ пользователя (детальный анализ)
- [ ] Профиль пользователя с основной информацией
- [ ] Статистика активности пользователя
- [ ] Временная линия всех действий
- [ ] Группировка действий по дням и сессиям
- [ ] Сравнение активности по периодам

#### Фильтры и группировка
- [ ] Расширенные фильтры по времени, типам, отделам
- [ ] Предустановки фильтров
- [ ] Сохранение персональных фильтров
- [ ] Комбинирование нескольких фильтров

### UI/UX критерии

#### Визуализация данных
- [ ] Читаемые и понятные графики
- [ ] Консистентная цветовая схема
- [ ] Адаптивный дизайн для всех экранов
- [ ] Быстрая загрузка интерфейса

#### Навигация и взаимодействие
- [ ] Интуитивное переключение между режимами анализа
- [ ] Сохранение контекста при навигации
- [ ] Быстрый поиск и фильтрация
- [ ] Обратная связь при действиях пользователя

### Технические критерии

#### Производительность
- [ ] Время загрузки < 2 секунд для основных данных
- [ ] Плавная анимация графиков
- [ ] Оптимизированная обработка больших объёмов данных
- [ ] Эффективное кеширование

#### Качество кода
- [ ] Компоненты соответствуют Vue.js best practices
- [ ] Читаемый и поддерживаемый код
- [ ] Подробные комментарии
- [ ] Обработка ошибок

---

## 🧪 Тестирование

### Unit тестирование

#### Тестирование сервисов
```javascript
// tests/unit/services/ActivityAnalyticsService.spec.js
describe('ActivityAnalyticsService', () => {
  describe('analyzeUsagePatterns', () => {
    test('should calculate peak hours correctly', () => {
      const activity = [
        { timestamp: '2026-01-12T10:00:00Z', type: 'app_entry' },
        { timestamp: '2026-01-12T14:00:00Z', type: 'app_entry' },
        { timestamp: '2026-01-12T14:30:00Z', type: 'page_visit' }
      ];

      const result = ActivityAnalyticsService.analyzeUsagePatterns(activity);

      expect(result.peakHours).toEqual({
        10: 1,
        14: 2
      });
    });

    test('should calculate average session duration', () => {
      // Тесты для расчёта средней длительности сессии
    });
  });

  describe('groupActivity', () => {
    test('should group by day correctly', () => {
      const activity = [
        { timestamp: '2026-01-12T10:00:00Z', user_id: 1 },
        { timestamp: '2026-01-12T14:00:00Z', user_id: 1 },
        { timestamp: '2026-01-13T10:00:00Z', user_id: 2 }
      ];

      const grouped = ActivityAnalyticsService.groupActivity(activity, 'day');

      expect(grouped).toHaveLength(2);
      expect(grouped[0].key).toBe('2026-01-12');
      expect(grouped[0].count).toBe(2);
    });
  });
});
```

#### Тестирование компонентов
```javascript
// tests/unit/components/ActivityDashboard.spec.js
describe('ActivityDashboard.vue', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = mount(ActivityDashboard, {
      props: {
        filters: { timeRange: 'week' }
      },
      global: {
        mocks: {
          $router: { push: vi.fn() }
        }
      }
    });
  });

  test('should load metrics on mount', async () => {
    await flushPromises();

    expect(wrapper.vm.metrics).toBeDefined();
    expect(wrapper.findAll('.metric-card')).toHaveLength(4);
  });

  test('should handle drill-down click', async () => {
    const metricCard = wrapper.find('.metric-card');
    await metricCard.trigger('click');

    expect(wrapper.vm.$router.push).toHaveBeenCalled();
  });
});
```

### E2E тестирование

#### Сценарии тестирования
```javascript
// tests/e2e/activity-analysis.spec.js
describe('Activity Analysis', () => {
  beforeEach(() => {
    // Авторизация как администратор
    cy.loginAsAdmin();
    cy.visit('/admin/users');
  });

  it('should display dashboard with metrics', () => {
    cy.get('.activity-dashboard').should('be.visible');
    cy.get('.metric-card').should('have.length.at.least', 4);

    // Проверка загрузки графиков
    cy.get('.time-chart').should('be.visible');
    cy.get('.distribution-chart').should('be.visible');
  });

  it('should filter activity by date range', () => {
    // Открыть фильтры
    cy.get('.advanced-filters-toggle').click();

    // Выбрать временной диапазон
    cy.get('[data-cy="time-range-preset"]').select('week');

    // Проверить применение фильтров
    cy.get('[data-cy="apply-filters"]').click();

    // Проверить обновление данных
    cy.get('.activity-dashboard').should('contain', 'обновлено');
  });

  it('should navigate to user profile', () => {
    // Клик на пользователя в списке
    cy.get('.user-item').first().click();

    // Проверить переход на страницу профиля
    cy.url().should('include', '/user/');
    cy.get('.user-profile').should('be.visible');
  });

  it('should export activity data', () => {
    // Клик на кнопку экспорта
    cy.get('[data-cy="export-button"]').click();

    // Выбрать формат
    cy.get('[data-cy="export-format"]').select('csv');

    // Начать экспорт
    cy.get('[data-cy="start-export"]').click();

    // Проверить скачивание файла
    cy.readFile('cypress/downloads/activity-export.csv').should('exist');
  });
});
```

### Интеграционное тестирование

#### Тестирование с API
```javascript
// tests/integration/api/activity-api.spec.js
describe('Activity API Integration', () => {
  test('should fetch activity with filters', async () => {
    const response = await request(app)
      .get('/api/user-activity-get.php')
      .query({
        user_id: 123,
        date_from: '2026-01-01',
        date_to: '2026-01-12',
        type: 'app_entry'
      });

    expect(response.status).toBe(200);
    expect(response.body.success).toBe(true);
    expect(Array.isArray(response.body.data)).toBe(true);
  });

  test('should handle large datasets efficiently', async () => {
    // Создать большой набор тестовых данных
    const largeDataset = generateLargeActivityDataset(5000);

    const startTime = Date.now();

    const response = await request(app)
      .get('/api/user-activity-get.php')
      .query({ limit: 5000 });

    const endTime = Date.now();
    const responseTime = endTime - startTime;

    expect(response.status).toBe(200);
    expect(responseTime).toBeLessThan(2000); // Менее 2 секунд
  });
});
```

### Производительность и нагрузочное тестирование

#### Бенчмарки производительности
```javascript
// tests/performance/activity-performance.spec.js
describe('Activity Analysis Performance', () => {
  test('should render dashboard within 2 seconds', async () => {
    const startTime = performance.now();

    // Монтирование компонента
    const wrapper = mount(ActivityDashboard);

    // Ожидание загрузки данных
    await flushPromises();

    const endTime = performance.now();
    const renderTime = endTime - startTime;

    expect(renderTime).toBeLessThan(2000);
  });

  test('should handle 10000 activity entries efficiently', async () => {
    const largeActivity = generateActivityData(10000);

    const startTime = performance.now();

    // Группировка данных
    const grouped = ActivityAnalyticsService.groupActivity(largeActivity, 'day');

    const endTime = performance.now();
    const processingTime = endTime - startTime;

    expect(processingTime).toBeLessThan(500); // Менее 500мс
    expect(grouped.length).toBeGreaterThan(0);
  });
});
```

### Тестирование доступности (Accessibility)

#### WCAG compliance
```javascript
// tests/accessibility/activity-accessibility.spec.js
describe('Activity Analysis Accessibility', () => {
  test('should have proper ARIA labels', () => {
    const wrapper = mount(ActivityDashboard);

    // Проверка ARIA меток на графиках
    expect(wrapper.find('[aria-label="График активности по времени"]')).toBeDefined();

    // Проверка альтернативного текста
    expect(wrapper.find('img[alt]')).toBeDefined();
  });

  test('should be navigable with keyboard', () => {
    const wrapper = mount(ActivityDashboard);

    // Фокус на интерактивных элементах
    wrapper.find('.metric-card').first().focus();

    // Проверка видимости фокуса
    expect(wrapper.find('.metric-card:focus')).toBeDefined();
  });

  test('should have sufficient color contrast', () => {
    // Проверка контрастности цветов через axe-core
    cy.injectAxe();
    cy.checkA11y('.activity-dashboard', {
      rules: {
        'color-contrast': { enabled: true }
      }
    });
  });
});
```

### Ручное тестирование (Manual QA)

#### Чек-лист для QA инженера
- [ ] **Dashboard loading:** Проверка быстрой загрузки дашборда
- [ ] **Filter application:** Применение различных комбинаций фильтров
- [ ] **Data accuracy:** Сверка рассчитанных метрик с исходными данными
- [ ] **Export functionality:** Тестирование экспорта в разных форматах
- [ ] **User profile navigation:** Переход к профилям пользователей
- [ ] **Responsive design:** Тестирование на мобильных устройствах
- [ ] **Error handling:** Проверка обработки ошибок и edge cases
- [ ] **Performance:** Мониторинг производительности при больших объёмах данных

---

## 🔗 Связанные документы

- [TASK-088](./TASK-088-user-management-module-analysis.md) - анализ модуля управления пользователями
- `vue-app/src/services/user-activity-service.js` - сервис активности
- `vue-app/src/components/users/` - существующие компоненты активности

---

## 📝 История изменений

- 2026-01-12 13:00 (UTC+3, Брест): Создан документ задачи на улучшение UI анализа активности пользователей

---

## 🎯 Следующие шаги

1. **UIX дизайнер:**
   - Создать детальные макеты интерфейса
   - Определить компоненты и взаимодействия
   - Подготовить дизайн-систему для графиков и метрик

2. **Vue.js разработчик:**
   - Реализовать новые компоненты анализа
   - Улучшить существующие компоненты
   - Интегрировать графики и визуализации
   - Оптимизировать производительность

**Ответственный:** UIX дизайнер → Vue.js разработчик  
**Срок выполнения:** 3 недели  
**Приоритет:** Средний

---

## 📋 Примеры реализации

### Макет главного дашборда
```
┌─────────────────────────────────────────────────────────────┐
│  📊 Дашборд активности пользователей                       │
├─────────────────────────────────────────────────────────────┤
│  [Метрики] [Время: Сегодня ▼] [Обновить ↻]                 │
│  ┌─────┬─────┬─────┬─────┐  ┌─────────────────────────────┐ │
│  │1,234│ 89  │456 │778 │  │  График активности по времени │ │
│  │+6.7%│+12% │-2% │+15%│  │  [██████████████░░░░░░░░░░░] │ │
│  │Всего│Уник.│Открыт│Перех│  └─────────────────────────────┘ │
│  └─────┴─────┴─────┴─────┘                                  │
├─────────────────────────────────────────────────────────────┤
│  [Фильтры: Тип ▼] [Группировка ▼] [Экспорт ▼]              │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────┬─────────────────┬─────────────────┐   │
│  │ Популярные      │ Топ             │ Активность по   │   │
│  │ страницы        │ пользователи    │ времени суток   │   │
│  │ • /dashboard    │ 1. Иван Ив.     │ ████████████░░░ │   │
│  │ • /reports      │ 2. Петр Пет.    │ 10:00 - 18:00   │   │
│  │ • /users        │ 3. Анна Ан.     │                 │   │
│  └─────────────────┴─────────────────┴─────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Детальная таблица активности                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │Время      │Пользователь │Действие    │Страница         │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │14:30:15   │Иван Ив.    │app_entry   │-                │ │
│  │14:31:22   │Петр Пет.   │page_visit  │/dashboard       │ │
│  │14:32:45   │Иван Ив.    │page_visit  │/reports         │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### Макет профиля пользователя
```
┌─────────────────────────────────────────────────────────────┐
│  👤 Иван Иванов (ID: 123) | [← Назад] [Экспорт]            │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────┬─────────────────────────────────────┐   │
│  │ 📧 ivan@...     │ Отделы: Битрикс24 отдел            │   │
│  │ 📅 Первый вход: │ Последний вход: 2026-01-12 14:30   │   │
│  │ 2026-01-01      │                                     │   │
│  └─────────────────┴─────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Статистика активности                                     │
│  ┌─────┬─────┬─────┬─────┬─────┐ ┌───────────────────────┐ │
│  │156  │23   │12м  │14   │8:00 │ │ График активности     │ │
│  │дейст│сессий│сессия│час │пиков│ │ [░░░███████████████] │ │
│  └─────┴─────┴─────┴─────┴─────┘ └───────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  Хронология действий                                       │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ [По дням ▼] [По сессиям ▼] [Фильтр ▼]                  │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │ 📅 2026-01-12 (5 действий) [+]                          │ │
│  │ ├── 14:30 app_entry                                     │ │
│  │ ├── 14:31 page_visit → /dashboard                       │ │
│  │ └── 14:45 page_visit → /reports                         │ │
│  │                                                         │ │
│  │ 📅 2026-01-11 (12 действий) [+]                         │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### Структура компонентов в коде
```vue
<!-- ActivityDashboard.vue -->
<template>
  <div class="activity-dashboard">
    <!-- Заголовок с контролами -->
    <div class="dashboard-header">
      <h1>📊 Дашборд активности пользователей</h1>
      <div class="header-controls">
        <TimeRangeSelector v-model="timeRange" />
        <button @click="refresh" class="refresh-btn">↻</button>
      </div>
    </div>

    <!-- Метрики -->
    <div class="metrics-grid">
      <MetricCard
        v-for="metric in metrics"
        :key="metric.id"
        :metric="metric"
        @drill-down="handleDrillDown"
      />
    </div>

    <!-- Фильтры -->
    <AdvancedFilters
      v-model="filters"
      @change="applyFilters"
    />

    <!-- Графики -->
    <div class="charts-section">
      <TimeChart :data="timeChartData" />
      <DistributionChart :data="distributionData" />
      <UserRanking :users="topUsers" />
    </div>

    <!-- Детальная таблица -->
    <ActivityDataTable
      :data="activityData"
      :loading="loading"
      @sort="handleSort"
      @export="handleExport"
    />
  </div>
</template>

<script>
import { ref, computed, onMounted } from 'vue';
import { UserActivityService } from '@/services/user-activity-service.js';
import { ActivityAnalyticsService } from '@/services/activity-analytics-service.js';

export default {
  name: 'ActivityDashboard',
  setup() {
    const timeRange = ref('week');
    const filters = ref({});
    const rawMetrics = ref({});
    const loading = ref(false);

    // Вычисляемые метрики с трендами
    const metrics = computed(() => {
      return ActivityAnalyticsService.calculateDashboardMetrics(
        rawMetrics.value,
        rawMetrics.value.previous // Данные за предыдущий период
      );
    });

    const loadDashboard = async () => {
      loading.value = true;
      try {
        rawMetrics.value = await UserActivityService.getDashboardMetrics({
          timeRange: timeRange.value,
          ...filters.value
        });
      } catch (error) {
        console.error('[ActivityDashboard] Error loading dashboard:', error);
      } finally {
        loading.value = false;
      }
    };

    onMounted(() => {
      loadDashboard();
    });

    return {
      timeRange,
      filters,
      metrics,
      loading,
      loadDashboard
    };
  }
};
</script>
```

---

## 🔗 Связанные документы

- [TASK-088](./TASK-088-user-management-module-analysis.md) - анализ модуля управления пользователями
- `vue-app/src/services/user-activity-service.js` - сервис активности
- `vue-app/src/components/users/` - существующие компоненты активности
- `api/user-activity-get.php` - API для получения активности
- `api/user-activity-log.php` - API для логирования активности

---

## ✅ Результат выполнения

### Реализованная функциональность

#### 🎯 Дашборд анализа активности
- **Метрики с трендами:** 4 ключевые метрики (записи, пользователи, сессии, длительность) с индикаторами изменений
- **Интерактивные графики:** Линейные графики активности по времени с группировкой (часы/дни/недели/месяцы)
- **Диаграммы распределения:** Круговые и столбчатые диаграммы по типам активности, устройствам, браузерам
- **Рейтинг пользователей:** Топ активных пользователей с drill-down на профили

#### 👤 Анализ профиля пользователя
- **Персональная статистика:** Общая активность, сессии, предпочтения устройств и времени
- **Графики активности:** Персональные графики посещений с возможностью зума
- **Временная линия сессий:** Детальный анализ сессий с группировкой по дням
- **Сравнение периодов:** Динамика активности между периодами

#### 🔍 Продвинутые фильтры
- **Предустановки:** Быстрые фильтры ("Сегодня", "Активные пользователи", "Новый трафик")
- **Множественная фильтрация:** По времени, типам активности, отделам, устройствам, браузерам
- **Сохранение фильтров:** Персональные фильтры в localStorage с возможностью экспорта/импорта
- **Группировка данных:** По времени, пользователям, страницам, сессиям

#### 📊 Компоненты визуализации
- **TimeChart:** Интерактивные графики с drill-down возможностями
- **DistributionChart:** Адаптивные диаграммы с легендой и подсказками
- **UserRanking:** Рейтинг с пагинацией и drill-down на профили
- **ActivityDataTable:** Таблица с поиском, сортировкой и виртуализацией

#### ⚡ Технические улучшения
- **Кеширование:** 5-минутный кеш API запросов для повышения производительности
- **Debounced поиск:** Оптимизированный поиск с 300мс задержкой
- **Виртуализация:** VirtualList компонент для больших наборов данных
- **Drill-down навигация:** Бесшовное переключение между режимами анализа

### Архитектура решения
```
vue-app/src/
├── services/
│   ├── activity-analytics-service.js     ✅ Создан
│   ├── user-activity-service.js          ✅ Расширен
│   └── visualization-helpers.js          ✅ Создан
└── components/
    ├── users/analysis/                   ✅ Новая папка
    │   ├── ActivityDashboard.vue         ✅ Главный дашборд
    │   ├── UserProfileAnalysis.vue       ✅ Профиль пользователя
    │   ├── AdvancedFilters.vue           ✅ Продвинутые фильтры
    │   ├── TimeChart.vue                 ✅ График по времени
    │   ├── DistributionChart.vue         ✅ Диаграммы распределения
    │   ├── UserRanking.vue               ✅ Рейтинг пользователей
    │   ├── ActivityDataTable.vue         ✅ Таблица данных
    │   └── MetricCard.vue                ✅ Карточки метрик
    └── common/
        └── VirtualList.vue               ✅ Виртуализация
```

### Критерии приёмки
- ✅ **Функциональные:** Все компоненты работают, фильтры применяются, drill-down навигация функционирует
- ✅ **UI/UX:** Адаптивный дизайн, интуитивное взаимодействие, визуализация данных
- ✅ **Технические:** Производительность оптимизирована, код соответствует стандартам
- ✅ **Интеграция:** Полная интеграция с существующими компонентами и API

### Связанные задачи
- ✅ [TASK-088-02](./TASK-088-02-fix-bitrix24-api-import-conflict.md) - Решение конфликта импортов Bitrix24 API

---

## 📝 История изменений

- 2026-01-12 13:00 (UTC+3, Брест): Создан документ задачи на улучшение UI анализа активности пользователей
- 2026-01-12 14:30 (UTC+3, Брест): Добавлены детальные технические спецификации, примеры кода и разделы о зависимостях, интеграции и тестировании
- 2026-01-12 18:00 (UTC+3, Брест): ✅ Задача полностью выполнена - реализованы все компоненты анализа активности, интегрирована drill-down навигация, оптимизирована производительность