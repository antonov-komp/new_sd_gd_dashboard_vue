# TASK-019-05-01: Создание базового компонента CreateSnapshotButton с кнопкой и модальным окном подтверждения

**Дата создания:** 2025-12-08 11:00 (UTC+3, Брест)  
**Статус:** Выполнена  
**Дата завершения:** 2025-12-08 13:54 (UTC+3, Брест)  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vue.js)  
**Родительская задача:** TASK-019-00 (Этап 5, часть 1)

---

## 📋 Цель задачи

Создать базовый Vue.js компонент `CreateSnapshotButton.vue` с кнопкой для ручного создания слепков состояния сектора. Компонент должен отображаться только для администраторов, иметь модальное окно подтверждения и базовую структуру для интеграции с сервисами.

---

## 🎯 Контекст

Для ручного создания слепков администраторами необходимо создать UI-компонент, который будет:
- Отображаться только для пользователей с правами администратора
- Иметь кнопку "Создать слепок" с понятным интерфейсом
- Показывать модальное окно подтверждения перед созданием
- Иметь базовую структуру для интеграции с сервисами (в следующей задаче)

**Требования:**
- Компонент должен быть переиспользуемым и изолированным
- Проверка прав администратора через `isAdmin()` из `access-config.js`
- Модальное окно с подтверждением действия
- Базовые стили, соответствующие гайдлайнам Bitrix24

**Зависимости:**
- Этап 2 (TASK-019-02-01, TASK-019-02-02) должен быть завершён — `SnapshotService` создан
- Этап 3 (TASK-019-03-01, TASK-019-03-02) должен быть завершён — нормализация данных реализована
- Композабл `useNotifications` существует и работает
- Функция `isAdmin()` из `access-config.js` доступна

---

## 🏗️ Архитектура решения

### Структура файлов

```
vue-app/src/components/graph-state/
├── CreateSnapshotButton.vue      # Основной компонент
└── CreateSnapshotModal.vue        # Модальное окно подтверждения (опционально, можно встроить)
```

### Компонент CreateSnapshotButton

**Паттерн:** Компонентный подход Vue.js 3 (Composition API)

**Props:**
- `user` (Object, опционально) — объект текущего пользователя. Если не передан, получается из `AccessControlService`

**Emits:**
- `snapshot-created` — событие при успешном создании слепка (передаёт созданный слепок)

**Composables:**
- `useNotifications` — для уведомлений об успехе/ошибке
- `AccessControlService` — для проверки прав доступа (опционально)

---

## 📝 Что нужно сделать

### 1. Создать базовую структуру компонента

**Расположение:** `vue-app/src/components/graph-state/CreateSnapshotButton.vue`

**Структура компонента:**
- Template с кнопкой "Создать слепок"
- Script setup с логикой компонента
- Style с базовыми стилями

**Функциональность:**
- Проверка прав администратора
- Отображение кнопки только для администраторов
- Обработчик клика для открытия модального окна
- Базовое состояние (isModalOpen, isLoading)

---

### 2. Реализовать модальное окно подтверждения

**Варианты реализации:**
- Встроенное модальное окно в том же компоненте
- Отдельный компонент `CreateSnapshotModal.vue` (рекомендуется для переиспользования)

**Содержимое модального окна:**
- Заголовок: "Создать слепок состояния сектора"
- Описание: "Будет создан слепок текущего состояния сектора 1С. Продолжить?"
- Кнопки:
  - "Отмена" — закрывает модальное окно
  - "Создать" — запускает процесс создания слепка

**Функциональность:**
- Открытие/закрытие модального окна
- Обработка клика по кнопке "Создать"
- Обработка клика по кнопке "Отмена"
- Закрытие по клику вне модального окна (опционально)
- Закрытие по нажатию Escape (опционально)

---

### 3. Добавить проверку прав администратора

**Логика:**
- Получить текущего пользователя (через `AccessControlService` или из props)
- Проверить права через `isAdmin(user)`
- Отображать компонент только если пользователь администратор

**Реализация:**
```javascript
import { isAdmin } from '@/config/access-config.js';
import { AccessControlService } from '@/services/access-control-service.js';

// В setup()
const currentUser = ref(null);
const isUserAdmin = computed(() => {
  if (!currentUser.value) {
    return false;
  }
  return isAdmin(currentUser.value);
});

// Загрузка пользователя при монтировании
onMounted(async () => {
  const accessResult = await AccessControlService.checkAccess();
  if (accessResult.allowed) {
    currentUser.value = accessResult.user;
  }
});
```

---

### 4. Добавить базовые стили

**Требования:**
- Стили должны соответствовать гайдлайнам Bitrix24
- Адаптивность для мобильных устройств
- Плавные анимации для модального окна
- Использование CSS-переменных для цветов (если есть)

---

## 💡 Примеры кода

### Базовый компонент CreateSnapshotButton.vue

```vue
<template>
  <div v-if="isUserAdmin" class="create-snapshot-button-container">
    <!-- Кнопка создания слепка -->
    <button
      class="create-snapshot-btn"
      @click="openModal"
      :disabled="isLoading"
      title="Создать слепок состояния сектора"
    >
      <span class="btn-icon">📸</span>
      <span class="btn-text">Создать слепок</span>
    </button>

    <!-- Модальное окно подтверждения -->
    <Teleport to="body">
      <Transition name="modal">
        <div
          v-if="isModalOpen"
          class="modal-overlay"
          @click.self="closeModal"
        >
          <div class="modal-content">
            <div class="modal-header">
              <h3 class="modal-title">Создать слепок состояния сектора</h3>
              <button
                class="modal-close"
                @click="closeModal"
                aria-label="Закрыть"
              >
                ✕
              </button>
            </div>

            <div class="modal-body">
              <p class="modal-description">
                Будет создан слепок текущего состояния сектора 1С.
                Слепок будет сохранён с типом "manual" (ручной).
              </p>
              <p class="modal-warning">
                ⚠️ Процесс создания слепка может занять некоторое время.
              </p>
            </div>

            <div class="modal-footer">
              <button
                class="btn btn-secondary"
                @click="closeModal"
                :disabled="isLoading"
              >
                Отмена
              </button>
              <button
                class="btn btn-primary"
                @click="handleCreate"
                :disabled="isLoading"
              >
                <span v-if="isLoading">Создание...</span>
                <span v-else>Создать</span>
              </button>
            </div>
          </div>
        </div>
      </Transition>
    </Teleport>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue';
import { isAdmin } from '@/config/access-config.js';
import { AccessControlService } from '@/services/access-control-service.js';
import { useNotifications } from '@/composables/useNotifications.js';

/**
 * Props компонента
 */
const props = defineProps({
  /**
   * Объект текущего пользователя (опционально)
   * Если не передан, будет получен через AccessControlService
   */
  user: {
    type: Object,
    default: null
  }
});

/**
 * Emits компонента
 */
const emit = defineEmits(['snapshot-created']);

/**
 * Состояние компонента
 */
const currentUser = ref(props.user);
const isModalOpen = ref(false);
const isLoading = ref(false);

/**
 * Композаблы
 */
const notifications = useNotifications();

/**
 * Проверка, является ли пользователь администратором
 */
const isUserAdmin = computed(() => {
  if (!currentUser.value) {
    return false;
  }
  return isAdmin(currentUser.value);
});

/**
 * Загрузка пользователя при монтировании (если не передан через props)
 */
onMounted(async () => {
  if (!props.user) {
    try {
      const accessResult = await AccessControlService.checkAccess();
      if (accessResult.allowed) {
        currentUser.value = accessResult.user;
      }
    } catch (error) {
      console.error('Error loading user:', error);
    }
  }
});

/**
 * Открытие модального окна
 */
const openModal = () => {
  if (!isUserAdmin.value) {
    notifications.warning('Только администраторы могут создавать слепки');
    return;
  }
  isModalOpen.value = true;
};

/**
 * Закрытие модального окна
 */
const closeModal = () => {
  if (isLoading.value) {
    return; // Не закрывать во время создания
  }
  isModalOpen.value = false;
};

/**
 * Обработка создания слепка
 * 
 * Пока только базовая структура, полная реализация будет в следующей задаче
 */
const handleCreate = async () => {
  if (isLoading.value) {
    return;
  }

  isLoading.value = true;

  try {
    // TODO: Реализовать создание слепка в следующей задаче
    // const snapshot = await createSnapshot();
    // emit('snapshot-created', snapshot);
    // notifications.success('Слепок успешно создан');
    
    // Временная заглушка
    await new Promise(resolve => setTimeout(resolve, 1000));
    notifications.info('Создание слепка будет реализовано в следующей задаче');
    
    closeModal();
  } catch (error) {
    console.error('Error creating snapshot:', error);
    notifications.error('Ошибка создания слепка: ' + error.message);
  } finally {
    isLoading.value = false;
  }
};

/**
 * Закрытие модального окна по Escape
 */
const handleEscape = (event) => {
  if (event.key === 'Escape' && isModalOpen.value && !isLoading.value) {
    closeModal();
  }
};

// Добавление обработчика Escape при монтировании
onMounted(() => {
  window.addEventListener('keydown', handleEscape);
});

// Удаление обработчика при размонтировании
import { onUnmounted } from 'vue';
onUnmounted(() => {
  window.removeEventListener('keydown', handleEscape);
});
</script>

<style scoped>
.create-snapshot-button-container {
  display: inline-block;
}

.create-snapshot-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 16px;
  background-color: #007bff;
  color: white;
  border: none;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: background-color 0.2s ease;
}

.create-snapshot-btn:hover:not(:disabled) {
  background-color: #0056b3;
}

.create-snapshot-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.btn-icon {
  font-size: 16px;
}

.btn-text {
  line-height: 1;
}

/* Модальное окно */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
  padding: 20px;
}

.modal-content {
  background: white;
  border-radius: 8px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
  max-width: 500px;
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px;
  border-bottom: 1px solid #e0e0e0;
}

.modal-title {
  margin: 0;
  font-size: 18px;
  font-weight: 600;
  color: #333;
}

.modal-close {
  background: none;
  border: none;
  font-size: 24px;
  color: #999;
  cursor: pointer;
  padding: 0;
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  transition: background-color 0.2s ease;
}

.modal-close:hover {
  background-color: #f5f5f5;
  color: #333;
}

.modal-body {
  padding: 20px;
}

.modal-description {
  margin: 0 0 12px 0;
  color: #666;
  line-height: 1.5;
}

.modal-warning {
  margin: 0;
  padding: 12px;
  background-color: #fff3cd;
  border: 1px solid #ffc107;
  border-radius: 4px;
  color: #856404;
  font-size: 14px;
  line-height: 1.5;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  padding: 20px;
  border-top: 1px solid #e0e0e0;
}

.btn {
  padding: 10px 20px;
  border: none;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
}

.btn-primary {
  background-color: #007bff;
  color: white;
}

.btn-primary:hover:not(:disabled) {
  background-color: #0056b3;
}

.btn-secondary {
  background-color: #6c757d;
  color: white;
}

.btn-secondary:hover:not(:disabled) {
  background-color: #545b62;
}

.btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

/* Анимации модального окна */
.modal-enter-active,
.modal-leave-active {
  transition: opacity 0.3s ease;
}

.modal-enter-from,
.modal-leave-to {
  opacity: 0;
}

.modal-enter-active .modal-content,
.modal-leave-active .modal-content {
  transition: transform 0.3s ease, opacity 0.3s ease;
}

.modal-enter-from .modal-content,
.modal-leave-to .modal-content {
  transform: scale(0.9);
  opacity: 0;
}

/* Адаптивность */
@media (max-width: 768px) {
  .modal-content {
    margin: 20px;
    max-width: calc(100% - 40px);
  }

  .modal-header,
  .modal-body,
  .modal-footer {
    padding: 16px;
  }

  .create-snapshot-btn {
    padding: 8px 12px;
    font-size: 13px;
  }

  .btn-text {
    display: none; /* Скрыть текст на мобильных, оставить только иконку */
  }
}
</style>
```

---

## ✅ Критерии приёмки

- [ ] Файл `vue-app/src/components/graph-state/CreateSnapshotButton.vue` создан
- [ ] Компонент отображается только для администраторов
- [ ] Проверка прав администратора работает корректно (через `isAdmin()`)
- [ ] Кнопка "Создать слепок" отображается и работает
- [ ] Модальное окно подтверждения открывается при клике на кнопку
- [ ] Модальное окно закрывается по кнопке "Отмена"
- [ ] Модальное окно закрывается по клику вне окна (опционально)
- [ ] Модальное окно закрывается по нажатию Escape (опционально)
- [ ] Базовые стили добавлены и соответствуют гайдлайнам Bitrix24
- [ ] Компонент адаптивен для мобильных устройств
- [ ] Анимации модального окна работают плавно
- [ ] Код соответствует стандартам проекта (ESLint, форматирование)
- [ ] Комментарии добавлены к основным блокам кода
- [ ] Компонент можно использовать в других местах приложения

---

## 📝 Действия

1. **Создать структуру папок:**
   - Убедиться, что папка `vue-app/src/components/graph-state/` существует

2. **Создать компонент CreateSnapshotButton.vue:**
   - Создать файл с базовой структурой (template, script, style)
   - Реализовать проверку прав администратора
   - Добавить кнопку "Создать слепок"
   - Реализовать модальное окно подтверждения
   - Добавить обработчики открытия/закрытия модального окна
   - Добавить базовую структуру для создания слепка (заглушка)

3. **Добавить стили:**
   - Стили для кнопки
   - Стили для модального окна
   - Анимации открытия/закрытия
   - Адаптивность для мобильных устройств

4. **Протестировать компонент:**
   - Проверить отображение только для администраторов
   - Проверить открытие/закрытие модального окна
   - Проверить работу на мобильных устройствах
   - Проверить анимации

---

## 📚 Связанные файлы и документы

- **Родительская задача:** `DOCS/TASKS/TASK-019-00-plan.md`
- **Предыдущие задачи:**
  - `DOCS/TASKS/TASK-019-02-01.md` (SnapshotService)
  - `DOCS/TASKS/TASK-019-03-01.md` (SectorDataAdapter)
  - `DOCS/TASKS/TASK-019-03-02.md` (нормализация данных)
- **Следующая задача:** `DOCS/TASKS/TASK-019-05-02.md` (интеграция с сервисами и обработка ошибок)
- **Существующие файлы:**
  - `vue-app/src/config/access-config.js` (проверка прав администратора)
  - `vue-app/src/services/access-control-service.js` (получение текущего пользователя)
  - `vue-app/src/composables/useNotifications.js` (уведомления)
- **Новые файлы:**
  - `vue-app/src/components/graph-state/CreateSnapshotButton.vue`

---

## ⏱️ Оценка времени

**2-3 часа**

**Разбивка:**
- Создание базовой структуры компонента: 30-45 минут
- Реализация модального окна: 45-60 минут
- Добавление стилей и анимаций: 30-45 минут
- Тестирование и отладка: 30-45 минут

---

## 🔗 Следующая задача

После завершения этой задачи перейти к **TASK-019-05-02: Интеграция CreateSnapshotButton с сервисами, добавление индикатора прогресса и обработка ошибок**.

---

## 💡 Дополнительные замечания

### Переиспользуемость

Компонент должен быть переиспользуемым:
- Можно использовать в разных местах приложения
- Можно передавать пользователя через props (опционально)
- Можно кастомизировать через props (текст кнопки, тип слепка)

### Доступность

Рекомендуется:
- Добавить ARIA-атрибуты для модального окна
- Обеспечить навигацию с клавиатуры
- Добавить фокус-ловку в модальном окне

### Производительность

Оптимизации:
- Ленивая загрузка модального окна (если нужно)
- Мемоизация вычисляемых свойств
- Оптимизация рендеринга

---

## История правок

- **2025-12-08 11:00 (UTC+3, Брест):** Создана задача
- **2025-12-08 11:23 (UTC+3, Брест):** Задача выполнена
- **2025-12-08 13:54 (UTC+3, Брест):** Задача закрыта как успешная (все задачи TASK-019 завершены)

---

## ✅ Выполненные действия

**Дата:** 2025-12-08 11:23 (UTC+3, Брест)

**Выполнено:**
- ✅ Создан файл `vue-app/src/components/graph-state/CreateSnapshotButton.vue` — базовый компонент для ручного создания слепков
- ✅ Реализована проверка прав администратора через `isAdmin()` из `access-config.js`
- ✅ Компонент отображается только для администраторов (через `v-if="isUserAdmin"`)
- ✅ Реализована загрузка пользователя через `AccessControlService.checkAccess()` (если не передан через props)
- ✅ Добавлена кнопка "Создать слепок" с иконкой и текстом
- ✅ Реализовано модальное окно подтверждения с использованием `Teleport` для рендеринга в body
- ✅ Модальное окно открывается при клике на кнопку
- ✅ Модальное окно закрывается по кнопке "Отмена"
- ✅ Модальное окно закрывается по клику вне окна (через `@click.self`)
- ✅ Модальное окно закрывается по нажатию Escape (обработчик `handleEscape`)
- ✅ Добавлена базовая структура для создания слепка (заглушка, будет реализована в следующей задаче)
- ✅ Добавлены базовые стили, соответствующие гайдлайнам Bitrix24:
  - Стили для кнопки с hover-эффектами
  - Стили для модального окна с тенью и скруглением
  - Анимации открытия/закрытия модального окна (Transition)
  - Адаптивность для мобильных устройств (скрытие текста кнопки на мобильных)
- ✅ Интегрирован с `useNotifications` для уведомлений
- ✅ Добавлена обработка состояния загрузки (`isLoading`)
- ✅ Добавлены комментарии к основным блокам кода
- ✅ Код проверен линтером — ошибок не найдено

**Результат:** Базовый компонент CreateSnapshotButton создан и готов к интеграции с сервисами. Все критерии приёмки выполнены.

**Примечание:** Полная реализация создания слепка будет добавлена в следующей задаче (TASK-019-05-02).

---

**Автор:** Технический писатель  
**Версия документа:** 1.1

