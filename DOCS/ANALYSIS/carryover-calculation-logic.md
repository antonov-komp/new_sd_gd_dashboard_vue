# Анализ логики расчёта блока «ПЕРЕХОДЯЩИЕ» в модуле «График приёма и закрытий сектора 1С»

**Дата создания:** 2025-12-23 10:53 (UTC+3, Брест)  
**Статус:** ✅ Анализ завершён  
**Приоритет:** Высокий  
**Исполнитель:** Технический писатель  
**Тип задачи:** Анализ и документация  
**Связанные задачи:** TASK-044, TASK-047, TASK-048, TASK-061

---

## 📋 Описание

Детальный анализ логики расчёта трёх цифр в блоке **«ПЕРЕХОДЯЩИЕ»** модуля «График приёма и закрытий сектора 1С» в режиме отображения данных за 4 недели.

---

## 🎯 Цель анализа

Объяснить, как именно рассчитываются три цифры в блоке «ПЕРЕХОДЯЩИЕ»:
1. **Общее количество переходящих тикетов** (основная цифра)
2. **Переходящие, созданные этой неделей** (первая разбивка)
3. **Переходящие, созданные предыдущими неделями** (вторая разбивка)

---

## 📊 Структура блока «ПЕРЕХОДЯЩИЕ»

### Визуальное представление

Блок «ПЕРЕХОДЯЩИЕ» отображается в виде карточки с тремя цифрами:

```
┌─────────────────────────────────────┐
│ Переходящие                         │
│                                     │
│         [150]  ← Основная цифра     │
│         ↑↓ +5%                      │
│                                     │
│  ✓ 45  этой недели                 │
│  ↻ 105 предыдущих                  │
└─────────────────────────────────────┘
```

### Три цифры

1. **Основная цифра (150)** — `carryoverTickets`
   - Общее количество переходящих тикетов на конец текущей недели

2. **Первая разбивка (45)** — `carryoverTicketsCreatedThisWeek`
   - Переходящие тикеты, которые были созданы в текущую неделю

3. **Вторая разбивка (105)** — `carryoverTicketsCreatedOtherWeek`
   - Переходящие тикеты, которые были созданы до начала текущей недели

---

## 🔍 Логика расчёта

### Определение переходящего тикета

**Переходящий тикет** для конкретной недели — это тикет, который соответствует **всем** следующим условиям:

1. **Создан до конца недели** (включительно)
   - `createdTime <= weekEnd`
   - Тикет должен быть создан не позже конца анализируемой недели

2. **Находится в рабочих стадиях** (текущее состояние)
   - `stageId` входит в список рабочих стадий:
     - `DT140_12:UC_0VHWE2` (formed)
     - `DT140_12:PREPARATION` (review)
     - `DT140_12:CLIENT` (execution)

3. **Не находится в закрывающих стадиях**
   - `stageId` НЕ входит в список закрывающих стадий:
     - `DT140_12:SUCCESS`
     - `DT140_12:FAIL`
     - `DT140_12:UC_0GBU8Z`

4. **Не был закрыт до конца недели**
   - Если тикет был закрыт, то `movedTime > weekEnd`
   - Если тикет в рабочих стадиях сейчас, значит он не был закрыт

### Логика накопления переходящих тикетов

**Важно:** Переходящие тикеты накапливаются от недели к неделе.

**Пример накопления:**
- **Неделя 48:** все тикеты, созданные до конца недели 48, находящиеся в рабочих стадиях сейчас
- **Неделя 49:** все тикеты, созданные до конца недели 49, находящиеся в рабочих стадиях сейчас
- **Неделя 50:** все тикеты, созданные до конца недели 50, находящиеся в рабочих стадиях сейчас
- **Неделя 51 (текущая):** все тикеты, созданные до конца недели 51, находящиеся в рабочих стадиях сейчас

**Почему происходит накопление:**
- Для каждой следующей недели добавляются новые тикеты, которые были созданы в эту неделю и остались в рабочих стадиях
- Тикеты, созданные в предыдущие недели и не закрытые, продолжают учитываться в переходящих

---

## 💻 Реализация в коде

### Функция `isCarryoverTicket()`

**Файл:** `api/graph-1c-admission-closure.php`  
**Строки:** 498-565

```php
/**
 * Проверка, является ли тикет переходящим для конкретной недели
 * 
 * Переходящий тикет для недели — это тикет, который:
 * 1. Создан до конца этой недели (createdTime <= weekEnd)
 * 2. Находится в рабочих стадиях (targetStages) - текущее состояние
 * 3. Не был закрыт до конца этой недели (если закрыт, то movedTime > weekEnd)
 */
function isCarryoverTicket(
    array $ticket, 
    DateTimeImmutable $weekStart, 
    DateTimeImmutable $weekEnd, 
    array $targetStages, 
    array $closingStages
): bool
{
    $createdTime = $ticket['createdTime'] ?? null;
    $movedTime = $ticket['movedTime'] ?? $ticket['updatedTime'] ?? null;
    $stageId = $ticket['stageId'] ?? null;
    
    if (!$stageId || !$createdTime) {
        return false;
    }
    
    // Нормализация stageId
    $stageId = strtoupper($stageId);
    
    // Условие 1: Тикет создан до конца этой недели (включительно)
    $createdDt = new DateTimeImmutable($createdTime, new DateTimeZone('UTC'));
    if ($createdDt > $weekEnd) {
        return false; // Создан после конца недели, не переходящий для этой недели
    }
    
    // Условие 2: Находится в рабочих стадиях (текущее состояние)
    if (!in_array($stageId, $targetStages, true)) {
        return false; // Не в рабочей стадии
    }
    
    // Условие 3: Не в закрывающих стадиях
    if (in_array($stageId, $closingStages, true)) {
        return false; // В закрывающей стадии
    }
    
    // Тикет создан до конца недели, находится в рабочих стадиях сейчас
    // Это переходящий тикет для этой недели
    return true;
}
```

### Функция `calculateWeekMetrics()`

**Файл:** `api/graph-1c-admission-closure.php`  
**Строки:** 313-461

**Логика расчёта трёх цифр:**

```php
function calculateWeekMetrics(
    DateTimeImmutable $weekStart,
    DateTimeImmutable $weekEnd,
    array $allTickets,
    array $targetStages,
    array $closingStages,
    int $keeperId,
    bool $debug = false
): array {
    $carryoverCount = 0;                          // Основная цифра
    $carryoverTicketsCreatedThisWeek = 0;         // Первая разбивка
    $carryoverTicketsCreatedOtherWeek = 0;        // Вторая разбивка
    
    foreach ($allTickets as $ticket) {
        // Проверка, является ли тикет переходящим
        if (isCarryoverTicket($ticket, $weekStart, $weekEnd, $targetStages, $closingStages)) {
            $carryoverCount++;  // Увеличиваем общее количество
            
            // Разбивка по критерию создания
            $createdInThisWeek = isInRange($createdTime, $weekStart, $weekEnd);
            if ($createdInThisWeek) {
                $carryoverTicketsCreatedThisWeek++;  // Создан в текущую неделю
            } else {
                $carryoverTicketsCreatedOtherWeek++; // Создан до начала текущей недели
            }
        }
    }
    
    return [
        'carryoverTickets' => $carryoverCount,
        'carryoverTicketsCreatedThisWeek' => $carryoverTicketsCreatedThisWeek,
        'carryoverTicketsCreatedOtherWeek' => $carryoverTicketsCreatedOtherWeek
    ];
}
```

---

## 📐 Детальный алгоритм расчёта

### Шаг 1: Определение границ недели

Для каждой недели определяются границы:
- **weekStart:** Понедельник 00:00:00 UTC
- **weekEnd:** Воскресенье 23:59:59 UTC

**Пример для недели 51:**
- weekStart: `2025-12-15 00:00:00 UTC`
- weekEnd: `2025-12-21 23:59:59 UTC`

### Шаг 2: Фильтрация тикетов

Для каждого тикета из всех загруженных тикетов проверяется:

1. **Проверка создания:**
   ```php
   $createdDt = new DateTimeImmutable($createdTime, new DateTimeZone('UTC'));
   if ($createdDt > $weekEnd) {
       // Тикет создан после конца недели — не переходящий
       continue;
   }
   ```

2. **Проверка стадии:**
   ```php
   if (!in_array($stageId, $targetStages, true)) {
       // Тикет не в рабочей стадии — не переходящий
       continue;
   }
   
   if (in_array($stageId, $closingStages, true)) {
       // Тикет в закрывающей стадии — не переходящий
       continue;
   }
   ```

3. **Если все условия выполнены:**
   - Тикет считается переходящим
   - Увеличивается счётчик `carryoverCount`

### Шаг 3: Разбивка по критерию создания

Для каждого переходящего тикета определяется, когда он был создан:

```php
$createdInThisWeek = isInRange($createdTime, $weekStart, $weekEnd);
```

**Логика:**
- Если `createdTime` попадает в диапазон `[weekStart, weekEnd]`:
  - Тикет создан в текущую неделю
  - Увеличивается `carryoverTicketsCreatedThisWeek`
- Если `createdTime < weekStart`:
  - Тикет создан до начала текущей недели
  - Увеличивается `carryoverTicketsCreatedOtherWeek`

---

## 📊 Примеры расчёта

### Пример 1: Тикет, созданный в текущую неделю

**Данные тикета:**
- `id`: 12345
- `createdTime`: `2025-12-18 10:30:00 UTC` (среда, неделя 51)
- `stageId`: `DT140_12:PREPARATION` (рабочая стадия)
- `movedTime`: `null` (не закрыт)

**Анализ для недели 51:**
- ✅ Создан до конца недели 51 (`2025-12-18 <= 2025-12-21`)
- ✅ Находится в рабочей стадии (`PREPARATION`)
- ✅ Не в закрывающей стадии
- ✅ Не закрыт

**Результат:**
- `carryoverTickets`: +1
- `carryoverTicketsCreatedThisWeek`: +1 (создан в неделю 51)
- `carryoverTicketsCreatedOtherWeek`: +0

### Пример 2: Тикет, созданный в предыдущую неделю

**Данные тикета:**
- `id`: 12346
- `createdTime`: `2025-12-10 14:20:00 UTC` (среда, неделя 50)
- `stageId`: `DT140_12:CLIENT` (рабочая стадия)
- `movedTime`: `null` (не закрыт)

**Анализ для недели 51:**
- ✅ Создан до конца недели 51 (`2025-12-10 <= 2025-12-21`)
- ✅ Находится в рабочей стадии (`CLIENT`)
- ✅ Не в закрывающей стадии
- ✅ Не закрыт

**Результат:**
- `carryoverTickets`: +1
- `carryoverTicketsCreatedThisWeek`: +0
- `carryoverTicketsCreatedOtherWeek`: +1 (создан до начала недели 51)

### Пример 3: Тикет, созданный после конца недели

**Данные тикета:**
- `id`: 12347
- `createdTime`: `2025-12-22 09:15:00 UTC` (понедельник, неделя 52)
- `stageId`: `DT140_12:PREPARATION` (рабочая стадия)
- `movedTime`: `null` (не закрыт)

**Анализ для недели 51:**
- ❌ Создан после конца недели 51 (`2025-12-22 > 2025-12-21`)
- Не переходящий для недели 51

**Результат:**
- `carryoverTickets`: +0
- `carryoverTicketsCreatedThisWeek`: +0
- `carryoverTicketsCreatedOtherWeek`: +0

### Пример 4: Тикет в закрывающей стадии

**Данные тикета:**
- `id`: 12348
- `createdTime`: `2025-12-15 11:00:00 UTC` (понедельник, неделя 51)
- `stageId`: `DT140_12:SUCCESS` (закрывающая стадия)
- `movedTime`: `2025-12-20 16:45:00 UTC`

**Анализ для недели 51:**
- ✅ Создан до конца недели 51
- ❌ Находится в закрывающей стадии (`SUCCESS`)
- Не переходящий

**Результат:**
- `carryoverTickets`: +0
- `carryoverTicketsCreatedThisWeek`: +0
- `carryoverTicketsCreatedOtherWeek`: +0

---

## 🔗 Связь между тремя цифрами

### Математическая формула

```
carryoverTickets = carryoverTicketsCreatedThisWeek + carryoverTicketsCreatedOtherWeek
```

**Пример:**
```
150 = 45 + 105
```

### Смысл разбивки

1. **`carryoverTicketsCreatedThisWeek`** (45)
   - Показывает, сколько переходящих тикетов было создано в текущую неделю
   - Это тикеты, которые были новыми в текущую неделю, но ещё не закрыты
   - Индикатор текущей нагрузки

2. **`carryoverTicketsCreatedOtherWeek`** (105)
   - Показывает, сколько переходящих тикетов было создано в предыдущие недели
   - Это тикеты, которые "перешли" из предыдущих недель и всё ещё в работе
   - Индикатор накопленного долга

3. **`carryoverTickets`** (150)
   - Общее количество переходящих тикетов
   - Сумма двух предыдущих значений
   - Показывает общую нагрузку на сектор

---

## 📈 Визуализация в интерфейсе

### Компонент: `GraphAdmissionClosureChart.vue`

**Файл:** `vue-app/src/components/graph-admission-closure/GraphAdmissionClosureChart.vue`  
**Строки:** 86-114

```vue
<div class="summary-card summary-card--carryover-breakdown">
  <div class="summary-card__label">Переходящие</div>
  <div class="summary-card__value-wrapper">
    <!-- Основная цифра -->
    <div class="summary-card__value-main">
      {{ currentWeekData?.carryoverTickets ?? 0 }}
    </div>
  </div>
  <div class="summary-card__breakdown">
    <!-- Первая разбивка -->
    <div class="breakdown-item breakdown-item--this-week">
      <span class="breakdown-item__icon">✓</span>
      <span class="breakdown-item__value">
        {{ currentWeekData?.carryoverTicketsCreatedThisWeek ?? 0 }}
      </span>
      <span class="breakdown-item__label">этой недели</span>
    </div>
    <!-- Вторая разбивка -->
    <div class="breakdown-item breakdown-item--other-week">
      <span class="breakdown-item__icon">↻</span>
      <span class="breakdown-item__value">
        {{ currentWeekData?.carryoverTicketsCreatedOtherWeek ?? 0 }}
      </span>
      <span class="breakdown-item__label">предыдущих</span>
    </div>
  </div>
</div>
```

---

## 🎯 Практическое применение

### Анализ данных

1. **Высокое значение `carryoverTicketsCreatedThisWeek`:**
   - Много новых тикетов создано в текущую неделю
   - Возможно, увеличение нагрузки или всплеск активности

2. **Высокое значение `carryoverTicketsCreatedOtherWeek`:**
   - Много тикетов из предыдущих недель всё ещё в работе
   - Возможно, накопление долга или проблемы с закрытием тикетов

3. **Рост `carryoverTickets` от недели к неделе:**
   - Накопление переходящих тикетов
   - Индикатор перегрузки сектора

### Рекомендации

1. **Мониторинг соотношения:**
   - Если `carryoverTicketsCreatedOtherWeek` постоянно растёт — проблема с закрытием старых тикетов
   - Если `carryoverTicketsCreatedThisWeek` постоянно растёт — увеличение нагрузки

2. **Целевые показатели:**
   - `carryoverTicketsCreatedOtherWeek` должно стремиться к нулю
   - `carryoverTickets` не должно расти бесконечно

---

## 📝 Итоговая формула

### Для каждой недели:

```
carryoverTickets = Σ (все тикеты, где:
    createdTime <= weekEnd AND
    stageId IN [рабочие стадии] AND
    stageId NOT IN [закрывающие стадии])

carryoverTicketsCreatedThisWeek = Σ (переходящие тикеты, где:
    weekStart <= createdTime <= weekEnd)

carryoverTicketsCreatedOtherWeek = Σ (переходящие тикеты, где:
    createdTime < weekStart)

carryoverTickets = carryoverTicketsCreatedThisWeek + carryoverTicketsCreatedOtherWeek
```

---

## 🔗 Связанные документы

- **TASK-044:** Добавление категории «Переходящие тикеты»
- **TASK-047:** Разбивка переходящих тикетов по критерию создания
- **TASK-048:** Режим отображения данных за 4 недели
- **TASK-061:** Детальный анализ модуля «График приёма и закрытий 1С»

---

## 📅 История изменений

- **2025-12-23 10:53 (UTC+3, Брест):** Создан документ с детальным анализом логики расчёта блока «ПЕРЕХОДЯЩИЕ»

---

## ✅ Выводы

1. **Три цифры в блоке «ПЕРЕХОДЯЩИЕ» рассчитываются последовательно:**
   - Сначала определяется общее количество переходящих тикетов
   - Затем выполняется разбивка по критерию создания (текущая неделя / предыдущие недели)

2. **Логика накопления:**
   - Переходящие тикеты накапливаются от недели к неделе
   - Каждая следующая неделя включает тикеты предыдущих недель + новые тикеты текущей недели

3. **Математическая связь:**
   - Общее количество = созданные этой неделей + созданные предыдущими неделями

4. **Практическое применение:**
   - Разбивка позволяет анализировать текущую нагрузку и накопленный долг
   - Помогает выявлять проблемы с закрытием тикетов



