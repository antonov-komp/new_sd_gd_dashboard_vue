# Анализ использования кеша в проекте

**Дата создания:** 2025-12-24 14:47 (UTC+3, Брест)  
**Версия:** 1.0  
**Статус:** Актуально  
**Связанная задача:** TASK-075 (планируется)

---

## 📋 Обзор

Данный документ содержит детальный анализ использования механизмов кеширования в проекте. В проекте используются два типа кеша:

1. **Файловый кеш (PHP)** — для кеширования результатов API-запросов на сервере
2. **In-memory кеш (Vue.js)** — для кеширования данных в браузере пользователя

**Текущая ситуация:**
- ✅ **2 активных модуля** используют кеш (файловый)
- ✅ **1 модуль** использует in-memory кеш (Vue.js)
- ❌ **2 модуля** не используют кеш (требуют внедрения)

---

## 🎯 Модули с кешированием

### 1. График приёма/закрытий 1С (GraphAdmissionClosureCache)

**Тип кеша:** Файловый (PHP)  
**Статус:** ✅ Активно используется  
**Расположение:** `api/cache/GraphAdmissionClosureCache.php`

#### Механизм работы:

**Структура кеша:**
```
api/cache/graph-admission-closure/
├── months/          # Кеш для режима "3 месяца"
│   └── *.json       # Файлы кеша с данными
└── weeks/           # Кеш для режима "4 недели"
    └── *.json       # Файлы кеша с данными
```

**Параметры кеша:**
- **TTL (Time To Live):**
  - Режим "3 месяца": **300 секунд (5 минут)**
  - Режим "4 недели": **120 секунд (2 минуты)**
- **Формат данных:** JSON с метаданными
- **Структура файла:**
  ```json
  {
    "data": { /* кешированные данные */ },
    "metadata": {
      "created_at": 1234567890,
      "expires_at": 1234568190,
      "ttl": 300
    }
  }
  ```

**Методы класса:**
- `GraphAdmissionClosureCache::get(string $key): ?array` — получение данных из кеша
- `GraphAdmissionClosureCache::set(string $key, array $data, int $ttl = 300): bool` — сохранение данных в кеш
- `GraphAdmissionClosureCache::clear(): bool` — очистка всего кеша
- `GraphAdmissionClosureCache::clearByMode(string $mode): bool` — очистка кеша для конкретного режима

**Использование:**
- API endpoint: `api/graph-1c-admission-closure.php`
- Сервис: `api/graph-admission-closure/service/GraphAdmissionClosureService.php`
- Ключи кеша:
  - `months_{period}_{filters}` — для режима "3 месяца"
  - `weeks_{period}_{filters}` — для режима "4 недели"

**Особенности:**
- Автоматическое создание директорий при первом сохранении
- Автоматическое удаление истёкших файлов при чтении
- Поддержка двух режимов (months/weeks) с разными TTL

---

### 2. Трудозатраты на Тикеты сектора 1С (TimeTrackingCache)

**Тип кеша:** Файловый (PHP)  
**Статус:** ✅ Активно используется  
**Расположение:** `api/cache/TimeTrackingCache.php`

#### Механизм работы:

**Структура кеша:**
```
api/cache/time-tracking-sector-1c/
└── *.json           # Файлы кеша с данными
```

**Параметры кеша:**
- **TTL:** **300 секунд (5 минут)**
- **Формат данных:** JSON с метаданными
- **Структура файла:** Аналогична `GraphAdmissionClosureCache`

**Методы класса:**
- `TimeTrackingCache::get(string $key): ?array` — получение данных из кеша
- `TimeTrackingCache::set(string $key, array $data, int $ttl = 300): bool` — сохранение данных в кеш
- `TimeTrackingCache::clear(): bool` — очистка всего кеша
- `TimeTrackingCache::delete(string $key): bool` — удаление конкретного ключа

**Использование:**
- API endpoint: `api/tickets-time-tracking-sector-1c.php`
- Сервис: `api/tickets-time-tracking-sector-1c/service/TimeTrackingService.php`
- Ключи кеша: `time-tracking_{filters}_{date_range}`

**Особенности:**
- Автоматическое создание директории при первом сохранении
- Автоматическое удаление истёкших файлов при чтении
- Интеграция с `CacheStore` для единообразного интерфейса

---

### 3. Дашборд сектора 1С (CacheManager)

**Тип кеша:** In-memory (Vue.js)  
**Статус:** ✅ Активно используется  
**Расположение:** `vue-app/src/services/dashboard-sector-1c/cache/cache-manager.js`

#### Механизм работы:

**Хранилище:** `Map<string, CacheEntry>` в памяти браузера

**Параметры кеша:**
- **TTL по умолчанию:** 5 минут (300 000 мс)
- **TTL для сотрудников:** 30 минут (1 800 000 мс)
- **TTL для тикетов:** 5 минут (300 000 мс)

**Структура записи:**
```javascript
{
  data: any,           // Кешированные данные
  timestamp: number,   // Время создания (Date.now())
  ttl: number          // Время жизни в миллисекундах
}
```

**Методы класса:**
- `CacheManager.get(key: string): any | null` — получение данных из кеша
- `CacheManager.set(key: string, data: any, ttl?: number): void` — сохранение данных в кеш
- `CacheManager.delete(key: string): void` — удаление записи из кеша
- `CacheManager.clear(): void` — очистка всего кеша
- `CacheManager.invalidateByPrefix(prefix: string): void` — инвалидация по префиксу
- `CacheManager.cleanExpired(): void` — очистка истёкших записей
- `CacheManager.getStats(): object` — получение статистики кеша

**Использование:**
- Компонент: `vue-app/src/components/dashboard/DashboardSector1C.vue`
- Сервисы:
  - `vue-app/src/services/dashboard-sector-1c/services/ticket-details-service.js`
  - `vue-app/src/services/dashboard-sector-1c/data/ticket-repository.js`
  - `vue-app/src/services/dashboard-sector-1c/data/employee-repository.js`

**Ключи кеша:**
- `tickets:stage:{stageId}` — кеш тикетов по стадии
- `tickets:all` — кеш всех тикетов
- `employees:ids:{sortedIds}` — кеш сотрудников
- `sector:data` — кеш данных сектора

**Особенности:**
- Автоматическая очистка истёкших записей каждые 5 минут
- LRU (Least Recently Used) не реализован (используется простой TTL)
- Кеш существует только в памяти браузера (не сохраняется между сессиями)

---

### 4. useCache Composable (Vue.js)

**Тип кеша:** In-memory (Vue.js)  
**Статус:** ✅ Реализован, но использование не подтверждено  
**Расположение:** `vue-app/src/composables/useCache.js`

#### Механизм работы:

**Хранилище:** `ref<Map<string, CacheEntry>>` (реактивный Map)

**Параметры кеша:**
- **TTL по умолчанию:** 5 минут (300 000 мс)
- **Максимальный размер:** 100 записей
- **Статистика:** Включена по умолчанию

**Структура записи:**
```javascript
{
  data: any,           // Кешированные данные
  timestamp: number,  // Время создания
  lastAccessed: number, // Время последнего доступа (для LRU)
  ttl: number         // Время жизни в миллисекундах
}
```

**Методы composable:**
- `get(key: string): any | null` — получение данных из кеша
- `set(key: string, data: any, customTTL?: number): void` — сохранение данных в кеш
- `clear(): void` — очистка всего кеша
- `invalidate(pattern: string | RegExp): number` — инвалидация по паттерну
- `cleanup(): number` — очистка устаревших записей
- `getCacheKey(url: string, params?: object): string` — генерация ключа кеша
- `getStats(): object` — получение статистики кеша

**Особенности:**
- Поддержка LRU (Least Recently Used) eviction
- Инвалидация по паттернам (regex)
- Автоматическая очистка каждые 5 минут
- Статистика использования (hits, misses, sets, invalidations)

**Использование:**
- ⚠️ **Требует проверки:** Не найдено активного использования в коде
- Потенциальное использование: для кеширования API-запросов в Vue.js компонентах

---

## ❌ Модули без кеширования

### 1. Модуль "Управление пользователями" (TASK-072)

**Статус:** ❌ Кеш не используется  
**Расположение:** `vue-app/src/pages/UsersManagementPage.vue`

#### Текущая реализация:

**Компоненты:**
- `vue-app/src/pages/UsersManagementPage.vue` — основная страница
- `vue-app/src/components/users/AccessManager.vue` — управление доступом
- `vue-app/src/components/users/ModulesManager.vue` — управление модулями
- `vue-app/src/services/access-management-service.js` — сервис работы с доступом
- `vue-app/src/services/modules-management-service.js` — сервис работы с модулями

**Данные:**
- Список отделов с доступом (`allowedDepartmentIds`)
- Конфигурация модулей (`reports-config.js`)
- Список пользователей с доступом

**Проблемы без кеша:**
- При каждом открытии страницы загружаются данные из конфигурационных файлов
- Нет кеширования списка отделов и пользователей
- Повторные запросы к Bitrix24 API для получения информации о пользователях

**Рекомендации:**
- Использовать файловый кеш для конфигурационных данных (TTL: 5 минут)
- Использовать in-memory кеш для списка пользователей (TTL: 30 минут)
- Кешировать данные отделов (TTL: 1 час)

---

### 2. Модуль "Отслеживание активности пользователей" (TASK-073)

**Статус:** ❌ Кеш не используется  
**Расположение:** `vue-app/src/pages/UsersManagementPage.vue` (раздел активности)

#### Текущая реализация:

**Компоненты:**
- `vue-app/src/components/users/UserActivityList.vue` — список активности
- `vue-app/src/components/users/UserActivityCard.vue` — карточка активности
- `vue-app/src/components/users/UserActivityFilters.vue` — фильтры
- `vue-app/src/services/user-activity-service.js` — сервис работы с активностью
- `vue-app/src/services/activity-logging-service.js` — сервис логирования

**Данные:**
- История активности пользователей (логи в `logs/user-activity/`)
- Статистика использования приложения
- Фильтры по пользователям, датам, страницам

**Проблемы без кеша:**
- При каждом открытии страницы загружаются все логи активности
- Нет кеширования статистики активности
- Повторные запросы к API для получения логов

**Рекомендации:**
- Использовать файловый кеш для статистики активности (TTL: 5 минут)
- Использовать in-memory кеш для списка активности (TTL: 2 минуты)
- Кешировать результаты фильтрации (TTL: 1 минута)

---

## 🔧 Модуль администрирования кеша

### TASK-074: Ручное управление кешем

**Статус:** ✅ Реализован  
**Расположение:** `vue-app/src/pages/CacheManagementPage.vue`

#### Функциональность:

**Backend API endpoints:**
- `api/admin/cache-status.php` — получение статуса кеша всех модулей
- `api/admin/cache-clear.php` — очистка кеша (конкретного модуля или всех)
- `api/admin/cache-stats.php` — детальная статистика кеша

**Frontend компоненты:**
- `vue-app/src/pages/CacheManagementPage.vue` — основная страница управления
- `vue-app/src/components/cache/CacheModuleCard.vue` — карточка модуля с кешем
- `vue-app/src/components/cache/CacheStats.vue` — общая статистика кеша
- `vue-app/src/components/cache/CacheActions.vue` — действия с кешем
- `vue-app/src/services/cache-management-service.js` — сервис работы с кешем

**Отображаемые модули:**
1. График приёма/закрытий 1С (3 месяца) — `graph-admission-closure-months`
2. График приёма/закрытий 1С (4 недели) — `graph-admission-closure-weeks`
3. Трудозатраты на Тикеты сектора 1С — `time-tracking-sector-1c`

**Функции:**
- Просмотр статуса кеша для каждого модуля
- Очистка кеша для конкретного модуля
- Очистка всего кеша
- Статистика кеша (размер, количество файлов, TTL)
- Информация о дате создания и истечения кеша

**Особенности:**
- ⚠️ **In-memory кеш (CacheManager, useCache) не отображается** в модуле управления
- Отображаются только файловые кеши (PHP)
- Требуется расширение для поддержки in-memory кешей

---

## 📊 Сравнительная таблица

| Модуль | Тип кеша | TTL | Статус | Управление через админ-панель |
|--------|----------|-----|--------|-------------------------------|
| График приёма/закрытий 1С (months) | Файловый (PHP) | 5 мин | ✅ Активен | ✅ Да |
| График приёма/закрытий 1С (weeks) | Файловый (PHP) | 2 мин | ✅ Активен | ✅ Да |
| Трудозатраты на Тикеты сектора 1С | Файловый (PHP) | 5 мин | ✅ Активен | ✅ Да |
| Дашборд сектора 1С | In-memory (Vue.js) | 5-30 мин | ✅ Активен | ❌ Нет |
| useCache Composable | In-memory (Vue.js) | 5 мин | ⚠️ Не используется | ❌ Нет |
| Управление пользователями | — | — | ❌ Нет кеша | ❌ Нет |
| Отслеживание активности | — | — | ❌ Нет кеша | ❌ Нет |

---

## 🔍 Детальный анализ механизмов кеширования

### Файловый кеш (PHP)

**Принцип работы:**
1. При запросе данных проверяется наличие файла кеша
2. Если файл существует и не истёк (TTL), данные возвращаются из кеша
3. Если файл отсутствует или истёк, данные запрашиваются из Bitrix24 API
4. Полученные данные сохраняются в файл кеша с метаданными

**Преимущества:**
- Кеш сохраняется между запросами
- Работает для всех пользователей (общий кеш)
- Не требует дополнительной памяти на сервере
- Простота реализации и отладки

**Недостатки:**
- Зависимость от файловой системы
- Потенциальные проблемы с правами доступа
- Необходимость очистки старых файлов
- Медленнее, чем in-memory кеш

**Оптимизация:**
- Автоматическое удаление истёкших файлов
- Использование хешей для ключей кеша (короткие имена файлов)
- Группировка файлов по директориям

---

### In-memory кеш (Vue.js)

**Принцип работы:**
1. При запросе данных проверяется наличие записи в Map
2. Если запись существует и не истёк (TTL), данные возвращаются из кеша
3. Если запись отсутствует или истёк, данные запрашиваются через API
4. Полученные данные сохраняются в Map с метаданными

**Преимущества:**
- Очень быстрый доступ к данным
- Не требует файловой системы
- Автоматическая очистка при закрытии страницы

**Недостатки:**
- Кеш не сохраняется между сессиями
- Работает только для текущего пользователя
- Занимает память браузера
- Не виден в модуле управления кешем

**Оптимизация:**
- Использование LRU для управления размером кеша
- Автоматическая очистка истёкших записей
- Статистика использования для мониторинга

---

## 🎯 Рекомендации по внедрению кеша

### Для модуля "Управление пользователями" (TASK-072)

**Рекомендуемый тип кеша:** Файловый (PHP) + In-memory (Vue.js)

**Что кешировать:**
1. **Список отделов с доступом** (файловый кеш, TTL: 1 час)
   - Данные из `access-config.js`
   - Обновление при изменении конфигурации

2. **Список пользователей** (in-memory кеш, TTL: 30 минут)
   - Данные из Bitrix24 API
   - Кеширование на стороне клиента

3. **Конфигурация модулей** (файловый кеш, TTL: 5 минут)
   - Данные из `reports-config.js`
   - Обновление при изменении конфигурации

**Реализация:**
- Создать `api/cache/UsersManagementCache.php` (аналогично `GraphAdmissionClosureCache`)
- Использовать `CacheManager` или `useCache` для in-memory кеша
- Интегрировать в `UsersManagementPage.vue`

---

### Для модуля "Отслеживание активности пользователей" (TASK-073)

**Рекомендуемый тип кеша:** Файловый (PHP) + In-memory (Vue.js)

**Что кешировать:**
1. **Статистика активности** (файловый кеш, TTL: 5 минут)
   - Агрегированные данные по активности
   - Обновление при новых записях активности

2. **Список активности** (in-memory кеш, TTL: 2 минуты)
   - Результаты запросов к API
   - Кеширование на стороне клиента

3. **Результаты фильтрации** (in-memory кеш, TTL: 1 минута)
   - Кеширование результатов фильтров
   - Инвалидация при изменении фильтров

**Реализация:**
- Создать `api/cache/UserActivityCache.php` (аналогично `TimeTrackingCache`)
- Использовать `useCache` composable для in-memory кеша
- Интегрировать в `UserActivityList.vue`

---

## 🔄 Расширение модуля управления кешем

### Текущие ограничения:

1. **In-memory кеш не отображается:**
   - `CacheManager` (DashboardSector1C) не виден в админ-панели
   - `useCache` composable не виден в админ-панели

2. **Не все модули отображаются:**
   - Модули без кеша не показываются
   - Нет информации о потенциальных модулях для кеширования

### Рекомендации по расширению:

1. **Добавить поддержку in-memory кешей:**
   - API endpoint для получения статистики in-memory кешей
   - Отображение в `CacheManagementPage.vue`
   - Возможность очистки in-memory кешей

2. **Добавить информацию о модулях без кеша:**
   - Список модулей, которые могут использовать кеш
   - Рекомендации по внедрению кеша
   - Статистика потенциальной экономии запросов

3. **Улучшить статистику:**
   - Hit rate для каждого модуля
   - Количество сэкономленных запросов
   - Время отклика с кешем и без

---

## 📝 Выводы

### Текущее состояние:

✅ **Работает:**
- 2 модуля используют файловый кеш (PHP)
- 1 модуль использует in-memory кеш (Vue.js)
- Модуль управления кешем реализован и работает

❌ **Требует внимания:**
- 2 модуля не используют кеш (требуют внедрения)
- In-memory кеш не отображается в модуле управления
- `useCache` composable не используется в проекте

### Приоритеты:

1. **Высокий приоритет:**
   - Внедрение кеша в модуль "Управление пользователями"
   - Внедрение кеша в модуль "Отслеживание активности"

2. **Средний приоритет:**
   - Расширение модуля управления кешем для поддержки in-memory кешей
   - Интеграция `useCache` composable в существующие модули

3. **Низкий приоритет:**
   - Оптимизация существующих механизмов кеширования
   - Добавление метрик и мониторинга кеша

---

## 🔗 Связанные документы

- `DOCS/TASKS/TASK-072-users-management-module.md` — модуль управления пользователями
- `DOCS/TASKS/TASK-073-user-activity-tracking.md` — модуль отслеживания активности
- `DOCS/TASKS/TASK-074-cache-management-module.md` — модуль управления кешем
- `DOCS/ARCHITECTURE/cache-mechanism-dashboard-sector-1c.md` — механизм кеширования дашборда
- `api/cache/GraphAdmissionClosureCache.php` — реализация файлового кеша для графика
- `api/cache/TimeTrackingCache.php` — реализация файлового кеша для трудозатрат

---

**История правок:**
- 2025-12-24 14:47 (UTC+3, Брест): Создан документ анализа использования кеша

